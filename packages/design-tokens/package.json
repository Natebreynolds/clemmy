{
  "name": "@clem/design-tokens",
  "version": "0.1.0",
  "private": true,
  "description": "The one semantic token layer shared by the desktop console and the mobile PWA. Plain CSS custom properties — no build step, no framework, so React (console) and Preact (mobile) never enter into it, and the Electron setup window can inline it verbatim.",
  "files": ["tokens.css"],
  "exports": { "./tokens.css": "./tokens.css" }
}
