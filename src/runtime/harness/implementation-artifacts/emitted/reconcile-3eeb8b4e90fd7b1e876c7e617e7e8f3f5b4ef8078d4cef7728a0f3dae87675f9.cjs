var import_meta_url = require("node:url").pathToFileURL(__filename).href;
"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __esm = (fn, res, err) => function __init() {
  if (err) throw err[0];
  try {
    return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
  } catch (e) {
    throw err = [e], e;
  }
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/runtime/security.ts
function isPlaceholderSecret(value) {
  const normalized = value.trim().toLowerCase();
  if (!normalized) return true;
  return normalized === "changeme" || normalized === "change-me" || normalized === "placeholder" || normalized === "secret" || normalized === "webhook_secret" || normalized.includes("replace_me") || normalized.includes("your_") || normalized.includes("example");
}
function isStrongLocalSecret(value) {
  const trimmed = value.trim();
  if (trimmed.length < 24) return false;
  if (isPlaceholderSecret(trimmed)) return false;
  return /[A-Za-z]/.test(trimmed) && /[0-9_-]/.test(trimmed);
}
function isSecretLikeKey(input) {
  const normalized = String(input ?? "").replace(/([a-z0-9])([A-Z])/g, "$1_$2").toLowerCase().replace(/[^a-z0-9]+/g, "_").replace(/^_+|_+$/g, "");
  if (!normalized) return false;
  const words = new Set(normalized.split("_").filter(Boolean));
  if (words.has("token") || words.has("tokens") || words.has("secret") || words.has("secrets") || words.has("password") || words.has("passwords") || words.has("passwd") || words.has("credential") || words.has("credentials") || words.has("authorization") || words.has("bearer") || words.has("jwt") || words.has("cookie") || normalized === "auth" || normalized === "proxy_authorization" || normalized === "set_cookie") {
    return true;
  }
  const joined = `_${normalized}_`;
  return joined.includes("_api_key_") || joined.includes("_private_key_") || joined.includes("_signing_key_") || joined.includes("_encryption_key_") || joined.includes("_access_key_") || normalized === "x_api_key" || normalized === "mcp_headers" || normalized === "mcp_env";
}
function redactSecretAssignments(input) {
  const redactQuoted = input.replace(
    /(["'])([^"'\r\n]{1,80})\1(\s*[:=]\s*)(["'])(.*?)\4/g,
    (match, keyQuote, key, separator, valueQuote) => isSecretLikeKey(key) ? `${keyQuote}${key}${keyQuote}${separator}${valueQuote}[REDACTED]${valueQuote}` : match
  );
  return redactQuoted.replace(
    /\b([A-Za-z][A-Za-z0-9_.-]{0,80})(\s*[:=]\s*)(?:(?:Bearer|Basic|Bot)\s+[A-Za-z0-9._~+/=-]+|"[^"\r\n]*"|'[^'\r\n]*'|[^\s,;]+)/gi,
    (match, key, separator) => isSecretLikeKey(key) ? `${key}${separator}[REDACTED]` : match
  );
}
function redactSensitiveText(input) {
  let text = typeof input === "string" ? input : String(input ?? "");
  if (!text) return text;
  text = redactSecretAssignments(text);
  text = text.replace(/\bsk-[A-Za-z0-9_-]{8,}\b/g, (match) => `${match.slice(0, 10)}...REDACTED`);
  text = text.replace(/\bBearer\s+[A-Za-z0-9._~+/=-]{12,}\b/gi, "Bearer [REDACTED]");
  text = text.replace(/\bBot\s+[A-Za-z0-9._~+/=-]{12,}\b/gi, "Bot [REDACTED]");
  text = text.replace(/([?&](?:token|access_token|refresh_token|api_key|secret)=)[^&#\s"']+/gi, "$1[REDACTED]");
  text = text.replace(/((?:Authorization|authorization)\s*[:=]\s*)(?:Bearer|Bot)?\s*[A-Za-z0-9._~+/=-]{12,}/g, "$1[REDACTED]");
  text = text.replace(
    /((?:OPENAI|COMPOSIO|DISCORD|WEBHOOK|RECALL|CODEX|MCP|AUTH)[A-Z0-9_]*(?:KEY|TOKEN|SECRET|PASSWORD|AUTH)\s*[=:]\s*)("[^"]+"|'[^']+'|\S+)/gi,
    "$1[REDACTED]"
  );
  text = text.replace(
    /("(?:api[_-]?key|access[_-]?token|refresh[_-]?token|authorization|secret|password|token|headers?)"\s*:\s*)("[^"]+"|\{[^}]*\}|\[[^\]]*\])/gi,
    '$1"[REDACTED]"'
  );
  return text;
}
function redactSensitiveValue(value, depth = 0) {
  if (depth > 6) return "[REDACTED]";
  if (typeof value === "string") return redactSensitiveText(value);
  if (Array.isArray(value)) return value.map((item) => redactSensitiveValue(item, depth + 1));
  if (!value || typeof value !== "object") return value;
  const redacted = {};
  for (const [key, item] of Object.entries(value)) {
    redacted[key] = isSecretLikeKey(key) || SECRET_NAME_RE.test(key) ? "[REDACTED]" : redactSensitiveValue(item, depth + 1);
  }
  return redacted;
}
var SECRET_NAME_RE;
var init_security = __esm({
  "src/runtime/security.ts"() {
    "use strict";
    SECRET_NAME_RE = /\b(?:OPENAI|COMPOSIO|DISCORD|WEBHOOK|RECALL|CODEX|AUTH|API|BEARER|TOKEN|SECRET|PASSWORD|PRIVATE_KEY|ACCESS_TOKEN|REFRESH_TOKEN)[A-Z0-9_/-]*\b|^MCP_(?:HEADERS?|ENV|.*(?:KEY|TOKEN|SECRET|PASSWORD|AUTH))$/i;
  }
});

// src/config.ts
function realUserHome() {
  try {
    return import_node_os.default.userInfo().homedir;
  } catch {
    return import_node_os.default.homedir();
  }
}
function isTestProcess() {
  return Boolean(
    process.env.NODE_TEST_CONTEXT || process.env.CLEMMY_TEST_ISOLATED_HOME === "1" || process.argv.includes("--test")
  );
}
function parseEnvFile(envPath) {
  if (!(0, import_node_fs.existsSync)(envPath)) return {};
  const result = {};
  for (const rawLine of (0, import_node_fs.readFileSync)(envPath, "utf-8").split("\n")) {
    const line = rawLine.replace(/^\s+|\r+$/g, "");
    if (!line || line.startsWith("#")) continue;
    const eqIndex = line.indexOf("=");
    if (eqIndex === -1) continue;
    const key = line.slice(0, eqIndex).trim();
    let value = line.slice(eqIndex + 1);
    if (value.startsWith('"') && value.endsWith('"') || value.startsWith("'") && value.endsWith("'")) {
      value = value.slice(1, -1);
    }
    result[key] = value;
  }
  return result;
}
function envSearchPaths() {
  if (process.env.CLEMMY_TEST_ISOLATED_HOME === "1") {
    return [import_node_path.default.join(BASE_DIR, ".env")];
  }
  return [
    import_node_path.default.join(PKG_DIR, ".env"),
    import_node_path.default.join(process.cwd(), ".env"),
    import_node_path.default.join(BASE_DIR, ".env")
  ];
}
function getEnv(key, fallback = "") {
  return process.env[key] ?? env[key] ?? fallback;
}
function parseCsvEnv(value) {
  return value.split(",").map((entry) => entry.trim()).filter(Boolean);
}
function normalizeWebhookHost(value) {
  const trimmed = value.trim();
  if (!trimmed || trimmed === "localhost") return "127.0.0.1";
  if (trimmed === "::1") return "::1";
  if (trimmed === "0.0.0.0" || trimmed === "::") return trimmed;
  if (/^[A-Za-z0-9.-]+$/.test(trimmed) || /^[0-9a-f:.]+$/i.test(trimmed)) return trimmed;
  return "127.0.0.1";
}
function captureRuntimeEnvironment() {
  const activeEnvFiles = envSearchPaths().filter((filePath, index, items) => (0, import_node_fs.existsSync)(filePath) && items.indexOf(filePath) === index);
  const currentEnv = Object.assign({}, ...activeEnvFiles.map((filePath) => parseEnvFile(filePath)));
  runtimeConfigCaptureObserverForTest?.("environment");
  return Object.freeze(currentEnv);
}
function captureSecretVault() {
  const vaultPath = import_node_path.default.join(BASE_DIR, "state", "secrets-vault.json");
  let entries = {};
  if ((0, import_node_fs.existsSync)(vaultPath)) {
    try {
      const parsed = JSON.parse((0, import_node_fs.readFileSync)(vaultPath, "utf-8"));
      if (parsed.version === "v1" && parsed.entries) entries = { ...parsed.entries };
    } catch {
      entries = {};
    }
  }
  runtimeConfigCaptureObserverForTest?.("secret_vault");
  return Object.freeze(entries);
}
function getRuntimeEnv(key, fallback = "") {
  runtimeEnvReadObserverForTest?.(key);
  const requestSnapshot = runtimeConfigRequestStorage.getStore();
  if (requestSnapshot) {
    return process.env[key] ?? requestSnapshot.environmentValue(key) ?? fallback;
  }
  return process.env[key] ?? captureRuntimeEnvironment()[key] ?? fallback;
}
function readSecretFromFileVaultSync(name) {
  const requestSnapshot = runtimeConfigRequestStorage.getStore();
  const value = requestSnapshot ? requestSnapshot.secretValue(name) : captureSecretVault()[name];
  return value && value.length > 0 ? value : void 0;
}
function getOpenAiApiKey() {
  const fromFile = readSecretFromFileVaultSync("openai_api_key");
  if (fromFile) return fromFile;
  const fromEnv = getRuntimeEnv("OPENAI_API_KEY", "");
  return fromEnv ?? "";
}
function resolveOpenAiAgentsTracingDisabled(configuredValue, apiKey) {
  const configured = configuredValue?.trim().toLowerCase() ?? "";
  if (configured === "1" || configured === "true" || configured === "on") return "1";
  if (configured) return void 0;
  return apiKey.trim() ? void 0 : "1";
}
function resolveDiscordEnabled(rawEnabled, hasToken) {
  const raw = rawEnabled.trim().toLowerCase();
  if (raw === "true") return true;
  if (raw === "false") return false;
  return hasToken;
}
var import_node_async_hooks, import_node_fs, import_node_os, import_node_path, import_node_url, __filename, __dirname, PKG_DIR, REAL_USER_HOME, REAL_DEFAULT_CLEMENTINE_HOME, DEFAULT_BASE_DIR, BASE_DIR, ACTIVE_ENV_FILES, env, runtimeEnvReadObserverForTest, runtimeConfigCaptureObserverForTest, runtimeConfigRequestStorage, agentsTracingDisabled, ASSISTANT_NAME, OWNER_NAME, OPENAI_API_KEY, AUTH_MODE, CODEX_AUTH_SOURCE_FILE, CODEX_EXECUTABLE, CODEX_INSTALL_PACKAGE, CODEX_SANDBOX_MODE, CODEX_USE_FULL_AUTO, VAULT_DIR, WEBHOOK_ENABLED, WEBHOOK_PORT, WEBHOOK_HOST, WEBHOOK_ALLOW_LAN, MOBILE_APP_PORT, MOBILE_APP_LISTENER_ENABLED, WEBHOOK_SECRET, WEBHOOK_SECRET_IS_STRONG, DISCORD_ENABLED_RAW, DISCORD_HARNESS_ENABLED, DISCORD_BOT_TOKEN, DISCORD_ENABLED, DISCORD_CLIENT_ID, DISCORD_REQUIRE_MENTION, DISCORD_DM_ALLOWED_USERS, DISCORD_ALLOWED_USERS, DISCORD_DM_POLL_INTERVAL_MS, DISCORD_ALLOWED_CHANNELS, DISCORD_PUSH_PROACTIVE_BRIEFS, SLACK_ENABLED_RAW, SLACK_BOT_TOKEN, SLACK_APP_TOKEN, SLACK_ENABLED, SLACK_REQUIRE_MENTION, SLACK_ALLOWED_USERS, SLACK_ALLOWED_CHANNELS, SLACK_PROACTIVE_CHANNEL, LOCAL_MCP_ENABLED, MCP_AUTO_IMPORT_ENABLED, MCP_SERVERS_FILE, COMPOSIO_API_KEY, COMPOSIO_USER_ID;
var init_config = __esm({
  "src/config.ts"() {
    "use strict";
    import_node_async_hooks = require("node:async_hooks");
    import_node_fs = require("node:fs");
    import_node_os = __toESM(require("node:os"), 1);
    import_node_path = __toESM(require("node:path"), 1);
    import_node_url = require("node:url");
    init_security();
    __filename = (0, import_node_url.fileURLToPath)(import_meta_url);
    __dirname = import_node_path.default.dirname(__filename);
    PKG_DIR = import_node_path.default.resolve(__dirname, "..");
    REAL_USER_HOME = process.env.CLEMMY_REAL_USER_HOME || realUserHome();
    REAL_DEFAULT_CLEMENTINE_HOME = import_node_path.default.resolve(import_node_path.default.join(REAL_USER_HOME, ".clementine-next"));
    DEFAULT_BASE_DIR = process.env.CLEMENTINE_HOME || REAL_DEFAULT_CLEMENTINE_HOME;
    if (isTestProcess() && import_node_path.default.resolve(DEFAULT_BASE_DIR) === REAL_DEFAULT_CLEMENTINE_HOME && process.env.CLEMMY_ALLOW_LIVE_HOME_TESTS !== "1") {
      throw new Error(
        "Refusing to bind BASE_DIR to the live Clementine home from a test process. Set CLEMENTINE_HOME to a unique temp directory before importing config, or set CLEMMY_ALLOW_LIVE_HOME_TESTS=1 for an explicit destructive test."
      );
    }
    BASE_DIR = DEFAULT_BASE_DIR;
    ACTIVE_ENV_FILES = envSearchPaths().filter((filePath, index, items) => (0, import_node_fs.existsSync)(filePath) && items.indexOf(filePath) === index);
    env = Object.assign({}, ...ACTIVE_ENV_FILES.map((filePath) => parseEnvFile(filePath)));
    runtimeConfigRequestStorage = new import_node_async_hooks.AsyncLocalStorage();
    agentsTracingDisabled = resolveOpenAiAgentsTracingDisabled(
      getEnv("OPENAI_AGENTS_DISABLE_TRACING", ""),
      getOpenAiApiKey()
    );
    if (agentsTracingDisabled) process.env.OPENAI_AGENTS_DISABLE_TRACING = agentsTracingDisabled;
    ASSISTANT_NAME = getEnv("ASSISTANT_NAME", "Clementine");
    OWNER_NAME = getEnv("OWNER_NAME", "");
    OPENAI_API_KEY = getEnv("OPENAI_API_KEY", "");
    AUTH_MODE = (() => {
      const raw = getEnv("AUTH_MODE", "api_key");
      if (raw === "codex_oauth") return "codex_oauth";
      if (raw === "claude_oauth") return "claude_oauth";
      return "api_key";
    })();
    CODEX_AUTH_SOURCE_FILE = getEnv("CODEX_AUTH_SOURCE_FILE", import_node_path.default.join(import_node_os.default.homedir(), ".codex", "auth.json"));
    CODEX_EXECUTABLE = getEnv("CODEX_EXECUTABLE", "codex");
    CODEX_INSTALL_PACKAGE = getEnv("CODEX_INSTALL_PACKAGE", "@openai/codex");
    CODEX_SANDBOX_MODE = getEnv("CODEX_SANDBOX_MODE", "workspace-write");
    CODEX_USE_FULL_AUTO = getEnv("CODEX_USE_FULL_AUTO", "true").toLowerCase() === "true";
    VAULT_DIR = import_node_path.default.join(BASE_DIR, "vault");
    WEBHOOK_ENABLED = getEnv("WEBHOOK_ENABLED", "false").toLowerCase() === "true";
    WEBHOOK_PORT = parseInt(getEnv("WEBHOOK_PORT", "8420"), 10);
    WEBHOOK_HOST = normalizeWebhookHost(getEnv("WEBHOOK_HOST", "127.0.0.1"));
    WEBHOOK_ALLOW_LAN = getEnv("WEBHOOK_ALLOW_LAN", "false").toLowerCase() === "true";
    MOBILE_APP_PORT = parseInt(getEnv("CLEMENTINE_MOBILE_APP_PORT", "8421"), 10);
    MOBILE_APP_LISTENER_ENABLED = getEnv("CLEMENTINE_MOBILE_APP_LISTENER", "on").toLowerCase() !== "off";
    WEBHOOK_SECRET = readSecretFromFileVaultSync("webhook_secret") || getEnv("WEBHOOK_SECRET", "") || "";
    WEBHOOK_SECRET_IS_STRONG = isStrongLocalSecret(WEBHOOK_SECRET);
    DISCORD_ENABLED_RAW = getEnv("DISCORD_ENABLED", "").toLowerCase();
    DISCORD_HARNESS_ENABLED = !["false", "0", "no", "off"].includes(
      getEnv("DISCORD_HARNESS_ENABLED", "true").toLowerCase()
    );
    DISCORD_BOT_TOKEN = readSecretFromFileVaultSync("discord_bot_token") || getEnv("DISCORD_BOT_TOKEN", "") || "";
    DISCORD_ENABLED = resolveDiscordEnabled(DISCORD_ENABLED_RAW, DISCORD_BOT_TOKEN.length > 0);
    DISCORD_CLIENT_ID = getEnv("DISCORD_CLIENT_ID", "");
    DISCORD_REQUIRE_MENTION = getEnv("DISCORD_REQUIRE_MENTION", "true").toLowerCase() === "true";
    DISCORD_DM_ALLOWED_USERS = parseCsvEnv(getEnv("DISCORD_DM_ALLOWED_USERS", ""));
    DISCORD_ALLOWED_USERS = parseCsvEnv(
      getEnv("DISCORD_ALLOWED_USERS", getEnv("DISCORD_DM_ALLOWED_USERS", ""))
    );
    DISCORD_DM_POLL_INTERVAL_MS = parseInt(getEnv("DISCORD_DM_POLL_INTERVAL_MS", "5000"), 10);
    DISCORD_ALLOWED_CHANNELS = parseCsvEnv(getEnv("DISCORD_ALLOWED_CHANNELS", ""));
    DISCORD_PUSH_PROACTIVE_BRIEFS = getEnv("DISCORD_PUSH_PROACTIVE_BRIEFS", "false").toLowerCase() === "true";
    SLACK_ENABLED_RAW = getEnv("SLACK_ENABLED", "").toLowerCase();
    SLACK_BOT_TOKEN = readSecretFromFileVaultSync("slack_bot_token") || getEnv("SLACK_BOT_TOKEN", "") || "";
    SLACK_APP_TOKEN = readSecretFromFileVaultSync("slack_app_token") || getEnv("SLACK_APP_TOKEN", "") || "";
    SLACK_ENABLED = resolveDiscordEnabled(
      SLACK_ENABLED_RAW,
      SLACK_BOT_TOKEN.length > 0 && SLACK_APP_TOKEN.length > 0
    );
    SLACK_REQUIRE_MENTION = getEnv("SLACK_REQUIRE_MENTION", "true").toLowerCase() === "true";
    SLACK_ALLOWED_USERS = parseCsvEnv(getEnv("SLACK_ALLOWED_USERS", ""));
    SLACK_ALLOWED_CHANNELS = parseCsvEnv(getEnv("SLACK_ALLOWED_CHANNELS", ""));
    SLACK_PROACTIVE_CHANNEL = getEnv("SLACK_PROACTIVE_CHANNEL", "").trim();
    LOCAL_MCP_ENABLED = getEnv("LOCAL_MCP_ENABLED", "true").toLowerCase() === "true";
    MCP_AUTO_IMPORT_ENABLED = getEnv("MCP_AUTO_IMPORT_ENABLED", "false").toLowerCase() === "true";
    MCP_SERVERS_FILE = import_node_path.default.join(BASE_DIR, "mcp", "servers.json");
    COMPOSIO_API_KEY = getEnv("COMPOSIO_API_KEY", "");
    COMPOSIO_USER_ID = getEnv("COMPOSIO_USER_ID", "");
  }
});

// src/runtime/machine-id.ts
var import_node_path2, MACHINE_ID_FILE;
var init_machine_id = __esm({
  "src/runtime/machine-id.ts"() {
    "use strict";
    import_node_path2 = __toESM(require("node:path"), 1);
    init_config();
    MACHINE_ID_FILE = import_node_path2.default.join(BASE_DIR, "state", "machine-id");
  }
});

// src/shared/stable-json-digest.ts
function stableJsonStringify(value) {
  if (value === null || typeof value !== "object") return JSON.stringify(value) ?? "null";
  if (Array.isArray(value)) return `[${value.map(stableJsonStringify).join(",")}]`;
  const entries = Object.entries(value).filter(([, nested]) => nested !== void 0).sort(([left], [right]) => left.localeCompare(right));
  return `{${entries.map(([key, nested]) => `${JSON.stringify(key)}:${stableJsonStringify(nested)}`).join(",")}}`;
}
function stableJsonDigest(value) {
  return (0, import_node_crypto.createHash)("sha256").update(stableJsonStringify(value)).digest("hex");
}
var import_node_crypto;
var init_stable_json_digest = __esm({
  "src/shared/stable-json-digest.ts"() {
    "use strict";
    import_node_crypto = require("node:crypto");
  }
});

// src/tools/tool-contract-store.ts
var import_node_path3, CONTRACTS_ROOT, CONTRACT_TTL_MS, MAX_CONTRACT_CACHE_BYTES;
var init_tool_contract_store = __esm({
  "src/tools/tool-contract-store.ts"() {
    "use strict";
    import_node_path3 = __toESM(require("node:path"), 1);
    init_config();
    init_machine_id();
    init_stable_json_digest();
    CONTRACTS_ROOT = import_node_path3.default.join(BASE_DIR, "memory", "tool-contracts");
    CONTRACT_TTL_MS = 30 * 24 * 60 * 6e4;
    MAX_CONTRACT_CACHE_BYTES = 64 * 1024 * 1024;
  }
});

// src/tools/composio-schema-cache.ts
var SCHEMA_TTL_MS;
var init_composio_schema_cache = __esm({
  "src/tools/composio-schema-cache.ts"() {
    "use strict";
    init_tool_contract_store();
    SCHEMA_TTL_MS = 30 * 6e4;
  }
});

// src/runtime/harness/capability-live-identity.ts
var import_node_path4, STORE;
var init_capability_live_identity = __esm({
  "src/runtime/harness/capability-live-identity.ts"() {
    "use strict";
    import_node_path4 = __toESM(require("node:path"), 1);
    init_config();
    STORE = import_node_path4.default.join(BASE_DIR, "state", "capability-live-identity.json");
  }
});

// src/runtime/harness/mutation-verification-contract.ts
function record(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function exactKeys(value, required, optional = []) {
  const permitted = /* @__PURE__ */ new Set([...required, ...optional]);
  return required.every((key) => Object.prototype.hasOwnProperty.call(value, key)) && Object.keys(value).every((key) => permitted.has(key));
}
function boundedIdentity(value) {
  return typeof value === "string" && value === value.trim() && value.length > 0 && value.length <= 128 && /^[A-Za-z0-9._:@/+-]+$/.test(value);
}
function isRfc6901Pointer(value) {
  if (typeof value !== "string" || value.length < 2 || value.length > 512 || value[0] !== "/") return false;
  return !/~(?:[^01]|$)/.test(value);
}
function onePointer(value) {
  return Array.isArray(value) && value.length === 1 && isRfc6901Pointer(value[0]);
}
function parseRangeProjection(value) {
  if (!record(value) || !exactKeys(value, [
    "version",
    "kind",
    "entriesPointer",
    "rangePointer",
    "valuesPointer"
  ])) return null;
  if (value.version !== 1 || value.kind !== "exact_range_values_v1" || !isRfc6901Pointer(value.entriesPointer) || !isRfc6901Pointer(value.rangePointer) || !isRfc6901Pointer(value.valuesPointer)) return null;
  return {
    version: 1,
    kind: "exact_range_values_v1",
    entriesPointer: value.entriesPointer,
    rangePointer: value.rangePointer,
    valuesPointer: value.valuesPointer
  };
}
function parseSingleRangeProjection(value) {
  if (!record(value) || !exactKeys(value, [
    "version",
    "kind",
    "rangePointer",
    "valuesPointer"
  ])) return null;
  if (value.version !== 1 || value.kind !== "exact_single_range_values_v1" || !isRfc6901Pointer(value.rangePointer) || !isRfc6901Pointer(value.valuesPointer)) return null;
  return {
    version: 1,
    kind: "exact_single_range_values_v1",
    rangePointer: value.rangePointer,
    valuesPointer: value.valuesPointer
  };
}
function parseContentProjection(value) {
  return parseRangeProjection(value) ?? parseSingleRangeProjection(value);
}
function parseMutationVerificationContract(value) {
  if (!record(value) || !exactKeys(value, [
    "version",
    "resourceFamily",
    "producedHandleKind",
    "proof",
    "target"
  ], ["resultEnvelope", "expectedContent"])) return null;
  if (value.version !== 1 || !boundedIdentity(value.resourceFamily) || !boundedIdentity(value.producedHandleKind) || value.proof !== "resource_identity_v1" && value.proof !== "exact_content_v1" || !record(value.target) || !exactKeys(value.target, ["source", "pointers"]) || value.target.source !== "authoritative_result" && value.target.source !== "provider_arguments" || !onePointer(value.target.pointers) || value.resultEnvelope !== void 0 && value.resultEnvelope !== "successful_data_envelope_v1") return null;
  let expectedContent;
  if (value.expectedContent !== void 0) {
    if (!record(value.expectedContent) || !exactKeys(value.expectedContent, [
      "projection",
      "verifierRequestRangePointer"
    ])) return null;
    const projection = parseContentProjection(value.expectedContent.projection);
    if (!projection || !isRfc6901Pointer(value.expectedContent.verifierRequestRangePointer)) return null;
    expectedContent = {
      projection,
      verifierRequestRangePointer: value.expectedContent.verifierRequestRangePointer
    };
  }
  if (value.proof === "exact_content_v1" !== Boolean(expectedContent)) return null;
  return {
    version: 1,
    resourceFamily: value.resourceFamily,
    producedHandleKind: value.producedHandleKind,
    proof: value.proof,
    target: { source: value.target.source, pointers: [value.target.pointers[0]] },
    ...value.resultEnvelope === "successful_data_envelope_v1" ? { resultEnvelope: value.resultEnvelope } : {},
    ...expectedContent ? { expectedContent } : {}
  };
}
function parseReadbackVerificationContract(value) {
  if (!record(value) || !exactKeys(value, [
    "version",
    "resourceFamily",
    "acceptedHandleKind",
    "requestTargetPointers"
  ], ["responseTargetPointers", "responseTarget", "resultEnvelope", "observedContent"])) return null;
  const responseTarget = record(value.responseTarget) && exactKeys(value.responseTarget, ["version", "kind"]) && value.responseTarget.version === 1 && value.responseTarget.kind === "request_bound_success_v1" ? { version: 1, kind: "request_bound_success_v1" } : null;
  const responsePointers = onePointer(value.responseTargetPointers) ? value.responseTargetPointers : null;
  if (value.version !== 1 || !boundedIdentity(value.resourceFamily) || !boundedIdentity(value.acceptedHandleKind) || !onePointer(value.requestTargetPointers) || Boolean(responsePointers) === Boolean(responseTarget) || value.resultEnvelope !== void 0 && value.resultEnvelope !== "successful_data_envelope_v1" || responseTarget && value.resultEnvelope !== "successful_data_envelope_v1") return null;
  let observedContent;
  if (value.observedContent !== void 0) {
    if (!record(value.observedContent) || !exactKeys(value.observedContent, [
      "projection",
      "requestRangePointer"
    ])) return null;
    const projection = parseContentProjection(value.observedContent.projection);
    if (!projection || !isRfc6901Pointer(value.observedContent.requestRangePointer)) return null;
    observedContent = {
      projection,
      requestRangePointer: value.observedContent.requestRangePointer
    };
  }
  return {
    version: 1,
    resourceFamily: value.resourceFamily,
    acceptedHandleKind: value.acceptedHandleKind,
    requestTargetPointers: [value.requestTargetPointers[0]],
    ...responsePointers ? { responseTargetPointers: [responsePointers[0]] } : {},
    ...responseTarget ? { responseTarget } : {},
    ...value.resultEnvelope === "successful_data_envelope_v1" ? { resultEnvelope: value.resultEnvelope } : {},
    ...observedContent ? { observedContent } : {}
  };
}
function parseOperationVerificationContract(value) {
  if (!record(value)) return null;
  if (exactKeys(value, ["mutation"])) {
    const mutation = parseMutationVerificationContract(value.mutation);
    return mutation ? { mutation } : null;
  }
  if (exactKeys(value, ["readback"])) {
    const readback = parseReadbackVerificationContract(value.readback);
    return readback ? { readback } : null;
  }
  return null;
}
var init_mutation_verification_contract = __esm({
  "src/runtime/harness/mutation-verification-contract.ts"() {
    "use strict";
  }
});

// src/shared/closed-canonical-json.ts
var CLOSED_CANONICAL_JSON_DEFAULTS;
var init_closed_canonical_json = __esm({
  "src/shared/closed-canonical-json.ts"() {
    "use strict";
    CLOSED_CANONICAL_JSON_DEFAULTS = Object.freeze({
      maxDepth: 32,
      maxNodes: 2e4,
      maxStringBytes: 64e3,
      maxTotalBytes: 512e3
    });
  }
});

// src/runtime/harness/provider-read-evidence.ts
var init_provider_read_evidence = __esm({
  "src/runtime/harness/provider-read-evidence.ts"() {
    "use strict";
  }
});

// src/runtime/harness/atomic-input-content-contract.ts
function record2(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function exactKeys2(value, keys) {
  const expected = [...keys].sort();
  const actual = Object.keys(value).sort();
  return actual.length === expected.length && actual.every((key, index) => key === expected[index]);
}
function pointerList(value) {
  if (!Array.isArray(value) || value.length < 1 || value.length > 32 || !value.every(isRfc6901Pointer) || new Set(value).size !== value.length) return null;
  return [...value];
}
function parseAtomicResultIdentityProjection(value) {
  if (!record2(value) || !exactKeys2(value, [
    "version",
    "kind",
    "idPointers",
    "handlePointers",
    "handleTemplate"
  ]) || value.version !== 1 || value.kind !== "pointer_resource_identity_v1" || !record2(value.handleTemplate) || !exactKeys2(value.handleTemplate, ["version", "kind", "prefix", "suffix"]) || value.handleTemplate.version !== 1 || value.handleTemplate.kind !== "prefix_suffix_v1" || typeof value.handleTemplate.prefix !== "string" || !value.handleTemplate.prefix || value.handleTemplate.prefix.length > 2048 || typeof value.handleTemplate.suffix !== "string" || value.handleTemplate.suffix.length > 1024) return null;
  const idPointers = pointerList(value.idPointers);
  const handlePointers = pointerList(value.handlePointers);
  if (!idPointers || !handlePointers || [...idPointers, ...handlePointers].length !== (/* @__PURE__ */ new Set([...idPointers, ...handlePointers])).size) return null;
  return {
    version: 1,
    kind: "pointer_resource_identity_v1",
    idPointers,
    handlePointers,
    handleTemplate: {
      version: 1,
      kind: "prefix_suffix_v1",
      prefix: value.handleTemplate.prefix,
      suffix: value.handleTemplate.suffix
    }
  };
}
function parseAtomicInputContentCommitDeclaration(value) {
  if (!record2(value) || value.version !== 1 || !record2(value.compiler)) return null;
  if (Object.keys(value).length !== 4 || !Object.hasOwn(value, "compiler") || !Object.hasOwn(value, "resultIdentity") || !Object.hasOwn(value, "evidence") || Object.keys(value.compiler).length !== 6 || value.compiler.version !== 1 || value.compiler.kind !== "tabular_record_set_v1" || !isRfc6901Pointer(value.compiler.namePointer) || !isRfc6901Pointer(value.compiler.recordsPointer) || value.compiler.namePointer === value.compiler.recordsPointer || value.compiler.recordsEncoding !== "json_or_value" || value.compiler.selector !== "a1_grid_v1" || !Array.isArray(value.evidence) || value.evidence.length !== 2 || value.evidence[0] !== "receipt" || value.evidence[1] !== "content_commit") return null;
  const resultIdentity = parseAtomicResultIdentityProjection(value.resultIdentity);
  if (!resultIdentity) return null;
  return {
    version: 1,
    compiler: {
      version: 1,
      kind: "tabular_record_set_v1",
      namePointer: value.compiler.namePointer,
      recordsPointer: value.compiler.recordsPointer,
      recordsEncoding: "json_or_value",
      selector: "a1_grid_v1"
    },
    resultIdentity,
    evidence: ["receipt", "content_commit"]
  };
}
var init_atomic_input_content_contract = __esm({
  "src/runtime/harness/atomic-input-content-contract.ts"() {
    "use strict";
    init_closed_canonical_json();
    init_mutation_verification_contract();
    init_provider_read_evidence();
  }
});

// src/runtime/harness/capability-manifest.ts
function boundKinds(kinds) {
  return kinds.slice(0, MAX_MANIFEST_KIND_COUNT).map((kind) => kind.slice(0, MAX_MANIFEST_KIND_CHARS)).filter((kind) => kind.trim().length > 0);
}
function boundManifestDescriptorFields(manifest) {
  return {
    ...manifest,
    purpose: manifest.purpose.slice(0, MAX_MANIFEST_PURPOSE_CHARS),
    acceptedInputKinds: boundKinds(manifest.acceptedInputKinds),
    producedOutputKinds: boundKinds(manifest.producedOutputKinds),
    applicableDeliverableKinds: boundKinds(manifest.applicableDeliverableKinds)
  };
}
function nonBlank(value) {
  return typeof value === "string" && value.trim().length > 0 && value === value.trim();
}
function canonicalManifestBytes(manifest) {
  return JSON.stringify({
    version: manifest.version,
    manifestId: manifest.manifestId,
    providerKind: manifest.providerKind,
    operationId: manifest.operationId,
    providerIdentity: manifest.providerIdentity,
    providerVersion: manifest.providerVersion,
    operationVersion: manifest.operationVersion,
    definitionFingerprint: manifest.definitionFingerprint,
    // Preserve byte compatibility for already-installed manifests: absence is
    // omitted rather than encoded as null. New external manifests bind these
    // facts into their digest.
    ...manifest.externalDefinition ? {
      externalDefinition: {
        version: manifest.externalDefinition.version,
        providerInputSchemaDigest: manifest.externalDefinition.providerInputSchemaDigest,
        ...manifest.externalDefinition.providerOutputSchemaObserved === true ? { providerOutputSchemaObserved: true } : {},
        ...manifest.externalDefinition.providerOutputSchemaDigest ? { providerOutputSchemaDigest: manifest.externalDefinition.providerOutputSchemaDigest } : {},
        semanticName: manifest.externalDefinition.semanticName,
        ...manifest.externalDefinition.verification ? { verification: manifest.externalDefinition.verification } : {},
        behaviorHints: { ...manifest.externalDefinition.behaviorHints }
      }
    } : {},
    effect: manifest.effect,
    ...manifest.operationSemantics ? { operationSemantics: manifest.operationSemantics } : {},
    destination: manifest.destination ?? null,
    accountId: manifest.accountId,
    idempotency: manifest.idempotency,
    reconciliation: manifest.reconciliation,
    outputContract: manifest.outputContract,
    purpose: manifest.purpose,
    acceptedInputKinds: [...manifest.acceptedInputKinds],
    producedOutputKinds: [...manifest.producedOutputKinds],
    applicableDeliverableKinds: [...manifest.applicableDeliverableKinds],
    evidenceContract: {
      kinds: [...manifest.evidenceContract.kinds],
      readbackRequired: manifest.evidenceContract.readbackRequired
    },
    continuationContract: manifest.continuationContract ?? null,
    readbackContract: manifest.readbackContract ?? null,
    provenance: manifest.provenance,
    lifecycle: manifest.lifecycle,
    delegatedFrom: manifest.delegatedFrom ?? null,
    argumentCompiler: manifest.argumentCompiler,
    invokePortId: manifest.invokePortId,
    reconcilePortId: manifest.reconcilePortId ?? null
  });
}
function capabilityManifestDigest(manifest) {
  return (0, import_node_crypto2.createHash)("sha256").update(canonicalManifestBytes(manifest), "utf8").digest("hex");
}
function validateCapabilityManifestV1(manifest) {
  if (!manifest || manifest.version !== CAPABILITY_MANIFEST_VERSION) {
    return { ok: false, reason: "incomplete" };
  }
  if (!nonBlank(manifest.manifestId) || !nonBlank(manifest.operationId) || !nonBlank(manifest.providerIdentity) || !nonBlank(manifest.providerVersion) || !nonBlank(manifest.operationVersion) || !nonBlank(manifest.definitionFingerprint) || !nonBlank(manifest.accountId) || !nonBlank(manifest.outputContract?.kind) || !nonBlank(manifest.purpose) || !Array.isArray(manifest.acceptedInputKinds) || manifest.acceptedInputKinds.length === 0 || !manifest.acceptedInputKinds.every((kind) => nonBlank(kind)) || !Array.isArray(manifest.producedOutputKinds) || manifest.producedOutputKinds.length === 0 || !manifest.producedOutputKinds.every((kind) => nonBlank(kind)) || !Array.isArray(manifest.applicableDeliverableKinds) || manifest.applicableDeliverableKinds.length === 0 || !manifest.applicableDeliverableKinds.every((kind) => nonBlank(kind)) || !manifest.idempotency || !manifest.reconciliation || !manifest.evidenceContract || !Array.isArray(manifest.evidenceContract.kinds) || !nonBlank(manifest.argumentCompiler?.id) || !nonBlank(manifest.argumentCompiler?.version) || !nonBlank(manifest.invokePortId)) {
    return { ok: false, reason: "incomplete" };
  }
  if (!PROVIDER_KINDS.has(manifest.providerKind)) {
    return { ok: false, reason: "unknown_provider_kind" };
  }
  if (!EFFECTS.has(manifest.effect) || manifest.effect === "unknown") {
    return { ok: false, reason: "unknown_effect" };
  }
  const operationSemantics = manifest.operationSemantics === void 0 ? null : parseCapabilityManifestOperationSemantics(manifest.operationSemantics);
  if (manifest.operationSemantics !== void 0 && !operationSemantics) {
    return { ok: false, reason: "incomplete" };
  }
  if (operationSemantics) {
    const readEffect = manifest.effect === "read";
    const destructive = manifest.externalDefinition?.behaviorHints.destructive;
    const atomic = operationSemantics.atomicInputContent;
    if (readEffect || operationSemantics.reversibility === "ordinary_non_destructive" && destructive === true || atomic !== void 0 && (manifest.effect !== "external_write" && manifest.effect !== "local_write" || manifest.destination?.posture !== "create_new" || manifest.idempotency.required !== true || manifest.reconciliation.supported !== true || manifest.evidenceContract.readbackRequired !== false || manifest.evidenceContract.kinds.length !== atomic.evidence.length || manifest.evidenceContract.kinds.some((kind, index) => kind !== atomic.evidence[index]) || destructive === true)) return { ok: false, reason: "incomplete" };
  }
  if (manifest.externalDefinition) {
    const definition = manifest.externalDefinition;
    const hints = definition.behaviorHints;
    const hint = (value) => value === true || value === false || value === null;
    if (manifest.providerKind !== "composio" && manifest.providerKind !== "native_mcp" || definition.version !== 1 || !/^[a-f0-9]{64}$/.test(definition.providerInputSchemaDigest) || definition.providerOutputSchemaObserved !== void 0 && definition.providerOutputSchemaObserved !== true || definition.providerOutputSchemaDigest !== void 0 && definition.providerOutputSchemaObserved !== true || definition.providerOutputSchemaDigest !== void 0 && !/^[a-f0-9]{64}$/.test(definition.providerOutputSchemaDigest) || !nonBlank(definition.semanticName) || definition.verification !== void 0 && !parseOperationVerificationContract(definition.verification) || !hints || !hint(hints.readOnly) || !hint(hints.destructive) || !hint(hints.idempotent) || !hint(hints.openWorld) || hints.readOnly !== null && hints.readOnly !== (manifest.effect === "read") || hints.destructive === true && manifest.effect !== "external_write" && manifest.effect !== "local_write" && manifest.effect !== "admin") return { ok: false, reason: "incomplete" };
  }
  if (manifest.provenance?.trusted !== true || !nonBlank(manifest.provenance.issuer) || !nonBlank(manifest.provenance.issuedAt)) {
    return { ok: false, reason: "untrusted_provenance" };
  }
  if (manifest.lifecycle?.state === "revoked") return { ok: false, reason: "revoked" };
  if (manifest.lifecycle?.state === "superseded") return { ok: false, reason: "superseded" };
  if (manifest.lifecycle?.state !== "current") return { ok: false, reason: "incomplete" };
  if (manifest.delegatedFrom && manifest.operationId === manifest.delegatedFrom) {
    return { ok: false, reason: "multiplexer_is_not_an_operation" };
  }
  if (Buffer.byteLength(manifest.purpose, "utf8") > MAX_MANIFEST_PURPOSE_CHARS || manifest.acceptedInputKinds.some((kind) => Buffer.byteLength(kind, "utf8") > MAX_MANIFEST_KIND_CHARS) || manifest.producedOutputKinds.some((kind) => Buffer.byteLength(kind, "utf8") > MAX_MANIFEST_KIND_CHARS) || manifest.applicableDeliverableKinds.some((kind) => Buffer.byteLength(kind, "utf8") > MAX_MANIFEST_KIND_CHARS) || manifest.acceptedInputKinds.length > MAX_MANIFEST_KIND_COUNT || manifest.producedOutputKinds.length > MAX_MANIFEST_KIND_COUNT || manifest.applicableDeliverableKinds.length > MAX_MANIFEST_KIND_COUNT) {
    return { ok: false, reason: "incomplete" };
  }
  return { ok: true, manifest: boundManifestDescriptorFields(manifest) };
}
function parseCapabilityManifestOperationSemantics(value) {
  if (!value || typeof value !== "object" || Array.isArray(value)) return null;
  const row = value;
  const hasReversibility = Object.hasOwn(row, "reversibility");
  const hasAtomic = Object.hasOwn(row, "atomicInputContent");
  const keys = Object.keys(row);
  const allowed = /* @__PURE__ */ new Set([
    "version",
    ...hasReversibility ? ["reversibility"] : [],
    ...hasAtomic ? ["atomicInputContent"] : []
  ]);
  const reversibilities = /* @__PURE__ */ new Set([
    "reversible",
    "ordinary_non_destructive",
    "irreversible"
  ]);
  const atomicInputContent = hasAtomic ? parseAtomicInputContentCommitDeclaration(row.atomicInputContent) : void 0;
  if (row.version !== 1 || keys.length !== allowed.size || keys.some((key) => !allowed.has(key)) || !hasReversibility && !hasAtomic || hasReversibility && !reversibilities.has(row.reversibility) || hasAtomic && !atomicInputContent) return null;
  return {
    version: 1,
    ...hasReversibility ? { reversibility: row.reversibility } : {},
    ...atomicInputContent ? { atomicInputContent } : {}
  };
}
function currentCapabilityManifest(manifest) {
  const checked = validateCapabilityManifestV1(manifest);
  return checked.ok ? checked.manifest : null;
}
var import_node_crypto2, CAPABILITY_MANIFEST_VERSION, PROVIDER_KINDS, EFFECTS, MAX_MANIFEST_PURPOSE_CHARS, MAX_MANIFEST_KIND_CHARS, MAX_MANIFEST_KIND_COUNT;
var init_capability_manifest = __esm({
  "src/runtime/harness/capability-manifest.ts"() {
    "use strict";
    import_node_crypto2 = require("node:crypto");
    init_mutation_verification_contract();
    init_atomic_input_content_contract();
    CAPABILITY_MANIFEST_VERSION = 1;
    PROVIDER_KINDS = /* @__PURE__ */ new Set([
      "local_registry",
      "composio",
      "native_mcp",
      "reviewed_cli"
    ]);
    EFFECTS = /* @__PURE__ */ new Set([
      "none",
      "read",
      "compute",
      "host_only",
      "unknown",
      "local_write",
      "external_write",
      "admin"
    ]);
    MAX_MANIFEST_PURPOSE_CHARS = 256;
    MAX_MANIFEST_KIND_CHARS = 64;
    MAX_MANIFEST_KIND_COUNT = 8;
  }
});

// src/runtime/harness/isolated-test-contract.ts
var init_isolated_test_contract = __esm({
  "src/runtime/harness/isolated-test-contract.ts"() {
    "use strict";
  }
});

// src/runtime/harness/implementation-artifacts/attested-transport.ts
function bindAttestedTransport(transport) {
  bound = transport;
}
function requireAttestedTransport() {
  if (!bound) {
    throw new Error("attested provider transport is not bound");
  }
  return bound;
}
var bound;
var init_attested_transport = __esm({
  "src/runtime/harness/implementation-artifacts/attested-transport.ts"() {
    "use strict";
    bound = null;
  }
});

// src/runtime/schema-normalizer.ts
function withDescription(source, target) {
  return source.description ? target.describe(source.description) : target;
}
function normalizeZodForDeferredJson(schema) {
  const def = schema._def;
  switch (def?.type) {
    case "optional":
      return withDescription(
        schema,
        normalizeZodForDeferredJson(def.innerType).nullish()
      );
    case "nullable":
      return withDescription(
        schema,
        // On the args_json transport a nullable tool field is the strict-mode
        // representation of "optional"; accept omission as well as null.
        normalizeZodForDeferredJson(def.innerType).nullish()
      );
    case "object": {
      const anySchema = schema;
      const shape = typeof anySchema.shape === "function" ? anySchema.shape() : anySchema.shape;
      const normalizedShape = Object.fromEntries(
        Object.entries(shape).map(([key, value]) => [
          key,
          normalizeZodForDeferredJson(value)
        ])
      );
      return withDescription(schema, import_zod.z.strictObject(normalizedShape));
    }
    case "array":
      return withDescription(schema, import_zod.z.array(normalizeZodForDeferredJson(def.element)));
    case "record": {
      const keyType = def.keyType ?? import_zod.z.string();
      const valueType = def.valueType ? normalizeZodForDeferredJson(def.valueType) : import_zod.z.json();
      return withDescription(schema, import_zod.z.record(keyType, valueType));
    }
    case "any":
    case "unknown":
      return withDescription(schema, import_zod.z.json());
    case "union": {
      const options = Array.isArray(def.options) ? def.options.map((item) => normalizeZodForDeferredJson(item)) : [];
      if (options.length === 0) return withDescription(schema, import_zod.z.string());
      if (options.length === 1) return withDescription(schema, options[0]);
      return withDescription(
        schema,
        import_zod.z.union(options)
      );
    }
    default:
      return schema;
  }
}
function normalizeShapeForDeferredJson(shape) {
  return Object.fromEntries(
    Object.entries(shape).map(([key, value]) => [
      key,
      normalizeZodForDeferredJson(value)
    ])
  );
}
function jsonSchemaAllowsNull(schemaValue) {
  if (!schemaValue || typeof schemaValue !== "object" || Array.isArray(schemaValue)) return false;
  const schema = schemaValue;
  if (schema.nullable === true || schema.type === "null") return true;
  if (Array.isArray(schema.type) && schema.type.includes("null")) return true;
  for (const branch of [schema.anyOf, schema.oneOf]) {
    if (Array.isArray(branch) && branch.some(jsonSchemaAllowsNull)) return true;
  }
  return false;
}
function relaxJsonSchemaForDeferred(schemaValue) {
  if (Array.isArray(schemaValue)) return schemaValue.map(relaxJsonSchemaForDeferred);
  if (!schemaValue || typeof schemaValue !== "object") return schemaValue;
  const source = schemaValue;
  const out = {};
  for (const [key, value] of Object.entries(source)) {
    out[key] = relaxJsonSchemaForDeferred(value);
  }
  if (Array.isArray(source.required) && source.properties && typeof source.properties === "object") {
    const properties = source.properties;
    const required = source.required.filter(
      (key) => typeof key === "string" && !jsonSchemaAllowsNull(properties[key])
    );
    if (required.length > 0) out.required = required;
    else delete out.required;
  }
  return out;
}
var import_zod;
var init_schema_normalizer = __esm({
  "src/runtime/schema-normalizer.ts"() {
    "use strict";
    import_zod = require("zod");
  }
});

// src/tools/tool-registry.ts
var TOOL_REGISTRY;
var init_tool_registry = __esm({
  "src/tools/tool-registry.ts"() {
    "use strict";
    TOOL_REGISTRY = [
      { name: "agent_propose", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", blockedFor: ["workflow-step", "worker"], actionTopologyRole: "control", description: "Draft a reusable team-agent proposal for user review." },
      { name: "agent_run_get", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", loopClass: "idempotent", actionTopologyRole: "control", actionControlContext: "task_recovery", description: "Fetch the full event timeline of a single autonomy cycle by runId." },
      { name: "agent_runs_recent", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", loopClass: "idempotent", actionTopologyRole: "control", actionControlContext: "task_recovery", description: "List recent autonomy cycles (daemon-source runs)." },
      { name: "answer_check_in", sideEffect: "read", tier: "discoverable", lanes: ["cli"], actionTopologyRole: "control", description: "Resolve an open check-in with an answer." },
      { name: "artifact_bundle_save", sideEffect: "write", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "agentic", needsApproval: false, loopClass: "mutating", localPlanning: { consequence: "local_artifact", reversibility: "create_only", destructive: false, purpose: "create_local_artifact_bundle", inputKind: "artifact_content", outputKind: "local_artifact", deliverableKind: "directory", destinationPosture: "create_new", advisoryRoles: ["author", "create", "destination"], safeMode: { id: "content_addressed", requiredEquals: { mode: "content_addressed" } } }, localExecution: { version: 1, adapter: "artifact_bundle_v1", idempotency: "content_addressed", reconciliation: "artifact_bundle_v1" }, description: "Atomically commit a bounded multi-file local artifact as one immutable content-addressed directory; exact replay verifies and reuses the same revision." },
      { name: "ask_user_question", sideEffect: "read", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", blockedFor: ["workflow-step", "worker"], loopClass: "mutating", actionTopologyRole: "control", description: "Ask the user a question when the answer would change what you do \u2014 scope, target, format, or a boundary only they can decide." },
      { name: "automation_opportunity_get", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", loopClass: "idempotent", actionTopologyRole: "control", description: "Read one inert automation opportunity proposal by its exact ID." },
      { name: "automation_opportunity_list", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", loopClass: "idempotent", actionTopologyRole: "control", description: "List inert automation opportunity proposals for review." },
      { name: "automation_opportunity_propose", sideEffect: "write", runtimeEffect: "host_only", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", loopClass: "mutating", actionTopologyRole: "control", description: "Persist an inert, reviewable automation opportunity without granting execution authority." },
      { name: "automation_opportunity_revise", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", loopClass: "mutating", actionTopologyRole: "control", description: "Revise an inert automation opportunity without granting execution authority." },
      { name: "automation_opportunity_review_request", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", blockedFor: ["workflow-step", "worker"], needsApproval: false, loopClass: "mutating", actionTopologyRole: "control", description: "Stage an exact proposal CAS and formal human decision card without granting execution authority." },
      { name: "automation_read_pilot_acquisition_list", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "read-only", blockedFor: ["workflow-step", "worker"], loopClass: "idempotent", actionTopologyRole: "control", description: "List exact host-issued live read acquisition references without granting authority." },
      { name: "automation_read_pilot_request", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", blockedFor: ["workflow-step", "worker"], needsApproval: false, loopClass: "mutating", actionTopologyRole: "control", description: "Create an exact one-read pilot projection and its formal approval card." },
      { name: "automation_read_pilot_workspace_create_request", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", blockedFor: ["workflow-step", "worker"], needsApproval: false, loopClass: "mutating", actionTopologyRole: "control", description: "Stage an exact local Workspace manifest and formal human creation card without granting workflow authority." },
      { name: "automation_read_pilot_workspace_list", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "read-only", blockedFor: ["workflow-step", "worker"], loopClass: "idempotent", actionTopologyRole: "control", description: "List exact current Workspace revisions without selecting or binding one." },
      { name: "automation_recurrence_request", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", blockedFor: ["workflow-step", "worker"], needsApproval: false, loopClass: "mutating", actionTopologyRole: "control", description: "Stage a disabled interval preview and formal recurrence-consent card from one exact clean pilot." },
      // The background-task family is control-plane (task-lifecycle coordination,
      // same family as run_worker/execution_create) — the default business role
      // made a plain status read on an act-authority turn a turn-killing 500
      // (live 2026-08-11, long-horizon-manifest origin: background_task_status →
      // ExpectedWorkBindingRequiredError).
      { name: "background_task_revise", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "read-only", loopClass: "mutating", blockedFor: ["workflow-step", "worker"], actionTopologyRole: "control", actionControlContext: "task_recovery", delegationPrimitive: true, description: "Version and course-correct an active durable background task on its existing session." },
      { name: "background_task_status", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", loopClass: "idempotent", actionTopologyRole: "control", actionControlContext: "task_recovery", description: "Inspect a durable background task by task id, run id, or session id." },
      { name: "background_tasks_recent", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", loopClass: "idempotent", actionTopologyRole: "control", actionControlContext: "task_recovery", description: "List recent durable background tasks with status, latest activity, approvals, and result\u2026" },
      { name: "browser_harness_run", sideEffect: "write", tier: "core", lanes: ["orchestrator", "cli"], delegationPrimitive: true, description: "Run a Browser Harness Python snippet against the user browser through the browser-harness\u2026" },
      { name: "browser_harness_status", sideEffect: "read", tier: "core", lanes: ["orchestrator", "cli"], actionTopologyRole: "control", description: "Check Browser Harness availability and setup state." },
      // Generic gated dispatcher for schema-on-demand (SCHEMA-ON-DEMAND-PLAN-2026-07-07,
      // Phase 1). Structural, orchestrator-lane only, added to assembledTools ONLY when
      // CLEMMY_CODEX_TOOL_SEARCH is on (lanes [] like request_approval/run_worker).
      // sideEffect 'write' = documented default-ask, but needsApproval is FALSE: the
      // runtime gate keys on the INNER tool (dispatchBatchItemTool), not call_tool.
      { name: "call_tool", sideEffect: "write", tier: "core", lanes: [], needsApproval: false, description: "Invoke a catalog-only built-in tool by name with a JSON args string; effects and gates key on the inner tool." },
      { name: "check_capability", sideEffect: "read", tier: "discoverable", lanes: ["cli"], actionTopologyRole: "control", delegationPrimitive: true, description: "Check whether a CLI / binary is available on this machine." },
      { name: "cli_setup", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], loopClass: "mutating", blockedFor: ["workflow-step", "worker"], delegationPrimitive: true, description: "Install or re-authenticate a local CLI via the approved runners: status | install | auth | job_status. Ask the user before install/auth." },
      { name: "project_run", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], loopClass: "mutating", blockedFor: ["workflow-step", "worker"], delegationPrimitive: true, description: "Inspect or stop historical Claude Code / Codex guest runs: status | runs | kill. start is unavailable until a durable delegated-execution root owns the child effects and receipt." },
      { name: "check_delegation", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", actionTopologyRole: "control", description: "Check a delegated task by ID or list delegations for an agent." },
      { name: "composio_execute_tool", sideEffect: "send", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "inner-dispatch", "cli"], sdkLayer: "agentic", innerDispatch: "write", loopClass: "mutating", description: "Execute any Composio action by exact slug (Outlook list-mail, Gmail search, Drive search,\u2026" },
      { name: "composio_list_tools", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "agentic", loopClass: "idempotent", cacheSafeRead: true, actionTopologyRole: "control", description: "List available Composio tools for one connected toolkit slug, such as gmail, slack, notio\u2026" },
      { name: "composio_search_tools", sideEffect: "read", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "agentic", loopClass: "idempotent", cacheSafeRead: true, actionTopologyRole: "control", description: "Search Composio for the right action slug." },
      { name: "composio_status", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", loopClass: "idempotent", actionTopologyRole: "control", description: "Inspect whether Composio is configured and list active third-party app connections availa\u2026" },
      { name: "convert_to_markdown", sideEffect: "write", tier: "discoverable", lanes: ["cli"], description: "Extract a non-text file (PDF, Word/Excel/PowerPoint, EPub, image, audio, \u2026) into Markdown\u2026" },
      { name: "create_agent", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", blockedFor: ["workflow-step", "worker"], actionTopologyRole: "control", delegationPrimitive: true, description: "Create a new active team agent with its own personality, tools, and project binding." },
      { name: "create_tool", sideEffect: "admin", tier: "discoverable", lanes: ["cli"], blockedFor: ["workflow-step", "worker"], description: "Create a reusable shell or python tool script in ~/.clementine-next/tools." },
      { name: "cron_progress_read", sideEffect: "read", tier: "discoverable", lanes: ["cli"], actionTopologyRole: "control", description: "Read saved progress state for a cron job." },
      { name: "delegate_task", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", blockedFor: ["workflow-step", "worker"], actionTopologyRole: "control", delegationPrimitive: true, description: "Delegate a task to another team agent using local delegation state." },
      { name: "delegation_inbox", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", actionTopologyRole: "control", description: "List delegated tasks assigned to the current team agent and still open." },
      { name: "complete_delegation", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "authoring", actionTopologyRole: "control", delegationPrimitive: true, description: "Finish a delegated task assigned to the current team agent by recording its result." },
      { name: "delete_agent", sideEffect: "admin", tier: "discoverable", lanes: ["cli"], blockedFor: ["workflow-step", "worker"], description: "Delete an agent definition." },
      { name: "desktop_status", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator"], actionTopologyRole: "control", description: "Read-only status for the locally installed Clementine desktop app, including installed bu\u2026" },
      { name: "discover_work", sideEffect: "read", tier: "discoverable", lanes: ["cli"], loopClass: "idempotent", actionTopologyRole: "control", description: "Scan handoffs, plans, goals, tasks, and inbox items to find prioritized work that should\u2026" },
      { name: "dispatch_background_task", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", actionTopologyRole: "control", delegationPrimitive: true, description: "Hand an AGREED, multi-step task to the reliable background runner (fire-and-forget)." },
      { name: "draft_plan", sideEffect: "read", tier: "core", lanes: [], actionTopologyRole: "control", description: "Draft a structured plan for multi-step work before executing it." },
      { name: "execution_complete", sideEffect: "write", tier: "core", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "full-extra", loopClass: "mutating", actionTopologyRole: "control", actionControlContext: "alternate_action_owner", description: "Mark a tracked execution complete after its criteria are verified. Args: id, summary." },
      // Creating an execution lane MINTS DURABLE MUTATION AUTHORITY: it is the
      // record that later external writes point at to prove they were wrapped. A
      // 'read' classification told every telemetry and guardrail consumer that
      // opening that authority costs nothing, so a spinning turn could mint lanes
      // freely and none of it read as mutating work. Classify it honestly as a
      // write, with the same two overrides call_tool documents above:
      //   needsApproval FALSE — this tool is the ESCAPE from the execution gate
      //     (execution-gate.ts EXEMPT_TOOL_NAMES). Approval belongs to the effects
      //     performed INSIDE the lane, never to opening it; gating it here would
      //     deadlock every tracked execution and reintroduce a confirmation beat.
      //   loopClass 'mutating' — repeating it does not corrupt a provider, but it
      //     mints duplicate authority records, which is exactly the repetition the
      //     tight guardrail thresholds exist to catch.
      { name: "execution_create", sideEffect: "write", tier: "core", lanes: ["orchestrator", "sdk-brain"], sdkLayer: "full-extra", needsApproval: false, loopClass: "mutating", actionTopologyRole: "control", actionControlContext: "alternate_action_owner", delegationPrimitive: true, description: "Create a tracked execution lane for multi-step or mutating external work." },
      { name: "execution_get", sideEffect: "read", tier: "core", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "full-extra", actionTopologyRole: "control", actionControlContext: "alternate_action_owner", description: "Fetch one execution by id with full context (objective, plan, next step, success criteria\u2026" },
      { name: "execution_list", sideEffect: "read", tier: "core", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "full-extra", actionTopologyRole: "control", actionControlContext: "alternate_action_owner", description: "List executions for inspection." },
      { name: "execution_mark_blocked", sideEffect: "write", tier: "core", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "full-extra", loopClass: "mutating", actionTopologyRole: "control", actionControlContext: "alternate_action_owner", description: "Mark an execution as blocked with a concrete blocker description." },
      { name: "execution_update_step", sideEffect: "write", tier: "core", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "full-extra", loopClass: "mutating", actionTopologyRole: "control", actionControlContext: "alternate_action_owner", delegationPrimitive: true, description: "Advance an execution: record the next concrete step and an optional summary of what just\u2026" },
      { name: "execution_reconcile_write", sideEffect: "write", tier: "core", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "full-extra", loopClass: "mutating", actionTopologyRole: "control", actionControlContext: "alternate_action_owner", delegationPrimitive: true, description: "Settle an ambiguous/orphaned external write from read-back evidence: absent unlocks one retry, present records it done." },
      { name: "focus_activate", sideEffect: "write", runtimeEffect: "host_only", tier: "discoverable", lanes: ["orchestrator", "sdk-brain"], sdkLayer: "authoring", blockedFor: ["workflow-step", "worker"], loopClass: "mutating", actionTopologyRole: "control", actionControlContext: "task_recovery", description: "Resume a previously parked focus." },
      { name: "focus_clear", sideEffect: "write", runtimeEffect: "host_only", tier: "discoverable", lanes: ["orchestrator", "sdk-brain"], sdkLayer: "authoring", blockedFor: ["workflow-step", "worker"], loopClass: "mutating", actionTopologyRole: "control", description: "Mark a focus as done." },
      { name: "focus_get", sideEffect: "read", tier: "core", lanes: ["orchestrator", "sdk-brain"], sdkLayer: "authoring", blockedFor: ["workflow-step", "worker"], loopClass: "idempotent", cacheSafeRead: true, readMutatedBy: ["focus_set", "focus_update", "focus_touch", "focus_park", "focus_activate", "focus_clear"], actionTopologyRole: "control", actionControlContext: "task_recovery", description: "Read the assistant's current attention pointer." },
      { name: "focus_park", sideEffect: "write", runtimeEffect: "host_only", tier: "discoverable", lanes: ["orchestrator", "sdk-brain"], sdkLayer: "authoring", blockedFor: ["workflow-step", "worker"], loopClass: "mutating", actionTopologyRole: "control", description: "Park the active focus \u2014 flips it from active to paused so it stays resumable but no longe\u2026" },
      { name: "focus_set", sideEffect: "write", runtimeEffect: "host_only", tier: "core", lanes: ["orchestrator", "sdk-brain"], sdkLayer: "authoring", blockedFor: ["workflow-step", "worker"], loopClass: "mutating", actionTopologyRole: "control", description: "Pin a NEW current focus." },
      { name: "artifact_claim_resolve", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator"], loopClass: "mutating", actionTopologyRole: "control", description: "Resolve an unresolved provider-create claim after read-only verification: bind the found resource id (evidence-gated) or release a provably-absent claim." },
      { name: "focus_touch", sideEffect: "write", runtimeEffect: "host_only", tier: "discoverable", lanes: ["orchestrator", "sdk-brain"], sdkLayer: "authoring", blockedFor: ["workflow-step", "worker"], loopClass: "mutating", actionTopologyRole: "control", actionControlContext: "task_recovery", description: "Bump the last-touched time + reset the idle-confirm window for an active focus." },
      { name: "focus_update", sideEffect: "write", runtimeEffect: "host_only", tier: "core", lanes: ["orchestrator", "sdk-brain"], sdkLayer: "authoring", blockedFor: ["workflow-step", "worker"], loopClass: "mutating", actionTopologyRole: "control", description: "Evolve an existing focus and optionally patch its sparse shared workstate." },
      { name: "git_status", sideEffect: "read", projectEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", loopClass: "idempotent", description: "Run a read-only git status in an allowed workspace directory." },
      { name: "goal_list", sideEffect: "read", tier: "core", lanes: ["orchestrator", "cli"], loopClass: "idempotent", actionTopologyRole: "control", description: "List persistent goals, optionally filtered by owner or status." },
      { name: "goal_stale", sideEffect: "write", tier: "core", lanes: [], actionTopologyRole: "control", description: "Detect or mark long-running goals that have gone stale (not updated in a while)." },
      { name: "goal_upsert", sideEffect: "write", tier: "core", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", loopClass: "mutating", actionTopologyRole: "control", description: "Create a persistent goal when no id matches, or update the existing goal when one does \u2014 the single durable-goal write tool." },
      { name: "harness_status", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", loopClass: "idempotent", actionTopologyRole: "control", description: "Inspect Clementine harness-internal capability health." },
      { name: "hold_task_for_later", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", actionTopologyRole: "control", delegationPrimitive: true, description: 'HOLD an agreed multi-step task for later instead of running it now \u2014 the "or you can ask\u2026' },
      { name: "list_files", sideEffect: "read", hostReadOnlyExecution: "pure_local", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "inner-dispatch", "cli"], sdkLayer: "read-only", innerDispatch: "read", loopClass: "idempotent", cacheSafeRead: true, readMutatedBy: ["write_file", "artifact_bundle_save", "replace_file", "run_shell_command"], description: "List files in an allowed workspace directory." },
      { name: "list_pending_check_ins", sideEffect: "read", tier: "discoverable", lanes: ["cli"], actionTopologyRole: "control", description: "List open check-ins waiting for a user answer." },
      { name: "local_cli_list", sideEffect: "read", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "inner-dispatch", "cli"], sdkLayer: "agentic", innerDispatch: "read", actionTopologyRole: "control", delegationPrimitive: true, description: "List CLIs installed on the local machine and detected on $PATH." },
      { name: "local_cli_probe", sideEffect: "read", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "inner-dispatch", "cli"], sdkLayer: "agentic", innerDispatch: "read", actionTopologyRole: "control", delegationPrimitive: true, description: "Probe a specific local CLI by running `<command> --version` and `<command> --help`." },
      { name: "mcp_add", sideEffect: "admin", tier: "discoverable", lanes: ["orchestrator", "cli"], localPlanning: { consequence: "runtime_configuration", reversibility: "unknown", destructive: false, purpose: "configure_runtime", inputKind: "configuration", outputKind: "configuration", deliverableKind: "runtime_configuration", destinationPosture: "create_new", advisoryRoles: ["admin"] }, description: "Create a NEW external MCP server configuration." },
      { name: "mcp_configure", sideEffect: "admin", tier: "discoverable", lanes: ["orchestrator", "cli"], localPlanning: { consequence: "runtime_configuration", reversibility: "unknown", destructive: false, purpose: "configure_runtime", inputKind: "configuration", outputKind: "configuration", deliverableKind: "runtime_configuration", destinationPosture: "named_existing", advisoryRoles: ["admin"] }, description: "Edit an EXISTING external MCP server's NON-SECRET fields (description/command/args/url/he\u2026" },
      { name: "mcp_list_tools", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", loopClass: "idempotent", actionTopologyRole: "control", description: "Search one configured external MCP server for exact callable names and the best match's real input schema." },
      { name: "mcp_reconnect", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "cli"], actionTopologyRole: "control", description: "Recover an external MCP server that is degraded/unavailable (stuck in the connection back\u2026" },
      { name: "mcp_status", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", actionTopologyRole: "control", description: "Inspect configured external MCP servers available to Clementine." },
      { name: "memory_embed_backfill", sideEffect: "write", tier: "discoverable", lanes: ["cli"], actionTopologyRole: "control", description: "Compute embeddings for vault chunks and/or durable facts using the active embedding provi\u2026" },
      { name: "memory_forget", sideEffect: "write", runtimeEffect: "host_only", tier: "core", lanes: ["orchestrator", "cli"], loopClass: "mutating", actionTopologyRole: "control", description: "Soft-delete a fact by id (sets active=0)." },
      { name: "memory_import", sideEffect: "write", tier: "discoverable", lanes: ["cli"], actionTopologyRole: "control", description: "Import ANOTHER agent's memory files (Claude Code memories, OpenClaw/Fermis stores, bare m\u2026" },
      { name: "memory_list_facts", sideEffect: "read", projectEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", loopClass: "idempotent", actionTopologyRole: "control", description: "List or query durable facts as filterable JSON. Pass query for targeted lookup." },
      { name: "memory_pin", sideEffect: "write", runtimeEffect: "host_only", tier: "discoverable", lanes: ["orchestrator", "cli"], actionTopologyRole: "control", description: "Pin a fact as a STANDING INSTRUCTION (always injected into context, exempt from the recen\u2026" },
      { name: "memory_read", sideEffect: "read", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "inner-dispatch", "cli"], sdkLayer: "read-only", innerDispatch: "read", loopClass: "idempotent", actionTopologyRole: "control", description: "Read a durable memory reference (fact:<id> or policy:<id>), a key memory file, or a vault-relative markdown path." },
      { name: "memory_recall", sideEffect: "read", projectEffect: "read", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "inner-dispatch", "cli"], sdkLayer: "read-only", innerDispatch: "read", loopClass: "idempotent", cacheSafeRead: true, readMutatedBy: ["memory_remember", "memory_forget"], actionTopologyRole: "control", description: "Recall vault chunks." },
      { name: "memory_recall_all", sideEffect: "read", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "inner-dispatch", "cli"], sdkLayer: "read-only", innerDispatch: "read", loopClass: "idempotent", cacheSafeRead: true, readMutatedBy: ["memory_remember", "memory_forget"], actionTopologyRole: "control", description: "Recall relevant facts, notes, entities, resources, episodes, policies, and proven tools through one evidence-backed pipeline." },
      { name: "memory_remember", sideEffect: "write", runtimeEffect: "host_only", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", loopClass: "mutating", actionTopologyRole: "control", description: "Record a durable fact in long-term memory." },
      { name: "memory_restore", sideEffect: "write", runtimeEffect: "host_only", tier: "discoverable", lanes: ["orchestrator", "cli"], actionTopologyRole: "control", description: "Restore (reactivate) a soft-deleted fact by id \u2014 the inverse of memory_forget." },
      { name: "memory_review_instructions", sideEffect: "read", tier: "core", lanes: ["orchestrator", "cli"], actionTopologyRole: "control", description: "Before a batch/irreversible external write, review the standing instructions in play." },
      { name: "memory_search", sideEffect: "read", projectEffect: "read", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "inner-dispatch", "cli"], sdkLayer: "read-only", innerDispatch: "read", loopClass: "idempotent", cacheSafeRead: true, readMutatedBy: ["memory_remember", "memory_forget"], actionTopologyRole: "control", description: "Search the local Clementine vault for relevant notes and memories." },
      { name: "memory_search_facts", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "inner-dispatch", "cli"], sdkLayer: "read-only", innerDispatch: "read", actionTopologyRole: "control", description: "Semantically search durable FACTS (your long-term memory of the user, projects, standing\u2026" },
      { name: "memory_self_heal", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "cli"], actionTopologyRole: "control", description: "Inspect or run the audited long-term-memory self-heal loop." },
      { name: "note_create", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", loopClass: "mutating", actionTopologyRole: "control", description: "Create a new note in the vault." },
      { name: "notify_user", sideEffect: "read", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "agentic", blockedFor: ["worker"], loopClass: "mutating", actionTopologyRole: "control", description: "Send a notification to the user via the notification queue." },
      { name: "pending_action_execute", sideEffect: "write", tier: "core", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", blockedFor: ["worker"], needsApproval: false, loopClass: "mutating", actionTopologyRole: "control", delegationPrimitive: true, description: "Execute the exact stored payload of an approved single-call pending action." },
      { name: "pending_action_get", sideEffect: "read", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", actionTopologyRole: "control", description: "Read one queued action with its exact payload, status, approval id, preview, and result h\u2026" },
      { name: "pending_action_list", sideEffect: "read", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", actionTopologyRole: "control", description: "List durable pending actions." },
      { name: "pending_action_queue", sideEffect: "write", runtimeEffect: "host_only", tier: "core", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", blockedFor: ["worker"], actionTopologyRole: "control", description: "Queue a fully prepared action payload before an irreversible external write/send/deploy o\u2026" },
      { name: "pending_action_record_result", sideEffect: "write", tier: "core", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", blockedFor: ["worker"], actionTopologyRole: "control", description: "After executing or cancelling a queued action, record the outcome so Clementine can repor\u2026" },
      { name: "plan_task", sideEffect: "write", runtimeEffect: "host_only", hostControlFrame: "sole", hostModelFrameClass: "fresh_plan_barrier", tier: "core", lanes: [], needsApproval: false, loopClass: "mutating", actionTopologyRole: "control", description: "Admit and freeze one primary-model action plan for the exact accepted request." },
      { name: "ping", sideEffect: "read", tier: "discoverable", lanes: ["sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", actionTopologyRole: "control", description: "Basic health-check tool for the local Clementine tool runtime." },
      { name: "propose_check_in_template", sideEffect: "read", tier: "discoverable", lanes: ["cli"], actionTopologyRole: "control", description: "Propose a NEW autonomous check-in template the user can approve." },
      { name: "read_file", sideEffect: "read", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "inner-dispatch", "cli"], sdkLayer: "read-only", innerDispatch: "read", loopClass: "idempotent", cacheSafeRead: true, readMutatedBy: ["write_file", "artifact_bundle_save", "replace_file", "run_shell_command"], description: "Read a file from an allowed workspace path." },
      { name: "recall_tool_result", sideEffect: "read", projectEffect: "read", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "inner-dispatch"], sdkLayer: "read-only", innerDispatch: "read", loopClass: "idempotent", actionTopologyRole: "control", description: "Retrieve the full verbatim output of a prior tool call by its call_id." },
      { name: "request_approval", sideEffect: "write", tier: "core", lanes: [], blockedFor: ["worker"], loopClass: "mutating", actionTopologyRole: "control", description: "Pause and ask the user to approve a high-risk action or one batch of same-shape external\u2026" },
      { name: "resume_held_task", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", actionTopologyRole: "control", actionControlContext: "task_recovery", delegationPrimitive: true, description: 'Resume a task the user previously asked you to HOLD (see your Current Focus "Held" list),\u2026' },
      { name: "run_batch", sideEffect: "write", tier: "core", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "full-extra", blockedFor: ["worker"], actionTopologyRole: "control", delegationPrimitive: true, description: "Deterministic batch executor for N same-shape tool calls: reason ONCE (bake every item's\u2026" },
      { name: "run_shell_command", sideEffect: "write", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "inner-dispatch", "cli"], sdkLayer: "agentic", innerDispatch: "write", loopClass: "mutating", description: "Run a shell command in an allowed workspace directory." },
      // actionTopologyRole 'control': run_worker is the fan-out COORDINATION
      // primitive — it spawns children whose business dispatches settle at their
      // own boundaries (same family as execution_create). Classifying it business
      // made act-routed fan-out structurally impossible: dropped from the direct
      // surface, absent from the carrier universe, and walled by the work-binding
      // gate (live 2026-08-11: '"run_worker" is not a deferred callable tool').
      { name: "run_worker", sideEffect: "write", tier: "core", lanes: ["orchestrator", "sdk-brain"], sdkLayer: "full-extra", blockedFor: ["workflow-step", "worker"], actionTopologyRole: "control", delegationPrimitive: true, description: "Spawn a stateless Worker on ONE item using a structured parent-planned job packet." },
      { name: "session_history", sideEffect: "read", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "inner-dispatch", "cli"], sdkLayer: "read-only", innerDispatch: "read", loopClass: "idempotent", actionTopologyRole: "control", actionControlContext: "task_recovery", description: "Read recent conversation history for a session." },
      { name: "session_pause", sideEffect: "write", tier: "discoverable", lanes: ["cli"], actionTopologyRole: "control", description: "Save a structured handoff for a session so work can resume cleanly after context drift, a\u2026" },
      { name: "session_resume", sideEffect: "write", tier: "discoverable", lanes: ["cli"], actionTopologyRole: "control", description: "Summarize a session using its continuity brief and recent transcript so work can resume c\u2026" },
      { name: "set_model_role", sideEffect: "write", tier: "core", lanes: ["orchestrator", "sdk-brain"], sdkLayer: "authoring", actionTopologyRole: "control", description: "Route a model ROLE to a specific model, when the user asks in chat (e.g." },
      { name: "set_timer", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", loopClass: "mutating", actionTopologyRole: "control", description: 'Schedule a one-time reminder notification ("remind me") at an exact time tonight or in N minutes, within 24 hours.' },
      { name: "share_plan", sideEffect: "read", tier: "core", lanes: ["orchestrator", "cli"], actionTopologyRole: "control", description: "Share a non-blocking working plan in the current chat before continuing." },
      { name: "skill_list", sideEffect: "read", projectEffect: "read", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "inner-dispatch", "cli"], sdkLayer: "read-only", innerDispatch: "read", loopClass: "idempotent", cacheSafeRead: true, actionTopologyRole: "control", description: "List installed SKILL.md skills (Anthropic Skills format) with name + one-line description." },
      { name: "skill_read", sideEffect: "read", projectEffect: "read", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "inner-dispatch", "cli"], sdkLayer: "read-only", innerDispatch: "read", loopClass: "idempotent", cacheSafeRead: true, actionTopologyRole: "control", description: "Load the full body of an installed SKILL.md skill into context." },
      { name: "source_map_upsert", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", actionTopologyRole: "control", description: "Record WHERE a resource lives in one of the user's connected sources \u2014 a Drive folder, an\u2026" },
      { name: "space_action_prepare", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain"], sdkLayer: "authoring", featureGroup: "spaces-dock", loopClass: "mutating", actionTopologyRole: "control", delegationPrimitive: true, description: "Prepare one declared Workspace action for exact approval, or execute it only when an exact existing standing approval covers the current action, arguments, and runner bytes." },
      { name: "space_diff", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker"], sdkLayer: "read-only", featureGroup: "spaces-dock", loopClass: "idempotent", actionTopologyRole: "control", description: "Compare two retained successful observations for one Workspace data source, defaulting to current versus prior." },
      { name: "space_edit_runner", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain"], sdkLayer: "authoring", featureGroup: "spaces-dock", actionTopologyRole: "control", delegationPrimitive: true, localPlanning: { consequence: "workspace_definition", reversibility: "reversible", destructive: false, purpose: "author_workspace_runner", inputKind: "workspace_runner_patch", outputKind: "workspace_revision", deliverableKind: "workspace", destinationPosture: "named_existing", advisoryRoles: ["author", "destination"] }, description: "Make a TARGETED, reversible edit to a Workspace runner's SOURCE \u2014 FAST, for changing what\u2026" },
      { name: "space_edit_view", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain"], sdkLayer: "authoring", featureGroup: "spaces-dock", actionTopologyRole: "control", localPlanning: { consequence: "workspace_definition", reversibility: "reversible", destructive: false, purpose: "author_workspace", inputKind: "workspace_patch", outputKind: "workspace_revision", deliverableKind: "workspace", destinationPosture: "named_existing", advisoryRoles: ["author", "destination"] }, description: "Make a TARGETED edit to an existing Workspace view \u2014 FAST, for small tweaks (a button, la\u2026" },
      { name: "space_get", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker"], sdkLayer: "read-only", featureGroup: "spaces-dock", actionTopologyRole: "control", description: "Read a Workspace: its manifest (title, status, data sources, re-engage contract), a snaps\u2026" },
      { name: "space_get_runner", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker"], sdkLayer: "read-only", featureGroup: "spaces-dock", actionTopologyRole: "control", description: "Read the SOURCE of a Workspace data/action RUNNER (the .mjs/.py/.sh script under data/ th\u2026" },
      { name: "space_get_view", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker"], sdkLayer: "read-only", featureGroup: "spaces-dock", actionTopologyRole: "control", description: "Read the CURRENT view HTML of a Workspace, line-numbered \u2014 this is the EXACT text you nee\u2026" },
      { name: "space_history", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker"], sdkLayer: "read-only", featureGroup: "spaces-dock", loopClass: "idempotent", actionTopologyRole: "control", description: "List bounded, metadata-only observation history for a Workspace without loading raw retained datasets." },
      { name: "space_list", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker"], sdkLayer: "read-only", featureGroup: "spaces-dock", actionTopologyRole: "control", description: "List the user's Workspaces (persistent interactive surfaces you built)." },
      { name: "space_publish", sideEffect: "send", tier: "discoverable", lanes: ["orchestrator", "sdk-brain"], sdkLayer: "authoring", featureGroup: "spaces-dock", description: "Export a Workspace as a STATIC, share-ready snapshot \u2014 the shareable counterpart to the l\u2026" },
      { name: "space_refresh", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain"], sdkLayer: "authoring", featureGroup: "spaces-dock", actionTopologyRole: "control", delegationPrimitive: true, description: "Re-run a Workspace's data source(s) NOW (server-side, no LLM) and persist the fresh datas\u2026" },
      { name: "space_revert_runner", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain"], sdkLayer: "authoring", featureGroup: "spaces-dock", actionTopologyRole: "control", delegationPrimitive: true, description: "Undo the most recent space_edit_runner on a runner, restoring its prior source from the s\u2026" },
      { name: "space_save", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain"], sdkLayer: "authoring", featureGroup: "spaces-dock", actionTopologyRole: "control", delegationPrimitive: true, localPlanning: { consequence: "workspace_definition", reversibility: "reversible", destructive: false, purpose: "author_workspace", inputKind: "workspace_definition", outputKind: "workspace_revision", deliverableKind: "workspace", destinationPosture: "create_new", advisoryRoles: ["author", "create", "destination"] }, description: "Create or update a Workspace \u2014 a persistent, interactive HTML surface you build for the u\u2026" },
      { name: "space_set_data", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain"], sdkLayer: "authoring", featureGroup: "spaces-dock", actionTopologyRole: "control", loopClass: "mutating", localPlanning: { consequence: "workspace_definition", reversibility: "reversible", destructive: false, purpose: "update_workspace_dataset", inputKind: "workspace_dataset", outputKind: "workspace_observation", deliverableKind: "workspace", destinationPosture: "named_existing", advisoryRoles: ["update", "destination"] }, localExecution: { version: 1, adapter: "workspace_dataset_v1", idempotency: "content_addressed", reconciliation: "workspace_dataset_v1" }, description: "Commit a dataset you ALREADY HAVE IN HAND directly into the workspace under a source id \u2014\u2026" },
      { name: "space_try_runner", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain"], sdkLayer: "authoring", featureGroup: "spaces-dock", actionTopologyRole: "control", description: "Statically inspect a legacy Workspace runner's declared role and provenance without executing it; execution remains behind space_refresh or the normal Workspace action approval path." },
      { name: "surface_plan", sideEffect: "read", tier: "core", lanes: ["orchestrator", "cli"], blockedFor: ["workflow-step", "worker"], actionTopologyRole: "control", description: "Surface a Plan you just received from `draft_plan` to the user for review." },
      { name: "task_add", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", loopClass: "mutating", actionTopologyRole: "control", description: "Add a passive one-time item to the user's TODO list; it does not fire at a scheduled time." },
      { name: "task_hygiene", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "cli"], actionTopologyRole: "control", description: "Repair and compact the task ledger so completed execution-owned tasks do not remain in th\u2026" },
      { name: "task_list", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", loopClass: "idempotent", actionTopologyRole: "control", description: "List tasks from the master task list." },
      { name: "task_update", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", loopClass: "mutating", actionTopologyRole: "control", description: "Update a task by ID and optionally move it between pending and completed." },
      { name: "team_list", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", actionTopologyRole: "control", description: "List all team agents and their messaging permissions." },
      { name: "team_message", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", actionTopologyRole: "control", delegationPrimitive: true, description: "Queue a message to another team agent." },
      { name: "team_pending_requests", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", actionTopologyRole: "control", description: "List pending requests assigned to the current team agent." },
      { name: "team_reply", sideEffect: "send", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", delegationPrimitive: true, description: "Reply to a queued team request and mark it completed." },
      { name: "team_request", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", actionTopologyRole: "control", delegationPrimitive: true, description: "Create a structured request for another team agent and queue it locally." },
      { name: "tool_choice_forget", sideEffect: "write", tier: "core", lanes: ["orchestrator"], actionTopologyRole: "control", description: "HARD-clear a remembered tool choice so the next request fully re-discovers it." },
      { name: "tool_choice_invalidate", sideEffect: "write", tier: "core", lanes: ["orchestrator", "cli"], loopClass: "mutating", actionTopologyRole: "control", description: "Mark the currently-recorded tool choice for an intent as broken." },
      { name: "tool_choice_recall", sideEffect: "read", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", loopClass: "idempotent", actionTopologyRole: "control", description: "Look up the previously-recorded tool choice for an intent (per-machine memory)." },
      { name: "tool_choice_remember", sideEffect: "write", tier: "core", lanes: ["orchestrator", "cli"], loopClass: "mutating", actionTopologyRole: "control", description: "Save the tool that worked for an intent so future runs skip discovery." },
      { name: "tool_output_query", sideEffect: "read", projectEffect: "read", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "inner-dispatch"], sdkLayer: "read-only", innerDispatch: "read", actionTopologyRole: "control", description: "Query a slice of a large prior tool output by its call_id, without loading the whole payl\u2026" },
      { name: "tool_search", sideEffect: "read", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", actionTopologyRole: "control", description: "Search the full built-in tool catalog by intent and get matching names, summaries, and sc\u2026" },
      { name: "update_agent", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", blockedFor: ["workflow-step", "worker"], actionTopologyRole: "control", delegationPrimitive: true, description: "Update an existing team agent." },
      { name: "user_profile_read", sideEffect: "read", projectEffect: "read", hostReadOnlyExecution: "pure_local", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "inner-dispatch", "cli"], sdkLayer: "read-only", innerDispatch: "read", loopClass: "idempotent", description: "Read the user's current profile (name, role, timezone, working hours, communication prefe\u2026" },
      { name: "workflow_apply_contract_fixes", sideEffect: "write", tier: "discoverable", lanes: ["cli"], actionTopologyRole: "control", description: "Apply safe, machine-readable fixes from a workflow visual contract." },
      { name: "workflow_capability_resolve", sideEffect: "write", runtimeEffect: "host_only", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", loopClass: "mutating", actionTopologyRole: "control", description: "Resolve an exact workflow capability pause by saving a human account choice or retrying after setup." },
      { name: "workflow_create", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", blockedFor: ["workflow-step", "worker"], actionTopologyRole: "control", delegationPrimitive: true, localPlanning: { consequence: "workflow_definition", reversibility: "reversible", destructive: false, purpose: "author_workflow", inputKind: "workflow_definition", outputKind: "workflow_revision", deliverableKind: "workflow", destinationPosture: "create_new", advisoryRoles: ["author", "create", "destination"] }, description: "Create a workflow." },
      { name: "workflow_delete", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "cli"], blockedFor: ["workflow-step", "worker"], actionTopologyRole: "control", localPlanning: { consequence: "workflow_definition", reversibility: "irreversible", destructive: true, purpose: "delete_workflow", inputKind: "workflow_reference", outputKind: "deletion_receipt", deliverableKind: "workflow", destinationPosture: "named_existing", advisoryRoles: ["delete", "destination"] }, description: "Permanently delete a workflow definition file." },
      { name: "workflow_edit_step", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", actionTopologyRole: "control", delegationPrimitive: true, localPlanning: { consequence: "workflow_definition", reversibility: "reversible", destructive: false, purpose: "author_workflow", inputKind: "workflow_patch", outputKind: "workflow_revision", deliverableKind: "workflow", destinationPosture: "named_existing", advisoryRoles: ["author", "destination"] }, description: "Make a TARGETED, reversible edit to ONE step's prompt in an existing workflow \u2014 the FAST,\u2026" },
      { name: "workflow_from_session", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", actionTopologyRole: "control", delegationPrimitive: true, description: "Turn what you JUST did in this chat into a reusable, repeatable workflow." },
      { name: "workflow_get", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", loopClass: "idempotent", actionTopologyRole: "control", description: "Fetch the full definition of a single workflow by name." },
      { name: "workflow_import_framework", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "cli"], blockedFor: ["workflow-step", "worker"], actionTopologyRole: "control", delegationPrimitive: true, description: "Import workflow framework packages from a local folder or GitHub repo." },
      { name: "workflow_import_status", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "cli"], actionTopologyRole: "control", description: "Check a workflow framework import job." },
      { name: "workflow_list", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", loopClass: "idempotent", actionTopologyRole: "control", description: "List all workflows with description, steps, and trigger metadata." },
      { name: "workflow_rerun_failed_items", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", loopClass: "mutating", actionTopologyRole: "control", delegationPrimitive: true, description: "Re-run only the failed forEach items from a prior workflow run." },
      { name: "workflow_run", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", blockedFor: ["workflow-step", "worker"], loopClass: "mutating", actionTopologyRole: "control", delegationPrimitive: true, localPlanning: { consequence: "workflow_definition", reversibility: "reversible", destructive: false, purpose: "dispatch_named_workflow", inputKind: "workflow_identity", outputKind: "workflow_run", deliverableKind: "workflow", destinationPosture: "create_new", advisoryRoles: ["control", "destination"] }, description: "Dispatch a workflow to run in the BACKGROUND (fire-and-forget) \u2014 it runs in the daemon an\u2026" },
      { name: "workflow_run_status", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", loopClass: "idempotent", actionTopologyRole: "control", description: "Check workflow runs." },
      { name: "workflow_schedule", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", blockedFor: ["workflow-step", "worker"], actionTopologyRole: "control", delegationPrimitive: true, description: "Schedule a workflow to fire on a cron expression." },
      { name: "workflow_set_enabled", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", blockedFor: ["workflow-step", "worker"], actionTopologyRole: "control", delegationPrimitive: true, description: "Approve or disable a workflow." },
      // Data-operation primitive (2026-07-21 capability audit #1): deterministic
      // reconcile/transform — the "spreadsheet brain". Pure read-class compute
      // (its only write is a transient staged spill file). Available everywhere
      // including workflow-step/worker/inner-dispatch: reconciliation is the middle of
      // the employee loop and must never require a bigger lane.
      { name: "table_ops", sideEffect: "read", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "workflow-step", "inner-dispatch", "cli"], sdkLayer: "read-only", innerDispatch: "read", loopClass: "idempotent", description: "Deterministic table algebra over lists/sheets: diff, intersect, join, dedupe, aggregate, select \u2014 sourced from inline rows, a prior tool call id (full parked output), or a staged file." },
      // Document production (2026-07-21 capability audit #4): render→PDF/DOCX +
      // template merge. A local reversible file write (the artifact chains into
      // uploads via the file pipeline); the SEND of that artifact stays gated at
      // the send tool as always.
      { name: "produce_document", sideEffect: "write", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "workflow-step", "cli"], sdkLayer: "read-only", loopClass: "mutating", description: "Produce a real PDF/DOCX/HTML file from markdown or HTML with {{var}} template merge \u2014 returns a local filePath that chains into uploads/attachments." },
      // Large-input retrieval (2026-07-21 capability audit #2 v1): deterministic
      // chunk-and-retrieve over big files (auto-converted) and parked outputs.
      // actionTopologyRole 'control': same recall class as recall_tool_result /
      // tool_output_query — a deterministic host read over ALREADY-LANDED output.
      // Defaulting to 'business' routed it through work_call on action surfaces
      // (live 2026-08-18: a program-carried file_query → work_binding_required
      // while the model was just trying to read its own search result).
      { name: "file_query", sideEffect: "read", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "workflow-step", "inner-dispatch", "cli"], sdkLayer: "read-only", innerDispatch: "read", loopClass: "idempotent", actionTopologyRole: "control", description: "Query a big document or prior tool output for relevant passages (heading-aware chunks, deterministic ranking) instead of reading a byte-clipped preview." },
      // Structured extraction (2026-07-21 capability audit #3): schema-guided
      // NL→payload with deterministic validation. Uses a model internally (the
      // boundary-judge routing) but mutates nothing — read-class; the CREATE that
      // consumes the payload stays gated as always.
      { name: "extract_structured", sideEffect: "read", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "workflow-step", "cli"], sdkLayer: "read-only", loopClass: "idempotent", description: "Extract validated structured data from text/files/tool outputs against a JSON Schema or a cached Composio action schema \u2014 required fields verified, never invented." },
      // Mutual availability (2026-07-21 capability audit #6): pure interval
      // algebra over attendee busy windows fetched via the calendar actions.
      { name: "time_slots", sideEffect: "read", projectEffect: "read", hostReadOnlyExecution: "pure_local", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "workflow-step", "inner-dispatch", "cli"], sdkLayer: "read-only", innerDispatch: "read", loopClass: "idempotent", description: "Compute mutual free meeting slots from attendees' busy intervals \u2014 exact interval algebra with working-hours/duration constraints." },
      // Employee-memory primitive (2026-07-21): durable cross-RUN state. Core +
      // available in the workflow-step/worker lanes — that's exactly where the
      // amnesia lived (an hourly scrape re-processed the same items every run).
      // sdkLayer read-only mirrors memory_remember: a LOCAL durable write is
      // allowed even on the read-only step lane (it mutates nothing external).
      { name: "workflow_reshape", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "workflow-step", "cli"], sdkLayer: "authoring", loopClass: "mutating", actionTopologyRole: "control", delegationPrimitive: true, description: "Add read-only prompt nodes with only a result channel and run-scoped artifact query, plus dependency edges into those nodes, on an active workflow run. Dynamic writes/sends, scripts, fan-out, and edge disable/enable are not supported; completed and in-flight targets are immutable." },
      { name: "workflow_state", sideEffect: "write", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "workflow-step", "cli"], sdkLayer: "read-only", loopClass: "mutating", actionTopologyRole: "control", description: "Durable per-workflow memory across runs: watermarks/cursors + a processed-item ledger (filter_unprocessed / mark_processed) so recurring runs never redo work." },
      { name: "workflow_unschedule", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", blockedFor: ["workflow-step", "worker"], actionTopologyRole: "control", description: "Disable a scheduled workflow so it stops firing, without deleting it." },
      { name: "workflow_update", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", blockedFor: ["workflow-step", "worker"], actionTopologyRole: "control", delegationPrimitive: true, localPlanning: { consequence: "workflow_definition", reversibility: "reversible", destructive: false, purpose: "author_workflow", inputKind: "workflow_patch", outputKind: "workflow_revision", deliverableKind: "workflow", destinationPosture: "named_existing", advisoryRoles: ["author", "destination"] }, description: "Patch workflow fields. If `steps` is provided it replaces the entire graph; use workflow_edit_step for one targeted prompt edit." },
      { name: "working_memory", sideEffect: "write", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "cli"], sdkLayer: "authoring", actionTopologyRole: "control", description: "Read, append, replace, or clear the working-memory scratchpad." },
      { name: "workspace_artifact_query", sideEffect: "read", projectEffect: "read", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "inner-dispatch", "cli"], sdkLayer: "read-only", innerDispatch: "read", loopClass: "idempotent", actionTopologyRole: "control", description: "Query exact rows, fields, and pages from a JSON/JSONL run-workspace artifact." },
      { name: "workspace_info", sideEffect: "read", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", loopClass: "idempotent", actionTopologyRole: "control", description: "Get detailed info about a local project including README, CLAUDE.md, manifest, and struct\u2026" },
      { name: "workspace_list", sideEffect: "read", projectEffect: "read", hostReadOnlyExecution: "pure_local", tier: "discoverable", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "cli"], sdkLayer: "read-only", loopClass: "idempotent", description: "List local projects found in configured workspace directories." },
      { name: "workspace_roots", sideEffect: "read", projectEffect: "read", hostReadOnlyExecution: "pure_local", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "inner-dispatch", "cli"], sdkLayer: "read-only", innerDispatch: "read", loopClass: "idempotent", description: "List directories Clementine is allowed to inspect or operate in." },
      { name: "write_file", sideEffect: "write", tier: "core", lanes: ["orchestrator", "sdk-brain", "sdk-worker", "inner-dispatch", "cli"], sdkLayer: "agentic", innerDispatch: "write", loopClass: "mutating", localPlanning: { consequence: "local_artifact", reversibility: "create_only", destructive: false, purpose: "create_local_artifact", inputKind: "artifact_content", outputKind: "local_artifact", deliverableKind: "file", destinationPosture: "create_new", advisoryRoles: ["author", "create", "destination"], safeMode: { id: "create", requiredEquals: { mode: "create" }, nullEquivalentToRequired: ["mode"], absentOrNull: ["append"] } }, localPlanningVariants: [{ consequence: "local_artifact", reversibility: "irreversible", destructive: false, purpose: "append_local_artifact", inputKind: "artifact_content", outputKind: "local_artifact", deliverableKind: "file", destinationPosture: "named_existing", advisoryRoles: ["author", "update", "destination"], safeMode: { id: "append", requiredEquals: {}, alternatives: [{ requiredEquals: { append: true }, allowedValues: { mode: ["create", "append", "overwrite", null] } }, { requiredEquals: { mode: "append" }, absentOrNull: ["append"] }] } }, { consequence: "local_artifact", reversibility: "irreversible", destructive: true, purpose: "overwrite_local_artifact", inputKind: "artifact_content", outputKind: "local_artifact", deliverableKind: "file", destinationPosture: "named_existing", advisoryRoles: ["author", "update", "destination"], safeMode: { id: "overwrite", requiredEquals: {}, alternatives: [{ requiredEquals: { append: false }, allowedValues: { mode: ["create", "append", "overwrite", null] } }, { requiredEquals: { mode: "overwrite" }, absentOrNull: ["append"] }] } }], description: "Create, append to, or overwrite a UTF-8 file inside an allowed local workspace path (content capped ~24KB/call \u2014 write big files in append:true chunks)." }
    ];
  }
});

// src/tools/artifact-bundle-core.ts
var ARTIFACT_BUNDLE_LIMITS;
var init_artifact_bundle_core = __esm({
  "src/tools/artifact-bundle-core.ts"() {
    "use strict";
    init_config();
    init_closed_canonical_json();
    ARTIFACT_BUNDLE_LIMITS = Object.freeze({
      maxFiles: 32,
      maxFileBytes: 64e3,
      maxTotalBytes: 256e3,
      maxPathBytes: 240,
      maxPathSegmentBytes: 120
    });
  }
});

// src/tools/artifact-bundle-contract.ts
var import_zod2, artifactBundleFileShape, ARTIFACT_BUNDLE_TOOL_PARAMETERS;
var init_artifact_bundle_contract = __esm({
  "src/tools/artifact-bundle-contract.ts"() {
    "use strict";
    import_zod2 = require("zod");
    init_artifact_bundle_core();
    artifactBundleFileShape = import_zod2.z.object({
      path: import_zod2.z.string().min(1).max(ARTIFACT_BUNDLE_LIMITS.maxPathBytes).describe('Safe relative POSIX path inside the bundle, for example "public/index.html". Absolute paths, backslashes, dot segments, and duplicates are refused.'),
      content: import_zod2.z.string().describe(`Exact UTF-8 file content (maximum ${ARTIFACT_BUNDLE_LIMITS.maxFileBytes} bytes per file).`)
    }).strict();
    ARTIFACT_BUNDLE_TOOL_PARAMETERS = {
      bundle_id: import_zod2.z.string().min(1).max(80).describe('Stable lowercase artifact id, for example "sales-portal". The revision digest is derived from every file path and byte sequence.'),
      mode: import_zod2.z.literal("content_addressed").describe("Required safety mode. Revisions are immutable and never overwrite a prior revision."),
      files: import_zod2.z.array(artifactBundleFileShape).min(1).max(ARTIFACT_BUNDLE_LIMITS.maxFiles)
    };
  }
});

// src/spaces/workspace-set-data-contract.ts
function sha256(value) {
  return (0, import_node_crypto3.createHash)("sha256").update(value, "utf8").digest("hex");
}
function utf8(value) {
  return Buffer.byteLength(value, "utf8");
}
function canonicalWorkspaceJson(value) {
  let nodes = 0;
  const visit = (input, depth) => {
    nodes += 1;
    if (nodes > MAX_JSON_NODES || depth > MAX_JSON_DEPTH) {
      throw new WorkspaceSetDataContractError(
        "json_limit_exceeded",
        "data_json exceeds the reviewed JSON traversal limit"
      );
    }
    if (input === null || typeof input === "string" || typeof input === "boolean") {
      return JSON.stringify(input);
    }
    if (typeof input === "number" && Number.isFinite(input)) return JSON.stringify(input);
    if (Array.isArray(input)) {
      return `[${input.map((entry) => visit(entry, depth + 1)).join(",")}]`;
    }
    if (input && typeof input === "object") {
      const object = input;
      return `{${Object.keys(object).sort().map(
        (key) => `${JSON.stringify(key)}:${visit(object[key], depth + 1)}`
      ).join(",")}}`;
    }
    throw new WorkspaceSetDataContractError(
      "invalid_json",
      "data_json contains a value outside the JSON domain"
    );
  };
  const canonical = visit(value, 0);
  if (utf8(canonical) > WORKSPACE_SET_DATA_MAX_BYTES) {
    throw new WorkspaceSetDataContractError(
      "json_limit_exceeded",
      `data_json exceeds the ${WORKSPACE_SET_DATA_MAX_BYTES}-byte limit`
    );
  }
  return canonical;
}
function workspaceDataContentDigest(value) {
  return sha256(canonicalWorkspaceJson(value));
}
function exactArguments(value) {
  const parsed = import_zod3.z.strictObject(WORKSPACE_SET_DATA_TOOL_PARAMETERS).safeParse(value);
  if (!parsed.success) {
    const record3 = value && typeof value === "object" && !Array.isArray(value) ? value : {};
    if (typeof record3.slug === "string" && !WORKSPACE_SLUG_RE.test(record3.slug)) {
      throw new WorkspaceSetDataContractError("invalid_slug", `invalid workspace slug "${record3.slug}"`);
    }
    throw new WorkspaceSetDataContractError(
      "arguments_invalid",
      "space_set_data arguments do not match the closed reviewed schema"
    );
  }
  return parsed.data;
}
function prepareWorkspaceSetData(value) {
  const args = exactArguments(value);
  if (!WORKSPACE_SLUG_RE.test(args.slug)) {
    throw new WorkspaceSetDataContractError("invalid_slug", `invalid workspace slug "${args.slug}"`);
  }
  const sourceId = args.source_id;
  if (sourceId !== sourceId.trim() || sourceId.length === 0 || sourceId.length > WORKSPACE_SET_DATA_MAX_SOURCE_CHARS || SOURCE_CONTROL_RE.test(sourceId)) {
    throw new WorkspaceSetDataContractError(
      "invalid_source",
      "source_id must already be canonical, bounded, and free of control characters"
    );
  }
  if (sourceId === "_meta") {
    throw new WorkspaceSetDataContractError(
      "reserved_source",
      '"_meta" is a reserved Workspace source key'
    );
  }
  if (utf8(args.data_json) > WORKSPACE_SET_DATA_MAX_BYTES) {
    throw new WorkspaceSetDataContractError(
      "json_limit_exceeded",
      `data_json exceeds the ${WORKSPACE_SET_DATA_MAX_BYTES}-byte limit`
    );
  }
  let data;
  try {
    data = JSON.parse(args.data_json);
  } catch (error) {
    throw new WorkspaceSetDataContractError(
      "invalid_json",
      `data_json is not valid JSON: ${error instanceof Error ? error.message : String(error)}`
    );
  }
  if (!data || typeof data !== "object") {
    throw new WorkspaceSetDataContractError(
      "invalid_json_root",
      "data_json must contain one complete JSON object or array"
    );
  }
  const canonicalData = canonicalWorkspaceJson(data);
  const contentDigest = sha256(canonicalData);
  const argsDigest = sha256([
    `${ARTIFACT_PREFIX}\0`,
    args.slug,
    "\0",
    sourceId,
    "\0",
    canonicalData
  ].join(""));
  return Object.freeze({
    args: Object.freeze({ ...args }),
    slug: args.slug,
    sourceId,
    data,
    canonicalData,
    contentDigest,
    argsDigest,
    refreshId: `${REFRESH_PREFIX}:${argsDigest}`
  });
}
function encodeSegment(value) {
  return Buffer.from(value, "utf8").toString("base64url");
}
function decodeSegment(value) {
  if (!/^[A-Za-z0-9_-]+$/.test(value)) return null;
  try {
    const decoded = Buffer.from(value, "base64url").toString("utf8");
    return encodeSegment(decoded) === value ? decoded : null;
  } catch {
    return null;
  }
}
function workspaceDatasetArtifactId(value) {
  return [
    ARTIFACT_PREFIX,
    encodeSegment(value.slug),
    encodeSegment(value.sourceId),
    value.argsDigest,
    value.contentDigest
  ].join(":");
}
function parseWorkspaceDatasetArtifactId(artifactId) {
  const parts = artifactId.split(":");
  if (parts.length !== 6 || `${parts[0]}:${parts[1]}` !== ARTIFACT_PREFIX) return null;
  const slug = decodeSegment(parts[2]);
  const sourceId = decodeSegment(parts[3]);
  const argsDigest = parts[4];
  const contentDigest = parts[5];
  if (!slug || !WORKSPACE_SLUG_RE.test(slug) || sourceId === null || sourceId !== sourceId.trim() || sourceId.length === 0 || sourceId.length > WORKSPACE_SET_DATA_MAX_SOURCE_CHARS || SOURCE_CONTROL_RE.test(sourceId) || sourceId === "_meta" || !DIGEST_RE.test(argsDigest) || !DIGEST_RE.test(contentDigest)) return null;
  return {
    slug,
    sourceId,
    argsDigest,
    contentDigest,
    refreshId: `${REFRESH_PREFIX}:${argsDigest}`
  };
}
var import_node_crypto3, import_zod3, WORKSPACE_SET_DATA_MAX_BYTES, WORKSPACE_SET_DATA_MAX_SOURCE_CHARS, WORKSPACE_SLUG_RE, SOURCE_CONTROL_RE, DIGEST_RE, ARTIFACT_PREFIX, REFRESH_PREFIX, MAX_JSON_NODES, MAX_JSON_DEPTH, WORKSPACE_SET_DATA_TOOL_PARAMETERS, WorkspaceSetDataContractError;
var init_workspace_set_data_contract = __esm({
  "src/spaces/workspace-set-data-contract.ts"() {
    "use strict";
    import_node_crypto3 = require("node:crypto");
    import_zod3 = require("zod");
    WORKSPACE_SET_DATA_MAX_BYTES = 5 * 1024 * 1024;
    WORKSPACE_SET_DATA_MAX_SOURCE_CHARS = 120;
    WORKSPACE_SLUG_RE = /^[a-z0-9][a-z0-9-]{0,61}[a-z0-9]$/;
    SOURCE_CONTROL_RE = /[\u0000-\u001f\u007f-\u009f]/;
    DIGEST_RE = /^[a-f0-9]{64}$/;
    ARTIFACT_PREFIX = "workspace-dataset:v1";
    REFRESH_PREFIX = "workspace-set-data:v1";
    MAX_JSON_NODES = 2e5;
    MAX_JSON_DEPTH = 64;
    WORKSPACE_SET_DATA_TOOL_PARAMETERS = {
      slug: import_zod3.z.string().min(2).max(63).regex(WORKSPACE_SLUG_RE).describe("The existing active workspace slug."),
      source_id: import_zod3.z.string().min(1).max(WORKSPACE_SET_DATA_MAX_SOURCE_CHARS).describe("The canonical non-reserved source id to replace."),
      data_json: import_zod3.z.string().min(1).max(WORKSPACE_SET_DATA_MAX_BYTES).describe("A complete JSON object or array for this source id.")
    };
    WorkspaceSetDataContractError = class extends Error {
      constructor(code, message) {
        super(message);
        this.code = code;
        this.name = "WorkspaceSetDataContractError";
      }
      code;
    };
  }
});

// src/runtime/harness/reviewed-local-tool-transport.ts
function isRecord(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function exactDeclaration(name) {
  const rows = TOOL_REGISTRY.filter((entry) => entry.name === name);
  return rows.length === 1 ? rows[0] : null;
}
function validExecutionContract(value) {
  if (!value || typeof value !== "object" || Array.isArray(value)) return false;
  const contract = value;
  return contract.version === 1 && contract.idempotency === "content_addressed" && (contract.adapter === "artifact_bundle_v1" && contract.reconciliation === "artifact_bundle_v1" || contract.adapter === "workspace_dataset_v1" && contract.reconciliation === "workspace_dataset_v1");
}
function safeToken(value) {
  const normalized = value.trim().toLowerCase().replace(/[^a-z0-9._/-]+/g, "_");
  return normalized && /^[a-z0-9][a-z0-9._/-]{0,63}$/.test(normalized) ? normalized : null;
}
function schemaAllowsExact(schema, expected) {
  if (!isRecord(schema)) return false;
  if (expected === null && schema.type === "null") return true;
  if (schema.const === expected) return true;
  if (Array.isArray(schema.enum) && schema.enum.some((value) => value === expected)) return true;
  for (const key of ["anyOf", "oneOf", "allOf"]) {
    const branches = schema[key];
    if (Array.isArray(branches) && branches.some((branch) => schemaAllowsExact(branch, expected))) return true;
  }
  return false;
}
function normalizedSemantics(semantics) {
  return {
    consequence: semantics.consequence,
    reversibility: semantics.reversibility,
    destructive: semantics.destructive,
    purpose: semantics.purpose.trim(),
    inputKind: semantics.inputKind.trim(),
    outputKind: semantics.outputKind.trim(),
    deliverableKind: semantics.deliverableKind.trim(),
    destinationPosture: semantics.destinationPosture,
    advisoryRoles: [...new Set(semantics.advisoryRoles.map((role) => role.trim()).filter(Boolean))],
    ...semantics.safeMode ? {
      safeMode: {
        id: semantics.safeMode.id.trim(),
        requiredEquals: { ...semantics.safeMode.requiredEquals },
        ...semantics.safeMode.nullEquivalentToRequired ? {
          nullEquivalentToRequired: [...new Set(
            semantics.safeMode.nullEquivalentToRequired.map((field) => field.trim()).filter(Boolean)
          )]
        } : {},
        ...semantics.safeMode.absentOrNull ? { absentOrNull: [...semantics.safeMode.absentOrNull] } : {}
      }
    } : {}
  };
}
function structurallyCarriesSafeMode(schema, semantics) {
  if (semantics.reversibility !== "create_only") return semantics.safeMode === void 0;
  const safeMode = semantics.safeMode;
  if (!safeMode || !safeToken(safeMode.id)) return false;
  const properties = isRecord(schema.properties) ? schema.properties : null;
  if (!properties) return false;
  for (const [field, expected] of Object.entries(safeMode.requiredEquals)) {
    if (!Object.hasOwn(properties, field) || !schemaAllowsExact(properties[field], expected)) return false;
  }
  const nullEquivalent = new Set(safeMode.nullEquivalentToRequired ?? []);
  if (nullEquivalent.size !== (safeMode.nullEquivalentToRequired?.length ?? 0)) return false;
  for (const field of nullEquivalent) {
    if (!Object.hasOwn(safeMode.requiredEquals, field) || !Object.hasOwn(properties, field) || !schemaAllowsExact(properties[field], null)) return false;
  }
  for (const field of safeMode.absentOrNull ?? []) {
    if (!Object.hasOwn(properties, field)) return false;
  }
  return true;
}
function deriveReviewedLocalDefinition(input) {
  const name = input.declaration.name.trim();
  const semantics = input.declaration.localPlanning;
  if (!name || input.declaration.sideEffect !== "write" || input.declaration.runtimeEffect === "host_only" || !semantics || semantics.destructive || semantics.reversibility !== "reversible" && semantics.reversibility !== "create_only") return null;
  const normalized = normalizedSemantics(semantics);
  if (!structurallyCarriesSafeMode(input.schema, normalized)) return null;
  const variant = safeToken(normalized.safeMode?.id ?? "reversible");
  const nameToken = safeToken(name);
  if (!variant || !nameToken) return null;
  const capabilityRef = `cap:local:${nameToken}:${variant}`;
  const schemaFingerprint = stableJsonDigest(input.schema);
  const registrySemanticsFingerprint = stableJsonDigest({
    version: 1,
    name,
    sideEffect: input.declaration.sideEffect,
    actionTopologyRole: input.declaration.actionTopologyRole ?? "business",
    semantics: normalized,
    localExecution: input.declaration.localExecution ?? null
  });
  const envelopeFingerprint = stableJsonDigest({
    version: 1,
    provenance: AUTHORIZED_LOCAL_REGISTRY_PROVENANCE,
    name,
    carrier: "work_call",
    schemaFingerprint,
    registrySemanticsFingerprint
  });
  const descriptor = Object.freeze({
    id: capabilityRef,
    effect: "local_write",
    purpose: normalized.purpose,
    acceptedInputKinds: ["evidence", normalized.inputKind],
    producedOutputKinds: ["evidence", normalized.outputKind],
    applicableDeliverableKinds: ["evidence", normalized.deliverableKind],
    inputShape: normalized.inputKind,
    outputShape: normalized.outputKind,
    outputKind: normalized.outputKind,
    deliverableKind: normalized.deliverableKind,
    destinationPosture: normalized.destinationPosture,
    evidenceKinds: ["local_commit_receipt"],
    handleRequired: normalized.destinationPosture !== null,
    readbackRequired: false,
    accountScope: REVIEWED_LOCAL_ACCOUNT,
    manifestDigest: stableJsonDigest({
      version: 1,
      provenance: AUTHORIZED_LOCAL_REGISTRY_PROVENANCE,
      capabilityRef,
      envelopeFingerprint
    }),
    advisoryRoles: [...normalized.advisoryRoles]
  });
  return Object.freeze({
    version: 1,
    provenance: AUTHORIZED_LOCAL_REGISTRY_PROVENANCE,
    name,
    carrier: "work_call",
    capabilityRef,
    schemaFingerprint,
    registrySemanticsFingerprint,
    envelopeFingerprint,
    consequence: normalized.consequence,
    reversibility: normalized.reversibility,
    destructive: false,
    accountIdentity: REVIEWED_LOCAL_ACCOUNT,
    safeMode: normalized.safeMode ? Object.freeze({ ...normalized.safeMode }) : null,
    descriptor
  });
}
function currentReviewedLocalSchema(execution) {
  const parametersShape = execution.adapter === "artifact_bundle_v1" ? ARTIFACT_BUNDLE_TOOL_PARAMETERS : WORKSPACE_SET_DATA_TOOL_PARAMETERS;
  const deferredParameters = import_zod4.z.strictObject(normalizeShapeForDeferredJson(parametersShape));
  const parameters = import_zod4.z.toJSONSchema(deferredParameters);
  const schema = relaxJsonSchemaForDeferred(parameters);
  return isRecord(schema) ? schema : null;
}
function observeReviewedLocalTool(operationId) {
  const name = operationId.trim();
  if (!name || name !== operationId) return null;
  const declaration = exactDeclaration(name);
  if (!declaration || !validExecutionContract(declaration.localExecution)) return null;
  const schema = currentReviewedLocalSchema(declaration.localExecution);
  if (!schema) return null;
  const definition = deriveReviewedLocalDefinition({ declaration, schema });
  if (!definition || definition.accountIdentity !== REVIEWED_LOCAL_ACCOUNT) return null;
  const manifestId = `${definition.capabilityRef}:exec:${definition.envelopeFingerprint}`;
  return Object.freeze({
    definition,
    execution: Object.freeze({ ...declaration.localExecution }),
    schema,
    manifestId,
    invokePortId: `port:${manifestId}:${name}`,
    reconcilePortId: `reconcile:port:${manifestId}:${name}`
  });
}
function reviewedLocalCapabilityManifest(observed) {
  const descriptor = observed.definition.descriptor;
  return currentCapabilityManifest({
    version: 1,
    manifestId: observed.manifestId,
    providerKind: "local_registry",
    operationId: observed.definition.name,
    providerIdentity: REVIEWED_LOCAL_PROVIDER_IDENTITY,
    providerVersion: REVIEWED_LOCAL_PROVIDER_VERSION,
    operationVersion: REVIEWED_LOCAL_OPERATION_VERSION,
    definitionFingerprint: observed.definition.envelopeFingerprint,
    effect: "local_write",
    ...descriptor.destinationPosture ? {
      destination: {
        family: observed.definition.consequence,
        posture: descriptor.destinationPosture
      }
    } : {},
    accountId: REVIEWED_LOCAL_ACCOUNT,
    idempotency: { required: true, policy: "key_before_dispatch" },
    reconciliation: { supported: true, policy: "exact_artifact" },
    outputContract: { kind: descriptor.outputKind ?? observed.definition.consequence },
    purpose: descriptor.purpose,
    acceptedInputKinds: [...descriptor.acceptedInputKinds],
    producedOutputKinds: [...descriptor.producedOutputKinds],
    applicableDeliverableKinds: [...descriptor.applicableDeliverableKinds],
    evidenceContract: { kinds: ["local_commit_receipt"], readbackRequired: false },
    provenance: {
      issuer: "host:reviewed-local-registry",
      issuedAt: "1970-01-01T00:00:00.000Z",
      trusted: true
    },
    lifecycle: { state: "current" },
    advisoryRoles: [...descriptor.advisoryRoles ?? []],
    argumentCompiler: { ...REVIEWED_LOCAL_ARGUMENT_COMPILER },
    invokePortId: observed.invokePortId,
    reconcilePortId: observed.reconcilePortId
  });
}
var import_zod4, REVIEWED_LOCAL_PROVIDER_IDENTITY, REVIEWED_LOCAL_PROVIDER_VERSION, REVIEWED_LOCAL_OPERATION_VERSION, REVIEWED_LOCAL_ACCOUNT, REVIEWED_LOCAL_ARGUMENT_COMPILER, AUTHORIZED_LOCAL_REGISTRY_PROVENANCE;
var init_reviewed_local_tool_transport = __esm({
  "src/runtime/harness/reviewed-local-tool-transport.ts"() {
    "use strict";
    import_zod4 = require("zod");
    init_schema_normalizer();
    init_stable_json_digest();
    init_capability_manifest();
    init_tool_registry();
    init_artifact_bundle_contract();
    init_artifact_bundle_core();
    init_workspace_set_data_contract();
    REVIEWED_LOCAL_PROVIDER_IDENTITY = "local_registry";
    REVIEWED_LOCAL_PROVIDER_VERSION = "local-registry-v1";
    REVIEWED_LOCAL_OPERATION_VERSION = "1";
    REVIEWED_LOCAL_ACCOUNT = "local_registry:host";
    REVIEWED_LOCAL_ARGUMENT_COMPILER = Object.freeze({
      id: "compile:reviewed-local-canonical-json:v1",
      version: "1"
    });
    AUTHORIZED_LOCAL_REGISTRY_PROVENANCE = "authorized_local_registry";
  }
});

// src/memory/policy-enforcement.ts
function standingPolicySourceSha256(content) {
  return (0, import_node_crypto4.createHash)("sha256").update(content, "utf8").digest("hex");
}
function canonicalJson(value) {
  if (value === null || typeof value !== "object") return JSON.stringify(value);
  if (Array.isArray(value)) return `[${value.map(canonicalJson).join(",")}]`;
  const object = value;
  return `{${Object.keys(object).sort().map((key) => `${JSON.stringify(key)}:${canonicalJson(object[key])}`).join(",")}}`;
}
function descriptorDigest(body) {
  return (0, import_node_crypto4.createHash)("sha256").update(canonicalJson(body), "utf8").digest("hex");
}
function sealStandingPolicyDescriptor(body) {
  return {
    ...body,
    seal: { algorithm: "sha256", digest: descriptorDigest(body) }
  };
}
function compilePromptStandingPolicyDescriptor(content, policyClass = "standing_preference") {
  return sealStandingPolicyDescriptor({
    schemaVersion: STANDING_POLICY_SCHEMA_VERSION,
    contract: "standing_policy",
    compiler: {
      protocol: STANDING_POLICY_COMPILER_PROTOCOL,
      adapterId: "prompt-context",
      version: 1
    },
    sourceSha256: standingPolicySourceSha256(content),
    deterministic: false,
    policyClass,
    gateways: [],
    bindings: [],
    directives: [],
    capabilityHints: [],
    reason: "Prompt-context policy; not a tool-dispatch constraint."
  });
}
var import_node_crypto4, STANDING_POLICY_SCHEMA_VERSION, STANDING_POLICY_COMPILER_PROTOCOL;
var init_policy_enforcement = __esm({
  "src/memory/policy-enforcement.ts"() {
    "use strict";
    import_node_crypto4 = require("node:crypto");
    STANDING_POLICY_SCHEMA_VERSION = 2;
    STANDING_POLICY_COMPILER_PROTOCOL = "standing-policy-compiler/v1";
  }
});

// src/integrations/composio/standing-policy-compiler.ts
function calendarIntentLabels(content) {
  const withoutIds = content.replace(new RegExp(CONNECTION_RE.source, "gi"), " ");
  const words = withoutIds.toLowerCase().match(/\b[a-z][a-z0-9'-]{2,}\b/g) ?? [];
  return [...new Set(words.filter((word) => !GENERIC_CALENDAR_RULE_TOKENS.has(word)))].slice(0, 64);
}
function baseBody(content) {
  return {
    schemaVersion: STANDING_POLICY_SCHEMA_VERSION,
    contract: "standing_policy",
    compiler: {
      protocol: STANDING_POLICY_COMPILER_PROTOCOL,
      adapterId: COMPOSIO_STANDING_POLICY_ADAPTER_ID,
      version: COMPOSIO_STANDING_POLICY_COMPILER_VERSION
    },
    sourceSha256: standingPolicySourceSha256(content)
  };
}
function promptToolkitBindings(content) {
  const bindings = [];
  const text = content.toLowerCase();
  if (/\boutlook\b/.test(text)) {
    bindings.push({ adapterId: COMPOSIO_STANDING_POLICY_ADAPTER_ID, kind: "toolkit", value: "outlook" });
  }
  if (/\bsalesforce\b/.test(text)) {
    bindings.push({ adapterId: COMPOSIO_STANDING_POLICY_ADAPTER_ID, kind: "toolkit", value: "salesforce" });
  }
  return bindings;
}
function unclassified(content) {
  const bindings = promptToolkitBindings(content);
  return sealStandingPolicyDescriptor({
    ...baseBody(content),
    deterministic: false,
    policyClass: "unclassified",
    gateways: [],
    bindings,
    directives: [],
    capabilityHints: [],
    reason: bindings.length > 0 ? "No deterministic compiler covers this rule; sealed toolkit bindings preserve it as prompt guidance only." : "No deterministic Composio standing-policy compiler covers this natural-language rule."
  });
}
function compileComposioStandingPolicy(content) {
  const text = content.replace(/\s+/g, " ").trim().toLowerCase();
  const email = content.match(EMAIL_RE)?.[0]?.toLowerCase();
  if (text.includes("outlook") && /\b(?:send|sending|mail|email)\b/.test(text) && email) {
    return sealStandingPolicyDescriptor({
      ...baseBody(content),
      deterministic: true,
      policyClass: "verified_sender_identity",
      gateways: ["composio_execute_tool"],
      bindings: [{ adapterId: COMPOSIO_STANDING_POLICY_ADAPTER_ID, kind: "toolkit", value: "outlook" }],
      directives: [{
        kind: "verified_identity",
        selector: {
          adapterId: COMPOSIO_STANDING_POLICY_ADAPTER_ID,
          allTags: ["channel:email", "effect:irreversible-send"]
        },
        preferenceSelector: {
          adapterId: COMPOSIO_STANDING_POLICY_ADAPTER_ID,
          allTags: ["provider:outlook", "effect:reversible-draft"]
        },
        identityKind: "mailbox",
        requiredValue: email,
        verifierCapability: "OUTLOOK_GET_PROFILE",
        reason: `The connected sending identity must verify as ${email}.`,
        violatingField: "sending identity",
        overrideAllowed: true,
        recovery: `Select and verify the connected account for ${email}.`
      }],
      capabilityHints: [],
      reason: "The adapter verifies the connected mailbox before an irreversible email send."
    });
  }
  const connection = content.match(CONNECTION_RE)?.[0];
  if (text.includes("outlook") && text.includes("calendar") && connection) {
    const intentLabels = calendarIntentLabels(content);
    return sealStandingPolicyDescriptor({
      ...baseBody(content),
      deterministic: true,
      policyClass: "connection_route",
      gateways: ["composio_execute_tool"],
      bindings: [{ adapterId: COMPOSIO_STANDING_POLICY_ADAPTER_ID, kind: "toolkit", value: "outlook" }],
      directives: [{
        kind: "route",
        selector: {
          adapterId: COMPOSIO_STANDING_POLICY_ADAPTER_ID,
          allTags: ["provider:outlook", "domain:calendar", "effect:read"]
        },
        bindingKind: "connected-account",
        bindingValue: connection,
        intentLabels,
        reason: `The matching read must use the sealed connected-account route ${connection}.`,
        violatingField: "connected account route",
        overrideAllowed: false,
        recovery: "Use an account-addressable provider route that can honor the sealed connection."
      }],
      capabilityHints: intentLabels.length === 0 ? [] : [{
        adapterId: COMPOSIO_STANDING_POLICY_ADAPTER_ID,
        intentLabels,
        requiresTemporalCue: true,
        allowedServerSlugs: ["outlook", "microsoft_outlook", "microsoft"],
        toolPatterns: ["outlook", "email", "mail", "calendar", "event"],
        priorityKeywords: ["list", "search", "calendar"],
        maxTools: 8
      }],
      reason: "The adapter binds a labeled calendar read to a sealed connected-account id."
    });
  }
  if (text.includes("salesforce") && text.includes("composio") && /\bsf\s+cli\b/.test(text) && /\b(?:never|do not|don't|expired|dead)\b/.test(text)) {
    return sealStandingPolicyDescriptor({
      ...baseBody(content),
      deterministic: true,
      policyClass: "route_denial",
      gateways: ["composio_execute_tool"],
      bindings: [{ adapterId: COMPOSIO_STANDING_POLICY_ADAPTER_ID, kind: "toolkit", value: "salesforce" }],
      directives: [{
        kind: "deny",
        selector: {
          adapterId: COMPOSIO_STANDING_POLICY_ADAPTER_ID,
          allTags: ["toolkit:salesforce"]
        },
        reason: "This provider gateway route is forbidden by the sealed standing policy; use the authenticated local sf CLI.",
        violatingField: "tool route",
        overrideAllowed: false,
        recovery: "Use the authenticated local sf CLI route named by the standing policy."
      }],
      capabilityHints: [],
      reason: "The adapter denies the provider gateway route and preserves the required local route."
    });
  }
  if (text.includes("salesforce") && text.includes("org") && (text.includes("sandbox") || text.includes("production_disabled"))) {
    const sandboxOnly = text.includes("sandbox");
    return sealStandingPolicyDescriptor({
      ...baseBody(content),
      deterministic: true,
      policyClass: "environment_constraint",
      gateways: ["composio_execute_tool"],
      bindings: [{ adapterId: COMPOSIO_STANDING_POLICY_ADAPTER_ID, kind: "toolkit", value: "salesforce" }],
      directives: [{
        kind: "field_constraint",
        selector: {
          adapterId: COMPOSIO_STANDING_POLICY_ADAPTER_ID,
          allTags: ["toolkit:salesforce"]
        },
        field: "target-environment",
        operator: sandboxOnly ? "require_contains" : "forbid_contains",
        values: [sandboxOnly ? "sandbox" : "production"],
        requirePresent: false,
        reason: sandboxOnly ? "The selected organization violates the sealed sandbox-only policy." : "The selected organization violates the sealed production-disabled policy.",
        violatingField: "target environment",
        overrideAllowed: false,
        recovery: "Select an environment allowed by the standing policy."
      }],
      capabilityHints: [],
      reason: "The adapter compares the normalized target environment with a sealed value constraint."
    });
  }
  if (text.includes("production") && text.includes("sandbox") && /\b(?:never|do not|don't|only|requires?)\b/.test(text)) {
    return sealStandingPolicyDescriptor({
      ...baseBody(content),
      deterministic: true,
      policyClass: "environment_constraint",
      gateways: ["composio_execute_tool"],
      bindings: [],
      directives: [{
        kind: "field_constraint",
        selector: {
          adapterId: COMPOSIO_STANDING_POLICY_ADAPTER_ID,
          allTags: ["effect:external-write"]
        },
        field: "target-environment",
        operator: "forbid_contains",
        values: ["production"],
        requirePresent: false,
        reason: "The normalized target is production, which the sealed standing policy forbids.",
        violatingField: "target environment",
        overrideAllowed: false,
        recovery: "Select a sandbox or staging target."
      }],
      capabilityHints: [],
      reason: "The adapter rejects a normalized production target for an external write."
    });
  }
  return unclassified(content);
}
function classifyConstraintEnforcementV1(content) {
  const text = content.replace(/\s+/g, " ").trim().toLowerCase();
  if (text.includes("outlook") && /\b(?:send|sending|mail|email)\b/.test(text) && EMAIL_RE.test(content)) {
    return { schemaVersion: 1, family: "outlook_sender", deterministic: true, tools: ["composio_execute_tool"], reason: "The dispatch gate verifies the connected Outlook mailbox before an irreversible send." };
  }
  if (text.includes("outlook") && text.includes("calendar") && CONNECTION_RE.test(content)) {
    return { schemaVersion: 1, family: "outlook_calendar_route", deterministic: true, tools: ["composio_execute_tool"], reason: "The dispatch router binds a named calendar lookup to the persisted connection id." };
  }
  if (text.includes("salesforce") && text.includes("composio") && /\bsf\s+cli\b/.test(text) && /\b(?:never|do not|don't|expired|dead)\b/.test(text)) {
    return { schemaVersion: 1, family: "salesforce_cli_only", deterministic: true, tools: ["composio_execute_tool"], reason: "Every Composio Salesforce dispatch is blocked so the required local sf CLI path is the only route." };
  }
  if (text.includes("salesforce") && text.includes("org") && (text.includes("sandbox") || text.includes("production_disabled"))) {
    return { schemaVersion: 1, family: "salesforce_environment", deterministic: true, tools: ["composio_execute_tool"], reason: "The dispatch gate compares the requested Salesforce organization with the allowed environment." };
  }
  if (text.includes("production") && text.includes("sandbox") && /\b(?:never|do not|don't|only|requires?)\b/.test(text)) {
    return { schemaVersion: 1, family: "production_environment", deterministic: true, tools: ["composio_execute_tool"], reason: "The dispatch gate rejects concrete production environment or table arguments." };
  }
  return { schemaVersion: 1, family: "unclassified", deterministic: false, tools: [], reason: "No deterministic dispatch compiler currently covers this natural-language rule." };
}
var COMPOSIO_STANDING_POLICY_ADAPTER_ID, COMPOSIO_STANDING_POLICY_COMPILER_VERSION, EMAIL_RE, CONNECTION_RE, GENERIC_CALENDAR_RULE_TOKENS;
var init_standing_policy_compiler = __esm({
  "src/integrations/composio/standing-policy-compiler.ts"() {
    "use strict";
    init_policy_enforcement();
    COMPOSIO_STANDING_POLICY_ADAPTER_ID = "composio";
    COMPOSIO_STANDING_POLICY_COMPILER_VERSION = 1;
    EMAIL_RE = /[\w.+-]+@[\w.-]+\.[a-z]{2,}/i;
    CONNECTION_RE = /\bca_[A-Za-z0-9_-]+\b/;
    GENERIC_CALENDAR_RULE_TOKENS = /* @__PURE__ */ new Set([
      "outlook",
      "calendar",
      "calendars",
      "connection",
      "connections",
      "connected",
      "account",
      "accounts",
      "lookup",
      "lookups",
      "event",
      "events",
      "meeting",
      "meetings",
      "schedule",
      "scheduling",
      "appointment",
      "appointments",
      "read",
      "reads",
      "reading",
      "check",
      "checks",
      "checking",
      "view",
      "get",
      "list",
      "use",
      "using",
      "used",
      "for",
      "the",
      "and",
      "this",
      "that",
      "with",
      "from",
      "only",
      "other",
      "others",
      "active",
      "returned",
      "return",
      "returns",
      "not",
      "none",
      "when",
      "all",
      "any",
      "was",
      "were",
      "been",
      "must",
      "should",
      "always",
      "never",
      "please",
      "its",
      "instead",
      "because",
      "via",
      "has",
      "have",
      "had",
      "one",
      "two",
      "both",
      "known"
    ]);
  }
});

// src/memory/db.ts
function ensureStateDir() {
  if (!(0, import_node_fs2.existsSync)(STATE_DIR)) {
    (0, import_node_fs2.mkdirSync)(STATE_DIR, { recursive: true });
  }
}
function readMemorySchemaVersion(db) {
  const hasVersionTable = db.prepare(
    "SELECT 1 FROM sqlite_master WHERE type = 'table' AND name = 'schema_version'"
  ).get();
  if (!hasVersionTable) return 0;
  return db.prepare("SELECT MAX(version) AS version FROM schema_version").get().version ?? 0;
}
function createPreMigrationMemorySnapshot(db, fromVersion, toVersion) {
  if (fromVersion <= 0 || fromVersion >= toVersion) {
    throw new Error(`invalid pre-migration snapshot boundary ${fromVersion}->${toVersion}`);
  }
  if (!(0, import_node_fs2.existsSync)(MEMORY_PRE_MIGRATION_BACKUP_DIR)) {
    (0, import_node_fs2.mkdirSync)(MEMORY_PRE_MIGRATION_BACKUP_DIR, { recursive: true });
  }
  const stamp = (/* @__PURE__ */ new Date()).toISOString().replace(/[:.]/g, "-");
  preMigrationSnapshotOrdinal += 1;
  const filename = [
    "memory",
    `v${fromVersion}-to-v${toVersion}`,
    stamp,
    `p${process.pid}`,
    String(preMigrationSnapshotOrdinal)
  ].join("-") + ".db";
  const snapshotPath = import_node_path5.default.join(MEMORY_PRE_MIGRATION_BACKUP_DIR, filename);
  try {
    db.exec(`VACUUM INTO '${snapshotPath.replace(/'/g, "''")}'`);
    const bytes = (0, import_node_fs2.statSync)(snapshotPath).size;
    if (bytes <= 0) throw new Error("snapshot is empty");
    const probe = new import_better_sqlite3.default(snapshotPath, { readonly: true });
    try {
      const integrity = probe.pragma("integrity_check");
      if (integrity.length !== 1 || integrity[0]?.integrity_check !== "ok") {
        throw new Error("snapshot failed integrity_check");
      }
      if (readMemorySchemaVersion(probe) !== fromVersion) {
        throw new Error("snapshot schema boundary does not match source");
      }
    } finally {
      probe.close();
    }
    return snapshotPath;
  } catch (error) {
    try {
      if ((0, import_node_fs2.existsSync)(snapshotPath)) (0, import_node_fs2.unlinkSync)(snapshotPath);
    } catch {
    }
    const detail = error instanceof Error ? error.message : String(error);
    throw new Error(`Refusing memory migration ${fromVersion}->${toVersion}: pre-migration snapshot failed (${detail})`);
  }
}
function migrateMemoryDatabaseHandle(db, options = {}) {
  const targetVersion = options.targetVersion ?? MEMORY_SCHEMA_VERSION;
  if (!Number.isInteger(targetVersion) || targetVersion < 0 || targetVersion > MEMORY_SCHEMA_VERSION) {
    throw new Error(`Invalid memory migration target ${targetVersion}; expected 0-${MEMORY_SCHEMA_VERSION}.`);
  }
  db.exec(`
    CREATE TABLE IF NOT EXISTS schema_version (
      version    INTEGER PRIMARY KEY,
      applied_at TEXT NOT NULL
    );
  `);
  const current = db.prepare("SELECT MAX(version) AS v FROM schema_version").get().v ?? 0;
  if (current > targetVersion) {
    throw new Error(
      `Memory schema ${current} is newer than migration target ${targetVersion}; refusing an implicit downgrade.`
    );
  }
  const apply = db.prepare("INSERT OR IGNORE INTO schema_version (version, applied_at) VALUES (?, ?)");
  for (const migration of MIGRATIONS) {
    if (migration.version <= current) continue;
    if (migration.version > targetVersion) break;
    if ("run" in migration) {
      migration.run(db);
      apply.run(migration.version, (/* @__PURE__ */ new Date()).toISOString());
      continue;
    }
    const tx = db.transaction(() => {
      db.exec(migration.sql);
      apply.run(migration.version, (/* @__PURE__ */ new Date()).toISOString());
    });
    tx();
  }
}
function openMemoryDb() {
  if (cached && !cached.open) cached = null;
  if (cached) return cached;
  if (process.env.NODE_TEST_CONTEXT && import_node_path5.default.resolve(BASE_DIR) === import_node_path5.default.resolve(REAL_DEFAULT_HOME)) {
    throw new Error(
      "Refusing to open the real Clementine memory database from a test process; set CLEMENTINE_HOME to a temp directory before importing memory modules."
    );
  }
  ensureStateDir();
  const db = new import_better_sqlite3.default(MEMORY_DB_PATH);
  db.pragma("journal_mode = WAL");
  db.pragma("synchronous = NORMAL");
  db.pragma("foreign_keys = ON");
  db.pragma("busy_timeout = 5000");
  db.pragma("wal_autocheckpoint = 256");
  try {
    const schemaBeforeOpen = readMemorySchemaVersion(db);
    if (schemaBeforeOpen > 0 && schemaBeforeOpen < MEMORY_SCHEMA_VERSION) {
      createPreMigrationMemorySnapshot(db, schemaBeforeOpen, MEMORY_SCHEMA_VERSION);
    }
    migrateMemoryDatabaseHandle(db);
  } catch (error) {
    db.close();
    throw error;
  }
  cached = db;
  return db;
}
function ensureMemoryForeignKeysEnabled(db) {
  const enabled = () => Number(db.pragma("foreign_keys", { simple: true }) ?? 0) === 1;
  if (!enabled()) db.pragma("foreign_keys = ON");
  if (!enabled()) {
    throw new Error(
      "Memory hard delete refused: SQLite foreign-key enforcement is disabled. Enable PRAGMA foreign_keys before opening the transaction."
    );
  }
}
function hardDeleteConsolidatedFacts(db, factIds) {
  const ids = [...new Set(factIds.filter((id) => Number.isSafeInteger(id) && id > 0))];
  if (ids.length === 0) return 0;
  ensureMemoryForeignKeysEnabled(db);
  const remove = () => {
    let deleted = 0;
    for (let offset = 0; offset < ids.length; offset += 400) {
      const chunk = ids.slice(offset, offset + 400);
      const placeholders = chunk.map(() => "?").join(",");
      db.prepare(`
        UPDATE consolidated_facts
        SET superseded_by_fact_id = NULL
        WHERE superseded_by_fact_id IN (${placeholders})
      `).run(...chunk);
      deleted += Number(db.prepare(
        `DELETE FROM consolidated_facts WHERE id IN (${placeholders})`
      ).run(...chunk).changes ?? 0);
    }
    return deleted;
  };
  return db.inTransaction ? remove() : db.transaction(remove)();
}
var import_better_sqlite3, import_node_fs2, import_node_path5, import_node_os2, REAL_DEFAULT_HOME, STATE_DIR, MEMORY_DB_PATH, MEMORY_BACKUP_DIR, MEMORY_BACKUP_COORDINATOR_PATH, MEMORY_PRE_MIGRATION_BACKUP_DIR, cached, MIGRATIONS, MEMORY_SCHEMA_VERSION, preMigrationSnapshotOrdinal, MEMORY_BACKUP_MIN_SAFETY_MARGIN_BYTES;
var init_db = __esm({
  "src/memory/db.ts"() {
    "use strict";
    import_better_sqlite3 = __toESM(require("better-sqlite3"), 1);
    import_node_fs2 = require("node:fs");
    import_node_path5 = __toESM(require("node:path"), 1);
    import_node_os2 = __toESM(require("node:os"), 1);
    init_config();
    init_standing_policy_compiler();
    init_policy_enforcement();
    REAL_DEFAULT_HOME = import_node_path5.default.join(import_node_os2.default.homedir(), ".clementine-next");
    STATE_DIR = import_node_path5.default.join(BASE_DIR, "state");
    MEMORY_DB_PATH = import_node_path5.default.join(STATE_DIR, "memory.db");
    MEMORY_BACKUP_DIR = import_node_path5.default.join(STATE_DIR, "backups");
    MEMORY_BACKUP_COORDINATOR_PATH = import_node_path5.default.join(MEMORY_BACKUP_DIR, ".coordinator.db");
    MEMORY_PRE_MIGRATION_BACKUP_DIR = import_node_path5.default.join(STATE_DIR, "pre-migration-backups");
    cached = null;
    MIGRATIONS = [
      {
        version: 1,
        sql: `
      CREATE TABLE IF NOT EXISTS vault_chunks (
        id            INTEGER PRIMARY KEY AUTOINCREMENT,
        path          TEXT NOT NULL,
        chunk_index   INTEGER NOT NULL,
        content       TEXT NOT NULL,
        title         TEXT,
        mtime         INTEGER NOT NULL,
        byte_size     INTEGER NOT NULL,
        content_hash  TEXT NOT NULL,
        UNIQUE(path, chunk_index)
      );

      CREATE INDEX IF NOT EXISTS idx_vault_chunks_path  ON vault_chunks(path);
      CREATE INDEX IF NOT EXISTS idx_vault_chunks_mtime ON vault_chunks(mtime DESC);

      -- FTS5 mirror, kept in sync via triggers. content='vault_chunks' is
      -- a contentless-style binding that lets us MATCH while joining back
      -- to the row by rowid = vault_chunks.id.
      CREATE VIRTUAL TABLE IF NOT EXISTS vault_chunks_fts USING fts5(
        content,
        title,
        path UNINDEXED,
        content='vault_chunks',
        content_rowid='id',
        tokenize='porter unicode61'
      );

      CREATE TRIGGER IF NOT EXISTS vault_chunks_ai AFTER INSERT ON vault_chunks BEGIN
        INSERT INTO vault_chunks_fts(rowid, content, title, path)
        VALUES (new.id, new.content, COALESCE(new.title, ''), new.path);
      END;

      CREATE TRIGGER IF NOT EXISTS vault_chunks_ad AFTER DELETE ON vault_chunks BEGIN
        INSERT INTO vault_chunks_fts(vault_chunks_fts, rowid, content, title, path)
        VALUES ('delete', old.id, old.content, COALESCE(old.title, ''), old.path);
      END;

      CREATE TRIGGER IF NOT EXISTS vault_chunks_au AFTER UPDATE ON vault_chunks BEGIN
        INSERT INTO vault_chunks_fts(vault_chunks_fts, rowid, content, title, path)
        VALUES ('delete', old.id, old.content, COALESCE(old.title, ''), old.path);
        INSERT INTO vault_chunks_fts(rowid, content, title, path)
        VALUES (new.id, new.content, COALESCE(new.title, ''), new.path);
      END;

      CREATE TABLE IF NOT EXISTS embeddings (
        chunk_id    INTEGER PRIMARY KEY REFERENCES vault_chunks(id) ON DELETE CASCADE,
        model       TEXT NOT NULL,
        dim         INTEGER NOT NULL,
        vector      BLOB NOT NULL,
        created_at  TEXT NOT NULL
      );

      CREATE TABLE IF NOT EXISTS consolidated_facts (
        id                INTEGER PRIMARY KEY AUTOINCREMENT,
        kind              TEXT NOT NULL CHECK (kind IN ('user','project','feedback','reference','constraint')),
        content           TEXT NOT NULL,
        content_hash      TEXT NOT NULL UNIQUE,
        source_session_id TEXT,
        source_path       TEXT,
        score             REAL NOT NULL DEFAULT 1.0,
        active            INTEGER NOT NULL DEFAULT 1,
        created_at        TEXT NOT NULL,
        updated_at        TEXT NOT NULL
      );

      CREATE INDEX IF NOT EXISTS idx_facts_active ON consolidated_facts(active, kind, score DESC);
    `
      },
      {
        version: 2,
        sql: `
      -- Inbound message inbox. Channels (Discord, dashboard, webhook)
      -- write 'received' before handing off to the gateway and mark
      -- 'replied' once delivery confirms. PRIMARY KEY enforces
      -- idempotency: redelivery of the same provider message id is a
      -- no-op. On daemon restart, anything not in ('replied','dropped')
      -- can be replayed without double-billing the model.
      CREATE TABLE IF NOT EXISTS inbound_messages (
        channel            TEXT NOT NULL,
        source_message_id  TEXT NOT NULL,
        session_id         TEXT,
        user_id            TEXT,
        run_id             TEXT,
        status             TEXT NOT NULL CHECK (status IN ('received','claimed','replied','failed','dropped')),
        attempts           INTEGER NOT NULL DEFAULT 0,
        error              TEXT,
        received_at        TEXT NOT NULL,
        claimed_at         TEXT,
        completed_at       TEXT,
        PRIMARY KEY (channel, source_message_id)
      );

      CREATE INDEX IF NOT EXISTS idx_inbound_status ON inbound_messages(status, received_at);
    `
      },
      {
        // v3 — Phase 1 brain architecture (Stanford Generative Agents
        // reflection loop + Tulving's memory taxonomy).
        //
        // Three additions:
        //  (a) Extend consolidated_facts with derivation provenance so a
        //      fact synthesized from a tool return links back to its
        //      source call_id. Enables the "go look at the original"
        //      pointer-first model — see [[project_brain_architecture]].
        //  (b) New `entities` table — first-class registry of people,
        //      companies, projects the brain knows about. Aliases enable
        //      cross-source matching ("Marlow" in Outlook = marlow@acme.example).
        //  (c) New `episodic_pointers` table — short labels that point at a
        //      specific tool_outputs row (session_id + call_id), so the
        //      agent can refer to "the pricing convo" and recall fetches
        //      the actual content via recall_tool_result.
        version: 3,
        sql: `
      ALTER TABLE consolidated_facts ADD COLUMN derived_from_session_id TEXT;
      ALTER TABLE consolidated_facts ADD COLUMN derived_from_call_id   TEXT;
      ALTER TABLE consolidated_facts ADD COLUMN derived_from_tool      TEXT;
      ALTER TABLE consolidated_facts ADD COLUMN trust_level             REAL;
      ALTER TABLE consolidated_facts ADD COLUMN extracted_at            TEXT;

      CREATE INDEX IF NOT EXISTS idx_facts_extracted_at
        ON consolidated_facts(extracted_at DESC) WHERE extracted_at IS NOT NULL;

      CREATE TABLE IF NOT EXISTS entities (
        id                INTEGER PRIMARY KEY AUTOINCREMENT,
        entity_type       TEXT NOT NULL CHECK (entity_type IN ('person','company','project','place','thing')),
        canonical_name    TEXT NOT NULL,
        canonical_name_lc TEXT NOT NULL,
        aliases_json      TEXT NOT NULL DEFAULT '[]',
        first_seen_at     TEXT NOT NULL,
        last_seen_at      TEXT NOT NULL,
        mention_count     INTEGER NOT NULL DEFAULT 1,
        UNIQUE(entity_type, canonical_name_lc)
      );

      CREATE INDEX IF NOT EXISTS idx_entities_last_seen
        ON entities(last_seen_at DESC);
      CREATE INDEX IF NOT EXISTS idx_entities_type_name
        ON entities(entity_type, canonical_name_lc);

      CREATE TABLE IF NOT EXISTS episodic_pointers (
        id          INTEGER PRIMARY KEY AUTOINCREMENT,
        session_id  TEXT NOT NULL,
        call_id     TEXT NOT NULL,
        label       TEXT NOT NULL,
        tool        TEXT,
        source_uri  TEXT,
        created_at  TEXT NOT NULL
      );

      CREATE INDEX IF NOT EXISTS idx_episodic_session
        ON episodic_pointers(session_id, created_at DESC);
      CREATE INDEX IF NOT EXISTS idx_episodic_call
        ON episodic_pointers(call_id);
    `
      },
      {
        // v4 — Brain architecture Phase 1 expansion, closing the Stanford
        // Generative Agents research gaps documented in
        // [[project_brain_phase1_gaps]]:
        //   - `importance` (1.0–10.0): Park et al §4.1's poignancy score.
        //     Used by reflection.ts to gate the trigger via sum-threshold
        //     (Stanford uses 150) AND as a retrieval weight in memory_search.
        //   - `last_accessed_at`: Stanford §4.1's "since last retrieval"
        //     anchor for the recency component (exp decay 0.995/hr). Touched
        //     on every memory_search/recall hit.
        version: 4,
        sql: `
      ALTER TABLE consolidated_facts ADD COLUMN importance REAL;
      ALTER TABLE consolidated_facts ADD COLUMN last_accessed_at TEXT;

      CREATE INDEX IF NOT EXISTS idx_facts_importance
        ON consolidated_facts(importance DESC) WHERE importance IS NOT NULL;
      CREATE INDEX IF NOT EXISTS idx_facts_last_accessed
        ON consolidated_facts(last_accessed_at DESC) WHERE last_accessed_at IS NOT NULL;
    `
      },
      {
        // v5 — Brain architecture Phase 2 (recursive reflection / Stanford
        // trees, Park et al §4.2). Lets a nightly job synthesize higher-order
        // patterns from accumulated atomic facts and record them in the same
        // table with a depth marker + provenance back to the source facts.
        //   derivation_depth      — 0 for atomic facts, 1 for first-level
        //                           recursive reflections, capped at 2.
        //   derived_from_fact_ids — JSON array of contributing fact ids; null
        //                           when depth=0.
        version: 5,
        sql: `
      ALTER TABLE consolidated_facts ADD COLUMN derivation_depth INTEGER NOT NULL DEFAULT 0;
      ALTER TABLE consolidated_facts ADD COLUMN derived_from_fact_ids TEXT;

      CREATE INDEX IF NOT EXISTS idx_facts_derivation_depth
        ON consolidated_facts(derivation_depth, created_at DESC);
    `
      },
      {
        // v6 — Current Focus: an attention pointer separate from long-term
        // goals (vault/goals/*.json) and from atomic facts. Solves the
        // "every Discord message starts a new session and loses the thread"
        // problem (2026-05-23). Single-active invariant enforced by a
        // partial unique index so two parallel writes can't both be active.
        // Per-user scoping deferred until multi-owner deployments exist;
        // this Clementine install is single-owner.
        version: 6,
        sql: `
      CREATE TABLE IF NOT EXISTS current_focus (
        id                  INTEGER PRIMARY KEY AUTOINCREMENT,
        resource_ref        TEXT NOT NULL,
        title               TEXT NOT NULL,
        summary             TEXT NOT NULL,
        status              TEXT NOT NULL CHECK (status IN ('active','paused','completed','abandoned')),
        resource_kind       TEXT,
        related_session_id  TEXT,
        related_goal_id     TEXT,
        created_at          TEXT NOT NULL,
        last_touched_at     TEXT NOT NULL,
        confirm_after       TEXT NOT NULL,
        parked_at           TEXT,
        parked_reason       TEXT,
        metadata_json       TEXT NOT NULL DEFAULT '{}'
      );

      CREATE INDEX IF NOT EXISTS idx_focus_status
        ON current_focus(status, last_touched_at DESC);

      -- DB-level enforcement: at most one row may be status='active'.
      -- Switching focus must park the current active first.
      CREATE UNIQUE INDEX IF NOT EXISTS idx_focus_one_active
        ON current_focus(status) WHERE status = 'active';
    `
      },
      {
        // v7 — Fact embeddings. The `embeddings` table (v1) is keyed to
        // vault_chunks only, so consolidated_facts had no semantic search:
        // the reflection conflict-resolver's candidate retrieval
        // (findSimilarFacts) fell back to LIKE-token matching and missed
        // paraphrased contradictions ("prefers Tuesday" vs "Wednesdays work
        // best"). This table gives facts the same Float32-BLOB treatment as
        // chunks. `content_hash` lets the backfill detect a fact whose
        // content changed (updateFact rewrites the hash) and re-embed it.
        // FK-cascaded so a hard fact delete drops its embedding for free.
        version: 7,
        sql: `
      CREATE TABLE IF NOT EXISTS fact_embeddings (
        fact_id      INTEGER PRIMARY KEY REFERENCES consolidated_facts(id) ON DELETE CASCADE,
        model        TEXT NOT NULL,
        dim          INTEGER NOT NULL,
        vector       BLOB NOT NULL,
        content_hash TEXT NOT NULL,
        created_at   TEXT NOT NULL
      );
    `
      },
      {
        // v8 — Pinned standing instructions. A pinned fact is ALWAYS injected
        // into the prompt, exempt from the top-N recency/relevance cap, so a
        // durable rule ("never email clients on Fridays") can't silently age
        // out of context as the fact pool grows. Additive column, default 0.
        version: 8,
        sql: `
      ALTER TABLE consolidated_facts ADD COLUMN pinned INTEGER NOT NULL DEFAULT 0;

      CREATE INDEX IF NOT EXISTS idx_facts_pinned
        ON consolidated_facts(pinned, active);
    `
      },
      {
        // v9 — Source app provenance (connectors-as-authoritative-writers).
        // Friendly name of the app a derived fact came from (e.g. "Salesforce",
        // "Outlook / Microsoft 365") when the fact was derived from a system of
        // record, so the UI/audit can show WHERE a fact originated and the
        // resolver can prefer ground truth. NULL for user-stated + generic
        // derived facts. The trust prior lives in the existing trust_level
        // column; this column is provenance only.
        version: 9,
        sql: `
      ALTER TABLE consolidated_facts ADD COLUMN source_app TEXT;
    `
      },
      {
        // v10 — Source-map / landscape memory. A POINTER-FIRST navigational
        // index of WHERE the user's data lives (Drive folders, Airtable bases,
        // CRM objects, …) and WHEN to use it — never the content itself. One row
        // per distinct resource (UNIQUE(app, ref)); touches bump mention_count +
        // last_seen so the map stays bounded. See docs/source-map-landscape-memory.md.
        version: 10,
        sql: `
      CREATE TABLE IF NOT EXISTS resource_pointers (
        id            INTEGER PRIMARY KEY AUTOINCREMENT,
        app           TEXT NOT NULL,
        kind          TEXT NOT NULL,
        ref           TEXT NOT NULL,
        name          TEXT NOT NULL,
        whats_here    TEXT,
        when_to_use   TEXT,
        parent_ref    TEXT,
        trust         REAL,
        source        TEXT NOT NULL DEFAULT 'reactive',
        first_seen_at TEXT NOT NULL,
        last_seen_at  TEXT NOT NULL,
        mention_count INTEGER NOT NULL DEFAULT 1,
        UNIQUE(app, ref)
      );

      CREATE INDEX IF NOT EXISTS idx_resource_last_seen
        ON resource_pointers(last_seen_at DESC);
      CREATE INDEX IF NOT EXISTS idx_resource_app
        ON resource_pointers(app, last_seen_at DESC);
    `
      },
      {
        // v11 — access-frequency reinforcement (Stanford "Generative Agents" §4:
        // retrieval frequency feeds the importance/recency loop). access_count
        // increments every time a fact is surfaced (touchFactAccess); the recall
        // score folds in log(1+access_count) so a repeatedly-useful fact ranks
        // higher AND resists decay — memory compounds and sharpens with use instead
        // of treating a fact recalled 50x the same as one never recalled.
        version: 11,
        sql: `
      ALTER TABLE consolidated_facts ADD COLUMN access_count INTEGER NOT NULL DEFAULT 0;
    `
      },
      {
        // v12 — widen the kind CHECK to admit 'constraint'. The 'constraint' kind
        // was added to the TS types (2026-06-10) but the CHECK was never migrated,
        // so every INSERT of a constraint fact threw and listConstraints() stayed
        // empty — which is why the dispatch-time constraint gate never enforced
        // anything (2026-06-11 wrong-mailbox incident). SQLite can't ALTER a
        // CHECK, so this is the documented full-rebuild procedure. It must run
        // with foreign_keys OFF: fact_embeddings references this table with
        // ON DELETE CASCADE, and a plain DROP would cascade-wipe every embedding.
        version: 12,
        run: (db) => {
          const ddl = db.prepare(
            `SELECT sql FROM sqlite_master WHERE type='table' AND name='consolidated_facts'`
          ).get()?.sql ?? "";
          if (ddl.includes("'constraint'")) return;
          db.pragma("foreign_keys = OFF");
          try {
            db.transaction(() => {
              db.exec(`
            CREATE TABLE consolidated_facts_new (
              id                INTEGER PRIMARY KEY AUTOINCREMENT,
              kind              TEXT NOT NULL CHECK (kind IN ('user','project','feedback','reference','constraint')),
              content           TEXT NOT NULL,
              content_hash      TEXT NOT NULL UNIQUE,
              source_session_id TEXT,
              source_path       TEXT,
              score             REAL NOT NULL DEFAULT 1.0,
              active            INTEGER NOT NULL DEFAULT 1,
              created_at        TEXT NOT NULL,
              updated_at        TEXT NOT NULL,
              derived_from_session_id TEXT,
              derived_from_call_id    TEXT,
              derived_from_tool       TEXT,
              trust_level             REAL,
              extracted_at            TEXT,
              importance              REAL,
              last_accessed_at        TEXT,
              derivation_depth        INTEGER NOT NULL DEFAULT 0,
              derived_from_fact_ids   TEXT,
              pinned                  INTEGER NOT NULL DEFAULT 0,
              source_app              TEXT,
              access_count            INTEGER NOT NULL DEFAULT 0
            );

            INSERT INTO consolidated_facts_new (
              id, kind, content, content_hash, source_session_id, source_path,
              score, active, created_at, updated_at, derived_from_session_id,
              derived_from_call_id, derived_from_tool, trust_level, extracted_at,
              importance, last_accessed_at, derivation_depth, derived_from_fact_ids,
              pinned, source_app, access_count
            )
            SELECT
              id, kind, content, content_hash, source_session_id, source_path,
              score, active, created_at, updated_at, derived_from_session_id,
              derived_from_call_id, derived_from_tool, trust_level, extracted_at,
              importance, last_accessed_at, derivation_depth, derived_from_fact_ids,
              pinned, source_app, access_count
            FROM consolidated_facts;

            DROP TABLE consolidated_facts;
            ALTER TABLE consolidated_facts_new RENAME TO consolidated_facts;

            CREATE INDEX idx_facts_active ON consolidated_facts(active, kind, score DESC);
            CREATE INDEX idx_facts_extracted_at
              ON consolidated_facts(extracted_at DESC) WHERE extracted_at IS NOT NULL;
            CREATE INDEX idx_facts_importance
              ON consolidated_facts(importance DESC) WHERE importance IS NOT NULL;
            CREATE INDEX idx_facts_last_accessed
              ON consolidated_facts(last_accessed_at DESC) WHERE last_accessed_at IS NOT NULL;
            CREATE INDEX idx_facts_derivation_depth
              ON consolidated_facts(derivation_depth, created_at DESC);
            CREATE INDEX idx_facts_pinned
              ON consolidated_facts(pinned, active);
          `);
              const orphans = db.pragma("foreign_key_check(fact_embeddings)");
              if (orphans.length > 0) {
                throw new Error(`consolidated_facts rebuild would orphan ${orphans.length} fact_embeddings rows`);
              }
            })();
          } finally {
            db.pragma("foreign_keys = ON");
          }
        }
      },
      {
        // v13 — STORED relationship layer (WS2). Until now EVERY cross-type edge
        // (fact↔entity, fact↔resource, entity↔entity) was either absent or
        // re-derived at render time by substring matching in the graph UI — so the
        // "connected knowledge graph" was an illusion produced only at the
        // visualization layer, invisible to recall (you couldn't traverse "what do
        // I know about entity X"). These three join tables persist the relationships
        // so the graph shows stored truth and recall can traverse. All FK-cascaded
        // so a hard fact/entity delete drops its links for free. Additive — the
        // legacy fact-only paths are untouched when the tables are empty.
        version: 13,
        sql: `
      CREATE TABLE IF NOT EXISTS fact_entities (
        fact_id    INTEGER NOT NULL REFERENCES consolidated_facts(id) ON DELETE CASCADE,
        entity_id  INTEGER NOT NULL REFERENCES entities(id) ON DELETE CASCADE,
        created_at TEXT NOT NULL,
        PRIMARY KEY (fact_id, entity_id)
      );
      CREATE INDEX IF NOT EXISTS idx_fact_entities_entity ON fact_entities(entity_id);

      CREATE TABLE IF NOT EXISTS fact_resources (
        fact_id     INTEGER NOT NULL REFERENCES consolidated_facts(id) ON DELETE CASCADE,
        resource_id INTEGER NOT NULL REFERENCES resource_pointers(id) ON DELETE CASCADE,
        created_at  TEXT NOT NULL,
        PRIMARY KEY (fact_id, resource_id)
      );
      CREATE INDEX IF NOT EXISTS idx_fact_resources_resource ON fact_resources(resource_id);

      -- Subject-predicate-object edges between entities ("Dana" -is CFO at- "Acme").
      -- Reinforced by recurrence, aged by last_seen so a stale relation can decay.
      CREATE TABLE IF NOT EXISTS entity_edges (
        subject_id       INTEGER NOT NULL REFERENCES entities(id) ON DELETE CASCADE,
        predicate        TEXT NOT NULL,
        object_id        INTEGER NOT NULL REFERENCES entities(id) ON DELETE CASCADE,
        recurrence_count INTEGER NOT NULL DEFAULT 1,
        first_seen_at    TEXT NOT NULL,
        last_seen_at     TEXT NOT NULL,
        PRIMARY KEY (subject_id, predicate, object_id)
      );
      CREATE INDEX IF NOT EXISTS idx_entity_edges_subject ON entity_edges(subject_id);
      CREATE INDEX IF NOT EXISTS idx_entity_edges_object ON entity_edges(object_id);
    `
      },
      {
        // v14 — durable reflection threshold buffer. The reflection importance gate
        // used to keep only an in-memory sum; sub-threshold extracted facts were
        // discarded, and a daemon restart erased the accumulated score. This table
        // stores the bounded extractor output until the session crosses the commit
        // threshold, so "low importance" means delayed, not forgotten.
        version: 14,
        sql: `
      CREATE TABLE IF NOT EXISTS reflection_pending_extractions (
        id              INTEGER PRIMARY KEY AUTOINCREMENT,
        session_id      TEXT NOT NULL,
        call_id         TEXT NOT NULL,
        tool            TEXT,
        extraction_json TEXT NOT NULL,
        importance      REAL NOT NULL,
        created_at      TEXT NOT NULL,
        UNIQUE(session_id, call_id)
      );

      CREATE INDEX IF NOT EXISTS idx_reflection_pending_session
        ON reflection_pending_extractions(session_id, created_at ASC);
    `
      },
      {
        // v15 — dependable recall + temporal evidence. This is intentionally
        // additive: consolidated_facts remains the canonical claim store while
        // episodes/evidence preserve the source that made each claim durable.
        version: 15,
        sql: `
      ALTER TABLE consolidated_facts ADD COLUMN valid_from TEXT;
      ALTER TABLE consolidated_facts ADD COLUMN valid_to TEXT;
      ALTER TABLE consolidated_facts ADD COLUMN superseded_by_fact_id INTEGER REFERENCES consolidated_facts(id);
      ALTER TABLE consolidated_facts ADD COLUMN confidence REAL;
      ALTER TABLE consolidated_facts ADD COLUMN impression_count INTEGER NOT NULL DEFAULT 0;
      ALTER TABLE consolidated_facts ADD COLUMN utility_count INTEGER NOT NULL DEFAULT 0;
      ALTER TABLE consolidated_facts ADD COLUMN last_used_at TEXT;

      UPDATE consolidated_facts
      SET valid_from = COALESCE(valid_from, created_at),
          confidence = COALESCE(confidence, trust_level, CASE WHEN derived_from_call_id IS NULL THEN 1.0 ELSE 0.6 END),
          impression_count = MAX(impression_count, access_count);

      CREATE INDEX IF NOT EXISTS idx_facts_temporal
        ON consolidated_facts(active, valid_from, valid_to);
      CREATE INDEX IF NOT EXISTS idx_facts_superseded
        ON consolidated_facts(superseded_by_fact_id) WHERE superseded_by_fact_id IS NOT NULL;
      CREATE INDEX IF NOT EXISTS idx_facts_utility
        ON consolidated_facts(utility_count DESC, last_used_at DESC);

      CREATE TABLE IF NOT EXISTS memory_episodes (
        id                 TEXT PRIMARY KEY,
        kind               TEXT NOT NULL CHECK (kind IN ('user_turn','tool_result','import','manual','reflection')),
        source_app         TEXT,
        session_id         TEXT,
        call_id            TEXT,
        source_uri         TEXT,
        occurred_at        TEXT NOT NULL,
        ingested_at        TEXT NOT NULL,
        content_hash       TEXT NOT NULL,
        evidence_excerpt   TEXT,
        raw_retained_until TEXT,
        status             TEXT NOT NULL CHECK (status IN ('available','partial','missing','pending','expired')),
        UNIQUE(session_id, call_id)
      );
      CREATE INDEX IF NOT EXISTS idx_memory_episodes_time ON memory_episodes(occurred_at DESC);
      CREATE INDEX IF NOT EXISTS idx_memory_episodes_source ON memory_episodes(session_id, call_id);
      CREATE INDEX IF NOT EXISTS idx_memory_episodes_status ON memory_episodes(status, ingested_at);

      CREATE TABLE IF NOT EXISTS fact_evidence (
        fact_id     INTEGER NOT NULL REFERENCES consolidated_facts(id) ON DELETE CASCADE,
        episode_id  TEXT NOT NULL REFERENCES memory_episodes(id) ON DELETE CASCADE,
        excerpt     TEXT NOT NULL,
        source_uri  TEXT,
        ordinal     INTEGER NOT NULL DEFAULT 0,
        created_at  TEXT NOT NULL,
        PRIMARY KEY (fact_id, episode_id, ordinal)
      );
      CREATE INDEX IF NOT EXISTS idx_fact_evidence_episode ON fact_evidence(episode_id);

      CREATE TABLE IF NOT EXISTS memory_policies (
        fact_id          INTEGER PRIMARY KEY REFERENCES consolidated_facts(id) ON DELETE CASCADE,
        policy_type      TEXT NOT NULL CHECK (policy_type IN ('hard_constraint','core_profile','standing_preference')),
        enforcement      TEXT NOT NULL CHECK (enforcement IN ('dispatch','prompt')),
        applies_to_json  TEXT NOT NULL DEFAULT '{}',
        priority         INTEGER NOT NULL DEFAULT 50,
        created_at       TEXT NOT NULL,
        updated_at       TEXT NOT NULL
      );
      CREATE INDEX IF NOT EXISTS idx_memory_policies_type ON memory_policies(policy_type, priority DESC);

      INSERT OR REPLACE INTO memory_policies
        (fact_id, policy_type, enforcement, applies_to_json, priority, created_at, updated_at)
      SELECT id,
        CASE
          WHEN kind = 'constraint' THEN 'hard_constraint'
          WHEN kind = 'user' THEN 'core_profile'
          ELSE 'standing_preference'
        END,
        CASE WHEN kind = 'constraint' THEN 'dispatch' ELSE 'prompt' END,
        '{}',
        CAST(ROUND(COALESCE(importance, 5) * 10) AS INTEGER),
        created_at,
        updated_at
      FROM consolidated_facts
      WHERE active = 1 AND (kind = 'constraint' OR pinned = 1);

      CREATE TRIGGER IF NOT EXISTS memory_policy_fact_ai
      AFTER INSERT ON consolidated_facts
      WHEN NEW.active = 1 AND (NEW.kind = 'constraint' OR NEW.pinned = 1)
      BEGIN
        INSERT OR REPLACE INTO memory_policies
          (fact_id, policy_type, enforcement, applies_to_json, priority, created_at, updated_at)
        VALUES (
          NEW.id,
          CASE WHEN NEW.kind = 'constraint' THEN 'hard_constraint' WHEN NEW.kind = 'user' THEN 'core_profile' ELSE 'standing_preference' END,
          CASE WHEN NEW.kind = 'constraint' THEN 'dispatch' ELSE 'prompt' END,
          '{}', CAST(ROUND(COALESCE(NEW.importance, 5) * 10) AS INTEGER), NEW.created_at, NEW.updated_at
        );
      END;

      CREATE TRIGGER IF NOT EXISTS memory_policy_fact_au
      AFTER UPDATE OF active, kind, pinned, importance, updated_at ON consolidated_facts
      BEGIN
        DELETE FROM memory_policies WHERE fact_id = NEW.id;
        INSERT INTO memory_policies
          (fact_id, policy_type, enforcement, applies_to_json, priority, created_at, updated_at)
        SELECT NEW.id,
          CASE WHEN NEW.kind = 'constraint' THEN 'hard_constraint' WHEN NEW.kind = 'user' THEN 'core_profile' ELSE 'standing_preference' END,
          CASE WHEN NEW.kind = 'constraint' THEN 'dispatch' ELSE 'prompt' END,
          '{}', CAST(ROUND(COALESCE(NEW.importance, 5) * 10) AS INTEGER), NEW.created_at, NEW.updated_at
        WHERE NEW.active = 1 AND (NEW.kind = 'constraint' OR NEW.pinned = 1);
      END;

      ALTER TABLE entity_edges ADD COLUMN confidence REAL NOT NULL DEFAULT 0.7;
      ALTER TABLE entity_edges ADD COLUMN evidence_episode_id TEXT REFERENCES memory_episodes(id);
      ALTER TABLE entity_edges ADD COLUMN valid_from TEXT;
      ALTER TABLE entity_edges ADD COLUMN valid_to TEXT;
      ALTER TABLE entity_edges ADD COLUMN invalidated_at TEXT;

      ALTER TABLE reflection_pending_extractions ADD COLUMN expires_at TEXT;
      ALTER TABLE reflection_pending_extractions ADD COLUMN status TEXT NOT NULL DEFAULT 'pending';
      UPDATE reflection_pending_extractions
      SET expires_at = datetime(created_at, '+7 days')
      WHERE expires_at IS NULL;
      CREATE INDEX IF NOT EXISTS idx_reflection_pending_expiry
        ON reflection_pending_extractions(status, expires_at);

      CREATE TABLE IF NOT EXISTS memory_generation (
        id INTEGER PRIMARY KEY CHECK (id = 1),
        generation INTEGER NOT NULL DEFAULT 1
      );
      INSERT OR IGNORE INTO memory_generation (id, generation) VALUES (1, 1);

      CREATE TRIGGER IF NOT EXISTS memory_generation_fact_ai AFTER INSERT ON consolidated_facts
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_fact_au AFTER UPDATE OF content_hash, active, kind ON consolidated_facts
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_fact_ad AFTER DELETE ON consolidated_facts
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_fact_embedding_ai AFTER INSERT ON fact_embeddings
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_fact_embedding_au AFTER UPDATE ON fact_embeddings
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_fact_embedding_ad AFTER DELETE ON fact_embeddings
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_fact_entity_ai AFTER INSERT ON fact_entities
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_fact_entity_ad AFTER DELETE ON fact_entities
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_fact_resource_ai AFTER INSERT ON fact_resources
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_fact_resource_ad AFTER DELETE ON fact_resources
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_entity_edge_ai AFTER INSERT ON entity_edges
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_entity_edge_au AFTER UPDATE ON entity_edges
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_entity_edge_ad AFTER DELETE ON entity_edges
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
    `
      },
      {
        // v16 — canonical entity identity. The original entities table only
        // deduplicated an exact lower-cased display name, so aliases such as
        // "Alex" and "Alexander Chen" became separate people even when both
        // carried the same email address. Sidecar tables preserve every original
        // entity row while adding evidence-aware aliases, stable identifiers, and
        // reversible redirects to a canonical identity.
        version: 16,
        sql: `
      CREATE TABLE IF NOT EXISTS entity_aliases (
        entity_id           INTEGER NOT NULL REFERENCES entities(id) ON DELETE CASCADE,
        alias               TEXT NOT NULL,
        alias_lc            TEXT NOT NULL,
        confidence          REAL NOT NULL DEFAULT 0.7,
        evidence_episode_id TEXT REFERENCES memory_episodes(id) ON DELETE SET NULL,
        source_uri          TEXT,
        first_seen_at       TEXT NOT NULL,
        last_seen_at        TEXT NOT NULL,
        PRIMARY KEY (entity_id, alias_lc)
      );
      CREATE INDEX IF NOT EXISTS idx_entity_aliases_lookup ON entity_aliases(alias_lc, confidence DESC);

      CREATE TABLE IF NOT EXISTS entity_identifiers (
        entity_id           INTEGER NOT NULL REFERENCES entities(id) ON DELETE CASCADE,
        scheme              TEXT NOT NULL,
        value               TEXT NOT NULL,
        value_norm          TEXT NOT NULL,
        confidence          REAL NOT NULL DEFAULT 0.9,
        evidence_episode_id TEXT REFERENCES memory_episodes(id) ON DELETE SET NULL,
        source_uri          TEXT,
        first_seen_at       TEXT NOT NULL,
        last_seen_at        TEXT NOT NULL,
        PRIMARY KEY (entity_id, scheme, value_norm)
      );
      CREATE INDEX IF NOT EXISTS idx_entity_identifiers_lookup
        ON entity_identifiers(scheme, value_norm, confidence DESC);

      CREATE TABLE IF NOT EXISTS entity_redirects (
        source_entity_id    INTEGER PRIMARY KEY REFERENCES entities(id) ON DELETE CASCADE,
        canonical_entity_id INTEGER NOT NULL REFERENCES entities(id) ON DELETE CASCADE,
        reason              TEXT NOT NULL,
        confidence          REAL NOT NULL DEFAULT 1.0,
        evidence_episode_id TEXT REFERENCES memory_episodes(id) ON DELETE SET NULL,
        created_at          TEXT NOT NULL,
        CHECK (source_entity_id <> canonical_entity_id)
      );
      CREATE INDEX IF NOT EXISTS idx_entity_redirects_canonical ON entity_redirects(canonical_entity_id);

      INSERT OR IGNORE INTO entity_aliases
        (entity_id, alias, alias_lc, confidence, evidence_episode_id, source_uri, first_seen_at, last_seen_at)
      SELECT e.id, TRIM(CAST(j.value AS TEXT)), LOWER(TRIM(CAST(j.value AS TEXT))),
             0.6, NULL, NULL, e.first_seen_at, e.last_seen_at
      FROM entities e,
           json_each(CASE WHEN json_valid(e.aliases_json) THEN e.aliases_json ELSE '[]' END) j
      WHERE j.type = 'text' AND LENGTH(TRIM(CAST(j.value AS TEXT))) > 0;

      -- Email aliases are sufficiently stable to seed identifier matching.
      -- Ambiguous identifiers remain separate candidates until explicitly
      -- reconciled; the migration never guesses which historical row wins.
      INSERT OR IGNORE INTO entity_identifiers
        (entity_id, scheme, value, value_norm, confidence, evidence_episode_id, source_uri, first_seen_at, last_seen_at)
      SELECT entity_id, 'email', alias, LOWER(alias), 0.9,
             evidence_episode_id, source_uri, first_seen_at, last_seen_at
      FROM entity_aliases
      WHERE alias_lc GLOB '*@*.*' AND alias_lc NOT GLOB '* *';

      INSERT OR IGNORE INTO entity_identifiers
        (entity_id, scheme, value, value_norm, confidence, evidence_episode_id, source_uri, first_seen_at, last_seen_at)
      SELECT id, 'email', canonical_name, canonical_name_lc, 0.9,
             NULL, NULL, first_seen_at, last_seen_at
      FROM entities
      WHERE canonical_name_lc GLOB '*@*.*' AND canonical_name_lc NOT GLOB '* *';

      CREATE TRIGGER IF NOT EXISTS memory_generation_entity_alias_ai AFTER INSERT ON entity_aliases
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_entity_alias_au AFTER UPDATE ON entity_aliases
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_entity_alias_ad AFTER DELETE ON entity_aliases
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_entity_identifier_ai AFTER INSERT ON entity_identifiers
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_entity_identifier_au AFTER UPDATE ON entity_identifiers
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_entity_identifier_ad AFTER DELETE ON entity_identifiers
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_entity_redirect_ai AFTER INSERT ON entity_redirects
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_entity_redirect_au AFTER UPDATE ON entity_redirects
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_entity_redirect_ad AFTER DELETE ON entity_redirects
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
    `
      },
      {
        // v17 — repeatable temporal validity. A single valid_from/valid_to pair on
        // consolidated_facts cannot represent true → false → true: reactivating an
        // exact claim reused its unique content hash but left the stale closing
        // timestamp in place. This interval ledger preserves every asserted period
        // while the fact row continues to expose the current period for compatibility.
        version: 17,
        sql: `
      CREATE TABLE IF NOT EXISTS fact_validity_intervals (
        id            INTEGER PRIMARY KEY AUTOINCREMENT,
        fact_id       INTEGER NOT NULL REFERENCES consolidated_facts(id) ON DELETE CASCADE,
        valid_from    TEXT NOT NULL,
        valid_to      TEXT,
        opened_reason TEXT NOT NULL DEFAULT 'asserted',
        closed_reason TEXT,
        created_at    TEXT NOT NULL,
        updated_at    TEXT NOT NULL,
        UNIQUE(fact_id, valid_from)
      );
      CREATE INDEX IF NOT EXISTS idx_fact_validity_lookup
        ON fact_validity_intervals(fact_id, valid_from, valid_to);
      CREATE INDEX IF NOT EXISTS idx_fact_validity_time
        ON fact_validity_intervals(valid_from, valid_to);

      -- Preserve the legacy interval exactly. Inactive rows without a semantic
      -- close time are bounded at their last update instead of appearing true
      -- forever in historical queries after they were forgotten/retired.
      INSERT OR IGNORE INTO fact_validity_intervals
        (fact_id, valid_from, valid_to, opened_reason, closed_reason, created_at, updated_at)
      SELECT id,
             COALESCE(valid_from, created_at),
             CASE WHEN active = 0 THEN COALESCE(valid_to, updated_at) ELSE valid_to END,
             'legacy',
             CASE
               WHEN active = 0 AND superseded_by_fact_id IS NOT NULL THEN 'superseded'
               WHEN active = 0 THEN 'deactivated'
               WHEN valid_to IS NOT NULL THEN 'legacy-reactivation-boundary'
               ELSE NULL
             END,
             created_at,
             updated_at
      FROM consolidated_facts;

      -- Repair rows that were reactivated by the old exact-hash path but kept
      -- their stale valid_to/superseded_by values. Their prior interval remains
      -- above; this inserts the currently-open period without inventing content.
      INSERT OR IGNORE INTO fact_validity_intervals
        (fact_id, valid_from, valid_to, opened_reason, closed_reason, created_at, updated_at)
      SELECT id,
             CASE WHEN updated_at > valid_to THEN updated_at ELSE valid_to END,
             NULL,
             'legacy-reactivated',
             NULL,
             updated_at,
             updated_at
      FROM consolidated_facts
      WHERE active = 1 AND valid_to IS NOT NULL;

      UPDATE consolidated_facts
      SET valid_to = COALESCE(valid_to, updated_at)
      WHERE active = 0 AND valid_to IS NULL;

      UPDATE consolidated_facts
      SET valid_from = CASE WHEN updated_at > valid_to THEN updated_at ELSE valid_to END,
          valid_to = NULL,
          superseded_by_fact_id = NULL
      WHERE active = 1 AND valid_to IS NOT NULL;

      CREATE TRIGGER IF NOT EXISTS fact_validity_fact_ai
      AFTER INSERT ON consolidated_facts
      BEGIN
        INSERT OR IGNORE INTO fact_validity_intervals
          (fact_id, valid_from, valid_to, opened_reason, closed_reason, created_at, updated_at)
        VALUES (
          NEW.id, COALESCE(NEW.valid_from, NEW.created_at), NEW.valid_to,
          'asserted', NULL, NEW.created_at, NEW.updated_at
        );
      END;

      CREATE TRIGGER IF NOT EXISTS fact_validity_fact_deactivate
      AFTER UPDATE OF active ON consolidated_facts
      WHEN OLD.active = 1 AND NEW.active = 0
      BEGIN
        UPDATE fact_validity_intervals
        SET valid_to = COALESCE(NEW.valid_to, NEW.updated_at),
            closed_reason = CASE WHEN NEW.superseded_by_fact_id IS NOT NULL THEN 'superseded' ELSE 'deactivated' END,
            updated_at = NEW.updated_at
        WHERE fact_id = NEW.id AND valid_to IS NULL;
      END;

      CREATE TRIGGER IF NOT EXISTS fact_validity_fact_reactivate
      AFTER UPDATE OF active ON consolidated_facts
      WHEN OLD.active = 0 AND NEW.active = 1
      BEGIN
        INSERT OR IGNORE INTO fact_validity_intervals
          (fact_id, valid_from, valid_to, opened_reason, closed_reason, created_at, updated_at)
        VALUES (
          NEW.id, COALESCE(NEW.valid_from, NEW.updated_at), NEW.valid_to,
          'reactivated', NULL, NEW.updated_at, NEW.updated_at
        );
      END;

      CREATE TRIGGER IF NOT EXISTS memory_generation_fact_validity_ai AFTER INSERT ON fact_validity_intervals
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_fact_validity_au AFTER UPDATE ON fact_validity_intervals
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_fact_validity_ad AFTER DELETE ON fact_validity_intervals
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
    `
      },
      {
        // v18 — first-class episode identity. Recordings and future durable event
        // families still use the stable episode kind contract, while subtype/title
        // make their real-world meaning visible to recall and the stored graph.
        version: 18,
        sql: `
      ALTER TABLE memory_episodes ADD COLUMN subtype TEXT;
      ALTER TABLE memory_episodes ADD COLUMN title TEXT;
      ALTER TABLE memory_episodes ADD COLUMN metadata_json TEXT NOT NULL DEFAULT '{}';
      CREATE INDEX IF NOT EXISTS idx_memory_episodes_subtype_time
        ON memory_episodes(subtype, occurred_at DESC);

      CREATE TRIGGER IF NOT EXISTS memory_generation_episode_ai AFTER INSERT ON memory_episodes
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_episode_au AFTER UPDATE ON memory_episodes
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_episode_ad AFTER DELETE ON memory_episodes
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
    `
      },
      {
        // v19 — measured recall utility. A recall result being displayed is an
        // impression; it becomes utility only when a consumer explicitly selects
        // one of that run's returned refs. Stable run ids and unique use rows make
        // attribution auditable and idempotent instead of a popularity counter.
        version: 19,
        sql: `
      CREATE TABLE IF NOT EXISTS memory_recall_runs (
        id                  TEXT PRIMARY KEY,
        objective           TEXT NOT NULL,
        surface             TEXT NOT NULL,
        answerability       TEXT NOT NULL CHECK (answerability IN ('supported','partial','insufficient')),
        candidate_refs_json TEXT NOT NULL,
        created_at          TEXT NOT NULL,
        expires_at          TEXT NOT NULL
      );
      CREATE INDEX IF NOT EXISTS idx_memory_recall_runs_created
        ON memory_recall_runs(created_at DESC);
      CREATE INDEX IF NOT EXISTS idx_memory_recall_runs_expiry
        ON memory_recall_runs(expires_at);

      CREATE TABLE IF NOT EXISTS memory_recall_uses (
        recall_id   TEXT NOT NULL REFERENCES memory_recall_runs(id) ON DELETE CASCADE,
        ref_type    TEXT NOT NULL,
        ref_id      TEXT NOT NULL,
        outcome     TEXT NOT NULL CHECK (outcome IN ('used','not_useful')),
        detail      TEXT,
        recorded_at TEXT NOT NULL,
        PRIMARY KEY (recall_id, ref_type, ref_id)
      );
      CREATE INDEX IF NOT EXISTS idx_memory_recall_uses_ref
        ON memory_recall_uses(ref_type, ref_id, recorded_at DESC);
      CREATE INDEX IF NOT EXISTS idx_memory_recall_uses_outcome
        ON memory_recall_uses(outcome, recorded_at DESC);
    `
      },
      {
        // v20 — grounded temporal entity relationships. The original entity_edges
        // row kept only one mutable evidence pointer and one validity range. That
        // made extractor retries look like independent corroboration and made
        // true → false → true relationships impossible to represent. Keep the
        // compact edge row for compatibility/fast traversal, but make evidence and
        // validity append-only ledgers the source of truth.
        version: 20,
        sql: `
      CREATE TABLE IF NOT EXISTS entity_edge_evidence (
        subject_id       INTEGER NOT NULL,
        predicate        TEXT NOT NULL,
        object_id        INTEGER NOT NULL,
        episode_id       TEXT NOT NULL REFERENCES memory_episodes(id) ON DELETE RESTRICT,
        excerpt_hash     TEXT NOT NULL,
        excerpt          TEXT NOT NULL,
        source_uri       TEXT,
        source_fact_id   INTEGER REFERENCES consolidated_facts(id) ON DELETE SET NULL,
        confidence       REAL NOT NULL DEFAULT 0.7,
        observed_at      TEXT NOT NULL,
        valid_from       TEXT,
        valid_to         TEXT,
        extraction_method TEXT NOT NULL DEFAULT 'reflection'
          CHECK (extraction_method IN ('reflection','fact_backfill','manual','import')),
        created_at       TEXT NOT NULL,
        PRIMARY KEY (subject_id, predicate, object_id, episode_id, excerpt_hash),
        FOREIGN KEY (subject_id, predicate, object_id)
          REFERENCES entity_edges(subject_id, predicate, object_id) ON DELETE CASCADE
      );
      CREATE INDEX IF NOT EXISTS idx_entity_edge_evidence_episode
        ON entity_edge_evidence(episode_id);
      CREATE INDEX IF NOT EXISTS idx_entity_edge_evidence_fact
        ON entity_edge_evidence(source_fact_id) WHERE source_fact_id IS NOT NULL;

      CREATE TABLE IF NOT EXISTS entity_edge_validity_intervals (
        id                  INTEGER PRIMARY KEY AUTOINCREMENT,
        subject_id          INTEGER NOT NULL,
        predicate           TEXT NOT NULL,
        object_id           INTEGER NOT NULL,
        valid_from          TEXT NOT NULL,
        valid_to            TEXT,
        opened_reason       TEXT NOT NULL DEFAULT 'asserted',
        closed_reason       TEXT,
        evidence_episode_id TEXT REFERENCES memory_episodes(id) ON DELETE SET NULL,
        created_at          TEXT NOT NULL,
        updated_at          TEXT NOT NULL,
        UNIQUE(subject_id, predicate, object_id, valid_from),
        FOREIGN KEY (subject_id, predicate, object_id)
          REFERENCES entity_edges(subject_id, predicate, object_id) ON DELETE CASCADE
      );
      CREATE INDEX IF NOT EXISTS idx_entity_edge_validity_lookup
        ON entity_edge_validity_intervals(subject_id, predicate, object_id, valid_from, valid_to);
      CREATE INDEX IF NOT EXISTS idx_entity_edge_validity_time
        ON entity_edge_validity_intervals(valid_from, valid_to);

      INSERT OR IGNORE INTO entity_edge_validity_intervals
        (subject_id, predicate, object_id, valid_from, valid_to, opened_reason,
         closed_reason, evidence_episode_id, created_at, updated_at)
      SELECT subject_id, predicate, object_id,
             COALESCE(valid_from, first_seen_at),
             COALESCE(valid_to, invalidated_at),
             'legacy',
             CASE WHEN invalidated_at IS NOT NULL THEN 'invalidated'
                  WHEN valid_to IS NOT NULL THEN 'ended' ELSE NULL END,
             evidence_episode_id,
             first_seen_at,
             last_seen_at
      FROM entity_edges;

      CREATE TRIGGER IF NOT EXISTS memory_generation_entity_edge_evidence_ai
      AFTER INSERT ON entity_edge_evidence
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_entity_edge_evidence_au
      AFTER UPDATE ON entity_edge_evidence
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_entity_edge_evidence_ad
      AFTER DELETE ON entity_edge_evidence
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;

      CREATE TRIGGER IF NOT EXISTS memory_generation_entity_edge_validity_ai
      AFTER INSERT ON entity_edge_validity_intervals
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_entity_edge_validity_au
      AFTER UPDATE ON entity_edge_validity_intervals
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_entity_edge_validity_ad
      AFTER DELETE ON entity_edge_validity_intervals
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
    `
      },
      {
        // v21 — relationship truth labels for persisted text matches. A row being
        // stored in SQLite does not make the relationship itself verified: the
        // historical nightly sync populated fact↔entity/resource joins by matching
        // names in text. Preserve those useful candidates, but label them inferred
        // so stored-truth graph mode can exclude them. New reviewed/extracted links
        // may carry grounded provenance on the same row.
        version: 21,
        sql: `
      ALTER TABLE fact_entities ADD COLUMN link_type TEXT NOT NULL DEFAULT 'inferred_text'
        CHECK (link_type IN ('stored','extracted','inferred_text'));
      ALTER TABLE fact_entities ADD COLUMN confidence REAL NOT NULL DEFAULT 0.55;
      ALTER TABLE fact_entities ADD COLUMN evidence_episode_id TEXT REFERENCES memory_episodes(id) ON DELETE SET NULL;
      ALTER TABLE fact_entities ADD COLUMN evidence_excerpt TEXT;

      ALTER TABLE fact_resources ADD COLUMN link_type TEXT NOT NULL DEFAULT 'inferred_text'
        CHECK (link_type IN ('stored','extracted','inferred_text'));
      ALTER TABLE fact_resources ADD COLUMN confidence REAL NOT NULL DEFAULT 0.55;
      ALTER TABLE fact_resources ADD COLUMN evidence_episode_id TEXT REFERENCES memory_episodes(id) ON DELETE SET NULL;
      ALTER TABLE fact_resources ADD COLUMN evidence_excerpt TEXT;

      CREATE INDEX IF NOT EXISTS idx_fact_entities_truth
        ON fact_entities(link_type, entity_id, fact_id);
      CREATE INDEX IF NOT EXISTS idx_fact_resources_truth
        ON fact_resources(link_type, resource_id, fact_id);

      CREATE TRIGGER IF NOT EXISTS memory_generation_fact_entity_au
      AFTER UPDATE OF link_type, confidence, evidence_episode_id, evidence_excerpt ON fact_entities
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_fact_resource_au
      AFTER UPDATE OF link_type, confidence, evidence_episode_id, evidence_excerpt ON fact_resources
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
    `
      },
      {
        // v22 — names are labels, not identities. The original entity registry
        // made (type, lowercased name) UNIQUE, which made it impossible to store
        // two different people called "Jordan Lee" and encouraged unsafe merges.
        // Rebuild the parent table without that constraint; identity convergence
        // now lives in entity-identity.ts, where strong evidence and ambiguity are
        // handled explicitly. Child foreign keys keep referencing `entities`.
        version: 22,
        run: (db) => {
          try {
            db.pragma("foreign_keys = OFF");
            db.transaction(() => {
              db.exec(`
            CREATE TABLE IF NOT EXISTS memory_migration_audit (
              id                INTEGER PRIMARY KEY AUTOINCREMENT,
              migration_version INTEGER NOT NULL,
              action            TEXT NOT NULL,
              affected_rows     INTEGER NOT NULL,
              detail_json       TEXT NOT NULL DEFAULT '{}',
              created_at        TEXT NOT NULL
            );
            CREATE INDEX IF NOT EXISTS idx_memory_migration_audit_version
              ON memory_migration_audit(migration_version, created_at DESC);
          `);
              const auditCleanup = db.prepare(`
            INSERT INTO memory_migration_audit
              (migration_version, action, affected_rows, detail_json, created_at)
            VALUES (22, ?, ?, ?, ?)
          `);
              const cleanedAt = (/* @__PURE__ */ new Date()).toISOString();
              const materialize = [
                {
                  action: "materialize_legacy_fact_entity_truth_defaults",
                  sql: `UPDATE fact_entities
                    SET link_type = COALESCE(link_type, 'inferred_text'),
                        confidence = COALESCE(confidence, 0.55)`,
                  detail: { table: "fact_entities", columns: ["link_type", "confidence"], defaults: ["inferred_text", 0.55] }
                },
                {
                  action: "materialize_legacy_fact_resource_truth_defaults",
                  sql: `UPDATE fact_resources
                    SET link_type = COALESCE(link_type, 'inferred_text'),
                        confidence = COALESCE(confidence, 0.55)`,
                  detail: { table: "fact_resources", columns: ["link_type", "confidence"], defaults: ["inferred_text", 0.55] }
                }
              ];
              for (const item of materialize) {
                const affected = Number(db.prepare(item.sql).run().changes ?? 0);
                if (affected > 0) auditCleanup.run(item.action, affected, JSON.stringify(item.detail), cleanedAt);
              }
              const cleanup = [
                {
                  action: "remove_orphaned_inferred_fact_entity_links",
                  detail: { table: "fact_entities", rebuildable: true, linkType: "inferred_text" },
                  sql: `DELETE FROM fact_entities
                    WHERE link_type = 'inferred_text'
                      AND (
                        NOT EXISTS (SELECT 1 FROM consolidated_facts f WHERE f.id = fact_entities.fact_id)
                        OR NOT EXISTS (SELECT 1 FROM entities e WHERE e.id = fact_entities.entity_id)
                      )`
                },
                {
                  action: "remove_orphaned_inferred_fact_resource_links",
                  detail: { table: "fact_resources", rebuildable: true, linkType: "inferred_text" },
                  sql: `DELETE FROM fact_resources
                    WHERE link_type = 'inferred_text'
                      AND (
                        NOT EXISTS (SELECT 1 FROM consolidated_facts f WHERE f.id = fact_resources.fact_id)
                        OR NOT EXISTS (SELECT 1 FROM resource_pointers r WHERE r.id = fact_resources.resource_id)
                      )`
                },
                {
                  action: "remove_orphaned_vault_embeddings",
                  detail: { table: "embeddings", rebuildable: true },
                  sql: `DELETE FROM embeddings
                    WHERE NOT EXISTS (SELECT 1 FROM vault_chunks c WHERE c.id = embeddings.chunk_id)`
                },
                {
                  action: "remove_orphaned_fact_embeddings",
                  detail: { table: "fact_embeddings", rebuildable: true },
                  sql: `DELETE FROM fact_embeddings
                    WHERE NOT EXISTS (SELECT 1 FROM consolidated_facts f WHERE f.id = fact_embeddings.fact_id)`
                }
              ];
              for (const item of cleanup) {
                const affected = Number(db.prepare(item.sql).run().changes ?? 0);
                if (affected > 0) auditCleanup.run(item.action, affected, JSON.stringify(item.detail), cleanedAt);
              }
              db.exec(`
            DROP TABLE IF EXISTS entities_v22;
            CREATE TABLE entities_v22 (
              id                INTEGER PRIMARY KEY AUTOINCREMENT,
              entity_type       TEXT NOT NULL CHECK (entity_type IN ('person','company','project','place','thing')),
              canonical_name    TEXT NOT NULL,
              canonical_name_lc TEXT NOT NULL,
              aliases_json      TEXT NOT NULL DEFAULT '[]',
              first_seen_at     TEXT NOT NULL,
              last_seen_at      TEXT NOT NULL,
              mention_count     INTEGER NOT NULL DEFAULT 1
            );

            INSERT INTO entities_v22
              (id, entity_type, canonical_name, canonical_name_lc, aliases_json,
               first_seen_at, last_seen_at, mention_count)
            SELECT id, entity_type, canonical_name, canonical_name_lc, aliases_json,
                   first_seen_at, last_seen_at, mention_count
            FROM entities;

            DROP TABLE entities;
            ALTER TABLE entities_v22 RENAME TO entities;

            CREATE INDEX idx_entities_last_seen ON entities(last_seen_at DESC);
            CREATE INDEX idx_entities_type_name ON entities(entity_type, canonical_name_lc);

            CREATE TRIGGER memory_generation_entity_ai AFTER INSERT ON entities
            BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
            CREATE TRIGGER memory_generation_entity_au
            AFTER UPDATE OF entity_type, canonical_name, canonical_name_lc, aliases_json ON entities
            BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
            CREATE TRIGGER memory_generation_entity_ad AFTER DELETE ON entities
            BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
          `);
              const violations = db.pragma("foreign_key_check");
              if (violations.length > 0) {
                throw new Error(
                  `entities rebuild would leave ${violations.length} non-rebuildable foreign-key violation(s)`
                );
              }
              const integrity = db.pragma("integrity_check");
              const integrityOk = integrity.length === 1 && integrity[0]?.integrity_check === "ok";
              if (!integrityOk) {
                throw new Error(
                  `memory migration failed integrity_check: ${integrity.slice(0, 5).map((row) => row.integrity_check).join("; ")}`
                );
              }
            })();
          } finally {
            db.pragma("foreign_keys = ON");
          }
        }
      },
      {
        // v23 — durable reflection receipts. The old replay guard lived only in a
        // process-local Set, so a daemon restart could re-extract the same tool
        // call and inflate fact reinforcement/entity mention counts. Receipts make
        // (session, call, content) processing exactly-once across restarts while a
        // bounded processing lease keeps crashed attempts safely retryable.
        version: 23,
        sql: `
      CREATE TABLE IF NOT EXISTS memory_reflection_receipts (
        session_id      TEXT NOT NULL,
        call_id         TEXT NOT NULL,
        input_hash      TEXT NOT NULL,
        status          TEXT NOT NULL
          CHECK (status IN ('processing','buffered','completed','failed')),
        attempts        INTEGER NOT NULL DEFAULT 1,
        first_seen_at   TEXT NOT NULL,
        last_attempt_at TEXT NOT NULL,
        completed_at    TEXT,
        result_json     TEXT,
        last_error      TEXT,
        PRIMARY KEY (session_id, call_id)
      );
      CREATE INDEX IF NOT EXISTS idx_memory_reflection_receipts_status
        ON memory_reflection_receipts(status, last_attempt_at);
    `
      },
      {
        // v24 — distinct entity observations. `entities.mention_count` was a
        // process counter: the same durable episode could reach upsertEntity via
        // fact annotation, reflection, and relationship capture and look like
        // three independent mentions. This ledger makes episode-backed identity
        // observation idempotent and gives the desktop a truthful source timeline.
        // Legacy mention_count is retained as an approximate historical total;
        // every new episode-backed observation is exact.
        version: 24,
        sql: `
      CREATE TABLE IF NOT EXISTS entity_observations (
        entity_id     INTEGER NOT NULL REFERENCES entities(id) ON DELETE CASCADE,
        episode_id    TEXT NOT NULL REFERENCES memory_episodes(id) ON DELETE CASCADE,
        source_fact_id INTEGER REFERENCES consolidated_facts(id) ON DELETE SET NULL,
        source_uri    TEXT,
        source_kind   TEXT NOT NULL DEFAULT 'entity_upsert',
        confidence    REAL NOT NULL DEFAULT 0.7,
        observed_at   TEXT NOT NULL,
        created_at    TEXT NOT NULL,
        PRIMARY KEY (entity_id, episode_id)
      );
      CREATE INDEX IF NOT EXISTS idx_entity_observations_episode
        ON entity_observations(episode_id, entity_id);
      CREATE INDEX IF NOT EXISTS idx_entity_observations_time
        ON entity_observations(entity_id, observed_at DESC);

      -- Backfill only observations that already carry a durable episode id.
      -- INSERT OR IGNORE intentionally collapses the same source episode seen
      -- through multiple extraction paths into one observation per identity.
      INSERT OR IGNORE INTO entity_observations
        (entity_id, episode_id, source_fact_id, source_uri, source_kind,
         confidence, observed_at, created_at)
      SELECT fe.entity_id, fe.evidence_episode_id, fe.fact_id,
             COALESCE((SELECT source_uri FROM fact_evidence fve
                       WHERE fve.fact_id = fe.fact_id AND fve.episode_id = fe.evidence_episode_id
                       ORDER BY ordinal LIMIT 1), me.source_uri),
             'fact_link', fe.confidence, me.occurred_at, me.ingested_at
      FROM fact_entities fe
      JOIN memory_episodes me ON me.id = fe.evidence_episode_id
      WHERE fe.evidence_episode_id IS NOT NULL AND fe.link_type <> 'inferred_text';

      INSERT OR IGNORE INTO entity_observations
        (entity_id, episode_id, source_fact_id, source_uri, source_kind,
         confidence, observed_at, created_at)
      SELECT ea.entity_id, ea.evidence_episode_id, NULL,
             COALESCE(ea.source_uri, me.source_uri), 'alias', ea.confidence,
             me.occurred_at, me.ingested_at
      FROM entity_aliases ea
      JOIN memory_episodes me ON me.id = ea.evidence_episode_id
      WHERE ea.evidence_episode_id IS NOT NULL;

      INSERT OR IGNORE INTO entity_observations
        (entity_id, episode_id, source_fact_id, source_uri, source_kind,
         confidence, observed_at, created_at)
      SELECT ei.entity_id, ei.evidence_episode_id, NULL,
             COALESCE(ei.source_uri, me.source_uri), 'identifier', ei.confidence,
             me.occurred_at, me.ingested_at
      FROM entity_identifiers ei
      JOIN memory_episodes me ON me.id = ei.evidence_episode_id
      WHERE ei.evidence_episode_id IS NOT NULL;

      INSERT OR IGNORE INTO entity_observations
        (entity_id, episode_id, source_fact_id, source_uri, source_kind,
         confidence, observed_at, created_at)
      SELECT eee.subject_id, eee.episode_id, eee.source_fact_id,
             COALESCE(eee.source_uri, me.source_uri), 'relationship',
             eee.confidence, eee.observed_at, eee.created_at
      FROM entity_edge_evidence eee
      JOIN memory_episodes me ON me.id = eee.episode_id;

      INSERT OR IGNORE INTO entity_observations
        (entity_id, episode_id, source_fact_id, source_uri, source_kind,
         confidence, observed_at, created_at)
      SELECT eee.object_id, eee.episode_id, eee.source_fact_id,
             COALESCE(eee.source_uri, me.source_uri), 'relationship',
             eee.confidence, eee.observed_at, eee.created_at
      FROM entity_edge_evidence eee
      JOIN memory_episodes me ON me.id = eee.episode_id;

      CREATE TRIGGER IF NOT EXISTS memory_generation_entity_observation_ai
      AFTER INSERT ON entity_observations
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_entity_observation_au
      AFTER UPDATE ON entity_observations
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
      CREATE TRIGGER IF NOT EXISTS memory_generation_entity_observation_ad
      AFTER DELETE ON entity_observations
      BEGIN UPDATE memory_generation SET generation = generation + 1 WHERE id = 1; END;
    `
      },
      {
        // v25 — durable identity-review decisions. Similar names and aliases are
        // useful review signals but never safe automatic merge authority. A user
        // can dismiss a proposed pair once; the decision survives restarts and
        // prevents the desktop from repeatedly nagging about the same identities.
        version: 25,
        sql: `
      CREATE TABLE IF NOT EXISTS entity_identity_review_decisions (
        entity_a_id INTEGER NOT NULL REFERENCES entities(id) ON DELETE CASCADE,
        entity_b_id INTEGER NOT NULL REFERENCES entities(id) ON DELETE CASCADE,
        status      TEXT NOT NULL CHECK (status IN ('dismissed')),
        reason      TEXT,
        reviewed_at TEXT NOT NULL,
        PRIMARY KEY (entity_a_id, entity_b_id),
        CHECK (entity_a_id < entity_b_id)
      );
      CREATE INDEX IF NOT EXISTS idx_entity_identity_review_status
        ON entity_identity_review_decisions(status, reviewed_at DESC);
    `
      },
      {
        // v26 — first-class reflection candidate lifecycle. Threshold buffering
        // previously stored only an opaque extraction batch, then deleted it on
        // promotion/expiry. That made it impossible to answer which proposed facts
        // were accepted, rejected as noise, or expired without enough corroborating
        // importance. The candidate ledger is append-only at identity level and
        // resolves each extracted claim explicitly while the legacy batch table
        // remains the bounded payload buffer used by the commit threshold.
        version: 26,
        sql: `
      CREATE TABLE IF NOT EXISTS memory_reflection_candidates (
        id                INTEGER PRIMARY KEY AUTOINCREMENT,
        episode_id        TEXT REFERENCES memory_episodes(id) ON DELETE SET NULL,
        session_id        TEXT NOT NULL,
        call_id           TEXT NOT NULL,
        candidate_hash    TEXT NOT NULL,
        kind              TEXT NOT NULL,
        text              TEXT NOT NULL,
        importance        REAL NOT NULL,
        status            TEXT NOT NULL
          CHECK (status IN ('pending','promoted','rejected','expired')),
        reason            TEXT,
        resulting_fact_id INTEGER REFERENCES consolidated_facts(id) ON DELETE SET NULL,
        created_at        TEXT NOT NULL,
        resolved_at       TEXT,
        UNIQUE(session_id, call_id, candidate_hash)
      );
      CREATE INDEX IF NOT EXISTS idx_memory_reflection_candidates_status
        ON memory_reflection_candidates(status, created_at DESC);
      CREATE INDEX IF NOT EXISTS idx_memory_reflection_candidates_episode
        ON memory_reflection_candidates(episode_id, status);
      CREATE INDEX IF NOT EXISTS idx_memory_reflection_candidates_fact
        ON memory_reflection_candidates(resulting_fact_id)
        WHERE resulting_fact_id IS NOT NULL;
    `
      },
      {
        // v27 — crash-safe consolidation intake. Automatic capture previously
        // launched the semantic conflict resolver from a fire-and-forget microtask
        // before recording any durable work item. A daemon exit in that window
        // silently lost a user-stated preference or standing rule. Keep the
        // existing learned-claim ledger, but make each row sufficient to replay
        // consolidation exactly once and expose the intake reason separately from
        // the eventual lifecycle decision.
        version: 27,
        run: (db) => {
          db.transaction(() => {
            const columns = new Set(
              db.pragma("table_info(memory_reflection_candidates)").map((row) => row.name)
            );
            const additions = [
              ["source_type", `TEXT NOT NULL DEFAULT 'tool_reflection'
            CHECK (source_type IN ('tool_reflection','recursive_reflection','auto_capture','manual','import'))`],
              ["intake_reason", "TEXT"],
              ["trust_level", "REAL"],
              ["authority", `TEXT CHECK (authority IS NULL OR authority IN ('user','derived','import','manual'))`],
              ["source_uri", "TEXT"],
              ["pin", "INTEGER NOT NULL DEFAULT 0"],
              ["attempt_count", "INTEGER NOT NULL DEFAULT 0"],
              ["next_attempt_at", "TEXT"],
              ["last_error", "TEXT"],
              ["processing_started_at", "TEXT"]
            ];
            for (const [name, definition] of additions) {
              if (!columns.has(name)) db.exec(`ALTER TABLE memory_reflection_candidates ADD COLUMN ${name} ${definition}`);
            }
            db.exec(`
          UPDATE memory_reflection_candidates
          SET source_type = CASE
            WHEN session_id = 'cron:recursive-reflection' THEN 'recursive_reflection'
            ELSE 'tool_reflection'
          END
          WHERE source_type IS NULL OR source_type = 'tool_reflection';

          CREATE INDEX IF NOT EXISTS idx_memory_reflection_candidates_retry
            ON memory_reflection_candidates(source_type, status, next_attempt_at, created_at)
            WHERE status = 'pending';
        `);
          })();
        }
      },
      {
        // v28 — truthful policy enforcement coverage. v15 labeled every legacy
        // `kind=constraint` row as dispatch-enforced even though the runtime only
        // compiled a few concrete rule families. Re-project every policy through
        // the same deterministic classifier used at write/dispatch time. Database
        // triggers now fail safe to prompt-only; canonical writers immediately
        // promote a rule only after a compiler proves it enforceable.
        version: 28,
        run: (db) => {
          db.transaction(() => {
            db.exec(`
          DROP TRIGGER IF EXISTS memory_policy_fact_ai;
          DROP TRIGGER IF EXISTS memory_policy_fact_au;

          CREATE TRIGGER memory_policy_fact_ai
          AFTER INSERT ON consolidated_facts
          WHEN NEW.active = 1 AND (NEW.kind = 'constraint' OR NEW.pinned = 1)
          BEGIN
            INSERT OR REPLACE INTO memory_policies
              (fact_id, policy_type, enforcement, applies_to_json, priority, created_at, updated_at)
            VALUES (
              NEW.id,
              CASE WHEN NEW.kind = 'user' THEN 'core_profile' ELSE 'standing_preference' END,
              'prompt',
              '{"schemaVersion":1,"family":"unclassified","deterministic":false,"tools":[],"reason":"Awaiting application policy classification."}',
              CAST(ROUND(COALESCE(NEW.importance, 5) * 10) AS INTEGER),
              NEW.created_at,
              NEW.updated_at
            );
          END;

          CREATE TRIGGER memory_policy_fact_au
          AFTER UPDATE OF active, kind, pinned, importance, content, updated_at ON consolidated_facts
          BEGIN
            DELETE FROM memory_policies WHERE fact_id = NEW.id;
            INSERT INTO memory_policies
              (fact_id, policy_type, enforcement, applies_to_json, priority, created_at, updated_at)
            SELECT NEW.id,
              CASE WHEN NEW.kind = 'user' THEN 'core_profile' ELSE 'standing_preference' END,
              'prompt',
              '{"schemaVersion":1,"family":"unclassified","deterministic":false,"tools":[],"reason":"Awaiting application policy classification."}',
              CAST(ROUND(COALESCE(NEW.importance, 5) * 10) AS INTEGER),
              NEW.created_at,
              NEW.updated_at
            WHERE NEW.active = 1 AND (NEW.kind = 'constraint' OR NEW.pinned = 1);
          END;
        `);
            const rows = db.prepare(`
          SELECT id, kind, content, pinned, importance, created_at, updated_at
          FROM consolidated_facts
          WHERE active = 1 AND (kind = 'constraint' OR pinned = 1)
        `).all();
            const upsert = db.prepare(`
          INSERT INTO memory_policies
            (fact_id, policy_type, enforcement, applies_to_json, priority, created_at, updated_at)
          VALUES (?, ?, ?, ?, ?, ?, ?)
          ON CONFLICT(fact_id) DO UPDATE SET
            policy_type = excluded.policy_type,
            enforcement = excluded.enforcement,
            applies_to_json = excluded.applies_to_json,
            priority = excluded.priority,
            updated_at = excluded.updated_at
        `);
            for (const row of rows) {
              const descriptor = row.kind === "constraint" ? classifyConstraintEnforcementV1(row.content) : null;
              const policyType = row.kind === "constraint" ? descriptor.deterministic ? "hard_constraint" : "standing_preference" : row.kind === "user" ? "core_profile" : "standing_preference";
              const enforcement = descriptor?.deterministic ? "dispatch" : "prompt";
              upsert.run(
                row.id,
                policyType,
                enforcement,
                JSON.stringify(descriptor ?? {
                  schemaVersion: 1,
                  family: row.kind === "user" ? "core_profile" : "standing_preference",
                  deterministic: false,
                  tools: [],
                  reason: "Prompt-context policy; not a tool-dispatch constraint."
                }),
                Math.round((row.importance ?? 5) * 10),
                row.created_at,
                row.updated_at
              );
            }
          })();
        }
      },
      {
        // v29 — meeting-analysis proposals join the durable claim-intake ledger.
        // SQLite cannot widen a CHECK constraint in place, so rebuild the table
        // transactionally and copy every lifecycle/retry/provenance field exactly.
        // No candidate is promoted, rejected, or otherwise reinterpreted here.
        version: 29,
        run: (db) => {
          db.transaction(() => {
            db.exec(`
          ALTER TABLE memory_reflection_candidates RENAME TO memory_reflection_candidates_v28;

          CREATE TABLE memory_reflection_candidates (
            id                    INTEGER PRIMARY KEY AUTOINCREMENT,
            episode_id            TEXT REFERENCES memory_episodes(id) ON DELETE SET NULL,
            session_id            TEXT NOT NULL,
            call_id               TEXT NOT NULL,
            candidate_hash        TEXT NOT NULL,
            kind                  TEXT NOT NULL,
            text                  TEXT NOT NULL,
            importance            REAL NOT NULL,
            status                TEXT NOT NULL
              CHECK (status IN ('pending','promoted','rejected','expired')),
            reason                TEXT,
            resulting_fact_id     INTEGER REFERENCES consolidated_facts(id) ON DELETE SET NULL,
            created_at            TEXT NOT NULL,
            resolved_at           TEXT,
            source_type           TEXT NOT NULL DEFAULT 'tool_reflection'
              CHECK (source_type IN ('tool_reflection','recursive_reflection','auto_capture','meeting_analysis','manual','import')),
            intake_reason         TEXT,
            trust_level           REAL,
            authority             TEXT CHECK (authority IS NULL OR authority IN ('user','derived','import','manual')),
            source_uri            TEXT,
            pin                   INTEGER NOT NULL DEFAULT 0,
            attempt_count         INTEGER NOT NULL DEFAULT 0,
            next_attempt_at       TEXT,
            last_error            TEXT,
            processing_started_at TEXT,
            UNIQUE(session_id, call_id, candidate_hash)
          );

          INSERT INTO memory_reflection_candidates
            (id, episode_id, session_id, call_id, candidate_hash, kind, text,
             importance, status, reason, resulting_fact_id, created_at, resolved_at,
             source_type, intake_reason, trust_level, authority, source_uri, pin,
             attempt_count, next_attempt_at, last_error, processing_started_at)
          SELECT id, episode_id, session_id, call_id, candidate_hash, kind, text,
             importance, status, reason, resulting_fact_id, created_at, resolved_at,
             source_type, intake_reason, trust_level, authority, source_uri, pin,
             attempt_count, next_attempt_at, last_error, processing_started_at
          FROM memory_reflection_candidates_v28;

          DROP TABLE memory_reflection_candidates_v28;

          CREATE INDEX idx_memory_reflection_candidates_status
            ON memory_reflection_candidates(status, created_at DESC);
          CREATE INDEX idx_memory_reflection_candidates_episode
            ON memory_reflection_candidates(episode_id, status);
          CREATE INDEX idx_memory_reflection_candidates_fact
            ON memory_reflection_candidates(resulting_fact_id)
            WHERE resulting_fact_id IS NOT NULL;
          CREATE INDEX idx_memory_reflection_candidates_retry
            ON memory_reflection_candidates(source_type, status, next_attempt_at, created_at)
            WHERE status = 'pending';
        `);
          })();
        }
      },
      {
        // v30 — session-stamped recall runs. The Claude Agent SDK lane runs its
        // memory tools in a SEPARATE MCP process, so the in-memory turn context
        // that carried recall-run ids to the post-turn auto-credit hook could
        // never cross that boundary — explicit tool recalls in that lane earned
        // zero utility credit forever (2026-07-31 audit: 50/1772 facts had any).
        // The shared memory.db IS the durable cross-process channel: stamp each
        // run with its session so the post-turn seam can read this turn's runs
        // back regardless of which process recorded them. Additive + nullable.
        version: 30,
        run: (db) => {
          const hasColumn = db.pragma("table_info(memory_recall_runs)").some((col) => col.name === "session_id");
          if (!hasColumn) db.exec("ALTER TABLE memory_recall_runs ADD COLUMN session_id TEXT");
          db.exec(`
        CREATE INDEX IF NOT EXISTS idx_memory_recall_runs_session
          ON memory_recall_runs(session_id, created_at DESC);
      `);
        }
      },
      {
        // v31 — durable transport identity for provider inbox rows. Discord can
        // redeliver after a daemon crash; payload/run/source binding must survive
        // so the retry cannot mint a second logical user turn or silently reuse a
        // provider message id for different authority.
        version: 31,
        run: (db) => {
          const columns = new Set(
            db.pragma("table_info(inbound_messages)").map((column) => column.name)
          );
          if (!columns.has("payload_hash")) db.exec("ALTER TABLE inbound_messages ADD COLUMN payload_hash TEXT");
          if (!columns.has("source_user_seq")) db.exec("ALTER TABLE inbound_messages ADD COLUMN source_user_seq INTEGER");
          db.exec(`
        CREATE INDEX IF NOT EXISTS idx_inbound_run_id
          ON inbound_messages(run_id) WHERE run_id IS NOT NULL;
      `);
        }
      },
      {
        // v32 — quarantine unresolved provider deliveries that predate v31's
        // payload authority. A legacy claimed/failed row cannot tell us whether
        // tools or an external write landed before the daemon stopped. Reclaiming
        // it under a newly backfilled payload/run would manufacture authority and
        // could repeat that write. Replied/dropped history remains byte-for-byte
        // status-compatible; only unresolved rows without a payload hash become
        // inert. This separate migration also repairs databases already opened by
        // the v31 release candidate, where editing v31 itself would never rerun.
        version: 32,
        run: (db) => {
          const reason = "legacy_inbound_without_durable_identity_v31_quarantine";
          db.transaction(() => {
            const result = db.prepare(`
          UPDATE inbound_messages
             SET status = 'dropped',
                 error = CASE
                   WHEN error IS NULL OR trim(error) = '' THEN ?
                   ELSE error || ' | ' || ?
                 END,
                 completed_at = COALESCE(completed_at, ?)
           WHERE status IN ('claimed', 'failed')
             AND payload_hash IS NULL
        `).run(reason, reason, (/* @__PURE__ */ new Date()).toISOString());
            const affected = Number(result.changes ?? 0);
            if (affected > 0) {
              db.prepare(`
            INSERT INTO memory_migration_audit
              (migration_version, action, affected_rows, detail_json, created_at)
            VALUES (32, 'quarantine_legacy_unresolved_inbound', ?, ?, ?)
          `).run(
                affected,
                JSON.stringify({
                  statuses: ["claimed", "failed"],
                  missingAuthority: "payload_hash",
                  resultingStatus: "dropped",
                  reason
                }),
                (/* @__PURE__ */ new Date()).toISOString()
              );
            }
          })();
        }
      },
      {
        // COMPOUNDING wave (2026-08-19): the fact store finally gets a real text
        // index. The lexical recall leg was `LOWER(content) LIKE '%tok%'` with no
        // ORDER BY — measured live: 134 candidates ranked per turn, 4.6 shown,
        // relevant facts routinely evicted by common-token matches. Same shape as
        // the shipped vault_chunks_fts (porter unicode61, external-content,
        // trigger-maintained), plus a full backfill of existing rows.
        version: 33,
        sql: `
      CREATE VIRTUAL TABLE IF NOT EXISTS consolidated_facts_fts USING fts5(
        content,
        content='consolidated_facts',
        content_rowid='id',
        tokenize='porter unicode61'
      );

      CREATE TRIGGER IF NOT EXISTS consolidated_facts_ai AFTER INSERT ON consolidated_facts BEGIN
        INSERT INTO consolidated_facts_fts(rowid, content) VALUES (new.id, new.content);
      END;

      CREATE TRIGGER IF NOT EXISTS consolidated_facts_ad AFTER DELETE ON consolidated_facts BEGIN
        INSERT INTO consolidated_facts_fts(consolidated_facts_fts, rowid, content)
        VALUES ('delete', old.id, old.content);
      END;

      CREATE TRIGGER IF NOT EXISTS consolidated_facts_au AFTER UPDATE ON consolidated_facts BEGIN
        INSERT INTO consolidated_facts_fts(consolidated_facts_fts, rowid, content)
        VALUES ('delete', old.id, old.content);
        INSERT INTO consolidated_facts_fts(rowid, content) VALUES (new.id, new.content);
      END;

      INSERT INTO consolidated_facts_fts(rowid, content)
        SELECT id, content FROM consolidated_facts;
    `
      },
      {
        // COMPOUNDING wave: honest recall conversion. The reaper deletes exactly
        // the uncredited runs, so any survivor-based rate reads ~3x too high
        // (measured live: 53.6% shown vs ~17% true). Daily tombstone counters
        // survive the reap; health reports conversion over ALL runs.
        version: 34,
        sql: `
      CREATE TABLE IF NOT EXISTS memory_recall_run_tombstones (
        day        TEXT PRIMARY KEY,
        runs_total INTEGER NOT NULL DEFAULT 0,
        used_total INTEGER NOT NULL DEFAULT 0
      );
    `
      },
      {
        // Terminal-batched semantic learning. Execution truth remains exclusively
        // in harness.db (accepted task, logical settlement and durable result
        // handles); these rows are a restart-safe MEMORY projection of that exact
        // source. The scanner cursor makes terminal discovery bounded without an
        // in-process queue, immutable members are the intake/disposition receipts,
        // and shard leases make extraction retryable without per-tool fan-out.
        version: 35,
        sql: `
      CREATE TABLE IF NOT EXISTS memory_learning_scan_state (
        id                   INTEGER PRIMARY KEY CHECK (id = 1),
        terminal_event_rowid INTEGER NOT NULL DEFAULT 0 CHECK (terminal_event_rowid >= 0),
        updated_at           TEXT NOT NULL
      );
      INSERT OR IGNORE INTO memory_learning_scan_state
        (id, terminal_event_rowid, updated_at)
      VALUES (1, 0, '1970-01-01T00:00:00.000Z');

      CREATE TABLE IF NOT EXISTS memory_learning_batches (
        batch_id             TEXT PRIMARY KEY,
        session_id           TEXT NOT NULL,
        source_user_seq      INTEGER NOT NULL CHECK (source_user_seq > 0),
        accepted_task_id     TEXT NOT NULL,
        terminal_event_id    TEXT NOT NULL,
        terminal_event_rowid INTEGER NOT NULL CHECK (terminal_event_rowid > 0),
        terminal_digest      TEXT NOT NULL CHECK (length(terminal_digest) = 64),
        member_manifest_hash TEXT NOT NULL CHECK (length(member_manifest_hash) = 64),
        member_count         INTEGER NOT NULL CHECK (member_count >= 0),
        shard_count          INTEGER NOT NULL CHECK (shard_count >= 0),
        status               TEXT NOT NULL
          CHECK (status IN ('pending','completed','dead_letter')),
        created_at           TEXT NOT NULL,
        updated_at           TEXT NOT NULL,
        completed_at         TEXT,
        last_error           TEXT,
        UNIQUE (session_id, source_user_seq, accepted_task_id, terminal_event_id)
      );
      CREATE INDEX IF NOT EXISTS idx_memory_learning_batches_status
        ON memory_learning_batches(status, terminal_event_rowid);

      CREATE TABLE IF NOT EXISTS memory_learning_members (
        member_id             TEXT PRIMARY KEY,
        batch_id              TEXT NOT NULL REFERENCES memory_learning_batches(batch_id) ON DELETE CASCADE,
        ordinal               INTEGER NOT NULL CHECK (ordinal >= 0),
        logical_tool_call_id  TEXT NOT NULL,
        result_handle_id      TEXT,
        result_digest         TEXT CHECK (result_digest IS NULL OR length(result_digest) = 64),
        tool_name             TEXT NOT NULL,
        resolved_tool         TEXT,
        outcome_kind          TEXT NOT NULL,
        effect_kind           TEXT,
        disposition           TEXT NOT NULL CHECK (disposition IN (
          'structured_task_evidence','resource_pointer','unstructured',
          'control','failed','write_ack','empty','unavailable'
        )),
        source_text_digest    TEXT CHECK (source_text_digest IS NULL OR length(source_text_digest) = 64),
        source_text_chars     INTEGER CHECK (source_text_chars IS NULL OR source_text_chars >= 0),
        selection_digest      TEXT CHECK (selection_digest IS NULL OR length(selection_digest) = 64),
        selection_chars       INTEGER CHECK (selection_chars IS NULL OR selection_chars >= 0),
        resource_ref          TEXT,
        created_at            TEXT NOT NULL,
        UNIQUE (batch_id, ordinal),
        UNIQUE (batch_id, logical_tool_call_id)
      );
      CREATE INDEX IF NOT EXISTS idx_memory_learning_members_disposition
        ON memory_learning_members(batch_id, disposition, ordinal);

      CREATE TABLE IF NOT EXISTS memory_learning_shards (
        shard_id           TEXT PRIMARY KEY,
        batch_id           TEXT NOT NULL REFERENCES memory_learning_batches(batch_id) ON DELETE CASCADE,
        ordinal            INTEGER NOT NULL CHECK (ordinal >= 0),
        manifest_json      TEXT NOT NULL,
        manifest_hash      TEXT NOT NULL CHECK (length(manifest_hash) = 64),
        reflection_call_id TEXT NOT NULL UNIQUE,
        status             TEXT NOT NULL
          CHECK (status IN ('pending','processing','completed','dead_letter')),
        attempts           INTEGER NOT NULL DEFAULT 0 CHECK (attempts BETWEEN 0 AND 4),
        lease_token        TEXT,
        lease_expires_at   TEXT,
        next_attempt_at    TEXT NOT NULL,
        last_error         TEXT,
        created_at         TEXT NOT NULL,
        updated_at         TEXT NOT NULL,
        completed_at       TEXT,
        UNIQUE (batch_id, ordinal),
        CHECK (
          (status = 'processing' AND lease_token IS NOT NULL AND lease_expires_at IS NOT NULL)
          OR
          (status != 'processing' AND lease_token IS NULL AND lease_expires_at IS NULL)
        )
      );
      CREATE INDEX IF NOT EXISTS idx_memory_learning_shards_drain
        ON memory_learning_shards(status, next_attempt_at, lease_expires_at, created_at);

      CREATE TRIGGER IF NOT EXISTS memory_learning_batch_identity_immutable
      BEFORE UPDATE OF batch_id, session_id, source_user_seq, accepted_task_id,
                       terminal_event_id, terminal_event_rowid, terminal_digest,
                       member_manifest_hash, member_count, shard_count
      ON memory_learning_batches
      BEGIN
        SELECT RAISE(ABORT, 'memory learning batch identity is immutable');
      END;

      CREATE TRIGGER IF NOT EXISTS memory_learning_member_immutable
      BEFORE UPDATE ON memory_learning_members
      BEGIN
        SELECT RAISE(ABORT, 'memory learning member receipt is immutable');
      END;

      CREATE TRIGGER IF NOT EXISTS memory_learning_shard_identity_immutable
      BEFORE UPDATE OF shard_id, batch_id, ordinal, manifest_json,
                       manifest_hash, reflection_call_id, created_at
      ON memory_learning_shards
      BEGIN
        SELECT RAISE(ABORT, 'memory learning shard identity is immutable');
      END;
    `
      },
      {
        // v36 — compiled standing policies become the sole dispatch authority.
        // v28 truthfully separated prompt-only from dispatch-enforced rows, but
        // execution still reparsed fact prose to recover operands. Recompile every
        // active projection into the sealed v2 contract at the Composio adapter
        // edge. Triggers remain conservative: direct SQL writes are prompt-only
        // until a canonical writer compiles and seals the exact content.
        version: 36,
        run: (db) => {
          db.transaction(() => {
            db.exec(`
          DROP TRIGGER IF EXISTS memory_policy_fact_ai;
          DROP TRIGGER IF EXISTS memory_policy_fact_au;

          CREATE TRIGGER memory_policy_fact_ai
          AFTER INSERT ON consolidated_facts
          WHEN NEW.active = 1 AND (NEW.kind = 'constraint' OR NEW.pinned = 1)
          BEGIN
            INSERT OR REPLACE INTO memory_policies
              (fact_id, policy_type, enforcement, applies_to_json, priority, created_at, updated_at)
            VALUES (
              NEW.id,
              CASE WHEN NEW.kind = 'user' THEN 'core_profile' ELSE 'standing_preference' END,
              'prompt',
              '{"schemaVersion":2,"status":"awaiting_canonical_compilation"}',
              CAST(ROUND(COALESCE(NEW.importance, 5) * 10) AS INTEGER),
              NEW.created_at,
              NEW.updated_at
            );
          END;

          CREATE TRIGGER memory_policy_fact_au
          AFTER UPDATE OF active, kind, pinned, importance, content, updated_at ON consolidated_facts
          BEGIN
            DELETE FROM memory_policies WHERE fact_id = NEW.id;
            INSERT INTO memory_policies
              (fact_id, policy_type, enforcement, applies_to_json, priority, created_at, updated_at)
            SELECT NEW.id,
              CASE WHEN NEW.kind = 'user' THEN 'core_profile' ELSE 'standing_preference' END,
              'prompt',
              '{"schemaVersion":2,"status":"awaiting_canonical_compilation"}',
              CAST(ROUND(COALESCE(NEW.importance, 5) * 10) AS INTEGER),
              NEW.created_at,
              NEW.updated_at
            WHERE NEW.active = 1 AND (NEW.kind = 'constraint' OR NEW.pinned = 1);
          END;
        `);
            const rows = db.prepare(`
          SELECT id, kind, content, importance, created_at, updated_at
          FROM consolidated_facts
          WHERE active = 1 AND (kind = 'constraint' OR pinned = 1)
        `).all();
            const upsert = db.prepare(`
          INSERT INTO memory_policies
            (fact_id, policy_type, enforcement, applies_to_json, priority, created_at, updated_at)
          VALUES (?, ?, ?, ?, ?, ?, ?)
          ON CONFLICT(fact_id) DO UPDATE SET
            policy_type = excluded.policy_type,
            enforcement = excluded.enforcement,
            applies_to_json = excluded.applies_to_json,
            priority = excluded.priority,
            updated_at = excluded.updated_at
        `);
            let dispatchPolicies = 0;
            for (const row of rows) {
              const descriptor = row.kind === "constraint" ? compileComposioStandingPolicy(row.content) : compilePromptStandingPolicyDescriptor(
                row.content,
                row.kind === "user" ? "core_profile" : "standing_preference"
              );
              const deterministic = row.kind === "constraint" && descriptor.deterministic;
              if (deterministic) dispatchPolicies += 1;
              upsert.run(
                row.id,
                deterministic ? "hard_constraint" : row.kind === "user" ? "core_profile" : "standing_preference",
                deterministic ? "dispatch" : "prompt",
                JSON.stringify(descriptor),
                Math.round((row.importance ?? 5) * 10),
                row.created_at,
                row.updated_at
              );
            }
            db.prepare(`
          INSERT INTO memory_migration_audit
            (migration_version, action, affected_rows, detail_json, created_at)
          VALUES (36, 'compile_sealed_standing_policy_v2', ?, ?, ?)
        `).run(
              rows.length,
              JSON.stringify({ policiesScanned: rows.length, dispatchPolicies }),
              (/* @__PURE__ */ new Date()).toISOString()
            );
          })();
        }
      }
    ];
    MEMORY_SCHEMA_VERSION = MIGRATIONS.at(-1)?.version ?? 0;
    preMigrationSnapshotOrdinal = 0;
    MEMORY_BACKUP_MIN_SAFETY_MARGIN_BYTES = 64 * 1024 * 1024;
  }
});

// src/runtime/sqlite-statement-cache.ts
var init_sqlite_statement_cache = __esm({
  "src/runtime/sqlite-statement-cache.ts"() {
    "use strict";
  }
});

// src/runtime/action-bus.ts
var import_node_events, ActionBusImpl, actionBus;
var init_action_bus = __esm({
  "src/runtime/action-bus.ts"() {
    "use strict";
    import_node_events = require("node:events");
    ActionBusImpl = class {
      emitter = new import_node_events.EventEmitter();
      constructor() {
        this.emitter.setMaxListeners(64);
      }
      emit(event) {
        try {
          this.emitter.emit("action", event);
        } catch {
        }
      }
      subscribe(listener) {
        const wrapped = (event) => {
          try {
            listener(event);
          } catch {
          }
        };
        this.emitter.on("action", wrapped);
        return () => {
          this.emitter.off("action", wrapped);
        };
      }
    };
    actionBus = new ActionBusImpl();
  }
});

// src/runtime/operational-telemetry.ts
function createOperationalEvent(input) {
  return {
    eventId: input.eventId ?? (0, import_node_crypto5.randomUUID)(),
    ts: (input.now ?? /* @__PURE__ */ new Date()).toISOString(),
    source: input.source,
    type: input.type,
    severity: input.severity ?? "info",
    workspaceId: input.workspaceId,
    workflowRunId: input.workflowRunId,
    workflowNodeRunId: input.workflowNodeRunId,
    sessionId: input.sessionId,
    modelCallId: input.modelCallId,
    toolCallId: input.toolCallId,
    actor: input.actor,
    payload: input.payload ?? {}
  };
}
function openOperationalTelemetryDb() {
  if (cachedDb) return cachedDb;
  ensureStateDir2();
  const db = new import_better_sqlite32.default(OPERATIONAL_TELEMETRY_DB_PATH);
  db.pragma("journal_mode = WAL");
  db.pragma("synchronous = NORMAL");
  db.pragma("busy_timeout = 5000");
  db.exec(OPERATIONAL_TELEMETRY_SCHEMA_SQL);
  cachedDb = db;
  return db;
}
function recordOperationalEvent(input, db) {
  const event = createOperationalEvent(input);
  const externalDb = !!db;
  try {
    writeOperationalEvent(db ?? openOperationalTelemetryDb(), event);
  } catch {
  }
  if (!externalDb) {
    try {
      actionBus.emit({ kind: "operational.event", event });
    } catch {
    }
  }
  return event;
}
function writeOperationalEvent(db, event, ignoreExisting = false) {
  const result = db.prepare(`
    INSERT OR ${ignoreExisting ? "IGNORE" : "REPLACE"} INTO operational_events (
      event_id, ts, source, type, severity, workspace_id, workflow_run_id,
      workflow_node_run_id, session_id, model_call_id, tool_call_id, actor,
      payload_json
    ) VALUES (
      @eventId, @ts, @source, @type, @severity, @workspaceId, @workflowRunId,
      @workflowNodeRunId, @sessionId, @modelCallId, @toolCallId, @actor,
      @payloadJson
    )
  `).run({
    eventId: event.eventId,
    ts: event.ts,
    source: event.source,
    type: event.type,
    severity: event.severity,
    workspaceId: event.workspaceId ?? null,
    workflowRunId: event.workflowRunId ?? null,
    workflowNodeRunId: event.workflowNodeRunId ?? null,
    sessionId: event.sessionId ?? null,
    modelCallId: event.modelCallId ?? null,
    toolCallId: event.toolCallId ?? null,
    actor: event.actor ?? null,
    payloadJson: JSON.stringify(redactPayload(event.payload))
  });
  return result.changes > 0;
}
function ensureStateDir2() {
  if (!(0, import_node_fs3.existsSync)(OPERATIONAL_TELEMETRY_STATE_DIR)) (0, import_node_fs3.mkdirSync)(OPERATIONAL_TELEMETRY_STATE_DIR, { recursive: true });
}
function isUsageMetricKey(key) {
  const normalized = String(key ?? "").replace(/([a-z0-9])([A-Z])/g, "$1_$2").toLowerCase();
  return USAGE_METRIC_KEY_RE.test(normalized);
}
function redactPayload(payload) {
  const preserved = {};
  const rest = {};
  for (const [key, value] of Object.entries(payload)) {
    if (typeof value === "number" && Number.isFinite(value) && isUsageMetricKey(key)) {
      preserved[key] = value;
    } else {
      rest[key] = value;
    }
  }
  const redacted = redactSensitiveValue(rest);
  if (redacted && typeof redacted === "object" && !Array.isArray(redacted)) {
    return { ...redacted, ...preserved };
  }
  return { value: redacted, ...preserved };
}
var import_node_crypto5, import_node_fs3, import_node_path6, import_better_sqlite32, WORKFLOW_OPERATIONAL_EVENT_TYPES, MODEL_OPERATIONAL_EVENT_TYPES, WORKSPACE_OPERATIONAL_EVENT_TYPES, MEMORY_OPERATIONAL_EVENT_TYPES, SAFETY_OPERATIONAL_EVENT_TYPES, TOOL_OPERATIONAL_EVENT_TYPES, HARNESS_OPERATIONAL_EVENT_TYPES, SCHEDULER_OPERATIONAL_EVENT_TYPES, OPERATIONAL_EVENT_TYPES, OPERATIONAL_EVENT_TYPE_SET, OPERATIONAL_TELEMETRY_STATE_DIR, OPERATIONAL_TELEMETRY_DB_PATH, OPERATIONAL_TELEMETRY_SCHEMA_SQL, cachedDb, USAGE_METRIC_KEY_RE;
var init_operational_telemetry = __esm({
  "src/runtime/operational-telemetry.ts"() {
    "use strict";
    import_node_crypto5 = require("node:crypto");
    import_node_fs3 = require("node:fs");
    import_node_path6 = __toESM(require("node:path"), 1);
    import_better_sqlite32 = __toESM(require("better-sqlite3"), 1);
    init_config();
    init_stable_json_digest();
    init_action_bus();
    init_security();
    WORKFLOW_OPERATIONAL_EVENT_TYPES = [
      "workflow_graph_created",
      "workflow_node_ready",
      "workflow_node_started",
      "workflow_node_completed",
      "workflow_node_blocked",
      "workflow_node_failed",
      "workflow_branch_evaluated",
      "workflow_graph_patch_proposed",
      "workflow_graph_patch_applied",
      "workflow_graph_patch_rejected",
      "workflow_checkpoint_created",
      "workflow_rollback_started",
      "workflow_rollback_completed",
      "workflow_trigger_fired",
      "workflow_trigger_deduped",
      // A missed scheduled occurrence was durably admitted but intentionally did
      // not execute: it is waiting for the user's Resume/Skip decision.
      "workflow_catchup_held",
      "workflow_resume_replayed",
      // A workflow step / forEach item failed transiently and is being retried
      // after backoff (mirrored from the runner's step_retry / item_retry events).
      "workflow_node_retried"
    ];
    MODEL_OPERATIONAL_EVENT_TYPES = [
      "model_route_decided",
      "model_call_started",
      "model_call_completed",
      "model_call_failed",
      "model_fallover",
      "brain_auth_dead",
      "brain_silent_cooldown",
      "route_policy_updated"
    ];
    WORKSPACE_OPERATIONAL_EVENT_TYPES = [
      "workspace_created",
      "workspace_file_changed",
      "workspace_data_refresh_started",
      "workspace_data_refresh_completed",
      "workspace_data_refresh_awaiting_approval",
      "workspace_data_refresh_failed",
      "workspace_action_requested",
      "workspace_action_approved",
      "workspace_action_executed",
      "workspace_action_failed",
      "workspace_memory_consolidated"
    ];
    MEMORY_OPERATIONAL_EVENT_TYPES = [
      "episodic_event_recorded",
      "semantic_candidate_extracted",
      "semantic_fact_upserted",
      "memory_consolidation_started",
      "memory_consolidation_completed",
      "memory_conflict_detected",
      "memory_conflict_resolved"
    ];
    SAFETY_OPERATIONAL_EVENT_TYPES = [
      "approval_required",
      "approval_resolved",
      "side_effect_planned",
      "side_effect_executed",
      "side_effect_compensated",
      // A MUTATING external call timed out — the harness stopped waiting (and, when
      // abort-on-timeout is on, cancelled the socket) but the write MAY have landed
      // server-side. Mirrored from harness external_write_orphaned so maybe-landed
      // writes are dashboard-visible, not just in the session ledger.
      "side_effect_orphaned",
      "transaction_guard_opened",
      "transaction_guard_committed",
      "transaction_guard_rolled_back",
      "specular_simulation_started",
      "specular_simulation_completed",
      "specular_simulation_failed",
      // A tool-boundary gate (guardrail) reached a verdict on an action —
      // allowed-with-warning / blocked. Mirrored from harness guardrail_tripped.
      "gate_verdict",
      // An LLM judge reached a verdict on a deliverable / irreversible write
      // (goal-alignment, output-grounding, or a fusion debate/verify checker).
      "judge_verdict"
    ];
    TOOL_OPERATIONAL_EVENT_TYPES = [
      "tool_call_started",
      "tool_call_completed",
      "tool_call_failed",
      "tool_approval_pending",
      // A successful Composio call was SHAPE-classified as a queued async receipt by the
      // family-agnostic generic detector (not a known family). Emitted so the heuristic's
      // precision is watchable in prod — payload carries slug/toolkit/jobId + the outcome
      // (parked | banner). A spike of these on normal completes is the false-positive signal.
      "composio_async_generic_detected"
    ];
    HARNESS_OPERATIONAL_EVENT_TYPES = [
      "harness_turn_started",
      "harness_turn_completed",
      "harness_run_completed",
      "harness_run_failed",
      "turn_graph_shadow_compiled",
      "worker_spawned",
      "worker_queued",
      "worker_completed",
      "worker_failed",
      "worker_capped",
      // A configured non-Claude WORKER model was ignored because the SDK-brain
      // cross-provider lane is off (CLEMMY_SDK_BRAIN_CROSS_WORKER=off) — a visible
      // warning instead of a silent fallback to the Claude brain model.
      "worker_model_ignored",
      "auto_continue",
      "background_task_created",
      "background_task_started",
      "background_task_finished",
      "background_task_parked",
      "background_self_resume_check",
      // B7b (2026-08-26): the approval-reaper gave a permanently-held revoked
      // invocation its terminal disposition after bounded rescans — quarantined,
      // no longer re-attempted every tick. Payload carries the exact identity and
      // the settlement refusal reason.
      "host_tool_recovery_abandoned"
    ];
    SCHEDULER_OPERATIONAL_EVENT_TYPES = [
      "cron_job_started",
      "cron_job_completed",
      "cron_job_failed"
    ];
    OPERATIONAL_EVENT_TYPES = [
      ...WORKFLOW_OPERATIONAL_EVENT_TYPES,
      ...MODEL_OPERATIONAL_EVENT_TYPES,
      ...WORKSPACE_OPERATIONAL_EVENT_TYPES,
      ...MEMORY_OPERATIONAL_EVENT_TYPES,
      ...SAFETY_OPERATIONAL_EVENT_TYPES,
      ...TOOL_OPERATIONAL_EVENT_TYPES,
      ...HARNESS_OPERATIONAL_EVENT_TYPES,
      ...SCHEDULER_OPERATIONAL_EVENT_TYPES
    ];
    OPERATIONAL_EVENT_TYPE_SET = new Set(OPERATIONAL_EVENT_TYPES);
    OPERATIONAL_TELEMETRY_STATE_DIR = import_node_path6.default.join(BASE_DIR, "state");
    OPERATIONAL_TELEMETRY_DB_PATH = import_node_path6.default.join(OPERATIONAL_TELEMETRY_STATE_DIR, "operational-telemetry.db");
    OPERATIONAL_TELEMETRY_SCHEMA_SQL = `
CREATE TABLE IF NOT EXISTS operational_events (
  event_id             TEXT PRIMARY KEY,
  ts                   TEXT NOT NULL,
  source               TEXT NOT NULL,
  type                 TEXT NOT NULL,
  severity             TEXT NOT NULL CHECK (severity IN ('debug','info','warn','error')),
  workspace_id         TEXT,
  workflow_run_id      TEXT,
  workflow_node_run_id TEXT,
  session_id           TEXT,
  model_call_id        TEXT,
  tool_call_id         TEXT,
  actor                TEXT,
  payload_json         TEXT NOT NULL DEFAULT '{}'
);

CREATE INDEX IF NOT EXISTS idx_operational_events_ts
  ON operational_events(ts DESC);
CREATE INDEX IF NOT EXISTS idx_operational_events_source_type_ts
  ON operational_events(source, type, ts DESC);
CREATE INDEX IF NOT EXISTS idx_operational_events_workspace_ts
  ON operational_events(workspace_id, ts DESC) WHERE workspace_id IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_operational_events_workflow_ts
  ON operational_events(workflow_run_id, ts DESC) WHERE workflow_run_id IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_operational_events_session_ts
  ON operational_events(session_id, ts DESC) WHERE session_id IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_operational_events_model_call
  ON operational_events(model_call_id) WHERE model_call_id IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_operational_events_tool_call
  ON operational_events(tool_call_id) WHERE tool_call_id IS NOT NULL;
`;
    cachedDb = null;
    USAGE_METRIC_KEY_RE = /^(?:input|cached_input|output|reasoning|total|prompt|completion|cache_read_input|cache_creation_input)_tokens$|^tokens?(?:_used|_count|_remaining)?$/;
  }
});

// src/runtime/harness/settled-read-replay-semantics.ts
var init_settled_read_replay_semantics = __esm({
  "src/runtime/harness/settled-read-replay-semantics.ts"() {
    "use strict";
  }
});

// src/runtime/harness/eventlog-operational-mirror.ts
var init_eventlog_operational_mirror = __esm({
  "src/runtime/harness/eventlog-operational-mirror.ts"() {
    "use strict";
    init_config();
    init_operational_telemetry();
    init_settled_read_replay_semantics();
  }
});

// src/runtime/audit-ledger.ts
var import_node_path7, import_pino, logger, AUDIT_DIR;
var init_audit_ledger = __esm({
  "src/runtime/audit-ledger.ts"() {
    "use strict";
    import_node_path7 = __toESM(require("node:path"), 1);
    import_pino = __toESM(require("pino"), 1);
    init_config();
    logger = (0, import_pino.default)({ name: "clementine.audit-ledger" });
    AUDIT_DIR = import_node_path7.default.join(BASE_DIR, "audit");
  }
});

// src/runtime/harness/envelope-narration.ts
var init_envelope_narration = __esm({
  "src/runtime/harness/envelope-narration.ts"() {
    "use strict";
  }
});

// src/runtime/harness/tool-narration-shapes.ts
var init_tool_narration_shapes = __esm({
  "src/runtime/harness/tool-narration-shapes.ts"() {
    "use strict";
  }
});

// src/runtime/harness/presentation-hygiene.ts
var init_presentation_hygiene = __esm({
  "src/runtime/harness/presentation-hygiene.ts"() {
    "use strict";
  }
});

// src/runtime/harness/typed-control-state.ts
var init_typed_control_state = __esm({
  "src/runtime/harness/typed-control-state.ts"() {
    "use strict";
  }
});

// src/runtime/harness/turn-outcome.ts
var init_turn_outcome = __esm({
  "src/runtime/harness/turn-outcome.ts"() {
    "use strict";
    init_envelope_narration();
    init_tool_narration_shapes();
    init_presentation_hygiene();
    init_typed_control_state();
  }
});

// src/runtime/harness/destination-gate.ts
var init_destination_gate = __esm({
  "src/runtime/harness/destination-gate.ts"() {
    "use strict";
    init_config();
  }
});

// src/runtime/harness/callable-contract.ts
var init_callable_contract = __esm({
  "src/runtime/harness/callable-contract.ts"() {
    "use strict";
  }
});

// src/runtime/harness/logical-call-contract.ts
var init_logical_call_contract = __esm({
  "src/runtime/harness/logical-call-contract.ts"() {
    "use strict";
    init_callable_contract();
    init_tool_effect();
  }
});

// src/runtime/harness/runtime-tool-identity.ts
var init_runtime_tool_identity = __esm({
  "src/runtime/harness/runtime-tool-identity.ts"() {
    "use strict";
  }
});

// src/runtime/harness/async-read-continuation-contract.ts
var init_async_read_continuation_contract = __esm({
  "src/runtime/harness/async-read-continuation-contract.ts"() {
    "use strict";
  }
});

// src/runtime/harness/capability-manifest-store.ts
var init_capability_manifest_store = __esm({
  "src/runtime/harness/capability-manifest-store.ts"() {
    "use strict";
    init_capability_manifest();
    init_eventlog();
  }
});

// src/runtime/harness/sealed-node-binding-digest.ts
var init_sealed_node_binding_digest = __esm({
  "src/runtime/harness/sealed-node-binding-digest.ts"() {
    "use strict";
  }
});

// src/runtime/harness/accepted-source-catalog-scope.ts
var import_node_async_hooks2, acceptedSourceCatalogScopeStorage;
var init_accepted_source_catalog_scope = __esm({
  "src/runtime/harness/accepted-source-catalog-scope.ts"() {
    "use strict";
    import_node_async_hooks2 = require("node:async_hooks");
    acceptedSourceCatalogScopeStorage = new import_node_async_hooks2.AsyncLocalStorage();
  }
});

// src/runtime/harness/host-capability-catalog-factory.ts
var init_host_capability_catalog_factory = __esm({
  "src/runtime/harness/host-capability-catalog-factory.ts"() {
    "use strict";
    init_eventlog();
    init_logical_call_contract();
    init_runtime_tool_identity();
    init_capability_manifest();
    init_mutation_verification_contract();
    init_async_read_continuation_contract();
    init_capability_manifest_store();
    init_sealed_node_binding_digest();
    init_accepted_source_catalog_scope();
  }
});

// src/runtime/mcp-declared-effects.ts
var init_mcp_declared_effects = __esm({
  "src/runtime/mcp-declared-effects.ts"() {
    "use strict";
  }
});

// src/runtime/harness/current-manifest-operation-semantics.ts
var init_current_manifest_operation_semantics = __esm({
  "src/runtime/harness/current-manifest-operation-semantics.ts"() {
    "use strict";
    init_host_capability_catalog_factory();
    init_runtime_tool_identity();
  }
});

// src/runtime/harness/execution-gate.ts
var init_execution_gate = __esm({
  "src/runtime/harness/execution-gate.ts"() {
    "use strict";
    init_mcp_declared_effects();
    init_current_manifest_operation_semantics();
    init_runtime_tool_identity();
  }
});

// src/tools/call-tool-alias.ts
var init_call_tool_alias = __esm({
  "src/tools/call-tool-alias.ts"() {
    "use strict";
  }
});

// src/runtime/harness/tool-effect.ts
var REGISTRY_EFFECTS, REGISTRY_RUNTIME_EFFECTS;
var init_tool_effect = __esm({
  "src/runtime/harness/tool-effect.ts"() {
    "use strict";
    init_tool_registry();
    init_destination_gate();
    init_host_capability_catalog_factory();
    init_execution_gate();
    init_call_tool_alias();
    init_runtime_tool_identity();
    REGISTRY_EFFECTS = new Map(
      TOOL_REGISTRY.map((decl) => [decl.name, decl.sideEffect])
    );
    REGISTRY_RUNTIME_EFFECTS = new Map(
      TOOL_REGISTRY.flatMap((decl) => decl.runtimeEffect ? [[decl.name, decl.runtimeEffect]] : [])
    );
  }
});

// src/runtime/harness/public-presentation.ts
var init_public_presentation = __esm({
  "src/runtime/harness/public-presentation.ts"() {
    "use strict";
    init_envelope_narration();
    init_turn_outcome();
    init_tool_narration_shapes();
    init_presentation_hygiene();
    init_tool_effect();
    init_settled_read_replay_semantics();
  }
});

// src/assistant/request-segments.ts
var RESULT_REFERENCE_SOURCE, RESULT_MUTATION_SOURCE, ARTIFACT_CONSTRUCTION_SOURCE, RESULT_DELIVERY_SOURCE, IMPLICIT_PIPELINE_BOUNDARY_RE, RESULT_ACTION_SEGMENT_RE;
var init_request_segments = __esm({
  "src/assistant/request-segments.ts"() {
    "use strict";
    RESULT_REFERENCE_SOURCE = "(?:it|them|these|those|the\\s+(?:outputs?|results?|selection))";
    RESULT_MUTATION_SOURCE = `(?:add|append|copy|delete|edit|insert|modify|move|place|put|remove|replace|save|update|upload)\\s+${RESULT_REFERENCE_SOURCE}\\b`;
    ARTIFACT_CONSTRUCTION_SOURCE = "(?:assemble|author|build|create|draft|generate|make|prepare|produce|write)\\s+(?:me\\s+)?(?:a|an|another|new|the)\\b";
    RESULT_DELIVERY_SOURCE = [
      `send\\s+(?:(?:me|us|him|her|them)\\b|${RESULT_REFERENCE_SOURCE}\\b)`,
      "(?:dm|email|message|notify)\\s+(?:me|us|him|her|them)\\b"
    ].join("|");
    IMPLICIT_PIPELINE_BOUNDARY_RE = new RegExp(
      // Do not split an infinitive/modal discussion ("how to put them").
      `(?<!\\bto)(?<!\\bcan)(?<!\\bmay)(?<!\\bwill)(?<!\\bcould)(?<!\\bshould)(?<!\\bwould)(?:\\s+(?=${RESULT_MUTATION_SOURCE})|\\s+(?:and\\s+then|and|then)\\s+(?=(?:${RESULT_MUTATION_SOURCE}|${ARTIFACT_CONSTRUCTION_SOURCE}|${RESULT_DELIVERY_SOURCE})))`,
      "i"
    );
    RESULT_ACTION_SEGMENT_RE = new RegExp(
      `^(?:${RESULT_MUTATION_SOURCE}|${ARTIFACT_CONSTRUCTION_SOURCE}|${RESULT_DELIVERY_SOURCE})`,
      "i"
    );
  }
});

// src/assistant/external-effect-taxonomy.ts
function publicationRules(bodies) {
  return bodies.map((body) => ({
    kind: "publication",
    pattern: new RegExp(`^(?:${body})`, "i")
  }));
}
var TECHNICAL_COMMUNICATION_OBJECT_SOURCE, TECHNICAL_SEND_TARGET_SOURCE, TECHNICAL_CALL_TARGET_SOURCE, TECHNICAL_CALL_INVOCATION_RE, EXTERNAL_EFFECT_RULES;
var init_external_effect_taxonomy = __esm({
  "src/assistant/external-effect-taxonomy.ts"() {
    "use strict";
    init_request_segments();
    TECHNICAL_COMMUNICATION_OBJECT_SOURCE = "address|api|body|column|command|endpoint|field|function|helper|method|property|schema|script|subject|template|tool|type|variable";
    TECHNICAL_SEND_TARGET_SOURCE = `${TECHNICAL_COMMUNICATION_OBJECT_SOURCE}|callback|class|database|db|host|module|object|parameter|queue|server|service`;
    TECHNICAL_CALL_TARGET_SOURCE = [
      String.raw`[A-Za-z][A-Za-z0-9_-]*(?:__|:)[A-Za-z0-9_.:-]+`,
      // A bare dotted handle can name a real contact (alice.smith). Only treat
      // dotted syntax as callable when its namespace is itself technical.
      String.raw`(?:api|calendar|client|composio|database|db|mcp|provider|sdk|service|tool|tools)\.[A-Za-z_][A-Za-z0-9_]*`,
      String.raw`[A-Za-z][A-Za-z0-9_.]*\s*\(`,
      String.raw`[A-Za-z][A-Za-z0-9_]*_(?:tool|tools|function|method|handler|command)\b`,
      String.raw`\`[^\`\r\n]+\``
    ].join("|");
    TECHNICAL_CALL_INVOCATION_RE = new RegExp(
      `\\bcall\\s+(?:${TECHNICAL_CALL_TARGET_SOURCE})`,
      "gi"
    );
    EXTERNAL_EFFECT_RULES = [
      {
        kind: "communication",
        pattern: new RegExp(
          `^send\\b[^.!?\\n;]{0,140}\\bto\\s+(?!(?:(?:my|our|the|this|that|your)\\s+)?(?:${TECHNICAL_SEND_TARGET_SOURCE})\\b)(?!(?:${TECHNICAL_CALL_TARGET_SOURCE}))(?:(?:my|our|the|this|that|your)\\s+)?\\S+`,
          "i"
        )
      },
      {
        kind: "communication",
        // Recipient-first delivery is the same external topology as a delivery
        // with a trailing recipient. Recognize its grammar without coupling the
        // classifier to a particular payload or provider vocabulary.
        pattern: /^send\s+(?:me|us|him|her|them)\s+(?!(?:to|toward|towards)\b)(?:(?:an?|the|this|that|these|those)\s+\S+|\S+)/i
      },
      {
        kind: "communication",
        pattern: new RegExp(
          `^(?:email|message|dm|notify)\\s+(?!(?:(?:my|our|the|this|that|your)\\s+)?(?:${TECHNICAL_COMMUNICATION_OBJECT_SOURCE})\\b)(?:(?:my|our|the|this|that|your)\\s+)?\\S+`,
          "i"
        )
      },
      {
        kind: "communication",
        pattern: /^(?:reply\s+to\s+\S+|forward\b[^.!?\n;]{0,100}\bto\b)/i
      },
      {
        kind: "communication",
        pattern: new RegExp(
          `^call\\s+(?!(?:(?:my|our|the|this|that|your)\\s+)?(?:${TECHNICAL_COMMUNICATION_OBJECT_SOURCE})\\b)(?!(?:${TECHNICAL_CALL_TARGET_SOURCE}))(?:(?:my|our|the|this|that|your)\\s+)?\\S+`,
          "i"
        )
      },
      {
        kind: "meeting_change",
        pattern: /^(?:book|cancel)\s+(?:(?:a|an|my|our|the|this|that|your)\s+)?(?:appointment|event|meeting|reservation)\b/i
      },
      {
        kind: "financial_transaction",
        pattern: /^(?:charge|pay|refund)\s+(?:(?:a|an|my|our|the|this|that|your)\s+)?(?:account|bill|card|charge|client|customer|invoice|order|payment|subscription|transaction|\$?\d)\b/i
      },
      {
        kind: "subscription_change",
        pattern: /^(?:subscribe|unsubscribe)\b[^.!?\n;]{0,100}\b(?:from|to)\b/i
      },
      {
        kind: "external_comment",
        pattern: /^comment\s+on\s+(?:(?:a|an|my|our|the|this|that|your)\s+)?(?:issue|post|pull\s+request|pr|thread|ticket)\b/i
      },
      {
        kind: "invitation_response",
        pattern: /^(?:rsvp\b(?!\s+(?:guide|instructions?|policy|process)\b)|(?:accept|decline|reject)\s+(?:(?:a|an|my|our|the|this|that|your)\s+)?(?:calendar\s+)?(?:invitation|invite)\b)/i
      },
      {
        kind: "social_reaction",
        pattern: /^(?:like|unlike|react\s+to)\s+(?:(?:a|my|our|the|this|that|your)\s+)?(?:(?:@[\w.-]+|[\w.-]+'s|facebook|instagram|linkedin|slack|tiktok|twitter|x)\s+){0,3}(?:it|message|photo|post|story|thread|tweet|update|video|this|that)\b/i
      },
      {
        kind: "social_repost",
        pattern: /^(?:quote\s+tweet|repost|retweet)\b(?:\s+\S+)?/i
      },
      {
        kind: "social_follow",
        pattern: /^(?:follow|unfollow)\s+(?!up\b)(?![^.!?\n;]{0,60}\b(?:directions?|guide|instructions?|path|plan|procedure|process|steps?)\b)\S+/i
      },
      {
        kind: "commerce",
        pattern: /^(?:buy|purchase)\s+(?!(?:time|the\s+(?:argument|idea|premise|story))\b)\S+/i
      },
      {
        kind: "commerce",
        pattern: /^(?:order|place\s+(?:an?|the)\s+order(?:\s+for)?)\s+(?!(?:(?:a|an|the|this|that)\s+)?(?:array|collection|data|items?|list|records?|results?|rows?|set)\b)(?!by\b)\S+/i
      },
      {
        kind: "reservation",
        pattern: /^reserve\s+(?:(?:a|an|my|our|the|this|that|your)\s+)?(?:appointment|booking|car|flight|hotel|rental|reservation|room|seat|table|tickets?|it|this|that)\b/i
      },
      {
        kind: "reservation",
        pattern: /^(?:(?:book|cancel)\s+(?:(?:a|an|my|our|the|this|that|your)\s+)?(?:booking|flight|hotel|reservation|room|table)\b|make\s+(?:(?:a|an|my|our|the|this|that|your)\s+)?reservation\b(?!\s+(?:component|fixture|mock|object|schema|test|type|widget)\b))/i
      },
      {
        kind: "document_signature",
        pattern: /^(?:e-?sign|sign)\s+(?:(?:a|an|my|our|the|this|that|your)\s+)?(?:docusign(?:\s+(?:agreement|document|envelope|form))?|agreement|contract|docusign\s+envelope|e-?signature\s+document|waiver)\b/i
      },
      {
        kind: "code_host_change",
        pattern: /^(?:approve|merge)\s+(?:(?:a|an|my|our|the|this|that|your)\s+)?(?:pull\s+request|pr)\b/i
      },
      {
        kind: "code_host_change",
        pattern: /^(?:star|unstar)\s+(?:(?:a|an|my|our|the|this|that|your)\s+)?(?:repository|repo)\b/i
      },
      {
        kind: "work_item_change",
        pattern: /^(?:close|reopen)\s+(?:(?:a|an|my|our|the|this|that|your)\s+)?(?:issue|ticket)\b/i
      },
      {
        kind: "work_item_change",
        pattern: /^assign\s+(?:(?:a|an|my|our|the|this|that|your)\s+)?(?:issue|ticket)\b/i
      },
      {
        kind: "poll_vote",
        pattern: /^(?:cast\s+(?:(?:my|our|the|this|that|your)\s+)?vote|vote)\b[^.!?\n;]{0,120}\bpoll\b/i
      },
      {
        kind: "flight_check_in",
        pattern: /^check\s+(?:(?:me|us)\s+)?in\b[^.!?\n;]{0,120}\bflight\b/i
      },
      {
        kind: "calendar_change",
        pattern: /^(?:add|create|put|save|schedule)\b[^.!?\n;]{0,120}\b(?:in|into|on|to)\s+(?:(?:my|our|the|your)\s+)?calendar\b/i
      },
      // PUBLICATION — making something available beyond this machine.
      //
      // Hosting, publishing, and deploying are external effects by their nature:
      // they stand a resource up somewhere other people can reach, on an account
      // that is not free to mutate. These key on the VERB plus an externality cue
      // rather than on any product noun — no provider name is required and none is
      // privileged.
      //
      // These remain clause-anchored like every family above. Blindly splitting
      // "and deploy" loses the outer clause's advice, question, or negation scope.
      // A missed early publication hint is recovered when the actual effect tool
      // is selected; a false hint could incorrectly pre-arm the whole turn.
      ...publicationRules([
        // "host it somewhere". Excludes the networking senses (host header/name).
        String.raw`host\s+(?!(?:header|key|machine|name|names|options?|os|process|status|strateg(?:y|ies)|system)\b)(?!(?:\S+\s+){0,5}?(?:locally|offline|on\s+(?:this|the)\s+(?:computer|machine))\b)(?:(?:a|an|it|my|our|the|this|that|your)\s+)?\S+`,
        // "deploy it", "publish the report". Excludes talking ABOUT deployment.
        String.raw`(?:deploy|publish)\s+(?!(?:approaches?|concepts?|docs?|documentation|guide|notes?|options?|plans?|process|status|strateg(?:y|ies))\b)(?:(?:a|an|it|my|our|the|this|that|your)\s+)?\S+`,
        // "put it online", "stand it up on a server".
        String.raw`(?:put|stand)\s+(?:\S+\s+){0,6}?(?:online|live|up\s+(?:on|at)|on\s+(?:a\s+)?(?:server|the\s+(?:internet|web)))\b`,
        // "make it public/live" or "make it available to the team". Merely
        // making something available locally/offline is a local action, not a
        // publication request.
        String.raw`make\s+(?![^.!?\n;]{0,80}\b(?:locally|offline|on\s+(?:this|the)\s+(?:computer|machine))\b)(?:\S+\s+){0,6}?(?:(?:public|live|online)\b|(?:available|accessible)\b[^.!?\n;]{0,40}\b(?:for|on|to)\s+(?!(?:local(?:host)?|(?:this|the)\s+(?:computer|machine))\b)\S+)`,
        // "share it with the team" — a RESOURCE with people, not an opinion.
        String.raw`share\s+(?!(?:your|my|our|a|an|the)?\s*(?:thought|thoughts|opinion|opinions|view|views|feedback|perspective)\b)(?:\S+\s+){0,6}?\bwith\s+\S+`
      ])
    ];
  }
});

// src/assistant/project-shape.ts
var DURABLE_ARTIFACT_OBJECT_SOURCE, BOUNDED_SUBARTIFACT_SOURCE, DURABLE_ARTIFACT_MODIFIER_SOURCE, DURABLE_ARTIFACT_OBJECT_RE, MAKE_DURABLE_ARTIFACT_OBJECT_RE, COMPOUND_DURABLE_ARTIFACT_RE;
var init_project_shape = __esm({
  "src/assistant/project-shape.ts"() {
    "use strict";
    DURABLE_ARTIFACT_OBJECT_SOURCE = "admin\\s+console|app(?:lication)?|automation|backend|book|course|crm|dashboard|database|data\\s+pipeline|hub|integration|knowledge\\s+base|platform|portal|service|site|system|web\\s+app|website|workflow|api";
    BOUNDED_SUBARTIFACT_SOURCE = "button|class|component|endpoint|field|footer|form|function|header|method|module|page|property|screen|section|table|type|widget";
    DURABLE_ARTIFACT_MODIFIER_SOURCE = "admin|client|company|custom|customer|internal|local-only|marketing|new|onboarding|operations|private|production|public|research|sales|shareable|team";
    DURABLE_ARTIFACT_OBJECT_RE = new RegExp(
      `\\b(?:assemble|author|automate|build|create|design|develop|generate|implement|migrate|prepare|produce|put\\s+together|redesign|scaffold|set\\s+up|stand\\s+up|write)\\s+(?:me\\s+)?(?:(?:a|an|my|our|the|this|that|your)\\s+)?(?:(?:${DURABLE_ARTIFACT_MODIFIER_SOURCE})\\s+){0,2}(?:${DURABLE_ARTIFACT_OBJECT_SOURCE})\\b(?!\\s+(?:${BOUNDED_SUBARTIFACT_SOURCE})\\b)`,
      "i"
    );
    MAKE_DURABLE_ARTIFACT_OBJECT_RE = new RegExp(
      `\\bmake\\s+(?:me\\s+)(?:(?:a|an|my|our|the|this|that|your)\\s+)?(?:(?:${DURABLE_ARTIFACT_MODIFIER_SOURCE})\\s+){0,2}(?:${DURABLE_ARTIFACT_OBJECT_SOURCE})\\b(?!\\s+(?:${BOUNDED_SUBARTIFACT_SOURCE})\\b)`,
      "i"
    );
    COMPOUND_DURABLE_ARTIFACT_RE = new RegExp(
      `(?:\\+|\\b(?:after\\s+that|and\\s+then|followed\\s+by|then)\\b)\\s*(?:(?:a|an|my|our|the|this|that|your)\\s+)?(?:(?:${DURABLE_ARTIFACT_MODIFIER_SOURCE})\\s+){0,2}(?:${DURABLE_ARTIFACT_OBJECT_SOURCE})\\b(?!\\s+(?:${BOUNDED_SUBARTIFACT_SOURCE})\\b)`,
      "i"
    );
  }
});

// src/assistant/message-intent.ts
function escapedCue(cue) {
  return cue.replace(/[.*+?^${}()|[\]\\]/g, "\\$&").replace(/\s+/g, "\\s+");
}
function actionContinuationPattern(includePlainAnd) {
  const transition = includePlainAnd ? "(?:and\\s+then|then|but|and)" : "(?:and\\s+then|then|but)";
  return new RegExp(
    `(?:(?:[,;:.!?\\n]|[\u2014\u2013])\\s*(?:please\\s+)?(?:${ACTION_CONTINUATION_CUE_SOURCE})\\b|(?:[,;:.!?\\n]|[\u2014\u2013]|\\b)\\s*${transition}\\s+(?:please\\s+)?(?:${ACTION_CONTINUATION_CUE_SOURCE})\\b)`,
    "i"
  );
}
var MUTATION_ACTION_CUES, AMBIGUOUS_ACTION_NOUNS, ACTION_CUES, ACTION_CONTINUATION_CUE_SOURCE, DIRECT_ACTION_AFTER_ACK_RE, ACTION_CONTINUATION_RE, ACTION_CONTINUATION_WITH_AND_RE;
var init_message_intent = __esm({
  "src/assistant/message-intent.ts"() {
    "use strict";
    init_external_effect_taxonomy();
    init_project_shape();
    init_request_segments();
    MUTATION_ACTION_CUES = [
      "accept",
      "add",
      "append",
      "approve",
      "archive",
      "assign",
      "block",
      "book",
      "bookmark",
      "build",
      "call",
      "cancel",
      "change",
      "charge",
      "close",
      "comment",
      "configure",
      "copy",
      "create",
      "delete",
      "deploy",
      "draft",
      "edit",
      "email",
      "execute",
      "follow",
      "forward",
      "generate",
      "insert",
      "install",
      "invite",
      "label",
      "launch",
      "link",
      "log",
      "make",
      "mark complete",
      "merge",
      "mark",
      "message",
      "modify",
      "move",
      "mute",
      "open",
      "pay",
      "pin",
      "post",
      "publish",
      "push",
      "put",
      "react",
      "record",
      "refund",
      "reject",
      "remove",
      "rename",
      "reopen",
      "replace",
      "reply",
      "reschedule",
      "restore",
      "run",
      "save",
      "schedule",
      "send",
      "set",
      "set up",
      "setup",
      "share",
      "star",
      "submit",
      "subscribe",
      "tag",
      "unarchive",
      "unblock",
      "unfollow",
      "unlink",
      "unmute",
      "unsubscribe",
      "update",
      "upload",
      "write"
    ];
    AMBIGUOUS_ACTION_NOUNS = /* @__PURE__ */ new Set([
      "block",
      "book",
      "bookmark",
      "call",
      "charge",
      "draft",
      "email",
      "label",
      "link",
      "log",
      "mark",
      "message",
      "post",
      "record",
      "schedule",
      "tag"
    ]);
    ACTION_CUES = [
      ...MUTATION_ACTION_CUES.filter((cue) => !AMBIGUOUS_ACTION_NOUNS.has(cue)),
      "produce",
      "prepare",
      "redesign",
      "author",
      "assemble",
      "automate",
      "monitor",
      "wire up",
      "wire",
      "implement",
      "design",
      "migrate",
      "refactor",
      "rewrite",
      "connect",
      "integrate",
      "ship",
      "release",
      "fix",
      "resolve",
      "patch",
      "debug",
      "finish",
      "complete",
      "continue",
      "get back to",
      "keep going",
      "pick up",
      "pick this up",
      "resume",
      "improve",
      "enhance",
      "optimize",
      "notify",
      "dm",
      "clean up",
      "let's",
      "lets do",
      "go ahead",
      "do it"
    ];
    ACTION_CONTINUATION_CUE_SOURCE = [...ACTION_CUES, ...AMBIGUOUS_ACTION_NOUNS].map(escapedCue).join("|");
    DIRECT_ACTION_AFTER_ACK_RE = new RegExp(
      `^(?:(?:please|kindly)\\s+|(?:can|could|would|will)\\s+you\\s+|(?:go\\s+ahead\\s+and|make\\s+sure\\s+to|let'?s)\\s+)*(?:${ACTION_CONTINUATION_CUE_SOURCE})\\b`,
      "i"
    );
    ACTION_CONTINUATION_RE = actionContinuationPattern(false);
    ACTION_CONTINUATION_WITH_AND_RE = actionContinuationPattern(true);
  }
});

// src/runtime/harness/json-repair.ts
var init_json_repair = __esm({
  "src/runtime/harness/json-repair.ts"() {
    "use strict";
  }
});

// src/runtime/harness/multi-item-intent.ts
var FIELD_ITEM, FIELD_LIST_TO_CONTAINER_RE, NO_MULTI_ITEM;
var init_multi_item_intent = __esm({
  "src/runtime/harness/multi-item-intent.ts"() {
    "use strict";
    FIELD_ITEM = String.raw`(?:the\s+)?(?!and\b|or\b)[a-z][a-z'-]+(?:s)?`;
    FIELD_LIST_TO_CONTAINER_RE = new RegExp(
      String.raw`\b(?:add|put|place|save|drop)\s+${FIELD_ITEM}(?:(?:\s*,\s*${FIELD_ITEM})+\s*,?\s+and\s+|\s+and\s+)${FIELD_ITEM}\s+(?:in(?:to)?|to|on)\s+(?:a|an|one|the|my|our|single)\s+[a-z][\w-]*`,
      "i"
    );
    NO_MULTI_ITEM = Object.freeze({
      isMultiItem: false,
      itemCount: 0,
      itemKind: null,
      sameShapeWork: false,
      explicitParallelRequest: false
    });
  }
});

// src/runtime/graph/turn-graph-obligations.ts
var init_turn_graph_obligations = __esm({
  "src/runtime/graph/turn-graph-obligations.ts"() {
    "use strict";
  }
});

// src/runtime/graph/accepted-goal.ts
var FIELD_LIST_RE;
var init_accepted_goal = __esm({
  "src/runtime/graph/accepted-goal.ts"() {
    "use strict";
    init_external_effect_taxonomy();
    FIELD_LIST_RE = new RegExp(
      String.raw`\b(?:add|put|place|save|drop|include|with)\s+(?:the\s+)?(?!and\b|or\b)([a-z][a-z'-]+)(?:(?:\s*,\s*(?:the\s+)?(?!and\b|or\b)([a-z][a-z'-]+))+\s*,?\s+and\s+|\s+and\s+)(?:the\s+)?(?!and\b|or\b)([a-z][a-z'-]+)`,
      "i"
    );
  }
});

// src/runtime/graph/admitted-turn-semantics.ts
var init_admitted_turn_semantics = __esm({
  "src/runtime/graph/admitted-turn-semantics.ts"() {
    "use strict";
  }
});

// src/runtime/graph/work-topology.ts
function plainRecord(value) {
  return Boolean(value && typeof value === "object" && !Array.isArray(value));
}
function exactKeys3(value, allowed, label, errors) {
  const allow = new Set(allowed);
  for (const key of Object.keys(value)) {
    if (!allow.has(key)) errors.push(`${label} contains unknown field ${key}`);
  }
}
function validId(value, label, errors) {
  if (typeof value !== "string" || !ID_PATTERN.test(value)) {
    errors.push(`${label} must be a bounded stable id`);
    return false;
  }
  return true;
}
function isBoundedWorkTopologyJsonPointer(value) {
  return typeof value === "string" && value.length <= 512 && (value === "" || value.startsWith("/")) && !/(?:~(?![01]))/.test(value);
}
function normalizeProposedOperation(raw) {
  const next = { ...raw };
  if (next.effect !== "read" && Object.prototype.hasOwnProperty.call(next, "coverage")) {
    delete next.coverage;
  }
  const asIds = (value) => Array.isArray(value) ? value.filter((entry) => typeof entry === "string" && entry.trim().length > 0) : null;
  const dataFrom = asIds(next.dataFrom);
  if (!dataFrom || dataFrom.length === 0) return next;
  const dependsOn = asIds(next.dependsOn);
  if (next.dependsOn !== void 0 && dependsOn === null) return next;
  next.dependsOn = [.../* @__PURE__ */ new Set([...dependsOn ?? [], ...dataFrom])];
  if (next.effect === "read") next.dataFrom = [];
  return next;
}
function stringList(value, label, errors) {
  if (!Array.isArray(value)) {
    errors.push(`${label} must be an array`);
    return null;
  }
  const result = [];
  for (const [index, entry] of value.entries()) {
    if (!validId(entry, `${label}[${index}]`, errors)) continue;
    result.push(entry);
  }
  if (new Set(result).size !== result.length) errors.push(`${label} contains duplicate ids`);
  return [...result].sort();
}
function hasCycle(operations) {
  const byId = new Map(operations.map((operation) => [operation.id, operation]));
  const visiting = /* @__PURE__ */ new Set();
  const visited = /* @__PURE__ */ new Set();
  const visit = (id) => {
    if (visiting.has(id)) return true;
    if (visited.has(id)) return false;
    visiting.add(id);
    for (const dependency of byId.get(id)?.dependsOn ?? []) {
      if (visit(dependency)) return true;
    }
    visiting.delete(id);
    visited.add(id);
    return false;
  };
  return operations.some((operation) => visit(operation.id));
}
function validateWorkTopology(value) {
  const errors = [];
  if (!plainRecord(value)) return { ok: false, errors: ["topology must be an object"] };
  exactKeys3(value, ["version", "operations", "universes"], "topology", errors);
  if (value.version !== WORK_TOPOLOGY_VERSION) errors.push("topology version must be 1");
  if (!Array.isArray(value.operations)) errors.push("topology operations must be an array");
  if (!Array.isArray(value.universes)) errors.push("topology universes must be an array");
  const rawOperations = Array.isArray(value.operations) ? value.operations : [];
  const rawUniverses = Array.isArray(value.universes) ? value.universes : [];
  if (rawOperations.length > WORK_TOPOLOGY_MAX_OPERATIONS) {
    errors.push(`topology exceeds ${WORK_TOPOLOGY_MAX_OPERATIONS} operations`);
  }
  if (rawUniverses.length > WORK_TOPOLOGY_MAX_UNIVERSES) {
    errors.push(`topology exceeds ${WORK_TOPOLOGY_MAX_UNIVERSES} universes`);
  }
  const operations = [];
  for (const [index, rawProposed] of rawOperations.entries()) {
    const label = `operation[${index}]`;
    if (!plainRecord(rawProposed)) {
      errors.push(`${label} must be an object`);
      continue;
    }
    const raw = normalizeProposedOperation(rawProposed);
    exactKeys3(raw, ["id", "effect", "coverage", "dependsOn", "dataFrom", "cardinality"], label, errors);
    const idOk = validId(raw.id, `${label}.id`, errors);
    const effectOk = raw.effect === "read" || raw.effect === "compute" || raw.effect === "local_write" || raw.effect === "external_write" || raw.effect === "admin";
    if (!effectOk) errors.push(`${label}.effect is invalid`);
    const hasCoverage = Object.prototype.hasOwnProperty.call(raw, "coverage");
    const coverageOk = raw.coverage === "single" || raw.coverage === "accepted_set" || raw.coverage === "complete_set" || raw.coverage === "resolved_operation";
    if (raw.effect === "read" && !coverageOk) errors.push(`${label}.coverage is required for reads`);
    if (raw.effect !== "read" && hasCoverage) errors.push(`${label}.coverage is only valid for reads`);
    const dependsOn = stringList(raw.dependsOn, `${label}.dependsOn`, errors);
    const dataFrom = stringList(raw.dataFrom, `${label}.dataFrom`, errors);
    if (!plainRecord(raw.cardinality)) {
      errors.push(`${label}.cardinality must be an object`);
      continue;
    }
    const cardinality = raw.cardinality;
    if (cardinality.kind === "once") {
      exactKeys3(cardinality, ["kind"], `${label}.cardinality`, errors);
    } else if (cardinality.kind === "each" || cardinality.kind === "set") {
      exactKeys3(cardinality, ["kind", "universeId"], `${label}.cardinality`, errors);
      validId(cardinality.universeId, `${label}.cardinality.universeId`, errors);
    } else {
      errors.push(`${label}.cardinality.kind is invalid`);
    }
    if (!idOk || !effectOk || !dependsOn || !dataFrom) continue;
    if (raw.effect === "read" && dataFrom.length > 0) {
      errors.push(`${label}.dataFrom is not valid for a source read`);
    }
    for (const source of dataFrom) {
      if (!dependsOn.includes(source)) errors.push(`${label}.dataFrom ${source} must also be a dependency`);
    }
    if (raw.effect === "read" && (raw.coverage === "accepted_set" && cardinality.kind !== "set" || cardinality.kind === "set" && raw.coverage !== "accepted_set" || raw.coverage === "complete_set" && cardinality.kind !== "once" || raw.coverage === "resolved_operation" && cardinality.kind !== "once" || cardinality.kind === "each" && raw.coverage !== "single")) {
      errors.push(`${label}.coverage and cardinality describe different read sets`);
    }
    if (cardinality.kind !== "once" && cardinality.kind !== "each" && cardinality.kind !== "set") continue;
    operations.push({
      id: String(raw.id),
      effect: raw.effect,
      ...raw.effect === "read" && coverageOk ? { coverage: raw.coverage } : {},
      dependsOn,
      dataFrom,
      cardinality: cardinality.kind === "once" ? { kind: "once" } : { kind: cardinality.kind, universeId: String(cardinality.universeId) }
    });
  }
  const universes = [];
  for (const [index, raw] of rawUniverses.entries()) {
    const label = `universe[${index}]`;
    if (!plainRecord(raw)) {
      errors.push(`${label} must be an object`);
      continue;
    }
    const idOk = validId(raw.id, `${label}.id`, errors);
    if (raw.seal === "accepted_input") {
      exactKeys3(raw, ["id", "seal", "members"], label, errors);
      if (!Array.isArray(raw.members) || raw.members.length === 0) {
        errors.push(`${label}.members must contain at least one accepted item`);
        continue;
      }
      if (raw.members.length > WORK_TOPOLOGY_MAX_UNIVERSE_MEMBERS) {
        errors.push(`${label}.members exceeds ${WORK_TOPOLOGY_MAX_UNIVERSE_MEMBERS}`);
      }
      const members = [];
      for (const [memberIndex, member] of raw.members.entries()) {
        if (typeof member !== "string" || !MEMBER_PATTERN.test(member)) {
          errors.push(`${label}.members[${memberIndex}] must be a bounded nonblank id`);
        } else {
          members.push(member);
        }
      }
      if (new Set(members).size !== members.length) errors.push(`${label}.members contains duplicates`);
      if (idOk) universes.push({ id: String(raw.id), seal: "accepted_input", members: [...members].sort() });
    } else if (raw.seal === "complete_source_receipt") {
      exactKeys3(raw, ["id", "seal", "producedBy", "memberIdPointer"], label, errors);
      const producerOk = validId(raw.producedBy, `${label}.producedBy`, errors);
      if (!isBoundedWorkTopologyJsonPointer(raw.memberIdPointer)) {
        errors.push(`${label}.memberIdPointer must be a bounded RFC 6901 pointer into one source record`);
      } else if (idOk && producerOk) {
        universes.push({
          id: String(raw.id),
          seal: "complete_source_receipt",
          producedBy: String(raw.producedBy),
          memberIdPointer: raw.memberIdPointer
        });
      }
    } else {
      exactKeys3(raw, ["id", "seal", "members", "producedBy", "memberIdPointer"], label, errors);
      errors.push(`${label}.seal is invalid`);
    }
  }
  const operationIds = /* @__PURE__ */ new Set();
  for (const operation of operations) {
    if (operationIds.has(operation.id)) errors.push(`duplicate operation id ${operation.id}`);
    operationIds.add(operation.id);
  }
  const universeIds = /* @__PURE__ */ new Set();
  for (const universe of universes) {
    if (universeIds.has(universe.id)) errors.push(`duplicate universe id ${universe.id}`);
    if (operationIds.has(universe.id)) errors.push(`id ${universe.id} is shared by an operation and universe`);
    universeIds.add(universe.id);
  }
  const byOperation = new Map(operations.map((operation) => [operation.id, operation]));
  for (const operation of operations) {
    for (const dependency of operation.dependsOn) {
      if (!byOperation.has(dependency)) errors.push(`${operation.id} depends on missing operation ${dependency}`);
      if (dependency === operation.id) errors.push(`${operation.id} cannot depend on itself`);
    }
    for (const source of operation.dataFrom) {
      if (!byOperation.has(source)) errors.push(`${operation.id} reads data from missing operation ${source}`);
    }
    if (operation.cardinality.kind !== "once" && !universeIds.has(operation.cardinality.universeId)) {
      errors.push(`${operation.id} references missing universe ${operation.cardinality.universeId}`);
    }
  }
  if (hasCycle(operations)) errors.push("operation dependencies must be acyclic");
  for (const universe of universes) {
    const consumers = operations.filter((operation) => operation.cardinality.kind !== "once" && operation.cardinality.universeId === universe.id);
    if (consumers.length === 0) errors.push(`universe ${universe.id} has no cardinality consumer`);
    if (universe.seal === "complete_source_receipt") {
      const producer = byOperation.get(universe.producedBy);
      if (!producer || producer.effect !== "read" || producer.coverage !== "complete_set" || producer.cardinality.kind !== "once") {
        errors.push(`universe ${universe.id} requires one complete-set source-read producer`);
      }
      if (consumers.some((consumer) => consumer.id === universe.producedBy)) {
        errors.push(`universe ${universe.id} cannot be produced by its own each-cardinality consumer`);
      }
    }
  }
  if (errors.length > 0) return { ok: false, errors: [...new Set(errors)].sort() };
  return {
    ok: true,
    topology: {
      version: WORK_TOPOLOGY_VERSION,
      operations: [...operations].sort((left, right) => left.id.localeCompare(right.id)),
      universes: [...universes].sort((left, right) => left.id.localeCompare(right.id))
    }
  };
}
var import_zod5, WORK_TOPOLOGY_VERSION, WORK_TOPOLOGY_MAX_OPERATIONS, WORK_TOPOLOGY_MAX_UNIVERSES, WORK_TOPOLOGY_MAX_UNIVERSE_MEMBERS, WORK_TOPOLOGY_MAX_INLINE_MODEL_MEMBERS, ID_PATTERN, MEMBER_PATTERN, WorkTopologyIdSchema, WorkTopologyMemberSchema, WorkTopologyEffectSchema, WorkTopologyModelCoverageSchema, WorkTopologyCoverageSchema, WorkTopologyCardinalitySchema, WorkTopologyOperationSchema, WorkTopologyUniverseSchema, WorkTopologySchema, ActionWorkTopologyBaseSchema, ActionWorkTopologySchema;
var init_work_topology = __esm({
  "src/runtime/graph/work-topology.ts"() {
    "use strict";
    import_zod5 = require("zod");
    init_closed_canonical_json();
    WORK_TOPOLOGY_VERSION = 1;
    WORK_TOPOLOGY_MAX_OPERATIONS = 32;
    WORK_TOPOLOGY_MAX_UNIVERSES = 16;
    WORK_TOPOLOGY_MAX_UNIVERSE_MEMBERS = 1e4;
    WORK_TOPOLOGY_MAX_INLINE_MODEL_MEMBERS = 256;
    ID_PATTERN = /^[A-Za-z0-9][A-Za-z0-9:._/-]{0,127}$/;
    MEMBER_PATTERN = /^\S(?:[\s\S]{0,254}\S)?$/;
    WorkTopologyIdSchema = import_zod5.z.string().min(1).max(128).regex(ID_PATTERN);
    WorkTopologyMemberSchema = import_zod5.z.string().min(1).max(256).regex(MEMBER_PATTERN);
    WorkTopologyEffectSchema = import_zod5.z.enum([
      "read",
      "compute",
      "local_write",
      "external_write",
      "admin"
    ]);
    WorkTopologyModelCoverageSchema = import_zod5.z.enum([
      "single",
      "accepted_set",
      "complete_set",
      "resolved_operation"
    ]);
    WorkTopologyCoverageSchema = import_zod5.z.enum([
      "single",
      "accepted_set",
      "complete_set",
      "resolved_operation"
    ]);
    WorkTopologyCardinalitySchema = import_zod5.z.discriminatedUnion("kind", [
      import_zod5.z.object({ kind: import_zod5.z.literal("once") }).strict(),
      import_zod5.z.object({ kind: import_zod5.z.literal("each"), universeId: WorkTopologyIdSchema }).strict(),
      import_zod5.z.object({ kind: import_zod5.z.literal("set"), universeId: WorkTopologyIdSchema }).strict()
    ]);
    WorkTopologyOperationSchema = import_zod5.z.object({
      id: WorkTopologyIdSchema,
      effect: WorkTopologyEffectSchema,
      coverage: WorkTopologyModelCoverageSchema.nullable(),
      dependsOn: import_zod5.z.array(WorkTopologyIdSchema).max(WORK_TOPOLOGY_MAX_OPERATIONS),
      dataFrom: import_zod5.z.array(WorkTopologyIdSchema).max(WORK_TOPOLOGY_MAX_OPERATIONS),
      cardinality: WorkTopologyCardinalitySchema
    }).strict();
    WorkTopologyUniverseSchema = import_zod5.z.discriminatedUnion("seal", [
      import_zod5.z.object({
        id: WorkTopologyIdSchema,
        seal: import_zod5.z.literal("accepted_input"),
        members: import_zod5.z.array(WorkTopologyMemberSchema).min(1).max(WORK_TOPOLOGY_MAX_UNIVERSE_MEMBERS)
      }).strict(),
      import_zod5.z.object({
        id: WorkTopologyIdSchema,
        seal: import_zod5.z.literal("complete_source_receipt"),
        producedBy: WorkTopologyIdSchema,
        memberIdPointer: import_zod5.z.string().max(512).describe(
          "RFC 6901 pointer to one member id inside ONE producer record; empty string when the record is itself the id."
        )
      }).strict()
    ]);
    WorkTopologySchema = import_zod5.z.object({
      version: import_zod5.z.literal(WORK_TOPOLOGY_VERSION),
      operations: import_zod5.z.array(WorkTopologyOperationSchema).max(WORK_TOPOLOGY_MAX_OPERATIONS),
      universes: import_zod5.z.array(WorkTopologyUniverseSchema).max(WORK_TOPOLOGY_MAX_UNIVERSES)
    }).strict();
    ActionWorkTopologyBaseSchema = WorkTopologySchema.extend({
      operations: import_zod5.z.array(WorkTopologyOperationSchema).min(1).max(WORK_TOPOLOGY_MAX_OPERATIONS)
    }).strict();
    ActionWorkTopologySchema = ActionWorkTopologyBaseSchema.superRefine((topology, ctx) => {
      const validated = validateWorkTopology(topology);
      if (!validated.ok) {
        ctx.addIssue({
          code: import_zod5.z.ZodIssueCode.custom,
          path: [],
          message: validated.errors.join("; ")
        });
      }
      for (const [index, universe] of topology.universes.entries()) {
        if (universe.seal === "accepted_input" && universe.members.length > WORK_TOPOLOGY_MAX_INLINE_MODEL_MEMBERS) {
          ctx.addIssue({
            code: import_zod5.z.ZodIssueCode.custom,
            path: ["universes", index, "members"],
            message: `model-authored accepted input is limited to ${WORK_TOPOLOGY_MAX_INLINE_MODEL_MEMBERS} inline members; use a complete_source_receipt universe for scalable sets`
          });
        }
      }
    });
  }
});

// src/runtime/graph/turn-graph-ir.ts
var init_turn_graph_ir = __esm({
  "src/runtime/graph/turn-graph-ir.ts"() {
    "use strict";
  }
});

// src/runtime/graph/turn-graph-compiler.ts
var init_turn_graph_compiler = __esm({
  "src/runtime/graph/turn-graph-compiler.ts"() {
    "use strict";
    init_turn_graph_obligations();
    init_project_shape();
    init_external_effect_taxonomy();
    init_message_intent();
    init_multi_item_intent();
    init_accepted_goal();
    init_admitted_turn_semantics();
    init_work_topology();
    init_turn_graph_ir();
  }
});

// src/runtime/harness/tool-evidence.ts
function regexAlternation(terms) {
  return terms.map((term) => term.replace(/[.*+?^${}()|[\]\\]/g, "\\$&").replace(/\s+/g, "\\s+")).sort((a, b) => b.length - a.length).join("|");
}
var MUTATION_ACTION_SOURCE, MUTATING_OBJECTIVE_RE, MUTATION_NOUN_REFERENCE_RE, EXTERNAL_PROVIDER_SOURCE, EXTERNAL_OPERATIONAL_OBJECT_SOURCE, EXTERNAL_DESTINATION_RE, NON_EXTERNAL_MUTATION_ACTIONS, EXTERNAL_MUTATION_ACTION_SOURCE, EXTERNAL_WRITE_ACTION_RE, DIRECT_EXTERNAL_PROVIDER_TARGET_RE, EXTERNAL_TARGET_PREPOSITION_RE, EXTERNAL_PROVIDER_OBJECT_RE, STAGED_CLAUSE_IMMEDIATE_EXECUTION_RE, PROVIDER_REFERENCING_ARTIFACT_SOURCE, PROVIDER_REFERENCING_ARTIFACT_RE, EXPLICIT_ARTIFACT_PROVIDER_DESTINATION_RE;
var init_tool_evidence = __esm({
  "src/runtime/harness/tool-evidence.ts"() {
    "use strict";
    init_message_intent();
    init_external_effect_taxonomy();
    init_json_repair();
    init_multi_item_intent();
    init_eventlog();
    init_tool_effect();
    init_turn_graph_compiler();
    MUTATION_ACTION_SOURCE = regexAlternation(MUTATION_ACTION_CUES);
    MUTATING_OBJECTIVE_RE = new RegExp(`\\b(?:${MUTATION_ACTION_SOURCE})\\b`, "i");
    MUTATION_NOUN_REFERENCE_RE = new RegExp(
      `\\b(?:a|an|the|this|that|these|those|my|our|your|its)\\s+(?:${MUTATION_ACTION_SOURCE})\\b`,
      "gi"
    );
    EXTERNAL_PROVIDER_SOURCE = "airtable|asana|box|crm|discord|facebook|figma|gmail|google\\s+(?:ads?|calendar|docs?|drive|sheets?)|googledocs?|googledrive|googlesheets?|github|heroku|hubspot|instagram|jira|linear|linkedin|meta|netlify|notion|one\\s*drive|onedrive|outlook|railway|salesforce|sharepoint|slack|stripe|supabase|teams|tiktok|trello|twitter|vercel|x|youtube|zoom|external\\s+(?:app|service|system)|connected\\s+(?:app|service|system)";
    EXTERNAL_OPERATIONAL_OBJECT_SOURCE = "accounts?|bases?|boards?|branches?|campaigns?|cards?|cells?|channels?|charges?|comments?|contacts?|customers?|databases?|deals?|docs?|documents?|drafts?|events?|files?|folders?|gists?|invoices?|issues?|leads?|lists?|meetings?|messages?|pages?|payments?|posts?|projects?|pull\\s+requests?|prs?|ranges?|records?|refunds?|releases?|reminders?|repositories?|repos?|rows?|services?|sites?|spreadsheets?|subscriptions?|tasks?|threads?|tickets?|transactions?|values?|views?";
    EXTERNAL_DESTINATION_RE = new RegExp(
      `\\b(?:${EXTERNAL_PROVIDER_SOURCE}|calendar|email|sheet\\s+(?:cell|cells|range|row|rows|tab)|spreadsheet)\\b`,
      "i"
    );
    NON_EXTERNAL_MUTATION_ACTIONS = /* @__PURE__ */ new Set([
      "build",
      "call",
      "execute",
      "generate",
      "install",
      "run"
    ]);
    EXTERNAL_MUTATION_ACTION_SOURCE = regexAlternation(
      MUTATION_ACTION_CUES.filter((term) => !NON_EXTERNAL_MUTATION_ACTIONS.has(term))
    );
    EXTERNAL_WRITE_ACTION_RE = new RegExp(
      `\\b(?:${EXTERNAL_MUTATION_ACTION_SOURCE})\\b`,
      "i"
    );
    DIRECT_EXTERNAL_PROVIDER_TARGET_RE = new RegExp(
      `\\b(?:${EXTERNAL_MUTATION_ACTION_SOURCE})\\b\\s+(?:(?:a|an|my|our|the|this|that|these|those|your)\\s+)?(?:${EXTERNAL_PROVIDER_SOURCE}|calendar|spreadsheet)\\b`,
      "i"
    );
    EXTERNAL_TARGET_PREPOSITION_RE = new RegExp(
      `\\b(?:${EXTERNAL_MUTATION_ACTION_SOURCE})\\b[^.!?\\n;]{0,160}\\b(?:as|in|into|on|through|to|via|within)\\s+(?:(?:my|our|the)\\s+)?(?:${EXTERNAL_PROVIDER_SOURCE})\\b`,
      "i"
    );
    EXTERNAL_PROVIDER_OBJECT_RE = new RegExp(
      `\\b(?:${EXTERNAL_PROVIDER_SOURCE})\\b[^.!?\\n;]{0,80}\\b(?:${EXTERNAL_OPERATIONAL_OBJECT_SOURCE})\\b`,
      "i"
    );
    STAGED_CLAUSE_IMMEDIATE_EXECUTION_RE = new RegExp(
      `\\b(?:(?:and|then)\\s+(?:actually\\s+)?(?:${EXTERNAL_MUTATION_ACTION_SOURCE}|invoke)|(?:${EXTERNAL_MUTATION_ACTION_SOURCE}|invoke)\\b[^.!?\\n;]{0,40}\\b(?:immediately|now|right\\s+away))\\b`,
      "i"
    );
    PROVIDER_REFERENCING_ARTIFACT_SOURCE = "analysis|brief|checklist|comparison|drafts?|guide|migration\\s+plan|notes?|plan|proposal|recommendations?|report|response|summary";
    PROVIDER_REFERENCING_ARTIFACT_RE = new RegExp(
      `\\b(?:${PROVIDER_REFERENCING_ARTIFACT_SOURCE})\\b`,
      "i"
    );
    EXPLICIT_ARTIFACT_PROVIDER_DESTINATION_RE = new RegExp(
      `(?:\\b(?:${EXTERNAL_MUTATION_ACTION_SOURCE})\\b[^.!?\\n;]{0,100}\\b(?:${PROVIDER_REFERENCING_ARTIFACT_SOURCE})\\b[^.!?\\n;]{0,50}\\b(?:in|into|within)\\s+(?:(?:my|our|the)\\s+)?(?:${EXTERNAL_PROVIDER_SOURCE})\\b|\\b(?:add|append|copy|move|save|send|share|submit|upload)\\b[^.!?\\n;]{0,100}\\b(?:${PROVIDER_REFERENCING_ARTIFACT_SOURCE})\\b[^.!?\\n;]{0,50}\\b(?:on|through|to|via)\\s+(?:(?:my|our|the)\\s+)?(?:${EXTERNAL_PROVIDER_SOURCE})\\b|\\bsave\\b[^.!?\\n;]{0,100}\\bas\\s+(?:(?:a|an|my|our|the)\\s+)?(?:${EXTERNAL_PROVIDER_SOURCE})\\b[^.!?\\n;]{0,50}\\b(?:${PROVIDER_REFERENCING_ARTIFACT_SOURCE})\\b|\\b(?:create|save)\\s+(?:(?:a|an|my|our|the)\\s+)?(?:${EXTERNAL_PROVIDER_SOURCE})\\b[^.!?\\n;]{0,50}\\bdrafts?\\b)`,
      "i"
    );
  }
});

// src/runtime/harness/result-facts.ts
var init_result_facts = __esm({
  "src/runtime/harness/result-facts.ts"() {
    "use strict";
    init_provider_read_evidence();
  }
});

// src/runtime/harness/result-payload-storage.ts
var import_node_path8, FIXED_BASE_DIRECTORY, RESULT_PAYLOAD_SPILL_DIRECTORY;
var init_result_payload_storage = __esm({
  "src/runtime/harness/result-payload-storage.ts"() {
    "use strict";
    import_node_path8 = __toESM(require("node:path"), 1);
    init_config();
    FIXED_BASE_DIRECTORY = import_node_path8.default.resolve(BASE_DIR);
    RESULT_PAYLOAD_SPILL_DIRECTORY = import_node_path8.default.join(
      FIXED_BASE_DIRECTORY,
      "state",
      "result-payloads",
      "sha256"
    );
  }
});

// src/runtime/harness/settlement-crossing-authority.ts
var init_settlement_crossing_authority = __esm({
  "src/runtime/harness/settlement-crossing-authority.ts"() {
    "use strict";
  }
});

// src/runtime/harness/documented-create-result-evidence.ts
var init_documented_create_result_evidence = __esm({
  "src/runtime/harness/documented-create-result-evidence.ts"() {
    "use strict";
    init_closed_canonical_json();
    init_atomic_input_content_contract();
  }
});

// src/runtime/harness/schema-version.ts
var HARNESS_SCHEMA_VERSION;
var init_schema_version = __esm({
  "src/runtime/harness/schema-version.ts"() {
    "use strict";
    HARNESS_SCHEMA_VERSION = 73;
  }
});

// src/runtime/harness/host-planned-resolution-coexistence.ts
function hostPlannedResolutionProofSql(resolutionRef, phase, protocol = "checkpoint_v71") {
  const rootPhase = phase === "insert" ? "root.state = 'open'" : "root.state IN ('open','closed','conflict')";
  const taskPhase = phase === "insert" ? "task.state != 'conflict'" : "task.state IN ('armed','manifested_verifying','terminal','conflict')";
  const checkpointJoin = protocol === "checkpoint_v71" ? `JOIN ${PLAN_TASK_PREPARATION_CHECKPOINTS_TABLE} checkpoint
        ON checkpoint.session_id = receipt.session_id
       AND checkpoint.source_user_seq = receipt.source_user_seq
       AND checkpoint.accepted_task_id = receipt.accepted_task_id
       AND checkpoint.logical_tool_call_id = receipt.logical_tool_call_id` : "";
  const legacySettlementJoins = protocol === "legacy_success" ? `JOIN logical_call_settlements settlement
        ON settlement.session_id = call.session_id
       AND settlement.source_user_seq = call.source_user_seq
       AND settlement.logical_tool_call_id = call.logical_tool_call_id
      JOIN durable_result_handles result
        ON result.handle_id = settlement.result_handle_id
       AND result.session_id = call.session_id
       AND result.source_user_seq = call.source_user_seq
       AND result.accepted_task_id = call.accepted_task_id
       AND result.logical_tool_call_id = call.logical_tool_call_id
       AND result.tool_name = call.tool_name
       AND result.argument_digest = call.argument_digest
      JOIN physical_dispatches physical
        ON physical.session_id = result.session_id
       AND physical.source_user_seq = result.source_user_seq
       AND physical.logical_tool_call_id = result.logical_tool_call_id
       AND physical.physical_dispatch_id = result.physical_dispatch_id
      JOIN logical_call_settlement_crossings crossing
        ON crossing.session_id = result.session_id
       AND crossing.source_user_seq = result.source_user_seq
       AND crossing.logical_tool_call_id = result.logical_tool_call_id
       AND crossing.physical_dispatch_id = result.physical_dispatch_id` : "";
  const settlementProof = protocol === "legacy_success" ? `AND call.state = 'settled'
       AND call.outcome_kind = 'succeeded'
       AND call.settlement_event_id = settlement.settlement_event_id
       AND settlement.protocol_version = 1
       AND settlement.execution_kind = 'local_execution'
       AND settlement.outcome_kind = 'succeeded'
       AND settlement.business_call = 0
       AND settlement.mutating = 0
       AND settlement.requirement_id IS NULL
       AND settlement.physical_crossing_count = 0
       AND settlement.host_crossing_count = 1
       AND settlement.crossing_authority_version = 2
       AND result.scope_kind = 'authoritative'
       AND result.success = 1
       AND result.rejection_reason IS NULL
       AND result.raw_payload_json IS NOT NULL
       AND result.raw_payload_sha256 IS NOT NULL
       AND json_valid(result.raw_payload_json)
       AND json_type(result.raw_payload_json, '$') = 'text'
       AND json_valid(json_extract(result.raw_payload_json, '$'))
       AND json_extract(json_extract(result.raw_payload_json, '$'), '$.ok') = 1
       AND json_extract(json_extract(result.raw_payload_json, '$'), '$.acceptedTaskId') = root.accepted_task_id
       AND json_extract(json_extract(result.raw_payload_json, '$'), '$.graphId') = task.graph_id
       AND json_extract(json_extract(result.raw_payload_json, '$'), '$.graphHash') = task.graph_hash
       AND json_extract(json_extract(result.raw_payload_json, '$'), '$.contractId') = contract.contract_id
       AND physical.accepted_task_id = root.accepted_task_id
       AND physical.tool_name = 'plan_task'
       AND physical.argument_digest = call.argument_digest
       AND physical.state = 'returned'
       AND physical.execution_site = 'host'
       AND crossing.ordinal = physical.ordinal
       AND crossing.tool_name = physical.tool_name
       AND crossing.argument_digest = physical.argument_digest
       AND crossing.terminal_state = 'returned'
       AND crossing.execution_site = 'host'
       AND (SELECT COUNT(*)
              FROM logical_call_settlement_crossings crossing_count
             WHERE crossing_count.session_id = call.session_id
               AND crossing_count.source_user_seq = call.source_user_seq
               AND crossing_count.logical_tool_call_id = call.logical_tool_call_id) = 1` : `AND checkpoint.checkpoint_version = 1
       AND checkpoint.plan_argument_digest = call.argument_digest
       AND checkpoint.graph_event_id = task.graph_event_id
       AND checkpoint.graph_id = task.graph_id
       AND checkpoint.graph_hash = task.graph_hash
       AND checkpoint.contract_id = contract.contract_id
       AND checkpoint.preamble_event_id = receipt.preamble_event_id
       AND checkpoint.preamble_event_digest = receipt.preamble_event_digest
       AND checkpoint.delivery_key = receipt.delivery_key
       AND (
         (checkpoint.delivery_owner = 'durable_conversation'
           AND receipt.transport_target = 'durable_conversation')
         OR
         (checkpoint.delivery_owner IN ('carrier_owned','legacy_unknown')
           AND receipt.transport_target != 'durable_conversation')
       )
       AND ${checkpointBackedSettledPlanTaskProofSql()}`;
  return `EXISTS (
    SELECT 1
      FROM accepted_turn_call_authorities root
      JOIN accepted_task_authority task
        ON task.session_id = root.session_id
       AND task.source_user_seq = root.source_user_seq
       AND task.accepted_task_id = root.accepted_task_id
      JOIN accepted_task_work_contracts contract
        ON contract.session_id = task.session_id
       AND contract.source_user_seq = task.source_user_seq
       AND contract.accepted_task_id = task.accepted_task_id
       AND contract.contract_id = task.work_contract_id
       AND contract.graph_event_id = task.graph_event_id
       AND contract.graph_id = task.graph_id
       AND contract.graph_hash = task.graph_hash
      JOIN ${PLAN_TASK_ACTIVATION_RECEIPTS_TABLE} receipt
        ON receipt.session_id = task.session_id
       AND receipt.source_user_seq = task.source_user_seq
       AND receipt.accepted_task_id = task.accepted_task_id
       AND receipt.graph_event_id = task.graph_event_id
       AND receipt.graph_id = task.graph_id
       AND receipt.graph_hash = task.graph_hash
       AND receipt.contract_id = contract.contract_id
      ${checkpointJoin}
      JOIN events source
        ON source.session_id = root.session_id
       AND source.seq = root.source_user_seq
       AND source.id = root.source_event_id
      JOIN events graph
        ON graph.session_id = root.session_id
       AND graph.id = task.graph_event_id
      JOIN events preamble
        ON preamble.session_id = root.session_id
       AND preamble.id = receipt.preamble_event_id
      JOIN events delivery
        ON delivery.session_id = root.session_id
       AND delivery.id = receipt.delivery_event_id
      JOIN logical_tool_calls call
        ON call.session_id = root.session_id
       AND call.source_user_seq = root.source_user_seq
       AND call.accepted_task_id = root.accepted_task_id
       AND call.logical_tool_call_id = receipt.logical_tool_call_id
       AND call.argument_digest = receipt.plan_argument_digest
      ${legacySettlementJoins}
     WHERE root.session_id = ${resolutionRef}.session_id
       AND root.source_user_seq = ${resolutionRef}.source_user_seq
       AND root.accepted_task_id = ${resolutionRef}.accepted_task_id
       AND root.authority_protocol = 1
       AND root.authority_kind = 'host_v1'
       AND root.engine_version = 'host_v1'
       AND root.surface_version = 'configured_harness_capability_surface_v1'
       AND root.effect_ceiling = 'admin'
       AND root.effect_bounds_json = '["admin","compute","external_write","host_only","local_write","read"]'
       AND root.graph_event_id IS NULL
       AND root.graph_hash IS NULL
       AND ${rootPhase}
       AND task.authority_protocol = 1
       AND task.expected_work_required = 1
       AND ${taskPhase}
       AND task.graph_event_id = ${resolutionRef}.graph_event_id
       AND task.graph_id = ${resolutionRef}.graph_id
       AND task.graph_hash = ${resolutionRef}.graph_hash
       AND contract.contract_version = 1
       AND contract.planner_source = 'structured_model'
       AND contract.graph_event_id = ${resolutionRef}.graph_event_id
       AND contract.graph_id = ${resolutionRef}.graph_id
       AND contract.graph_hash = ${resolutionRef}.graph_hash
       AND source.role = 'user'
       AND source.type = 'user_input_received'
       AND source.turn = root.source_turn
       AND graph.role = 'system'
       AND graph.type = 'turn_graph_compiled'
       AND graph.parent_event_id = source.id
       AND graph.turn = source.turn
       AND json_extract(graph.data_json, '$.sourceUserSeq') = root.source_user_seq
       AND json_extract(graph.data_json, '$.graphId') = ${resolutionRef}.graph_id
       AND json_extract(graph.data_json, '$.graphHash') = ${resolutionRef}.graph_hash
       AND json_extract(graph.data_json, '$.compilerVersion') = ${resolutionRef}.compiler_version
       AND json_extract(graph.data_json, '$.route') = 'act'
       AND json_extract(graph.data_json, '$.effectCeiling') = ${resolutionRef}.effect_ceiling
       AND json_extract(graph.data_json, '$.graph.classification.route') = 'act'
       AND json_extract(graph.data_json, '$.graph.graphId') = ${resolutionRef}.graph_id
       AND json_extract(graph.data_json, '$.graph.compiler.graphHash') = ${resolutionRef}.graph_hash
       AND ${resolutionRef}.route = 'act'
       AND ${resolutionRef}.state IN ('open','finalized')
       AND receipt.receipt_version = 1
       AND receipt.accepted_task_id = ${resolutionRef}.accepted_task_id
       AND receipt.delivery_status IN ('delivered','not_applicable')
       AND ((receipt.delivery_status = 'delivered'
             AND receipt.delivery_reason IS NULL)
         OR (receipt.delivery_status = 'not_applicable'
             AND receipt.delivery_reason IN ('quiet_presentation','non_user_surface')))
       AND preamble.role = 'Clem'
       AND preamble.type = 'conversation_preamble'
       AND preamble.parent_event_id = source.id
       AND preamble.turn = source.turn
       AND json_extract(preamble.data_json, '$.version') = 1
       AND json_extract(preamble.data_json, '$.kind') = 'pre_execution'
       AND json_extract(preamble.data_json, '$.sourceUserSeq') = root.source_user_seq
       AND json_type(preamble.data_json, '$.text') = 'text'
       AND length(trim(json_extract(preamble.data_json, '$.text'))) > 0
       AND delivery.role = 'system'
       AND delivery.type = 'conversation_preamble_delivered'
       AND delivery.parent_event_id = preamble.id
       AND delivery.turn = preamble.turn
       AND json_extract(delivery.data_json, '$.version') = 1
       AND json_extract(delivery.data_json, '$.sourceUserSeq') = root.source_user_seq
       AND json_extract(delivery.data_json, '$.acceptedTaskId') = root.accepted_task_id
       AND json_extract(delivery.data_json, '$.logicalToolCallId') = call.logical_tool_call_id
       AND json_extract(delivery.data_json, '$.planArgumentDigest') = call.argument_digest
       AND json_extract(delivery.data_json, '$.preambleEventId') = preamble.id
       AND json_extract(delivery.data_json, '$.preambleEventDigest') = receipt.preamble_event_digest
       AND json_extract(delivery.data_json, '$.deliveryKey') = receipt.delivery_key
       AND json_extract(delivery.data_json, '$.deliveryStatus') = receipt.delivery_status
       AND json_extract(delivery.data_json, '$.deliveryReason') IS receipt.delivery_reason
       AND json_extract(delivery.data_json, '$.transportReceiptDigest') = receipt.transport_receipt_digest
       AND json_extract(delivery.data_json, '$.transportReceipt.version') = 1
       AND json_extract(delivery.data_json, '$.transportReceipt.deliveryKey') = receipt.delivery_key
       AND json_extract(delivery.data_json, '$.transportReceipt.eventId') = preamble.id
       AND json_extract(delivery.data_json, '$.transportReceipt.eventDigest') = receipt.preamble_event_digest
       AND json_extract(delivery.data_json, '$.transportReceipt.surface') =
           CASE receipt.delivery_status WHEN 'delivered' THEN 'channel_message' ELSE 'not_applicable' END
       AND json_extract(delivery.data_json, '$.transportReceipt.target') = receipt.transport_target
       AND json_type(delivery.data_json, '$.transportReceipt') = 'object'
       AND (SELECT COUNT(*) FROM json_each(delivery.data_json, '$.transportReceipt')) = 6
       AND NOT EXISTS (
             SELECT 1 FROM json_each(delivery.data_json, '$.transportReceipt') transport_field
              WHERE transport_field.key NOT IN ('version','deliveryKey','eventId','eventDigest','surface','target')
           )
       AND call.tool_name = 'plan_task'
       ${settlementProof}
       AND (SELECT COUNT(*)
              FROM ${PLAN_TASK_ACTIVATION_RECEIPTS_TABLE} receipt_count
             WHERE receipt_count.session_id = root.session_id
               AND receipt_count.source_user_seq = root.source_user_seq) = 1
  )`;
}
function checkpointBackedSettledPlanTaskProofSql() {
  return `(call.state = 'settled'
    AND EXISTS (
      SELECT 1
        FROM logical_call_settlements settlement
        JOIN physical_dispatches physical
          ON physical.session_id = settlement.session_id
         AND physical.source_user_seq = settlement.source_user_seq
         AND physical.logical_tool_call_id = settlement.logical_tool_call_id
        JOIN logical_call_settlement_crossings crossing
          ON crossing.session_id = physical.session_id
         AND crossing.source_user_seq = physical.source_user_seq
         AND crossing.logical_tool_call_id = physical.logical_tool_call_id
         AND crossing.physical_dispatch_id = physical.physical_dispatch_id
       WHERE settlement.session_id = call.session_id
         AND settlement.source_user_seq = call.source_user_seq
         AND settlement.logical_tool_call_id = call.logical_tool_call_id
         AND call.settlement_event_id = settlement.settlement_event_id
         AND call.outcome_kind = settlement.outcome_kind
         AND settlement.protocol_version = 1
         AND settlement.execution_kind = 'local_execution'
         AND settlement.business_call = 0
         AND settlement.mutating = 0
         AND settlement.requirement_id IS NULL
         AND settlement.physical_crossing_count = 0
         AND settlement.host_crossing_count = 1
         AND settlement.crossing_authority_version = 2
         AND settlement.outcome_kind != 'uncertain_write'
         AND physical.accepted_task_id = call.accepted_task_id
         AND physical.tool_name = 'plan_task'
         AND physical.argument_digest = call.argument_digest
         AND physical.state IN ('returned','threw','timed_out','cancelled','unknown')
         AND physical.execution_site = 'host'
         AND crossing.ordinal = physical.ordinal
         AND crossing.tool_name = physical.tool_name
         AND crossing.argument_digest = physical.argument_digest
         AND crossing.terminal_state = physical.state
         AND crossing.execution_site = 'host'
         AND (SELECT COUNT(*)
                FROM logical_call_settlements settlement_count
               WHERE settlement_count.session_id = call.session_id
                 AND settlement_count.source_user_seq = call.source_user_seq
                 AND settlement_count.logical_tool_call_id = call.logical_tool_call_id) = 1
         AND (SELECT COUNT(*)
                FROM logical_call_settlement_crossings crossing_count
               WHERE crossing_count.session_id = call.session_id
                 AND crossing_count.source_user_seq = call.source_user_seq
                 AND crossing_count.logical_tool_call_id = call.logical_tool_call_id) = 1
         AND (SELECT COUNT(*)
                FROM physical_dispatches physical_count
               WHERE physical_count.session_id = call.session_id
                 AND physical_count.source_user_seq = call.source_user_seq
                 AND physical_count.logical_tool_call_id = call.logical_tool_call_id) = 1
         AND (
           settlement.outcome_kind != 'succeeded'
           OR EXISTS (
             SELECT 1
               FROM durable_result_handles result
              WHERE result.handle_id = settlement.result_handle_id
                AND result.session_id = call.session_id
                AND result.source_user_seq = call.source_user_seq
                AND result.accepted_task_id = call.accepted_task_id
                AND result.logical_tool_call_id = call.logical_tool_call_id
                AND result.physical_dispatch_id = physical.physical_dispatch_id
                AND result.tool_name = call.tool_name
                AND result.argument_digest = call.argument_digest
                AND result.scope_kind = 'authoritative'
                AND result.success = 1
                AND result.rejection_reason IS NULL
                AND result.raw_payload_json IS NOT NULL
                AND result.raw_payload_sha256 IS NOT NULL
                AND json_valid(result.raw_payload_json)
                AND json_type(result.raw_payload_json, '$') = 'text'
                AND json_valid(json_extract(result.raw_payload_json, '$'))
                AND json_type(json_extract(result.raw_payload_json, '$'), '$') = 'object'
                AND json_extract(json_extract(result.raw_payload_json, '$'), '$.ok') = 1
                AND json_extract(json_extract(result.raw_payload_json, '$'), '$.acceptedTaskId') = root.accepted_task_id
                AND json_extract(json_extract(result.raw_payload_json, '$'), '$.graphId') = task.graph_id
                AND json_extract(json_extract(result.raw_payload_json, '$'), '$.graphHash') = task.graph_hash
                AND json_extract(json_extract(result.raw_payload_json, '$'), '$.contractId') = contract.contract_id
                AND (SELECT COUNT(*)
                       FROM durable_result_handles result_count
                      WHERE result_count.scope_kind = 'authoritative'
                        AND result_count.session_id = call.session_id
                        AND result_count.source_user_seq = call.source_user_seq
                        AND result_count.logical_tool_call_id = call.logical_tool_call_id) = 1
           )
         )
    ))`;
}
function planTaskReceiptCallStateProofSql() {
  return `(call.state = 'open' OR (
    call.state = 'settled'
    AND call.outcome_kind = 'succeeded'
    AND EXISTS (
      SELECT 1
        FROM logical_call_settlements settlement
        JOIN durable_result_handles result
          ON result.handle_id = settlement.result_handle_id
         AND result.session_id = call.session_id
         AND result.source_user_seq = call.source_user_seq
         AND result.accepted_task_id = call.accepted_task_id
         AND result.logical_tool_call_id = call.logical_tool_call_id
         AND result.tool_name = call.tool_name
         AND result.argument_digest = call.argument_digest
        JOIN physical_dispatches physical
          ON physical.session_id = result.session_id
         AND physical.source_user_seq = result.source_user_seq
         AND physical.logical_tool_call_id = result.logical_tool_call_id
         AND physical.physical_dispatch_id = result.physical_dispatch_id
        JOIN logical_call_settlement_crossings crossing
          ON crossing.session_id = result.session_id
         AND crossing.source_user_seq = result.source_user_seq
         AND crossing.logical_tool_call_id = result.logical_tool_call_id
         AND crossing.physical_dispatch_id = result.physical_dispatch_id
       WHERE settlement.session_id = call.session_id
         AND settlement.source_user_seq = call.source_user_seq
         AND settlement.logical_tool_call_id = call.logical_tool_call_id
         AND call.settlement_event_id = settlement.settlement_event_id
         AND settlement.protocol_version = 1
         AND settlement.execution_kind = 'local_execution'
         AND settlement.outcome_kind = 'succeeded'
         AND settlement.business_call = 0
         AND settlement.mutating = 0
         AND settlement.requirement_id IS NULL
         AND settlement.physical_crossing_count = 0
         AND settlement.host_crossing_count = 1
         AND settlement.crossing_authority_version = 2
         AND result.scope_kind = 'authoritative'
         AND result.success = 1
         AND result.rejection_reason IS NULL
         AND result.raw_payload_json IS NOT NULL
         AND result.raw_payload_sha256 IS NOT NULL
         AND json_valid(result.raw_payload_json)
         AND json_type(result.raw_payload_json, '$') = 'text'
         AND json_valid(json_extract(result.raw_payload_json, '$'))
         AND json_type(json_extract(result.raw_payload_json, '$'), '$') = 'object'
         AND json_extract(json_extract(result.raw_payload_json, '$'), '$.ok') = 1
         AND json_extract(json_extract(result.raw_payload_json, '$'), '$.acceptedTaskId') = root.accepted_task_id
         AND json_extract(json_extract(result.raw_payload_json, '$'), '$.graphId') = task.graph_id
         AND json_extract(json_extract(result.raw_payload_json, '$'), '$.graphHash') = task.graph_hash
         AND json_extract(json_extract(result.raw_payload_json, '$'), '$.contractId') = contract.contract_id
         AND physical.accepted_task_id = root.accepted_task_id
         AND physical.tool_name = 'plan_task'
         AND physical.argument_digest = call.argument_digest
         AND physical.state = 'returned'
         AND physical.execution_site = 'host'
         AND crossing.ordinal = physical.ordinal
         AND crossing.tool_name = physical.tool_name
         AND crossing.argument_digest = physical.argument_digest
         AND crossing.terminal_state = 'returned'
         AND crossing.execution_site = 'host'
         AND (SELECT COUNT(*)
                FROM logical_call_settlements settlement_count
               WHERE settlement_count.session_id = call.session_id
                 AND settlement_count.source_user_seq = call.source_user_seq
                 AND settlement_count.logical_tool_call_id = call.logical_tool_call_id) = 1
         AND (SELECT COUNT(*)
                FROM logical_call_settlement_crossings crossing_count
               WHERE crossing_count.session_id = call.session_id
                 AND crossing_count.source_user_seq = call.source_user_seq
                 AND crossing_count.logical_tool_call_id = call.logical_tool_call_id) = 1
         AND (SELECT COUNT(*)
                FROM physical_dispatches physical_count
               WHERE physical_count.session_id = call.session_id
                 AND physical_count.source_user_seq = call.source_user_seq
                 AND physical_count.logical_tool_call_id = call.logical_tool_call_id) = 1
         AND (SELECT COUNT(*)
                FROM durable_result_handles result_count
               WHERE result_count.scope_kind = 'authoritative'
                 AND result_count.session_id = call.session_id
                 AND result_count.source_user_seq = call.source_user_seq
                 AND result_count.logical_tool_call_id = call.logical_tool_call_id) = 1
    )
  ))`;
}
function planTaskActivationReceiptInsertTriggerSql(ifNotExists, checkpointBacked = false) {
  const checkpointJoin = checkpointBacked ? `JOIN ${PLAN_TASK_PREPARATION_CHECKPOINTS_TABLE} checkpoint
          ON checkpoint.session_id = root.session_id
         AND checkpoint.source_user_seq = root.source_user_seq
         AND checkpoint.accepted_task_id = root.accepted_task_id
         AND checkpoint.logical_tool_call_id = NEW.logical_tool_call_id` : "";
  const checkpointProof = checkpointBacked ? `AND checkpoint.checkpoint_version = 1
         AND checkpoint.plan_argument_digest = NEW.plan_argument_digest
         AND checkpoint.graph_event_id = NEW.graph_event_id
         AND checkpoint.graph_id = NEW.graph_id
         AND checkpoint.graph_hash = NEW.graph_hash
         AND checkpoint.contract_id = NEW.contract_id
         AND checkpoint.preamble_event_id = NEW.preamble_event_id
         AND checkpoint.preamble_event_digest = NEW.preamble_event_digest
         AND checkpoint.delivery_key = NEW.delivery_key
         AND (
           (checkpoint.delivery_owner = 'durable_conversation'
             AND NEW.delivery_status = 'delivered'
             AND NEW.delivery_reason IS NULL
             AND NEW.transport_target = 'durable_conversation')
           OR
           (checkpoint.delivery_owner IN ('carrier_owned','legacy_unknown')
             AND NEW.transport_target != 'durable_conversation')
         )` : "";
  const callStateProof = checkpointBacked ? `(call.state = 'open' OR ${checkpointBackedSettledPlanTaskProofSql()})` : planTaskReceiptCallStateProofSql();
  return `CREATE TRIGGER ${ifNotExists ? "IF NOT EXISTS " : ""}${PLAN_TASK_ACTIVATION_RECEIPT_INSERT_TRIGGER}
    BEFORE INSERT ON ${PLAN_TASK_ACTIVATION_RECEIPTS_TABLE}
    WHEN NOT EXISTS (
      SELECT 1
        FROM accepted_turn_call_authorities root
        JOIN accepted_task_authority task
          ON task.session_id = root.session_id
         AND task.source_user_seq = root.source_user_seq
         AND task.accepted_task_id = root.accepted_task_id
        JOIN accepted_task_work_contracts contract
          ON contract.session_id = task.session_id
         AND contract.source_user_seq = task.source_user_seq
         AND contract.accepted_task_id = task.accepted_task_id
         AND contract.contract_id = task.work_contract_id
        ${checkpointJoin}
        JOIN logical_tool_calls call
          ON call.session_id = root.session_id
         AND call.source_user_seq = root.source_user_seq
         AND call.accepted_task_id = root.accepted_task_id
         AND call.logical_tool_call_id = NEW.logical_tool_call_id
        JOIN events source
          ON source.session_id = root.session_id
         AND source.seq = root.source_user_seq
         AND source.id = root.source_event_id
        JOIN events graph
          ON graph.session_id = root.session_id
         AND graph.id = task.graph_event_id
        JOIN events preamble
          ON preamble.session_id = root.session_id
         AND preamble.id = NEW.preamble_event_id
        JOIN events delivery
          ON delivery.session_id = root.session_id
         AND delivery.id = NEW.delivery_event_id
       WHERE root.session_id = NEW.session_id
         AND root.source_user_seq = NEW.source_user_seq
         AND root.accepted_task_id = NEW.accepted_task_id
         AND root.authority_kind = 'host_v1'
         AND root.engine_version = 'host_v1'
         AND root.state = 'open'
         AND root.graph_event_id IS NULL
         AND root.graph_hash IS NULL
         AND task.authority_protocol = 1
         AND task.state != 'conflict'
         AND task.expected_work_required = 0
         AND task.graph_event_id = NEW.graph_event_id
         AND task.graph_id = NEW.graph_id
         AND task.graph_hash = NEW.graph_hash
         AND task.work_contract_id = NEW.contract_id
         AND contract.contract_version = 1
         AND contract.planner_source = 'structured_model'
         AND contract.graph_event_id = NEW.graph_event_id
         AND contract.graph_id = NEW.graph_id
         AND contract.graph_hash = NEW.graph_hash
         ${checkpointProof}
         AND call.tool_name = 'plan_task'
         AND call.argument_digest = NEW.plan_argument_digest
         AND ${callStateProof}
         AND source.role = 'user'
         AND source.type = 'user_input_received'
         AND source.turn = root.source_turn
         AND graph.role = 'system'
         AND graph.type = 'turn_graph_compiled'
         AND graph.parent_event_id = source.id
         AND graph.turn = source.turn
         AND json_extract(graph.data_json, '$.route') = 'act'
         AND json_extract(graph.data_json, '$.sourceUserSeq') = root.source_user_seq
         AND json_extract(graph.data_json, '$.graphId') = NEW.graph_id
         AND json_extract(graph.data_json, '$.graphHash') = NEW.graph_hash
         AND preamble.role = 'Clem'
         AND preamble.type = 'conversation_preamble'
         AND preamble.parent_event_id = source.id
         AND preamble.turn = source.turn
         AND json_extract(preamble.data_json, '$.version') = 1
         AND json_extract(preamble.data_json, '$.kind') = 'pre_execution'
         AND json_extract(preamble.data_json, '$.sourceUserSeq') = root.source_user_seq
         AND json_type(preamble.data_json, '$.text') = 'text'
         AND delivery.role = 'system'
         AND delivery.type = 'conversation_preamble_delivered'
         AND delivery.parent_event_id = preamble.id
         AND delivery.turn = preamble.turn
         AND json_extract(delivery.data_json, '$.version') = 1
         AND json_extract(delivery.data_json, '$.sourceUserSeq') = root.source_user_seq
         AND json_extract(delivery.data_json, '$.acceptedTaskId') = root.accepted_task_id
         AND json_extract(delivery.data_json, '$.logicalToolCallId') = call.logical_tool_call_id
         AND json_extract(delivery.data_json, '$.planArgumentDigest') = call.argument_digest
         AND json_extract(delivery.data_json, '$.preambleEventId') = preamble.id
         AND json_extract(delivery.data_json, '$.preambleEventDigest') = NEW.preamble_event_digest
         AND json_extract(delivery.data_json, '$.deliveryKey') = NEW.delivery_key
         AND json_extract(delivery.data_json, '$.deliveryStatus') = NEW.delivery_status
         AND json_extract(delivery.data_json, '$.deliveryReason') IS NEW.delivery_reason
         AND json_extract(delivery.data_json, '$.transportReceiptDigest') = NEW.transport_receipt_digest
         AND json_extract(delivery.data_json, '$.transportReceipt.version') = 1
         AND json_extract(delivery.data_json, '$.transportReceipt.deliveryKey') = NEW.delivery_key
         AND json_extract(delivery.data_json, '$.transportReceipt.eventId') = preamble.id
         AND json_extract(delivery.data_json, '$.transportReceipt.eventDigest') = NEW.preamble_event_digest
         AND json_extract(delivery.data_json, '$.transportReceipt.surface') =
             CASE NEW.delivery_status WHEN 'delivered' THEN 'channel_message' ELSE 'not_applicable' END
         AND json_extract(delivery.data_json, '$.transportReceipt.target') = NEW.transport_target
         AND json_type(delivery.data_json, '$.transportReceipt') = 'object'
         AND (SELECT COUNT(*) FROM json_each(delivery.data_json, '$.transportReceipt')) = 6
         AND NOT EXISTS (
               SELECT 1 FROM json_each(delivery.data_json, '$.transportReceipt') transport_field
                WHERE transport_field.key NOT IN ('version','deliveryKey','eventId','eventDigest','surface','target')
             )
         AND json_type(NEW.transport_receipt_json, '$') = 'object'
         AND (SELECT COUNT(*) FROM json_each(NEW.transport_receipt_json)) = 6
         AND NOT EXISTS (
               SELECT 1 FROM json_each(NEW.transport_receipt_json) transport_field
                WHERE transport_field.key NOT IN ('version','deliveryKey','eventId','eventDigest','surface','target')
             )
         AND json_extract(NEW.transport_receipt_json, '$.version') = 1
         AND json_extract(NEW.transport_receipt_json, '$.deliveryKey') = NEW.delivery_key
         AND json_extract(NEW.transport_receipt_json, '$.eventId') = preamble.id
         AND json_extract(NEW.transport_receipt_json, '$.eventDigest') = NEW.preamble_event_digest
         AND json_extract(NEW.transport_receipt_json, '$.surface') =
             CASE NEW.delivery_status WHEN 'delivered' THEN 'channel_message' ELSE 'not_applicable' END
         AND json_extract(NEW.transport_receipt_json, '$.target') = NEW.transport_target
    )
    BEGIN
      SELECT RAISE(ABORT, 'plan_task receipt requires exact host, graph, contract, call, preamble, and delivery authority');
    END;`;
}
function refreshPlanTaskActivationReceiptInsertTrigger(db) {
  db.exec(`
    DROP TRIGGER IF EXISTS ${PLAN_TASK_ACTIVATION_RECEIPT_INSERT_TRIGGER};
    ${planTaskActivationReceiptInsertTriggerSql(false)}
  `);
}
function refreshCheckpointBackedPlanTaskActivationReceiptInsertTrigger(db) {
  db.exec(`
    DROP TRIGGER IF EXISTS ${PLAN_TASK_ACTIVATION_RECEIPT_INSERT_TRIGGER};
    ${planTaskActivationReceiptInsertTriggerSql(false, true)}
  `);
}
function createHostPlannedResolutionCoexistenceSchema(db) {
  db.exec(`
    CREATE TABLE IF NOT EXISTS ${PLAN_TASK_ACTIVATION_RECEIPTS_TABLE} (
      session_id               TEXT NOT NULL,
      source_user_seq          INTEGER NOT NULL CHECK (source_user_seq > 0),
      accepted_task_id         TEXT NOT NULL,
      logical_tool_call_id     TEXT NOT NULL,
      receipt_version          INTEGER NOT NULL CHECK (receipt_version = 1),
      plan_argument_digest     TEXT NOT NULL CHECK (length(plan_argument_digest) = 64),
      graph_event_id           TEXT NOT NULL,
      graph_id                 TEXT NOT NULL,
      graph_hash               TEXT NOT NULL CHECK (length(graph_hash) = 64),
      contract_id              TEXT NOT NULL,
      preamble_event_id        TEXT NOT NULL REFERENCES events(id) ON DELETE RESTRICT,
      preamble_event_digest    TEXT NOT NULL CHECK (length(preamble_event_digest) = 64),
      delivery_key             TEXT NOT NULL UNIQUE,
      delivery_event_id        TEXT NOT NULL UNIQUE REFERENCES events(id) ON DELETE RESTRICT,
      delivery_status          TEXT NOT NULL CHECK (delivery_status IN ('delivered','not_applicable')),
      delivery_reason          TEXT CHECK (delivery_reason IN ('quiet_presentation','non_user_surface')),
      transport_receipt_json   TEXT NOT NULL CHECK (json_valid(transport_receipt_json)),
      transport_receipt_digest TEXT NOT NULL CHECK (length(transport_receipt_digest) = 64),
      transport_target         TEXT NOT NULL CHECK (length(transport_target) BETWEEN 1 AND 512),
      recorded_at              TEXT NOT NULL,
      PRIMARY KEY (session_id, source_user_seq),
      UNIQUE (session_id, source_user_seq, logical_tool_call_id),
      FOREIGN KEY (session_id, source_user_seq, logical_tool_call_id)
        REFERENCES logical_tool_calls(session_id, source_user_seq, logical_tool_call_id)
        ON DELETE CASCADE,
      FOREIGN KEY (session_id, source_user_seq)
        REFERENCES accepted_task_work_contracts(session_id, source_user_seq)
        ON DELETE CASCADE,
      FOREIGN KEY (contract_id)
        REFERENCES accepted_task_work_contracts(contract_id)
        ON DELETE RESTRICT,
      CHECK ((delivery_status = 'delivered' AND delivery_reason IS NULL)
          OR (delivery_status = 'not_applicable' AND delivery_reason IS NOT NULL))
    );

    CREATE TRIGGER IF NOT EXISTS trg_plan_task_activation_receipt_update_immutable
    BEFORE UPDATE ON ${PLAN_TASK_ACTIVATION_RECEIPTS_TABLE}
    BEGIN
      SELECT RAISE(ABORT, 'plan_task activation receipts are immutable');
    END;
    CREATE TRIGGER IF NOT EXISTS trg_plan_task_activation_receipt_delete_immutable
    BEFORE DELETE ON ${PLAN_TASK_ACTIVATION_RECEIPTS_TABLE}
    WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
    BEGIN
      SELECT RAISE(ABORT, 'plan_task activation receipts are immutable');
    END;
  `);
  refreshPlanTaskActivationReceiptInsertTrigger(db);
}
function planTaskPreparationCheckpointInsertTriggerSql(ifNotExists) {
  return `CREATE TRIGGER ${ifNotExists ? "IF NOT EXISTS " : ""}${PLAN_TASK_PREPARATION_CHECKPOINT_INSERT_TRIGGER}
    BEFORE INSERT ON ${PLAN_TASK_PREPARATION_CHECKPOINTS_TABLE}
    WHEN NOT EXISTS (
      SELECT 1
        FROM accepted_turn_call_authorities root
        JOIN accepted_task_authority task
          ON task.session_id = root.session_id
         AND task.source_user_seq = root.source_user_seq
         AND task.accepted_task_id = root.accepted_task_id
        JOIN accepted_task_work_contracts contract
          ON contract.session_id = task.session_id
         AND contract.source_user_seq = task.source_user_seq
         AND contract.accepted_task_id = task.accepted_task_id
         AND contract.contract_id = task.work_contract_id
        JOIN ${PLAN_TASK_BINDING_SEAL_INTENTS_TABLE} intent
          ON intent.session_id = root.session_id
         AND intent.source_user_seq = root.source_user_seq
         AND intent.accepted_task_id = root.accepted_task_id
         AND intent.logical_tool_call_id = NEW.logical_tool_call_id
        JOIN logical_tool_calls call
          ON call.session_id = root.session_id
         AND call.source_user_seq = root.source_user_seq
         AND call.accepted_task_id = root.accepted_task_id
         AND call.logical_tool_call_id = NEW.logical_tool_call_id
        JOIN events source
          ON source.session_id = root.session_id
         AND source.seq = root.source_user_seq
         AND source.id = root.source_event_id
        JOIN events graph
          ON graph.session_id = root.session_id
         AND graph.id = task.graph_event_id
        JOIN events preamble
          ON preamble.session_id = root.session_id
         AND preamble.id = NEW.preamble_event_id
       WHERE root.session_id = NEW.session_id
         AND root.source_user_seq = NEW.source_user_seq
         AND root.accepted_task_id = NEW.accepted_task_id
         AND root.authority_kind = 'host_v1'
         AND root.engine_version = 'host_v1'
         AND (
           root.state = 'open'
           OR (task.expected_work_required = 1 AND root.state IN ('closed','conflict'))
         )
         AND root.graph_event_id IS NULL
         AND root.graph_hash IS NULL
         AND task.authority_protocol = 1
         AND (task.state != 'conflict' OR task.expected_work_required = 1)
         AND task.expected_work_required IN (0, 1)
         AND task.graph_event_id = NEW.graph_event_id
         AND task.graph_id = NEW.graph_id
         AND task.graph_hash = NEW.graph_hash
         AND task.work_contract_id = NEW.contract_id
         AND contract.contract_version = 1
         AND contract.planner_source = 'structured_model'
         AND contract.graph_event_id = NEW.graph_event_id
         AND contract.graph_id = NEW.graph_id
         AND contract.graph_hash = NEW.graph_hash
         AND intent.intent_version = 1
         AND intent.plan_argument_digest = NEW.plan_argument_digest
         AND intent.graph_event_id = NEW.graph_event_id
         AND intent.graph_id = NEW.graph_id
         AND intent.graph_hash = NEW.graph_hash
         AND intent.contract_id = NEW.contract_id
         AND intent.preamble_text = json_extract(preamble.data_json, '$.text')
         AND intent.delivery_owner = NEW.delivery_owner
         AND call.tool_name = 'plan_task'
         AND call.argument_digest = NEW.plan_argument_digest
         AND (call.state = 'open' OR ${checkpointBackedSettledPlanTaskProofSql()})
         AND source.role = 'user'
         AND source.type = 'user_input_received'
         AND source.turn = root.source_turn
         AND graph.role = 'system'
         AND graph.type = 'turn_graph_compiled'
         AND graph.parent_event_id = source.id
         AND graph.turn = source.turn
         AND json_extract(graph.data_json, '$.route') = 'act'
         AND json_extract(graph.data_json, '$.sourceUserSeq') = root.source_user_seq
         AND json_extract(graph.data_json, '$.graphId') = NEW.graph_id
         AND json_extract(graph.data_json, '$.graphHash') = NEW.graph_hash
         AND preamble.role = 'Clem'
         AND preamble.type = 'conversation_preamble'
         AND preamble.parent_event_id = source.id
         AND preamble.turn = source.turn
         AND json_extract(preamble.data_json, '$.version') = 1
         AND json_extract(preamble.data_json, '$.kind') = 'pre_execution'
         AND json_extract(preamble.data_json, '$.sourceUserSeq') = root.source_user_seq
         AND json_type(preamble.data_json, '$.text') = 'text'
    )
    BEGIN
      SELECT RAISE(ABORT, 'plan_task checkpoint requires exact host, graph, contract, call, and preamble authority');
    END;`;
}
function planTaskBindingSealIntentInsertTriggerSql(ifNotExists) {
  return `CREATE TRIGGER ${ifNotExists ? "IF NOT EXISTS " : ""}${PLAN_TASK_BINDING_SEAL_INTENT_INSERT_TRIGGER}
    BEFORE INSERT ON ${PLAN_TASK_BINDING_SEAL_INTENTS_TABLE}
    WHEN NOT EXISTS (
      SELECT 1
        FROM accepted_turn_call_authorities root
        JOIN logical_tool_calls call
          ON call.session_id = root.session_id
         AND call.source_user_seq = root.source_user_seq
         AND call.accepted_task_id = root.accepted_task_id
         AND call.logical_tool_call_id = NEW.logical_tool_call_id
        JOIN events source
          ON source.session_id = root.session_id
         AND source.seq = root.source_user_seq
         AND source.id = root.source_event_id
        JOIN events graph
          ON graph.session_id = root.session_id
         AND graph.id = NEW.graph_event_id
       WHERE root.session_id = NEW.session_id
         AND root.source_user_seq = NEW.source_user_seq
         AND root.accepted_task_id = NEW.accepted_task_id
         AND root.authority_protocol = 1
         AND root.authority_kind = 'host_v1'
         AND root.engine_version = 'host_v1'
         AND root.surface_version = 'configured_harness_capability_surface_v1'
         AND root.state IN ('open','closed','conflict')
         AND root.graph_event_id IS NULL
         AND root.graph_hash IS NULL
         AND call.tool_name = 'plan_task'
         AND call.argument_digest = NEW.plan_argument_digest
         AND source.role = 'user'
         AND source.type = 'user_input_received'
         AND source.turn = root.source_turn
         AND graph.role = 'system'
         AND graph.type = 'turn_graph_compiled'
         AND graph.parent_event_id = source.id
         AND graph.turn = source.turn
         AND json_extract(graph.data_json, '$.sourceUserSeq') = root.source_user_seq
         AND json_extract(graph.data_json, '$.route') = 'act'
         AND json_extract(graph.data_json, '$.graphId') = NEW.graph_id
         AND json_extract(graph.data_json, '$.graphHash') = NEW.graph_hash
         AND (
           NEW.intent_origin = 'legacy_backfill'
           OR (
             call.state = 'open'
             AND json_extract(graph.data_json, '$.graph.graphId') = NEW.graph_id
             AND json_extract(graph.data_json, '$.graph.compiler.graphHash') = NEW.graph_hash
             AND json_extract(graph.data_json, '$.graph.source.inputHash') = NEW.semantic_input_digest
             AND json_type(graph.data_json, '$.graph.workTopology.topology.operations') = 'array'
             AND json_array_length(NEW.operation_ids_json) =
                 json_array_length(graph.data_json, '$.graph.workTopology.topology.operations')
             AND NOT EXISTS (
               SELECT 1 FROM json_each(NEW.operation_ids_json) requested
                WHERE json_type(NEW.operation_ids_json, '$[' || requested.key || ']') != 'text'
                   OR NOT EXISTS (
                     SELECT 1
                       FROM json_each(graph.data_json, '$.graph.workTopology.topology.operations') operation
                      WHERE json_extract(operation.value, '$.id') = requested.value
                   )
             )
             AND NOT EXISTS (
               SELECT 1
                 FROM json_each(graph.data_json, '$.graph.workTopology.topology.operations') operation
                WHERE NOT EXISTS (
                  SELECT 1 FROM json_each(NEW.operation_ids_json) requested
                   WHERE requested.value = json_extract(operation.value, '$.id')
                )
             )
             AND (SELECT COUNT(DISTINCT value) FROM json_each(NEW.operation_ids_json)) =
                 json_array_length(NEW.operation_ids_json)
           )
         )
    )
    BEGIN
      SELECT RAISE(ABORT, 'plan_task binding-seal intent requires exact host call and admitted graph authority');
    END;`;
}
function createPlanTaskPreparationCheckpointSchema(db) {
  db.exec(`
    CREATE TABLE IF NOT EXISTS ${PLAN_TASK_BINDING_SEAL_INTENTS_TABLE} (
      session_id             TEXT NOT NULL,
      source_user_seq        INTEGER NOT NULL CHECK (source_user_seq > 0),
      accepted_task_id       TEXT NOT NULL,
      logical_tool_call_id   TEXT NOT NULL,
      intent_version         INTEGER NOT NULL CHECK (intent_version = 1),
      intent_origin          TEXT NOT NULL CHECK (intent_origin IN ('current','legacy_backfill')),
      plan_argument_digest   TEXT NOT NULL CHECK (length(plan_argument_digest) = 64),
      graph_event_id         TEXT NOT NULL REFERENCES events(id) ON DELETE RESTRICT,
      graph_id               TEXT NOT NULL,
      graph_hash             TEXT NOT NULL CHECK (length(graph_hash) = 64),
      contract_id            TEXT NOT NULL CHECK (contract_id GLOB 'expected-work:v1:*'),
      objective_text         TEXT NOT NULL CHECK (length(objective_text) BETWEEN 1 AND 16000),
      objective_digest       TEXT NOT NULL CHECK (length(objective_digest) = 64),
      semantic_input_digest  TEXT NOT NULL CHECK (length(semantic_input_digest) = 64),
      operation_ids_json     TEXT NOT NULL CHECK (json_valid(operation_ids_json) AND json_type(operation_ids_json) = 'array'),
      operation_ids_digest   TEXT NOT NULL CHECK (length(operation_ids_digest) = 64),
      preamble_text          TEXT NOT NULL CHECK (length(preamble_text) BETWEEN 1 AND 1000),
      preamble_text_digest   TEXT NOT NULL CHECK (length(preamble_text_digest) = 64),
      delivery_owner         TEXT NOT NULL CHECK (delivery_owner IN ('durable_conversation','carrier_owned','legacy_unknown')),
      recorded_at            TEXT NOT NULL,
      PRIMARY KEY (session_id, source_user_seq),
      UNIQUE (session_id, source_user_seq, logical_tool_call_id),
      FOREIGN KEY (session_id, source_user_seq, logical_tool_call_id)
        REFERENCES logical_tool_calls(session_id, source_user_seq, logical_tool_call_id)
        ON DELETE CASCADE
    );

    CREATE TRIGGER IF NOT EXISTS trg_plan_task_binding_seal_intent_update_immutable
    BEFORE UPDATE ON ${PLAN_TASK_BINDING_SEAL_INTENTS_TABLE}
    BEGIN
      SELECT RAISE(ABORT, 'plan_task binding-seal intents are immutable');
    END;
    CREATE TRIGGER IF NOT EXISTS trg_plan_task_binding_seal_intent_delete_immutable
    BEFORE DELETE ON ${PLAN_TASK_BINDING_SEAL_INTENTS_TABLE}
    WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
    BEGIN
      SELECT RAISE(ABORT, 'plan_task binding-seal intents are immutable');
    END;

    CREATE TABLE IF NOT EXISTS ${PLAN_TASK_BINDING_SEAL_RECOVERY_CURSOR_TABLE} (
      cursor_key             INTEGER PRIMARY KEY CHECK (cursor_key = 1),
      cursor_recorded_at     TEXT NOT NULL,
      cursor_session_id      TEXT NOT NULL,
      cursor_source_user_seq INTEGER NOT NULL CHECK (cursor_source_user_seq > 0),
      updated_at             TEXT NOT NULL
    );

    CREATE TABLE IF NOT EXISTS ${PLAN_TASK_PREPARATION_CHECKPOINTS_TABLE} (
      session_id             TEXT NOT NULL,
      source_user_seq        INTEGER NOT NULL CHECK (source_user_seq > 0),
      accepted_task_id       TEXT NOT NULL,
      logical_tool_call_id   TEXT NOT NULL,
      checkpoint_version     INTEGER NOT NULL CHECK (checkpoint_version = 1),
      plan_argument_digest   TEXT NOT NULL CHECK (length(plan_argument_digest) = 64),
      graph_event_id         TEXT NOT NULL REFERENCES events(id) ON DELETE RESTRICT,
      graph_id               TEXT NOT NULL,
      graph_hash             TEXT NOT NULL CHECK (length(graph_hash) = 64),
      contract_id            TEXT NOT NULL,
      preamble_event_id      TEXT NOT NULL UNIQUE REFERENCES events(id) ON DELETE RESTRICT,
      preamble_event_digest  TEXT NOT NULL CHECK (length(preamble_event_digest) = 64),
      delivery_key           TEXT NOT NULL UNIQUE,
      delivery_owner         TEXT NOT NULL CHECK (delivery_owner IN ('durable_conversation','carrier_owned','legacy_unknown')),
      recorded_at            TEXT NOT NULL,
      PRIMARY KEY (session_id, source_user_seq),
      UNIQUE (session_id, source_user_seq, logical_tool_call_id),
      FOREIGN KEY (session_id, source_user_seq, logical_tool_call_id)
        REFERENCES logical_tool_calls(session_id, source_user_seq, logical_tool_call_id)
        ON DELETE CASCADE,
      FOREIGN KEY (session_id, source_user_seq)
        REFERENCES accepted_task_work_contracts(session_id, source_user_seq)
        ON DELETE CASCADE,
      FOREIGN KEY (contract_id)
        REFERENCES accepted_task_work_contracts(contract_id)
        ON DELETE RESTRICT
    );

    CREATE TRIGGER IF NOT EXISTS trg_plan_task_preparation_checkpoint_update_immutable
    BEFORE UPDATE ON ${PLAN_TASK_PREPARATION_CHECKPOINTS_TABLE}
    BEGIN
      SELECT RAISE(ABORT, 'plan_task preparation checkpoints are immutable');
    END;
    CREATE TRIGGER IF NOT EXISTS trg_plan_task_preparation_checkpoint_delete_immutable
    BEFORE DELETE ON ${PLAN_TASK_PREPARATION_CHECKPOINTS_TABLE}
    WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
    BEGIN
      SELECT RAISE(ABORT, 'plan_task preparation checkpoints are immutable');
    END;
  `);
  db.exec(`
    DROP TRIGGER IF EXISTS ${PLAN_TASK_BINDING_SEAL_INTENT_INSERT_TRIGGER};
    ${planTaskBindingSealIntentInsertTriggerSql(false)}
    DROP TRIGGER IF EXISTS ${PLAN_TASK_PREPARATION_CHECKPOINT_INSERT_TRIGGER};
    ${planTaskPreparationCheckpointInsertTriggerSql(false)}
  `);
  refreshCheckpointBackedPlanTaskActivationReceiptInsertTrigger(db);
}
var PLAN_TASK_ACTIVATION_RECEIPTS_TABLE, PLAN_TASK_BINDING_SEAL_INTENTS_TABLE, PLAN_TASK_PREPARATION_CHECKPOINTS_TABLE, PLAN_TASK_BINDING_SEAL_RECOVERY_CURSOR_TABLE, PLAN_TASK_ACTIVATION_RECEIPT_INSERT_TRIGGER, PLAN_TASK_PREPARATION_CHECKPOINT_INSERT_TRIGGER, PLAN_TASK_BINDING_SEAL_INTENT_INSERT_TRIGGER;
var init_host_planned_resolution_coexistence = __esm({
  "src/runtime/harness/host-planned-resolution-coexistence.ts"() {
    "use strict";
    PLAN_TASK_ACTIVATION_RECEIPTS_TABLE = "plan_task_activation_receipts";
    PLAN_TASK_BINDING_SEAL_INTENTS_TABLE = "plan_task_binding_seal_intents";
    PLAN_TASK_PREPARATION_CHECKPOINTS_TABLE = "plan_task_preparation_checkpoints";
    PLAN_TASK_BINDING_SEAL_RECOVERY_CURSOR_TABLE = "plan_task_binding_seal_recovery_cursor";
    PLAN_TASK_ACTIVATION_RECEIPT_INSERT_TRIGGER = "trg_plan_task_activation_receipt_exact_insert";
    PLAN_TASK_PREPARATION_CHECKPOINT_INSERT_TRIGGER = "trg_plan_task_preparation_checkpoint_exact_insert";
    PLAN_TASK_BINDING_SEAL_INTENT_INSERT_TRIGGER = "trg_plan_task_binding_seal_intent_exact_insert";
  }
});

// src/runtime/harness/async-read-refinement-schema.ts
function terminalReceiptTableSql(tableName) {
  return `
    CREATE TABLE ${tableName} (
      session_id                    TEXT NOT NULL,
      source_user_seq               INTEGER NOT NULL CHECK (source_user_seq > 0),
      accepted_task_id              TEXT NOT NULL,
      start_logical_tool_call_id    TEXT NOT NULL,
      receipt_version               INTEGER NOT NULL CHECK (receipt_version = 1),
      recipe_digest                 TEXT NOT NULL CHECK (length(recipe_digest) = 64),
      terminal_kind                 TEXT NOT NULL CHECK (terminal_kind IN (
        'insufficient_evidence', 'provider_failed', 'provider_cancelled',
        'deadline_exhausted', 'attempts_exhausted', 'user_cancelled'
      )),
      reason                        TEXT NOT NULL CHECK (length(reason) BETWEEN 1 AND 300),
      getter_logical_tool_call_id   TEXT,
      getter_result_handle_id       TEXT,
      getter_raw_payload_sha256     TEXT CHECK (
        getter_raw_payload_sha256 IS NULL OR length(getter_raw_payload_sha256) = 64
      ),
      cancellation_run_attempt_id   TEXT,
      cancellation_requested_at    TEXT,
      recorded_at                   TEXT NOT NULL,
      CHECK (
        (getter_logical_tool_call_id IS NULL AND getter_result_handle_id IS NULL AND getter_raw_payload_sha256 IS NULL)
        OR
        (getter_logical_tool_call_id IS NOT NULL AND getter_result_handle_id IS NOT NULL AND getter_raw_payload_sha256 IS NOT NULL)
      ),
      CHECK (
        (terminal_kind = 'user_cancelled' AND cancellation_run_attempt_id IS NOT NULL AND cancellation_requested_at IS NOT NULL)
        OR
        (terminal_kind != 'user_cancelled' AND cancellation_run_attempt_id IS NULL AND cancellation_requested_at IS NULL)
      ),
      PRIMARY KEY (session_id, source_user_seq, start_logical_tool_call_id),
      UNIQUE (session_id, source_user_seq, getter_logical_tool_call_id),
      FOREIGN KEY (session_id, source_user_seq, start_logical_tool_call_id)
        REFERENCES ${ASYNC_READ_REFINEMENT_INTENTS_TABLE}(session_id, source_user_seq, start_logical_tool_call_id)
        ON DELETE CASCADE,
      FOREIGN KEY (getter_result_handle_id)
        REFERENCES durable_result_handles(handle_id) ON DELETE RESTRICT,
      FOREIGN KEY (cancellation_run_attempt_id)
        REFERENCES run_attempts(attempt_id) ON DELETE RESTRICT
    )`;
}
function terminalReceiptTableHasV72Authority(db) {
  const table = db.prepare(`SELECT sql FROM sqlite_master WHERE type = 'table' AND name = ?`).get(ASYNC_READ_REFINEMENT_TERMINAL_RECEIPTS_TABLE);
  if (!table) return false;
  const columnRows = db.prepare(`PRAGMA table_info(${ASYNC_READ_REFINEMENT_TERMINAL_RECEIPTS_TABLE})`).all();
  const columns = columnRows.map((column) => column.name);
  if (JSON.stringify(columns) !== JSON.stringify(TERMINAL_COLUMNS)) return false;
  const normalized = table.sql.replace(/\s+/gu, " ").toLowerCase();
  if (!normalized.includes("getter_raw_payload_sha256 is null or length(getter_raw_payload_sha256) = 64") || !normalized.includes("getter_logical_tool_call_id is null and getter_result_handle_id is null and getter_raw_payload_sha256 is null") || !normalized.includes("unique (session_id, source_user_seq, getter_logical_tool_call_id)") || !normalized.includes("'user_cancelled'") || !normalized.includes("terminal_kind = 'user_cancelled' and cancellation_run_attempt_id is not null")) return false;
  const foreignKeys = db.prepare(`PRAGMA foreign_key_list(${ASYNC_READ_REFINEMENT_TERMINAL_RECEIPTS_TABLE})`).all();
  return foreignKeys.some((entry) => entry.table === "durable_result_handles" && entry.from === "getter_result_handle_id" && entry.to === "handle_id" && entry.on_delete.toUpperCase() === "RESTRICT") && foreignKeys.some((entry) => entry.table === ASYNC_READ_REFINEMENT_INTENTS_TABLE && entry.from === "start_logical_tool_call_id" && entry.to === "start_logical_tool_call_id" && entry.on_delete.toUpperCase() === "CASCADE") && foreignKeys.some((entry) => entry.table === "run_attempts" && entry.from === "cancellation_run_attempt_id" && entry.to === "attempt_id" && entry.on_delete.toUpperCase() === "RESTRICT");
}
function normalizeEarlierTerminalReceiptTable(db) {
  const exists = db.prepare(`SELECT 1 AS ok FROM sqlite_master WHERE type = 'table' AND name = ?`).get(ASYNC_READ_REFINEMENT_TERMINAL_RECEIPTS_TABLE);
  if (!exists || terminalReceiptTableHasV72Authority(db)) return;
  const columnRows = db.prepare(`PRAGMA table_info(${ASYNC_READ_REFINEMENT_TERMINAL_RECEIPTS_TABLE})`).all();
  const columns = columnRows.map((column) => column.name);
  const currentColumns = JSON.stringify(columns) === JSON.stringify(TERMINAL_COLUMNS);
  const earlierColumns = JSON.stringify(columns) === JSON.stringify(EARLIER_TERMINAL_COLUMNS);
  if (!currentColumns && !earlierColumns) {
    throw new Error("earlier async terminal receipt table has an unsupported column shape");
  }
  const rebuilt = `${ASYNC_READ_REFINEMENT_TERMINAL_RECEIPTS_TABLE}_v72_rebuild`;
  db.exec(`
    DROP TRIGGER IF EXISTS trg_async_read_refinement_completion_excludes_terminal;
    DROP TRIGGER IF EXISTS trg_async_read_refinement_terminal_excludes_completion;
    DROP TRIGGER IF EXISTS trg_async_read_refinement_terminal_receipt_immutable;
    DROP TRIGGER IF EXISTS trg_async_read_refinement_terminal_receipt_delete_immutable;
    DROP TABLE IF EXISTS ${rebuilt};
    ${terminalReceiptTableSql(rebuilt)};
    INSERT INTO ${rebuilt} (${TERMINAL_COLUMNS.join(",")})
      SELECT ${EARLIER_TERMINAL_COLUMNS.slice(0, -1).join(",")},
             ${currentColumns ? "cancellation_run_attempt_id" : "NULL"},
             ${currentColumns ? "cancellation_requested_at" : "NULL"},
             recorded_at
        FROM ${ASYNC_READ_REFINEMENT_TERMINAL_RECEIPTS_TABLE};
    DROP TABLE ${ASYNC_READ_REFINEMENT_TERMINAL_RECEIPTS_TABLE};
    ALTER TABLE ${rebuilt} RENAME TO ${ASYNC_READ_REFINEMENT_TERMINAL_RECEIPTS_TABLE};
  `);
}
function createAsyncReadRefinementSchema(db) {
  db.exec(`
    CREATE TABLE IF NOT EXISTS ${ASYNC_READ_REFINEMENT_INTENTS_TABLE} (
      session_id                    TEXT NOT NULL,
      source_user_seq               INTEGER NOT NULL CHECK (source_user_seq > 0),
      accepted_task_id              TEXT NOT NULL,
      start_logical_tool_call_id    TEXT NOT NULL,
      requirement_id                TEXT NOT NULL,
      recipe_digest                 TEXT NOT NULL CHECK (length(recipe_digest) = 64),
      owner_binding_digest          TEXT NOT NULL CHECK (length(owner_binding_digest) = 64),
      start_argument_digest         TEXT NOT NULL CHECK (length(start_argument_digest) = 64),
      source_logical_tool_call_id   TEXT NOT NULL,
      source_result_handle_id       TEXT NOT NULL,
      source_projection_call_id     TEXT NOT NULL,
      source_projection_digest      TEXT NOT NULL CHECK (length(source_projection_digest) = 64),
      candidates_json               TEXT NOT NULL CHECK (json_valid(candidates_json) AND json_type(candidates_json) = 'array'),
      candidate_digest              TEXT NOT NULL CHECK (length(candidate_digest) = 64),
      accepted_at                   TEXT NOT NULL,
      max_age_days                  INTEGER NOT NULL CHECK (max_age_days BETWEEN 1 AND 30),
      min_distinct_records          INTEGER NOT NULL CHECK (min_distinct_records BETWEEN 1 AND 8),
      recorded_at                   TEXT NOT NULL,
      PRIMARY KEY (session_id, source_user_seq, start_logical_tool_call_id),
      UNIQUE (session_id, source_user_seq, requirement_id),
      FOREIGN KEY (session_id, source_user_seq, start_logical_tool_call_id)
        REFERENCES expected_work_call_bindings(session_id, source_user_seq, logical_tool_call_id)
        ON DELETE CASCADE,
      FOREIGN KEY (source_result_handle_id)
        REFERENCES durable_result_handles(handle_id) ON DELETE RESTRICT
    );

    CREATE TABLE IF NOT EXISTS ${ASYNC_READ_REFINEMENT_START_RECEIPTS_TABLE} (
      session_id                    TEXT NOT NULL,
      source_user_seq               INTEGER NOT NULL CHECK (source_user_seq > 0),
      accepted_task_id              TEXT NOT NULL,
      start_logical_tool_call_id    TEXT NOT NULL,
      provider_start_logical_tool_call_id TEXT NOT NULL,
      receipt_version               INTEGER NOT NULL CHECK (receipt_version = 1),
      recipe_digest                 TEXT NOT NULL CHECK (length(recipe_digest) = 64),
      start_result_handle_id        TEXT NOT NULL,
      start_raw_payload_sha256      TEXT NOT NULL CHECK (length(start_raw_payload_sha256) = 64),
      job_id                        TEXT NOT NULL CHECK (length(job_id) BETWEEN 1 AND 512),
      job_url                       TEXT NOT NULL CHECK (length(job_url) BETWEEN 8 AND 2048),
      recorded_at                   TEXT NOT NULL,
      PRIMARY KEY (session_id, source_user_seq, start_logical_tool_call_id),
      UNIQUE (session_id, source_user_seq, provider_start_logical_tool_call_id),
      FOREIGN KEY (session_id, source_user_seq, start_logical_tool_call_id)
        REFERENCES ${ASYNC_READ_REFINEMENT_INTENTS_TABLE}(session_id, source_user_seq, start_logical_tool_call_id)
        ON DELETE CASCADE,
      FOREIGN KEY (start_result_handle_id)
        REFERENCES durable_result_handles(handle_id) ON DELETE RESTRICT
    );

    CREATE TABLE IF NOT EXISTS ${ASYNC_READ_REFINEMENT_COMPLETION_RECEIPTS_TABLE} (
      session_id                    TEXT NOT NULL,
      source_user_seq               INTEGER NOT NULL CHECK (source_user_seq > 0),
      accepted_task_id              TEXT NOT NULL,
      start_logical_tool_call_id    TEXT NOT NULL,
      receipt_version               INTEGER NOT NULL CHECK (receipt_version = 1),
      recipe_digest                 TEXT NOT NULL CHECK (length(recipe_digest) = 64),
      getter_logical_tool_call_id   TEXT NOT NULL,
      getter_result_handle_id       TEXT NOT NULL,
      getter_raw_payload_sha256     TEXT NOT NULL CHECK (length(getter_raw_payload_sha256) = 64),
      evidence_json                 TEXT NOT NULL CHECK (json_valid(evidence_json) AND json_type(evidence_json) = 'object'),
      evidence_digest               TEXT NOT NULL CHECK (length(evidence_digest) = 64),
      recorded_at                   TEXT NOT NULL,
      PRIMARY KEY (session_id, source_user_seq, start_logical_tool_call_id),
      UNIQUE (session_id, source_user_seq, getter_logical_tool_call_id),
      FOREIGN KEY (session_id, source_user_seq, start_logical_tool_call_id)
        REFERENCES ${ASYNC_READ_REFINEMENT_START_RECEIPTS_TABLE}(session_id, source_user_seq, start_logical_tool_call_id)
        ON DELETE CASCADE,
      FOREIGN KEY (getter_result_handle_id)
        REFERENCES durable_result_handles(handle_id) ON DELETE RESTRICT
    );

    CREATE TRIGGER IF NOT EXISTS trg_async_read_refinement_intent_immutable
    BEFORE UPDATE ON ${ASYNC_READ_REFINEMENT_INTENTS_TABLE}
    BEGIN SELECT RAISE(ABORT, 'async read refinement intents are immutable'); END;
    CREATE TRIGGER IF NOT EXISTS trg_async_read_refinement_start_receipt_immutable
    BEFORE UPDATE ON ${ASYNC_READ_REFINEMENT_START_RECEIPTS_TABLE}
    BEGIN SELECT RAISE(ABORT, 'async read refinement start receipts are immutable'); END;
    CREATE TRIGGER IF NOT EXISTS trg_async_read_refinement_completion_receipt_immutable
    BEFORE UPDATE ON ${ASYNC_READ_REFINEMENT_COMPLETION_RECEIPTS_TABLE}
    BEGIN SELECT RAISE(ABORT, 'async read refinement completion receipts are immutable'); END;

    CREATE TRIGGER IF NOT EXISTS trg_async_read_refinement_intent_delete_immutable
    BEFORE DELETE ON ${ASYNC_READ_REFINEMENT_INTENTS_TABLE}
    WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
    BEGIN SELECT RAISE(ABORT, 'async read refinement intents are immutable'); END;
    CREATE TRIGGER IF NOT EXISTS trg_async_read_refinement_start_receipt_delete_immutable
    BEFORE DELETE ON ${ASYNC_READ_REFINEMENT_START_RECEIPTS_TABLE}
    WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
    BEGIN SELECT RAISE(ABORT, 'async read refinement start receipts are immutable'); END;
    CREATE TRIGGER IF NOT EXISTS trg_async_read_refinement_completion_receipt_delete_immutable
    BEFORE DELETE ON ${ASYNC_READ_REFINEMENT_COMPLETION_RECEIPTS_TABLE}
    WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
    BEGIN SELECT RAISE(ABORT, 'async read refinement completion receipts are immutable'); END;
  `);
}
function createAsyncReadRefinementTerminalRecoverySchemaV72(db) {
  db.exec(`
    CREATE TABLE IF NOT EXISTS ${ASYNC_READ_REFINEMENT_TERMINAL_RECEIPTS_TABLE} (
      session_id TEXT NOT NULL,
      source_user_seq INTEGER NOT NULL CHECK (source_user_seq > 0),
      accepted_task_id TEXT NOT NULL,
      start_logical_tool_call_id TEXT NOT NULL,
      receipt_version INTEGER NOT NULL CHECK (receipt_version = 1),
      recipe_digest TEXT NOT NULL CHECK (length(recipe_digest) = 64),
      terminal_kind TEXT NOT NULL CHECK (terminal_kind IN (
        'insufficient_evidence', 'provider_failed', 'provider_cancelled',
        'deadline_exhausted', 'attempts_exhausted'
      )),
      reason TEXT NOT NULL CHECK (length(reason) BETWEEN 1 AND 300),
      getter_logical_tool_call_id TEXT,
      getter_result_handle_id TEXT,
      getter_raw_payload_sha256 TEXT CHECK (
        getter_raw_payload_sha256 IS NULL OR length(getter_raw_payload_sha256) = 64
      ),
      recorded_at TEXT NOT NULL,
      CHECK (
        (getter_logical_tool_call_id IS NULL AND getter_result_handle_id IS NULL AND getter_raw_payload_sha256 IS NULL)
        OR
        (getter_logical_tool_call_id IS NOT NULL AND getter_result_handle_id IS NOT NULL AND getter_raw_payload_sha256 IS NOT NULL)
      ),
      PRIMARY KEY (session_id, source_user_seq, start_logical_tool_call_id),
      UNIQUE (session_id, source_user_seq, getter_logical_tool_call_id),
      FOREIGN KEY (session_id, source_user_seq, start_logical_tool_call_id)
        REFERENCES ${ASYNC_READ_REFINEMENT_START_RECEIPTS_TABLE}(session_id, source_user_seq, start_logical_tool_call_id)
        ON DELETE CASCADE,
      FOREIGN KEY (getter_result_handle_id)
        REFERENCES durable_result_handles(handle_id) ON DELETE RESTRICT
    );
    CREATE TABLE IF NOT EXISTS ${ASYNC_READ_REFINEMENT_RECOVERY_CURSOR_TABLE} (
      cursor_key INTEGER PRIMARY KEY CHECK (cursor_key = 1),
      cursor_recorded_at TEXT NOT NULL,
      cursor_session_id TEXT NOT NULL,
      cursor_source_user_seq INTEGER NOT NULL CHECK (cursor_source_user_seq > 0),
      cursor_logical_tool_call_id TEXT NOT NULL,
      updated_at TEXT NOT NULL
    );
    CREATE TRIGGER IF NOT EXISTS trg_async_read_refinement_completion_excludes_terminal
    BEFORE INSERT ON ${ASYNC_READ_REFINEMENT_COMPLETION_RECEIPTS_TABLE}
    WHEN EXISTS (
      SELECT 1 FROM ${ASYNC_READ_REFINEMENT_TERMINAL_RECEIPTS_TABLE} terminal
       WHERE terminal.session_id = NEW.session_id
         AND terminal.source_user_seq = NEW.source_user_seq
         AND terminal.start_logical_tool_call_id = NEW.start_logical_tool_call_id
    )
    BEGIN SELECT RAISE(ABORT, 'async read refinement already has a terminal receipt'); END;
    CREATE TRIGGER IF NOT EXISTS trg_async_read_refinement_terminal_excludes_completion
    BEFORE INSERT ON ${ASYNC_READ_REFINEMENT_TERMINAL_RECEIPTS_TABLE}
    WHEN EXISTS (
      SELECT 1 FROM ${ASYNC_READ_REFINEMENT_COMPLETION_RECEIPTS_TABLE} completion
       WHERE completion.session_id = NEW.session_id
         AND completion.source_user_seq = NEW.source_user_seq
         AND completion.start_logical_tool_call_id = NEW.start_logical_tool_call_id
    )
    BEGIN SELECT RAISE(ABORT, 'async read refinement already has a completion receipt'); END;
    CREATE TRIGGER IF NOT EXISTS trg_async_read_refinement_terminal_receipt_immutable
    BEFORE UPDATE ON ${ASYNC_READ_REFINEMENT_TERMINAL_RECEIPTS_TABLE}
    BEGIN SELECT RAISE(ABORT, 'async read refinement terminal receipts are immutable'); END;
    CREATE TRIGGER IF NOT EXISTS trg_async_read_refinement_terminal_receipt_delete_immutable
    BEFORE DELETE ON ${ASYNC_READ_REFINEMENT_TERMINAL_RECEIPTS_TABLE}
    WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
    BEGIN SELECT RAISE(ABORT, 'async read refinement terminal receipts are immutable'); END;
  `);
}
function createAsyncReadRefinementTerminalRecoverySchema(db) {
  normalizeEarlierTerminalReceiptTable(db);
  const terminalExists = db.prepare(`SELECT 1 AS ok FROM sqlite_master WHERE type = 'table' AND name = ?`).get(ASYNC_READ_REFINEMENT_TERMINAL_RECEIPTS_TABLE);
  if (!terminalExists) db.exec(terminalReceiptTableSql(ASYNC_READ_REFINEMENT_TERMINAL_RECEIPTS_TABLE));
  db.exec(`
    CREATE TABLE IF NOT EXISTS ${ASYNC_READ_REFINEMENT_RECOVERY_CURSOR_TABLE} (
      cursor_key                    INTEGER PRIMARY KEY CHECK (cursor_key = 1),
      cursor_recorded_at            TEXT NOT NULL,
      cursor_session_id             TEXT NOT NULL,
      cursor_source_user_seq        INTEGER NOT NULL CHECK (cursor_source_user_seq > 0),
      cursor_logical_tool_call_id   TEXT NOT NULL,
      updated_at                    TEXT NOT NULL
    );

    CREATE TRIGGER IF NOT EXISTS trg_async_read_refinement_completion_excludes_terminal
    BEFORE INSERT ON ${ASYNC_READ_REFINEMENT_COMPLETION_RECEIPTS_TABLE}
    WHEN EXISTS (
      SELECT 1 FROM ${ASYNC_READ_REFINEMENT_TERMINAL_RECEIPTS_TABLE} terminal
       WHERE terminal.session_id = NEW.session_id
         AND terminal.source_user_seq = NEW.source_user_seq
         AND terminal.start_logical_tool_call_id = NEW.start_logical_tool_call_id
    )
    BEGIN SELECT RAISE(ABORT, 'async read refinement already has a terminal receipt'); END;

    CREATE TRIGGER IF NOT EXISTS trg_async_read_refinement_terminal_excludes_completion
    BEFORE INSERT ON ${ASYNC_READ_REFINEMENT_TERMINAL_RECEIPTS_TABLE}
    WHEN EXISTS (
      SELECT 1 FROM ${ASYNC_READ_REFINEMENT_COMPLETION_RECEIPTS_TABLE} completion
       WHERE completion.session_id = NEW.session_id
         AND completion.source_user_seq = NEW.source_user_seq
         AND completion.start_logical_tool_call_id = NEW.start_logical_tool_call_id
    )
    BEGIN SELECT RAISE(ABORT, 'async read refinement already has a completion receipt'); END;

    CREATE TRIGGER IF NOT EXISTS trg_async_read_refinement_terminal_receipt_immutable
    BEFORE UPDATE ON ${ASYNC_READ_REFINEMENT_TERMINAL_RECEIPTS_TABLE}
    BEGIN SELECT RAISE(ABORT, 'async read refinement terminal receipts are immutable'); END;
    CREATE TRIGGER IF NOT EXISTS trg_async_read_refinement_terminal_receipt_delete_immutable
    BEFORE DELETE ON ${ASYNC_READ_REFINEMENT_TERMINAL_RECEIPTS_TABLE}
    WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
    BEGIN SELECT RAISE(ABORT, 'async read refinement terminal receipts are immutable'); END;
  `);
}
var ASYNC_READ_REFINEMENT_INTENTS_TABLE, ASYNC_READ_REFINEMENT_START_RECEIPTS_TABLE, ASYNC_READ_REFINEMENT_COMPLETION_RECEIPTS_TABLE, ASYNC_READ_REFINEMENT_TERMINAL_RECEIPTS_TABLE, ASYNC_READ_REFINEMENT_RECOVERY_CURSOR_TABLE, TERMINAL_COLUMNS, EARLIER_TERMINAL_COLUMNS;
var init_async_read_refinement_schema = __esm({
  "src/runtime/harness/async-read-refinement-schema.ts"() {
    "use strict";
    ASYNC_READ_REFINEMENT_INTENTS_TABLE = "async_read_refinement_intents";
    ASYNC_READ_REFINEMENT_START_RECEIPTS_TABLE = "async_read_refinement_start_receipts";
    ASYNC_READ_REFINEMENT_COMPLETION_RECEIPTS_TABLE = "async_read_refinement_completion_receipts";
    ASYNC_READ_REFINEMENT_TERMINAL_RECEIPTS_TABLE = "async_read_refinement_terminal_receipts";
    ASYNC_READ_REFINEMENT_RECOVERY_CURSOR_TABLE = "async_read_refinement_recovery_cursor";
    TERMINAL_COLUMNS = [
      "session_id",
      "source_user_seq",
      "accepted_task_id",
      "start_logical_tool_call_id",
      "receipt_version",
      "recipe_digest",
      "terminal_kind",
      "reason",
      "getter_logical_tool_call_id",
      "getter_result_handle_id",
      "getter_raw_payload_sha256",
      "cancellation_run_attempt_id",
      "cancellation_requested_at",
      "recorded_at"
    ];
    EARLIER_TERMINAL_COLUMNS = TERMINAL_COLUMNS.filter((column) => column !== "cancellation_run_attempt_id" && column !== "cancellation_requested_at");
  }
});

// src/runtime/harness/eventlog-schema.ts
function canonicalProjectionJsonForMigration(value, ancestors = /* @__PURE__ */ new Set()) {
  if (value === null) return "null";
  if (typeof value === "string" || typeof value === "boolean") return JSON.stringify(value);
  if (typeof value === "number") {
    if (!Number.isFinite(value)) throw new Error("logical model result contains a non-finite number");
    return JSON.stringify(value);
  }
  if (typeof value === "undefined") return "null";
  if (typeof value !== "object") throw new Error("logical model result is not JSON-serializable");
  if (ancestors.has(value)) throw new Error("logical model result is cyclic");
  ancestors.add(value);
  try {
    if (Array.isArray(value)) {
      return `[${value.map((entry) => canonicalProjectionJsonForMigration(entry, ancestors)).join(",")}]`;
    }
    const record3 = value;
    return `{${Object.keys(record3).filter((key) => record3[key] !== void 0).sort().map((key) => `${JSON.stringify(key)}:${canonicalProjectionJsonForMigration(record3[key], ancestors)}`).join(",")}}`;
  } finally {
    ancestors.delete(value);
  }
}
function hostDispositionMessageForMigration(disposition, retry) {
  if (disposition === "effect_unknown") {
    return "Execution may have started. Do not retry this call; reconciliation is required.";
  }
  if (retry === "do_not_retry") {
    return "This exact call is unavailable for this request. Do not retry it; use another capability or explain the limitation.";
  }
  return disposition === "not_started" ? "This call was not started because another call in the same frame could not safely proceed. No effect occurred; replan from the paired results." : "This call was refused before execution. No effect occurred; correct the call or choose another capability.";
}
function exactHostProjectionClassForMigration(item, callId, toolName) {
  const rejected = {
    type: "function_call_result",
    callId,
    name: toolName,
    status: "completed",
    output: { type: "text", text: USER_REJECTED_RESULT_FOR_MIGRATION }
  };
  if (canonicalProjectionJsonForMigration(item) === canonicalProjectionJsonForMigration(rejected)) {
    return "user_rejected";
  }
  const output = item.output && typeof item.output === "object" && !Array.isArray(item.output) ? item.output : null;
  if (output?.type !== "text" || typeof output.text !== "string") return null;
  let decoded;
  try {
    decoded = JSON.parse(output.text);
  } catch {
    return null;
  }
  if (!decoded || typeof decoded !== "object" || Array.isArray(decoded)) return null;
  const marker = decoded;
  const disposition = marker.disposition;
  if (marker.protocol !== HOST_RESULT_PROTOCOL_FOR_MIGRATION || disposition !== "refused_pre_dispatch" && disposition !== "not_started" && disposition !== "effect_unknown" || typeof marker.frameDigest !== "string" || !LOWER_HEX_SHA256.test(marker.frameDigest) || !Number.isSafeInteger(marker.frameIndex) || Number(marker.frameIndex) < 0 || !Number.isSafeInteger(marker.frameSize) || Number(marker.frameSize) <= 0 || Number(marker.frameIndex) >= Number(marker.frameSize) || marker.retry !== "replan" && marker.retry !== "do_not_retry") return null;
  const unknown = disposition === "effect_unknown";
  if (unknown && (marker.countsRefusal !== void 0 || marker.effect !== "may_have_started" || marker.retry !== "do_not_retry" || marker.requiresReconciliation !== true || marker.diagnostic !== void 0) || !unknown && (marker.effect !== "none" || marker.requiresReconciliation !== false || marker.countsRefusal !== void 0 && marker.countsRefusal !== true || marker.diagnostic !== void 0 && (typeof marker.diagnostic !== "string" || !marker.diagnostic) || disposition === "not_started" && (marker.retry !== "replan" || marker.countsRefusal !== void 0))) return null;
  const rebuiltMarker = {
    protocol: HOST_RESULT_PROTOCOL_FOR_MIGRATION,
    disposition,
    frameDigest: marker.frameDigest,
    frameIndex: Number(marker.frameIndex),
    frameSize: Number(marker.frameSize),
    ...marker.countsRefusal === true ? { countsRefusal: true } : {},
    effect: unknown ? "may_have_started" : "none",
    retry: marker.retry,
    requiresReconciliation: unknown,
    message: hostDispositionMessageForMigration(disposition, marker.retry),
    ...typeof marker.diagnostic === "string" ? { diagnostic: marker.diagnostic } : {}
  };
  const rebuilt = {
    type: "function_call_result",
    callId,
    name: toolName,
    status: "completed",
    output: { type: "text", text: JSON.stringify(rebuiltMarker) }
  };
  return canonicalProjectionJsonForMigration(item) === canonicalProjectionJsonForMigration(rebuilt) ? disposition : null;
}
function projectionClassForMigration(item, callId, toolName) {
  const reserved = exactHostProjectionClassForMigration(item, callId, toolName);
  if (reserved) return reserved;
  const output = item.output;
  const entries = Array.isArray(output) ? output : [output];
  if (entries.some((entry) => {
    if (!entry || typeof entry !== "object" || Array.isArray(entry)) return false;
    const type = entry.type;
    return type === "image" || type === "file" || type === "input_image" || type === "input_file";
  })) return "media";
  if (typeof output === "string" || output !== null && typeof output === "object" && !Array.isArray(output) && output.type === "text" && typeof output.text === "string" && Object.keys(output).every((key) => key === "type" || key === "text")) return "text";
  return "structured";
}
function projectionMaterialForMigration(item) {
  if (!item || typeof item !== "object" || Array.isArray(item)) return null;
  const row = item;
  const callId = typeof row.callId === "string" ? row.callId.trim() : "";
  const toolName = typeof row.name === "string" ? row.name.trim() : "";
  if (row.namespace !== void 0 && typeof row.namespace !== "string") return null;
  const callNamespace = typeof row.namespace === "string" ? row.namespace : null;
  if (row.type !== "function_call_result" || row.status !== "completed" || !callId || !toolName || row.output === void 0) return null;
  const canonicalBytes = canonicalProjectionJsonForMigration(row);
  return {
    callId,
    toolName,
    callNamespace,
    resultClass: projectionClassForMigration(row, callId, toolName),
    resultItemBytes: Buffer.byteLength(canonicalBytes, "utf8"),
    resultItemSha256: (0, import_node_crypto6.createHash)("sha256").update(canonicalBytes, "utf8").digest("hex")
  };
}
function inspectProtocolForMigration(history, requireBalanced) {
  const calls = /* @__PURE__ */ new Map();
  const open = /* @__PURE__ */ new Map();
  const results = /* @__PURE__ */ new Set();
  let valid = true;
  for (const candidate of history) {
    if (!candidate || typeof candidate !== "object" || Array.isArray(candidate)) continue;
    const item = candidate;
    if (item.type === "function_call") {
      const callId = typeof item.callId === "string" && item.callId ? item.callId : "";
      const name = typeof item.name === "string" && item.name ? item.name : "";
      if (!callId || !name || calls.has(callId) || item.namespace !== void 0 && typeof item.namespace !== "string") {
        valid = false;
        continue;
      }
      const call = {
        name,
        namespace: typeof item.namespace === "string" ? item.namespace : null
      };
      calls.set(callId, call);
      open.set(callId, call);
      continue;
    }
    if (item.type === "function_call_result") {
      const callId = typeof item.callId === "string" && item.callId ? item.callId : "";
      const call = calls.get(callId);
      if (!call || results.has(callId)) {
        valid = false;
        continue;
      }
      if (item.name !== void 0 && item.name !== call.name || item.namespace !== void 0 && item.namespace !== call.namespace) valid = false;
      results.add(callId);
      open.delete(callId);
      continue;
    }
    if ((item.role === "user" || item.role === "assistant" || item.role === "system") && open.size > 0) valid = false;
  }
  if (requireBalanced && open.size > 0) valid = false;
  return { valid, open };
}
function exactOpenAdmissionFrameForMigration(preHistory, frameHistory) {
  const pre = inspectProtocolForMigration(preHistory, true);
  if (!pre.valid) return false;
  if (frameHistory.some((candidate) => candidate && typeof candidate === "object" && !Array.isArray(candidate) && candidate.type === "function_call_result")) return false;
  const frameCalls = frameHistory.filter((candidate) => candidate && typeof candidate === "object" && !Array.isArray(candidate) && candidate.type === "function_call");
  if (frameCalls.length === 0) return false;
  const combined = inspectProtocolForMigration([...preHistory, ...frameHistory], false);
  if (!combined.valid || combined.open.size !== frameCalls.length) return false;
  const ids = frameCalls.map((candidate) => candidate.callId);
  return ids.every((callId) => typeof callId === "string" && combined.open.has(callId)) && new Set(ids).size === ids.length;
}
function acceptedTurnSourceEventDigest(input) {
  return (0, import_node_crypto6.createHash)("sha256").update(JSON.stringify({
    id: input.id,
    sessionId: input.sessionId,
    seq: input.seq,
    turn: input.turn,
    role: input.role,
    type: input.type,
    parentEventId: input.parentEventId,
    dataJson: input.dataJson,
    createdAt: input.createdAt
  })).digest("hex");
}
function acceptedTurnCallAuthorityDigest(input) {
  return (0, import_node_crypto6.createHash)("sha256").update(JSON.stringify({
    protocolVersion: 1,
    ...input
  })).digest("hex");
}
function acceptedTurnCallSurfaceDigest(input) {
  return (0, import_node_crypto6.createHash)("sha256").update(JSON.stringify({
    protocolVersion: 1,
    ...input
  })).digest("hex");
}
function quotedSchemaIdentifier(value) {
  return `"${value.replace(/"/g, '""')}"`;
}
function foreignKeyClosure(db, roots) {
  const relevant = new Set(roots);
  const tables = db.prepare(
    `SELECT name FROM sqlite_master WHERE type = 'table' AND name NOT LIKE 'sqlite_%'`
  ).all().map((row) => row.name);
  let changed = true;
  while (changed) {
    changed = false;
    for (const name of tables) {
      if (relevant.has(name)) continue;
      const reachesRelevantParent = db.prepare(
        `PRAGMA foreign_key_list(${quotedSchemaIdentifier(name)})`
      ).all().some((fk) => relevant.has(fk.table));
      if (reachesRelevantParent) {
        relevant.add(name);
        changed = true;
      }
    }
  }
  return relevant;
}
function createHostAwareLogicalRefinementTrigger(db) {
  db.exec(`
    CREATE TRIGGER trg_logical_call_contract_refinement_once
    BEFORE UPDATE OF argument_digest, effective_argument_digest,
                     refined_at, refinement_event_id
    ON logical_tool_calls
    WHEN NOT (
      OLD.state = 'open'
      AND NEW.state = 'open'
      AND OLD.argument_digest = OLD.raw_argument_digest
      AND OLD.effective_argument_digest IS NULL
      AND OLD.refined_at IS NULL
      AND OLD.refinement_event_id IS NULL
      AND NEW.raw_argument_digest = OLD.raw_argument_digest
      AND NEW.effective_argument_digest IS NOT NULL
      AND length(NEW.effective_argument_digest) = 64
      AND NEW.argument_digest = NEW.effective_argument_digest
      AND NEW.argument_digest != NEW.raw_argument_digest
      AND NEW.refined_at IS NOT NULL
      AND NEW.refinement_event_id IS NOT NULL
      AND EXISTS (
        SELECT 1 FROM accepted_turn_call_authorities a
         WHERE a.session_id = OLD.session_id
           AND a.source_user_seq = OLD.source_user_seq
           AND a.accepted_task_id = OLD.accepted_task_id
           AND a.state = 'open'
           AND (
             a.authority_kind IN (
               'host_v1','host_v1_read_only','workflow_v1_read_only','workflow_v2_paginated_read'
             )
             OR EXISTS (
               SELECT 1 FROM accepted_task_resolutions r
                WHERE r.session_id = OLD.session_id
                  AND r.source_user_seq = OLD.source_user_seq
                  AND r.accepted_task_id = OLD.accepted_task_id
                  AND r.state = 'open'
             )
           )
      )
      AND NOT EXISTS (
        SELECT 1 FROM physical_dispatches p
         WHERE p.session_id = OLD.session_id
           AND p.source_user_seq = OLD.source_user_seq
           AND p.logical_tool_call_id = OLD.logical_tool_call_id
      )
      AND NOT EXISTS (
        SELECT 1 FROM logical_call_settlements s
         WHERE s.session_id = OLD.session_id
           AND s.source_user_seq = OLD.source_user_seq
           AND s.logical_tool_call_id = OLD.logical_tool_call_id
      )
    )
    BEGIN
      SELECT RAISE(ABORT, 'logical call contract refinement is not monotonic');
    END;
  `);
}
function rebuildLogicalToolCallAuthorityParent(db) {
  const table = db.prepare(
    `SELECT sql FROM sqlite_master WHERE type = 'table' AND name = 'logical_tool_calls'`
  ).get();
  if (!table?.sql) return;
  const root = db.prepare(
    `SELECT 1 AS ok FROM sqlite_master
      WHERE type = 'table' AND name = 'accepted_turn_call_authorities'`
  ).get();
  if (!root) throw new Error("schema v50 call-authority root is missing");
  const parentFks = db.prepare("PRAGMA foreign_key_list(logical_tool_calls)").all();
  const currentParent = new Set(parentFks.filter((row) => row.from === "session_id" || row.from === "source_user_seq").map((row) => row.table));
  if (currentParent.size === 1 && currentParent.has("accepted_turn_call_authorities")) return;
  if (currentParent.size !== 1 || !currentParent.has("accepted_task_resolutions")) {
    throw new Error("schema v50 refuses an unknown logical-tool-call parent");
  }
  const columns = db.prepare("PRAGMA table_info(logical_tool_calls)").all().map((column) => column.name);
  if (!columns.length) throw new Error("schema v50 logical-tool-call columns are missing");
  const beforeRows = db.prepare("SELECT COUNT(*) AS n FROM logical_tool_calls").get().n;
  const objects = db.prepare(`
    SELECT type, name, sql FROM sqlite_master
     WHERE tbl_name = 'logical_tool_calls'
       AND type IN ('index','trigger') AND sql IS NOT NULL
     ORDER BY type, name
  `).all();
  const childTables = db.prepare(`SELECT name FROM sqlite_master WHERE type = 'table'`).all().map((row) => row.name).filter((name) => db.prepare(`PRAGMA foreign_key_list(${quotedSchemaIdentifier(name)})`).all().some((fk) => fk.table === "logical_tool_calls"));
  const childSnapshot = new Map(childTables.map((name) => [name, {
    rows: db.prepare(`SELECT COUNT(*) AS n FROM ${quotedSchemaIdentifier(name)}`).get().n,
    foreignKeys: JSON.stringify(db.prepare(`PRAGMA foreign_key_list(${quotedSchemaIdentifier(name)})`).all())
  }]));
  const parentReference = /REFERENCES\s+accepted_task_resolutions\s*\(\s*session_id\s*,\s*source_user_seq\s*\)/gi;
  const matches = table.sql.match(parentReference) ?? [];
  if (matches.length !== 1) {
    throw new Error(`schema v50 expected one logical parent reference, found ${matches.length}`);
  }
  const rebuiltSql = table.sql.replace(
    parentReference,
    "REFERENCES accepted_turn_call_authorities(session_id, source_user_seq)"
  );
  const priorLegacyRename = Number(db.pragma("legacy_alter_table", { simple: true })) === 1;
  db.pragma("legacy_alter_table = ON");
  try {
    for (const object of objects) {
      db.exec(`DROP ${object.type.toUpperCase()} ${quotedSchemaIdentifier(object.name)}`);
    }
    db.exec("ALTER TABLE logical_tool_calls RENAME TO logical_tool_calls_v49");
    db.exec(rebuiltSql);
    const columnList = columns.map(quotedSchemaIdentifier).join(", ");
    db.exec(`INSERT INTO logical_tool_calls (${columnList})
      SELECT ${columnList} FROM logical_tool_calls_v49`);
    const copiedRows = db.prepare("SELECT COUNT(*) AS n FROM logical_tool_calls").get().n;
    if (copiedRows !== beforeRows) {
      throw new Error(`schema v50 logical row-count mismatch: ${beforeRows} -> ${copiedRows}`);
    }
    db.exec("DROP TABLE logical_tool_calls_v49");
    for (const object of objects) {
      if (object.name === "trg_logical_call_contract_refinement_once") continue;
      db.exec(object.sql);
    }
    createHostAwareLogicalRefinementTrigger(db);
  } finally {
    db.pragma(`legacy_alter_table = ${priorLegacyRename ? "ON" : "OFF"}`);
  }
  for (const [name, before] of childSnapshot) {
    const rows = db.prepare(`SELECT COUNT(*) AS n FROM ${quotedSchemaIdentifier(name)}`).get().n;
    const foreignKeys = JSON.stringify(db.prepare(`PRAGMA foreign_key_list(${quotedSchemaIdentifier(name)})`).all());
    if (rows !== before.rows || foreignKeys !== before.foreignKeys) {
      throw new Error(`schema v50 changed child-table rows or foreign keys for ${name}`);
    }
  }
  const relevantTables = /* @__PURE__ */ new Set(["accepted_turn_call_authorities", "logical_tool_calls", ...childTables]);
  const violations = db.pragma("foreign_key_check");
  const relevantViolations = violations.filter((violation) => relevantTables.has(violation.table));
  if (relevantViolations.length > 0) {
    throw new Error(`schema v50 foreign-key check failed for ${relevantViolations.length} call-authority row(s)`);
  }
  const integrity = db.pragma("integrity_check");
  if (integrity.length !== 1 || integrity[0]?.integrity_check !== "ok") {
    throw new Error(`schema v50 integrity check failed: ${JSON.stringify(integrity).slice(0, 240)}`);
  }
}
function createV51AcceptedCallAuthoritySchema(db, includePaginated = false, includeProductionHost = false, includeWorkflowV3 = false) {
  db.exec(`
    CREATE TABLE accepted_turn_call_authorities (
      session_id                TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
      source_user_seq           INTEGER NOT NULL CHECK (source_user_seq > 0),
      accepted_task_id          TEXT NOT NULL UNIQUE,
      authority_protocol        INTEGER NOT NULL CHECK (authority_protocol = 1),
      authority_kind            TEXT NOT NULL
                                CHECK (authority_kind IN (
                                  'turn_graph','host_v1_read_only','workflow_v1_read_only'
                                  ${includeProductionHost ? ",'host_v1'" : ""}
                                  ${includePaginated ? ",'workflow_v2_paginated_read'" : ""}
                                  ${includeWorkflowV3 ? ",'workflow_v3_call'" : ""}
                                )),
      source_event_id           TEXT NOT NULL REFERENCES events(id) ON DELETE RESTRICT,
      source_event_digest       TEXT NOT NULL CHECK (length(source_event_digest) = 64),
      source_turn               INTEGER NOT NULL CHECK (source_turn >= 0),
      engine_version            TEXT NOT NULL CHECK (length(engine_version) BETWEEN 1 AND 128),
      surface_version           TEXT NOT NULL CHECK (length(surface_version) BETWEEN 1 AND 128),
      surface_digest            TEXT NOT NULL CHECK (length(surface_digest) = 64),
      effect_ceiling            TEXT NOT NULL CHECK (length(effect_ceiling) BETWEEN 1 AND 64),
      effect_bounds_json        TEXT NOT NULL
                                CHECK (json_valid(effect_bounds_json)
                                  AND json_type(effect_bounds_json) = 'array'),
      max_logical_calls         INTEGER CHECK (max_logical_calls IS NULL OR max_logical_calls > 0),
      max_parallel_calls        INTEGER CHECK (max_parallel_calls IS NULL OR max_parallel_calls > 0),
      catalog_revision_digest   TEXT
                                CHECK (catalog_revision_digest IS NULL OR length(catalog_revision_digest) = 64),
      binding_revision_digest   TEXT
                                CHECK (binding_revision_digest IS NULL OR length(binding_revision_digest) = 64),
      graph_event_id            TEXT REFERENCES events(id) ON DELETE RESTRICT,
      graph_hash                TEXT CHECK (graph_hash IS NULL OR length(graph_hash) = 64),
      workflow_activation_id    TEXT REFERENCES workflow_node_invocation_activations(activation_id)
                                ON DELETE RESTRICT,
      workflow_activation_digest TEXT
                                CHECK (workflow_activation_digest IS NULL OR length(workflow_activation_digest) = 64),
      workflow_id               TEXT CHECK (workflow_id IS NULL OR length(workflow_id) BETWEEN 1 AND 256),
      workflow_revision         INTEGER CHECK (workflow_revision IS NULL OR workflow_revision > 0),
      workflow_digest           TEXT CHECK (workflow_digest IS NULL OR length(workflow_digest) = 64),
      run_id                    TEXT CHECK (run_id IS NULL OR length(run_id) BETWEEN 1 AND 256),
      run_occurrence_id         TEXT CHECK (run_occurrence_id IS NULL OR length(run_occurrence_id) BETWEEN 1 AND 256),
      workflow_node_id          TEXT CHECK (workflow_node_id IS NULL OR length(workflow_node_id) BETWEEN 1 AND 256),
      workflow_node_attempt     INTEGER CHECK (workflow_node_attempt IS NULL OR workflow_node_attempt > 0),
      invocation_plan_digest    TEXT CHECK (invocation_plan_digest IS NULL OR length(invocation_plan_digest) = 64),
      binding_snapshot_digest   TEXT CHECK (binding_snapshot_digest IS NULL OR length(binding_snapshot_digest) = 64),
      control_digest            TEXT CHECK (control_digest IS NULL OR length(control_digest) = 64),
      workflow_logical_call_id  TEXT
                                CHECK (workflow_logical_call_id IS NULL OR length(workflow_logical_call_id) BETWEEN 1 AND 512),
      authority_digest          TEXT NOT NULL UNIQUE CHECK (length(authority_digest) = 64),
      state                     TEXT NOT NULL DEFAULT 'open'
                                CHECK (state IN ('open','closed','conflict')),
      revision                  INTEGER NOT NULL DEFAULT 0 CHECK (revision >= 0),
      opened_at                 TEXT NOT NULL,
      closed_at                 TEXT,
      close_reason              TEXT CHECK (close_reason IS NULL OR length(close_reason) BETWEEN 1 AND 160),
      PRIMARY KEY (session_id, source_user_seq),
      CHECK (
        authority_kind IN ('workflow_v1_read_only'
          ${includePaginated ? ",'workflow_v2_paginated_read'" : ""}
          ${includeWorkflowV3 ? ",'workflow_v3_call'" : ""})
        OR accepted_task_id = 'task:' || session_id || '#' || source_user_seq
      ),
      CHECK (
        (state = 'open' AND closed_at IS NULL AND close_reason IS NULL)
        OR (state IN ('closed','conflict') AND closed_at IS NOT NULL AND close_reason IS NOT NULL)
      ),
      CHECK (
        (authority_kind = 'host_v1_read_only'
          AND source_turn >= 0
          AND engine_version = 'host_v1_read_only'
          AND effect_ceiling = 'read_compute_host_only'
          AND effect_bounds_json = '["compute","host_only","read"]'
          AND max_logical_calls IS NOT NULL
          AND max_parallel_calls IS NOT NULL
          AND max_parallel_calls <= max_logical_calls
          AND catalog_revision_digest IS NOT NULL
          AND binding_revision_digest IS NOT NULL
          AND graph_event_id IS NULL
          AND graph_hash IS NULL
          AND workflow_activation_id IS NULL
          AND workflow_activation_digest IS NULL
          AND workflow_id IS NULL
          AND workflow_revision IS NULL
          AND workflow_digest IS NULL
          AND run_id IS NULL
          AND run_occurrence_id IS NULL
          AND workflow_node_id IS NULL
          AND workflow_node_attempt IS NULL
          AND invocation_plan_digest IS NULL
          AND binding_snapshot_digest IS NULL
          AND control_digest IS NULL
          AND workflow_logical_call_id IS NULL)
        ${includeProductionHost ? `
        OR
        (authority_kind = 'host_v1'
          AND source_turn >= 0
          AND engine_version = 'host_v1'
          AND surface_version = 'configured_harness_capability_surface_v1'
          AND effect_ceiling = 'admin'
          AND effect_bounds_json = '["admin","compute","external_write","host_only","local_write","read"]'
          AND max_logical_calls IS NOT NULL
          AND max_parallel_calls IS NOT NULL
          AND max_parallel_calls <= max_logical_calls
          AND catalog_revision_digest IS NOT NULL
          AND binding_revision_digest IS NOT NULL
          AND graph_event_id IS NULL
          AND graph_hash IS NULL
          AND workflow_activation_id IS NULL
          AND workflow_activation_digest IS NULL
          AND workflow_id IS NULL
          AND workflow_revision IS NULL
          AND workflow_digest IS NULL
          AND run_id IS NULL
          AND run_occurrence_id IS NULL
          AND workflow_node_id IS NULL
          AND workflow_node_attempt IS NULL
          AND invocation_plan_digest IS NULL
          AND binding_snapshot_digest IS NULL
          AND control_digest IS NULL
          AND workflow_logical_call_id IS NULL)
        ` : ""}
        OR
        (authority_kind = 'turn_graph'
          AND source_turn >= 0
          AND surface_version = 'turn_graph_ir_v1'
          AND effect_bounds_json = '[]'
          AND catalog_revision_digest IS NULL
          AND graph_event_id IS NOT NULL
          AND graph_hash IS NOT NULL
          AND binding_revision_digest = graph_hash
          AND max_logical_calls IS NULL
          AND max_parallel_calls IS NULL
          AND workflow_activation_id IS NULL
          AND workflow_activation_digest IS NULL
          AND workflow_id IS NULL
          AND workflow_revision IS NULL
          AND workflow_digest IS NULL
          AND run_id IS NULL
          AND run_occurrence_id IS NULL
          AND workflow_node_id IS NULL
          AND workflow_node_attempt IS NULL
          AND invocation_plan_digest IS NULL
          AND binding_snapshot_digest IS NULL
          AND control_digest IS NULL
          AND workflow_logical_call_id IS NULL)
        OR
        (authority_kind = 'workflow_v1_read_only'
          AND engine_version = 'workflow_v1_read_only'
          AND surface_version = 'workflow_node_invocation_plan_v1'
          AND effect_ceiling = 'read'
          AND effect_bounds_json = '["read"]'
          AND max_logical_calls = 1
          AND max_parallel_calls = 1
          AND catalog_revision_digest = binding_snapshot_digest
          AND binding_revision_digest = invocation_plan_digest
          AND graph_event_id IS NULL
          AND graph_hash IS NULL
          AND workflow_activation_id IS NOT NULL
          AND workflow_activation_digest IS NOT NULL
          AND workflow_id IS NOT NULL
          AND workflow_revision IS NOT NULL
          AND workflow_digest IS NOT NULL
          AND run_id IS NOT NULL
          AND run_occurrence_id IS NOT NULL
          AND workflow_node_id IS NOT NULL
          AND workflow_node_attempt IS NOT NULL
          AND invocation_plan_digest IS NOT NULL
          AND binding_snapshot_digest IS NOT NULL
          AND control_digest IS NOT NULL
          AND workflow_logical_call_id IS NOT NULL)
        ${includePaginated ? `
        OR
        (authority_kind = 'workflow_v2_paginated_read'
          AND engine_version = 'workflow_v2_paginated_read'
          AND surface_version = 'workflow_paginated_read_plan_v1'
          AND effect_ceiling = 'read'
          AND effect_bounds_json = '["read"]'
          AND max_logical_calls IS NOT NULL
          AND max_logical_calls BETWEEN 1 AND 10000
          AND max_parallel_calls = 1
          AND catalog_revision_digest = binding_snapshot_digest
          AND binding_revision_digest = invocation_plan_digest
          AND graph_event_id IS NULL
          AND graph_hash IS NULL
          AND workflow_activation_id IS NULL
          AND workflow_activation_digest IS NOT NULL
          AND workflow_id IS NOT NULL
          AND workflow_revision IS NOT NULL
          AND workflow_digest IS NOT NULL
          AND run_id IS NOT NULL
          AND run_occurrence_id IS NOT NULL
          AND workflow_node_id IS NOT NULL
          AND workflow_node_attempt IS NOT NULL
          AND invocation_plan_digest IS NOT NULL
          AND binding_snapshot_digest IS NOT NULL
          AND control_digest IS NOT NULL
          AND workflow_logical_call_id IS NULL)
        ` : ""}
        ${includeWorkflowV3 ? `
        OR
        (authority_kind = 'workflow_v3_call'
          AND engine_version = 'workflow_v3_call'
          AND surface_version = 'workflow_node_invocation_plan_v1'
          AND effect_ceiling IN ('host_only','local_write','external_write','admin')
          AND effect_bounds_json = '["' || effect_ceiling || '"]'
          AND max_logical_calls = 1
          AND max_parallel_calls = 1
          AND catalog_revision_digest = binding_snapshot_digest
          AND binding_revision_digest IS NOT NULL
          AND graph_event_id IS NULL
          AND graph_hash IS NULL
          AND workflow_activation_id IS NOT NULL
          AND workflow_activation_digest IS NOT NULL
          AND workflow_id IS NOT NULL
          AND workflow_revision IS NOT NULL
          AND workflow_digest IS NOT NULL
          AND run_id IS NOT NULL
          AND run_occurrence_id IS NOT NULL
          AND workflow_node_id IS NOT NULL
          AND workflow_node_attempt IS NOT NULL
          AND invocation_plan_digest IS NOT NULL
          AND binding_snapshot_digest IS NOT NULL
          AND control_digest IS NOT NULL
          AND workflow_logical_call_id IS NOT NULL)
        ` : ""}
      )
    );

    CREATE INDEX idx_accepted_turn_call_authority_state
      ON accepted_turn_call_authorities(authority_kind, state, opened_at);

    CREATE INDEX idx_accepted_workflow_call_authority_occurrence
      ON accepted_turn_call_authorities(
        workflow_id, workflow_revision, run_occurrence_id,
        workflow_node_id, workflow_node_attempt
      ) WHERE authority_kind = 'workflow_v1_read_only';

    ${includePaginated ? `
    CREATE INDEX idx_accepted_paginated_workflow_call_authority_occurrence
      ON accepted_turn_call_authorities(
        workflow_id, workflow_revision, run_occurrence_id,
        workflow_node_id, workflow_node_attempt
      ) WHERE authority_kind = 'workflow_v2_paginated_read';
    ` : ""}

    ${includeWorkflowV3 ? `
    CREATE INDEX idx_accepted_workflow_v3_call_authority_occurrence
      ON accepted_turn_call_authorities(
        workflow_id, workflow_revision, run_occurrence_id,
        workflow_node_id, workflow_node_attempt
      ) WHERE authority_kind = 'workflow_v3_call';
    ` : ""}

    CREATE TRIGGER trg_accepted_turn_call_authority_source_exact
    BEFORE INSERT ON accepted_turn_call_authorities
    WHEN NEW.authority_kind NOT IN ('workflow_v1_read_only'
      ${includePaginated ? ",'workflow_v2_paginated_read'" : ""}
      ${includeWorkflowV3 ? ",'workflow_v3_call'" : ""}) AND NOT EXISTS (
      SELECT 1 FROM events e
       WHERE e.id = NEW.source_event_id
         AND e.seq = NEW.source_user_seq
         AND e.session_id = NEW.session_id
         AND e.turn = NEW.source_turn
         AND e.role = 'user'
         AND e.type = 'user_input_received'
    )
    BEGIN
      SELECT RAISE(ABORT, 'accepted-turn call authority requires its exact user source event');
    END;

    CREATE TRIGGER trg_accepted_workflow_call_authority_source_exact
    BEFORE INSERT ON accepted_turn_call_authorities
    WHEN NEW.authority_kind = 'workflow_v1_read_only' AND NOT EXISTS (
      SELECT 1 FROM workflow_node_invocation_activations w
       WHERE w.activation_id = NEW.workflow_activation_id
         AND w.activation_digest = NEW.workflow_activation_digest
         AND w.authority_root_id = NEW.accepted_task_id
         AND w.session_id = NEW.session_id
         AND w.source_event_seq = NEW.source_user_seq
         AND w.source_event_id = NEW.source_event_id
         AND w.source_event_digest = NEW.source_event_digest
         AND w.workflow_id = NEW.workflow_id
         AND w.workflow_revision = NEW.workflow_revision
         AND w.workflow_digest = NEW.workflow_digest
         AND w.run_id = NEW.run_id
         AND w.run_occurrence_id = NEW.run_occurrence_id
         AND w.node_id = NEW.workflow_node_id
         AND w.node_attempt = NEW.workflow_node_attempt
         AND w.invocation_plan_digest = NEW.invocation_plan_digest
         AND w.binding_snapshot_digest = NEW.binding_snapshot_digest
         AND w.control_digest = NEW.control_digest
         AND w.logical_call_id = NEW.workflow_logical_call_id
    )
    BEGIN
      SELECT RAISE(ABORT, 'workflow call authority requires its exact durable activation');
    END;

    ${includePaginated ? `
    CREATE TRIGGER trg_accepted_paginated_workflow_call_authority_source_exact
    BEFORE INSERT ON accepted_turn_call_authorities
    WHEN NEW.authority_kind = 'workflow_v2_paginated_read' AND NOT EXISTS (
      SELECT 1 FROM workflow_paginated_read_activations w
       WHERE w.authority_root_id = NEW.accepted_task_id
         AND w.activation_digest = NEW.workflow_activation_digest
         AND w.session_id = NEW.session_id
         AND w.source_event_seq = NEW.source_user_seq
         AND w.source_event_id = NEW.source_event_id
         AND w.source_event_digest = NEW.source_event_digest
         AND w.workflow_id = NEW.workflow_id
         AND w.workflow_revision = NEW.workflow_revision
         AND w.workflow_digest = NEW.workflow_digest
         AND w.run_id = NEW.run_id
         AND w.run_occurrence_id = NEW.run_occurrence_id
         AND w.node_id = NEW.workflow_node_id
         AND w.node_attempt = NEW.workflow_node_attempt
         AND w.invocation_plan_digest = NEW.invocation_plan_digest
         AND w.binding_snapshot_digest = NEW.binding_snapshot_digest
         AND w.control_digest = NEW.control_digest
         AND w.max_pages = NEW.max_logical_calls
    )
    BEGIN
      SELECT RAISE(ABORT, 'paginated workflow call authority requires its exact durable activation');
    END;
    ` : ""}

    ${includeWorkflowV3 ? `
    CREATE TRIGGER trg_accepted_workflow_v3_call_authority_source_exact
    BEFORE INSERT ON accepted_turn_call_authorities
    WHEN NEW.authority_kind = 'workflow_v3_call' AND NOT EXISTS (
      SELECT 1
        FROM workflow_node_invocation_activations w
        JOIN workflow_v3_call_activation_bindings b
          ON b.activation_id = w.activation_id
       WHERE w.activation_id = NEW.workflow_activation_id
         AND w.activation_digest = NEW.workflow_activation_digest
         AND w.authority_root_id = NEW.accepted_task_id
         AND w.session_id = NEW.session_id
         AND w.source_event_seq = NEW.source_user_seq
         AND w.source_event_id = NEW.source_event_id
         AND w.source_event_digest = NEW.source_event_digest
         AND w.workflow_id = NEW.workflow_id
         AND w.workflow_revision = NEW.workflow_revision
         AND w.workflow_digest = NEW.workflow_digest
         AND w.run_id = NEW.run_id
         AND w.run_occurrence_id = NEW.run_occurrence_id
         AND w.node_id = NEW.workflow_node_id
         AND w.node_attempt = NEW.workflow_node_attempt
         AND w.invocation_plan_digest = NEW.invocation_plan_digest
         AND w.binding_snapshot_digest = NEW.binding_snapshot_digest
         AND w.control_digest = NEW.control_digest
         AND w.logical_call_id = NEW.workflow_logical_call_id
         AND b.session_id = NEW.session_id
         AND b.authority_binding_digest = NEW.binding_revision_digest
         AND b.effect = NEW.effect_ceiling
    )
    BEGIN
      SELECT RAISE(ABORT, 'workflow v3 call authority requires its exact durable activation binding');
    END;
    ` : ""}

    CREATE TRIGGER trg_accepted_turn_call_authority_graph_exact
    BEFORE INSERT ON accepted_turn_call_authorities
    WHEN NEW.authority_kind = 'turn_graph' AND NOT EXISTS (
      SELECT 1 FROM accepted_task_resolutions r
       WHERE r.session_id = NEW.session_id
         AND r.source_user_seq = NEW.source_user_seq
         AND r.accepted_task_id = NEW.accepted_task_id
         AND r.graph_event_id = NEW.graph_event_id
         AND r.graph_hash = NEW.graph_hash
         AND r.compiler_version = NEW.engine_version
         AND r.effect_ceiling = NEW.effect_ceiling
    )
    BEGIN
      SELECT RAISE(ABORT, 'graph call authority requires its exact graph resolution');
    END;

    CREATE TRIGGER trg_accepted_turn_call_authority_host_graphless
    BEFORE INSERT ON accepted_turn_call_authorities
    WHEN NEW.authority_kind != 'turn_graph' AND EXISTS (
      SELECT 1 FROM accepted_task_resolutions r
       WHERE r.session_id = NEW.session_id
         AND r.source_user_seq = NEW.source_user_seq
    )
    BEGIN
      SELECT RAISE(ABORT, 'non-graph call authority cannot replace graph resolution authority');
    END;

    CREATE TRIGGER trg_accepted_turn_call_authority_identity_immutable
    BEFORE UPDATE ON accepted_turn_call_authorities
    WHEN OLD.session_id IS NOT NEW.session_id
      OR OLD.source_user_seq IS NOT NEW.source_user_seq
      OR OLD.accepted_task_id IS NOT NEW.accepted_task_id
      OR OLD.authority_protocol IS NOT NEW.authority_protocol
      OR OLD.authority_kind IS NOT NEW.authority_kind
      OR OLD.source_event_id IS NOT NEW.source_event_id
      OR OLD.source_event_digest IS NOT NEW.source_event_digest
      OR OLD.source_turn IS NOT NEW.source_turn
      OR OLD.engine_version IS NOT NEW.engine_version
      OR OLD.surface_version IS NOT NEW.surface_version
      OR OLD.surface_digest IS NOT NEW.surface_digest
      OR OLD.effect_ceiling IS NOT NEW.effect_ceiling
      OR OLD.effect_bounds_json IS NOT NEW.effect_bounds_json
      OR OLD.max_logical_calls IS NOT NEW.max_logical_calls
      OR OLD.max_parallel_calls IS NOT NEW.max_parallel_calls
      OR OLD.catalog_revision_digest IS NOT NEW.catalog_revision_digest
      OR OLD.binding_revision_digest IS NOT NEW.binding_revision_digest
      OR OLD.graph_event_id IS NOT NEW.graph_event_id
      OR OLD.graph_hash IS NOT NEW.graph_hash
      OR OLD.workflow_activation_id IS NOT NEW.workflow_activation_id
      OR OLD.workflow_activation_digest IS NOT NEW.workflow_activation_digest
      OR OLD.workflow_id IS NOT NEW.workflow_id
      OR OLD.workflow_revision IS NOT NEW.workflow_revision
      OR OLD.workflow_digest IS NOT NEW.workflow_digest
      OR OLD.run_id IS NOT NEW.run_id
      OR OLD.run_occurrence_id IS NOT NEW.run_occurrence_id
      OR OLD.workflow_node_id IS NOT NEW.workflow_node_id
      OR OLD.workflow_node_attempt IS NOT NEW.workflow_node_attempt
      OR OLD.invocation_plan_digest IS NOT NEW.invocation_plan_digest
      OR OLD.binding_snapshot_digest IS NOT NEW.binding_snapshot_digest
      OR OLD.control_digest IS NOT NEW.control_digest
      OR OLD.workflow_logical_call_id IS NOT NEW.workflow_logical_call_id
      OR OLD.authority_digest IS NOT NEW.authority_digest
      OR OLD.opened_at IS NOT NEW.opened_at
    BEGIN
      SELECT RAISE(ABORT, 'accepted-turn call authority identity is immutable');
    END;

    CREATE TRIGGER trg_accepted_turn_call_authority_state_machine
    BEFORE UPDATE OF state, revision, closed_at, close_reason
    ON accepted_turn_call_authorities
    WHEN NOT (
      NEW.revision = OLD.revision + 1
      AND (
        (OLD.state = 'open' AND NEW.state IN ('closed','conflict'))
        OR (OLD.state = 'closed' AND NEW.state = 'conflict')
      )
      AND NEW.closed_at IS NOT NULL
      AND NEW.close_reason IS NOT NULL
    )
    BEGIN
      SELECT RAISE(ABORT, 'invalid accepted-turn call authority transition');
    END;
  `);
}
function createWorkflowNodeInvocationActivationSchema(db) {
  db.exec(`
    CREATE TABLE IF NOT EXISTS workflow_node_invocation_activations (
      activation_id            TEXT PRIMARY KEY CHECK (length(activation_id) BETWEEN 1 AND 512),
      authority_root_id        TEXT NOT NULL UNIQUE CHECK (length(authority_root_id) BETWEEN 1 AND 512),
      session_id               TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
      source_event_seq         INTEGER NOT NULL CHECK (source_event_seq > 0),
      source_event_id          TEXT NOT NULL UNIQUE REFERENCES events(id) ON DELETE RESTRICT,
      source_event_digest      TEXT NOT NULL CHECK (length(source_event_digest) = 64),
      workflow_id              TEXT NOT NULL CHECK (length(workflow_id) BETWEEN 1 AND 256),
      workflow_revision        INTEGER NOT NULL CHECK (workflow_revision > 0),
      workflow_digest          TEXT NOT NULL CHECK (length(workflow_digest) = 64),
      run_id                   TEXT NOT NULL CHECK (length(run_id) BETWEEN 1 AND 256),
      run_occurrence_id        TEXT NOT NULL CHECK (length(run_occurrence_id) BETWEEN 1 AND 256),
      node_id                  TEXT NOT NULL CHECK (length(node_id) BETWEEN 1 AND 256),
      node_attempt             INTEGER NOT NULL CHECK (node_attempt > 0),
      invocation_plan_digest  TEXT NOT NULL CHECK (length(invocation_plan_digest) = 64),
      binding_snapshot_digest TEXT NOT NULL CHECK (length(binding_snapshot_digest) = 64),
      control_digest          TEXT NOT NULL CHECK (length(control_digest) = 64),
      logical_call_id         TEXT NOT NULL CHECK (length(logical_call_id) BETWEEN 1 AND 512),
      one_shot_authorization_approval_id TEXT
                                REFERENCES pending_approvals(approval_id) ON DELETE RESTRICT,
      one_shot_authorization_resume_key TEXT,
      one_shot_authorization_decision_digest TEXT
                                CHECK (one_shot_authorization_decision_digest IS NULL
                                  OR length(one_shot_authorization_decision_digest) = 64),
      activation_digest       TEXT NOT NULL UNIQUE CHECK (length(activation_digest) = 64),
      activated_at            TEXT NOT NULL,
      UNIQUE (session_id, source_event_seq),
      CHECK (
        (one_shot_authorization_approval_id IS NULL
          AND one_shot_authorization_resume_key IS NULL
          AND one_shot_authorization_decision_digest IS NULL)
        OR
        (one_shot_authorization_approval_id IS NOT NULL
          AND length(one_shot_authorization_approval_id) BETWEEN 1 AND 128
          AND one_shot_authorization_resume_key IS NOT NULL
          AND length(one_shot_authorization_resume_key) BETWEEN 1 AND 1024
          AND one_shot_authorization_decision_digest IS NOT NULL)
      ),
      UNIQUE (
        workflow_id, workflow_revision, run_id, run_occurrence_id,
        node_id, node_attempt
      )
    );

    CREATE INDEX IF NOT EXISTS idx_workflow_node_invocation_activation_run
      ON workflow_node_invocation_activations(
        workflow_id, workflow_revision, run_occurrence_id, node_id, node_attempt
      );

    CREATE UNIQUE INDEX IF NOT EXISTS idx_workflow_node_invocation_activation_one_shot_authorization
      ON workflow_node_invocation_activations(one_shot_authorization_approval_id)
      WHERE one_shot_authorization_approval_id IS NOT NULL;

    CREATE TRIGGER IF NOT EXISTS trg_workflow_node_invocation_activation_source_exact
    BEFORE INSERT ON workflow_node_invocation_activations
    WHEN NOT EXISTS (
      SELECT 1 FROM events e
       WHERE e.id = NEW.source_event_id
         AND e.session_id = NEW.session_id
         AND e.seq = NEW.source_event_seq
         AND e.turn = 0
         AND e.role = 'system'
         AND e.type = 'workflow_node_invocation_activated'
         AND e.created_at = NEW.activated_at
         AND json_valid(e.data_json)
         AND json_extract(e.data_json, '$.protocolVersion') = 1
         AND json_extract(e.data_json, '$.activationId') = NEW.activation_id
         AND json_extract(e.data_json, '$.authorityRootId') = NEW.authority_root_id
         AND json_extract(e.data_json, '$.activationDigest') = NEW.activation_digest
         AND json_extract(e.data_json, '$.workflowId') = NEW.workflow_id
         AND json_extract(e.data_json, '$.workflowRevision') = NEW.workflow_revision
         AND json_extract(e.data_json, '$.workflowDigest') = NEW.workflow_digest
         AND json_extract(e.data_json, '$.runId') = NEW.run_id
         AND json_extract(e.data_json, '$.runOccurrenceId') = NEW.run_occurrence_id
         AND json_extract(e.data_json, '$.nodeId') = NEW.node_id
         AND json_extract(e.data_json, '$.nodeAttempt') = NEW.node_attempt
         AND json_extract(e.data_json, '$.invocationPlanDigest') = NEW.invocation_plan_digest
         AND json_extract(e.data_json, '$.bindingSnapshotDigest') = NEW.binding_snapshot_digest
         AND json_extract(e.data_json, '$.controlDigest') = NEW.control_digest
         AND json_extract(e.data_json, '$.logicalCallId') = NEW.logical_call_id
         AND (
           (NEW.one_shot_authorization_approval_id IS NULL
             AND json_type(e.data_json, '$.oneShotActivationAuthorization') IS NULL
             AND (SELECT COUNT(*) FROM json_each(e.data_json)) = 15)
           OR
           (NEW.one_shot_authorization_approval_id IS NOT NULL
             AND json_type(e.data_json, '$.oneShotActivationAuthorization') = 'object'
             AND json_extract(e.data_json, '$.oneShotActivationAuthorization.approvalId')
                   = NEW.one_shot_authorization_approval_id
             AND json_extract(e.data_json, '$.oneShotActivationAuthorization.resumeKey')
                   = NEW.one_shot_authorization_resume_key
             AND json_extract(e.data_json, '$.oneShotActivationAuthorization.decisionDigest')
                   = NEW.one_shot_authorization_decision_digest
             AND (SELECT COUNT(*)
                    FROM json_each(e.data_json, '$.oneShotActivationAuthorization')) = 3
             AND (SELECT COUNT(*) FROM json_each(e.data_json)) = 16)
         )
    )
    BEGIN
      SELECT RAISE(ABORT, 'workflow activation requires its exact system event');
    END;

    CREATE TRIGGER IF NOT EXISTS trg_workflow_node_invocation_activation_authorization_exact
    BEFORE INSERT ON workflow_node_invocation_activations
    WHEN NEW.one_shot_authorization_approval_id IS NOT NULL AND NOT EXISTS (
      SELECT 1 FROM pending_approvals p
       WHERE p.approval_id = NEW.one_shot_authorization_approval_id
         AND p.resume_key = NEW.one_shot_authorization_resume_key
         AND p.status = 'resolved'
         AND p.resolution = 'approved'
         AND p.consumed_at IS NOT NULL
    )
    BEGIN
      SELECT RAISE(ABORT, 'workflow activation requires its exact consumed one-shot authorization');
    END;

    CREATE TRIGGER IF NOT EXISTS trg_workflow_node_invocation_activation_immutable
    BEFORE UPDATE ON workflow_node_invocation_activations
    BEGIN
      SELECT RAISE(ABORT, 'workflow node invocation activation is immutable');
    END;
  `);
}
function createWorkflowPaginatedReadSchema(db) {
  db.exec(`
    CREATE TABLE IF NOT EXISTS workflow_paginated_read_activations (
      activation_id            TEXT PRIMARY KEY CHECK (length(activation_id) BETWEEN 1 AND 512),
      authority_root_id        TEXT NOT NULL UNIQUE CHECK (length(authority_root_id) BETWEEN 1 AND 512),
      session_id               TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
      source_event_seq         INTEGER NOT NULL CHECK (source_event_seq > 0),
      source_event_id          TEXT NOT NULL UNIQUE REFERENCES events(id) ON DELETE RESTRICT,
      source_event_digest      TEXT NOT NULL CHECK (length(source_event_digest) = 64),
      workflow_id              TEXT NOT NULL CHECK (length(workflow_id) BETWEEN 1 AND 256),
      workflow_revision        INTEGER NOT NULL CHECK (workflow_revision > 0),
      workflow_digest          TEXT NOT NULL CHECK (length(workflow_digest) = 64),
      run_id                   TEXT NOT NULL CHECK (length(run_id) BETWEEN 1 AND 256),
      run_occurrence_id        TEXT NOT NULL CHECK (length(run_occurrence_id) BETWEEN 1 AND 256),
      node_id                  TEXT NOT NULL CHECK (length(node_id) BETWEEN 1 AND 256),
      node_attempt             INTEGER NOT NULL CHECK (node_attempt > 0),
      invocation_plan_digest  TEXT NOT NULL CHECK (length(invocation_plan_digest) = 64),
      binding_snapshot_digest TEXT NOT NULL CHECK (length(binding_snapshot_digest) = 64),
      control_digest          TEXT NOT NULL CHECK (length(control_digest) = 64),
      max_pages               INTEGER NOT NULL CHECK (max_pages BETWEEN 1 AND 10000),
      cursor_argument         TEXT NOT NULL CHECK (length(cursor_argument) BETWEEN 1 AND 128),
      next_cursor_path        TEXT NOT NULL CHECK (length(next_cursor_path) BETWEEN 1 AND 512),
      exhausted_path          TEXT NOT NULL CHECK (length(exhausted_path) BETWEEN 1 AND 512),
      one_shot_authorization_approval_id TEXT
                                REFERENCES pending_approvals(approval_id) ON DELETE RESTRICT,
      one_shot_authorization_resume_key TEXT,
      one_shot_authorization_decision_digest TEXT
                                CHECK (one_shot_authorization_decision_digest IS NULL
                                  OR length(one_shot_authorization_decision_digest) = 64),
      activation_digest       TEXT NOT NULL UNIQUE CHECK (length(activation_digest) = 64),
      aggregate_state         TEXT NOT NULL DEFAULT 'open'
                                CHECK (aggregate_state IN (
                                  'open','complete','partial','failed','cancelled','conflict'
                                )),
      next_page_ordinal       INTEGER NOT NULL DEFAULT 0
                                CHECK (next_page_ordinal BETWEEN 0 AND 10000),
      latest_page_receipt_digest TEXT
                                CHECK (latest_page_receipt_digest IS NULL
                                  OR length(latest_page_receipt_digest) = 64),
      terminal_aggregate_receipt_id TEXT UNIQUE,
      terminal_aggregate_receipt_digest TEXT
                                CHECK (terminal_aggregate_receipt_digest IS NULL
                                  OR length(terminal_aggregate_receipt_digest) = 64),
      activated_at            TEXT NOT NULL,
      closed_at               TEXT,
      close_reason            TEXT CHECK (close_reason IS NULL OR length(close_reason) BETWEEN 1 AND 160),
      UNIQUE (session_id, source_event_seq),
      UNIQUE (
        workflow_id, workflow_revision, run_id, run_occurrence_id,
        node_id, node_attempt
      ),
      CHECK (
        (one_shot_authorization_approval_id IS NULL
          AND one_shot_authorization_resume_key IS NULL
          AND one_shot_authorization_decision_digest IS NULL)
        OR
        (one_shot_authorization_approval_id IS NOT NULL
          AND length(one_shot_authorization_approval_id) BETWEEN 1 AND 128
          AND one_shot_authorization_resume_key IS NOT NULL
          AND length(one_shot_authorization_resume_key) BETWEEN 1 AND 1024
          AND one_shot_authorization_decision_digest IS NOT NULL)
      ),
      CHECK (
        (aggregate_state = 'open'
          AND terminal_aggregate_receipt_id IS NULL
          AND terminal_aggregate_receipt_digest IS NULL
          AND closed_at IS NULL AND close_reason IS NULL)
        OR
        (aggregate_state != 'open'
          AND terminal_aggregate_receipt_id IS NOT NULL
          AND terminal_aggregate_receipt_digest IS NOT NULL
          AND closed_at IS NOT NULL AND close_reason IS NOT NULL)
      )
    );

    CREATE INDEX IF NOT EXISTS idx_workflow_paginated_read_activation_run
      ON workflow_paginated_read_activations(
        workflow_id, workflow_revision, run_occurrence_id, node_id, node_attempt
      );

    CREATE UNIQUE INDEX IF NOT EXISTS idx_workflow_paginated_read_one_shot_authorization
      ON workflow_paginated_read_activations(one_shot_authorization_approval_id)
      WHERE one_shot_authorization_approval_id IS NOT NULL;

    CREATE TABLE IF NOT EXISTS workflow_paginated_read_pages (
      activation_id            TEXT NOT NULL
                                REFERENCES workflow_paginated_read_activations(activation_id)
                                ON DELETE CASCADE,
      page_ordinal             INTEGER NOT NULL CHECK (page_ordinal BETWEEN 0 AND 9999),
      prior_page_receipt_digest TEXT
                                CHECK (prior_page_receipt_digest IS NULL
                                  OR length(prior_page_receipt_digest) = 64),
      input_cursor_digest      TEXT
                                CHECK (input_cursor_digest IS NULL OR length(input_cursor_digest) = 64),
      binding_identity_digest TEXT NOT NULL CHECK (length(binding_identity_digest) = 64),
      argument_digest          TEXT NOT NULL CHECK (length(argument_digest) = 64),
      logical_call_id          TEXT NOT NULL CHECK (length(logical_call_id) BETWEEN 1 AND 512),
      physical_dispatch_id     TEXT NOT NULL CHECK (length(physical_dispatch_id) BETWEEN 1 AND 512),
      state                    TEXT NOT NULL DEFAULT 'reserved'
                                CHECK (state IN (
                                  'reserved','settled','failed','uncertain','cancelled','conflict'
                                )),
      page_receipt_id          TEXT UNIQUE,
      page_receipt_digest      TEXT UNIQUE
                                CHECK (page_receipt_digest IS NULL OR length(page_receipt_digest) = 64),
      result_handle_id         TEXT REFERENCES durable_result_handles(handle_id) ON DELETE RESTRICT,
      settled_result_digest    TEXT
                                CHECK (settled_result_digest IS NULL OR length(settled_result_digest) = 64),
      next_cursor_digest       TEXT
                                CHECK (next_cursor_digest IS NULL OR length(next_cursor_digest) = 64),
      provider_exhausted_truth TEXT CHECK (provider_exhausted_truth IN ('true','false','unknown')),
      item_count               INTEGER CHECK (item_count IS NULL OR item_count BETWEEN 0 AND 2147483647),
      evidence_digest          TEXT CHECK (evidence_digest IS NULL OR length(evidence_digest) = 64),
      evidence_valid           INTEGER CHECK (evidence_valid IN (0,1)),
      continuation_state       TEXT CHECK (continuation_state IN (
                                  'exhausted','continue','missing_cursor','repeated_cursor',
                                  'unknown_exhaustion','malformed_evidence'
                                )),
      reserved_at              TEXT NOT NULL,
      settled_at               TEXT,
      PRIMARY KEY (activation_id, page_ordinal),
      UNIQUE (activation_id, logical_call_id),
      UNIQUE (activation_id, physical_dispatch_id),
      CHECK (
        (page_ordinal = 0
          AND prior_page_receipt_digest IS NULL AND input_cursor_digest IS NULL)
        OR
        (page_ordinal > 0
          AND prior_page_receipt_digest IS NOT NULL AND input_cursor_digest IS NOT NULL)
      ),
      CHECK (
        (state = 'reserved'
          AND page_receipt_id IS NULL AND page_receipt_digest IS NULL
          AND result_handle_id IS NULL AND settled_result_digest IS NULL
          AND provider_exhausted_truth IS NULL AND item_count IS NULL
          AND evidence_digest IS NULL AND evidence_valid IS NULL
          AND continuation_state IS NULL AND settled_at IS NULL)
        OR
        (state = 'settled'
          AND page_receipt_id IS NOT NULL AND page_receipt_digest IS NOT NULL
          AND result_handle_id IS NOT NULL AND settled_result_digest IS NOT NULL
          AND provider_exhausted_truth IS NOT NULL AND item_count IS NOT NULL
          AND evidence_digest IS NOT NULL AND evidence_valid IS NOT NULL
          AND continuation_state IS NOT NULL AND settled_at IS NOT NULL)
        OR state IN ('failed','uncertain','cancelled','conflict')
      )
    );

    CREATE INDEX IF NOT EXISTS idx_workflow_paginated_read_page_state
      ON workflow_paginated_read_pages(activation_id, state, page_ordinal);

    CREATE TABLE IF NOT EXISTS workflow_paginated_cursor_visits (
      activation_id       TEXT NOT NULL,
      cursor_digest       TEXT NOT NULL CHECK (length(cursor_digest) = 64),
      first_page_ordinal  INTEGER NOT NULL CHECK (first_page_ordinal BETWEEN 1 AND 9999),
      visited_at          TEXT NOT NULL,
      PRIMARY KEY (activation_id, cursor_digest),
      UNIQUE (activation_id, first_page_ordinal),
      FOREIGN KEY (activation_id, first_page_ordinal)
        REFERENCES workflow_paginated_read_pages(activation_id, page_ordinal)
        ON DELETE CASCADE
    );

    CREATE TABLE IF NOT EXISTS workflow_paginated_aggregate_receipts (
      aggregate_receipt_id       TEXT PRIMARY KEY CHECK (length(aggregate_receipt_id) BETWEEN 1 AND 512),
      activation_id              TEXT NOT NULL UNIQUE
                                   REFERENCES workflow_paginated_read_activations(activation_id)
                                   ON DELETE RESTRICT,
      activation_digest          TEXT NOT NULL CHECK (length(activation_digest) = 64),
      authority_root_id          TEXT NOT NULL CHECK (length(authority_root_id) BETWEEN 1 AND 512),
      invocation_plan_digest     TEXT NOT NULL CHECK (length(invocation_plan_digest) = 64),
      binding_snapshot_digest    TEXT NOT NULL CHECK (length(binding_snapshot_digest) = 64),
      control_digest             TEXT NOT NULL CHECK (length(control_digest) = 64),
      page_receipt_digests_json  TEXT NOT NULL
                                   CHECK (json_valid(page_receipt_digests_json)
                                     AND json_type(page_receipt_digests_json) = 'array'),
      page_result_handles_json   TEXT NOT NULL
                                   CHECK (json_valid(page_result_handles_json)
                                     AND json_type(page_result_handles_json) = 'array'),
      page_count                 INTEGER NOT NULL CHECK (page_count BETWEEN 0 AND 10000),
      total_item_count           INTEGER NOT NULL CHECK (total_item_count >= 0),
      final_exhausted_truth      TEXT NOT NULL
                                   CHECK (final_exhausted_truth IN ('true','false','unknown','none')),
      coverage_state             TEXT NOT NULL CHECK (coverage_state IN ('complete','partial','unknown')),
      outcome                    TEXT NOT NULL
                                   CHECK (outcome IN ('complete','partial','failed','cancelled','conflict')),
      reason                     TEXT NOT NULL CHECK (length(reason) BETWEEN 1 AND 160),
      aggregate_receipt_digest   TEXT NOT NULL UNIQUE CHECK (length(aggregate_receipt_digest) = 64),
      created_at                 TEXT NOT NULL,
      CHECK (
        outcome != 'complete'
        OR (coverage_state = 'complete' AND final_exhausted_truth = 'true' AND page_count > 0)
      )
    );

    CREATE TRIGGER IF NOT EXISTS trg_workflow_paginated_read_activation_source_exact
    BEFORE INSERT ON workflow_paginated_read_activations
    WHEN NOT EXISTS (
      SELECT 1 FROM events e
       WHERE e.id = NEW.source_event_id
         AND e.session_id = NEW.session_id
         AND e.seq = NEW.source_event_seq
         AND e.turn = 0
         AND e.role = 'system'
         AND e.type = 'workflow_paginated_read_activated'
         AND e.created_at = NEW.activated_at
         AND json_valid(e.data_json)
         AND json_extract(e.data_json, '$.protocolVersion') = 1
         AND json_extract(e.data_json, '$.activationId') = NEW.activation_id
         AND json_extract(e.data_json, '$.authorityRootId') = NEW.authority_root_id
         AND json_extract(e.data_json, '$.activationDigest') = NEW.activation_digest
         AND json_extract(e.data_json, '$.workflowId') = NEW.workflow_id
         AND json_extract(e.data_json, '$.workflowRevision') = NEW.workflow_revision
         AND json_extract(e.data_json, '$.workflowDigest') = NEW.workflow_digest
         AND json_extract(e.data_json, '$.runId') = NEW.run_id
         AND json_extract(e.data_json, '$.runOccurrenceId') = NEW.run_occurrence_id
         AND json_extract(e.data_json, '$.nodeId') = NEW.node_id
         AND json_extract(e.data_json, '$.nodeAttempt') = NEW.node_attempt
         AND json_extract(e.data_json, '$.invocationPlanDigest') = NEW.invocation_plan_digest
         AND json_extract(e.data_json, '$.bindingSnapshotDigest') = NEW.binding_snapshot_digest
         AND json_extract(e.data_json, '$.controlDigest') = NEW.control_digest
         AND json_extract(e.data_json, '$.maxPages') = NEW.max_pages
         AND json_extract(e.data_json, '$.cursorArgument') = NEW.cursor_argument
         AND json_extract(e.data_json, '$.nextCursorPath') = NEW.next_cursor_path
         AND json_extract(e.data_json, '$.exhaustedPath') = NEW.exhausted_path
         AND (
           (NEW.one_shot_authorization_approval_id IS NULL
             AND json_type(e.data_json, '$.oneShotActivationAuthorization') IS NULL
             AND (SELECT COUNT(*) FROM json_each(e.data_json)) = 18)
           OR
           (NEW.one_shot_authorization_approval_id IS NOT NULL
             AND json_type(e.data_json, '$.oneShotActivationAuthorization') = 'object'
             AND json_extract(e.data_json, '$.oneShotActivationAuthorization.approvalId')
                   = NEW.one_shot_authorization_approval_id
             AND json_extract(e.data_json, '$.oneShotActivationAuthorization.resumeKey')
                   = NEW.one_shot_authorization_resume_key
             AND json_extract(e.data_json, '$.oneShotActivationAuthorization.decisionDigest')
                   = NEW.one_shot_authorization_decision_digest
             AND (SELECT COUNT(*)
                    FROM json_each(e.data_json, '$.oneShotActivationAuthorization')) = 3
             AND (SELECT COUNT(*) FROM json_each(e.data_json)) = 19)
         )
    )
    BEGIN
      SELECT RAISE(ABORT, 'paginated workflow activation requires its exact system event');
    END;

    CREATE TRIGGER IF NOT EXISTS trg_workflow_paginated_read_activation_authorization_exact
    BEFORE INSERT ON workflow_paginated_read_activations
    WHEN NEW.one_shot_authorization_approval_id IS NOT NULL AND NOT EXISTS (
      SELECT 1 FROM pending_approvals p
       WHERE p.approval_id = NEW.one_shot_authorization_approval_id
         AND p.resume_key = NEW.one_shot_authorization_resume_key
         AND p.status = 'resolved'
         AND p.resolution = 'approved'
         AND p.consumed_at IS NOT NULL
    )
    BEGIN
      SELECT RAISE(ABORT, 'paginated workflow activation requires its exact consumed one-shot authorization');
    END;

    CREATE TRIGGER IF NOT EXISTS trg_workflow_paginated_read_excludes_single_call_activation
    BEFORE INSERT ON workflow_paginated_read_activations
    WHEN EXISTS (
      SELECT 1 FROM workflow_node_invocation_activations w
       WHERE w.workflow_id = NEW.workflow_id
         AND w.workflow_revision = NEW.workflow_revision
         AND w.run_id = NEW.run_id
         AND w.run_occurrence_id = NEW.run_occurrence_id
         AND w.node_id = NEW.node_id
         AND w.node_attempt = NEW.node_attempt
    )
    BEGIN
      SELECT RAISE(ABORT, 'workflow node attempt already has a single-call activation');
    END;

    CREATE TRIGGER IF NOT EXISTS trg_workflow_node_invocation_excludes_paginated_activation
    BEFORE INSERT ON workflow_node_invocation_activations
    WHEN EXISTS (
      SELECT 1 FROM workflow_paginated_read_activations w
       WHERE w.workflow_id = NEW.workflow_id
         AND w.workflow_revision = NEW.workflow_revision
         AND w.run_id = NEW.run_id
         AND w.run_occurrence_id = NEW.run_occurrence_id
         AND w.node_id = NEW.node_id
         AND w.node_attempt = NEW.node_attempt
    )
    BEGIN
      SELECT RAISE(ABORT, 'workflow node attempt already has a paginated activation');
    END;

    CREATE TRIGGER IF NOT EXISTS trg_workflow_paginated_read_activation_identity_immutable
    BEFORE UPDATE ON workflow_paginated_read_activations
    WHEN OLD.activation_id IS NOT NEW.activation_id
      OR OLD.authority_root_id IS NOT NEW.authority_root_id
      OR OLD.session_id IS NOT NEW.session_id
      OR OLD.source_event_seq IS NOT NEW.source_event_seq
      OR OLD.source_event_id IS NOT NEW.source_event_id
      OR OLD.source_event_digest IS NOT NEW.source_event_digest
      OR OLD.workflow_id IS NOT NEW.workflow_id
      OR OLD.workflow_revision IS NOT NEW.workflow_revision
      OR OLD.workflow_digest IS NOT NEW.workflow_digest
      OR OLD.run_id IS NOT NEW.run_id
      OR OLD.run_occurrence_id IS NOT NEW.run_occurrence_id
      OR OLD.node_id IS NOT NEW.node_id
      OR OLD.node_attempt IS NOT NEW.node_attempt
      OR OLD.invocation_plan_digest IS NOT NEW.invocation_plan_digest
      OR OLD.binding_snapshot_digest IS NOT NEW.binding_snapshot_digest
      OR OLD.control_digest IS NOT NEW.control_digest
      OR OLD.max_pages IS NOT NEW.max_pages
      OR OLD.cursor_argument IS NOT NEW.cursor_argument
      OR OLD.next_cursor_path IS NOT NEW.next_cursor_path
      OR OLD.exhausted_path IS NOT NEW.exhausted_path
      OR OLD.one_shot_authorization_approval_id IS NOT NEW.one_shot_authorization_approval_id
      OR OLD.one_shot_authorization_resume_key IS NOT NEW.one_shot_authorization_resume_key
      OR OLD.one_shot_authorization_decision_digest IS NOT NEW.one_shot_authorization_decision_digest
      OR OLD.activation_digest IS NOT NEW.activation_digest
      OR OLD.activated_at IS NOT NEW.activated_at
    BEGIN
      SELECT RAISE(ABORT, 'paginated workflow activation identity is immutable');
    END;

    CREATE TRIGGER IF NOT EXISTS trg_workflow_paginated_read_activation_progress_monotonic
    BEFORE UPDATE ON workflow_paginated_read_activations
    WHEN NOT (
      (OLD.aggregate_state = 'open' AND NEW.aggregate_state = 'open'
        AND NEW.next_page_ordinal IN (OLD.next_page_ordinal, OLD.next_page_ordinal + 1)
        AND NEW.terminal_aggregate_receipt_id IS NULL
        AND NEW.terminal_aggregate_receipt_digest IS NULL
        AND NEW.closed_at IS NULL AND NEW.close_reason IS NULL)
      OR
      (OLD.aggregate_state = 'open' AND NEW.aggregate_state != 'open'
        AND NEW.next_page_ordinal = OLD.next_page_ordinal
        AND NEW.terminal_aggregate_receipt_id IS NOT NULL
        AND NEW.terminal_aggregate_receipt_digest IS NOT NULL
        AND NEW.closed_at IS NOT NULL AND NEW.close_reason IS NOT NULL
        AND EXISTS (
          SELECT 1 FROM workflow_paginated_aggregate_receipts r
           WHERE r.aggregate_receipt_id = NEW.terminal_aggregate_receipt_id
             AND r.activation_id = NEW.activation_id
             AND r.aggregate_receipt_digest = NEW.terminal_aggregate_receipt_digest
        ))
    )
    BEGIN
      SELECT RAISE(ABORT, 'paginated workflow activation transition is not monotonic');
    END;

    CREATE TRIGGER IF NOT EXISTS trg_workflow_paginated_read_page_identity_immutable
    BEFORE UPDATE ON workflow_paginated_read_pages
    WHEN OLD.activation_id IS NOT NEW.activation_id
      OR OLD.page_ordinal IS NOT NEW.page_ordinal
      OR OLD.prior_page_receipt_digest IS NOT NEW.prior_page_receipt_digest
      OR OLD.input_cursor_digest IS NOT NEW.input_cursor_digest
      OR OLD.binding_identity_digest IS NOT NEW.binding_identity_digest
      OR OLD.argument_digest IS NOT NEW.argument_digest
      OR OLD.logical_call_id IS NOT NEW.logical_call_id
      OR OLD.physical_dispatch_id IS NOT NEW.physical_dispatch_id
      OR OLD.reserved_at IS NOT NEW.reserved_at
    BEGIN
      SELECT RAISE(ABORT, 'paginated workflow page identity is immutable');
    END;

    CREATE TRIGGER IF NOT EXISTS trg_workflow_paginated_read_page_state_once
    BEFORE UPDATE ON workflow_paginated_read_pages
    WHEN OLD.state != 'reserved' OR NEW.state = 'reserved'
    BEGIN
      SELECT RAISE(ABORT, 'paginated workflow page settles once');
    END;

    CREATE TRIGGER IF NOT EXISTS trg_workflow_paginated_aggregate_receipt_immutable
    BEFORE UPDATE ON workflow_paginated_aggregate_receipts
    BEGIN
      SELECT RAISE(ABORT, 'paginated workflow aggregate receipt is immutable');
    END;
  `);
}
function rebuildAcceptedCallAuthoritiesForWorkflow(db) {
  const table = db.prepare(
    `SELECT sql FROM sqlite_master WHERE type = 'table' AND name = 'accepted_turn_call_authorities'`
  ).get();
  if (!table?.sql) throw new Error("schema v51 call-authority root is missing");
  const existingColumns = new Set(db.prepare(
    "PRAGMA table_info(accepted_turn_call_authorities)"
  ).all().map((column) => column.name));
  if (existingColumns.has("workflow_activation_id") && existingColumns.has("workflow_logical_call_id") && table.sql.includes("'workflow_v1_read_only'")) {
    const logicalParents = new Set(db.prepare(
      "PRAGMA foreign_key_list(logical_tool_calls)"
    ).all().filter((fk) => fk.from === "session_id" || fk.from === "source_user_seq").map((fk) => fk.table));
    if (logicalParents.size !== 1 || !logicalParents.has("accepted_turn_call_authorities")) {
      throw new Error("schema v51 existing workflow root has the wrong logical-call parent");
    }
    const relevantTables = /* @__PURE__ */ new Set([
      "workflow_node_invocation_activations",
      "accepted_turn_call_authorities",
      "logical_tool_calls",
      ...db.prepare(`SELECT name FROM sqlite_master WHERE type = 'table'`).all().map((row) => row.name).filter((name) => db.prepare(
        `PRAGMA foreign_key_list(${quotedSchemaIdentifier(name)})`
      ).all().some((fk) => fk.table === "logical_tool_calls"))
    ]);
    const violations2 = db.pragma("foreign_key_check").filter((violation) => relevantTables.has(violation.table));
    if (violations2.length > 0) {
      throw new Error(`schema v51 existing workflow root has ${violations2.length} relevant foreign-key violation(s)`);
    }
    const integrity2 = db.pragma("integrity_check");
    if (integrity2.length !== 1 || integrity2[0]?.integrity_check !== "ok") {
      throw new Error(`schema v51 existing workflow-root integrity failed: ${JSON.stringify(integrity2).slice(0, 240)}`);
    }
    return;
  }
  const beforeRows = db.prepare(
    "SELECT COUNT(*) AS n FROM accepted_turn_call_authorities"
  ).get().n;
  const logicalRows = db.prepare(
    "SELECT COUNT(*) AS n FROM logical_tool_calls"
  ).get().n;
  const logicalFks = JSON.stringify(db.prepare("PRAGMA foreign_key_list(logical_tool_calls)").all());
  const logicalObjects = JSON.stringify(db.prepare(`
    SELECT type, name, sql FROM sqlite_master
     WHERE tbl_name = 'logical_tool_calls'
       AND type IN ('index','trigger') AND sql IS NOT NULL
     ORDER BY type, name
  `).all());
  const logicalChildTables = db.prepare(
    `SELECT name FROM sqlite_master WHERE type = 'table'`
  ).all().map((row) => row.name).filter((name) => db.prepare(
    `PRAGMA foreign_key_list(${quotedSchemaIdentifier(name)})`
  ).all().some((fk) => fk.table === "logical_tool_calls"));
  const logicalChildren = new Map(logicalChildTables.map((name) => [name, {
    rows: db.prepare(`SELECT COUNT(*) AS n FROM ${quotedSchemaIdentifier(name)}`).get().n,
    foreignKeys: JSON.stringify(db.prepare(
      `PRAGMA foreign_key_list(${quotedSchemaIdentifier(name)})`
    ).all())
  }]));
  const priorLegacyRename = Number(db.pragma("legacy_alter_table", { simple: true })) === 1;
  db.pragma("legacy_alter_table = ON");
  try {
    db.exec(`
      DROP TRIGGER IF EXISTS trg_accepted_turn_call_authority_source_exact;
      DROP TRIGGER IF EXISTS trg_accepted_turn_call_authority_graph_exact;
      DROP TRIGGER IF EXISTS trg_accepted_turn_call_authority_host_graphless;
      DROP TRIGGER IF EXISTS trg_accepted_turn_call_authority_identity_immutable;
      DROP TRIGGER IF EXISTS trg_accepted_turn_call_authority_state_machine;
      DROP INDEX IF EXISTS idx_accepted_turn_call_authority_state;
      ALTER TABLE accepted_turn_call_authorities RENAME TO accepted_turn_call_authorities_v50;
    `);
    createV51AcceptedCallAuthoritySchema(db);
    db.exec(`
      INSERT INTO accepted_turn_call_authorities (
        session_id, source_user_seq, accepted_task_id, authority_protocol,
        authority_kind, source_event_id, source_event_digest, source_turn,
        engine_version, surface_version, surface_digest, effect_ceiling,
        effect_bounds_json, max_logical_calls, max_parallel_calls,
        catalog_revision_digest, binding_revision_digest, graph_event_id,
        graph_hash, authority_digest, state, revision, opened_at, closed_at,
        close_reason
      )
      SELECT session_id, source_user_seq, accepted_task_id, authority_protocol,
             authority_kind, source_event_id, source_event_digest, source_turn,
             engine_version, surface_version, surface_digest, effect_ceiling,
             effect_bounds_json, max_logical_calls, max_parallel_calls,
             catalog_revision_digest, binding_revision_digest, graph_event_id,
             graph_hash, authority_digest, state, revision, opened_at, closed_at,
             close_reason
        FROM accepted_turn_call_authorities_v50;
      DROP TABLE accepted_turn_call_authorities_v50;
    `);
  } finally {
    db.pragma(`legacy_alter_table = ${priorLegacyRename ? "ON" : "OFF"}`);
  }
  const afterRows = db.prepare(
    "SELECT COUNT(*) AS n FROM accepted_turn_call_authorities"
  ).get().n;
  const afterLogicalRows = db.prepare(
    "SELECT COUNT(*) AS n FROM logical_tool_calls"
  ).get().n;
  const afterLogicalFks = JSON.stringify(db.prepare("PRAGMA foreign_key_list(logical_tool_calls)").all());
  const afterLogicalObjects = JSON.stringify(db.prepare(`
    SELECT type, name, sql FROM sqlite_master
     WHERE tbl_name = 'logical_tool_calls'
       AND type IN ('index','trigger') AND sql IS NOT NULL
     ORDER BY type, name
  `).all());
  if (afterRows !== beforeRows || afterLogicalRows !== logicalRows || afterLogicalFks !== logicalFks || afterLogicalObjects !== logicalObjects) {
    throw new Error(
      `schema v51 changed authority/logical rows, foreign keys, indexes, or triggers: ${beforeRows}/${logicalRows} -> ${afterRows}/${afterLogicalRows}`
    );
  }
  for (const [name, before] of logicalChildren) {
    const rows = db.prepare(`SELECT COUNT(*) AS n FROM ${quotedSchemaIdentifier(name)}`).get().n;
    const foreignKeys = JSON.stringify(db.prepare(
      `PRAGMA foreign_key_list(${quotedSchemaIdentifier(name)})`
    ).all());
    if (rows !== before.rows || foreignKeys !== before.foreignKeys) {
      throw new Error(`schema v51 changed logical child rows or foreign keys for ${name}`);
    }
  }
  const violations = db.pragma("foreign_key_check");
  if (violations.length > 0) {
    throw new Error(`schema v51 foreign-key check failed for ${violations.length} row(s)`);
  }
  const integrity = db.pragma("integrity_check");
  if (integrity.length !== 1 || integrity[0]?.integrity_check !== "ok") {
    throw new Error(`schema v51 integrity check failed: ${JSON.stringify(integrity).slice(0, 240)}`);
  }
}
function rebuildAcceptedCallAuthoritiesForPagination(db) {
  const table = db.prepare(
    `SELECT sql FROM sqlite_master WHERE type = 'table' AND name = 'accepted_turn_call_authorities'`
  ).get();
  if (!table?.sql) throw new Error("schema v52 call-authority root is missing");
  if (table.sql.includes("'workflow_v2_paginated_read'")) {
    const relevantTables2 = foreignKeyClosure(db, ["accepted_turn_call_authorities"]);
    const violations2 = db.pragma("foreign_key_check").filter((violation) => relevantTables2.has(violation.table));
    if (violations2.length > 0) {
      throw new Error(`schema v52 existing paginated root has ${violations2.length} relevant foreign-key violation(s)`);
    }
    const integrity2 = db.pragma("integrity_check");
    if (integrity2.length !== 1 || integrity2[0]?.integrity_check !== "ok") {
      throw new Error(`schema v52 existing paginated-root integrity failed: ${JSON.stringify(integrity2).slice(0, 240)}`);
    }
    return;
  }
  const columns = db.prepare(
    "PRAGMA table_info(accepted_turn_call_authorities)"
  ).all().map((column) => column.name);
  const beforeRows = db.prepare(
    "SELECT COUNT(*) AS n FROM accepted_turn_call_authorities"
  ).get().n;
  const logicalRows = db.prepare(
    "SELECT COUNT(*) AS n FROM logical_tool_calls"
  ).get().n;
  const logicalFks = JSON.stringify(db.prepare("PRAGMA foreign_key_list(logical_tool_calls)").all());
  const logicalObjects = JSON.stringify(db.prepare(`
    SELECT type, name, sql FROM sqlite_master
     WHERE tbl_name = 'logical_tool_calls'
       AND type IN ('index','trigger') AND sql IS NOT NULL
     ORDER BY type, name
  `).all());
  const rootObjects = db.prepare(`
    SELECT type, name FROM sqlite_master
     WHERE tbl_name = 'accepted_turn_call_authorities'
       AND type IN ('index','trigger') AND sql IS NOT NULL
     ORDER BY type, name
  `).all();
  const childTables = db.prepare(
    `SELECT name FROM sqlite_master WHERE type = 'table'`
  ).all().map((row) => row.name).filter((name) => db.prepare(
    `PRAGMA foreign_key_list(${quotedSchemaIdentifier(name)})`
  ).all().some((fk) => fk.table === "accepted_turn_call_authorities"));
  const childSnapshot = new Map(childTables.map((name) => [name, {
    rows: db.prepare(`SELECT COUNT(*) AS n FROM ${quotedSchemaIdentifier(name)}`).get().n,
    foreignKeys: JSON.stringify(db.prepare(
      `PRAGMA foreign_key_list(${quotedSchemaIdentifier(name)})`
    ).all())
  }]));
  const priorLegacyRename = Number(db.pragma("legacy_alter_table", { simple: true })) === 1;
  db.pragma("legacy_alter_table = ON");
  try {
    for (const object of rootObjects) {
      db.exec(`DROP ${object.type.toUpperCase()} ${quotedSchemaIdentifier(object.name)}`);
    }
    db.exec("ALTER TABLE accepted_turn_call_authorities RENAME TO accepted_turn_call_authorities_v51");
    createV51AcceptedCallAuthoritySchema(db, true);
    const columnList = columns.map(quotedSchemaIdentifier).join(", ");
    db.exec(`INSERT INTO accepted_turn_call_authorities (${columnList})
      SELECT ${columnList} FROM accepted_turn_call_authorities_v51`);
    db.exec("DROP TABLE accepted_turn_call_authorities_v51");
  } finally {
    db.pragma(`legacy_alter_table = ${priorLegacyRename ? "ON" : "OFF"}`);
  }
  const afterRows = db.prepare(
    "SELECT COUNT(*) AS n FROM accepted_turn_call_authorities"
  ).get().n;
  const afterLogicalRows = db.prepare(
    "SELECT COUNT(*) AS n FROM logical_tool_calls"
  ).get().n;
  const afterLogicalFks = JSON.stringify(db.prepare("PRAGMA foreign_key_list(logical_tool_calls)").all());
  const afterLogicalObjects = JSON.stringify(db.prepare(`
    SELECT type, name, sql FROM sqlite_master
     WHERE tbl_name = 'logical_tool_calls'
       AND type IN ('index','trigger') AND sql IS NOT NULL
     ORDER BY type, name
  `).all());
  if (beforeRows !== afterRows || logicalRows !== afterLogicalRows || logicalFks !== afterLogicalFks || logicalObjects !== afterLogicalObjects) {
    throw new Error(
      `schema v52 changed authority/logical rows, foreign keys, indexes, or triggers: ${beforeRows}/${logicalRows} -> ${afterRows}/${afterLogicalRows}`
    );
  }
  for (const [name, before] of childSnapshot) {
    const rows = db.prepare(`SELECT COUNT(*) AS n FROM ${quotedSchemaIdentifier(name)}`).get().n;
    const foreignKeys = JSON.stringify(db.prepare(
      `PRAGMA foreign_key_list(${quotedSchemaIdentifier(name)})`
    ).all());
    if (rows !== before.rows || foreignKeys !== before.foreignKeys) {
      throw new Error(`schema v52 changed authority child rows or foreign keys for ${name}`);
    }
  }
  const relevantTables = foreignKeyClosure(db, ["accepted_turn_call_authorities"]);
  const violations = db.pragma("foreign_key_check").filter((violation) => relevantTables.has(violation.table));
  if (violations.length > 0) {
    throw new Error(`schema v52 foreign-key check failed for ${violations.length} authority row(s)`);
  }
  const integrity = db.pragma("integrity_check");
  if (integrity.length !== 1 || integrity[0]?.integrity_check !== "ok") {
    throw new Error(`schema v52 integrity check failed: ${JSON.stringify(integrity).slice(0, 240)}`);
  }
}
function rebuildAcceptedCallAuthoritiesForProductionHost(db, includeWorkflowV3 = false) {
  const table = db.prepare(
    `SELECT sql FROM sqlite_master WHERE type = 'table' AND name = 'accepted_turn_call_authorities'`
  ).get();
  const schemaVersion = includeWorkflowV3 ? 64 : 54;
  if (!table?.sql) throw new Error(`schema v${schemaVersion} call-authority root is missing`);
  if (table.sql.includes(includeWorkflowV3 ? "'workflow_v3_call'" : "'host_v1'")) {
    const relevantTables2 = foreignKeyClosure(db, ["accepted_turn_call_authorities"]);
    const violations2 = db.pragma("foreign_key_check").filter((violation) => relevantTables2.has(violation.table));
    if (violations2.length > 0) {
      throw new Error(`schema v${schemaVersion} existing host root has ${violations2.length} relevant foreign-key violation(s)`);
    }
    const integrity2 = db.pragma("integrity_check");
    if (integrity2.length !== 1 || integrity2[0]?.integrity_check !== "ok") {
      throw new Error(`schema v${schemaVersion} existing host-root integrity failed: ${JSON.stringify(integrity2).slice(0, 240)}`);
    }
    return;
  }
  const columns = db.prepare(
    "PRAGMA table_info(accepted_turn_call_authorities)"
  ).all().map((column) => column.name);
  const beforeRows = db.prepare(
    "SELECT COUNT(*) AS n FROM accepted_turn_call_authorities"
  ).get().n;
  const rootBytes = JSON.stringify(db.prepare(`
    SELECT * FROM accepted_turn_call_authorities
     ORDER BY session_id, source_user_seq
  `).all());
  const logicalRows = db.prepare(
    "SELECT COUNT(*) AS n FROM logical_tool_calls"
  ).get().n;
  const logicalFks = JSON.stringify(db.prepare("PRAGMA foreign_key_list(logical_tool_calls)").all());
  const logicalObjectNames = JSON.stringify(db.prepare(`
    SELECT type, name FROM sqlite_master
     WHERE tbl_name = 'logical_tool_calls'
       AND type IN ('index','trigger') AND sql IS NOT NULL
     ORDER BY type, name
  `).all());
  const rootObjects = db.prepare(`
    SELECT type, name FROM sqlite_master
     WHERE tbl_name = 'accepted_turn_call_authorities'
       AND type IN ('index','trigger') AND sql IS NOT NULL
     ORDER BY type, name
  `).all();
  const childTables = db.prepare(
    `SELECT name FROM sqlite_master WHERE type = 'table'`
  ).all().map((row) => row.name).filter((name) => db.prepare(
    `PRAGMA foreign_key_list(${quotedSchemaIdentifier(name)})`
  ).all().some((fk) => fk.table === "accepted_turn_call_authorities"));
  const childSnapshot = new Map(childTables.map((name) => [name, {
    rows: db.prepare(`SELECT COUNT(*) AS n FROM ${quotedSchemaIdentifier(name)}`).get().n,
    foreignKeys: JSON.stringify(db.prepare(
      `PRAGMA foreign_key_list(${quotedSchemaIdentifier(name)})`
    ).all())
  }]));
  const priorLegacyRename = Number(db.pragma("legacy_alter_table", { simple: true })) === 1;
  db.pragma("legacy_alter_table = ON");
  try {
    for (const object of rootObjects) {
      db.exec(`DROP ${object.type.toUpperCase()} ${quotedSchemaIdentifier(object.name)}`);
    }
    const retiredName = includeWorkflowV3 ? "accepted_turn_call_authorities_v63" : "accepted_turn_call_authorities_v53";
    db.exec(`ALTER TABLE accepted_turn_call_authorities RENAME TO ${retiredName}`);
    createV51AcceptedCallAuthoritySchema(db, true, true, includeWorkflowV3);
    const columnList = columns.map(quotedSchemaIdentifier).join(", ");
    db.exec(`INSERT INTO accepted_turn_call_authorities (${columnList})
      SELECT ${columnList} FROM ${retiredName}`);
    db.exec(`DROP TABLE ${retiredName}`);
  } finally {
    db.pragma(`legacy_alter_table = ${priorLegacyRename ? "ON" : "OFF"}`);
  }
  const afterRows = db.prepare(
    "SELECT COUNT(*) AS n FROM accepted_turn_call_authorities"
  ).get().n;
  const afterRootBytes = JSON.stringify(db.prepare(`
    SELECT * FROM accepted_turn_call_authorities
     ORDER BY session_id, source_user_seq
  `).all());
  const afterLogicalRows = db.prepare(
    "SELECT COUNT(*) AS n FROM logical_tool_calls"
  ).get().n;
  const afterLogicalFks = JSON.stringify(db.prepare("PRAGMA foreign_key_list(logical_tool_calls)").all());
  const afterLogicalObjectNames = JSON.stringify(db.prepare(`
    SELECT type, name FROM sqlite_master
     WHERE tbl_name = 'logical_tool_calls'
       AND type IN ('index','trigger') AND sql IS NOT NULL
     ORDER BY type, name
  `).all());
  if (beforeRows !== afterRows || rootBytes !== afterRootBytes || logicalRows !== afterLogicalRows || logicalFks !== afterLogicalFks || logicalObjectNames !== afterLogicalObjectNames) {
    throw new Error(`schema v${schemaVersion} changed authority/logical rows, foreign keys, indexes, or triggers`);
  }
  for (const [name, before] of childSnapshot) {
    const rows = db.prepare(`SELECT COUNT(*) AS n FROM ${quotedSchemaIdentifier(name)}`).get().n;
    const foreignKeys = JSON.stringify(db.prepare(
      `PRAGMA foreign_key_list(${quotedSchemaIdentifier(name)})`
    ).all());
    if (rows !== before.rows || foreignKeys !== before.foreignKeys) {
      throw new Error(`schema v${schemaVersion} changed authority child rows or foreign keys for ${name}`);
    }
  }
  const relevantTables = foreignKeyClosure(db, ["accepted_turn_call_authorities"]);
  const violations = db.pragma("foreign_key_check").filter((violation) => relevantTables.has(violation.table));
  if (violations.length > 0) {
    throw new Error(`schema v${schemaVersion} foreign-key check failed for ${violations.length} authority row(s)`);
  }
  const integrity = db.pragma("integrity_check");
  if (integrity.length !== 1 || integrity[0]?.integrity_check !== "ok") {
    throw new Error(`schema v${schemaVersion} integrity check failed: ${JSON.stringify(integrity).slice(0, 240)}`);
  }
}
function widenPendingApprovalSystemCancellationResolution(db) {
  const table = db.prepare(
    `SELECT sql FROM sqlite_master WHERE type = 'table' AND name = 'pending_approvals'`
  ).get();
  if (!table?.sql || table.sql.includes("'cancelled_by_system'")) return;
  const oldConstraint = "resolution IN ('approved','rejected','expired','cancelled_by_user')";
  if (!table.sql.includes(oldConstraint)) {
    return;
  }
  const widenedSql = table.sql.replace(
    oldConstraint,
    "resolution IN ('approved','rejected','expired','cancelled_by_user','cancelled_by_system')"
  );
  const legacyExists = db.prepare(
    `SELECT 1 FROM sqlite_master WHERE type = 'table' AND name = 'pending_approvals_v54'`
  ).get();
  if (legacyExists) throw new Error("schema v55 rebuild source already exists");
  const columns = db.prepare("PRAGMA table_info(pending_approvals)").all().map((column) => column.name);
  if (columns.length === 0 || !columns.includes("approval_id") || !columns.includes("resolution")) {
    throw new Error("schema v55 canonical pending approvals columns are missing");
  }
  const objects = db.prepare(`
    SELECT type, name, sql FROM sqlite_master
     WHERE tbl_name = 'pending_approvals'
       AND type IN ('index','trigger')
       AND sql IS NOT NULL
     ORDER BY type, name
  `).all();
  const beforeRows = db.prepare("SELECT COUNT(*) AS n FROM pending_approvals").get().n;
  const beforeBytes = JSON.stringify(db.prepare(
    "SELECT * FROM pending_approvals ORDER BY approval_id"
  ).all());
  const columnList = columns.map(quotedSchemaIdentifier).join(", ");
  const dependentTriggersBefore = JSON.stringify(db.prepare(`
    SELECT name, tbl_name, sql FROM sqlite_master
     WHERE type = 'trigger'
       AND tbl_name != 'pending_approvals'
       AND sql IS NOT NULL
       AND lower(sql) LIKE '%pending_approvals%'
     ORDER BY name
  `).all());
  const priorLegacyRename = Number(db.pragma("legacy_alter_table", { simple: true })) === 1;
  db.pragma("legacy_alter_table = ON");
  try {
    for (const object of objects) {
      db.exec(`DROP ${object.type.toUpperCase()} ${quotedSchemaIdentifier(object.name)}`);
    }
    db.exec("ALTER TABLE pending_approvals RENAME TO pending_approvals_v54");
    db.exec(widenedSql);
    db.exec(`
      INSERT INTO pending_approvals (${columnList})
        SELECT ${columnList} FROM pending_approvals_v54;
      DROP TABLE pending_approvals_v54;
    `);
    for (const object of objects) db.exec(object.sql);
  } finally {
    db.pragma(`legacy_alter_table = ${priorLegacyRename ? "ON" : "OFF"}`);
  }
  const afterRows = db.prepare("SELECT COUNT(*) AS n FROM pending_approvals").get().n;
  const afterBytes = JSON.stringify(db.prepare(
    "SELECT * FROM pending_approvals ORDER BY approval_id"
  ).all());
  const finalSql = db.prepare(
    `SELECT sql FROM sqlite_master WHERE type = 'table' AND name = 'pending_approvals'`
  ).get();
  const dependentTriggersAfter = JSON.stringify(db.prepare(`
    SELECT name, tbl_name, sql FROM sqlite_master
     WHERE type = 'trigger'
       AND tbl_name != 'pending_approvals'
       AND sql IS NOT NULL
       AND lower(sql) LIKE '%pending_approvals%'
     ORDER BY name
  `).all());
  if (beforeRows !== afterRows || beforeBytes !== afterBytes || dependentTriggersBefore !== dependentTriggersAfter || !finalSql?.sql?.includes("'cancelled_by_system'")) {
    throw new Error("schema v55 changed approval rows/dependent triggers or failed to widen the resolution constraint");
  }
  const relevantTables = foreignKeyClosure(db, ["pending_approvals"]);
  const violations = db.pragma("foreign_key_check").filter((violation) => relevantTables.has(violation.table));
  if (violations.length > 0) {
    throw new Error(`schema v55 foreign-key check failed for ${violations.length} approval row(s)`);
  }
  const integrity = db.pragma("integrity_check");
  if (integrity.length !== 1 || integrity[0]?.integrity_check !== "ok") {
    throw new Error(`schema v55 integrity check failed: ${JSON.stringify(integrity).slice(0, 240)}`);
  }
}
function createV57HostCallCapabilityBindingSchema(db) {
  db.exec(`
    CREATE TABLE IF NOT EXISTS host_call_capability_bindings (
      protocol_version                 INTEGER NOT NULL CHECK (protocol_version = 1),
      root_authority_kind              TEXT NOT NULL CHECK (root_authority_kind = 'host_v1'),
      root_graph_event_id              TEXT REFERENCES events(id) ON DELETE RESTRICT,
      root_graph_hash                  TEXT CHECK (root_graph_hash IS NULL OR length(root_graph_hash) = 64),
      session_id                       TEXT NOT NULL,
      source_user_seq                  INTEGER NOT NULL CHECK (source_user_seq > 0),
      accepted_task_id                 TEXT NOT NULL,
      source_event_id                  TEXT NOT NULL REFERENCES events(id) ON DELETE RESTRICT,
      source_event_digest              TEXT NOT NULL CHECK (length(source_event_digest) = 64),
      logical_tool_call_id             TEXT NOT NULL CHECK (length(logical_tool_call_id) BETWEEN 1 AND 512),
      tool_name                        TEXT NOT NULL CHECK (length(tool_name) BETWEEN 1 AND 512),
      attested_argument_digest         TEXT NOT NULL CHECK (length(attested_argument_digest) = 64),
      logical_raw_argument_digest      TEXT NOT NULL CHECK (length(logical_raw_argument_digest) = 64),
      bound_effective_argument_digest  TEXT CHECK (
        bound_effective_argument_digest IS NULL OR length(bound_effective_argument_digest) = 64
      ),
      effect                           TEXT NOT NULL CHECK (
        effect IN ('admin','compute','external_write','host_only','local_write','read')
      ),
      binding_kind                     TEXT NOT NULL CHECK (binding_kind IN ('local_envelope','catalog_manifest')),
      capability_id                    TEXT NOT NULL CHECK (length(capability_id) BETWEEN 1 AND 512),
      provider_input_schema_digest     TEXT CHECK (
        provider_input_schema_digest IS NULL OR length(provider_input_schema_digest) = 64
      ),
      schema_fingerprint               TEXT NOT NULL CHECK (length(schema_fingerprint) = 64),
      account_id                       TEXT NOT NULL,
      invoke_port_id                   TEXT NOT NULL CHECK (length(invoke_port_id) BETWEEN 1 AND 512),
      operation_id                     TEXT NOT NULL CHECK (length(operation_id) BETWEEN 1 AND 512),
      manifest_id                      TEXT NOT NULL,
      manifest_digest                  TEXT NOT NULL,
      host_binding_digest              TEXT NOT NULL CHECK (length(host_binding_digest) = 64),
      engine_version                   TEXT NOT NULL CHECK (length(engine_version) BETWEEN 1 AND 128),
      surface_version                  TEXT NOT NULL CHECK (length(surface_version) BETWEEN 1 AND 128),
      authority_digest                 TEXT NOT NULL CHECK (length(authority_digest) = 64),
      authority_revision               INTEGER NOT NULL CHECK (authority_revision >= 0),
      surface_digest                   TEXT NOT NULL CHECK (length(surface_digest) = 64),
      catalog_revision_digest          TEXT NOT NULL CHECK (length(catalog_revision_digest) = 64),
      binding_revision_digest          TEXT NOT NULL CHECK (length(binding_revision_digest) = 64),
      durable_binding_digest           TEXT NOT NULL UNIQUE CHECK (length(durable_binding_digest) = 64),
      bound_at                         TEXT NOT NULL,
      PRIMARY KEY (session_id, source_user_seq, logical_tool_call_id),
      FOREIGN KEY (session_id, source_user_seq)
        REFERENCES accepted_turn_call_authorities(session_id, source_user_seq)
        ON DELETE CASCADE,
      FOREIGN KEY (session_id, source_user_seq, logical_tool_call_id)
        REFERENCES logical_tool_calls(session_id, source_user_seq, logical_tool_call_id)
        ON DELETE CASCADE,
      CHECK (accepted_task_id = 'task:' || session_id || '#' || source_user_seq),
      CHECK (
        root_graph_event_id IS NULL AND root_graph_hash IS NULL
      ),
      CHECK (
        (binding_kind = 'catalog_manifest'
          AND length(account_id) BETWEEN 1 AND 512
          AND length(manifest_id) BETWEEN 1 AND 512
          AND length(manifest_digest) = 64)
        OR
        (binding_kind = 'local_envelope'
          AND provider_input_schema_digest IS NULL
          AND account_id = '' AND manifest_id = '' AND manifest_digest = '')
      ),
      CHECK (
        (bound_effective_argument_digest IS NULL
          AND attested_argument_digest = logical_raw_argument_digest)
        OR
        (bound_effective_argument_digest IS NOT NULL
          AND attested_argument_digest = bound_effective_argument_digest
          AND bound_effective_argument_digest != logical_raw_argument_digest)
      )
    );

    CREATE INDEX IF NOT EXISTS idx_host_call_capability_provider
      ON host_call_capability_bindings(
        account_id, operation_id, provider_input_schema_digest
      ) WHERE binding_kind = 'catalog_manifest';

    CREATE TRIGGER IF NOT EXISTS trg_host_call_capability_binding_root_exact
    BEFORE INSERT ON host_call_capability_bindings
    WHEN NOT EXISTS (
      SELECT 1 FROM accepted_turn_call_authorities root
       WHERE root.session_id = NEW.session_id
         AND root.source_user_seq = NEW.source_user_seq
         AND root.accepted_task_id = NEW.accepted_task_id
         AND root.authority_protocol = 1
         AND root.authority_kind = 'host_v1'
         AND NEW.root_authority_kind = 'host_v1'
         AND root.graph_event_id IS NEW.root_graph_event_id
         AND root.graph_hash IS NEW.root_graph_hash
         AND root.source_event_id = NEW.source_event_id
         AND root.source_event_digest = NEW.source_event_digest
         AND root.engine_version = NEW.engine_version
         AND root.surface_version = NEW.surface_version
         AND root.authority_digest = NEW.authority_digest
         AND root.revision = NEW.authority_revision
         AND root.surface_digest = NEW.surface_digest
         AND root.catalog_revision_digest = NEW.catalog_revision_digest
         AND root.binding_revision_digest = NEW.binding_revision_digest
         AND root.state = 'open'
    )
    BEGIN
      SELECT RAISE(ABORT, 'host-call capability binding requires its exact open host root');
    END;

    CREATE TRIGGER IF NOT EXISTS trg_host_call_capability_binding_logical_exact
    BEFORE INSERT ON host_call_capability_bindings
    WHEN NOT EXISTS (
      SELECT 1 FROM logical_tool_calls call
       WHERE call.session_id = NEW.session_id
         AND call.source_user_seq = NEW.source_user_seq
         AND call.accepted_task_id = NEW.accepted_task_id
         AND call.logical_tool_call_id = NEW.logical_tool_call_id
         AND call.tool_name = NEW.tool_name
         AND call.argument_digest = NEW.attested_argument_digest
         AND call.raw_argument_digest = NEW.logical_raw_argument_digest
         AND call.effective_argument_digest IS NEW.bound_effective_argument_digest
         AND call.state = 'open'
    )
    BEGIN
      SELECT RAISE(ABORT, 'host-call capability binding requires its exact open logical call');
    END;

    CREATE TRIGGER IF NOT EXISTS trg_host_call_capability_binding_update_immutable
    BEFORE UPDATE ON host_call_capability_bindings
    BEGIN
      SELECT RAISE(ABORT, 'host-call capability bindings are immutable');
    END;

    CREATE TRIGGER IF NOT EXISTS trg_host_call_capability_binding_delete_immutable
    BEFORE DELETE ON host_call_capability_bindings
    WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
    BEGIN
      SELECT RAISE(ABORT, 'host-call capability bindings are immutable');
    END;
  `);
  const columns = db.prepare("PRAGMA table_info(host_call_capability_bindings)").all().map((column) => column.name);
  const expectedColumns = [
    "protocol_version",
    "root_authority_kind",
    "root_graph_event_id",
    "root_graph_hash",
    "session_id",
    "source_user_seq",
    "accepted_task_id",
    "source_event_id",
    "source_event_digest",
    "logical_tool_call_id",
    "tool_name",
    "attested_argument_digest",
    "logical_raw_argument_digest",
    "bound_effective_argument_digest",
    "effect",
    "binding_kind",
    "capability_id",
    "provider_input_schema_digest",
    "schema_fingerprint",
    "account_id",
    "invoke_port_id",
    "operation_id",
    "manifest_id",
    "manifest_digest",
    "host_binding_digest",
    "engine_version",
    "surface_version",
    "authority_digest",
    "authority_revision",
    "surface_digest",
    "catalog_revision_digest",
    "binding_revision_digest",
    "durable_binding_digest",
    "bound_at"
  ];
  if (JSON.stringify(columns) !== JSON.stringify(expectedColumns)) {
    throw new Error("schema v57 host-call capability binding columns are not exact");
  }
  const parents = new Set(db.prepare("PRAGMA foreign_key_list(host_call_capability_bindings)").all().map((fk) => fk.table));
  if (!parents.has("events") || !parents.has("logical_tool_calls") || !parents.has("accepted_turn_call_authorities")) {
    throw new Error("schema v57 host-call capability binding foreign keys are incomplete");
  }
}
function canonicalHistoricalTriggerSql(sql) {
  let canonical = "";
  let inString = false;
  for (let index = 0; index < sql.length; index += 1) {
    const character = sql[index];
    if (inString) {
      canonical += character;
      if (character === "'") {
        if (sql[index + 1] === "'") {
          canonical += sql[index + 1];
          index += 1;
        } else {
          inString = false;
        }
      }
      continue;
    }
    if (character === "'") {
      inString = true;
      canonical += character;
      continue;
    }
    if (/\s/.test(character)) continue;
    canonical += character.toLowerCase();
  }
  if (inString) throw new Error("schema v70 amendment trigger SQL has an unterminated string");
  canonical = canonical.replace(/^createtriggerifnotexists/, "createtrigger");
  return canonical.replace(/;+$/, "");
}
function assertExactAmendmentImmutabilityTriggers(objects) {
  const triggers = objects.filter((object) => object.type === "trigger");
  if (triggers.length !== EXACT_AMENDMENT_TRIGGER_SQL.size) {
    throw new Error("schema v70 amendment trigger set is not exact");
  }
  for (const trigger of triggers) {
    const expected = EXACT_AMENDMENT_TRIGGER_SQL.get(trigger.name);
    if (!expected || canonicalHistoricalTriggerSql(trigger.sql) !== expected) {
      throw new Error(`schema v70 amendment trigger is not exact: ${trigger.name}`);
    }
  }
}
function rebuildExpectedWorkUniverseAmendmentCascade(db) {
  const tableName = "expected_work_universe_amendments";
  const table = db.prepare(
    `SELECT sql FROM sqlite_master WHERE type = 'table' AND name = ?`
  ).get(tableName);
  if (!table?.sql) return;
  const prerequisites = ["sessions", "events", "accepted_task_work_contracts"];
  const tables = new Set(db.prepare(
    `SELECT name FROM sqlite_master WHERE type = 'table'`
  ).all().map((row) => row.name));
  for (const prerequisite of prerequisites) {
    if (!tables.has(prerequisite)) {
      throw new Error(`schema v70 prerequisite missing: ${prerequisite}`);
    }
  }
  const expectedColumns = [
    "session_id",
    "source_user_seq",
    "contract_id",
    "universe_id",
    "prior_member_id_pointer",
    "member_id_pointer",
    "motivating_refusal",
    "sealed_member_count",
    "amended_at",
    "amendment_event_id"
  ];
  const columns = db.prepare(
    `PRAGMA table_info(${quotedSchemaIdentifier(tableName)})`
  ).all().map((column) => column.name);
  if (JSON.stringify(columns) !== JSON.stringify(expectedColumns)) {
    throw new Error("schema v70 amendment columns are not exact");
  }
  const objects = db.prepare(`
    SELECT type, name, sql FROM sqlite_master
     WHERE tbl_name = ?
       AND type IN ('index','trigger')
       AND sql IS NOT NULL
     ORDER BY type, name
  `).all(tableName);
  assertExactAmendmentImmutabilityTriggers(objects);
  db.exec(`
    CREATE UNIQUE INDEX IF NOT EXISTS uq_accepted_task_work_contract_exact_identity
      ON accepted_task_work_contracts(session_id, source_user_seq, contract_id);
    CREATE UNIQUE INDEX IF NOT EXISTS uq_event_exact_session_identity
      ON events(session_id, id);
  `);
  const exactIndexSql = new Map(db.prepare(`
    SELECT name, sql FROM sqlite_master
     WHERE type = 'index'
       AND name IN (
         'uq_accepted_task_work_contract_exact_identity',
         'uq_event_exact_session_identity'
       )
  `).all().map((row) => [row.name, row.sql ?? ""]));
  if (!/accepted_task_work_contracts\s*\(\s*session_id\s*,\s*source_user_seq\s*,\s*contract_id\s*\)/i.test(exactIndexSql.get("uq_accepted_task_work_contract_exact_identity") ?? "") || !/events\s*\(\s*session_id\s*,\s*id\s*\)/i.test(exactIndexSql.get("uq_event_exact_session_identity") ?? "")) {
    throw new Error("schema v70 exact parent identity index is missing or incompatible");
  }
  const exactCascade = () => {
    const foreignKeys = db.prepare(
      `PRAGMA foreign_key_list(${quotedSchemaIdentifier(tableName)})`
    ).all();
    const groups = /* @__PURE__ */ new Map();
    for (const row of foreignKeys) {
      const group = groups.get(row.id) ?? [];
      group.push(row);
      groups.set(row.id, group);
    }
    const canonical = [...groups.values()].map((group) => group.sort((left, right) => left.seq - right.seq).map((row) => `${row.from}:${row.table}.${row.to}:${row.on_delete.toUpperCase()}`).join("|"));
    return canonical.length === 3 && canonical.includes("session_id:sessions.id:CASCADE") && canonical.includes(
      "session_id:accepted_task_work_contracts.session_id:CASCADE|source_user_seq:accepted_task_work_contracts.source_user_seq:CASCADE|contract_id:accepted_task_work_contracts.contract_id:CASCADE"
    ) && canonical.includes(
      "session_id:events.session_id:CASCADE|amendment_event_id:events.id:CASCADE"
    );
  };
  const verify = () => {
    if (!exactCascade()) {
      throw new Error("schema v70 amendment session/contract/event cascade is not exact");
    }
    const finalObjects = db.prepare(`
      SELECT type, name, sql FROM sqlite_master
       WHERE tbl_name = ?
         AND type IN ('index','trigger')
         AND sql IS NOT NULL
       ORDER BY type, name
    `).all(tableName);
    assertExactAmendmentImmutabilityTriggers(finalObjects);
    const relevantTables = foreignKeyClosure(db, [tableName]);
    const violations = db.pragma("foreign_key_check").filter((violation) => relevantTables.has(violation.table));
    if (violations.length > 0) {
      throw new Error(`schema v70 foreign-key check failed for ${violations.length} amendment row(s)`);
    }
    const integrity = db.pragma("integrity_check");
    if (integrity.length !== 1 || integrity[0]?.integrity_check !== "ok") {
      throw new Error(`schema v70 integrity check failed: ${JSON.stringify(integrity).slice(0, 240)}`);
    }
  };
  if (exactCascade()) {
    verify();
    return;
  }
  const legacyExists = db.prepare(
    `SELECT 1 FROM sqlite_master WHERE type = 'table'
      AND name = 'expected_work_universe_amendments_v69'`
  ).get();
  if (legacyExists) throw new Error("schema v70 amendment rebuild source already exists");
  const mismatchedContracts = db.prepare(`
    SELECT COUNT(*) AS n
      FROM expected_work_universe_amendments a
      LEFT JOIN accepted_task_work_contracts c
        ON c.session_id = a.session_id
       AND c.source_user_seq = a.source_user_seq
       AND c.contract_id = a.contract_id
     WHERE c.contract_id IS NULL
  `).get().n;
  const mismatchedEvents = db.prepare(`
    SELECT COUNT(*) AS n
      FROM expected_work_universe_amendments a
      LEFT JOIN events e
        ON e.session_id = a.session_id
       AND e.id = a.amendment_event_id
     WHERE e.id IS NULL
  `).get().n;
  if (mismatchedContracts > 0 || mismatchedEvents > 0) {
    throw new Error(
      `schema v70 refuses mixed amendment identities: contracts=${mismatchedContracts}, events=${mismatchedEvents}`
    );
  }
  const beforeRows = db.prepare(
    "SELECT COUNT(*) AS n FROM expected_work_universe_amendments"
  ).get().n;
  const beforeBytes = JSON.stringify(db.prepare(`
    SELECT * FROM expected_work_universe_amendments
     ORDER BY session_id, source_user_seq, contract_id, universe_id
  `).all());
  const dependentObjectsBefore = JSON.stringify(db.prepare(`
    SELECT type, name, tbl_name, sql FROM sqlite_master
     WHERE tbl_name != 'expected_work_universe_amendments'
       AND sql IS NOT NULL
       AND lower(sql) LIKE '%expected_work_universe_amendments%'
     ORDER BY type, name
  `).all());
  const priorLegacyRename = Number(db.pragma("legacy_alter_table", { simple: true })) === 1;
  db.pragma("legacy_alter_table = ON");
  try {
    for (const object of objects) {
      db.exec(`DROP ${object.type.toUpperCase()} ${quotedSchemaIdentifier(object.name)}`);
    }
    db.exec(`
      ALTER TABLE expected_work_universe_amendments
        RENAME TO expected_work_universe_amendments_v69;

      CREATE TABLE expected_work_universe_amendments (
        session_id              TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
        source_user_seq         INTEGER NOT NULL CHECK (source_user_seq > 0),
        contract_id             TEXT NOT NULL,
        universe_id             TEXT NOT NULL,
        prior_member_id_pointer TEXT NOT NULL,
        member_id_pointer       TEXT NOT NULL
                                CHECK (member_id_pointer != prior_member_id_pointer),
        motivating_refusal      TEXT NOT NULL,
        sealed_member_count     INTEGER NOT NULL CHECK (sealed_member_count > 0),
        amended_at              TEXT NOT NULL,
        amendment_event_id      TEXT NOT NULL,
        PRIMARY KEY (session_id, source_user_seq, contract_id, universe_id),
        FOREIGN KEY (session_id, source_user_seq, contract_id)
          REFERENCES accepted_task_work_contracts(session_id, source_user_seq, contract_id)
          ON DELETE CASCADE,
        FOREIGN KEY (session_id, amendment_event_id)
          REFERENCES events(session_id, id)
          ON DELETE CASCADE
      );

      INSERT INTO expected_work_universe_amendments (
        session_id, source_user_seq, contract_id, universe_id,
        prior_member_id_pointer, member_id_pointer, motivating_refusal,
        sealed_member_count, amended_at, amendment_event_id
      )
      SELECT session_id, source_user_seq, contract_id, universe_id,
             prior_member_id_pointer, member_id_pointer, motivating_refusal,
             sealed_member_count, amended_at, amendment_event_id
        FROM expected_work_universe_amendments_v69;

      DROP TABLE expected_work_universe_amendments_v69;
    `);
    for (const object of objects) db.exec(object.sql);
  } finally {
    db.pragma(`legacy_alter_table = ${priorLegacyRename ? "ON" : "OFF"}`);
  }
  const afterRows = db.prepare(
    "SELECT COUNT(*) AS n FROM expected_work_universe_amendments"
  ).get().n;
  const afterBytes = JSON.stringify(db.prepare(`
    SELECT * FROM expected_work_universe_amendments
     ORDER BY session_id, source_user_seq, contract_id, universe_id
  `).all());
  const dependentObjectsAfter = JSON.stringify(db.prepare(`
    SELECT type, name, tbl_name, sql FROM sqlite_master
     WHERE tbl_name != 'expected_work_universe_amendments'
       AND sql IS NOT NULL
       AND lower(sql) LIKE '%expected_work_universe_amendments%'
     ORDER BY type, name
  `).all());
  if (beforeRows !== afterRows || beforeBytes !== afterBytes || dependentObjectsBefore !== dependentObjectsAfter) {
    throw new Error(
      `schema v70 changed amendment rows or dependent objects: ${beforeRows} -> ${afterRows}`
    );
  }
  verify();
}
function v71PreambleAddress(input) {
  let raw;
  try {
    const parsed = JSON.parse(input.data_json);
    if (!parsed || typeof parsed !== "object" || Array.isArray(parsed)) return null;
    raw = parsed;
  } catch {
    return null;
  }
  if (Object.keys(raw).some((key) => ![
    "version",
    "kind",
    "sourceUserSeq",
    "text",
    "intentKey"
  ].includes(key))) return null;
  const sourceUserSeq = raw.sourceUserSeq;
  const text = typeof raw.text === "string" ? raw.text.trim() : "";
  const intentKey = raw.intentKey === void 0 ? void 0 : typeof raw.intentKey === "string" ? raw.intentKey.trim() : "";
  if (input.role !== "Clem" || input.type !== "conversation_preamble" || !input.parent_event_id || raw.version !== 1 || raw.kind !== "pre_execution" || !Number.isSafeInteger(sourceUserSeq) || Number(sourceUserSeq) <= 0 || !text || text.length > 8e3 || text.includes("\0") || intentKey !== void 0 && !/^[A-Za-z0-9][A-Za-z0-9_.:-]{0,127}$/.test(intentKey)) return null;
  const data = {
    version: 1,
    kind: "pre_execution",
    sourceUserSeq: Number(sourceUserSeq),
    text,
    ...intentKey ? { intentKey } : {}
  };
  const eventDigest = (0, import_node_crypto6.createHash)("sha256").update(JSON.stringify({
    version: 1,
    seq: input.seq,
    id: input.id,
    sessionId: input.session_id,
    turn: input.turn,
    role: input.role,
    type: input.type,
    parentEventId: input.parent_event_id,
    data,
    createdAt: input.created_at
  }), "utf8").digest("hex");
  return {
    eventDigest,
    deliveryKey: `preamble-delivery:v1:${(0, import_node_crypto6.createHash)("sha256").update(JSON.stringify({ version: 1, eventId: input.id, eventDigest }), "utf8").digest("hex")}`
  };
}
function backfillPlanTaskPreparationCheckpointsV71(db) {
  db.exec(`
    INSERT INTO ${PLAN_TASK_BINDING_SEAL_INTENTS_TABLE}
      (session_id, source_user_seq, accepted_task_id, logical_tool_call_id,
       intent_version, intent_origin, plan_argument_digest, graph_event_id,
       graph_id, graph_hash, contract_id, objective_text, objective_digest,
       semantic_input_digest, operation_ids_json, operation_ids_digest,
       preamble_text, preamble_text_digest, delivery_owner, recorded_at)
    SELECT receipt.session_id, receipt.source_user_seq, receipt.accepted_task_id,
           receipt.logical_tool_call_id, 1, 'legacy_backfill',
           receipt.plan_argument_digest, receipt.graph_event_id,
           receipt.graph_id, receipt.graph_hash, receipt.contract_id,
           COALESCE(NULLIF(trim(json_extract(source.data_json, '$.displayText')), ''),
                    NULLIF(trim(json_extract(source.data_json, '$.text')), ''),
                    'legacy-v70'),
           COALESCE(json_extract(graph.data_json, '$.graph.source.inputHash'), receipt.graph_hash),
           COALESCE(json_extract(graph.data_json, '$.graph.source.inputHash'), receipt.graph_hash),
           COALESCE((SELECT json_group_array(json_extract(operation.value, '$.id'))
              FROM json_each(contract.contract_json, '$.operations') operation), '[]'),
           receipt.graph_hash,
           json_extract(preamble.data_json, '$.text'), receipt.graph_hash,
           CASE receipt.transport_target
             WHEN 'durable_conversation' THEN 'durable_conversation'
             ELSE 'carrier_owned'
           END,
           receipt.recorded_at
      FROM ${PLAN_TASK_ACTIVATION_RECEIPTS_TABLE} receipt
      JOIN accepted_task_work_contracts contract
        ON contract.session_id = receipt.session_id
       AND contract.source_user_seq = receipt.source_user_seq
       AND contract.contract_id = receipt.contract_id
      JOIN events graph ON graph.id = receipt.graph_event_id
      JOIN events source
        ON source.session_id = receipt.session_id
       AND source.seq = receipt.source_user_seq
      JOIN events preamble ON preamble.id = receipt.preamble_event_id
     WHERE NOT EXISTS (
       SELECT 1 FROM ${PLAN_TASK_BINDING_SEAL_INTENTS_TABLE} intent
        WHERE intent.session_id = receipt.session_id
          AND intent.source_user_seq = receipt.source_user_seq
     );

    INSERT INTO ${PLAN_TASK_PREPARATION_CHECKPOINTS_TABLE}
      (session_id, source_user_seq, accepted_task_id, logical_tool_call_id,
       checkpoint_version, plan_argument_digest, graph_event_id, graph_id,
       graph_hash, contract_id, preamble_event_id, preamble_event_digest,
       delivery_key, delivery_owner, recorded_at)
    SELECT receipt.session_id, receipt.source_user_seq, receipt.accepted_task_id,
           receipt.logical_tool_call_id, 1, receipt.plan_argument_digest,
           receipt.graph_event_id, receipt.graph_id, receipt.graph_hash,
           receipt.contract_id, receipt.preamble_event_id,
           receipt.preamble_event_digest, receipt.delivery_key,
           CASE receipt.transport_target
             WHEN 'durable_conversation' THEN 'durable_conversation'
             ELSE 'carrier_owned'
           END,
           receipt.recorded_at
      FROM ${PLAN_TASK_ACTIVATION_RECEIPTS_TABLE} receipt
     WHERE NOT EXISTS (
       SELECT 1 FROM ${PLAN_TASK_PREPARATION_CHECKPOINTS_TABLE} checkpoint
        WHERE checkpoint.session_id = receipt.session_id
          AND checkpoint.source_user_seq = receipt.source_user_seq
     );
  `);
  const candidates = db.prepare(`
    SELECT root.session_id, root.source_user_seq, root.accepted_task_id,
           call.logical_tool_call_id, call.argument_digest,
           task.graph_event_id, task.graph_id, task.graph_hash,
           contract.contract_id,
           COALESCE(NULLIF(trim(json_extract(source.data_json, '$.displayText')), ''),
                    NULLIF(trim(json_extract(source.data_json, '$.text')), ''),
                    'legacy-v70') AS objective_text,
           COALESCE(json_extract(graph.data_json, '$.graph.source.inputHash'), task.graph_hash)
             AS semantic_input_digest,
           COALESCE((SELECT json_group_array(json_extract(operation.value, '$.id'))
              FROM json_each(contract.contract_json, '$.operations') operation), '[]')
             AS operation_ids_json,
           json_extract(preamble.data_json, '$.text') AS preamble_text,
           preamble.seq, preamble.id, preamble.turn, preamble.role,
           preamble.type, preamble.parent_event_id, preamble.data_json,
           preamble.created_at
      FROM accepted_turn_call_authorities root
      JOIN accepted_task_authority task
        ON task.session_id = root.session_id
       AND task.source_user_seq = root.source_user_seq
       AND task.accepted_task_id = root.accepted_task_id
      JOIN accepted_task_work_contracts contract
        ON contract.session_id = task.session_id
       AND contract.source_user_seq = task.source_user_seq
       AND contract.accepted_task_id = task.accepted_task_id
       AND contract.contract_id = task.work_contract_id
      JOIN events source
        ON source.session_id = root.session_id
       AND source.seq = root.source_user_seq
       AND source.id = root.source_event_id
      JOIN events graph
        ON graph.session_id = root.session_id
       AND graph.id = task.graph_event_id
      JOIN events preamble
        ON preamble.session_id = source.session_id
       AND preamble.parent_event_id = source.id
       AND preamble.turn = source.turn
       AND preamble.role = 'Clem'
       AND preamble.type = 'conversation_preamble'
      JOIN logical_tool_calls call
        ON call.session_id = root.session_id
       AND call.source_user_seq = root.source_user_seq
       AND call.accepted_task_id = root.accepted_task_id
       AND call.tool_name = 'plan_task'
       AND call.opened_at <= preamble.created_at
       AND (call.settled_at IS NULL OR call.settled_at >= preamble.created_at)
     WHERE root.authority_kind = 'host_v1'
       AND root.engine_version = 'host_v1'
       AND root.state = 'open'
       AND task.state != 'conflict'
       AND task.expected_work_required = 0
       AND contract.contract_version = 1
       AND contract.planner_source = 'structured_model'
       AND NOT EXISTS (
         SELECT 1 FROM ${PLAN_TASK_ACTIVATION_RECEIPTS_TABLE} receipt
          WHERE receipt.session_id = root.session_id
            AND receipt.source_user_seq = root.source_user_seq
       )
       AND NOT EXISTS (
         SELECT 1 FROM ${PLAN_TASK_PREPARATION_CHECKPOINTS_TABLE} checkpoint
          WHERE checkpoint.session_id = root.session_id
            AND checkpoint.source_user_seq = root.source_user_seq
       )
     ORDER BY root.session_id, root.source_user_seq, call.opened_at, call.logical_tool_call_id
  `).all();
  const bySource = /* @__PURE__ */ new Map();
  for (const candidate of candidates) {
    const key = `${candidate.session_id}\0${candidate.source_user_seq}`;
    const grouped = bySource.get(key) ?? [];
    grouped.push(candidate);
    bySource.set(key, grouped);
  }
  const insert = db.prepare(`
    INSERT INTO ${PLAN_TASK_PREPARATION_CHECKPOINTS_TABLE}
      (session_id, source_user_seq, accepted_task_id, logical_tool_call_id,
       checkpoint_version, plan_argument_digest, graph_event_id, graph_id,
       graph_hash, contract_id, preamble_event_id, preamble_event_digest,
       delivery_key, delivery_owner, recorded_at)
    VALUES (?, ?, ?, ?, 1, ?, ?, ?, ?, ?, ?, ?, ?, 'legacy_unknown', ?)
  `);
  const insertIntent = db.prepare(`
    INSERT INTO ${PLAN_TASK_BINDING_SEAL_INTENTS_TABLE}
      (session_id, source_user_seq, accepted_task_id, logical_tool_call_id,
       intent_version, intent_origin, plan_argument_digest, graph_event_id,
       graph_id, graph_hash, contract_id, objective_text, objective_digest,
       semantic_input_digest, operation_ids_json, operation_ids_digest,
       preamble_text, preamble_text_digest, delivery_owner, recorded_at)
    VALUES (?, ?, ?, ?, 1, 'legacy_backfill', ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, 'legacy_unknown', ?)
  `);
  for (const grouped of bySource.values()) {
    if (grouped.length !== 1) continue;
    const candidate = grouped[0];
    const address = v71PreambleAddress(candidate);
    if (!address) continue;
    insertIntent.run(
      candidate.session_id,
      candidate.source_user_seq,
      candidate.accepted_task_id,
      candidate.logical_tool_call_id,
      candidate.argument_digest,
      candidate.graph_event_id,
      candidate.graph_id,
      candidate.graph_hash,
      candidate.contract_id,
      candidate.objective_text,
      candidate.semantic_input_digest,
      candidate.semantic_input_digest,
      candidate.operation_ids_json,
      candidate.graph_hash,
      candidate.preamble_text,
      candidate.graph_hash,
      candidate.created_at
    );
    insert.run(
      candidate.session_id,
      candidate.source_user_seq,
      candidate.accepted_task_id,
      candidate.logical_tool_call_id,
      candidate.argument_digest,
      candidate.graph_event_id,
      candidate.graph_id,
      candidate.graph_hash,
      candidate.contract_id,
      candidate.id,
      address.eventDigest,
      address.deliveryKey,
      candidate.created_at
    );
  }
}
function refreshCheckpointBackedPlanContinuationTriggersV71(db) {
  db.exec(`
    DROP TRIGGER IF EXISTS trg_accepted_task_resolution_excludes_host_root;
    CREATE TRIGGER trg_accepted_task_resolution_excludes_host_root
    BEFORE INSERT ON accepted_task_resolutions
    WHEN EXISTS (
      SELECT 1 FROM accepted_turn_call_authorities a
       WHERE a.session_id = NEW.session_id
         AND a.source_user_seq = NEW.source_user_seq
         AND a.authority_kind != 'turn_graph'
    ) AND NOT ${hostPlannedResolutionProofSql("NEW", "insert")}
    BEGIN
      SELECT RAISE(ABORT, 'graph resolution cannot replace non-graph call authority');
    END;

    DROP TRIGGER IF EXISTS trg_accepted_model_batch_admission_chain;
    CREATE TRIGGER trg_accepted_model_batch_admission_chain
    BEFORE INSERT ON accepted_model_batch_admissions
    WHEN NOT (
      (
        NEW.batch_ordinal = 1
        AND NOT EXISTS (
          SELECT 1 FROM accepted_model_batch_admissions prior
           WHERE prior.session_id = NEW.session_id
             AND prior.source_user_seq = NEW.source_user_seq
        )
      )
      OR
      EXISTS (
        SELECT 1
          FROM accepted_model_batch_checkpoints prior
         WHERE prior.session_id = NEW.session_id
           AND prior.source_user_seq = NEW.source_user_seq
           AND prior.batch_ordinal = NEW.batch_ordinal - 1
           AND prior.history_digest = NEW.pre_history_digest
           AND prior.last_response_id IS NEW.previous_response_id
           AND prior.authority_digest = NEW.authority_digest
           AND (
             (
               prior.graph_event_id IS NEW.graph_event_id
               AND prior.graph_hash IS NEW.graph_hash
               AND prior.work_contract_id IS NEW.work_contract_id
             )
             OR
             (
               prior.graph_event_id IS NULL
               AND prior.graph_hash IS NULL
               AND prior.work_contract_id IS NULL
               AND NEW.graph_event_id IS NOT NULL
               AND NEW.graph_hash IS NOT NULL
               AND NEW.work_contract_id IS NOT NULL
               AND EXISTS (
                 SELECT 1
                   FROM accepted_task_resolutions r
                   JOIN accepted_task_work_contracts contract
                     ON contract.session_id = r.session_id
                    AND contract.source_user_seq = r.source_user_seq
                    AND contract.accepted_task_id = r.accepted_task_id
                    AND contract.graph_event_id = r.graph_event_id
                    AND contract.graph_hash = r.graph_hash
                  WHERE r.session_id = NEW.session_id
                    AND r.source_user_seq = NEW.source_user_seq
                    AND r.accepted_task_id = NEW.accepted_task_id
                    AND r.graph_event_id = NEW.graph_event_id
                    AND r.graph_hash = NEW.graph_hash
                    AND contract.contract_id = NEW.work_contract_id
                    AND ${hostPlannedResolutionProofSql("r", "existing")}
               )
             )
           )
      )
    )
    BEGIN
      SELECT RAISE(ABORT, 'model batch admission requires the exact prior balanced checkpoint');
    END;
  `);
}
function ensureAuthorityPrivacySchema(db) {
  const sealed = db.prepare(
    `SELECT 1 AS ok FROM sqlite_master WHERE type = 'table' AND name = 'physical_dispatch_authority_sealed'`
  ).get();
  if (sealed) {
    const columns = new Set(
      db.prepare("PRAGMA table_info(physical_dispatch_authority_sealed)").all().map((column) => column.name)
    );
    if (!columns.has("argument_cipher")) {
      db.exec("ALTER TABLE physical_dispatch_authority_sealed ADD COLUMN argument_cipher TEXT");
    }
  }
  db.exec(`
    CREATE TABLE IF NOT EXISTS capability_port_implementations (
      invoke_port_id TEXT NOT NULL,
      reconcile_port_id TEXT NOT NULL DEFAULT '',
      implementation_digest TEXT NOT NULL,
      recorded_at TEXT NOT NULL,
      PRIMARY KEY (invoke_port_id, reconcile_port_id)
    );
    CREATE TABLE IF NOT EXISTS physical_dispatch_owner_fences (
      session_id TEXT NOT NULL,
      source_user_seq INTEGER NOT NULL,
      physical_dispatch_id TEXT NOT NULL,
      owner_fence TEXT NOT NULL,
      PRIMARY KEY (session_id, source_user_seq, physical_dispatch_id)
    );
  `);
  const scrub = (table) => {
    const exists = db.prepare(
      `SELECT 1 AS ok FROM sqlite_master WHERE type = 'table' AND name = ?`
    ).get(table);
    if (!exists) return;
    const columns = new Set(
      db.prepare(`PRAGMA table_info(${table})`).all().map((column) => column.name)
    );
    if (!columns.has("authority_json")) return;
    if (table === "physical_dispatch_authority_payload") {
      db.prepare(
        `DELETE FROM physical_dispatch_authority_payload
          WHERE authority_json LIKE '%canonicalArgs%'
            AND authority_json NOT LIKE '%"argsRedacted":true%'`
      ).run();
      return;
    }
    db.prepare(
      `UPDATE physical_dispatch_authority SET authority_json = NULL
        WHERE authority_json LIKE '%canonicalArgs%'`
    ).run();
  };
  scrub("physical_dispatch_authority_payload");
  scrub("physical_dispatch_authority");
}
var import_node_crypto6, HOST_RESULT_PROTOCOL_FOR_MIGRATION, USER_REJECTED_RESULT_FOR_MIGRATION, LOWER_HEX_SHA256, EXACT_AMENDMENT_TRIGGER_SQL, MIGRATIONS2, newestMigrationVersion, STRICT_TAIL_TABLES;
var init_eventlog_schema = __esm({
  "src/runtime/harness/eventlog-schema.ts"() {
    "use strict";
    import_node_crypto6 = require("node:crypto");
    init_schema_version();
    init_host_planned_resolution_coexistence();
    init_async_read_refinement_schema();
    HOST_RESULT_PROTOCOL_FOR_MIGRATION = "host_tool_disposition_v1";
    USER_REJECTED_RESULT_FOR_MIGRATION = "The user rejected this action. Do not retry it; continue without it or explain what changes.";
    LOWER_HEX_SHA256 = /^[a-f0-9]{64}$/;
    EXACT_AMENDMENT_TRIGGER_SQL = new Map([
      [
        "trg_expected_work_universe_amendment_update_immutable",
        `CREATE TRIGGER trg_expected_work_universe_amendment_update_immutable
     BEFORE UPDATE ON expected_work_universe_amendments
     BEGIN
       SELECT RAISE(ABORT, 'a universe amendment is immutable');
     END`
      ],
      [
        "trg_expected_work_universe_amendment_delete_immutable",
        `CREATE TRIGGER trg_expected_work_universe_amendment_delete_immutable
     BEFORE DELETE ON expected_work_universe_amendments
     WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
     BEGIN
       SELECT RAISE(ABORT, 'a universe amendment is immutable');
     END`
      ]
    ].map(([name, sql]) => [name, canonicalHistoricalTriggerSql(sql)]));
    MIGRATIONS2 = [
      {
        version: 1,
        sql: `
      CREATE TABLE IF NOT EXISTS sessions (
        id              TEXT PRIMARY KEY,
        kind            TEXT NOT NULL CHECK (kind IN ('chat','execution','workflow','agent')),
        channel         TEXT,
        user_id         TEXT,
        created_at      TEXT NOT NULL,
        updated_at      TEXT NOT NULL,
        status          TEXT NOT NULL CHECK (status IN ('active','paused','completed','failed','cancelled')),
        title           TEXT,
        objective       TEXT,
        token_budget    INTEGER,
        tokens_used     INTEGER NOT NULL DEFAULT 0,
        current_plan_id TEXT,
        metadata_json   TEXT NOT NULL DEFAULT '{}'
      );
      CREATE INDEX IF NOT EXISTS idx_sessions_status_updated ON sessions(status, updated_at DESC);
      CREATE INDEX IF NOT EXISTS idx_sessions_channel ON sessions(channel) WHERE channel IS NOT NULL;

      CREATE TABLE IF NOT EXISTS events (
        seq             INTEGER PRIMARY KEY AUTOINCREMENT,
        id              TEXT NOT NULL UNIQUE,
        session_id      TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
        turn            INTEGER NOT NULL,
        role            TEXT NOT NULL,
        type            TEXT NOT NULL,
        parent_event_id TEXT,
        data_json       TEXT NOT NULL,
        created_at      TEXT NOT NULL
      );
      CREATE INDEX IF NOT EXISTS idx_events_session_seq ON events(session_id, seq);
      CREATE INDEX IF NOT EXISTS idx_events_session_type ON events(session_id, type);

      CREATE TABLE IF NOT EXISTS kill_switches (
        session_id   TEXT PRIMARY KEY REFERENCES sessions(id) ON DELETE CASCADE,
        requested_at TEXT NOT NULL,
        reason       TEXT
      );
    `
      },
      {
        // Reliability pass v0.4.20:
        //   - session_locks: legacy cross-process lock table. Its withSessionLock
        //     helper was removed in the 2026-07-09 subtraction pass (no live caller);
        //     the table CREATE is retained as an inert vestige — dropping it is a
        //     separate schema change, out of scope for that pass.
        //   - pending_approvals: addressable approval requests with per-row TTL.
        //     One row per `approval_requested` event. The reaper expires stale
        //     rows; the approval-registry resolves them by approval_id so a
        //     bare "approve" reply on a busy channel never silently routes to
        //     the wrong paused session.
        //
        // Both tables reference sessions(id) so they cascade on session delete.
        // session_locks is a small set (one row per actively-locked session,
        // typically <10 at peak); pending_approvals grows with usage but the
        // reaper keeps it bounded.
        version: 2,
        sql: `
      CREATE TABLE IF NOT EXISTS session_locks (
        session_id   TEXT PRIMARY KEY REFERENCES sessions(id) ON DELETE CASCADE,
        owner_pid    INTEGER NOT NULL,
        owner_token  TEXT NOT NULL,
        acquired_at  INTEGER NOT NULL,
        expires_at   INTEGER NOT NULL
      );

      CREATE TABLE IF NOT EXISTS pending_approvals (
        approval_id   TEXT PRIMARY KEY,
        session_id    TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
        channel       TEXT,
        channel_id    TEXT,
        requested_at  TEXT NOT NULL,
        expires_at    TEXT NOT NULL,
        subject       TEXT NOT NULL,
        tool          TEXT,
        args_json     TEXT,
        status        TEXT NOT NULL DEFAULT 'pending'
                      CHECK (status IN ('pending','resolved','expired','cancelled')),
        resolution    TEXT
                      CHECK (resolution IS NULL OR resolution IN ('approved','rejected','expired','cancelled_by_user')),
        resolver      TEXT,
        resolved_at   TEXT
      );
      CREATE INDEX IF NOT EXISTS idx_pending_approvals_session_status
        ON pending_approvals(session_id, status);
      CREATE INDEX IF NOT EXISTS idx_pending_approvals_channel_status
        ON pending_approvals(channel_id, status) WHERE channel_id IS NOT NULL;
      CREATE INDEX IF NOT EXISTS idx_pending_approvals_expires
        ON pending_approvals(expires_at) WHERE status = 'pending';
    `
      },
      {
        // v0.5.10 auto-compact: lossless tool-output storage keyed by call_id.
        // The event log clips tool_returned payloads to 8KB at write-time
        // (see hooks.ts:202) for readability; that loss broke the
        // recall_tool_result promise. This table stores the full output
        // (up to 200KB) so an agent that sees `[clipped: ... call
        // recall_tool_result …]` stub can retrieve the verbatim
        // original. Append-only; cascade-deleted with the session.
        version: 3,
        sql: `
      CREATE TABLE IF NOT EXISTS tool_outputs (
        session_id          TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
        call_id             TEXT NOT NULL,
        tool                TEXT,
        output_full         TEXT NOT NULL,
        content_bytes       INTEGER NOT NULL,
        truncated_at_write  INTEGER NOT NULL DEFAULT 0,
        created_at          TEXT NOT NULL,
        PRIMARY KEY (session_id, call_id)
      );
      CREATE INDEX IF NOT EXISTS idx_tool_outputs_session ON tool_outputs(session_id);
    `
      },
      {
        // v0.5.19 F6 — persist tool-guardrail recent-call queue so the
        // loop-detection thresholds survive daemon restarts. Until v0.5.19
        // tool-guardrail.ts held SessionTrackerState only in-memory, which
        // meant multi-hour workflows that crossed a restart (autonomy
        // loops, cron-scheduled runs) lost their loop-detection history.
        // Append-only blob — one row per session_id, replaced on every
        // write-through (debounced every N calls). Cascade-deleted with
        // the session.
        version: 4,
        sql: `
      CREATE TABLE IF NOT EXISTS tool_guardrail_state (
        session_id  TEXT PRIMARY KEY REFERENCES sessions(id) ON DELETE CASCADE,
        recent_json TEXT NOT NULL,
        updated_at  TEXT NOT NULL
      );
    `
      },
      {
        // Workflow-owned Claude SDK approval parking. A workflow query must be able
        // to release its child process + drain slot while a human reviews the exact
        // tool payload, then reuse that decision once after a daemon restart. The
        // resume key identifies the session/tool/payload; consumed_at is claimed
        // atomically before the approved call is allowed through.
        version: 5,
        sql: `
      ALTER TABLE pending_approvals ADD COLUMN resume_key TEXT;
      ALTER TABLE pending_approvals ADD COLUMN consumed_at TEXT;
      CREATE UNIQUE INDEX IF NOT EXISTS idx_pending_approvals_pending_resume_key
        ON pending_approvals(resume_key)
        WHERE resume_key IS NOT NULL AND status = 'pending';
      CREATE INDEX IF NOT EXISTS idx_pending_approvals_resume_history
        ON pending_approvals(resume_key, requested_at DESC)
        WHERE resume_key IS NOT NULL;
    `
      },
      {
        // Guardrail trackers are keyed by an EXECUTION SCOPE, not always by a real
        // harness session id. Nested dispatch, certified batches, and workers append
        // `::nestedDispatch`, `::batch:*`, or `::w:*` to the parent session. Legacy
        // databases may still contain the historical `::codeMode` suffix. The v4 table
        // incorrectly made that synthetic key a direct FK to sessions(id), so every
        // fifth scoped tool call failed to persist with FOREIGN KEY constraint
        // errors. Keep the scope isolated while anchoring its lifecycle to the real
        // parent session for cascade cleanup.
        version: 6,
        sql: `
      CREATE TABLE IF NOT EXISTS tool_guardrail_scope_state (
        scope_id          TEXT PRIMARY KEY,
        parent_session_id TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
        recent_json       TEXT NOT NULL,
        updated_at        TEXT NOT NULL
      );
      CREATE INDEX IF NOT EXISTS idx_tool_guardrail_scope_parent
        ON tool_guardrail_scope_state(parent_session_id);
    `,
        backfill: (db) => {
          const hasTable2 = (name) => Boolean(db.prepare(
            `SELECT 1 FROM sqlite_master WHERE type = 'table' AND name = ?`
          ).get(name));
          if (!hasTable2("sessions") || !hasTable2("tool_guardrail_state")) return;
          db.exec(`
        INSERT OR IGNORE INTO tool_guardrail_scope_state
          (scope_id, parent_session_id, recent_json, updated_at)
        SELECT legacy.session_id,
               CASE
                 WHEN instr(legacy.session_id, '::') > 0
                   THEN substr(legacy.session_id, 1, instr(legacy.session_id, '::') - 1)
                 ELSE legacy.session_id
               END,
               legacy.recent_json,
               legacy.updated_at
          FROM tool_guardrail_state AS legacy
          JOIN sessions AS parent
            ON parent.id = CASE
              WHEN instr(legacy.session_id, '::') > 0
                THEN substr(legacy.session_id, 1, instr(legacy.session_id, '::') - 1)
              ELSE legacy.session_id
            END;
      `);
        }
      },
      {
        // Turn-control reliability: cancellation belongs to one concrete run
        // attempt, not to a reusable chat session forever. `kill_switches` is kept
        // for compatibility with the Codex-loop callers, while the two additive
        // tables below carry the precise run/attempt identity used by interactive
        // channels and the Claude SDK brain.
        //
        // The terminal-key index makes a brain attempt's
        // `conversation_completed` append atomic/idempotent. A session legitimately
        // has many completion events across turns, so uniqueness is scoped to the
        // explicit terminalKey rather than merely (session,type).
        version: 7,
        sql: "",
        backfill: (db) => {
          const hasTable2 = (name) => Boolean(db.prepare(
            `SELECT 1 FROM sqlite_master WHERE type = 'table' AND name = ?`
          ).get(name));
          if (!hasTable2("sessions")) return;
          db.exec(`
        CREATE TABLE IF NOT EXISTS run_attempts (
          attempt_id  TEXT PRIMARY KEY,
          session_id  TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
          run_id      TEXT,
          started_at  TEXT NOT NULL,
          finished_at TEXT,
          status      TEXT NOT NULL
        );
        CREATE INDEX IF NOT EXISTS idx_run_attempts_session_active
          ON run_attempts(session_id, finished_at, started_at DESC);

        CREATE TABLE IF NOT EXISTS run_kill_requests (
          session_id  TEXT PRIMARY KEY REFERENCES sessions(id) ON DELETE CASCADE,
          attempt_id  TEXT REFERENCES run_attempts(attempt_id) ON DELETE CASCADE,
          run_id      TEXT,
          requested_at TEXT NOT NULL,
          reason      TEXT
        );
      `);
          if (hasTable2("events")) {
            db.exec(`
          CREATE UNIQUE INDEX IF NOT EXISTS idx_events_terminal_key
            ON events(session_id, type, json_extract(data_json, '$.terminalKey'))
            WHERE type = 'conversation_completed'
              AND json_extract(data_json, '$.terminalKey') IS NOT NULL;
        `);
          }
        }
      },
      {
        // Desktop POST idempotency: the client owns request_id before sending, and
        // this durable receipt binds it to the server-created session, run identity,
        // original SSE cursor, and exact payload. A retry after a lost 202 or daemon
        // restart therefore rejoins the same turn instead of starting a second run.
        version: 8,
        sql: `
      CREATE TABLE IF NOT EXISTS harness_chat_requests (
        request_id TEXT PRIMARY KEY,
        session_id TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
        run_id TEXT NOT NULL UNIQUE,
        input_hash TEXT NOT NULL,
        since_seq INTEGER NOT NULL,
        created_at TEXT NOT NULL
      );
      CREATE INDEX IF NOT EXISTS idx_harness_chat_requests_session
        ON harness_chat_requests(session_id, created_at DESC);
    `
      },
      {
        // A durable request receipt is only half of restart safety: an unfinished
        // attempt also needs bounded ownership. The desktop route renews this
        // lease while its process is alive; a new daemon interrupts foreign-owner
        // attempts at startup, and an expired lease can be reclaimed. This keeps a
        // crash between the 202 and terminal event from making a replay inert
        // forever, without permitting a second executor while the first is alive.
        version: 9,
        sql: "",
        backfill: (db) => {
          const hasAttempts = Boolean(db.prepare(
            `SELECT 1 FROM sqlite_master WHERE type = 'table' AND name = 'run_attempts'`
          ).get());
          if (!hasAttempts) return;
          const columns = new Set(
            db.prepare("PRAGMA table_info(run_attempts)").all().map((row) => row.name)
          );
          if (!columns.has("lease_owner")) db.exec("ALTER TABLE run_attempts ADD COLUMN lease_owner TEXT");
          if (!columns.has("lease_expires_at")) db.exec("ALTER TABLE run_attempts ADD COLUMN lease_expires_at TEXT");
          db.exec(`CREATE INDEX IF NOT EXISTS idx_run_attempts_lease
        ON run_attempts(finished_at, lease_expires_at)`);
        }
      },
      {
        // A run attempt must point at the exact user-input event that created it.
        // Timestamps are not an identity: a reusable desktop chat can receive a new
        // input while the prior attempt is still the newest row, and recovery/UI
        // projections otherwise guess the wrong scope. Keep this additive so old
        // attempts remain valid (NULL means the historical source was not recorded).
        version: 10,
        sql: "",
        backfill: (db) => {
          const hasAttempts = Boolean(db.prepare(
            `SELECT 1 FROM sqlite_master WHERE type = 'table' AND name = 'run_attempts'`
          ).get());
          if (!hasAttempts) return;
          const columns = new Set(
            db.prepare("PRAGMA table_info(run_attempts)").all().map((row) => row.name)
          );
          if (!columns.has("source_user_seq")) {
            db.exec("ALTER TABLE run_attempts ADD COLUMN source_user_seq INTEGER REFERENCES events(seq)");
          }
          db.exec(`CREATE INDEX IF NOT EXISTS idx_run_attempts_source_user
        ON run_attempts(session_id, source_user_seq)`);
        }
      },
      {
        // A reusable chat can briefly have attempt A still executing while attempt
        // B is accepted (for example, Move to background followed by a new message).
        // The v7 kill table used PRIMARY KEY(session_id), so B could overwrite or
        // clear A's stop before A observed it. Store independent latches per target;
        // session-scoped rows remain only as the legacy/no-active compatibility
        // shape. The old kill_switch mirror is rebuilt from session rows so a v7
        // targeted latch cannot accidentally become a global stop after migration.
        version: 11,
        sql: "",
        backfill: (db) => {
          const hasTable2 = (name) => Boolean(db.prepare(
            `SELECT 1 FROM sqlite_master WHERE type = 'table' AND name = ?`
          ).get(name));
          const hasKillTable = hasTable2("run_kill_requests");
          if (!hasKillTable) return;
          db.exec(`
        CREATE TABLE IF NOT EXISTS kill_switches (
          session_id   TEXT PRIMARY KEY REFERENCES sessions(id) ON DELETE CASCADE,
          requested_at TEXT NOT NULL,
          reason       TEXT
        );
        ALTER TABLE run_kill_requests RENAME TO run_kill_requests_v7;
        CREATE TABLE run_kill_requests (
          session_id   TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
          scope_key    TEXT NOT NULL,
          attempt_id   TEXT REFERENCES run_attempts(attempt_id) ON DELETE CASCADE,
          run_id       TEXT,
          requested_at TEXT NOT NULL,
          reason       TEXT,
          PRIMARY KEY (session_id, scope_key)
        );
        CREATE INDEX idx_run_kill_requests_attempt
          ON run_kill_requests(attempt_id) WHERE attempt_id IS NOT NULL;
        CREATE INDEX idx_run_kill_requests_run
          ON run_kill_requests(session_id, run_id) WHERE run_id IS NOT NULL;

        INSERT INTO run_kill_requests
          (session_id, scope_key, attempt_id, run_id, requested_at, reason)
        SELECT session_id,
               CASE
                 WHEN attempt_id IS NOT NULL THEN 'attempt:' || attempt_id
                 WHEN run_id IS NOT NULL THEN 'run:' || run_id
                 ELSE 'session:*'
               END,
               attempt_id, run_id, requested_at, reason
          FROM run_kill_requests_v7;

        INSERT OR IGNORE INTO run_kill_requests
          (session_id, scope_key, attempt_id, run_id, requested_at, reason)
        SELECT legacy.session_id, 'session:*', NULL, NULL,
               legacy.requested_at, legacy.reason
          FROM kill_switches AS legacy
         WHERE NOT EXISTS (
           SELECT 1 FROM run_kill_requests AS scoped
            WHERE scoped.session_id = legacy.session_id
         );

        DROP TABLE run_kill_requests_v7;
        DELETE FROM kill_switches;
        INSERT INTO kill_switches (session_id, requested_at, reason)
        SELECT session_id, requested_at, reason
          FROM run_kill_requests
         WHERE scope_key = 'session:*';
      `);
          if (hasTable2("sessions") && hasTable2("tool_guardrail_state")) {
            db.exec(`DELETE FROM tool_guardrail_state
          WHERE NOT EXISTS (
            SELECT 1 FROM sessions WHERE sessions.id = tool_guardrail_state.session_id
          )`);
          }
          if (hasTable2("sessions") && hasTable2("pending_approvals")) {
            db.exec(`DELETE FROM pending_approvals
          WHERE NOT EXISTS (
            SELECT 1 FROM sessions WHERE sessions.id = pending_approvals.session_id
          )`);
          }
        }
      },
      {
        // Artifact/resource truth and pre-acknowledgement Stop authority must be
        // present before a turn begins. The artifact ledger originally guarded its
        // tables with lazy CREATE statements; keep that repair path, but move the
        // canonical schema into this numbered migration. Chat cancellation rows
        // intentionally have no session FK because Stop can arrive before the
        // server has accepted the request and created/bound its session receipt.
        version: 12,
        sql: `
      CREATE TABLE IF NOT EXISTS run_artifacts (
        id             TEXT PRIMARY KEY,
        session_id     TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
        run_scope_id   TEXT NOT NULL,
        slot_key       TEXT NOT NULL,
        kind           TEXT NOT NULL,
        provider       TEXT NOT NULL,
        title          TEXT,
        create_shape   TEXT NOT NULL,
        status         TEXT NOT NULL CHECK (status IN ('pending','bound','uncertain')),
        resource_id    TEXT,
        uri            TEXT,
        source_call_id TEXT,
        binding_verified_at TEXT,
        verification_call_id TEXT,
        verification_shape TEXT,
        verification_fingerprint TEXT,
        created_at     TEXT NOT NULL,
        updated_at     TEXT NOT NULL,
        UNIQUE(session_id, run_scope_id, slot_key)
      );
      CREATE INDEX IF NOT EXISTS idx_run_artifacts_session
        ON run_artifacts(session_id, run_scope_id, updated_at DESC);
      CREATE INDEX IF NOT EXISTS idx_run_artifacts_resource
        ON run_artifacts(provider, resource_id);

      CREATE TABLE IF NOT EXISTS artifact_run_scopes (
        session_id       TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
        attempt_scope_id TEXT NOT NULL,
        root_scope_id    TEXT NOT NULL,
        source_user_seq  INTEGER NOT NULL DEFAULT 0,
        reason           TEXT NOT NULL,
        created_at       TEXT NOT NULL,
        PRIMARY KEY(session_id, attempt_scope_id)
      );
      CREATE INDEX IF NOT EXISTS idx_artifact_run_scopes_user
        ON artifact_run_scopes(session_id, source_user_seq DESC, created_at DESC);

      CREATE TABLE IF NOT EXISTS artifact_source_roots (
        session_id      TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
        source_user_seq INTEGER NOT NULL,
        root_scope_id   TEXT NOT NULL,
        created_at      TEXT NOT NULL,
        PRIMARY KEY(session_id, source_user_seq)
      );

      CREATE TABLE IF NOT EXISTS harness_chat_request_cancellations (
        request_id   TEXT PRIMARY KEY,
        requested_at TEXT NOT NULL,
        reason       TEXT
      );
    `,
        backfill: (db) => {
          const columns = new Set(
            db.prepare("PRAGMA table_info(run_artifacts)").all().map((row) => row.name)
          );
          for (const [name, declaration] of [
            ["binding_verified_at", "binding_verified_at TEXT"],
            ["verification_call_id", "verification_call_id TEXT"],
            ["verification_shape", "verification_shape TEXT"],
            ["verification_fingerprint", "verification_fingerprint TEXT"]
          ]) {
            if (!columns.has(name)) db.exec(`ALTER TABLE run_artifacts ADD COLUMN ${declaration}`);
          }
          const hasSessions = Boolean(db.prepare(
            `SELECT 1 FROM sqlite_master WHERE type = 'table' AND name = 'sessions'`
          ).get());
          if (!hasSessions) return;
          db.exec(`
        INSERT OR IGNORE INTO artifact_source_roots
          (session_id, source_user_seq, root_scope_id, created_at)
        SELECT s.session_id, s.source_user_seq, s.root_scope_id, s.created_at
          FROM artifact_run_scopes s
         WHERE s.source_user_seq > 0
           AND EXISTS (
             SELECT 1 FROM sessions owner WHERE owner.id = s.session_id
           )
           AND NOT EXISTS (
             SELECT 1
               FROM artifact_run_scopes earlier
              WHERE earlier.session_id = s.session_id
                AND earlier.source_user_seq = s.source_user_seq
                AND (
                  earlier.created_at < s.created_at
                  OR (earlier.created_at = s.created_at AND earlier.rowid < s.rowid)
                )
           );
      `);
        }
      },
      {
        // session_locks was left as a knowing vestige by the 2026-07-09 subtraction
        // (withSessionLock removed; CREATE kept "inert"). The 2026-07-22 legacy
        // sweep confirmed zero readers/writers remain — close the loop.
        version: 13,
        sql: "DROP TABLE IF EXISTS session_locks;"
      },
      {
        // A fresh human approval may authorize one deliberate duplicate send, but
        // it is not standing permission for unlimited later replays. Keep this
        // consumption independent from the workflow payload-consumption column:
        // the approval gate may consume `consumed_at` immediately before the
        // duplicate wall evaluates the same approved call.
        version: 14,
        sql: "",
        backfill(db) {
          const table = db.prepare(`
        SELECT 1 AS present
          FROM sqlite_master
         WHERE type = 'table' AND name = 'pending_approvals'
      `).get();
          if (!table) return;
          const columns = new Set(
            db.prepare("PRAGMA table_info(pending_approvals)").all().map((column) => column.name)
          );
          if (!columns.has("resend_consumed_at")) {
            db.exec("ALTER TABLE pending_approvals ADD COLUMN resend_consumed_at TEXT");
            columns.add("resend_consumed_at");
          }
          if (["session_id", "resolved_at", "status", "resolution", "resend_consumed_at"].every((column) => columns.has(column))) {
            db.exec(`
          CREATE INDEX IF NOT EXISTS idx_pending_approvals_resend_consent
            ON pending_approvals(session_id, resolved_at DESC)
            WHERE status = 'resolved'
              AND resolution = 'approved'
              AND resend_consumed_at IS NULL
        `);
          }
        }
      },
      {
        // Physical model attempts are cancelable transports, not dispatch
        // authority. A provider can acknowledge cancel and still deliver a late
        // tool call after a retry/recovery has begun. Keep one durable generation
        // per execution scope so every process (including the Claude local-MCP
        // child) can reject work from a superseded generation before bookkeeping
        // or provider dispatch.
        version: 15,
        sql: `
      CREATE TABLE IF NOT EXISTS run_dispatch_leases (
        scope_id       TEXT PRIMARY KEY,
        session_id     TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
        lease_id       TEXT NOT NULL,
        run_attempt_id TEXT REFERENCES run_attempts(attempt_id) ON DELETE CASCADE,
        activated_at   TEXT NOT NULL,
        revoked_at     TEXT
      );
      CREATE INDEX IF NOT EXISTS idx_run_dispatch_leases_session
        ON run_dispatch_leases(session_id, revoked_at);
    `
      },
      {
        // Internal provider retries own a child generation rather than borrowing
        // the caller's shared lease. Persist the exact parent so a parent revoke
        // invalidates every query child across in-process and stdio MCP transports.
        version: 16,
        sql: "",
        backfill: (db) => {
          const table = db.prepare(`
        SELECT 1 AS present
          FROM sqlite_master
         WHERE type = 'table' AND name = 'run_dispatch_leases'
      `).get();
          if (!table) return;
          const columns = new Set(
            db.prepare("PRAGMA table_info(run_dispatch_leases)").all().map((column) => column.name)
          );
          if (!columns.has("parent_scope_id")) {
            db.exec("ALTER TABLE run_dispatch_leases ADD COLUMN parent_scope_id TEXT");
          }
          if (!columns.has("parent_lease_id")) {
            db.exec("ALTER TABLE run_dispatch_leases ADD COLUMN parent_lease_id TEXT");
          }
          db.exec(`
        CREATE INDEX IF NOT EXISTS idx_run_dispatch_leases_parent
          ON run_dispatch_leases(parent_scope_id, parent_lease_id)
      `);
        }
      },
      {
        // One public terminal belongs to one accepted user_input_received event,
        // even across rolling upgrades where an older process still writes the
        // former brain:<attempt> key. A trigger can be installed safely when a
        // historical database already contains duplicate rows (a UNIQUE index
        // cannot); it prevents every future writer, including an old binary, from
        // adding another terminal for an already-settled logical source.
        version: 17,
        sql: "",
        backfill: (db) => {
          const hasEvents = Boolean(db.prepare(
            `SELECT 1 FROM sqlite_master WHERE type = 'table' AND name = 'events'`
          ).get());
          if (!hasEvents) return;
          db.exec(`
        CREATE TRIGGER IF NOT EXISTS trg_events_one_terminal_per_user_source
        BEFORE INSERT ON events
        WHEN NEW.type = 'conversation_completed'
          AND COALESCE(
            json_extract(NEW.data_json, '$.sourceUserSeq'),
            json_extract(NEW.data_json, '$.presentation.identity.sourceUserSeq')
          ) IS NOT NULL
          AND EXISTS (
            SELECT 1
              FROM events AS settled
             WHERE settled.session_id = NEW.session_id
               AND settled.type = 'conversation_completed'
               AND COALESCE(
                 json_extract(settled.data_json, '$.sourceUserSeq'),
                 json_extract(settled.data_json, '$.presentation.identity.sourceUserSeq')
               ) = COALESCE(
                 json_extract(NEW.data_json, '$.sourceUserSeq'),
                 json_extract(NEW.data_json, '$.presentation.identity.sourceUserSeq')
               )
          )
        BEGIN
          SELECT RAISE(ABORT, 'logical terminal source already exists');
        END;
      `);
        }
      },
      {
        // One verified/success settlement per exact pre-dispatch reservation. New
        // writers carry settlementKey; historical rows remain untouched so an
        // additive upgrade never rewrites ambiguous external-effect history.
        version: 18,
        sql: "",
        backfill: (db) => {
          const hasEvents = Boolean(db.prepare(
            `SELECT 1 FROM sqlite_master WHERE type = 'table' AND name = 'events'`
          ).get());
          if (hasEvents) {
            db.exec(`
          CREATE UNIQUE INDEX IF NOT EXISTS idx_events_external_write_settlement_key
            ON events(session_id, type, json_extract(data_json, '$.settlementKey'))
            WHERE type = 'external_write_succeeded'
              AND json_extract(data_json, '$.settlementKey') IS NOT NULL;
        `);
          }
          const hasArtifacts = Boolean(db.prepare(
            `SELECT 1 FROM sqlite_master WHERE type = 'table' AND name = 'run_artifacts'`
          ).get());
          if (!hasArtifacts) return;
          const columns = new Set(
            db.prepare("PRAGMA table_info(run_artifacts)").all().map((column) => column.name)
          );
          for (const [name, declaration] of [
            ["external_write_event_id", "external_write_event_id TEXT"],
            ["external_write_action_key", "external_write_action_key TEXT"],
            ["external_write_tool_name", "external_write_tool_name TEXT"]
          ]) {
            if (!columns.has(name)) db.exec(`ALTER TABLE run_artifacts ADD COLUMN ${declaration}`);
          }
        }
      },
      {
        // Exact settlement/readback bytes need invocation identity stronger than an
        // SDK call id. Keep them in a parallel nonce-keyed store so concurrent or
        // reused call ids cannot overwrite one another, while the legacy call-id
        // recall store retains its backwards-compatible longest-output behavior.
        // v19 is deliberately separate: local canary databases ran an earlier v18
        // while this patch was under review.
        version: 19,
        sql: `
      CREATE TABLE IF NOT EXISTS tool_output_invocations (
        session_id          TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
        call_id             TEXT NOT NULL,
        invocation_nonce    TEXT NOT NULL,
        tool                TEXT,
        output_full         TEXT NOT NULL,
        content_bytes       INTEGER NOT NULL,
        truncated_at_write  INTEGER NOT NULL DEFAULT 0,
        created_at          TEXT NOT NULL,
        PRIMARY KEY (session_id, call_id, invocation_nonce)
      );
      CREATE INDEX IF NOT EXISTS idx_tool_output_invocations_session
        ON tool_output_invocations(session_id, created_at);
    `
      },
      {
        // Authority lookups sit on completion, grounding, artifact verification,
        // and memory-write boundaries.  A long-horizon session can contain tens of
        // thousands of tool events, so resolving one call id must not deserialize
        // the entire session.  Index the durable SDK presentation id directly.
        version: 20,
        sql: "",
        backfill: (db) => {
          const hasEvents = Boolean(db.prepare(
            `SELECT 1 FROM sqlite_master WHERE type = 'table' AND name = 'events'`
          ).get());
          if (!hasEvents) return;
          db.exec(`
        CREATE INDEX IF NOT EXISTS idx_events_tool_lifecycle_call
          ON events(session_id, type, json_extract(data_json, '$.callId'), seq)
          WHERE type IN ('tool_called', 'tool_returned');
      `);
        }
      },
      {
        // Discovery is a per-accepted-source resource, not a per-process courtesy.
        // Persist both policy and the category claim so daemon restarts and
        // concurrent workers cannot silently reset or double-spend its allowance.
        version: 21,
        sql: `
      CREATE TABLE IF NOT EXISTS discovery_governor_tasks (
        session_id        TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
        source_user_seq   INTEGER NOT NULL CHECK (source_user_seq > 0),
        known_capability  INTEGER NOT NULL CHECK (known_capability IN (0, 1)),
        initialized_at    TEXT NOT NULL,
        updated_at        TEXT NOT NULL,
        PRIMARY KEY (session_id, source_user_seq)
      );

      CREATE TABLE IF NOT EXISTS discovery_governor_claims (
        session_id       TEXT NOT NULL,
        source_user_seq  INTEGER NOT NULL,
        category         TEXT NOT NULL
                         CHECK (category IN ('broad_discovery', 'exact_schema_refresh')),
        call_id          TEXT NOT NULL,
        outcome          TEXT NOT NULL DEFAULT 'pending'
                         CHECK (outcome IN ('pending', 'succeeded', 'empty', 'failed', 'timed_out')),
        outcome_detail   TEXT,
        admitted_at      TEXT NOT NULL,
        settled_at       TEXT,
        PRIMARY KEY (session_id, source_user_seq, category),
        FOREIGN KEY (session_id, source_user_seq)
          REFERENCES discovery_governor_tasks(session_id, source_user_seq)
          ON DELETE CASCADE
      );

      CREATE INDEX IF NOT EXISTS idx_discovery_governor_claims_call
        ON discovery_governor_claims(session_id, source_user_seq, call_id);
    `
      },
      {
        // Clem 4 accepted-task evidence authority.
        //
        // Resolution is mutable state (open -> finalized), so unlike telemetry it
        // belongs in normalized rows with database-enforced ownership. Operation
        // admission, the mirror event, and finalization are committed by the domain
        // API in one IMMEDIATE transaction. A process crash therefore cannot leave
        // an event claiming a transition the state machine did not make, or vice
        // versa. Raw provider arguments never enter these tables.
        version: 22,
        sql: `
      -- Existing feature code created this table lazily. Fresh homes get the
      -- complete schema here; the guarded backfill below upgrades partial dev
      -- schemas without pretending an old two-column claim carried evidence.
      CREATE TABLE IF NOT EXISTS obligation_transitions (
        obligation_key       TEXT PRIMARY KEY,
        session_id           TEXT NOT NULL,
        source_user_seq      INTEGER NOT NULL,
        manifest_id          TEXT NOT NULL,
        node_id              TEXT NOT NULL,
        obligation           TEXT NOT NULL,
        receipt_id           TEXT NOT NULL,
        physical_attempt_id  TEXT NOT NULL,
        logical_tool_call_id TEXT,
        physical_dispatch_id TEXT,
        claimed_at           TEXT NOT NULL
      );

      -- Compatibility only. It ceases to be settlement authority in the next
      -- slice, but centralizing its schema prevents another lazy-schema fork.
      CREATE TABLE IF NOT EXISTS settlement_claims (
        settlement_key TEXT PRIMARY KEY,
        session_id     TEXT NOT NULL,
        claimed_at     TEXT NOT NULL
      );
    `,
        backfill: (db) => {
          const hasEvents = Boolean(db.prepare(
            `SELECT 1 FROM sqlite_master WHERE type = 'table' AND name = 'events'`
          ).get());
          if (hasEvents) {
            db.exec(`
          CREATE TABLE IF NOT EXISTS accepted_task_resolutions (
            session_id                TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
            source_user_seq           INTEGER NOT NULL CHECK (source_user_seq > 0),
            accepted_task_id          TEXT NOT NULL UNIQUE,
            graph_event_id            TEXT NOT NULL REFERENCES events(id) ON DELETE RESTRICT,
            graph_id                  TEXT NOT NULL,
            graph_hash                TEXT NOT NULL,
            compiler_version          TEXT NOT NULL,
            route                     TEXT NOT NULL CHECK (route IN ('direct_reply','retrieve','act')),
            work_node_id              TEXT,
            work_kind                 TEXT NOT NULL CHECK (work_kind IN ('conversation','retrieve','execute','fanout')),
            effect_ceiling            TEXT NOT NULL,
            external_effect_requested INTEGER NOT NULL CHECK (external_effect_requested IN (0, 1)),
            external_effect_kinds_json TEXT NOT NULL DEFAULT '[]',
            state                     TEXT NOT NULL DEFAULT 'open'
                                      CHECK (state IN ('open','finalized','legacy_ambiguous')),
            revision                  INTEGER NOT NULL DEFAULT 0 CHECK (revision >= 0),
            operation_count           INTEGER NOT NULL DEFAULT 0 CHECK (operation_count >= 0),
            operations_digest         TEXT,
            expectations_satisfied    INTEGER CHECK (expectations_satisfied IN (0, 1)),
            opened_at                 TEXT NOT NULL,
            finalized_at              TEXT,
            finalize_event_id         TEXT REFERENCES events(id) ON DELETE RESTRICT,
            PRIMARY KEY (session_id, source_user_seq)
          );

          CREATE TABLE IF NOT EXISTS accepted_task_operations (
            session_id             TEXT NOT NULL,
            source_user_seq        INTEGER NOT NULL,
            operation_id           TEXT NOT NULL,
            logical_tool_call_id   TEXT NOT NULL,
            graph_node_id          TEXT NOT NULL,
            resolved_tool          TEXT NOT NULL,
            effect_kind            TEXT NOT NULL
                                   CHECK (effect_kind IN ('read','compute','local_write','external_write','admin','unknown')),
            reversibility          TEXT NOT NULL
                                   CHECK (reversibility IN ('read_only','reversible','irreversible','not_applicable','unknown')),
            effect_source          TEXT NOT NULL,
            argument_keys_json     TEXT NOT NULL DEFAULT '[]',
            argument_digest        TEXT NOT NULL,
            physical_dispatch_id  TEXT,
            outcome_kind           TEXT,
            dispatch_state         TEXT CHECK (dispatch_state IN ('not_started','dispatched')),
            recorded_at            TEXT NOT NULL,
            operation_event_id     TEXT NOT NULL REFERENCES events(id) ON DELETE RESTRICT,
            PRIMARY KEY (session_id, source_user_seq, operation_id),
            UNIQUE (session_id, source_user_seq, logical_tool_call_id),
            FOREIGN KEY (session_id, source_user_seq)
              REFERENCES accepted_task_resolutions(session_id, source_user_seq)
              ON DELETE CASCADE
          );

          CREATE INDEX IF NOT EXISTS idx_accepted_task_operations_effect
            ON accepted_task_operations(session_id, source_user_seq, effect_kind);
        `);
          }
          const table = db.prepare(
            `SELECT 1 FROM sqlite_master WHERE type = 'table' AND name = 'obligation_transitions'`
          ).get();
          if (!table) return;
          const columns = new Set(
            db.prepare("PRAGMA table_info(obligation_transitions)").all().map((row) => row.name)
          );
          const required = [
            "obligation_key",
            "session_id",
            "source_user_seq",
            "manifest_id",
            "node_id",
            "obligation",
            "receipt_id",
            "physical_attempt_id",
            "logical_tool_call_id",
            "physical_dispatch_id",
            "claimed_at"
          ];
          const missing = required.filter((name) => !columns.has(name));
          if (missing.length === 0) {
            db.exec(`CREATE INDEX IF NOT EXISTS idx_obligation_transitions_task
          ON obligation_transitions(session_id, source_user_seq)`);
            return;
          }
          const priorAuthorityColumns = required.filter((name) => name !== "logical_tool_call_id" && name !== "physical_dispatch_id");
          if (priorAuthorityColumns.every((name) => columns.has(name))) {
            if (!columns.has("logical_tool_call_id")) {
              db.exec("ALTER TABLE obligation_transitions ADD COLUMN logical_tool_call_id TEXT");
            }
            if (!columns.has("physical_dispatch_id")) {
              db.exec("ALTER TABLE obligation_transitions ADD COLUMN physical_dispatch_id TEXT");
            }
            db.exec(`CREATE INDEX IF NOT EXISTS idx_obligation_transitions_task
          ON obligation_transitions(session_id, source_user_seq)`);
            return;
          }
          const legacyExists = db.prepare(
            `SELECT 1 FROM sqlite_master WHERE type = 'table' AND name = 'obligation_transitions_legacy_v21'`
          ).get();
          if (!legacyExists) {
            db.exec("ALTER TABLE obligation_transitions RENAME TO obligation_transitions_legacy_v21");
          } else {
            db.exec("DROP TABLE obligation_transitions");
          }
          db.exec(`
        CREATE TABLE obligation_transitions (
          obligation_key       TEXT PRIMARY KEY,
          session_id           TEXT NOT NULL,
          source_user_seq      INTEGER NOT NULL,
          manifest_id          TEXT NOT NULL,
          node_id              TEXT NOT NULL,
          obligation           TEXT NOT NULL,
          receipt_id           TEXT NOT NULL,
          physical_attempt_id  TEXT NOT NULL,
          logical_tool_call_id TEXT,
          physical_dispatch_id TEXT,
          claimed_at           TEXT NOT NULL
        );
        CREATE INDEX IF NOT EXISTS idx_obligation_transitions_task
          ON obligation_transitions(session_id, source_user_seq);
      `);
        }
      },
      {
        // Clem 4 logical-call and paid-crossing authority. Inserting a physical
        // dispatch is the permission to let control leave for a provider, not
        // merely telemetry. Domain APIs mirror events in the same transaction and
        // refuse provider I/O when this state cannot be committed.
        version: 23,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events") || !tables.has("accepted_task_resolutions")) return;
          db.exec(`
        CREATE TABLE IF NOT EXISTS logical_tool_calls (
          session_id           TEXT NOT NULL,
          source_user_seq      INTEGER NOT NULL,
          accepted_task_id     TEXT NOT NULL,
          logical_tool_call_id TEXT NOT NULL,
          tool_name            TEXT NOT NULL,
          argument_digest      TEXT NOT NULL,
          state                TEXT NOT NULL DEFAULT 'open'
                               CHECK (state IN ('open','settled','conflict')),
          opened_at            TEXT NOT NULL,
          settled_at           TEXT,
          settlement_event_id  TEXT REFERENCES events(id) ON DELETE RESTRICT,
          outcome_kind         TEXT,
          PRIMARY KEY (session_id, source_user_seq, logical_tool_call_id),
          FOREIGN KEY (session_id, source_user_seq)
            REFERENCES accepted_task_resolutions(session_id, source_user_seq)
            ON DELETE CASCADE
        );

        CREATE TABLE IF NOT EXISTS physical_dispatches (
          session_id           TEXT NOT NULL,
          source_user_seq      INTEGER NOT NULL,
          accepted_task_id     TEXT NOT NULL,
          logical_tool_call_id TEXT NOT NULL,
          physical_dispatch_id TEXT NOT NULL,
          ordinal              INTEGER NOT NULL CHECK (ordinal > 0),
          relation             TEXT NOT NULL
                               CHECK (relation IN ('primary','retry','poll','probe','child')),
          retry_of             TEXT,
          tool_name            TEXT NOT NULL,
          argument_digest      TEXT NOT NULL,
          state                TEXT NOT NULL DEFAULT 'started'
                               CHECK (state IN ('started','returned','threw','timed_out','cancelled','unknown')),
          started_at           TEXT NOT NULL,
          settled_at           TEXT,
          start_event_id       TEXT NOT NULL REFERENCES events(id) ON DELETE RESTRICT,
          settle_event_id      TEXT REFERENCES events(id) ON DELETE RESTRICT,
          PRIMARY KEY (session_id, source_user_seq, physical_dispatch_id),
          UNIQUE (session_id, source_user_seq, logical_tool_call_id, ordinal),
          FOREIGN KEY (session_id, source_user_seq, logical_tool_call_id)
            REFERENCES logical_tool_calls(session_id, source_user_seq, logical_tool_call_id)
            ON DELETE CASCADE
        );

        CREATE INDEX IF NOT EXISTS idx_physical_dispatches_open
          ON physical_dispatches(session_id, source_user_seq, state);
        CREATE INDEX IF NOT EXISTS idx_physical_dispatches_logical
          ON physical_dispatches(session_id, source_user_seq, logical_tool_call_id, ordinal);
      `);
        }
      },
      {
        // Clem 4 atomic logical-call settlement.  Migration 23 established paid
        // crossing admission, but left logical settlement as a separate legacy
        // claim plus a best-effort event.  These normalized rows let one domain
        // transaction freeze the exact semantic result and every paid crossing,
        // mirror it once, and close the logical call by CAS.
        //
        // Do not promote `settlement_claims`: those rows carry neither accepted
        // source, result, contract nor crossing evidence and cannot be upgraded
        // honestly.  They remain forensic compatibility data until cutover.
        version: 24,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) return;
          for (const prerequisite of [
            "accepted_task_resolutions",
            "logical_tool_calls",
            "physical_dispatches"
          ]) {
            if (!tables.has(prerequisite)) {
              throw new Error(`schema v24 prerequisite missing: ${prerequisite}`);
            }
          }
          db.exec(`
        CREATE UNIQUE INDEX IF NOT EXISTS uq_physical_dispatch_exact_parent
          ON physical_dispatches(
            session_id, source_user_seq, logical_tool_call_id, physical_dispatch_id
          );

        CREATE UNIQUE INDEX IF NOT EXISTS uq_logical_call_settlement_event
          ON logical_tool_calls(settlement_event_id)
          WHERE settlement_event_id IS NOT NULL;

        CREATE TABLE IF NOT EXISTS logical_call_settlements (
          session_id                TEXT NOT NULL,
          source_user_seq           INTEGER NOT NULL CHECK (source_user_seq > 0),
          logical_tool_call_id      TEXT NOT NULL,
          protocol_version          INTEGER NOT NULL CHECK (protocol_version = 1),
          semantic_digest           TEXT NOT NULL CHECK (length(semantic_digest) = 64),
          execution_kind            TEXT NOT NULL
                                    CHECK (execution_kind IN (
                                      'refused_pre_dispatch',
                                      'local_execution',
                                      'provider_execution'
                                    )),
          outcome_kind              TEXT NOT NULL
                                    CHECK (outcome_kind IN (
                                      'succeeded','invalid_arguments','transient',
                                      'unsupported_capability','ignored_requirement',
                                      'input_required','auth_failure','policy_denial',
                                      'uncertain_write','empty_result','unknown'
                                    )),
          outcome_evidence          TEXT NOT NULL
                                    CHECK (outcome_evidence IN ('nominal','structured','text')),
          provider_status           TEXT CHECK (provider_status IS NULL OR length(provider_status) <= 64),
          outcome_detail            TEXT CHECK (outcome_detail IS NULL OR length(outcome_detail) <= 160),
          business_call             INTEGER NOT NULL CHECK (business_call IN (0, 1)),
          mutating                  INTEGER NOT NULL CHECK (mutating IN (0, 1)),
          requirement_id            TEXT CHECK (requirement_id IS NULL OR length(requirement_id) <= 256),
          continues_requirement     INTEGER NOT NULL CHECK (continues_requirement IN (0, 1)),
          recovery_action           TEXT NOT NULL
                                    CHECK (recovery_action IN (
                                      'settle','repair_arguments','retry_with_backoff',
                                      'try_sibling_candidate','ask_user','recover_connection',
                                      'stop_and_explain','reconcile_then_decide'
                                    )),
          retry_same_candidate      INTEGER NOT NULL CHECK (retry_same_candidate IN (0, 1)),
          eliminates_candidate      INTEGER NOT NULL CHECK (eliminates_candidate IN (0, 1)),
          discovery_epoch_requested INTEGER NOT NULL CHECK (discovery_epoch_requested IN (0, 1)),
          requires_reconciliation   INTEGER NOT NULL CHECK (requires_reconciliation IN (0, 1)),
          progress_key_digest       TEXT CHECK (
                                      progress_key_digest IS NULL OR length(progress_key_digest) = 64
                                    ),
          progress_claimed          INTEGER NOT NULL CHECK (progress_claimed IN (0, 1)),
          physical_crossing_count   INTEGER NOT NULL CHECK (physical_crossing_count >= 0),
          physical_crossings_digest TEXT NOT NULL CHECK (length(physical_crossings_digest) = 64),
          observer_lane             TEXT NOT NULL
                                    CHECK (observer_lane IN (
                                      'agents_runner','native_mcp','claude_sdk',
                                      'composio','code_mode','byo'
                                    )),
          observer_call_id          TEXT CHECK (observer_call_id IS NULL OR length(observer_call_id) <= 256),
          settlement_event_id       TEXT NOT NULL UNIQUE
                                    REFERENCES events(id) ON DELETE RESTRICT,
          settled_at                TEXT NOT NULL,
          PRIMARY KEY (session_id, source_user_seq, logical_tool_call_id),
          FOREIGN KEY (session_id, source_user_seq, logical_tool_call_id)
            REFERENCES logical_tool_calls(
              session_id, source_user_seq, logical_tool_call_id
            ) ON DELETE CASCADE,
          CHECK (
            (execution_kind IN ('refused_pre_dispatch','local_execution')
              AND physical_crossing_count = 0)
            OR
            (execution_kind = 'provider_execution'
              AND physical_crossing_count > 0)
          ),
          CHECK (
            execution_kind != 'refused_pre_dispatch'
            OR outcome_kind NOT IN ('succeeded','empty_result','uncertain_write')
          ),
          CHECK (progress_claimed = 0 OR progress_key_digest IS NOT NULL)
        );

        CREATE TABLE IF NOT EXISTS logical_call_settlement_crossings (
          session_id            TEXT NOT NULL,
          source_user_seq       INTEGER NOT NULL,
          logical_tool_call_id  TEXT NOT NULL,
          physical_dispatch_id  TEXT NOT NULL,
          ordinal               INTEGER NOT NULL CHECK (ordinal > 0),
          relation              TEXT NOT NULL
                                CHECK (relation IN ('primary','retry','poll','probe','child')),
          retry_of              TEXT,
          tool_name             TEXT NOT NULL,
          argument_digest       TEXT NOT NULL CHECK (length(argument_digest) = 64),
          PRIMARY KEY (
            session_id, source_user_seq,
            logical_tool_call_id, physical_dispatch_id
          ),
          UNIQUE (session_id, source_user_seq, logical_tool_call_id, ordinal),
          FOREIGN KEY (session_id, source_user_seq, logical_tool_call_id)
            REFERENCES logical_call_settlements(
              session_id, source_user_seq, logical_tool_call_id
            ) ON DELETE CASCADE,
          FOREIGN KEY (
            session_id, source_user_seq,
            logical_tool_call_id, physical_dispatch_id
          ) REFERENCES physical_dispatches(
            session_id, source_user_seq,
            logical_tool_call_id, physical_dispatch_id
          ) ON DELETE RESTRICT
        );

        CREATE TABLE IF NOT EXISTS logical_call_progress_claims (
          session_id           TEXT NOT NULL,
          source_user_seq      INTEGER NOT NULL,
          progress_key_digest TEXT NOT NULL CHECK (length(progress_key_digest) = 64),
          logical_tool_call_id TEXT NOT NULL,
          claimed_at           TEXT NOT NULL,
          PRIMARY KEY (session_id, source_user_seq, progress_key_digest),
          FOREIGN KEY (session_id, source_user_seq, logical_tool_call_id)
            REFERENCES logical_call_settlements(
              session_id, source_user_seq, logical_tool_call_id
            ) ON DELETE CASCADE
        );

        CREATE TRIGGER IF NOT EXISTS trg_physical_dispatch_requires_open_logical
        BEFORE INSERT ON physical_dispatches
        WHEN NOT EXISTS (
          SELECT 1 FROM logical_tool_calls
           WHERE session_id = NEW.session_id
             AND source_user_seq = NEW.source_user_seq
             AND logical_tool_call_id = NEW.logical_tool_call_id
             AND accepted_task_id = NEW.accepted_task_id
             AND state = 'open'
        )
        BEGIN
          SELECT RAISE(ABORT, 'physical dispatch requires its exact open logical parent');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_physical_dispatch_identity_immutable
        BEFORE UPDATE OF accepted_task_id, logical_tool_call_id,
                         physical_dispatch_id, ordinal, relation, retry_of,
                         tool_name, argument_digest
        ON physical_dispatches
        WHEN OLD.accepted_task_id IS NOT NEW.accepted_task_id
          OR OLD.logical_tool_call_id IS NOT NEW.logical_tool_call_id
          OR OLD.physical_dispatch_id IS NOT NEW.physical_dispatch_id
          OR OLD.ordinal IS NOT NEW.ordinal
          OR OLD.relation IS NOT NEW.relation
          OR OLD.retry_of IS NOT NEW.retry_of
          OR OLD.tool_name IS NOT NEW.tool_name
          OR OLD.argument_digest IS NOT NEW.argument_digest
        BEGIN
          SELECT RAISE(ABORT, 'physical dispatch identity is immutable');
        END;
      `);
        }
      },
      {
        // Settlement is also the recovery linearization point. Keep the governor
        // decision beside the normalized call so a crash cannot durably settle a
        // failed candidate while losing the discovery epoch that makes recovery
        // possible (or credit a step without its one task-scoped progress claim).
        version: 25,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) return;
          if (!tables.has("logical_call_settlements")) {
            throw new Error("schema v25 prerequisite missing: logical_call_settlements");
          }
          const columns = new Set(
            db.prepare("PRAGMA table_info(logical_call_settlements)").all().map((row) => row.name)
          );
          if (!columns.has("governor_evidence_kind")) {
            db.exec(`
          ALTER TABLE logical_call_settlements ADD COLUMN governor_evidence_kind TEXT
            CHECK (governor_evidence_kind IS NULL OR governor_evidence_kind IN (
              'candidate_unsupported','candidate_unavailable','catalog_revision_changed',
              'auth_recovered','capability_satisfied','user_input_provided'
            ));
          ALTER TABLE logical_call_settlements ADD COLUMN governor_evidence_detail TEXT;
          ALTER TABLE logical_call_settlements ADD COLUMN governor_requires_progress INTEGER NOT NULL DEFAULT 0
            CHECK (governor_requires_progress IN (0, 1));
          ALTER TABLE logical_call_settlements ADD COLUMN governor_outcome TEXT
            CHECK (governor_outcome IS NULL OR governor_outcome IN (
              'epoch_opened','epoch_already_fresh','epoch_ceiling_reached','task_not_initialized'
            ));
          ALTER TABLE logical_call_settlements ADD COLUMN opened_discovery_epoch INTEGER NOT NULL DEFAULT 0
            CHECK (opened_discovery_epoch IN (0, 1));
          ALTER TABLE logical_call_settlements ADD COLUMN credited_progress INTEGER NOT NULL DEFAULT 0
            CHECK (credited_progress IN (0, 1));
        `);
          }
        }
      },
      {
        // Per-accepted-source cutover state. Presence is not inferred from a
        // manifest: a source is explicitly armed before provider work, so losing a
        // later manifest can never fall through to legacy success. Manifest freeze,
        // repair grants, and terminal publication advance this row by CAS.
        version: 26,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) return;
          db.exec(`
        CREATE TABLE IF NOT EXISTS accepted_task_authority (
          session_id             TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
          source_user_seq        INTEGER NOT NULL CHECK (source_user_seq > 0),
          accepted_task_id       TEXT NOT NULL UNIQUE,
          authority_protocol     INTEGER NOT NULL CHECK (authority_protocol = 1),
          graph_event_id         TEXT NOT NULL REFERENCES events(id) ON DELETE RESTRICT,
          graph_id               TEXT NOT NULL,
          graph_hash             TEXT NOT NULL,
          state                  TEXT NOT NULL DEFAULT 'armed'
                                 CHECK (state IN ('armed','manifested_verifying','terminal','conflict')),
          manifest_id            TEXT,
          revision               INTEGER NOT NULL DEFAULT 0 CHECK (revision >= 0),
          repair_grants_used     INTEGER NOT NULL DEFAULT 0 CHECK (repair_grants_used BETWEEN 0 AND 1),
          repair_grant_id        TEXT,
          repair_grant_status    TEXT NOT NULL DEFAULT 'none'
                                 CHECK (repair_grant_status IN ('none','issued','consumed')),
          repair_grant_issued_at TEXT,
          repair_grant_consumed_at TEXT,
          terminal_event_id      TEXT REFERENCES events(id) ON DELETE RESTRICT,
          backstop_event_id      TEXT REFERENCES events(id) ON DELETE RESTRICT,
          armed_at               TEXT NOT NULL,
          updated_at             TEXT NOT NULL,
          PRIMARY KEY (session_id, source_user_seq),
          CHECK (
            (repair_grant_status = 'none' AND repair_grant_id IS NULL AND repair_grants_used = 0)
            OR
            (repair_grant_status IN ('issued','consumed') AND repair_grant_id IS NOT NULL AND repair_grants_used = 1)
          ),
          CHECK (state = 'armed' OR state = 'conflict' OR manifest_id IS NOT NULL)
        );

        CREATE INDEX IF NOT EXISTS idx_accepted_task_authority_state
          ON accepted_task_authority(state, updated_at);

        CREATE TRIGGER IF NOT EXISTS trg_accepted_task_authority_identity_immutable
        BEFORE UPDATE ON accepted_task_authority
        WHEN OLD.session_id IS NOT NEW.session_id
          OR OLD.source_user_seq IS NOT NEW.source_user_seq
          OR OLD.accepted_task_id IS NOT NEW.accepted_task_id
          OR OLD.authority_protocol IS NOT NEW.authority_protocol
          OR OLD.graph_event_id IS NOT NEW.graph_event_id
          OR OLD.graph_id IS NOT NEW.graph_id
          OR OLD.graph_hash IS NOT NEW.graph_hash
        BEGIN
          SELECT RAISE(ABORT, 'accepted task authority identity is immutable');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_accepted_task_authority_state_machine
        BEFORE UPDATE OF state ON accepted_task_authority
        WHEN OLD.state IS NOT NEW.state AND NOT (
          (OLD.state = 'armed' AND NEW.state IN ('manifested_verifying','conflict'))
          OR (OLD.state = 'manifested_verifying' AND NEW.state IN ('terminal','conflict'))
        )
        BEGIN
          SELECT RAISE(ABORT, 'invalid accepted task authority transition');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_accepted_task_authority_grant_monotonic
        BEFORE UPDATE OF repair_grants_used ON accepted_task_authority
        WHEN NEW.repair_grants_used < OLD.repair_grants_used
          OR NEW.repair_grants_used > OLD.repair_grants_used + 1
        BEGIN
          SELECT RAISE(ABORT, 'terminal repair grant count is monotonic');
        END;
      `);
        }
      },
      {
        // Durable result authority. Provider payloads and opaque continuations used
        // to live in process-local Maps, so a restart made an apparently valid
        // handle irredeemable and removed the only host-side copy of its cursor.
        // Authoritative rows are tied to the exact accepted source, logical call,
        // physical crossing and canonical base call. A legacy-unscoped mode exists
        // only for projection helpers that have not entered a dispatch boundary;
        // those rows can never satisfy task-scoped redemption.
        version: 27,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) return;
          for (const prerequisite of [
            "accepted_task_resolutions",
            "logical_tool_calls",
            "physical_dispatches"
          ]) {
            if (!tables.has(prerequisite)) {
              throw new Error(`schema v27 prerequisite missing: ${prerequisite}`);
            }
          }
          db.exec(`
        CREATE TABLE IF NOT EXISTS durable_result_handles (
          handle_id              TEXT PRIMARY KEY,
          scope_kind             TEXT NOT NULL
                                 CHECK (scope_kind IN ('authoritative','legacy_unscoped')),
          session_id             TEXT,
          source_user_seq        INTEGER,
          accepted_task_id       TEXT,
          logical_tool_call_id   TEXT,
          physical_dispatch_id   TEXT,
          continuation_chain_id  TEXT NOT NULL
                                 CHECK (length(continuation_chain_id) BETWEEN 1 AND 256),
          tool_name              TEXT NOT NULL,
          argument_digest        TEXT NOT NULL CHECK (length(argument_digest) = 64),
          base_argument_digest   TEXT NOT NULL CHECK (length(base_argument_digest) = 64),
          raw_location           TEXT UNIQUE,
          raw_payload_json       TEXT,
          raw_payload_sha256     TEXT CHECK (
                                   raw_payload_sha256 IS NULL OR length(raw_payload_sha256) = 64
                                 ),
          raw_byte_count         INTEGER NOT NULL CHECK (raw_byte_count >= 0),
          rejection_reason       TEXT CHECK (rejection_reason IN (
                                   'unserializable','oversized','cursor_oversized','raw_store_skipped'
                                 )),
          success                INTEGER NOT NULL CHECK (success IN (0, 1)),
          record_path            TEXT,
          record_count           INTEGER NOT NULL CHECK (record_count >= 0),
          envelope_meta_json     TEXT,
          completeness           TEXT NOT NULL CHECK (completeness IN ('complete','partial','unknown')),
          projected_records_json TEXT NOT NULL,
          status_code            INTEGER,
          continuation_ref       TEXT UNIQUE,
          cursor_bytes           BLOB,
          cursor_sha256          TEXT CHECK (cursor_sha256 IS NULL OR length(cursor_sha256) = 64),
          cursor_repeated        INTEGER NOT NULL DEFAULT 0 CHECK (cursor_repeated IN (0, 1)),
          created_at             TEXT NOT NULL,
          FOREIGN KEY (session_id, source_user_seq, logical_tool_call_id, physical_dispatch_id)
            REFERENCES physical_dispatches(
              session_id, source_user_seq, logical_tool_call_id, physical_dispatch_id
            ) ON DELETE CASCADE,
          CHECK (
            (scope_kind = 'authoritative'
              AND session_id IS NOT NULL
              AND source_user_seq IS NOT NULL AND source_user_seq > 0
              AND accepted_task_id IS NOT NULL
              AND logical_tool_call_id IS NOT NULL
              AND physical_dispatch_id IS NOT NULL)
            OR
            (scope_kind = 'legacy_unscoped'
              AND session_id IS NULL
              AND source_user_seq IS NULL
              AND accepted_task_id IS NULL
              AND logical_tool_call_id IS NULL
              AND physical_dispatch_id IS NULL)
          ),
          CHECK (
            (raw_payload_json IS NOT NULL
              AND raw_location IS NOT NULL
              AND raw_payload_sha256 IS NOT NULL
              AND rejection_reason IS NULL)
            OR
            (raw_payload_json IS NULL
              AND raw_location IS NULL
              AND rejection_reason IS NOT NULL)
          ),
          CHECK (
            (continuation_ref IS NULL AND cursor_bytes IS NULL AND cursor_sha256 IS NULL
              AND cursor_repeated = 0)
            OR
            (continuation_ref IS NOT NULL AND cursor_bytes IS NOT NULL
              AND cursor_sha256 IS NOT NULL)
          )
        );

        CREATE UNIQUE INDEX IF NOT EXISTS uq_durable_result_physical
          ON durable_result_handles(
            session_id, source_user_seq, logical_tool_call_id, physical_dispatch_id
          )
          WHERE scope_kind = 'authoritative';

        CREATE INDEX IF NOT EXISTS idx_durable_result_cursor_history
          ON durable_result_handles(
            session_id, source_user_seq, continuation_chain_id,
            base_argument_digest, cursor_sha256, created_at
          )
          WHERE cursor_sha256 IS NOT NULL;

        CREATE TRIGGER IF NOT EXISTS trg_durable_result_exact_authority
        BEFORE INSERT ON durable_result_handles
        WHEN NEW.scope_kind = 'authoritative' AND NOT EXISTS (
          SELECT 1
            FROM physical_dispatches p
           WHERE p.session_id = NEW.session_id
             AND p.source_user_seq = NEW.source_user_seq
             AND p.accepted_task_id = NEW.accepted_task_id
             AND p.logical_tool_call_id = NEW.logical_tool_call_id
             AND p.physical_dispatch_id = NEW.physical_dispatch_id
             AND p.tool_name = NEW.tool_name
             AND p.argument_digest = NEW.argument_digest
             AND p.state = 'returned'
        )
        BEGIN
          SELECT RAISE(ABORT, 'durable result requires its exact returned physical crossing');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_durable_result_base_immutable
        BEFORE INSERT ON durable_result_handles
        WHEN NEW.scope_kind = 'authoritative' AND EXISTS (
          SELECT 1
            FROM durable_result_handles h
           WHERE h.scope_kind = 'authoritative'
             AND h.session_id = NEW.session_id
             AND h.source_user_seq = NEW.source_user_seq
             AND h.continuation_chain_id = NEW.continuation_chain_id
             AND (h.tool_name IS NOT NEW.tool_name
               OR h.base_argument_digest IS NOT NEW.base_argument_digest)
        )
        BEGIN
          SELECT RAISE(ABORT, 'durable result base call is immutable per logical call');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_durable_result_identity_immutable
        BEFORE UPDATE ON durable_result_handles
        BEGIN
          SELECT RAISE(ABORT, 'durable result handles are immutable');
        END;
      `);
        }
      },
      {
        // A logical invocation enters before policy and resolver gates with the
        // exact model/carrier arguments, but trusted host resolution may replace
        // references, remove routing-only metadata, or materialize strict nullable
        // fields before provider I/O. Preserve both value-opaque digests and allow
        // one monotonic raw -> effective transition before the first crossing.
        version: 28,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) return;
          for (const prerequisite of [
            "accepted_task_resolutions",
            "logical_tool_calls",
            "physical_dispatches",
            "logical_call_settlements"
          ]) {
            if (!tables.has(prerequisite)) {
              throw new Error(`schema v28 prerequisite missing: ${prerequisite}`);
            }
          }
          const columns = new Set(
            db.prepare("PRAGMA table_info(logical_tool_calls)").all().map((column) => column.name)
          );
          if (!columns.has("raw_argument_digest")) {
            db.exec("ALTER TABLE logical_tool_calls ADD COLUMN raw_argument_digest TEXT");
          }
          if (!columns.has("effective_argument_digest")) {
            db.exec("ALTER TABLE logical_tool_calls ADD COLUMN effective_argument_digest TEXT");
          }
          if (!columns.has("refined_at")) {
            db.exec("ALTER TABLE logical_tool_calls ADD COLUMN refined_at TEXT");
          }
          if (!columns.has("refinement_event_id")) {
            db.exec(`ALTER TABLE logical_tool_calls ADD COLUMN refinement_event_id TEXT
          REFERENCES events(id) ON DELETE RESTRICT`);
          }
          db.exec(`
        UPDATE logical_tool_calls
           SET raw_argument_digest = argument_digest
         WHERE raw_argument_digest IS NULL;

        CREATE UNIQUE INDEX IF NOT EXISTS uq_logical_call_refinement_event
          ON logical_tool_calls(refinement_event_id)
          WHERE refinement_event_id IS NOT NULL;

        CREATE TRIGGER IF NOT EXISTS trg_logical_call_contract_insert_valid
        BEFORE INSERT ON logical_tool_calls
        WHEN NEW.raw_argument_digest IS NULL
          OR length(NEW.raw_argument_digest) != 64
          OR NEW.argument_digest IS NOT NEW.raw_argument_digest
          OR NEW.effective_argument_digest IS NOT NULL
          OR NEW.refined_at IS NOT NULL
          OR NEW.refinement_event_id IS NOT NULL
        BEGIN
          SELECT RAISE(ABORT, 'new logical call requires one exact raw contract');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_logical_call_contract_identity_immutable
        BEFORE UPDATE OF accepted_task_id, logical_tool_call_id, tool_name, raw_argument_digest
        ON logical_tool_calls
        WHEN OLD.accepted_task_id IS NOT NEW.accepted_task_id
          OR OLD.logical_tool_call_id IS NOT NEW.logical_tool_call_id
          OR OLD.tool_name IS NOT NEW.tool_name
          OR OLD.raw_argument_digest IS NOT NEW.raw_argument_digest
        BEGIN
          SELECT RAISE(ABORT, 'logical call raw contract identity is immutable');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_logical_call_contract_refinement_once
        BEFORE UPDATE OF argument_digest, effective_argument_digest,
                         refined_at, refinement_event_id
        ON logical_tool_calls
        WHEN NOT (
          OLD.state = 'open'
          AND NEW.state = 'open'
          AND OLD.argument_digest = OLD.raw_argument_digest
          AND OLD.effective_argument_digest IS NULL
          AND OLD.refined_at IS NULL
          AND OLD.refinement_event_id IS NULL
          AND NEW.raw_argument_digest = OLD.raw_argument_digest
          AND NEW.effective_argument_digest IS NOT NULL
          AND length(NEW.effective_argument_digest) = 64
          AND NEW.argument_digest = NEW.effective_argument_digest
          AND NEW.argument_digest != NEW.raw_argument_digest
          AND NEW.refined_at IS NOT NULL
          AND NEW.refinement_event_id IS NOT NULL
          AND EXISTS (
            SELECT 1 FROM accepted_task_resolutions r
             WHERE r.session_id = OLD.session_id
               AND r.source_user_seq = OLD.source_user_seq
               AND r.accepted_task_id = OLD.accepted_task_id
               AND r.state = 'open'
          )
          AND NOT EXISTS (
            SELECT 1 FROM physical_dispatches p
             WHERE p.session_id = OLD.session_id
               AND p.source_user_seq = OLD.source_user_seq
               AND p.logical_tool_call_id = OLD.logical_tool_call_id
          )
          AND NOT EXISTS (
            SELECT 1 FROM logical_call_settlements s
             WHERE s.session_id = OLD.session_id
               AND s.source_user_seq = OLD.source_user_seq
               AND s.logical_tool_call_id = OLD.logical_tool_call_id
          )
        )
        BEGIN
          SELECT RAISE(ABORT, 'logical call contract refinement is not monotonic');
        END;
      `);
        }
      },
      {
        // Immutable expected-work authority for one exact accepted source.
        //
        // This is deliberately separate from accepted_task_operations: expected
        // work is fixed before business dispatch, while observed calls may only
        // discharge it later.  The nullable marker on accepted_task_authority is a
        // staged cutover — direct/retrieve turns can bind now, while action turns
        // remain on the existing runtime until the bounded planner seam exists.
        version: 29,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) return;
          if (!tables.has("accepted_task_authority")) {
            throw new Error("schema v29 prerequisite missing: accepted_task_authority");
          }
          const authorityColumns = new Set(
            db.prepare("PRAGMA table_info(accepted_task_authority)").all().map((column) => column.name)
          );
          if (!authorityColumns.has("work_contract_id")) {
            db.exec("ALTER TABLE accepted_task_authority ADD COLUMN work_contract_id TEXT");
          }
          db.exec(`
        CREATE TABLE IF NOT EXISTS accepted_task_work_contracts (
          session_id         TEXT NOT NULL,
          source_user_seq    INTEGER NOT NULL CHECK (source_user_seq > 0),
          accepted_task_id   TEXT NOT NULL UNIQUE,
          contract_version   INTEGER NOT NULL CHECK (contract_version = 1),
          contract_id        TEXT NOT NULL UNIQUE CHECK (length(contract_id) = 81),
          graph_event_id     TEXT NOT NULL REFERENCES events(id) ON DELETE RESTRICT,
          graph_id           TEXT NOT NULL,
          graph_hash         TEXT NOT NULL CHECK (length(graph_hash) = 64),
          planner_source     TEXT NOT NULL
                             CHECK (planner_source IN ('deterministic','structured_model')),
          contract_json      TEXT NOT NULL,
          operation_count    INTEGER NOT NULL CHECK (operation_count BETWEEN 0 AND 32),
          universe_count     INTEGER NOT NULL CHECK (universe_count BETWEEN 0 AND 16),
          fixed_at           TEXT NOT NULL,
          PRIMARY KEY (session_id, source_user_seq),
          FOREIGN KEY (session_id, source_user_seq)
            REFERENCES accepted_task_authority(session_id, source_user_seq)
            ON DELETE CASCADE
        );

        CREATE TRIGGER IF NOT EXISTS trg_accepted_task_work_contract_exact_authority
        BEFORE INSERT ON accepted_task_work_contracts
        WHEN NOT EXISTS (
          SELECT 1 FROM accepted_task_authority a
           WHERE a.session_id = NEW.session_id
             AND a.source_user_seq = NEW.source_user_seq
             AND a.accepted_task_id = NEW.accepted_task_id
             AND a.graph_event_id = NEW.graph_event_id
             AND a.graph_id = NEW.graph_id
             AND a.graph_hash = NEW.graph_hash
             AND a.state != 'conflict'
        )
        BEGIN
          SELECT RAISE(ABORT, 'expected-work contract requires its exact accepted authority');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_accepted_task_work_contracts_update_immutable
        BEFORE UPDATE ON accepted_task_work_contracts
        BEGIN
          SELECT RAISE(ABORT, 'accepted task work contracts are immutable');
        END;

        -- Do not block DELETE here: session retention owns parent cascades.
        -- A standalone deletion leaves the authority's immutable contract id
        -- behind, so rehydration fails closed and no replacement can bind.

        CREATE TRIGGER IF NOT EXISTS trg_accepted_task_authority_contract_binding
        BEFORE UPDATE OF work_contract_id ON accepted_task_authority
        WHEN (
          OLD.work_contract_id IS NOT NULL
          AND OLD.work_contract_id IS NOT NEW.work_contract_id
        ) OR (
          NEW.work_contract_id IS NOT NULL
          AND NOT EXISTS (
            SELECT 1 FROM accepted_task_work_contracts c
             WHERE c.session_id = NEW.session_id
               AND c.source_user_seq = NEW.source_user_seq
               AND c.accepted_task_id = NEW.accepted_task_id
               AND c.graph_event_id = NEW.graph_event_id
               AND c.graph_id = NEW.graph_id
               AND c.graph_hash = NEW.graph_hash
               AND c.contract_id = NEW.work_contract_id
          )
        )
        BEGIN
          SELECT RAISE(ABORT, 'accepted task work-contract binding is invalid or immutable');
        END;
      `);
        }
      },
      {
        // Host-issued evidence authority. A successful result becomes evidence
        // only when the logical settlement names the handle in the same commit;
        // finding a handle later beside a returned crossing is not proof that it
        // was the result which closed the call. Read receipts then bind that exact
        // settlement result to one manifest node and one declared obligation.
        version: 30,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) return;
          for (const prerequisite of [
            "accepted_task_authority",
            "logical_tool_calls",
            "physical_dispatches",
            "logical_call_settlements",
            "logical_call_settlement_crossings",
            "durable_result_handles",
            "obligation_transitions"
          ]) {
            if (!tables.has(prerequisite)) {
              throw new Error(`schema v30 prerequisite missing: ${prerequisite}`);
            }
          }
          const settlementColumns = new Set(
            db.prepare("PRAGMA table_info(logical_call_settlements)").all().map((column) => column.name)
          );
          if (!settlementColumns.has("result_handle_id")) {
            db.exec(`ALTER TABLE logical_call_settlements ADD COLUMN result_handle_id TEXT
          REFERENCES durable_result_handles(handle_id) ON DELETE RESTRICT`);
          }
          db.exec(`
        UPDATE logical_call_settlements AS s
           SET result_handle_id = (
             SELECT h.handle_id
               FROM events e
               JOIN durable_result_handles h
                 ON h.handle_id = json_extract(e.data_json, '$.resultHandleId')
               JOIN logical_tool_calls l
                 ON l.session_id = s.session_id
                AND l.source_user_seq = s.source_user_seq
                AND l.logical_tool_call_id = s.logical_tool_call_id
               JOIN physical_dispatches p
                 ON p.session_id = h.session_id
                AND p.source_user_seq = h.source_user_seq
                AND p.logical_tool_call_id = h.logical_tool_call_id
                AND p.physical_dispatch_id = h.physical_dispatch_id
              WHERE e.id = s.settlement_event_id
                AND e.session_id = s.session_id
                AND e.type = 'tool_attempt_settled'
                AND json_extract(e.data_json, '$.sourceUserSeq') = s.source_user_seq
                AND json_extract(e.data_json, '$.acceptedTaskId') = l.accepted_task_id
                AND json_extract(e.data_json, '$.logicalToolCallId') = s.logical_tool_call_id
                AND json_extract(e.data_json, '$.physicalDispatchId') = h.physical_dispatch_id
                AND h.scope_kind = 'authoritative'
                AND h.session_id = s.session_id
                AND h.source_user_seq = s.source_user_seq
                AND h.accepted_task_id = l.accepted_task_id
                AND h.logical_tool_call_id = s.logical_tool_call_id
                AND h.tool_name = l.tool_name
                AND h.argument_digest = l.argument_digest
                AND h.success = 1
                AND p.state = 'returned'
                AND p.ordinal = (
                  SELECT MAX(p2.ordinal) FROM physical_dispatches p2
                   WHERE p2.session_id = s.session_id
                     AND p2.source_user_seq = s.source_user_seq
                     AND p2.logical_tool_call_id = s.logical_tool_call_id
                )
              LIMIT 1
           )
         WHERE s.result_handle_id IS NULL
           AND s.execution_kind = 'provider_execution'
           AND s.outcome_kind IN ('succeeded','empty_result')
           AND EXISTS (
             SELECT 1 FROM events e
              WHERE e.id = s.settlement_event_id
                AND json_type(e.data_json, '$.resultHandleId') = 'text'
           );

        CREATE INDEX IF NOT EXISTS idx_logical_settlement_result_handle
          ON logical_call_settlements(result_handle_id)
          WHERE result_handle_id IS NOT NULL;

        CREATE TRIGGER IF NOT EXISTS trg_logical_settlement_result_required
        BEFORE INSERT ON logical_call_settlements
        WHEN (
          NEW.execution_kind = 'provider_execution'
          AND NEW.outcome_kind IN ('succeeded','empty_result')
          AND NEW.result_handle_id IS NULL
        ) OR (
          NOT (
            NEW.execution_kind = 'provider_execution'
            AND NEW.outcome_kind IN ('succeeded','empty_result')
          )
          AND NEW.result_handle_id IS NOT NULL
        )
        BEGIN
          SELECT RAISE(ABORT, 'logical settlement result-handle binding is inconsistent');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_logical_settlement_result_exact
        BEFORE INSERT ON logical_call_settlements
        WHEN NEW.result_handle_id IS NOT NULL AND NOT EXISTS (
          SELECT 1
            FROM durable_result_handles h
            JOIN logical_tool_calls l
              ON l.session_id = NEW.session_id
             AND l.source_user_seq = NEW.source_user_seq
             AND l.logical_tool_call_id = NEW.logical_tool_call_id
            JOIN physical_dispatches p
              ON p.session_id = h.session_id
             AND p.source_user_seq = h.source_user_seq
             AND p.logical_tool_call_id = h.logical_tool_call_id
             AND p.physical_dispatch_id = h.physical_dispatch_id
           WHERE h.handle_id = NEW.result_handle_id
             AND h.scope_kind = 'authoritative'
             AND h.session_id = NEW.session_id
             AND h.source_user_seq = NEW.source_user_seq
             AND h.accepted_task_id = l.accepted_task_id
             AND h.logical_tool_call_id = NEW.logical_tool_call_id
             AND h.tool_name = l.tool_name
             AND h.argument_digest = l.argument_digest
             AND h.success = 1
             AND p.state = 'returned'
             AND p.ordinal = (
               SELECT MAX(p2.ordinal) FROM physical_dispatches p2
                WHERE p2.session_id = NEW.session_id
                  AND p2.source_user_seq = NEW.source_user_seq
                  AND p2.logical_tool_call_id = NEW.logical_tool_call_id
             )
        )
        BEGIN
          SELECT RAISE(ABORT, 'logical settlement requires its exact returned result handle');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_logical_settlement_result_immutable
        BEFORE UPDATE OF result_handle_id ON logical_call_settlements
        WHEN OLD.result_handle_id IS NOT NEW.result_handle_id
        BEGIN
          SELECT RAISE(ABORT, 'logical settlement result handle is immutable');
        END;

        CREATE TABLE IF NOT EXISTS evidence_receipts (
          receipt_id             TEXT PRIMARY KEY,
          protocol_version       INTEGER NOT NULL CHECK (protocol_version = 1),
          semantic_digest        TEXT NOT NULL UNIQUE CHECK (length(semantic_digest) = 64),
          kind                   TEXT NOT NULL CHECK (kind IN ('observation','collection')),
          session_id             TEXT NOT NULL,
          source_user_seq        INTEGER NOT NULL CHECK (source_user_seq > 0),
          accepted_task_id       TEXT NOT NULL,
          manifest_id            TEXT NOT NULL,
          node_id                TEXT NOT NULL,
          obligation             TEXT NOT NULL CHECK (obligation IN ('source_observed','source_completeness')),
          logical_tool_call_id   TEXT NOT NULL,
          physical_dispatch_id   TEXT NOT NULL,
          result_handle_id       TEXT NOT NULL REFERENCES durable_result_handles(handle_id) ON DELETE RESTRICT,
          tool_name              TEXT NOT NULL,
          operation_mode         TEXT NOT NULL CHECK (operation_mode IN ('point_read','collection_read')),
          raw_payload_sha256     TEXT NOT NULL CHECK (length(raw_payload_sha256) = 64),
          raw_byte_count         INTEGER NOT NULL CHECK (raw_byte_count >= 0),
          record_identities_json TEXT NOT NULL,
          aggregate_digest       TEXT NOT NULL CHECK (length(aggregate_digest) = 64),
          completeness           TEXT NOT NULL CHECK (completeness IN ('complete','partial','unknown')),
          continuation_outstanding INTEGER NOT NULL CHECK (continuation_outstanding IN (0, 1)),
          cursor_repeated        INTEGER NOT NULL CHECK (cursor_repeated IN (0, 1)),
          receipt_event_id       TEXT NOT NULL UNIQUE REFERENCES events(id) ON DELETE RESTRICT,
          issued_at              TEXT NOT NULL,
          UNIQUE (session_id, source_user_seq, manifest_id, node_id, obligation),
          FOREIGN KEY (session_id, source_user_seq)
            REFERENCES accepted_task_authority(session_id, source_user_seq) ON DELETE CASCADE,
          FOREIGN KEY (session_id, source_user_seq, logical_tool_call_id)
            REFERENCES logical_call_settlements(session_id, source_user_seq, logical_tool_call_id)
            ON DELETE RESTRICT
        );

        CREATE INDEX IF NOT EXISTS idx_evidence_receipts_task
          ON evidence_receipts(session_id, source_user_seq, manifest_id);

        CREATE TRIGGER IF NOT EXISTS trg_evidence_receipt_exact_authority
        BEFORE INSERT ON evidence_receipts
        WHEN NOT EXISTS (
          SELECT 1
            FROM accepted_task_authority a
            JOIN logical_call_settlements s
              ON s.session_id = a.session_id
             AND s.source_user_seq = a.source_user_seq
             AND s.logical_tool_call_id = NEW.logical_tool_call_id
            JOIN logical_tool_calls l
              ON l.session_id = s.session_id
             AND l.source_user_seq = s.source_user_seq
             AND l.logical_tool_call_id = s.logical_tool_call_id
            JOIN durable_result_handles h
              ON h.handle_id = s.result_handle_id
            JOIN events e
              ON e.id = NEW.receipt_event_id
           WHERE a.session_id = NEW.session_id
             AND a.source_user_seq = NEW.source_user_seq
             AND a.accepted_task_id = NEW.accepted_task_id
             AND a.manifest_id = NEW.manifest_id
             AND a.state = 'manifested_verifying'
             AND l.accepted_task_id = NEW.accepted_task_id
             AND l.state = 'settled'
             AND s.execution_kind = 'provider_execution'
             AND s.outcome_kind IN ('succeeded','empty_result')
             AND s.result_handle_id = NEW.result_handle_id
             AND h.scope_kind = 'authoritative'
             AND h.session_id = NEW.session_id
             AND h.source_user_seq = NEW.source_user_seq
             AND h.accepted_task_id = NEW.accepted_task_id
             AND h.logical_tool_call_id = NEW.logical_tool_call_id
             AND h.physical_dispatch_id = NEW.physical_dispatch_id
             AND h.tool_name = NEW.tool_name
             AND h.raw_payload_sha256 = NEW.raw_payload_sha256
             AND h.raw_byte_count = NEW.raw_byte_count
             AND h.success = 1
             AND e.session_id = NEW.session_id
             AND e.type = 'evidence_receipt'
             AND json_extract(e.data_json, '$.receiptId') = NEW.receipt_id
             AND json_extract(e.data_json, '$.sourceUserSeq') = NEW.source_user_seq
             AND json_extract(e.data_json, '$.acceptedTaskId') = NEW.accepted_task_id
             AND json_extract(e.data_json, '$.manifestId') = NEW.manifest_id
             AND json_extract(e.data_json, '$.nodeId') = NEW.node_id
             AND json_extract(e.data_json, '$.obligation') = NEW.obligation
             AND json_extract(e.data_json, '$.logicalToolCallId') = NEW.logical_tool_call_id
             AND json_extract(e.data_json, '$.physicalDispatchId') = NEW.physical_dispatch_id
             AND json_extract(e.data_json, '$.resultHandleId') = NEW.result_handle_id
        )
        BEGIN
          SELECT RAISE(ABORT, 'evidence receipt requires exact manifested settlement authority');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_evidence_receipt_identity_immutable
        BEFORE UPDATE ON evidence_receipts
        BEGIN
          SELECT RAISE(ABORT, 'evidence receipts are immutable');
        END;
      `);
        }
      },
      {
        // A settlement is the normalized authority for outcome, requirement
        // routing, business-vs-discovery identity and continuation state. v30 made
        // its result-handle binding immutable; v31 closes the wider row so those
        // other fields cannot be rewritten beneath expected-work replay.
        version: 31,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) return;
          if (!tables.has("logical_call_settlements")) {
            throw new Error("schema v31 prerequisite missing: logical_call_settlements");
          }
          db.exec(`
        CREATE TRIGGER IF NOT EXISTS trg_logical_call_settlement_row_immutable
        BEFORE UPDATE ON logical_call_settlements
        BEGIN
          SELECT RAISE(ABORT, 'logical call settlements are immutable');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_logical_call_settlement_delete_immutable
        BEFORE DELETE ON logical_call_settlements
        -- Parent session retention remains the one deletion authority. During
        -- its FK cascade the parent session row is already absent; a direct
        -- settlement/logical-call delete still sees the live parent and stops.
        WHEN EXISTS (
          SELECT 1 FROM sessions WHERE id = OLD.session_id
        )
        BEGIN
          SELECT RAISE(ABORT, 'logical call settlements are immutable');
        END;
      `);
        }
      },
      {
        // Action expected-work admission. One immutable row binds an already-open,
        // zero-crossing logical call to the exact frozen semantic requirement it
        // is allowed to discharge. The same row carries the request-side witness
        // needed by read-evidence refinement; no parallel read authority exists.
        //
        // The two cleanup statements are deliberately parent-session based. A
        // live session may legitimately retain a historical run_attempt whose
        // source event was lost by an old/partial writer; that is degraded history,
        // not an orphan attempt. Only rows whose owning session is absent are
        // unreachable and safe to remove before foreign keys are relied upon.
        version: 32,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) return;
          if (tables.has("run_dispatch_leases")) {
            db.exec(`
          DELETE FROM run_dispatch_leases
           WHERE NOT EXISTS (
             SELECT 1 FROM sessions s WHERE s.id = run_dispatch_leases.session_id
           )
        `);
          }
          if (tables.has("run_attempts")) {
            db.exec(`
          DELETE FROM run_attempts
           WHERE NOT EXISTS (
             SELECT 1 FROM sessions s WHERE s.id = run_attempts.session_id
           )
        `);
          }
          for (const prerequisite of [
            "accepted_task_authority",
            "accepted_task_work_contracts",
            "logical_tool_calls",
            "physical_dispatches"
          ]) {
            if (!tables.has(prerequisite)) {
              throw new Error(`schema v32 prerequisite missing: ${prerequisite}`);
            }
          }
          const authorityColumns = new Set(
            db.prepare("PRAGMA table_info(accepted_task_authority)").all().map((column) => column.name)
          );
          if (!authorityColumns.has("expected_work_required")) {
            db.exec(`ALTER TABLE accepted_task_authority
          ADD COLUMN expected_work_required INTEGER NOT NULL DEFAULT 0
          CHECK (expected_work_required IN (0, 1))`);
          }
          db.exec(`
        CREATE TRIGGER IF NOT EXISTS trg_accepted_task_expected_work_activation
        BEFORE UPDATE OF expected_work_required ON accepted_task_authority
        WHEN NOT (
          OLD.expected_work_required = NEW.expected_work_required
          OR (
            OLD.expected_work_required = 0
            AND NEW.expected_work_required = 1
            AND OLD.state = 'armed'
          )
        )
        BEGIN
          SELECT RAISE(ABORT, 'expected-work activation is one-way and requires armed authority');
        END;

        CREATE TABLE IF NOT EXISTS expected_work_call_bindings (
          session_id              TEXT NOT NULL,
          source_user_seq         INTEGER NOT NULL CHECK (source_user_seq > 0),
          accepted_task_id        TEXT NOT NULL,
          logical_tool_call_id    TEXT NOT NULL,
          contract_id             TEXT NOT NULL,
          requirement_id          TEXT NOT NULL,
          tool_name                TEXT NOT NULL,
          argument_digest          TEXT NOT NULL CHECK (length(argument_digest) = 64),
          effect_kind              TEXT NOT NULL
                                   CHECK (effect_kind IN ('read','compute','local_write','external_write','admin')),
          cardinality_kind         TEXT NOT NULL
                                   CHECK (cardinality_kind IN ('once','each','set')),
          universe_id              TEXT,
          universe_seal            TEXT
                                   CHECK (universe_seal IS NULL OR universe_seal IN ('accepted_input','complete_source_receipt')),
          universe_item_id         TEXT,
          universe_selector_json   TEXT,
          universe_member_digest   TEXT
                                   CHECK (universe_member_digest IS NULL OR length(universe_member_digest) = 64),
          universe_member_count    INTEGER
                                   CHECK (universe_member_count IS NULL OR universe_member_count > 0),
          input_source_kind        TEXT
                                   CHECK (input_source_kind IS NULL OR input_source_kind IN ('accepted_user_input','complete_source_receipt')),
          input_source_ref         TEXT,
          input_source_digest      TEXT
                                   CHECK (input_source_digest IS NULL OR length(input_source_digest) = 64),
          evidence_mode            TEXT
                                   CHECK (evidence_mode IS NULL OR evidence_mode IN ('point_read','collection_read','finite_read')),
          evidence_basis           TEXT,
          schema_fingerprint       TEXT,
          schema_digest            TEXT
                                   CHECK (schema_digest IS NULL OR length(schema_digest) = 64),
          bound_at                 TEXT NOT NULL,
          PRIMARY KEY (session_id, source_user_seq, logical_tool_call_id),
          FOREIGN KEY (session_id, source_user_seq)
            REFERENCES accepted_task_work_contracts(session_id, source_user_seq)
            ON DELETE CASCADE,
          FOREIGN KEY (session_id, source_user_seq, logical_tool_call_id)
            REFERENCES logical_tool_calls(session_id, source_user_seq, logical_tool_call_id)
            ON DELETE CASCADE,
          FOREIGN KEY (contract_id)
            REFERENCES accepted_task_work_contracts(contract_id)
            ON DELETE RESTRICT,
          CHECK (
            (cardinality_kind = 'once'
              AND universe_id IS NULL
              AND universe_seal IS NULL
              AND universe_item_id IS NULL
              AND universe_selector_json IS NULL
              AND universe_member_digest IS NULL
              AND universe_member_count IS NULL
              AND input_source_kind IS NULL
              AND input_source_ref IS NULL
              AND input_source_digest IS NULL)
            OR
            (cardinality_kind = 'each'
              AND universe_id IS NOT NULL
              AND universe_seal IS NOT NULL
              AND universe_item_id IS NOT NULL
              AND universe_selector_json IS NOT NULL
              AND json_valid(universe_selector_json)
              AND json_type(universe_selector_json) = 'object'
              AND universe_member_digest IS NOT NULL
              AND universe_member_count = 1
              AND input_source_kind IS NOT NULL
              AND input_source_ref IS NOT NULL
              AND input_source_digest IS NOT NULL)
            OR
            (cardinality_kind = 'set'
              AND universe_id IS NOT NULL
              AND universe_seal IS NOT NULL
              AND universe_item_id IS NULL
              AND universe_selector_json IS NOT NULL
              AND json_valid(universe_selector_json)
              AND json_type(universe_selector_json) = 'object'
              AND universe_member_digest IS NOT NULL
              AND universe_member_count > 0
              AND input_source_kind IS NOT NULL
              AND input_source_ref IS NOT NULL
              AND input_source_digest IS NOT NULL)
          ),
          CHECK (
            (evidence_mode IS NULL AND evidence_basis IS NULL)
            OR (evidence_mode IS NOT NULL AND evidence_basis IS NOT NULL)
          ),
          CHECK (
            (schema_fingerprint IS NULL AND schema_digest IS NULL)
            OR (schema_fingerprint IS NOT NULL AND schema_digest IS NOT NULL)
          )
        );

        CREATE INDEX IF NOT EXISTS idx_expected_work_call_bindings_requirement
          ON expected_work_call_bindings(
            session_id, source_user_seq, requirement_id, universe_item_id
          );

        CREATE TRIGGER IF NOT EXISTS trg_expected_work_call_binding_exact_authority
        BEFORE INSERT ON expected_work_call_bindings
        WHEN NOT EXISTS (
          SELECT 1
            FROM accepted_task_authority a
            JOIN accepted_task_work_contracts c
              ON c.session_id = a.session_id
             AND c.source_user_seq = a.source_user_seq
            JOIN logical_tool_calls l
              ON l.session_id = a.session_id
             AND l.source_user_seq = a.source_user_seq
             AND l.logical_tool_call_id = NEW.logical_tool_call_id
           WHERE a.session_id = NEW.session_id
             AND a.source_user_seq = NEW.source_user_seq
             AND a.accepted_task_id = NEW.accepted_task_id
             AND a.expected_work_required = 1
             AND a.work_contract_id = NEW.contract_id
             AND a.state = 'armed'
             AND c.contract_id = NEW.contract_id
             AND l.accepted_task_id = NEW.accepted_task_id
             AND l.tool_name = NEW.tool_name
             AND l.argument_digest = NEW.argument_digest
             AND l.state = 'open'
             AND NOT EXISTS (
               SELECT 1 FROM physical_dispatches p
                WHERE p.session_id = l.session_id
                  AND p.source_user_seq = l.source_user_seq
                  AND p.logical_tool_call_id = l.logical_tool_call_id
             )
        )
        BEGIN
          SELECT RAISE(ABORT, 'expected-work call binding requires exact open zero-crossing authority');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_expected_work_call_bindings_update_immutable
        BEFORE UPDATE ON expected_work_call_bindings
        BEGIN
          SELECT RAISE(ABORT, 'expected-work call bindings are immutable');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_expected_work_call_bindings_delete_immutable
        BEFORE DELETE ON expected_work_call_bindings
        WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
        BEGIN
          SELECT RAISE(ABORT, 'expected-work call bindings are immutable');
        END;
      `);
        }
      },
      {
        // Provider-neutral write proof authority.  The pre-dispatch binding is
        // intentionally independent of an obligation manifest: selectors and
        // projections must be fixed before provider I/O, while the authoritative
        // manifest is only available after observed resolution closes.  A later
        // content-addressed proof receipt binds the immutable call contract to the
        // exact manifest node without rewriting either artifact.
        //
        // This migration performs no historical promotion or broad cleanup.  Old
        // event prose and external-write rows do not contain the schema, target,
        // exact input, or crossing identity needed to manufacture v33 authority.
        version: 33,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) return;
          for (const prerequisite of [
            "accepted_task_authority",
            "accepted_task_work_contracts",
            "expected_work_call_bindings",
            "logical_tool_calls",
            "physical_dispatches",
            "logical_call_settlements",
            "durable_result_handles",
            "obligation_transitions"
          ]) {
            if (!tables.has(prerequisite)) {
              throw new Error(`schema v33 prerequisite missing: ${prerequisite}`);
            }
          }
          db.exec(`
        CREATE TABLE IF NOT EXISTS write_evidence_bindings (
          binding_id                  TEXT PRIMARY KEY CHECK (length(binding_id) = 81),
          protocol_version            INTEGER NOT NULL CHECK (protocol_version = 1),
          semantic_digest             TEXT NOT NULL UNIQUE CHECK (length(semantic_digest) = 64),
          session_id                  TEXT NOT NULL,
          source_user_seq             INTEGER NOT NULL CHECK (source_user_seq > 0),
          accepted_task_id            TEXT NOT NULL,
          work_contract_id            TEXT NOT NULL,
          requirement_id              TEXT NOT NULL,
          logical_tool_call_id        TEXT NOT NULL,
          tool_name                   TEXT NOT NULL,
          argument_digest             TEXT NOT NULL CHECK (length(argument_digest) = 64),
          effect_kind                 TEXT NOT NULL CHECK (effect_kind IN ('external_write','admin')),
          reversibility               TEXT NOT NULL CHECK (reversibility IN ('reversible','irreversible')),
          target_selector_json        TEXT NOT NULL CHECK (
                                         json_valid(target_selector_json)
                                         AND json_type(target_selector_json) = 'array'
                                       ),
          target_digest               TEXT NOT NULL CHECK (length(target_digest) = 64),
          write_input_json            TEXT NOT NULL CHECK (json_valid(write_input_json)),
          write_input_digest          TEXT NOT NULL CHECK (length(write_input_digest) = 64),
          input_schema_json           TEXT NOT NULL CHECK (json_valid(input_schema_json)),
          source_requirement_ids_json TEXT NOT NULL CHECK (
                                         json_valid(source_requirement_ids_json)
                                         AND json_type(source_requirement_ids_json) = 'array'
                                       ),
          verification_json           TEXT NOT NULL CHECK (
                                         json_valid(verification_json)
                                         AND json_type(verification_json) = 'object'
                                       ),
          schema_digest               TEXT NOT NULL CHECK (length(schema_digest) = 64),
          mapping_digest              TEXT NOT NULL CHECK (length(mapping_digest) = 64),
          frozen_at                   TEXT NOT NULL,
          UNIQUE (session_id, source_user_seq, logical_tool_call_id),
          FOREIGN KEY (session_id, source_user_seq, logical_tool_call_id)
            REFERENCES expected_work_call_bindings(session_id, source_user_seq, logical_tool_call_id)
            ON DELETE CASCADE,
          FOREIGN KEY (work_contract_id)
            REFERENCES accepted_task_work_contracts(contract_id) ON DELETE RESTRICT
        );

        CREATE INDEX IF NOT EXISTS idx_write_evidence_bindings_task
          ON write_evidence_bindings(session_id, source_user_seq, requirement_id);

        CREATE TRIGGER IF NOT EXISTS trg_write_evidence_binding_exact_authority
        BEFORE INSERT ON write_evidence_bindings
        WHEN NOT EXISTS (
          SELECT 1
            FROM expected_work_call_bindings b
            JOIN accepted_task_authority a
              ON a.session_id = b.session_id
             AND a.source_user_seq = b.source_user_seq
            JOIN logical_tool_calls l
              ON l.session_id = b.session_id
             AND l.source_user_seq = b.source_user_seq
             AND l.logical_tool_call_id = b.logical_tool_call_id
           WHERE b.session_id = NEW.session_id
             AND b.source_user_seq = NEW.source_user_seq
             AND b.logical_tool_call_id = NEW.logical_tool_call_id
             AND b.accepted_task_id = NEW.accepted_task_id
             AND b.contract_id = NEW.work_contract_id
             AND b.requirement_id = NEW.requirement_id
             AND b.tool_name = NEW.tool_name
             AND b.argument_digest = NEW.argument_digest
             AND b.effect_kind = NEW.effect_kind
             AND a.accepted_task_id = NEW.accepted_task_id
             AND a.work_contract_id = NEW.work_contract_id
             AND a.expected_work_required = 1
             AND a.state = 'armed'
             AND l.accepted_task_id = NEW.accepted_task_id
             AND l.tool_name = NEW.tool_name
             AND l.argument_digest = NEW.argument_digest
             AND l.state = 'open'
             AND NOT EXISTS (
               SELECT 1 FROM physical_dispatches p
                WHERE p.session_id = NEW.session_id
                  AND p.source_user_seq = NEW.source_user_seq
                  AND p.logical_tool_call_id = NEW.logical_tool_call_id
             )
        )
        BEGIN
          SELECT RAISE(ABORT, 'write evidence binding requires exact pre-dispatch work authority');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_write_evidence_bindings_update_immutable
        BEFORE UPDATE ON write_evidence_bindings
        BEGIN
          SELECT RAISE(ABORT, 'write evidence bindings are immutable');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_write_evidence_bindings_delete_immutable
        BEFORE DELETE ON write_evidence_bindings
        WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
        BEGIN
          SELECT RAISE(ABORT, 'write evidence bindings are immutable');
        END;

        CREATE TABLE IF NOT EXISTS write_evidence_dispatch_reservations (
          reservation_id       TEXT PRIMARY KEY CHECK (length(reservation_id) = 85),
          binding_id           TEXT NOT NULL REFERENCES write_evidence_bindings(binding_id) ON DELETE CASCADE,
          session_id           TEXT NOT NULL,
          source_user_seq      INTEGER NOT NULL CHECK (source_user_seq > 0),
          accepted_task_id     TEXT NOT NULL,
          logical_tool_call_id TEXT NOT NULL,
          physical_dispatch_id TEXT NOT NULL,
          ordinal              INTEGER NOT NULL CHECK (ordinal > 0),
          target_digest        TEXT NOT NULL CHECK (length(target_digest) = 64),
          write_input_digest   TEXT NOT NULL CHECK (length(write_input_digest) = 64),
          reserved_at          TEXT NOT NULL,
          UNIQUE (session_id, source_user_seq, physical_dispatch_id),
          UNIQUE (binding_id, ordinal),
          FOREIGN KEY (session_id, source_user_seq, logical_tool_call_id)
            REFERENCES logical_tool_calls(session_id, source_user_seq, logical_tool_call_id)
            ON DELETE CASCADE
        );

        CREATE TRIGGER IF NOT EXISTS trg_write_evidence_reservation_exact_binding
        BEFORE INSERT ON write_evidence_dispatch_reservations
        WHEN NOT EXISTS (
          SELECT 1 FROM write_evidence_bindings b
           WHERE b.binding_id = NEW.binding_id
             AND b.session_id = NEW.session_id
             AND b.source_user_seq = NEW.source_user_seq
             AND b.accepted_task_id = NEW.accepted_task_id
             AND b.logical_tool_call_id = NEW.logical_tool_call_id
             AND b.target_digest = NEW.target_digest
             AND b.write_input_digest = NEW.write_input_digest
        )
        BEGIN
          SELECT RAISE(ABORT, 'write reservation conflicts with its frozen binding');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_bound_write_dispatch_requires_reservation
        BEFORE INSERT ON physical_dispatches
        WHEN EXISTS (
          SELECT 1 FROM write_evidence_bindings b
           WHERE b.session_id = NEW.session_id
             AND b.source_user_seq = NEW.source_user_seq
             AND b.logical_tool_call_id = NEW.logical_tool_call_id
        ) AND NOT EXISTS (
          SELECT 1
            FROM write_evidence_dispatch_reservations r
            JOIN write_evidence_bindings b ON b.binding_id = r.binding_id
           WHERE r.session_id = NEW.session_id
             AND r.source_user_seq = NEW.source_user_seq
             AND r.accepted_task_id = NEW.accepted_task_id
             AND r.logical_tool_call_id = NEW.logical_tool_call_id
             AND r.physical_dispatch_id = NEW.physical_dispatch_id
             AND r.ordinal = NEW.ordinal
             AND b.tool_name = NEW.tool_name
             AND b.argument_digest = NEW.argument_digest
        )
        BEGIN
          SELECT RAISE(ABORT, 'bound write dispatch requires an atomic reservation');
        END;

        CREATE TABLE IF NOT EXISTS write_evidence_dispatch_outcomes (
          outcome_id            TEXT PRIMARY KEY CHECK (length(outcome_id) = 81),
          reservation_id        TEXT NOT NULL UNIQUE
                                REFERENCES write_evidence_dispatch_reservations(reservation_id)
                                ON DELETE CASCADE,
          binding_id            TEXT NOT NULL REFERENCES write_evidence_bindings(binding_id) ON DELETE CASCADE,
          session_id            TEXT NOT NULL,
          source_user_seq       INTEGER NOT NULL CHECK (source_user_seq > 0),
          accepted_task_id      TEXT NOT NULL,
          logical_tool_call_id  TEXT NOT NULL,
          physical_dispatch_id  TEXT NOT NULL,
          kind                  TEXT NOT NULL CHECK (kind IN ('succeeded','failed','orphaned')),
          result_handle_id      TEXT REFERENCES durable_result_handles(handle_id) ON DELETE RESTRICT,
          settlement_event_id   TEXT NOT NULL REFERENCES events(id) ON DELETE RESTRICT,
          target_digest         TEXT NOT NULL CHECK (length(target_digest) = 64),
          write_input_digest    TEXT NOT NULL CHECK (length(write_input_digest) = 64),
          recorded_at           TEXT NOT NULL,
          FOREIGN KEY (session_id, source_user_seq, logical_tool_call_id)
            REFERENCES logical_call_settlements(session_id, source_user_seq, logical_tool_call_id)
            ON DELETE RESTRICT,
          CHECK ((kind = 'succeeded' AND result_handle_id IS NOT NULL)
              OR (kind != 'succeeded' AND result_handle_id IS NULL))
        );

        CREATE TRIGGER IF NOT EXISTS trg_write_evidence_outcome_exact_authority
        BEFORE INSERT ON write_evidence_dispatch_outcomes
        WHEN NOT EXISTS (
          SELECT 1
            FROM write_evidence_dispatch_reservations r
            JOIN write_evidence_bindings b ON b.binding_id = r.binding_id
            JOIN logical_call_settlements s
              ON s.session_id = r.session_id
             AND s.source_user_seq = r.source_user_seq
             AND s.logical_tool_call_id = r.logical_tool_call_id
            JOIN physical_dispatches p
              ON p.session_id = r.session_id
             AND p.source_user_seq = r.source_user_seq
             AND p.logical_tool_call_id = r.logical_tool_call_id
             AND p.physical_dispatch_id = r.physical_dispatch_id
           WHERE r.reservation_id = NEW.reservation_id
             AND r.binding_id = NEW.binding_id
             AND r.session_id = NEW.session_id
             AND r.source_user_seq = NEW.source_user_seq
             AND r.accepted_task_id = NEW.accepted_task_id
             AND r.logical_tool_call_id = NEW.logical_tool_call_id
             AND r.physical_dispatch_id = NEW.physical_dispatch_id
             AND r.target_digest = NEW.target_digest
             AND r.write_input_digest = NEW.write_input_digest
             AND s.settlement_event_id = NEW.settlement_event_id
             AND p.state != 'started'
             AND (
               (NEW.kind = 'succeeded'
                 AND p.state = 'returned'
                 AND s.execution_kind = 'provider_execution'
                 AND s.outcome_kind IN ('succeeded','empty_result')
                 AND s.result_handle_id = NEW.result_handle_id
                 AND EXISTS (
                   SELECT 1 FROM durable_result_handles h
                    WHERE h.handle_id = NEW.result_handle_id
                      AND h.session_id = NEW.session_id
                      AND h.source_user_seq = NEW.source_user_seq
                      AND h.accepted_task_id = NEW.accepted_task_id
                      AND h.logical_tool_call_id = NEW.logical_tool_call_id
                      AND h.physical_dispatch_id = NEW.physical_dispatch_id
                 ))
               OR
               (NEW.kind != 'succeeded'
                 AND s.outcome_kind NOT IN ('succeeded','empty_result'))
             )
        )
        BEGIN
          SELECT RAISE(ABORT, 'write outcome conflicts with reservation, settlement, or result');
        END;

        CREATE TABLE IF NOT EXISTS write_evidence_readback_bindings (
          readback_binding_id      TEXT PRIMARY KEY CHECK (length(readback_binding_id) = 90),
          binding_id               TEXT NOT NULL REFERENCES write_evidence_bindings(binding_id) ON DELETE CASCADE,
          session_id               TEXT NOT NULL,
          source_user_seq          INTEGER NOT NULL CHECK (source_user_seq > 0),
          accepted_task_id         TEXT NOT NULL,
          work_contract_id         TEXT NOT NULL,
          write_requirement_id     TEXT NOT NULL,
          read_requirement_id      TEXT NOT NULL,
          read_logical_tool_call_id TEXT NOT NULL,
          read_tool_name           TEXT NOT NULL,
          read_argument_digest     TEXT NOT NULL CHECK (length(read_argument_digest) = 64),
          target_selector_json     TEXT NOT NULL CHECK (
                                     json_valid(target_selector_json)
                                     AND json_type(target_selector_json) = 'array'
                                   ),
          target_digest            TEXT NOT NULL CHECK (length(target_digest) = 64),
          schema_digest            TEXT NOT NULL CHECK (length(schema_digest) = 64),
          verification_contract_id TEXT NOT NULL,
          frozen_at                TEXT NOT NULL,
          UNIQUE (binding_id, read_logical_tool_call_id),
          FOREIGN KEY (session_id, source_user_seq, read_logical_tool_call_id)
            REFERENCES expected_work_call_bindings(session_id, source_user_seq, logical_tool_call_id)
            ON DELETE CASCADE
        );

        CREATE TRIGGER IF NOT EXISTS trg_write_readback_exact_authority
        BEFORE INSERT ON write_evidence_readback_bindings
        WHEN NOT EXISTS (
          SELECT 1
            FROM write_evidence_bindings w
            JOIN expected_work_call_bindings r
              ON r.session_id = w.session_id
             AND r.source_user_seq = w.source_user_seq
             AND r.logical_tool_call_id = NEW.read_logical_tool_call_id
            JOIN logical_tool_calls l
              ON l.session_id = r.session_id
             AND l.source_user_seq = r.source_user_seq
             AND l.logical_tool_call_id = r.logical_tool_call_id
           WHERE w.binding_id = NEW.binding_id
             AND w.session_id = NEW.session_id
             AND w.source_user_seq = NEW.source_user_seq
             AND w.accepted_task_id = NEW.accepted_task_id
             AND w.work_contract_id = NEW.work_contract_id
             AND w.requirement_id = NEW.write_requirement_id
             AND w.target_digest = NEW.target_digest
             AND w.binding_id = NEW.verification_contract_id
             AND r.accepted_task_id = NEW.accepted_task_id
             AND r.contract_id = NEW.work_contract_id
             AND r.requirement_id = NEW.read_requirement_id
             AND r.effect_kind = 'read'
             AND r.tool_name = NEW.read_tool_name
             AND r.argument_digest = NEW.read_argument_digest
             AND l.state = 'open'
             AND NOT EXISTS (
               SELECT 1 FROM physical_dispatches p
                WHERE p.session_id = NEW.session_id
                  AND p.source_user_seq = NEW.source_user_seq
                  AND p.logical_tool_call_id = NEW.read_logical_tool_call_id
             )
        )
        BEGIN
          SELECT RAISE(ABORT, 'readback binding requires exact pre-dispatch read authority');
        END;

        CREATE TABLE IF NOT EXISTS write_evidence_derivations (
          derivation_id             TEXT PRIMARY KEY CHECK (length(derivation_id) = 84),
          binding_id                TEXT NOT NULL UNIQUE REFERENCES write_evidence_bindings(binding_id) ON DELETE CASCADE,
          session_id                TEXT NOT NULL,
          source_user_seq           INTEGER NOT NULL CHECK (source_user_seq > 0),
          accepted_task_id          TEXT NOT NULL,
          work_contract_id          TEXT NOT NULL,
          requirement_id            TEXT NOT NULL,
          output_digest             TEXT NOT NULL CHECK (length(output_digest) = 64),
          transform_artifact_digest TEXT NOT NULL CHECK (length(transform_artifact_digest) = 64),
          source_count              INTEGER NOT NULL CHECK (source_count > 0),
          semantic_digest           TEXT NOT NULL UNIQUE CHECK (length(semantic_digest) = 64),
          recorded_at               TEXT NOT NULL
        );

        CREATE TABLE IF NOT EXISTS write_evidence_derivation_sources (
          derivation_id         TEXT NOT NULL REFERENCES write_evidence_derivations(derivation_id) ON DELETE CASCADE,
          session_id            TEXT NOT NULL,
          source_user_seq       INTEGER NOT NULL CHECK (source_user_seq > 0),
          requirement_id        TEXT NOT NULL,
          logical_tool_call_id  TEXT NOT NULL,
          result_handle_id      TEXT NOT NULL REFERENCES durable_result_handles(handle_id) ON DELETE RESTRICT,
          content_digest        TEXT NOT NULL CHECK (length(content_digest) = 64),
          PRIMARY KEY (derivation_id, requirement_id),
          UNIQUE (derivation_id, logical_tool_call_id),
          FOREIGN KEY (session_id, source_user_seq, logical_tool_call_id)
            REFERENCES logical_call_settlements(session_id, source_user_seq, logical_tool_call_id)
            ON DELETE RESTRICT
        );

        CREATE TABLE IF NOT EXISTS write_evidence_execution_snapshots (
          snapshot_id          TEXT PRIMARY KEY CHECK (length(snapshot_id) = 82),
          binding_id           TEXT NOT NULL UNIQUE REFERENCES write_evidence_bindings(binding_id) ON DELETE CASCADE,
          session_id           TEXT NOT NULL,
          source_user_seq      INTEGER NOT NULL CHECK (source_user_seq > 0),
          accepted_task_id     TEXT NOT NULL,
          executions_json      TEXT NOT NULL CHECK (
                                 json_valid(executions_json)
                                 AND json_type(executions_json) = 'array'
                               ),
          opened_ids_json      TEXT NOT NULL CHECK (
                                 json_valid(opened_ids_json)
                                 AND json_type(opened_ids_json) = 'array'
                               ),
          semantic_digest      TEXT NOT NULL UNIQUE CHECK (length(semantic_digest) = 64),
          recorded_at          TEXT NOT NULL
        );

        CREATE TABLE IF NOT EXISTS write_evidence_proofs (
          proof_id                   TEXT PRIMARY KEY CHECK (length(proof_id) = 82),
          protocol_version           INTEGER NOT NULL CHECK (protocol_version = 1),
          binding_id                 TEXT NOT NULL REFERENCES write_evidence_bindings(binding_id) ON DELETE RESTRICT,
          session_id                 TEXT NOT NULL,
          source_user_seq            INTEGER NOT NULL CHECK (source_user_seq > 0),
          accepted_task_id           TEXT NOT NULL,
          work_contract_id           TEXT NOT NULL,
          manifest_id                TEXT NOT NULL,
          node_id                    TEXT NOT NULL,
          requirement_id             TEXT NOT NULL,
          obligation                 TEXT NOT NULL CHECK (obligation IN (
                                             'derivation_from_current_source','commit_effect',
                                             'verify_committed_readback','stale_destination_reconciled',
                                             'verify_committed_receipt','execution_terminal'
                                           )),
          logical_tool_call_id       TEXT NOT NULL,
          anchor_physical_dispatch_id TEXT NOT NULL,
          target_digest              TEXT NOT NULL CHECK (length(target_digest) = 64),
          physical_dispatch_ids_json TEXT NOT NULL CHECK (
                                         json_valid(physical_dispatch_ids_json)
                                         AND json_type(physical_dispatch_ids_json) = 'array'
                                       ),
          evidence_digests_json      TEXT NOT NULL CHECK (
                                         json_valid(evidence_digests_json)
                                         AND json_type(evidence_digests_json) = 'array'
                                       ),
          proof_json                 TEXT NOT NULL CHECK (
                                         json_valid(proof_json)
                                         AND json_type(proof_json) = 'object'
                                       ),
          receipt_event_id           TEXT NOT NULL UNIQUE REFERENCES events(id) ON DELETE RESTRICT,
          issued_at                  TEXT NOT NULL,
          UNIQUE (session_id, source_user_seq, manifest_id, node_id, obligation),
          FOREIGN KEY (session_id, source_user_seq)
            REFERENCES accepted_task_authority(session_id, source_user_seq) ON DELETE CASCADE,
          FOREIGN KEY (session_id, source_user_seq, logical_tool_call_id, anchor_physical_dispatch_id)
            REFERENCES physical_dispatches(
              session_id, source_user_seq, logical_tool_call_id, physical_dispatch_id
            ) ON DELETE RESTRICT
        );

        CREATE INDEX IF NOT EXISTS idx_write_evidence_proofs_task
          ON write_evidence_proofs(session_id, source_user_seq, manifest_id);

        CREATE TRIGGER IF NOT EXISTS trg_write_evidence_proof_exact_authority
        BEFORE INSERT ON write_evidence_proofs
        WHEN NOT EXISTS (
          SELECT 1
            FROM write_evidence_bindings b
            JOIN accepted_task_authority a
              ON a.session_id = b.session_id
             AND a.source_user_seq = b.source_user_seq
            JOIN events e ON e.id = NEW.receipt_event_id
           WHERE b.binding_id = NEW.binding_id
             AND b.session_id = NEW.session_id
             AND b.source_user_seq = NEW.source_user_seq
             AND b.accepted_task_id = NEW.accepted_task_id
             AND b.work_contract_id = NEW.work_contract_id
             AND b.requirement_id = NEW.requirement_id
             AND b.logical_tool_call_id = NEW.logical_tool_call_id
             AND b.target_digest = NEW.target_digest
             AND a.accepted_task_id = NEW.accepted_task_id
             AND a.work_contract_id = NEW.work_contract_id
             AND a.manifest_id = NEW.manifest_id
             AND a.state = 'manifested_verifying'
             AND e.session_id = NEW.session_id
             AND e.type = 'write_evidence_proved'
             AND json_extract(e.data_json, '$.proofId') = NEW.proof_id
             AND json_extract(e.data_json, '$.sourceUserSeq') = NEW.source_user_seq
             AND json_extract(e.data_json, '$.acceptedTaskId') = NEW.accepted_task_id
             AND json_extract(e.data_json, '$.manifestId') = NEW.manifest_id
             AND json_extract(e.data_json, '$.nodeId') = NEW.node_id
             AND json_extract(e.data_json, '$.obligation') = NEW.obligation
        )
        BEGIN
          SELECT RAISE(ABORT, 'write proof requires exact manifested authority and event mirror');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_write_evidence_normalized_rows_immutable
        BEFORE UPDATE ON write_evidence_dispatch_reservations
        BEGIN SELECT RAISE(ABORT, 'write evidence reservations are immutable'); END;
        CREATE TRIGGER IF NOT EXISTS trg_write_evidence_outcomes_immutable
        BEFORE UPDATE ON write_evidence_dispatch_outcomes
        BEGIN SELECT RAISE(ABORT, 'write evidence outcomes are immutable'); END;
        CREATE TRIGGER IF NOT EXISTS trg_write_evidence_readbacks_immutable
        BEFORE UPDATE ON write_evidence_readback_bindings
        BEGIN SELECT RAISE(ABORT, 'write evidence readback bindings are immutable'); END;
        CREATE TRIGGER IF NOT EXISTS trg_write_evidence_derivations_immutable
        BEFORE UPDATE ON write_evidence_derivations
        BEGIN SELECT RAISE(ABORT, 'write evidence derivations are immutable'); END;
        CREATE TRIGGER IF NOT EXISTS trg_write_evidence_derivation_sources_immutable
        BEFORE UPDATE ON write_evidence_derivation_sources
        BEGIN SELECT RAISE(ABORT, 'write evidence derivation sources are immutable'); END;
        CREATE TRIGGER IF NOT EXISTS trg_write_evidence_execution_snapshots_immutable
        BEFORE UPDATE ON write_evidence_execution_snapshots
        BEGIN SELECT RAISE(ABORT, 'write evidence execution snapshots are immutable'); END;
        CREATE TRIGGER IF NOT EXISTS trg_write_evidence_proofs_immutable
        BEFORE UPDATE ON write_evidence_proofs
        BEGIN SELECT RAISE(ABORT, 'write evidence proofs are immutable'); END;

        CREATE TRIGGER IF NOT EXISTS trg_write_evidence_reservations_delete_immutable
        BEFORE DELETE ON write_evidence_dispatch_reservations
        WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
        BEGIN SELECT RAISE(ABORT, 'write evidence reservations are immutable'); END;
        CREATE TRIGGER IF NOT EXISTS trg_write_evidence_outcomes_delete_immutable
        BEFORE DELETE ON write_evidence_dispatch_outcomes
        WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
        BEGIN SELECT RAISE(ABORT, 'write evidence outcomes are immutable'); END;
        CREATE TRIGGER IF NOT EXISTS trg_write_evidence_readbacks_delete_immutable
        BEFORE DELETE ON write_evidence_readback_bindings
        WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
        BEGIN SELECT RAISE(ABORT, 'write evidence readback bindings are immutable'); END;
        CREATE TRIGGER IF NOT EXISTS trg_write_evidence_derivations_delete_immutable
        BEFORE DELETE ON write_evidence_derivations
        WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
        BEGIN SELECT RAISE(ABORT, 'write evidence derivations are immutable'); END;
        CREATE TRIGGER IF NOT EXISTS trg_write_evidence_derivation_sources_delete_immutable
        BEFORE DELETE ON write_evidence_derivation_sources
        WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
        BEGIN SELECT RAISE(ABORT, 'write evidence derivation sources are immutable'); END;
        CREATE TRIGGER IF NOT EXISTS trg_write_evidence_execution_snapshots_delete_immutable
        BEFORE DELETE ON write_evidence_execution_snapshots
        WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
        BEGIN SELECT RAISE(ABORT, 'write evidence execution snapshots are immutable'); END;
        CREATE TRIGGER IF NOT EXISTS trg_write_evidence_proofs_delete_immutable
        BEFORE DELETE ON write_evidence_proofs
        WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
        BEGIN SELECT RAISE(ABORT, 'write evidence proofs are immutable'); END;
      `);
        }
      },
      {
        // Exact host completion authority for acknowledgement-only durable-memory
        // actions. The normalized receipt is independent of provider prose and is
        // bound to one accepted source, graph, memory episode, intake call, and
        // candidate-row digest. Historical telemetry is deliberately not
        // backfilled: it lacks enough evidence to manufacture this authority.
        version: 34,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) return;
          if (!tables.has("accepted_task_authority")) {
            throw new Error("schema v34 prerequisite missing: accepted_task_authority");
          }
          const authorityColumns = new Set(
            db.prepare("PRAGMA table_info(accepted_task_authority)").all().map((row) => row.name)
          );
          if (!authorityColumns.has("host_completion_receipt_id")) {
            db.exec("ALTER TABLE accepted_task_authority ADD COLUMN host_completion_receipt_id TEXT");
          }
          if (!authorityColumns.has("host_completion_event_id")) {
            db.exec("ALTER TABLE accepted_task_authority ADD COLUMN host_completion_event_id TEXT REFERENCES events(id) ON DELETE RESTRICT");
          }
          db.exec(`
        CREATE TABLE IF NOT EXISTS durable_memory_intake_receipts (
          receipt_id             TEXT PRIMARY KEY
                                 CHECK (length(receipt_id) = 81 AND receipt_id LIKE 'memory-intake:v1:%'),
          protocol_version       INTEGER NOT NULL CHECK (protocol_version = 1),
          session_id             TEXT NOT NULL,
          source_user_seq        INTEGER NOT NULL CHECK (source_user_seq > 0),
          accepted_task_id       TEXT NOT NULL,
          graph_event_id         TEXT NOT NULL,
          graph_id               TEXT NOT NULL,
          graph_hash             TEXT NOT NULL CHECK (length(graph_hash) = 64),
          source_event_id        TEXT NOT NULL,
          source_message_digest  TEXT NOT NULL CHECK (length(source_message_digest) = 64),
          episode_id             TEXT NOT NULL,
          call_id                TEXT NOT NULL,
          episode_content_hash   TEXT NOT NULL CHECK (length(episode_content_hash) = 64),
          candidate_count        INTEGER NOT NULL CHECK (candidate_count > 0 AND candidate_count <= 3),
          candidate_digest       TEXT NOT NULL CHECK (length(candidate_digest) = 64),
          evidence_digest        TEXT NOT NULL CHECK (length(evidence_digest) = 64),
          receipt_json           TEXT NOT NULL CHECK (
                                   json_valid(receipt_json)
                                   AND json_type(receipt_json) = 'object'
                                 ),
          receipt_event_id       TEXT NOT NULL UNIQUE REFERENCES events(id) ON DELETE RESTRICT,
          issued_at              TEXT NOT NULL,
          UNIQUE (session_id, source_user_seq),
          FOREIGN KEY (session_id, source_user_seq)
            REFERENCES accepted_task_authority(session_id, source_user_seq) ON DELETE CASCADE
        );

        CREATE INDEX IF NOT EXISTS idx_durable_memory_intake_receipt_task
          ON durable_memory_intake_receipts(accepted_task_id);

        CREATE TRIGGER IF NOT EXISTS trg_durable_memory_intake_receipt_exact_authority
        BEFORE INSERT ON durable_memory_intake_receipts
        WHEN NOT EXISTS (
          SELECT 1
            FROM accepted_task_authority a
            JOIN events e ON e.id = NEW.receipt_event_id
           WHERE a.session_id = NEW.session_id
             AND a.source_user_seq = NEW.source_user_seq
             AND a.accepted_task_id = NEW.accepted_task_id
             AND a.graph_event_id = NEW.graph_event_id
             AND a.graph_id = NEW.graph_id
             AND a.graph_hash = NEW.graph_hash
             AND a.state = 'armed'
             AND a.expected_work_required = 1
             AND a.work_contract_id IS NULL
             AND a.host_completion_receipt_id IS NULL
             AND e.session_id = NEW.session_id
             AND e.type = 'durable_memory_intake_receipt'
             AND json_extract(e.data_json, '$.receiptId') = NEW.receipt_id
             AND json_extract(e.data_json, '$.sourceUserSeq') = NEW.source_user_seq
             AND json_extract(e.data_json, '$.acceptedTaskId') = NEW.accepted_task_id
        )
        BEGIN
          SELECT RAISE(ABORT, 'durable memory receipt requires exact armed host authority');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_durable_memory_intake_receipts_update_immutable
        BEFORE UPDATE ON durable_memory_intake_receipts
        BEGIN
          SELECT RAISE(ABORT, 'durable memory intake receipts are immutable');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_durable_memory_intake_receipts_delete_immutable
        BEFORE DELETE ON durable_memory_intake_receipts
        WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
        BEGIN
          SELECT RAISE(ABORT, 'durable memory intake receipts are immutable');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_accepted_task_host_completion_monotonic
        BEFORE UPDATE OF host_completion_receipt_id, host_completion_event_id
        ON accepted_task_authority
        WHEN (OLD.host_completion_receipt_id IS NOT NULL
              AND OLD.host_completion_receipt_id IS NOT NEW.host_completion_receipt_id)
          OR (OLD.host_completion_event_id IS NOT NULL
              AND OLD.host_completion_event_id IS NOT NEW.host_completion_event_id)
          OR (NEW.host_completion_receipt_id IS NULL) IS NOT (NEW.host_completion_event_id IS NULL)
          OR (NEW.host_completion_receipt_id IS NOT NULL AND (
            NEW.work_contract_id IS NOT NULL
            OR NEW.manifest_id IS NOT NEW.host_completion_receipt_id
            OR NEW.backstop_event_id IS NOT NEW.host_completion_event_id
            OR NEW.state NOT IN ('manifested_verifying','terminal')
          ))
          OR (NEW.host_completion_receipt_id IS NOT NULL AND NOT EXISTS (
            SELECT 1 FROM durable_memory_intake_receipts r
             WHERE r.receipt_id = NEW.host_completion_receipt_id
               AND r.receipt_event_id = NEW.host_completion_event_id
               AND r.session_id = NEW.session_id
               AND r.source_user_seq = NEW.source_user_seq
               AND r.accepted_task_id = NEW.accepted_task_id
               AND r.graph_event_id = NEW.graph_event_id
               AND r.graph_id = NEW.graph_id
               AND r.graph_hash = NEW.graph_hash
          ))
        BEGIN
          SELECT RAISE(ABORT, 'accepted task host-completion binding is not exact or monotonic');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_accepted_task_host_terminal_exact
        BEFORE UPDATE OF state ON accepted_task_authority
        WHEN OLD.host_completion_receipt_id IS NOT NULL
          AND NEW.state = 'terminal'
          AND NOT EXISTS (
            SELECT 1
              FROM durable_memory_intake_receipts r
              JOIN events receipt_event ON receipt_event.id = r.receipt_event_id
              JOIN events terminal_event ON terminal_event.id = NEW.terminal_event_id
             WHERE r.receipt_id = OLD.host_completion_receipt_id
               AND r.receipt_event_id = OLD.host_completion_event_id
               AND r.session_id = NEW.session_id
               AND r.source_user_seq = NEW.source_user_seq
               AND r.accepted_task_id = NEW.accepted_task_id
               AND r.graph_event_id = NEW.graph_event_id
               AND r.graph_id = NEW.graph_id
               AND r.graph_hash = NEW.graph_hash
               AND NEW.manifest_id = r.receipt_id
               AND NEW.backstop_event_id = r.receipt_event_id
               AND NEW.work_contract_id IS NULL
               AND receipt_event.session_id = NEW.session_id
               AND receipt_event.type = 'durable_memory_intake_receipt'
               AND terminal_event.session_id = NEW.session_id
               AND terminal_event.type = 'conversation_completed'
               AND COALESCE(
                 json_extract(terminal_event.data_json, '$.sourceUserSeq'),
                 json_extract(terminal_event.data_json, '$.presentation.identity.sourceUserSeq')
               ) = NEW.source_user_seq
          )
        BEGIN
          SELECT RAISE(ABORT, 'host-completed terminal requires its exact receipt and terminal event');
        END;
      `);
        }
      },
      {
        // The host's own in-process execution is a crossing too.
        //
        // Evidence redemption, dependency discharge and terminal projection all
        // key on a physical dispatch, so work the host ran itself produced no
        // evidence at all and no contract naming a local read or local_write
        // could ever be proved (live 2026-08-11: a contracted local source read
        // settled with zero dispatches, zero handles, zero operations).
        //
        // Recording those crossings makes them provable, and this column keeps the
        // table honest about which ones left the machine — a paid provider call
        // and a filesystem write must stay distinguishable to anything that counts
        // crossings. Deliberately an ADD COLUMN, not a widened relation CHECK:
        // three tables carry foreign keys into physical_dispatches, so the table
        // rebuild a CHECK change requires would put those references and a live
        // multi-hundred-megabyte store at risk for a label. NULL means what it has
        // always meant — a crossing that left the process.
        version: 35,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("physical_dispatches")) return;
          const columns = new Set(
            db.prepare("PRAGMA table_info(physical_dispatches)").all().map((row) => row.name)
          );
          if (!columns.has("execution_site")) {
            db.exec("ALTER TABLE physical_dispatches ADD COLUMN execution_site TEXT");
          }
          if (!tables.has("logical_call_settlements")) return;
          const settlementColumns = new Set(
            db.prepare("PRAGMA table_info(logical_call_settlements)").all().map((row) => row.name)
          );
          if (!settlementColumns.has("host_crossing_count")) {
            db.exec("ALTER TABLE logical_call_settlements ADD COLUMN host_crossing_count INTEGER");
          }
          if (!tables.has("logical_tool_calls")) return;
          const logicalColumns = new Set(
            db.prepare("PRAGMA table_info(logical_tool_calls)").all().map((row) => row.name)
          );
          if (!logicalColumns.has("conflict_reason")) {
            db.exec("ALTER TABLE logical_tool_calls ADD COLUMN conflict_reason TEXT");
          }
          db.exec(`
        DROP TRIGGER IF EXISTS trg_logical_settlement_result_required;
        CREATE TRIGGER trg_logical_settlement_result_required
        BEFORE INSERT ON logical_call_settlements
        WHEN (
          NEW.execution_kind = 'provider_execution'
          AND NEW.outcome_kind IN ('succeeded','empty_result')
          AND NEW.result_handle_id IS NULL
        ) OR (
          NEW.result_handle_id IS NOT NULL
          AND NOT (
            NEW.execution_kind IN ('provider_execution','local_execution')
            AND NEW.outcome_kind IN ('succeeded','empty_result')
          )
        )
        BEGIN
          SELECT RAISE(ABORT, 'logical settlement result-handle binding is inconsistent');
        END;
      `);
        }
      },
      {
        /**
         * Re-assert v35's complete end state.
         *
         * A migration is gated on its version number, so EDITING an already-shipped
         * version is a permanent no-op on every store that recorded it. v35 grew a
         * column after a live daemon had already applied and recorded it, leaving
         * that store with three of v35's four changes and no way to ever receive
         * the fourth — while the code shipping alongside it writes to that column on
         * every poisoned call (found on the live store 2026-08-11, which applied v35
         * at 19:52Z without conflict_reason).
         *
         * Every step is guarded, so this is a no-op on a store that received all of
         * v35 and a repair on one that received part of it. The rule it encodes: a
         * shipped migration is immutable, and a correction ships as its own version.
         */
        version: 36,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          const columnsOf = (table) => new Set(
            db.prepare(`PRAGMA table_info(${table})`).all().map((row) => row.name)
          );
          if (tables.has("physical_dispatches") && !columnsOf("physical_dispatches").has("execution_site")) {
            db.exec("ALTER TABLE physical_dispatches ADD COLUMN execution_site TEXT");
          }
          if (tables.has("logical_call_settlements")) {
            if (!columnsOf("logical_call_settlements").has("host_crossing_count")) {
              db.exec("ALTER TABLE logical_call_settlements ADD COLUMN host_crossing_count INTEGER");
            }
            db.exec(`
          DROP TRIGGER IF EXISTS trg_logical_settlement_result_required;
          CREATE TRIGGER trg_logical_settlement_result_required
          BEFORE INSERT ON logical_call_settlements
          WHEN (
            NEW.execution_kind = 'provider_execution'
            AND NEW.outcome_kind IN ('succeeded','empty_result')
            AND NEW.result_handle_id IS NULL
          ) OR (
            NEW.result_handle_id IS NOT NULL
            AND NOT (
              NEW.execution_kind IN ('provider_execution','local_execution')
              AND NEW.outcome_kind IN ('succeeded','empty_result')
            )
          )
          BEGIN
            SELECT RAISE(ABORT, 'logical settlement result-handle binding is inconsistent');
          END;
        `);
          }
          if (tables.has("logical_tool_calls") && !columnsOf("logical_tool_calls").has("conflict_reason")) {
            db.exec("ALTER TABLE logical_tool_calls ADD COLUMN conflict_reason TEXT");
          }
        }
      },
      {
        /**
         * ONE bounded correction to a source universe's member-id pointer.
         *
         * The pointer says where member identity lives inside a producer record,
         * and the contract freezes it BEFORE the read that would prove it. A wrong
         * guess was therefore fatal for the turn: the seal refused, the contract
         * was immutable, and the per-item lane died with no way back (live
         * 2026-08-11 — records keyed "Id", a natural proposal of '/id').
         *
         * This is seal METADATA, deliberately not part of the contract: the
         * contract is content-addressed, so amending it in place would change its
         * id and orphan every binding. Operations, effects, coverage, dependencies
         * and membership rules stay immutable and unamendable. The primary key is
         * the "exactly once" rule — a second amendment cannot be written at all.
         */
        version: 37,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("accepted_task_work_contracts") || !tables.has("events")) return;
          db.exec(`
        CREATE TABLE IF NOT EXISTS expected_work_universe_amendments (
          session_id              TEXT NOT NULL,
          source_user_seq         INTEGER NOT NULL CHECK (source_user_seq > 0),
          contract_id             TEXT NOT NULL,
          universe_id             TEXT NOT NULL,
          prior_member_id_pointer TEXT NOT NULL,
          member_id_pointer       TEXT NOT NULL
                                  CHECK (member_id_pointer != prior_member_id_pointer),
          motivating_refusal      TEXT NOT NULL,
          sealed_member_count     INTEGER NOT NULL CHECK (sealed_member_count > 0),
          amended_at              TEXT NOT NULL,
          amendment_event_id      TEXT NOT NULL REFERENCES events(id) ON DELETE RESTRICT,
          PRIMARY KEY (session_id, source_user_seq, contract_id, universe_id),
          FOREIGN KEY (contract_id)
            REFERENCES accepted_task_work_contracts(contract_id) ON DELETE RESTRICT
        );

        CREATE TRIGGER IF NOT EXISTS trg_expected_work_universe_amendment_update_immutable
        BEFORE UPDATE ON expected_work_universe_amendments
        BEGIN
          SELECT RAISE(ABORT, 'a universe amendment is immutable');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_expected_work_universe_amendment_delete_immutable
        BEFORE DELETE ON expected_work_universe_amendments
        WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
        BEGIN
          SELECT RAISE(ABORT, 'a universe amendment is immutable');
        END;
      `);
        }
      },
      {
        // v28's receipt-authority trigger admitted only provider executions. The
        // host's own returned execution carries the same redeemable evidence — a
        // 'host'-site crossing and a byte-bound result handle — and a local read
        // satisfying the deterministic retrieve route could mint no receipt at
        // all (live 2026-08-12). Shipped migrations are immutable, so the widened
        // trigger ships as its own version: drop and recreate with the host door.
        version: 38,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("evidence_receipts") || !tables.has("physical_dispatches")) return;
          db.exec(`
        DROP TRIGGER IF EXISTS trg_evidence_receipt_exact_authority;
        CREATE TRIGGER trg_evidence_receipt_exact_authority
        BEFORE INSERT ON evidence_receipts
        WHEN NOT EXISTS (
          SELECT 1
            FROM accepted_task_authority a
            JOIN logical_call_settlements s
              ON s.session_id = a.session_id
             AND s.source_user_seq = a.source_user_seq
             AND s.logical_tool_call_id = NEW.logical_tool_call_id
            JOIN logical_tool_calls l
              ON l.session_id = s.session_id
             AND l.source_user_seq = s.source_user_seq
             AND l.logical_tool_call_id = s.logical_tool_call_id
            JOIN durable_result_handles h
              ON h.handle_id = s.result_handle_id
            JOIN physical_dispatches p
              ON p.session_id = h.session_id
             AND p.source_user_seq = h.source_user_seq
             AND p.logical_tool_call_id = h.logical_tool_call_id
             AND p.physical_dispatch_id = h.physical_dispatch_id
            JOIN events e
              ON e.id = NEW.receipt_event_id
           WHERE a.session_id = NEW.session_id
             AND a.source_user_seq = NEW.source_user_seq
             AND a.accepted_task_id = NEW.accepted_task_id
             AND a.manifest_id = NEW.manifest_id
             AND a.state = 'manifested_verifying'
             AND l.accepted_task_id = NEW.accepted_task_id
             AND l.state = 'settled'
             AND (
               s.execution_kind = 'provider_execution'
               OR (s.execution_kind = 'local_execution' AND p.execution_site = 'host')
             )
             AND s.outcome_kind IN ('succeeded','empty_result')
             AND s.result_handle_id = NEW.result_handle_id
             AND h.scope_kind = 'authoritative'
             AND h.session_id = NEW.session_id
             AND h.source_user_seq = NEW.source_user_seq
             AND h.accepted_task_id = NEW.accepted_task_id
             AND h.logical_tool_call_id = NEW.logical_tool_call_id
             AND h.physical_dispatch_id = NEW.physical_dispatch_id
             AND h.tool_name = NEW.tool_name
             AND h.raw_payload_sha256 = NEW.raw_payload_sha256
             AND h.raw_byte_count = NEW.raw_byte_count
             AND h.success = 1
             AND e.session_id = NEW.session_id
             AND e.type = 'evidence_receipt'
             AND json_extract(e.data_json, '$.receiptId') = NEW.receipt_id
             AND json_extract(e.data_json, '$.sourceUserSeq') = NEW.source_user_seq
             AND json_extract(e.data_json, '$.acceptedTaskId') = NEW.accepted_task_id
             AND json_extract(e.data_json, '$.manifestId') = NEW.manifest_id
             AND json_extract(e.data_json, '$.nodeId') = NEW.node_id
             AND json_extract(e.data_json, '$.obligation') = NEW.obligation
             AND json_extract(e.data_json, '$.logicalToolCallId') = NEW.logical_tool_call_id
             AND json_extract(e.data_json, '$.physicalDispatchId') = NEW.physical_dispatch_id
             AND json_extract(e.data_json, '$.resultHandleId') = NEW.result_handle_id
        )
        BEGIN
          SELECT RAISE(ABORT, 'evidence receipt requires exact manifested settlement authority');
        END;
      `);
        }
      },
      {
        /**
         * Provider-neutral requirement identities for broad discovery.
         *
         * A task-wide broad-search row made two different unresolved requirements
         * fight for one slot, while keying by query/provider wording would let one
         * requirement buy unlimited synonymous slots. The capability resolver now
         * supplies opaque role keys for the exact accepted request. Persist that
         * closed membership before the model runs; claims may then reuse the
         * existing `subject` column as the per-role key without trusting model text.
         *
         * The set row distinguishes a deliberately empty/all-resolved projection
         * from a legacy task that has not adopted role-scoped discovery. Requirement
         * text is represented only by a digest. Resolution can tighten from open to
         * resolved, but role identity and source membership are immutable.
         */
        version: 39,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("discovery_governor_tasks")) return;
          db.exec(`
        CREATE TABLE IF NOT EXISTS discovery_governor_role_sets (
          session_id         TEXT NOT NULL,
          source_user_seq    INTEGER NOT NULL CHECK (source_user_seq > 0),
          projection_digest  TEXT NOT NULL CHECK (length(projection_digest) = 64),
          role_count         INTEGER NOT NULL CHECK (role_count >= 0),
          unresolved_count   INTEGER NOT NULL CHECK (
                               unresolved_count >= 0 AND unresolved_count <= role_count
                             ),
          initialized_at     TEXT NOT NULL,
          updated_at         TEXT NOT NULL,
          PRIMARY KEY (session_id, source_user_seq),
          FOREIGN KEY (session_id, source_user_seq)
            REFERENCES discovery_governor_tasks(session_id, source_user_seq)
            ON DELETE CASCADE
        );

        CREATE TABLE IF NOT EXISTS discovery_governor_roles (
          session_id          TEXT NOT NULL,
          source_user_seq     INTEGER NOT NULL CHECK (source_user_seq > 0),
          role_key            TEXT NOT NULL CHECK (length(role_key) BETWEEN 1 AND 128),
          requirement_index   INTEGER NOT NULL CHECK (requirement_index >= 0),
          requirement_digest  TEXT NOT NULL CHECK (length(requirement_digest) = 64),
          resolved            INTEGER NOT NULL CHECK (resolved IN (0, 1)),
          registered_at       TEXT NOT NULL,
          resolved_at         TEXT,
          PRIMARY KEY (session_id, source_user_seq, role_key),
          UNIQUE (session_id, source_user_seq, requirement_index),
          FOREIGN KEY (session_id, source_user_seq)
            REFERENCES discovery_governor_role_sets(session_id, source_user_seq)
            ON DELETE CASCADE,
          CHECK (
            (resolved = 0 AND resolved_at IS NULL)
            OR (resolved = 1 AND resolved_at IS NOT NULL)
          )
        );

        CREATE INDEX IF NOT EXISTS idx_discovery_governor_roles_open
          ON discovery_governor_roles(session_id, source_user_seq, resolved, requirement_index);

        CREATE TRIGGER IF NOT EXISTS trg_discovery_governor_role_identity_immutable
        BEFORE UPDATE ON discovery_governor_roles
        WHEN OLD.session_id IS NOT NEW.session_id
          OR OLD.source_user_seq IS NOT NEW.source_user_seq
          OR OLD.role_key IS NOT NEW.role_key
          OR OLD.requirement_index IS NOT NEW.requirement_index
          OR OLD.requirement_digest IS NOT NEW.requirement_digest
          OR OLD.registered_at IS NOT NEW.registered_at
          OR NEW.resolved < OLD.resolved
          OR (OLD.resolved = 1 AND OLD.resolved_at IS NOT NEW.resolved_at)
        BEGIN
          SELECT RAISE(ABORT, 'discovery requirement role identity is immutable');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_discovery_governor_role_set_identity_immutable
        BEFORE UPDATE ON discovery_governor_role_sets
        WHEN OLD.session_id IS NOT NEW.session_id
          OR OLD.source_user_seq IS NOT NEW.source_user_seq
          OR OLD.projection_digest IS NOT NEW.projection_digest
          OR OLD.role_count IS NOT NEW.role_count
          OR OLD.initialized_at IS NOT NEW.initialized_at
          OR NEW.unresolved_count > OLD.unresolved_count
        BEGIN
          SELECT RAISE(ABORT, 'discovery requirement role set is immutable or monotonic');
        END;
      `);
        }
      },
      {
        /** Immutable generated-Sheet source/content/readback authority. */
        version: 40,
        sql: "",
        backfill: (db) => {
          db.exec(`
        CREATE TABLE IF NOT EXISTS run_artifacts (
          id TEXT PRIMARY KEY,
          session_id TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
          run_scope_id TEXT NOT NULL, slot_key TEXT NOT NULL, kind TEXT NOT NULL,
          provider TEXT NOT NULL, title TEXT, create_shape TEXT NOT NULL,
          status TEXT NOT NULL CHECK (status IN ('pending','bound','uncertain')),
          resource_id TEXT, uri TEXT, source_call_id TEXT,
          external_write_event_id TEXT, external_write_action_key TEXT,
          external_write_tool_name TEXT, binding_verified_at TEXT,
          verification_call_id TEXT, verification_shape TEXT,
          verification_fingerprint TEXT, created_at TEXT NOT NULL, updated_at TEXT NOT NULL,
          UNIQUE(session_id, run_scope_id, slot_key)
        );
        CREATE INDEX IF NOT EXISTS idx_run_artifacts_session
          ON run_artifacts(session_id, run_scope_id, updated_at DESC);
        CREATE INDEX IF NOT EXISTS idx_run_artifacts_resource
          ON run_artifacts(provider, resource_id);
        CREATE TABLE IF NOT EXISTS artifact_run_scopes (
          session_id TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
          attempt_scope_id TEXT NOT NULL, root_scope_id TEXT NOT NULL,
          source_user_seq INTEGER NOT NULL DEFAULT 0, reason TEXT NOT NULL,
          created_at TEXT NOT NULL, PRIMARY KEY(session_id, attempt_scope_id)
        );
        CREATE INDEX IF NOT EXISTS idx_artifact_run_scopes_user
          ON artifact_run_scopes(session_id, source_user_seq DESC, created_at DESC);
        CREATE TABLE IF NOT EXISTS artifact_source_roots (
          session_id TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
          source_user_seq INTEGER NOT NULL, root_scope_id TEXT NOT NULL,
          created_at TEXT NOT NULL, PRIMARY KEY(session_id, source_user_seq)
        );
        CREATE TABLE IF NOT EXISTS expected_work_source_lineage_identities (
          session_id           TEXT NOT NULL,
          source_user_seq      INTEGER NOT NULL,
          logical_tool_call_id TEXT NOT NULL,
          profile_id           TEXT NOT NULL,
          profile_digest       TEXT NOT NULL CHECK (profile_digest GLOB 'sha256:*'),
          created_at           TEXT NOT NULL,
          PRIMARY KEY (session_id, source_user_seq, logical_tool_call_id),
          FOREIGN KEY (session_id, source_user_seq, logical_tool_call_id)
            REFERENCES expected_work_call_bindings(session_id, source_user_seq, logical_tool_call_id)
            ON DELETE CASCADE
        );
        CREATE TRIGGER IF NOT EXISTS trg_expected_work_source_lineage_identity_immutable
        BEFORE UPDATE ON expected_work_source_lineage_identities
        BEGIN SELECT RAISE(ABORT, 'expected-work source lineage identity is immutable'); END;

        CREATE TABLE IF NOT EXISTS expected_work_generated_artifact_contracts (
          session_id           TEXT NOT NULL,
          source_user_seq      INTEGER NOT NULL,
          logical_tool_call_id TEXT NOT NULL,
          contract_json        TEXT NOT NULL CHECK (json_valid(contract_json)),
          created_at           TEXT NOT NULL,
          PRIMARY KEY (session_id, source_user_seq, logical_tool_call_id),
          FOREIGN KEY (session_id, source_user_seq, logical_tool_call_id)
            REFERENCES expected_work_call_bindings(session_id, source_user_seq, logical_tool_call_id)
            ON DELETE CASCADE
        );
        CREATE TRIGGER IF NOT EXISTS trg_expected_work_generated_artifact_contract_immutable
        BEFORE UPDATE ON expected_work_generated_artifact_contracts
        BEGIN SELECT RAISE(ABORT, 'expected-work generated artifact contract is immutable'); END;

        CREATE TABLE IF NOT EXISTS artifact_content_verifications (
          artifact_id                  TEXT PRIMARY KEY REFERENCES run_artifacts(id) ON DELETE CASCADE,
          session_id                   TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
          run_scope_id                 TEXT NOT NULL,
          create_logical_tool_call_id  TEXT NOT NULL,
          contract_json                TEXT NOT NULL CHECK (json_valid(contract_json)),
          content_verified_at          TEXT,
          verification_logical_call_id TEXT,
          verification_fingerprint     TEXT,
          created_at                   TEXT NOT NULL,
          CHECK ((content_verified_at IS NULL AND verification_logical_call_id IS NULL
                    AND verification_fingerprint IS NULL)
              OR (content_verified_at IS NOT NULL AND verification_logical_call_id IS NOT NULL
                    AND verification_fingerprint IS NOT NULL))
        );
        CREATE INDEX IF NOT EXISTS idx_artifact_content_source
          ON artifact_content_verifications(session_id, run_scope_id, create_logical_tool_call_id);
        CREATE TRIGGER IF NOT EXISTS trg_artifact_content_contract_immutable
        BEFORE UPDATE OF artifact_id, session_id, run_scope_id,
                         create_logical_tool_call_id, contract_json
        ON artifact_content_verifications
        BEGIN SELECT RAISE(ABORT, 'artifact content contract is immutable'); END;
        CREATE TRIGGER IF NOT EXISTS trg_artifact_content_verification_once
        BEFORE UPDATE OF content_verified_at, verification_logical_call_id, verification_fingerprint
        ON artifact_content_verifications
        WHEN OLD.content_verified_at IS NOT NULL AND (
          OLD.content_verified_at IS NOT NEW.content_verified_at
          OR OLD.verification_logical_call_id IS NOT NEW.verification_logical_call_id
          OR OLD.verification_fingerprint IS NOT NEW.verification_fingerprint)
        BEGIN SELECT RAISE(ABORT, 'artifact content verification is immutable'); END;
      `);
        }
      },
      {
        /**
         * Human decisions for autonomous irreversible sends still use the exact
         * approval ledger, but their user surface is an ordinary question rather
         * than a formal approval card. Keep that presentation contract beside the
         * frozen row (not inside execution args) so restart/replay cannot infer it
         * from a later policy setting and exact payload authority stays unchanged.
         */
        version: 41,
        sql: "",
        backfill: (db) => {
          const table = db.prepare(
            `SELECT 1 FROM sqlite_master WHERE type = 'table' AND name = 'pending_approvals'`
          ).get();
          if (!table) return;
          const columns = new Set(
            db.prepare("PRAGMA table_info(pending_approvals)").all().map((column) => column.name)
          );
          if (!columns.has("presentation_json")) {
            db.exec("ALTER TABLE pending_approvals ADD COLUMN presentation_json TEXT");
          }
          if (columns.has("session_id") && columns.has("status")) {
            db.exec(`
          CREATE INDEX IF NOT EXISTS idx_pending_approvals_conversational_surface
            ON pending_approvals(session_id, status)
            WHERE presentation_json IS NOT NULL;
        `);
          }
        }
      },
      {
        /**
         * Freeze clarification-continuation audience and answer interpretation.
         * The task_continuity_packets table is lazy and may not exist yet; add the
         * columns only when present. Existing rows intentionally remain NULL and
         * fail closed rather than being reinterpreted under a newer resolver.
         */
        version: 42,
        sql: "",
        backfill: (db) => {
          const table = db.prepare(
            `SELECT 1 FROM sqlite_master WHERE type = 'table' AND name = 'task_continuity_packets'`
          ).get();
          if (!table) return;
          const columns = new Set(
            db.prepare("PRAGMA table_info(task_continuity_packets)").all().map((column) => column.name)
          );
          for (const [name, type] of [
            ["origin_audience_hash", "TEXT"],
            ["consumer_audience_hash", "TEXT"],
            ["resolver_version", "TEXT"],
            ["resolution_disposition", "TEXT"],
            ["resolution_selected_option", "TEXT"],
            ["resolution_active_task_input", "TEXT"],
            ["resolution_semantic_input_hash", "TEXT"]
          ]) {
            if (!columns.has(name)) {
              db.exec(`ALTER TABLE task_continuity_packets ADD COLUMN ${name} ${type}`);
            }
          }
          db.exec(`
        CREATE TRIGGER IF NOT EXISTS task_continuity_origin_audience_immutable
        BEFORE UPDATE OF origin_audience_hash, consumer_audience_hash ON task_continuity_packets
        FOR EACH ROW
        WHEN OLD.consumer_audience_hash IS NOT NULL
          OR OLD.origin_audience_hash IS NOT NEW.origin_audience_hash
        BEGIN
          SELECT RAISE(ABORT, 'task continuity origin audience is immutable');
        END;

        CREATE TRIGGER IF NOT EXISTS task_continuity_resolution_immutable
        BEFORE UPDATE OF resolver_version, resolution_disposition,
                         resolution_selected_option, resolution_active_task_input,
                         resolution_semantic_input_hash
          ON task_continuity_packets
        FOR EACH ROW
        WHEN OLD.consumed_at IS NOT NULL
        BEGIN
          SELECT RAISE(ABORT, 'task continuity frozen resolution is immutable');
        END;
      `);
        }
      },
      {
        /**
         * Per-source semantic interpretation claim and exact slot identity on
         * continuity packets. The claim table is CAS, not a semantic store.
         */
        version: 43,
        sql: `
      CREATE TABLE IF NOT EXISTS turn_semantics_claims (
        session_id TEXT NOT NULL,
        source_user_seq INTEGER NOT NULL,
        owner TEXT NOT NULL,
        created_at TEXT NOT NULL,
        event_id TEXT,
        PRIMARY KEY (session_id, source_user_seq)
      );
    `,
        backfill: (db) => {
          const table = db.prepare(
            `SELECT 1 FROM sqlite_master WHERE type = 'table' AND name = 'task_continuity_packets'`
          ).get();
          if (!table) return;
          const columns = new Set(
            db.prepare("PRAGMA table_info(task_continuity_packets)").all().map((column) => column.name)
          );
          if (!columns.has("pause_slot_json")) {
            db.exec("ALTER TABLE task_continuity_packets ADD COLUMN pause_slot_json TEXT");
          }
        }
      },
      {
        /**
         * Fence semantic claims by owner token and bind the winning result.
         * A stolen owner cannot persist after the row is replaced.
         */
        version: 44,
        sql: `
      CREATE TABLE IF NOT EXISTS graph_node_leases (
        lease_key TEXT PRIMARY KEY,
        owner TEXT NOT NULL,
        fence INTEGER NOT NULL,
        revision INTEGER NOT NULL,
        expires_at INTEGER NOT NULL,
        released INTEGER NOT NULL DEFAULT 0
      );
      CREATE TABLE IF NOT EXISTS graph_journal_entries (
        session_id TEXT NOT NULL,
        source_user_seq INTEGER NOT NULL,
        seq INTEGER NOT NULL,
        entry_json TEXT NOT NULL,
        PRIMARY KEY (session_id, source_user_seq, seq)
      );
    `,
        backfill: (db) => {
          const columns = new Set(
            db.prepare("PRAGMA table_info(turn_semantics_claims)").all().map((column) => column.name)
          );
          for (const name of ["input_hash", "audience_hash", "policy_revision"]) {
            if (!columns.has(name)) {
              db.exec(`ALTER TABLE turn_semantics_claims ADD COLUMN ${name} TEXT`);
            }
          }
        }
      },
      {
        /**
         * Durable semantic participation, request-scoped catalog snapshots,
         * exact node bindings, and trusted capability manifests. These used
         * to be created ad-hoc at runtime; they are now a contiguous schema.
         */
        version: 45,
        sql: `
      CREATE TABLE IF NOT EXISTS turn_semantics_dispositions (
        session_id TEXT NOT NULL,
        source_user_seq INTEGER NOT NULL,
        participation TEXT NOT NULL,
        outcome TEXT,
        created_at TEXT NOT NULL,
        PRIMARY KEY (session_id, source_user_seq)
      );
      CREATE TABLE IF NOT EXISTS accepted_source_catalog_snapshots (
        session_id TEXT NOT NULL,
        source_user_seq INTEGER NOT NULL,
        snapshot_digest TEXT NOT NULL,
        snapshot_json TEXT NOT NULL,
        PRIMARY KEY (session_id, source_user_seq)
      );
      CREATE TABLE IF NOT EXISTS graph_node_bindings (
        session_id TEXT NOT NULL,
        source_user_seq INTEGER NOT NULL,
        node_id TEXT NOT NULL,
        binding_json TEXT NOT NULL,
        binding_digest TEXT NOT NULL,
        PRIMARY KEY (session_id, source_user_seq, node_id)
      );
      CREATE TABLE IF NOT EXISTS capability_manifests (
        manifest_id TEXT PRIMARY KEY,
        digest TEXT NOT NULL,
        manifest_json TEXT NOT NULL,
        lifecycle TEXT NOT NULL,
        installed_at TEXT NOT NULL
      );
    `
      },
      {
        /**
         * Persist the minted call-authority digest and provider-argument digest
         * with every physical reservation. Replay and settlement must match them.
         */
        version: 46,
        sql: `
      CREATE TABLE IF NOT EXISTS physical_dispatch_authority (
        session_id TEXT NOT NULL,
        source_user_seq INTEGER NOT NULL,
        physical_dispatch_id TEXT NOT NULL,
        authority_digest TEXT NOT NULL,
        provider_argument_digest TEXT NOT NULL,
        PRIMARY KEY (session_id, source_user_seq, physical_dispatch_id)
      );
    `,
        backfill: (db) => {
          const table = db.prepare(
            `SELECT 1 FROM sqlite_master WHERE type = 'table' AND name = 'physical_dispatches'`
          ).get();
          if (!table) return;
          const columns = new Set(
            db.prepare("PRAGMA table_info(physical_dispatches)").all().map((column) => column.name)
          );
          if (!columns.has("authority_digest")) {
            db.exec("ALTER TABLE physical_dispatches ADD COLUMN authority_digest TEXT");
          }
          if (!columns.has("provider_argument_digest")) {
            db.exec("ALTER TABLE physical_dispatches ADD COLUMN provider_argument_digest TEXT");
          }
        }
      },
      {
        /**
         * Persist reconstructable typed call-authority bytes with the reservation.
         * A digest alone cannot be verified after restart.
         */
        version: 47,
        sql: `
      CREATE TABLE IF NOT EXISTS physical_dispatch_authority_payload (
        session_id TEXT NOT NULL,
        source_user_seq INTEGER NOT NULL,
        physical_dispatch_id TEXT NOT NULL,
        authority_digest TEXT NOT NULL,
        provider_argument_digest TEXT NOT NULL,
        authority_json TEXT NOT NULL,
        PRIMARY KEY (session_id, source_user_seq, physical_dispatch_id)
      );
    `,
        backfill: (db) => {
          const table = db.prepare(
            `SELECT 1 FROM sqlite_master WHERE type = 'table' AND name = 'physical_dispatch_authority'`
          ).get();
          if (!table) return;
          const columns = new Set(
            db.prepare("PRAGMA table_info(physical_dispatch_authority)").all().map((column) => column.name)
          );
          if (!columns.has("authority_json")) {
            db.exec("ALTER TABLE physical_dispatch_authority ADD COLUMN authority_json TEXT");
          }
        }
      },
      {
        /**
         * Privacy-safe sealed authority: one digest, one crossing, no plaintext
         * provider arguments. Schema 47 payload rows are not copied forward.
         */
        version: 48,
        sql: `
      CREATE TABLE IF NOT EXISTS physical_dispatch_authority_sealed (
        session_id TEXT NOT NULL,
        source_user_seq INTEGER NOT NULL,
        physical_dispatch_id TEXT NOT NULL,
        authority_digest TEXT NOT NULL,
        provider_argument_digest TEXT NOT NULL,
        observation_digest TEXT NOT NULL,
        sealed_json TEXT NOT NULL,
        byte_length INTEGER NOT NULL,
        retention_class TEXT NOT NULL,
        created_at TEXT NOT NULL,
        argument_cipher TEXT,
        PRIMARY KEY (session_id, source_user_seq, physical_dispatch_id)
      );
      CREATE UNIQUE INDEX IF NOT EXISTS physical_dispatch_authority_sealed_digest
        ON physical_dispatch_authority_sealed (authority_digest);
    `,
        backfill: (db) => {
          ensureAuthorityPrivacySchema(db);
        }
      },
      {
        /**
         * One consequential-crossing kernel. Provider-I/O ownership used to live in
         * a runtime-created `graph_dispatch_io` table with its own state machine,
         * so a reservation and its I/O claim could disagree about who owned the
         * crossing. The claim belongs to the reservation it fences: these columns
         * move it onto `physical_dispatches`, where the exact authority, argument
         * digests and settlement already live.
         *
         * `io_claimed_at IS NULL` means unclaimed and therefore replayable by a
         * legitimate lease takeover; non-NULL means provider I/O was claimed and
         * recovery must reconcile rather than redispatch.
         */
        version: 49,
        sql: "",
        backfill: (db) => {
          const columns = new Set(
            db.prepare("PRAGMA table_info(physical_dispatches)").all().map((column) => column.name)
          );
          if (!columns.size) return;
          if (!columns.has("io_claimed_at")) db.exec("ALTER TABLE physical_dispatches ADD COLUMN io_claimed_at TEXT");
          if (!columns.has("io_owner")) db.exec("ALTER TABLE physical_dispatches ADD COLUMN io_owner TEXT");
          if (!columns.has("io_fence")) db.exec("ALTER TABLE physical_dispatches ADD COLUMN io_fence INTEGER");
          if (!columns.has("io_revision")) db.exec("ALTER TABLE physical_dispatches ADD COLUMN io_revision INTEGER");
          const legacy = db.prepare(
            `SELECT 1 AS ok FROM sqlite_master WHERE type = 'table' AND name = 'graph_dispatch_io'`
          ).get();
          if (!legacy) return;
          const orphaned = db.prepare(`
        SELECT COUNT(*) AS n FROM graph_dispatch_io io
         WHERE io.io_started = 1
           AND NOT EXISTS (
             SELECT 1 FROM physical_dispatches p
              WHERE p.session_id = io.session_id
                AND p.source_user_seq = io.source_user_seq
                AND p.physical_dispatch_id = io.physical_dispatch_id
           )
      `).get();
          if (orphaned.n > 0) {
            throw new Error(
              `schema v49 refuses to drop ${orphaned.n} claimed provider-I/O marker(s) with no physical reservation`
            );
          }
          db.exec(`
        UPDATE physical_dispatches
           SET io_claimed_at = COALESCE(io_claimed_at, started_at),
               io_owner = COALESCE(
                 io_owner,
                 (SELECT io.io_owner FROM graph_dispatch_io io
                   WHERE io.session_id = physical_dispatches.session_id
                     AND io.source_user_seq = physical_dispatches.source_user_seq
                     AND io.physical_dispatch_id = physical_dispatches.physical_dispatch_id)
               ),
               io_fence = COALESCE(
                 io_fence,
                 (SELECT io.io_fence FROM graph_dispatch_io io
                   WHERE io.session_id = physical_dispatches.session_id
                     AND io.source_user_seq = physical_dispatches.source_user_seq
                     AND io.physical_dispatch_id = physical_dispatches.physical_dispatch_id)
               ),
               io_revision = COALESCE(
                 io_revision,
                 (SELECT io.io_revision FROM graph_dispatch_io io
                   WHERE io.session_id = physical_dispatches.session_id
                     AND io.source_user_seq = physical_dispatches.source_user_seq
                     AND io.physical_dispatch_id = physical_dispatches.physical_dispatch_id)
               )
         WHERE EXISTS (
           SELECT 1 FROM graph_dispatch_io io
            WHERE io.session_id = physical_dispatches.session_id
              AND io.source_user_seq = physical_dispatches.source_user_seq
              AND io.physical_dispatch_id = physical_dispatches.physical_dispatch_id
              AND io.io_started = 1
         );

        DROP TABLE graph_dispatch_io;
      `);
        }
      },
      {
        /**
         * Graph-neutral accepted-turn call authority.
         *
         * Foreground host chat must be able to admit bounded read/compute calls
         * without manufacturing a TurnGraphIR. Graph turns retain their exact
         * accepted_task_resolutions and expected-work machinery; both lanes share
         * this one parent and the existing logical/physical/settlement ledgers.
         */
        version: 50,
        foreignKeysOff: true,
        sql: `
      CREATE TABLE IF NOT EXISTS accepted_turn_call_authorities (
        session_id                TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
        source_user_seq           INTEGER NOT NULL CHECK (source_user_seq > 0),
        accepted_task_id          TEXT NOT NULL UNIQUE,
        authority_protocol        INTEGER NOT NULL CHECK (authority_protocol = 1),
        authority_kind            TEXT NOT NULL
                                  CHECK (authority_kind IN ('turn_graph','host_v1_read_only')),
        source_event_id           TEXT NOT NULL REFERENCES events(id) ON DELETE RESTRICT,
        source_event_digest       TEXT NOT NULL CHECK (length(source_event_digest) = 64),
        source_turn               INTEGER NOT NULL CHECK (source_turn >= 0),
        engine_version            TEXT NOT NULL CHECK (length(engine_version) BETWEEN 1 AND 128),
        surface_version           TEXT NOT NULL CHECK (length(surface_version) BETWEEN 1 AND 128),
        surface_digest            TEXT NOT NULL CHECK (length(surface_digest) = 64),
        effect_ceiling            TEXT NOT NULL CHECK (length(effect_ceiling) BETWEEN 1 AND 64),
        effect_bounds_json        TEXT NOT NULL
                                  CHECK (json_valid(effect_bounds_json)
                                    AND json_type(effect_bounds_json) = 'array'),
        max_logical_calls         INTEGER CHECK (max_logical_calls IS NULL OR max_logical_calls > 0),
        max_parallel_calls        INTEGER CHECK (max_parallel_calls IS NULL OR max_parallel_calls > 0),
        catalog_revision_digest   TEXT
                                  CHECK (catalog_revision_digest IS NULL OR length(catalog_revision_digest) = 64),
        binding_revision_digest   TEXT
                                  CHECK (binding_revision_digest IS NULL OR length(binding_revision_digest) = 64),
        graph_event_id            TEXT REFERENCES events(id) ON DELETE RESTRICT,
        graph_hash                TEXT CHECK (graph_hash IS NULL OR length(graph_hash) = 64),
        authority_digest          TEXT NOT NULL UNIQUE CHECK (length(authority_digest) = 64),
        state                     TEXT NOT NULL DEFAULT 'open'
                                  CHECK (state IN ('open','closed','conflict')),
        revision                  INTEGER NOT NULL DEFAULT 0 CHECK (revision >= 0),
        opened_at                 TEXT NOT NULL,
        closed_at                 TEXT,
        close_reason              TEXT CHECK (close_reason IS NULL OR length(close_reason) BETWEEN 1 AND 160),
        PRIMARY KEY (session_id, source_user_seq),
        CHECK (accepted_task_id = 'task:' || session_id || '#' || source_user_seq),
        CHECK (
          (state = 'open' AND closed_at IS NULL AND close_reason IS NULL)
          OR (state IN ('closed','conflict') AND closed_at IS NOT NULL AND close_reason IS NOT NULL)
        ),
        CHECK (
          (authority_kind = 'host_v1_read_only'
            AND engine_version = 'host_v1_read_only'
            AND effect_ceiling = 'read_compute_host_only'
            AND effect_bounds_json = '["compute","host_only","read"]'
            AND max_logical_calls IS NOT NULL
            AND max_parallel_calls IS NOT NULL
            AND max_parallel_calls <= max_logical_calls
            AND catalog_revision_digest IS NOT NULL
            AND binding_revision_digest IS NOT NULL
            AND graph_event_id IS NULL
            AND graph_hash IS NULL)
          OR
          (authority_kind = 'turn_graph'
            AND surface_version = 'turn_graph_ir_v1'
            AND effect_bounds_json = '[]'
            AND catalog_revision_digest IS NULL
            AND graph_event_id IS NOT NULL
            AND graph_hash IS NOT NULL
            AND binding_revision_digest = graph_hash
            AND max_logical_calls IS NULL
            AND max_parallel_calls IS NULL)
        )
      );

      CREATE INDEX IF NOT EXISTS idx_accepted_turn_call_authority_state
        ON accepted_turn_call_authorities(authority_kind, state, opened_at);

      CREATE TRIGGER IF NOT EXISTS trg_accepted_turn_call_authority_source_exact
      BEFORE INSERT ON accepted_turn_call_authorities
      WHEN NOT EXISTS (
        SELECT 1 FROM events e
         WHERE e.id = NEW.source_event_id
           AND e.seq = NEW.source_user_seq
           AND e.session_id = NEW.session_id
           AND e.turn = NEW.source_turn
           AND e.role = 'user'
           AND e.type = 'user_input_received'
      )
      BEGIN
        SELECT RAISE(ABORT, 'accepted-turn call authority requires its exact user source event');
      END;

      CREATE TRIGGER IF NOT EXISTS trg_accepted_turn_call_authority_graph_exact
      BEFORE INSERT ON accepted_turn_call_authorities
      WHEN NEW.authority_kind = 'turn_graph' AND NOT EXISTS (
        SELECT 1 FROM accepted_task_resolutions r
         WHERE r.session_id = NEW.session_id
           AND r.source_user_seq = NEW.source_user_seq
           AND r.accepted_task_id = NEW.accepted_task_id
           AND r.graph_event_id = NEW.graph_event_id
           AND r.graph_hash = NEW.graph_hash
           AND r.compiler_version = NEW.engine_version
           AND r.effect_ceiling = NEW.effect_ceiling
      )
      BEGIN
        SELECT RAISE(ABORT, 'graph call authority requires its exact graph resolution');
      END;

      CREATE TRIGGER IF NOT EXISTS trg_accepted_turn_call_authority_host_graphless
      BEFORE INSERT ON accepted_turn_call_authorities
      WHEN NEW.authority_kind = 'host_v1_read_only' AND EXISTS (
        SELECT 1 FROM accepted_task_resolutions r
         WHERE r.session_id = NEW.session_id
           AND r.source_user_seq = NEW.source_user_seq
      )
      BEGIN
        SELECT RAISE(ABORT, 'host call authority cannot replace graph resolution authority');
      END;

      CREATE TRIGGER IF NOT EXISTS trg_accepted_turn_call_authority_identity_immutable
      BEFORE UPDATE ON accepted_turn_call_authorities
      WHEN OLD.session_id IS NOT NEW.session_id
        OR OLD.source_user_seq IS NOT NEW.source_user_seq
        OR OLD.accepted_task_id IS NOT NEW.accepted_task_id
        OR OLD.authority_protocol IS NOT NEW.authority_protocol
        OR OLD.authority_kind IS NOT NEW.authority_kind
        OR OLD.source_event_id IS NOT NEW.source_event_id
        OR OLD.source_event_digest IS NOT NEW.source_event_digest
        OR OLD.source_turn IS NOT NEW.source_turn
        OR OLD.engine_version IS NOT NEW.engine_version
        OR OLD.surface_version IS NOT NEW.surface_version
        OR OLD.surface_digest IS NOT NEW.surface_digest
        OR OLD.effect_ceiling IS NOT NEW.effect_ceiling
        OR OLD.effect_bounds_json IS NOT NEW.effect_bounds_json
        OR OLD.max_logical_calls IS NOT NEW.max_logical_calls
        OR OLD.max_parallel_calls IS NOT NEW.max_parallel_calls
        OR OLD.catalog_revision_digest IS NOT NEW.catalog_revision_digest
        OR OLD.binding_revision_digest IS NOT NEW.binding_revision_digest
        OR OLD.graph_event_id IS NOT NEW.graph_event_id
        OR OLD.graph_hash IS NOT NEW.graph_hash
        OR OLD.authority_digest IS NOT NEW.authority_digest
        OR OLD.opened_at IS NOT NEW.opened_at
      BEGIN
        SELECT RAISE(ABORT, 'accepted-turn call authority identity is immutable');
      END;

      CREATE TRIGGER IF NOT EXISTS trg_accepted_turn_call_authority_state_machine
      BEFORE UPDATE OF state, revision, closed_at, close_reason
      ON accepted_turn_call_authorities
      WHEN NOT (
        NEW.revision = OLD.revision + 1
        AND (
          (OLD.state = 'open' AND NEW.state IN ('closed','conflict'))
          OR (OLD.state = 'closed' AND NEW.state = 'conflict')
        )
        AND NEW.closed_at IS NOT NULL
        AND NEW.close_reason IS NOT NULL
      )
      BEGIN
        SELECT RAISE(ABORT, 'invalid accepted-turn call authority transition');
      END;
    `,
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) {
            db.exec("DROP TABLE accepted_turn_call_authorities");
            return;
          }
          if (tables.has("accepted_task_resolutions")) {
            db.exec(`
          CREATE TRIGGER IF NOT EXISTS trg_accepted_task_resolution_excludes_host_root
          BEFORE INSERT ON accepted_task_resolutions
          WHEN EXISTS (
            SELECT 1 FROM accepted_turn_call_authorities a
             WHERE a.session_id = NEW.session_id
               AND a.source_user_seq = NEW.source_user_seq
               AND a.authority_kind = 'host_v1_read_only'
          )
          BEGIN
            SELECT RAISE(ABORT, 'graph resolution cannot replace host call authority');
          END;
        `);
            const rows = db.prepare(`
          SELECT r.session_id, r.source_user_seq, r.accepted_task_id,
                 r.graph_event_id, r.graph_hash, r.compiler_version,
                 r.effect_ceiling, r.state, r.opened_at, r.finalized_at,
                 e.id AS source_event_id, e.turn AS source_turn,
                 e.role AS source_role, e.type AS source_type,
                 e.parent_event_id, e.data_json, e.created_at
            FROM accepted_task_resolutions r
            LEFT JOIN events e
              ON e.session_id = r.session_id AND e.seq = r.source_user_seq
           ORDER BY r.session_id, r.source_user_seq
        `).all();
            const insert = db.prepare(`
          INSERT INTO accepted_turn_call_authorities
            (session_id, source_user_seq, accepted_task_id, authority_protocol,
             authority_kind, source_event_id, source_event_digest, source_turn,
             engine_version, surface_version, surface_digest, effect_ceiling,
             effect_bounds_json, max_logical_calls, max_parallel_calls,
             catalog_revision_digest, binding_revision_digest,
             graph_event_id, graph_hash, authority_digest, state, revision,
             opened_at, closed_at, close_reason)
          VALUES (?, ?, ?, 1, 'turn_graph', ?, ?, ?, ?, 'turn_graph_ir_v1', ?, ?,
                  '[]', NULL, NULL, NULL, ?, ?, ?, ?, ?, ?, ?, ?, ?)
        `);
            for (const row of rows) {
              if (!row.source_event_id || row.source_turn === null || row.source_role !== "user" || row.source_type !== "user_input_received" || row.data_json === null || row.created_at === null) {
                throw new Error(
                  `schema v50 graph resolution ${row.session_id}#${row.source_user_seq} lacks its exact user source`
                );
              }
              const acceptedTaskId = `task:${row.session_id}#${row.source_user_seq}`;
              if (row.accepted_task_id !== acceptedTaskId) {
                throw new Error(`schema v50 graph resolution ${row.session_id}#${row.source_user_seq} has an invalid task id`);
              }
              const sourceEventDigest = acceptedTurnSourceEventDigest({
                id: row.source_event_id,
                sessionId: row.session_id,
                seq: row.source_user_seq,
                turn: row.source_turn,
                role: row.source_role,
                type: row.source_type,
                parentEventId: row.parent_event_id,
                dataJson: row.data_json,
                createdAt: row.created_at
              });
              const effectBoundsJson = "[]";
              const surfaceDigest = acceptedTurnCallSurfaceDigest({
                authorityKind: "turn_graph",
                engineVersion: row.compiler_version,
                surfaceVersion: "turn_graph_ir_v1",
                effectCeiling: row.effect_ceiling,
                effectBoundsJson,
                maxLogicalCalls: null,
                maxParallelCalls: null,
                catalogRevisionDigest: null,
                bindingRevisionDigest: row.graph_hash,
                graphEventId: row.graph_event_id,
                graphHash: row.graph_hash
              });
              const authorityDigest = acceptedTurnCallAuthorityDigest({
                authorityKind: "turn_graph",
                sessionId: row.session_id,
                sourceUserSeq: row.source_user_seq,
                acceptedTaskId,
                sourceEventId: row.source_event_id,
                sourceEventDigest,
                sourceTurn: row.source_turn,
                engineVersion: row.compiler_version,
                surfaceVersion: "turn_graph_ir_v1",
                surfaceDigest,
                effectCeiling: row.effect_ceiling,
                effectBoundsJson,
                maxLogicalCalls: null,
                maxParallelCalls: null,
                catalogRevisionDigest: null,
                bindingRevisionDigest: row.graph_hash,
                graphEventId: row.graph_event_id,
                graphHash: row.graph_hash
              });
              const state = row.state === "open" ? "open" : row.state === "finalized" ? "closed" : "conflict";
              const closedAt = state === "open" ? null : row.finalized_at ?? row.opened_at;
              const closeReason = state === "open" ? null : state === "closed" ? "graph_finalized" : "graph_legacy_ambiguous";
              const existing = db.prepare(`
            SELECT authority_digest FROM accepted_turn_call_authorities
             WHERE session_id = ? AND source_user_seq = ?
          `).get(row.session_id, row.source_user_seq);
              if (existing) {
                if (existing.authority_digest !== authorityDigest) {
                  throw new Error(`schema v50 existing call authority conflicts for ${row.session_id}#${row.source_user_seq}`);
                }
                continue;
              }
              insert.run(
                row.session_id,
                row.source_user_seq,
                acceptedTaskId,
                row.source_event_id,
                sourceEventDigest,
                row.source_turn,
                row.compiler_version,
                surfaceDigest,
                row.effect_ceiling,
                row.graph_hash,
                row.graph_event_id,
                row.graph_hash,
                authorityDigest,
                state,
                state === "open" ? 0 : 1,
                row.opened_at,
                closedAt,
                closeReason
              );
            }
          }
          if (tables.has("logical_tool_calls")) {
            const orphaned = db.prepare(`
          SELECT COUNT(*) AS n FROM logical_tool_calls l
           WHERE NOT EXISTS (
             SELECT 1 FROM accepted_turn_call_authorities a
              WHERE a.session_id = l.session_id
                AND a.source_user_seq = l.source_user_seq
                AND a.accepted_task_id = l.accepted_task_id
           )
        `).get().n;
            if (orphaned > 0) {
              throw new Error(`schema v50 refuses ${orphaned} logical call(s) without an accepted-turn authority root`);
            }
            rebuildLogicalToolCallAuthorityParent(db);
          }
        }
      },
      {
        /**
         * A real workflow-node activation can now own the same call kernel without
         * masquerading as a user turn or manufacturing graph topology. Existing
         * graph and host roots are copied byte-for-byte; logical/physical and all
         * settlement children retain their exact identities and foreign keys.
         */
        version: 51,
        foreignKeysOff: true,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) return;
          for (const prerequisite of ["accepted_turn_call_authorities", "logical_tool_calls"]) {
            if (!tables.has(prerequisite)) throw new Error(`schema v51 prerequisite missing: ${prerequisite}`);
          }
          createWorkflowNodeInvocationActivationSchema(db);
          rebuildAcceptedCallAuthoritiesForWorkflow(db);
          db.exec(`
        DROP TRIGGER IF EXISTS trg_accepted_task_resolution_excludes_host_root;
        CREATE TRIGGER trg_accepted_task_resolution_excludes_host_root
        BEFORE INSERT ON accepted_task_resolutions
        WHEN EXISTS (
          SELECT 1 FROM accepted_turn_call_authorities a
           WHERE a.session_id = NEW.session_id
             AND a.source_user_seq = NEW.source_user_seq
             AND a.authority_kind != 'turn_graph'
        )
        BEGIN
          SELECT RAISE(ABORT, 'graph resolution cannot replace non-graph call authority');
        END;
      `);
        }
      },
      {
        /**
         * One durable cursor chain now owns many sequential child calls without
         * minting workflow retries. Page bodies keep using the shared logical,
         * physical, settlement and result-handle tables; only cursor/coverage truth
         * is normalized here.
         */
        version: 52,
        foreignKeysOff: true,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) return;
          for (const prerequisite of [
            "accepted_turn_call_authorities",
            "logical_tool_calls",
            "workflow_node_invocation_activations"
          ]) {
            if (!tables.has(prerequisite)) throw new Error(`schema v52 prerequisite missing: ${prerequisite}`);
          }
          createWorkflowPaginatedReadSchema(db);
          rebuildAcceptedCallAuthoritiesForPagination(db);
          db.exec(`
        DROP TRIGGER IF EXISTS trg_accepted_task_resolution_excludes_host_root;
        CREATE TRIGGER trg_accepted_task_resolution_excludes_host_root
        BEFORE INSERT ON accepted_task_resolutions
        WHEN EXISTS (
          SELECT 1 FROM accepted_turn_call_authorities a
           WHERE a.session_id = NEW.session_id
             AND a.source_user_seq = NEW.source_user_seq
             AND a.authority_kind != 'turn_graph'
        )
        BEGIN
          SELECT RAISE(ABORT, 'graph resolution cannot replace non-graph call authority');
        END;
      `);
        }
      },
      {
        /**
         * Host-owned per-call stopping authority.
         *
         * A run-level dispatch lease could fence a late provider call, but it could
         * not say which accepted model call owned an already-started crossing. That
         * made a host deadline choose between hanging forever and detaching an
         * unidentifiable write. Bind a child generation to the exact logical call,
         * bind every crossing opened beneath it to that generation, and freeze the
         * terminal state/site into new logical settlements. Historical settlements
         * keep their v1 identity-only crossing digest; new settlements use v2.
         */
        version: 53,
        foreignKeysOff: true,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) return;
          for (const prerequisite of [
            "run_dispatch_leases",
            "physical_dispatches",
            "logical_call_settlements",
            "logical_call_settlement_crossings"
          ]) {
            if (!tables.has(prerequisite)) throw new Error(`schema v53 prerequisite missing: ${prerequisite}`);
          }
          const columnsOf = (table) => new Set(
            db.prepare(`PRAGMA table_info(${table})`).all().map((row) => row.name)
          );
          const leaseColumns = columnsOf("run_dispatch_leases");
          if (!leaseColumns.has("source_user_seq")) {
            db.exec("ALTER TABLE run_dispatch_leases ADD COLUMN source_user_seq INTEGER");
          }
          if (!leaseColumns.has("accepted_task_id")) {
            db.exec("ALTER TABLE run_dispatch_leases ADD COLUMN accepted_task_id TEXT");
          }
          if (!leaseColumns.has("logical_tool_call_id")) {
            db.exec("ALTER TABLE run_dispatch_leases ADD COLUMN logical_tool_call_id TEXT");
          }
          if (!leaseColumns.has("recovery_effect")) {
            db.exec(`
          ALTER TABLE run_dispatch_leases ADD COLUMN recovery_effect TEXT
            CHECK (recovery_effect IS NULL OR recovery_effect IN (
              'read','compute','host_only','local_write','external_write','admin','unknown'
            ))
        `);
          }
          if (!leaseColumns.has("recovery_business_call")) {
            db.exec(`
          ALTER TABLE run_dispatch_leases ADD COLUMN recovery_business_call INTEGER
            CHECK (recovery_business_call IS NULL OR recovery_business_call IN (0, 1))
        `);
          }
          if (!leaseColumns.has("recovery_tool_name")) {
            db.exec(`
          ALTER TABLE run_dispatch_leases ADD COLUMN recovery_tool_name TEXT
            CHECK (recovery_tool_name IS NULL OR length(recovery_tool_name) BETWEEN 1 AND 512)
        `);
          }
          if (!leaseColumns.has("recovery_argument_digest")) {
            db.exec(`
          ALTER TABLE run_dispatch_leases ADD COLUMN recovery_argument_digest TEXT
            CHECK (recovery_argument_digest IS NULL OR length(recovery_argument_digest) = 64)
        `);
          }
          if (!leaseColumns.has("recovery_argument_cipher")) {
            db.exec(`
          ALTER TABLE run_dispatch_leases ADD COLUMN recovery_argument_cipher TEXT
            CHECK (recovery_argument_cipher IS NULL OR length(recovery_argument_cipher) BETWEEN 1 AND 48000)
        `);
          }
          if (!leaseColumns.has("recovery_turn")) {
            db.exec(`
          ALTER TABLE run_dispatch_leases ADD COLUMN recovery_turn INTEGER
            CHECK (recovery_turn IS NULL OR recovery_turn > 0)
        `);
          }
          const physicalColumns = columnsOf("physical_dispatches");
          if (!physicalColumns.has("lease_scope_id")) {
            db.exec("ALTER TABLE physical_dispatches ADD COLUMN lease_scope_id TEXT");
          }
          if (!physicalColumns.has("lease_id")) {
            db.exec("ALTER TABLE physical_dispatches ADD COLUMN lease_id TEXT");
          }
          const settlementColumns = columnsOf("logical_call_settlements");
          if (!settlementColumns.has("crossing_authority_version")) {
            db.exec(`
          ALTER TABLE logical_call_settlements
          ADD COLUMN crossing_authority_version INTEGER NOT NULL DEFAULT 1
            CHECK (crossing_authority_version IN (1, 2))
        `);
          }
          const frozenColumns = columnsOf("logical_call_settlement_crossings");
          if (!frozenColumns.has("terminal_state")) {
            db.exec(`
          ALTER TABLE logical_call_settlement_crossings ADD COLUMN terminal_state TEXT
            CHECK (terminal_state IS NULL OR terminal_state IN (
              'returned','threw','timed_out','cancelled','unknown'
            ))
        `);
          }
          if (!frozenColumns.has("execution_site")) {
            db.exec(`
          ALTER TABLE logical_call_settlement_crossings ADD COLUMN execution_site TEXT
            CHECK (execution_site IS NULL OR execution_site = 'host')
        `);
          }
          db.exec(`
        CREATE INDEX IF NOT EXISTS idx_run_dispatch_leases_call_owner
          ON run_dispatch_leases(
            session_id, source_user_seq, accepted_task_id,
            logical_tool_call_id, lease_id, revoked_at
          );
        CREATE INDEX IF NOT EXISTS idx_run_dispatch_leases_host_recovery
          ON run_dispatch_leases(revoked_at, session_id, source_user_seq, logical_tool_call_id)
          WHERE source_user_seq IS NOT NULL;
        CREATE INDEX IF NOT EXISTS idx_physical_dispatches_lease_generation
          ON physical_dispatches(session_id, lease_scope_id, lease_id, state);

        CREATE TRIGGER IF NOT EXISTS trg_run_dispatch_call_recovery_complete_insert
        BEFORE INSERT ON run_dispatch_leases
        WHEN (
          ((NEW.source_user_seq IS NOT NULL)
            + (NEW.accepted_task_id IS NOT NULL)
            + (NEW.logical_tool_call_id IS NOT NULL)) NOT IN (0, 3)
          OR ((NEW.source_user_seq IS NOT NULL) != (NEW.recovery_effect IS NOT NULL))
          OR ((NEW.source_user_seq IS NOT NULL) != (NEW.recovery_business_call IS NOT NULL))
          OR ((NEW.source_user_seq IS NOT NULL) != (NEW.recovery_tool_name IS NOT NULL))
          OR ((NEW.source_user_seq IS NOT NULL) != (NEW.recovery_argument_digest IS NOT NULL))
          OR ((NEW.source_user_seq IS NOT NULL) != (NEW.recovery_argument_cipher IS NOT NULL))
          OR (
            NEW.source_user_seq IS NOT NULL
            AND NOT EXISTS (
              SELECT 1 FROM logical_tool_calls call
               WHERE call.session_id = NEW.session_id
                 AND call.source_user_seq = NEW.source_user_seq
                 AND call.accepted_task_id = NEW.accepted_task_id
                 AND call.logical_tool_call_id = NEW.logical_tool_call_id
                 AND call.tool_name = NEW.recovery_tool_name
                 AND (call.argument_digest = NEW.recovery_argument_digest
                   OR call.raw_argument_digest = NEW.recovery_argument_digest)
            )
          )
        )
        BEGIN
          SELECT RAISE(ABORT, 'call lease requires one frozen recovery contract');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_run_dispatch_call_recovery_immutable
        BEFORE UPDATE OF session_id, lease_id, run_attempt_id,
                         parent_scope_id, parent_lease_id,
                         source_user_seq, accepted_task_id, logical_tool_call_id,
                         recovery_effect, recovery_business_call, recovery_tool_name,
                         recovery_argument_digest, recovery_argument_cipher, recovery_turn,
                         activated_at
        ON run_dispatch_leases
        WHEN OLD.source_user_seq IS NOT NULL OR NEW.source_user_seq IS NOT NULL
        BEGIN
          SELECT RAISE(ABORT, 'call lease identity and recovery contract are immutable');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_physical_dispatch_lease_owner
        BEFORE INSERT ON physical_dispatches
        WHEN (
          (NEW.lease_scope_id IS NULL) != (NEW.lease_id IS NULL)
          OR (
            NEW.lease_scope_id IS NOT NULL
            AND NOT EXISTS (
              SELECT 1 FROM run_dispatch_leases lease
               WHERE lease.session_id = NEW.session_id
                 AND lease.scope_id = NEW.lease_scope_id
                 AND lease.lease_id = NEW.lease_id
                 AND lease.revoked_at IS NULL
                 AND lease.source_user_seq = NEW.source_user_seq
                 AND lease.accepted_task_id = NEW.accepted_task_id
                 AND lease.logical_tool_call_id = NEW.logical_tool_call_id
                 AND lease.recovery_effect IS NOT NULL
                 AND lease.recovery_business_call IS NOT NULL
                 AND lease.recovery_tool_name = NEW.tool_name
                 AND lease.recovery_argument_digest = NEW.argument_digest
            )
          )
        )
        BEGIN
          SELECT RAISE(ABORT, 'physical dispatch requires its exact current call lease');
        END;

        DROP TRIGGER IF EXISTS trg_physical_dispatch_identity_immutable;
        CREATE TRIGGER trg_physical_dispatch_identity_immutable
        BEFORE UPDATE OF accepted_task_id, logical_tool_call_id,
                         physical_dispatch_id, ordinal, relation, retry_of,
                         tool_name, argument_digest, lease_scope_id, lease_id
        ON physical_dispatches
        WHEN OLD.accepted_task_id IS NOT NEW.accepted_task_id
          OR OLD.logical_tool_call_id IS NOT NEW.logical_tool_call_id
          OR OLD.physical_dispatch_id IS NOT NEW.physical_dispatch_id
          OR OLD.ordinal IS NOT NEW.ordinal
          OR OLD.relation IS NOT NEW.relation
          OR OLD.retry_of IS NOT NEW.retry_of
          OR OLD.tool_name IS NOT NEW.tool_name
          OR OLD.argument_digest IS NOT NEW.argument_digest
          OR OLD.lease_scope_id IS NOT NEW.lease_scope_id
          OR OLD.lease_id IS NOT NEW.lease_id
        BEGIN
          SELECT RAISE(ABORT, 'physical dispatch identity is immutable');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_logical_settlement_crossing_update_immutable
        BEFORE UPDATE ON logical_call_settlement_crossings
        BEGIN
          SELECT RAISE(ABORT, 'logical settlement crossings are immutable');
        END;
        CREATE TRIGGER IF NOT EXISTS trg_logical_settlement_crossing_delete_immutable
        BEFORE DELETE ON logical_call_settlement_crossings
        WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
        BEGIN
          SELECT RAISE(ABORT, 'logical settlement crossings are immutable');
        END;
      `);
        }
      },
      {
        /** A distinct effect-capable foreground host authority. V53 call leases,
         * crossings and settlements remain the execution kernel; this migration
         * only teaches their shared accepted-source parent the new closed kind. */
        version: 54,
        foreignKeysOff: true,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) return;
          for (const prerequisite of ["accepted_turn_call_authorities", "logical_tool_calls"]) {
            if (!tables.has(prerequisite)) throw new Error(`schema v54 prerequisite missing: ${prerequisite}`);
          }
          rebuildAcceptedCallAuthoritiesForProductionHost(db);
          db.exec("DROP TRIGGER IF EXISTS trg_logical_call_contract_refinement_once");
          createHostAwareLogicalRefinementTrigger(db);
        }
      },
      {
        /** Preserve historical user decisions while giving daemon/session cleanup
         * a truthful terminal outcome of its own. This is an enum-only widening;
         * no existing approval row is reclassified. */
        version: 55,
        foreignKeysOff: true,
        sql: "",
        backfill: widenPendingApprovalSystemCancellationResolution
      },
      {
        /**
         * A settled foreground plan may add graph topology/evidence beneath the
         * already-open host_v1 call root. The root itself is never rewritten or
         * replaced. One immutable delivery receipt plus the exact local plan
         * settlement/result is required before a resolution row can coexist.
         */
        version: 56,
        foreignKeysOff: true,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) return;
          for (const prerequisite of [
            "accepted_turn_call_authorities",
            "accepted_task_authority",
            "accepted_task_work_contracts",
            "accepted_task_resolutions",
            "logical_tool_calls",
            "logical_call_settlements",
            "logical_call_settlement_crossings",
            "physical_dispatches",
            "durable_result_handles"
          ]) {
            if (!tables.has(prerequisite)) throw new Error(`schema v56 prerequisite missing: ${prerequisite}`);
          }
          createHostPlannedResolutionCoexistenceSchema(db);
          db.exec(`
        DROP TRIGGER IF EXISTS trg_accepted_task_resolution_excludes_host_root;
        CREATE TRIGGER trg_accepted_task_resolution_excludes_host_root
        BEFORE INSERT ON accepted_task_resolutions
        WHEN EXISTS (
          SELECT 1 FROM accepted_turn_call_authorities a
           WHERE a.session_id = NEW.session_id
             AND a.source_user_seq = NEW.source_user_seq
             AND a.authority_kind != 'turn_graph'
        ) AND NOT ${hostPlannedResolutionProofSql("NEW", "insert", "legacy_success")}
        BEGIN
          SELECT RAISE(ABORT, 'graph resolution cannot replace non-graph call authority');
        END;
      `);
        }
      },
      {
        /**
         * Foreground host calls previously carried their exact capability envelope
         * only in process. Freeze it after logical admission and before any child
         * lease/body so restart, replay and terminal proof reopen the same provider
         * account/schema/tool/argument authority without inventing graph topology.
         */
        version: 57,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) return;
          for (const prerequisite of [
            "accepted_turn_call_authorities",
            "logical_tool_calls"
          ]) {
            if (!tables.has(prerequisite)) throw new Error(`schema v57 prerequisite missing: ${prerequisite}`);
          }
          createV57HostCallCapabilityBindingSchema(db);
          refreshPlanTaskActivationReceiptInsertTrigger(db);
          const relevantTables = foreignKeyClosure(db, [
            "accepted_turn_call_authorities",
            "logical_tool_calls",
            "host_call_capability_bindings"
          ]);
          const violations = db.pragma("foreign_key_check").filter((violation) => relevantTables.has(violation.table));
          if (violations.length > 0) {
            throw new Error(`schema v57 foreign-key check failed for ${violations.length} host-call binding row(s)`);
          }
          const integrity = db.pragma("integrity_check");
          if (integrity.length !== 1 || integrity[0]?.integrity_check !== "ok") {
            throw new Error(`schema v57 integrity check failed: ${JSON.stringify(integrity).slice(0, 240)}`);
          }
        }
      },
      {
        /**
         * A call lease is frozen before a trusted resolver materializes optional
         * schema fields, while the physical row correctly names the logical call's
         * one immutable effective digest. The v53 trigger accepted only byte-equal
         * recovery/physical digests, so exact local host execution could complete
         * without its evidence crossing whenever materialization refined the
         * contract. Admit only the durable row's own verified raw -> effective
         * chain; every unrelated digest, tool, call, lease, or later refinement
         * remains outside the trigger proof.
         */
        version: 58,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) return;
          for (const prerequisite of [
            "run_dispatch_leases",
            "logical_tool_calls",
            "physical_dispatches"
          ]) {
            if (!tables.has(prerequisite)) throw new Error(`schema v58 prerequisite missing: ${prerequisite}`);
          }
          db.exec(`
        DROP TRIGGER IF EXISTS trg_physical_dispatch_lease_owner;
        CREATE TRIGGER trg_physical_dispatch_lease_owner
        BEFORE INSERT ON physical_dispatches
        WHEN (
          (NEW.lease_scope_id IS NULL) != (NEW.lease_id IS NULL)
          OR (
            NEW.lease_scope_id IS NOT NULL
            AND NOT EXISTS (
              SELECT 1 FROM run_dispatch_leases lease
               WHERE lease.session_id = NEW.session_id
                 AND lease.scope_id = NEW.lease_scope_id
                 AND lease.lease_id = NEW.lease_id
                 AND lease.revoked_at IS NULL
                 AND lease.source_user_seq = NEW.source_user_seq
                 AND lease.accepted_task_id = NEW.accepted_task_id
                 AND lease.logical_tool_call_id = NEW.logical_tool_call_id
                 AND lease.recovery_effect IS NOT NULL
                 AND lease.recovery_business_call IS NOT NULL
                 AND lease.recovery_tool_name = NEW.tool_name
                 AND (
                   lease.recovery_argument_digest = NEW.argument_digest
                   OR EXISTS (
                     SELECT 1 FROM logical_tool_calls call
                      WHERE call.session_id = NEW.session_id
                        AND call.source_user_seq = NEW.source_user_seq
                        AND call.accepted_task_id = NEW.accepted_task_id
                        AND call.logical_tool_call_id = NEW.logical_tool_call_id
                        AND call.tool_name = NEW.tool_name
                        AND call.state = 'open'
                        AND call.raw_argument_digest = lease.recovery_argument_digest
                        AND call.effective_argument_digest IS NOT NULL
                        AND call.effective_argument_digest != call.raw_argument_digest
                        AND call.argument_digest = call.effective_argument_digest
                        AND NEW.argument_digest = call.effective_argument_digest
                   )
                 )
            )
          )
        )
        BEGIN
          SELECT RAISE(ABORT, 'physical dispatch requires its exact current call lease');
        END;
      `);
          const integrity = db.pragma("integrity_check");
          if (integrity.length !== 1 || integrity[0]?.integrity_check !== "ok") {
            throw new Error(`schema v58 integrity check failed: ${JSON.stringify(integrity).slice(0, 240)}`);
          }
        }
      },
      {
        /**
         * Durable, source-bound model/tool batch checkpoints for foreground host
         * turns.  A model frame is admitted before any tool body can start; the
         * balanced checkpoint is a separate append-only row written only after the
         * frame can be reconstructed from durable call evidence.  This two-record
         * protocol closes both restart windows without turning a process-local
         * history array into execution authority.
         *
         * The schema is deliberately capability-neutral.  It binds the same
         * accepted-turn call root, optional graph and immutable work contract used
         * by the execution kernel; no provider, connector or business-operation
         * names appear here.
         */
        version: 59,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) return;
          for (const prerequisite of [
            "accepted_turn_call_authorities",
            "accepted_task_resolutions",
            "accepted_task_work_contracts"
          ]) {
            if (!tables.has(prerequisite)) throw new Error(`schema v59 prerequisite missing: ${prerequisite}`);
          }
          db.exec(`
        CREATE TABLE IF NOT EXISTS accepted_model_batch_admissions (
          session_id                 TEXT NOT NULL,
          source_user_seq            INTEGER NOT NULL CHECK (source_user_seq > 0),
          accepted_task_id           TEXT NOT NULL,
          batch_ordinal              INTEGER NOT NULL CHECK (batch_ordinal > 0),
          batch_id                   TEXT NOT NULL UNIQUE CHECK (length(batch_id) = 64),
          protocol_version           INTEGER NOT NULL CHECK (protocol_version = 1),
          authority_digest           TEXT NOT NULL CHECK (length(authority_digest) = 64),
          source_event_digest        TEXT NOT NULL CHECK (length(source_event_digest) = 64),
          source_turn                INTEGER NOT NULL CHECK (source_turn >= 0),
          engine_version             TEXT NOT NULL CHECK (length(engine_version) BETWEEN 1 AND 128),
          graph_event_id             TEXT REFERENCES events(id) ON DELETE RESTRICT,
          graph_hash                 TEXT CHECK (graph_hash IS NULL OR length(graph_hash) = 64),
          work_contract_id           TEXT CHECK (work_contract_id IS NULL OR length(work_contract_id) = 81),
          previous_response_id       TEXT CHECK (
                                       previous_response_id IS NULL
                                       OR length(previous_response_id) BETWEEN 1 AND 512
                                     ),
          provider_response_id       TEXT CHECK (
                                       provider_response_id IS NULL
                                       OR length(provider_response_id) BETWEEN 1 AND 512
                                     ),
          accepted_response_digest   TEXT NOT NULL CHECK (length(accepted_response_digest) = 64),
          pre_history_json           TEXT NOT NULL CHECK (
                                       json_valid(pre_history_json)
                                       AND json_type(pre_history_json) = 'array'
                                     ),
          pre_history_digest         TEXT NOT NULL CHECK (length(pre_history_digest) = 64),
          pre_history_item_count     INTEGER NOT NULL CHECK (pre_history_item_count >= 0),
          frame_history_json         TEXT NOT NULL CHECK (
                                       json_valid(frame_history_json)
                                       AND json_type(frame_history_json) = 'array'
                                     ),
          frame_history_digest       TEXT NOT NULL CHECK (length(frame_history_digest) = 64),
          frame_history_item_count   INTEGER NOT NULL CHECK (frame_history_item_count > 0),
          call_ids_json              TEXT NOT NULL CHECK (
                                       json_valid(call_ids_json)
                                       AND json_type(call_ids_json) = 'array'
                                     ),
          call_count                 INTEGER NOT NULL CHECK (call_count > 0),
          admitted_at                TEXT NOT NULL,
          PRIMARY KEY (session_id, source_user_seq, batch_ordinal),
          UNIQUE (session_id, source_user_seq, batch_ordinal, batch_id),
          FOREIGN KEY (session_id, source_user_seq)
            REFERENCES accepted_turn_call_authorities(session_id, source_user_seq)
            ON DELETE CASCADE,
          CHECK (
            (graph_event_id IS NULL AND graph_hash IS NULL AND work_contract_id IS NULL)
            OR
            (graph_event_id IS NOT NULL AND graph_hash IS NOT NULL AND work_contract_id IS NOT NULL)
          )
        );

        CREATE TABLE IF NOT EXISTS accepted_model_batch_checkpoints (
          session_id                 TEXT NOT NULL,
          source_user_seq            INTEGER NOT NULL CHECK (source_user_seq > 0),
          accepted_task_id           TEXT NOT NULL,
          batch_ordinal              INTEGER NOT NULL CHECK (batch_ordinal > 0),
          batch_id                   TEXT NOT NULL UNIQUE CHECK (length(batch_id) = 64),
          protocol_version           INTEGER NOT NULL CHECK (protocol_version = 1),
          authority_digest           TEXT NOT NULL CHECK (length(authority_digest) = 64),
          graph_event_id             TEXT REFERENCES events(id) ON DELETE RESTRICT,
          graph_hash                 TEXT CHECK (graph_hash IS NULL OR length(graph_hash) = 64),
          work_contract_id           TEXT CHECK (work_contract_id IS NULL OR length(work_contract_id) = 81),
          disposition                TEXT NOT NULL
                                     CHECK (disposition IN ('ready','reconciliation_required')),
          history_json               TEXT NOT NULL CHECK (
                                       json_valid(history_json)
                                       AND json_type(history_json) = 'array'
                                     ),
          history_digest             TEXT NOT NULL CHECK (length(history_digest) = 64),
          history_item_count         INTEGER NOT NULL CHECK (history_item_count > 0),
          last_response_id           TEXT CHECK (
                                       last_response_id IS NULL
                                       OR length(last_response_id) BETWEEN 1 AND 512
                                     ),
          committed_at               TEXT NOT NULL,
          PRIMARY KEY (session_id, source_user_seq, batch_ordinal),
          FOREIGN KEY (session_id, source_user_seq, batch_ordinal, batch_id)
            REFERENCES accepted_model_batch_admissions(
              session_id, source_user_seq, batch_ordinal, batch_id
            ) ON DELETE CASCADE,
          CHECK (
            (graph_event_id IS NULL AND graph_hash IS NULL AND work_contract_id IS NULL)
            OR
            (graph_event_id IS NOT NULL AND graph_hash IS NOT NULL AND work_contract_id IS NOT NULL)
          )
        );

        CREATE INDEX IF NOT EXISTS idx_accepted_model_batch_admissions_source
          ON accepted_model_batch_admissions(session_id, source_user_seq, batch_ordinal);
        CREATE INDEX IF NOT EXISTS idx_accepted_model_batch_checkpoints_source
          ON accepted_model_batch_checkpoints(session_id, source_user_seq, batch_ordinal);

        CREATE TRIGGER IF NOT EXISTS trg_accepted_model_batch_admission_exact_root
        BEFORE INSERT ON accepted_model_batch_admissions
        WHEN NOT EXISTS (
          SELECT 1 FROM accepted_turn_call_authorities root
           WHERE root.session_id = NEW.session_id
             AND root.source_user_seq = NEW.source_user_seq
             AND root.accepted_task_id = NEW.accepted_task_id
             AND root.authority_digest = NEW.authority_digest
             AND root.source_event_digest = NEW.source_event_digest
             AND root.source_turn = NEW.source_turn
             AND root.engine_version = NEW.engine_version
             AND root.authority_kind IN ('host_v1','host_v1_read_only')
             AND root.state = 'open'
        )
        BEGIN
          SELECT RAISE(ABORT, 'model batch admission requires its exact open host call root');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_accepted_model_batch_admission_exact_graph_contract
        BEFORE INSERT ON accepted_model_batch_admissions
        WHEN NOT (
          (
            NEW.graph_event_id IS NULL
            AND NEW.graph_hash IS NULL
            AND NEW.work_contract_id IS NULL
            AND NOT EXISTS (
              SELECT 1 FROM accepted_task_resolutions resolution
               WHERE resolution.session_id = NEW.session_id
                 AND resolution.source_user_seq = NEW.source_user_seq
            )
          )
          OR
          EXISTS (
            SELECT 1
              FROM accepted_task_resolutions resolution
              JOIN accepted_task_work_contracts contract
                ON contract.session_id = resolution.session_id
               AND contract.source_user_seq = resolution.source_user_seq
               AND contract.accepted_task_id = resolution.accepted_task_id
               AND contract.graph_event_id = resolution.graph_event_id
               AND contract.graph_hash = resolution.graph_hash
             WHERE resolution.session_id = NEW.session_id
               AND resolution.source_user_seq = NEW.source_user_seq
               AND resolution.accepted_task_id = NEW.accepted_task_id
               AND resolution.graph_event_id = NEW.graph_event_id
               AND resolution.graph_hash = NEW.graph_hash
               AND resolution.state != 'legacy_ambiguous'
               AND contract.contract_id = NEW.work_contract_id
          )
        )
        BEGIN
          SELECT RAISE(ABORT, 'model batch admission graph/contract binding is not exact');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_accepted_model_batch_admission_chain
        BEFORE INSERT ON accepted_model_batch_admissions
        WHEN NOT (
          (
            NEW.batch_ordinal = 1
            AND NOT EXISTS (
              SELECT 1 FROM accepted_model_batch_admissions prior
               WHERE prior.session_id = NEW.session_id
                 AND prior.source_user_seq = NEW.source_user_seq
            )
          )
          OR
          EXISTS (
            SELECT 1
              FROM accepted_model_batch_checkpoints prior
             WHERE prior.session_id = NEW.session_id
               AND prior.source_user_seq = NEW.source_user_seq
               AND prior.batch_ordinal = NEW.batch_ordinal - 1
               AND prior.history_digest = NEW.pre_history_digest
               AND prior.last_response_id IS NEW.previous_response_id
               AND prior.authority_digest = NEW.authority_digest
               AND (
                 (
                   prior.graph_event_id IS NEW.graph_event_id
                   AND prior.graph_hash IS NEW.graph_hash
                   AND prior.work_contract_id IS NEW.work_contract_id
                 )
                 OR
                 (
                   prior.graph_event_id IS NULL
                   AND prior.graph_hash IS NULL
                   AND prior.work_contract_id IS NULL
                   AND NEW.graph_event_id IS NOT NULL
                   AND NEW.graph_hash IS NOT NULL
                   AND NEW.work_contract_id IS NOT NULL
                   AND EXISTS (
                     SELECT 1
                       FROM accepted_task_resolutions r
                       JOIN accepted_task_work_contracts contract
                         ON contract.session_id = r.session_id
                        AND contract.source_user_seq = r.source_user_seq
                        AND contract.accepted_task_id = r.accepted_task_id
                        AND contract.graph_event_id = r.graph_event_id
                        AND contract.graph_hash = r.graph_hash
                      WHERE r.session_id = NEW.session_id
                        AND r.source_user_seq = NEW.source_user_seq
                        AND r.accepted_task_id = NEW.accepted_task_id
                        AND r.graph_event_id = NEW.graph_event_id
                        AND r.graph_hash = NEW.graph_hash
                        AND contract.contract_id = NEW.work_contract_id
                        AND ${hostPlannedResolutionProofSql("r", "existing", "legacy_success")}
                   )
                 )
               )
          )
        )
        BEGIN
          SELECT RAISE(ABORT, 'model batch admission requires the exact prior balanced checkpoint');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_accepted_model_batch_admission_immutable
        BEFORE UPDATE ON accepted_model_batch_admissions
        BEGIN
          SELECT RAISE(ABORT, 'model batch admissions are append-only');
        END;
        CREATE TRIGGER IF NOT EXISTS trg_accepted_model_batch_admission_delete_immutable
        BEFORE DELETE ON accepted_model_batch_admissions
        WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
        BEGIN
          SELECT RAISE(ABORT, 'model batch admissions are append-only');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_accepted_model_batch_checkpoint_exact_admission
        BEFORE INSERT ON accepted_model_batch_checkpoints
        WHEN NOT EXISTS (
          SELECT 1 FROM accepted_model_batch_admissions admission
           WHERE admission.session_id = NEW.session_id
             AND admission.source_user_seq = NEW.source_user_seq
             AND admission.accepted_task_id = NEW.accepted_task_id
             AND admission.batch_ordinal = NEW.batch_ordinal
             AND admission.batch_id = NEW.batch_id
             AND admission.protocol_version = NEW.protocol_version
             AND admission.authority_digest = NEW.authority_digest
             AND NEW.last_response_id IS COALESCE(
                   admission.provider_response_id,
                   admission.previous_response_id
                 )
             AND (
               (
                 admission.graph_event_id IS NEW.graph_event_id
                 AND admission.graph_hash IS NEW.graph_hash
                 AND admission.work_contract_id IS NEW.work_contract_id
               )
               OR
               (
                 admission.graph_event_id IS NULL
                 AND admission.graph_hash IS NULL
                 AND admission.work_contract_id IS NULL
                 AND NEW.graph_event_id IS NOT NULL
                 AND NEW.graph_hash IS NOT NULL
                 AND NEW.work_contract_id IS NOT NULL
               )
             )
        )
        BEGIN
          SELECT RAISE(ABORT, 'model batch checkpoint requires its exact immutable admission');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_accepted_model_batch_checkpoint_exact_root
        BEFORE INSERT ON accepted_model_batch_checkpoints
        WHEN NOT EXISTS (
          SELECT 1 FROM accepted_turn_call_authorities root
           WHERE root.session_id = NEW.session_id
             AND root.source_user_seq = NEW.source_user_seq
             AND root.accepted_task_id = NEW.accepted_task_id
             AND root.authority_digest = NEW.authority_digest
             AND root.authority_kind IN ('host_v1','host_v1_read_only')
             AND root.state = 'open'
        )
        BEGIN
          SELECT RAISE(ABORT, 'model batch checkpoint requires its exact open host call root');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_accepted_model_batch_checkpoint_exact_graph_contract
        BEFORE INSERT ON accepted_model_batch_checkpoints
        WHEN NOT (
          (
            NEW.graph_event_id IS NULL
            AND NEW.graph_hash IS NULL
            AND NEW.work_contract_id IS NULL
            AND NOT EXISTS (
              SELECT 1 FROM accepted_task_resolutions resolution
               WHERE resolution.session_id = NEW.session_id
                 AND resolution.source_user_seq = NEW.source_user_seq
            )
          )
          OR
          EXISTS (
            SELECT 1
              FROM accepted_task_resolutions resolution
              JOIN accepted_task_work_contracts contract
                ON contract.session_id = resolution.session_id
               AND contract.source_user_seq = resolution.source_user_seq
               AND contract.accepted_task_id = resolution.accepted_task_id
               AND contract.graph_event_id = resolution.graph_event_id
               AND contract.graph_hash = resolution.graph_hash
             WHERE resolution.session_id = NEW.session_id
               AND resolution.source_user_seq = NEW.source_user_seq
               AND resolution.accepted_task_id = NEW.accepted_task_id
               AND resolution.graph_event_id = NEW.graph_event_id
               AND resolution.graph_hash = NEW.graph_hash
               AND resolution.state != 'legacy_ambiguous'
               AND contract.contract_id = NEW.work_contract_id
          )
        )
        BEGIN
          SELECT RAISE(ABORT, 'model batch checkpoint graph/contract binding is not exact');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_accepted_model_batch_checkpoint_immutable
        BEFORE UPDATE ON accepted_model_batch_checkpoints
        BEGIN
          SELECT RAISE(ABORT, 'model batch checkpoints are append-only');
        END;
        CREATE TRIGGER IF NOT EXISTS trg_accepted_model_batch_checkpoint_delete_immutable
        BEFORE DELETE ON accepted_model_batch_checkpoints
        WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
        BEGIN
          SELECT RAISE(ABORT, 'model batch checkpoints are append-only');
        END;
      `);
          const relevantTables = foreignKeyClosure(db, [
            "accepted_model_batch_admissions",
            "accepted_model_batch_checkpoints"
          ]);
          const violations = db.pragma("foreign_key_check").filter((violation) => relevantTables.has(violation.table));
          if (violations.length > 0) {
            throw new Error(`schema v59 foreign-key check failed for ${violations.length} model-batch row(s)`);
          }
          const integrity = db.pragma("integrity_check");
          if (integrity.length !== 1 || integrity[0]?.integrity_check !== "ok") {
            throw new Error(`schema v59 integrity check failed: ${JSON.stringify(integrity).slice(0, 240)}`);
          }
        }
      },
      {
        // Durable, audience-keyed conversational head plus immutable accepted-
        // source selection. Selection and pointer advancement occur inside one
        // IMMEDIATE transaction; a provider retry reopens its exact binding.
        version: 60,
        sql: `
      CREATE TABLE IF NOT EXISTS accepted_source_session_pointers (
        -- Opaque lineage key. It intentionally is not a sessions FK: a TTL
        -- reap of the historical parent must not strand a live successor.
        root_session_id TEXT NOT NULL,
        continuity_digest TEXT NOT NULL
          CHECK (length(continuity_digest) = 64 AND continuity_digest NOT GLOB '*[^0-9a-f]*'),
        head_session_id TEXT NOT NULL REFERENCES sessions(id) ON DELETE RESTRICT,
        revision INTEGER NOT NULL DEFAULT 0 CHECK (revision >= 0),
        updated_at TEXT NOT NULL,
        PRIMARY KEY (root_session_id, continuity_digest)
      );

      CREATE TABLE IF NOT EXISTS accepted_source_session_bindings (
        durable_source_digest TEXT PRIMARY KEY
          CHECK (length(durable_source_digest) = 64 AND durable_source_digest NOT GLOB '*[^0-9a-f]*'),
        root_session_id TEXT NOT NULL,
        continuity_digest TEXT NOT NULL,
        session_id TEXT NOT NULL REFERENCES sessions(id) ON DELETE RESTRICT,
        disposition TEXT NOT NULL
          CHECK (disposition IN ('reused','branched','identity_split','bound_control')),
        selected_after_seq INTEGER NOT NULL CHECK (selected_after_seq >= 0),
        created_at TEXT NOT NULL,
        FOREIGN KEY (root_session_id, continuity_digest)
          REFERENCES accepted_source_session_pointers(root_session_id, continuity_digest)
          ON DELETE RESTRICT
      );

      CREATE INDEX IF NOT EXISTS idx_accepted_source_session_bindings_session
        ON accepted_source_session_bindings(session_id, created_at DESC);

      CREATE TRIGGER IF NOT EXISTS trg_accepted_source_session_binding_immutable
      BEFORE UPDATE ON accepted_source_session_bindings
      BEGIN
        SELECT RAISE(ABORT, 'accepted source session bindings are immutable');
      END;

      CREATE TRIGGER IF NOT EXISTS trg_accepted_source_session_pointer_identity_immutable
      BEFORE UPDATE ON accepted_source_session_pointers
      WHEN OLD.root_session_id != NEW.root_session_id
        OR OLD.continuity_digest != NEW.continuity_digest
      BEGIN
        SELECT RAISE(ABORT, 'accepted source session pointer identity is immutable');
      END;
    `
      },
      {
        /**
         * Staged provider-file authority and crash-safe physical returns.
         *
         * A transfer side effect is never hidden inside the parent's provider
         * call. Every source read, presign, object-store transfer, business POST,
         * download and local commit owns an exact logical/physical/stage tuple.
         * Only the business stage may reuse the parent logical call; every other
         * stage is a control dependency that is forbidden from binding or
         * discharging expected work.
         *
         * Provider return bytes remain outside SQLite in an authenticated encrypted
         * spill. The checkpoint stores only digests/counts and is valid only beside
         * its exact returned physical crossing. It is forensic recovery authority,
         * not a result handle or a second success settlement.
         */
        version: 61,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) return;
          for (const prerequisite of [
            "logical_tool_calls",
            "physical_dispatches",
            "logical_call_settlements",
            "expected_work_call_bindings",
            "host_call_capability_bindings",
            "capability_manifests",
            "run_dispatch_leases",
            "pending_approvals"
          ]) {
            if (!tables.has(prerequisite)) {
              throw new Error(`schema v61 prerequisite missing: ${prerequisite}`);
            }
          }
          db.exec(`
        CREATE TABLE IF NOT EXISTS staged_transfer_plans (
          plan_id                    TEXT PRIMARY KEY
                                     CHECK (length(plan_id) BETWEEN 1 AND 256),
          session_id                 TEXT NOT NULL,
          source_user_seq            INTEGER NOT NULL CHECK (source_user_seq > 0),
          accepted_task_id           TEXT NOT NULL,
          parent_logical_tool_call_id TEXT NOT NULL,
          parent_tool_name           TEXT NOT NULL CHECK (length(parent_tool_name) BETWEEN 1 AND 512),
          parent_argument_digest     TEXT NOT NULL
                                     CHECK (length(parent_argument_digest) = 64
                                       AND parent_argument_digest NOT GLOB '*[^0-9a-f]*'),
          parent_effect_kind         TEXT NOT NULL CHECK (parent_effect_kind IN (
                                       'read','compute','local_write','external_write','admin'
                                     )),
          expected_work_contract_id  TEXT NOT NULL CHECK (length(expected_work_contract_id) BETWEEN 1 AND 256),
          expected_work_requirement_id TEXT NOT NULL CHECK (length(expected_work_requirement_id) BETWEEN 1 AND 256),
          expected_work_binding_digest TEXT NOT NULL
                                     CHECK (length(expected_work_binding_digest) = 64
                                       AND expected_work_binding_digest NOT GLOB '*[^0-9a-f]*'),
          host_durable_binding_digest TEXT NOT NULL
                                     CHECK (length(host_durable_binding_digest) = 64
                                       AND host_durable_binding_digest NOT GLOB '*[^0-9a-f]*'),
          host_capability_id          TEXT NOT NULL CHECK (length(host_capability_id) BETWEEN 1 AND 512),
          host_account_id             TEXT NOT NULL CHECK (length(host_account_id) BETWEEN 1 AND 512),
          host_invoke_port_id         TEXT NOT NULL CHECK (length(host_invoke_port_id) BETWEEN 1 AND 512),
          host_operation_id           TEXT NOT NULL CHECK (length(host_operation_id) BETWEEN 1 AND 512),
          host_provider_input_schema_digest TEXT NOT NULL
                                     CHECK (length(host_provider_input_schema_digest) = 64
                                       AND host_provider_input_schema_digest NOT GLOB '*[^0-9a-f]*'),
          host_schema_fingerprint     TEXT NOT NULL
                                     CHECK (length(host_schema_fingerprint) = 64
                                       AND host_schema_fingerprint NOT GLOB '*[^0-9a-f]*'),
          host_definition_fingerprint TEXT NOT NULL
                                     CHECK (length(host_definition_fingerprint) = 64
                                       AND host_definition_fingerprint NOT GLOB '*[^0-9a-f]*'),
          host_manifest_id            TEXT NOT NULL CHECK (length(host_manifest_id) BETWEEN 1 AND 512),
          host_manifest_digest        TEXT NOT NULL
                                     CHECK (length(host_manifest_digest) = 64
                                       AND host_manifest_digest NOT GLOB '*[^0-9a-f]*'),
          input_schema_digest        TEXT NOT NULL
                                     CHECK (length(input_schema_digest) = 64
                                       AND input_schema_digest NOT GLOB '*[^0-9a-f]*'),
          output_schema_digest       TEXT
                                     CHECK (output_schema_digest IS NULL OR (
                                       length(output_schema_digest) = 64
                                       AND output_schema_digest NOT GLOB '*[^0-9a-f]*'
                                     )),
          operation_version          TEXT NOT NULL CHECK (length(operation_version) BETWEEN 1 AND 160),
          toolkit_slug               TEXT NOT NULL CHECK (length(toolkit_slug) BETWEEN 1 AND 160),
          account_identity_digest    TEXT NOT NULL
                                     CHECK (length(account_identity_digest) = 64
                                       AND account_identity_digest NOT GLOB '*[^0-9a-f]*'),
          transfer_manifest_digest   TEXT NOT NULL
                                     CHECK (length(transfer_manifest_digest) = 64
                                       AND transfer_manifest_digest NOT GLOB '*[^0-9a-f]*'),
          manifest_binding_digest    TEXT NOT NULL
                                     CHECK (length(manifest_binding_digest) = 64
                                       AND manifest_binding_digest NOT GLOB '*[^0-9a-f]*'),
          manifest_payload_id        TEXT NOT NULL UNIQUE
                                     CHECK (manifest_payload_id GLOB 'authority-payload:*'),
          manifest_format            TEXT NOT NULL
                                     CHECK (manifest_format = 'staged_transfer_manifest_v1'),
          manifest_plaintext_sha256  TEXT NOT NULL
                                     CHECK (length(manifest_plaintext_sha256) = 64
                                       AND manifest_plaintext_sha256 NOT GLOB '*[^0-9a-f]*'),
          manifest_plaintext_bytes   INTEGER NOT NULL CHECK (manifest_plaintext_bytes >= 0),
          manifest_chunk_count       INTEGER NOT NULL CHECK (manifest_chunk_count >= 0),
          manifest_sealed_sha256     TEXT NOT NULL
                                     CHECK (length(manifest_sealed_sha256) = 64
                                       AND manifest_sealed_sha256 NOT GLOB '*[^0-9a-f]*'),
          manifest_sealed_bytes      INTEGER NOT NULL CHECK (manifest_sealed_bytes > 0),
          state                      TEXT NOT NULL DEFAULT 'prepared'
                                     CHECK (state IN (
                                       'prepared','executing','business_returned',
                                       'downloads_complete','settled'
                                     )),
          created_at                 TEXT NOT NULL,
          updated_at                 TEXT NOT NULL,
          CHECK (input_schema_digest = host_provider_input_schema_digest),
          FOREIGN KEY (session_id, source_user_seq, parent_logical_tool_call_id)
            REFERENCES logical_tool_calls(session_id, source_user_seq, logical_tool_call_id)
            ON DELETE CASCADE
        );

        CREATE UNIQUE INDEX IF NOT EXISTS uq_staged_transfer_parent_call
          ON staged_transfer_plans(session_id, source_user_seq, parent_logical_tool_call_id);

        CREATE TABLE IF NOT EXISTS staged_transfer_stage_authorities (
          stage_authority_id       TEXT PRIMARY KEY CHECK (length(stage_authority_id) BETWEEN 1 AND 256),
          plan_id                  TEXT NOT NULL REFERENCES staged_transfer_plans(plan_id) ON DELETE CASCADE,
          session_id               TEXT NOT NULL,
          source_user_seq          INTEGER NOT NULL CHECK (source_user_seq > 0),
          accepted_task_id         TEXT NOT NULL,
          stage_ordinal            INTEGER NOT NULL CHECK (stage_ordinal > 0),
          stage_kind               TEXT NOT NULL CHECK (stage_kind IN (
                                     'local_snapshot','source_download','upload_presign','upload_transfer',
                                     'business_execute','download_transfer','local_commit'
                                   )),
          json_pointer_digest      TEXT
                                   CHECK (json_pointer_digest IS NULL OR (
                                     length(json_pointer_digest) = 64
                                     AND json_pointer_digest NOT GLOB '*[^0-9a-f]*'
                                   )),
          logical_tool_call_id     TEXT NOT NULL,
          physical_dispatch_id     TEXT NOT NULL,
          tool_name                TEXT NOT NULL CHECK (length(tool_name) BETWEEN 1 AND 512),
          argument_digest          TEXT NOT NULL
                                   CHECK (length(argument_digest) = 64
                                     AND argument_digest NOT GLOB '*[^0-9a-f]*'),
          effect_kind              TEXT NOT NULL CHECK (effect_kind IN (
                                     'read','compute','local_write','external_write','admin'
                                   )),
          lease_scope_id           TEXT NOT NULL CHECK (length(lease_scope_id) BETWEEN 1 AND 512),
          lease_id                 TEXT NOT NULL CHECK (length(lease_id) BETWEEN 1 AND 512),
          depends_on_stage_ordinal INTEGER CHECK (
                                     depends_on_stage_ordinal IS NULL
                                     OR depends_on_stage_ordinal > 0
                                   ),
          authority_digest         TEXT NOT NULL UNIQUE
                                   CHECK (length(authority_digest) = 64
                                     AND authority_digest NOT GLOB '*[^0-9a-f]*'),
          created_at               TEXT NOT NULL,
          UNIQUE (plan_id, stage_ordinal),
          UNIQUE (session_id, source_user_seq, logical_tool_call_id),
          UNIQUE (session_id, source_user_seq, physical_dispatch_id),
          FOREIGN KEY (session_id, source_user_seq, logical_tool_call_id)
            REFERENCES logical_tool_calls(session_id, source_user_seq, logical_tool_call_id)
            ON DELETE CASCADE
        );

        CREATE TABLE IF NOT EXISTS physical_dispatch_return_checkpoints (
          checkpoint_id          TEXT PRIMARY KEY CHECK (length(checkpoint_id) BETWEEN 1 AND 256),
          session_id             TEXT NOT NULL,
          source_user_seq        INTEGER NOT NULL CHECK (source_user_seq > 0),
          accepted_task_id       TEXT NOT NULL,
          logical_tool_call_id   TEXT NOT NULL,
          physical_dispatch_id   TEXT NOT NULL,
          stage_authority_id     TEXT NOT NULL UNIQUE
                                 REFERENCES staged_transfer_stage_authorities(stage_authority_id)
                                 ON DELETE CASCADE,
          stage_kind             TEXT NOT NULL CHECK (stage_kind IN (
                                   'local_snapshot','source_download','upload_presign','upload_transfer',
                                   'business_execute','download_transfer','local_commit'
                                 )),
          stage_ordinal          INTEGER NOT NULL CHECK (stage_ordinal > 0),
          tool_name              TEXT NOT NULL CHECK (length(tool_name) BETWEEN 1 AND 512),
          argument_digest        TEXT NOT NULL
                                 CHECK (length(argument_digest) = 64
                                   AND argument_digest NOT GLOB '*[^0-9a-f]*'),
          lease_scope_id         TEXT NOT NULL CHECK (length(lease_scope_id) BETWEEN 1 AND 512),
          lease_id               TEXT NOT NULL CHECK (length(lease_id) BETWEEN 1 AND 512),
          payload_id             TEXT NOT NULL UNIQUE CHECK (payload_id GLOB 'authority-payload:*'),
          payload_plaintext_sha256 TEXT NOT NULL
                                 CHECK (length(payload_plaintext_sha256) = 64
                                   AND payload_plaintext_sha256 NOT GLOB '*[^0-9a-f]*'),
          payload_plaintext_bytes INTEGER NOT NULL CHECK (payload_plaintext_bytes >= 0),
          payload_chunk_count    INTEGER NOT NULL CHECK (payload_chunk_count >= 0),
          payload_sealed_sha256  TEXT NOT NULL
                                 CHECK (length(payload_sealed_sha256) = 64
                                   AND payload_sealed_sha256 NOT GLOB '*[^0-9a-f]*'),
          payload_sealed_bytes   INTEGER NOT NULL CHECK (payload_sealed_bytes > 0),
          created_at             TEXT NOT NULL,
          UNIQUE (session_id, source_user_seq, physical_dispatch_id),
          FOREIGN KEY (session_id, source_user_seq, logical_tool_call_id, physical_dispatch_id)
            REFERENCES physical_dispatches(
              session_id, source_user_seq, logical_tool_call_id, physical_dispatch_id
            ) ON DELETE CASCADE
        );

        CREATE TABLE IF NOT EXISTS staged_transfer_stage_receipts (
          stage_authority_id     TEXT PRIMARY KEY
                                 REFERENCES staged_transfer_stage_authorities(stage_authority_id)
                                 ON DELETE CASCADE,
          plan_id                TEXT NOT NULL REFERENCES staged_transfer_plans(plan_id) ON DELETE CASCADE,
          session_id             TEXT NOT NULL,
          source_user_seq        INTEGER NOT NULL CHECK (source_user_seq > 0),
          stage_ordinal          INTEGER NOT NULL CHECK (stage_ordinal > 0),
          physical_dispatch_id   TEXT NOT NULL,
          terminal_state         TEXT NOT NULL CHECK (terminal_state IN (
                                   'returned','threw','timed_out','cancelled','unknown'
                                 )),
          result_digest          TEXT CHECK (result_digest IS NULL OR (
                                   length(result_digest) = 64
                                   AND result_digest NOT GLOB '*[^0-9a-f]*'
                                 )),
          recorded_at            TEXT NOT NULL,
          UNIQUE (plan_id, stage_ordinal),
          UNIQUE (session_id, source_user_seq, physical_dispatch_id),
          FOREIGN KEY (session_id, source_user_seq, physical_dispatch_id)
            REFERENCES physical_dispatches(session_id, source_user_seq, physical_dispatch_id)
            ON DELETE CASCADE,
          CHECK (
            (terminal_state = 'returned' AND result_digest IS NOT NULL)
            OR terminal_state != 'returned'
          )
        );

        CREATE INDEX IF NOT EXISTS idx_staged_transfer_plan_state
          ON staged_transfer_plans(session_id, source_user_seq, state, updated_at);
        CREATE INDEX IF NOT EXISTS idx_staged_transfer_stage_plan
          ON staged_transfer_stage_authorities(plan_id, stage_ordinal);

        CREATE TRIGGER IF NOT EXISTS trg_staged_transfer_plan_exact_parent
        BEFORE INSERT ON staged_transfer_plans
        WHEN NOT EXISTS (
          SELECT 1
            FROM logical_tool_calls l
            JOIN expected_work_call_bindings b
              ON b.session_id = l.session_id
             AND b.source_user_seq = l.source_user_seq
             AND b.logical_tool_call_id = l.logical_tool_call_id
           WHERE l.session_id = NEW.session_id
             AND l.source_user_seq = NEW.source_user_seq
             AND l.accepted_task_id = NEW.accepted_task_id
             AND l.logical_tool_call_id = NEW.parent_logical_tool_call_id
             AND l.tool_name = NEW.parent_tool_name
             AND l.argument_digest = NEW.parent_argument_digest
             AND l.state = 'open'
             AND b.accepted_task_id = NEW.accepted_task_id
             AND b.contract_id = NEW.expected_work_contract_id
             AND b.requirement_id = NEW.expected_work_requirement_id
             AND b.tool_name = NEW.parent_tool_name
             AND b.argument_digest = NEW.parent_argument_digest
             AND b.effect_kind = NEW.parent_effect_kind
             AND EXISTS (
               SELECT 1 FROM host_call_capability_bindings host
                WHERE host.session_id = NEW.session_id
                  AND host.source_user_seq = NEW.source_user_seq
                  AND host.accepted_task_id = NEW.accepted_task_id
                  AND host.logical_tool_call_id = NEW.parent_logical_tool_call_id
                  AND host.tool_name = NEW.parent_tool_name
                  AND host.attested_argument_digest = NEW.parent_argument_digest
                  AND host.effect = NEW.parent_effect_kind
                  AND host.binding_kind = 'catalog_manifest'
                  AND host.capability_id = NEW.host_capability_id
                  AND host.account_id = NEW.host_account_id
                  AND host.invoke_port_id = NEW.host_invoke_port_id
                  AND host.operation_id = NEW.host_operation_id
                  AND host.provider_input_schema_digest = NEW.host_provider_input_schema_digest
                  AND host.schema_fingerprint = NEW.host_schema_fingerprint
                  AND host.manifest_id = NEW.host_manifest_id
                  AND host.manifest_digest = NEW.host_manifest_digest
                  AND host.durable_binding_digest = NEW.host_durable_binding_digest
                  AND EXISTS (
                    SELECT 1 FROM capability_manifests manifest
                     WHERE manifest.manifest_id = NEW.host_manifest_id
                       AND manifest.digest = NEW.host_manifest_digest
                       AND manifest.lifecycle = 'current'
                       AND json_valid(manifest.manifest_json)
                       AND json_extract(manifest.manifest_json, '$.version') = 1
                       AND json_extract(manifest.manifest_json, '$.manifestId') = NEW.host_manifest_id
                       AND json_extract(manifest.manifest_json, '$.providerKind') = 'composio'
                       AND json_extract(manifest.manifest_json, '$.operationId') = NEW.host_operation_id
                       AND json_extract(manifest.manifest_json, '$.operationVersion') = NEW.operation_version
                       AND json_extract(manifest.manifest_json, '$.definitionFingerprint') = NEW.host_definition_fingerprint
                       AND json_extract(manifest.manifest_json, '$.externalDefinition.providerInputSchemaDigest')
                           = NEW.host_provider_input_schema_digest
                       AND json_extract(manifest.manifest_json, '$.accountId') = NEW.host_account_id
                       AND json_extract(manifest.manifest_json, '$.invokePortId') = NEW.host_invoke_port_id
                       AND json_extract(manifest.manifest_json, '$.lifecycle.state') = 'current'
                  )
             )
        )
        BEGIN
          SELECT RAISE(ABORT, 'staged transfer requires its exact open parent call');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_staged_transfer_plan_identity_immutable
        BEFORE UPDATE ON staged_transfer_plans
        WHEN OLD.plan_id IS NOT NEW.plan_id
          OR OLD.session_id IS NOT NEW.session_id
          OR OLD.source_user_seq IS NOT NEW.source_user_seq
          OR OLD.accepted_task_id IS NOT NEW.accepted_task_id
          OR OLD.parent_logical_tool_call_id IS NOT NEW.parent_logical_tool_call_id
          OR OLD.parent_tool_name IS NOT NEW.parent_tool_name
          OR OLD.parent_argument_digest IS NOT NEW.parent_argument_digest
          OR OLD.parent_effect_kind IS NOT NEW.parent_effect_kind
          OR OLD.expected_work_contract_id IS NOT NEW.expected_work_contract_id
          OR OLD.expected_work_requirement_id IS NOT NEW.expected_work_requirement_id
          OR OLD.expected_work_binding_digest IS NOT NEW.expected_work_binding_digest
          OR OLD.host_durable_binding_digest IS NOT NEW.host_durable_binding_digest
          OR OLD.host_capability_id IS NOT NEW.host_capability_id
          OR OLD.host_account_id IS NOT NEW.host_account_id
          OR OLD.host_invoke_port_id IS NOT NEW.host_invoke_port_id
          OR OLD.host_operation_id IS NOT NEW.host_operation_id
          OR OLD.host_provider_input_schema_digest IS NOT NEW.host_provider_input_schema_digest
          OR OLD.host_schema_fingerprint IS NOT NEW.host_schema_fingerprint
          OR OLD.host_definition_fingerprint IS NOT NEW.host_definition_fingerprint
          OR OLD.host_manifest_id IS NOT NEW.host_manifest_id
          OR OLD.host_manifest_digest IS NOT NEW.host_manifest_digest
          OR OLD.input_schema_digest IS NOT NEW.input_schema_digest
          OR OLD.output_schema_digest IS NOT NEW.output_schema_digest
          OR OLD.operation_version IS NOT NEW.operation_version
          OR OLD.toolkit_slug IS NOT NEW.toolkit_slug
          OR OLD.account_identity_digest IS NOT NEW.account_identity_digest
          OR OLD.transfer_manifest_digest IS NOT NEW.transfer_manifest_digest
          OR OLD.manifest_binding_digest IS NOT NEW.manifest_binding_digest
          OR OLD.manifest_payload_id IS NOT NEW.manifest_payload_id
          OR OLD.manifest_format IS NOT NEW.manifest_format
          OR OLD.manifest_plaintext_sha256 IS NOT NEW.manifest_plaintext_sha256
          OR OLD.manifest_plaintext_bytes IS NOT NEW.manifest_plaintext_bytes
          OR OLD.manifest_chunk_count IS NOT NEW.manifest_chunk_count
          OR OLD.manifest_sealed_sha256 IS NOT NEW.manifest_sealed_sha256
          OR OLD.manifest_sealed_bytes IS NOT NEW.manifest_sealed_bytes
          OR OLD.created_at IS NOT NEW.created_at
        BEGIN
          SELECT RAISE(ABORT, 'staged transfer plan authority is immutable');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_staged_transfer_plan_delete_immutable
        BEFORE DELETE ON staged_transfer_plans
        WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
        BEGIN
          SELECT RAISE(ABORT, 'staged transfer plan authority is immutable');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_staged_transfer_plan_state_machine
        BEFORE UPDATE OF state ON staged_transfer_plans
        WHEN OLD.state IS NOT NEW.state AND NOT (
          (OLD.state = 'prepared' AND NEW.state = 'executing')
          OR (OLD.state = 'executing' AND NEW.state = 'business_returned')
          OR (OLD.state = 'business_returned' AND NEW.state IN ('downloads_complete','settled'))
          OR (OLD.state = 'downloads_complete' AND NEW.state = 'settled')
        )
        BEGIN
          SELECT RAISE(ABORT, 'invalid staged transfer plan transition');
        END;

        /* State is a bounded index hint, never recovery authority. Even that
         * hint may advance only beside the exact receipts/parent settlement;
         * recovery recomputes progress from immutable rows and encrypted
         * definition/result authority instead of trusting this column. */
        CREATE TRIGGER IF NOT EXISTS trg_staged_transfer_plan_state_receipts
        BEFORE UPDATE OF state ON staged_transfer_plans
        WHEN OLD.state IS NOT NEW.state AND (
          (NEW.state IN ('business_returned','downloads_complete','settled')
            AND NOT EXISTS (
              SELECT 1
                FROM staged_transfer_stage_authorities business
                JOIN staged_transfer_stage_receipts receipt
                  ON receipt.stage_authority_id = business.stage_authority_id
               WHERE business.plan_id = NEW.plan_id
                 AND business.stage_kind = 'business_execute'
                 AND receipt.terminal_state = 'returned'
            ))
          OR (NEW.state IN ('downloads_complete','settled') AND EXISTS (
            SELECT 1 FROM staged_transfer_stage_authorities download
             WHERE download.plan_id = NEW.plan_id
               AND download.stage_kind IN ('download_transfer','local_commit')
               AND NOT EXISTS (
                 SELECT 1 FROM staged_transfer_stage_receipts receipt
                  WHERE receipt.stage_authority_id = download.stage_authority_id
                    AND receipt.terminal_state = 'returned'
               )
          ))
          OR (NEW.state = 'settled' AND NOT EXISTS (
            SELECT 1 FROM logical_call_settlements settled
             WHERE settled.session_id = NEW.session_id
               AND settled.source_user_seq = NEW.source_user_seq
               AND settled.logical_tool_call_id = NEW.parent_logical_tool_call_id
          ))
        )
        BEGIN
          SELECT RAISE(ABORT, 'staged transfer plan state lacks exact receipts');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_staged_transfer_stage_exact_plan
        BEFORE INSERT ON staged_transfer_stage_authorities
        WHEN NOT EXISTS (
          SELECT 1
            FROM staged_transfer_plans p
            JOIN logical_tool_calls l
              ON l.session_id = NEW.session_id
             AND l.source_user_seq = NEW.source_user_seq
             AND l.logical_tool_call_id = NEW.logical_tool_call_id
           WHERE p.plan_id = NEW.plan_id
             AND p.session_id = NEW.session_id
             AND p.source_user_seq = NEW.source_user_seq
             AND p.accepted_task_id = NEW.accepted_task_id
             AND l.accepted_task_id = NEW.accepted_task_id
             AND l.tool_name = NEW.tool_name
             AND l.argument_digest = NEW.argument_digest
             AND l.state = 'open'
             AND (
               (NEW.stage_kind = 'business_execute'
                 AND NEW.logical_tool_call_id = p.parent_logical_tool_call_id)
               OR
               (NEW.stage_kind != 'business_execute'
                 AND NEW.logical_tool_call_id != p.parent_logical_tool_call_id
                 AND NOT EXISTS (
                   SELECT 1 FROM expected_work_call_bindings b
                    WHERE b.session_id = NEW.session_id
                      AND b.source_user_seq = NEW.source_user_seq
                      AND b.logical_tool_call_id = NEW.logical_tool_call_id
                 ))
             )
             AND (NEW.stage_kind NOT IN ('download_transfer','local_commit')
               OR p.output_schema_digest IS NOT NULL)
             AND (
               (NEW.stage_kind = 'local_snapshot' AND NEW.effect_kind = 'local_write')
               OR (NEW.stage_kind IN ('source_download','download_transfer') AND NEW.effect_kind = 'read')
               OR (NEW.stage_kind IN ('upload_presign','upload_transfer') AND NEW.effect_kind = 'external_write')
               OR (NEW.stage_kind = 'local_commit' AND NEW.effect_kind = 'local_write')
               OR (NEW.stage_kind = 'business_execute' AND NEW.effect_kind = p.parent_effect_kind)
             )
             AND EXISTS (
               SELECT 1 FROM run_dispatch_leases lease
                WHERE lease.scope_id = NEW.lease_scope_id
                  AND lease.lease_id = NEW.lease_id
                  AND lease.session_id = NEW.session_id
                  AND lease.source_user_seq = NEW.source_user_seq
                  AND lease.accepted_task_id = NEW.accepted_task_id
                  AND lease.logical_tool_call_id = NEW.logical_tool_call_id
                  AND lease.revoked_at IS NULL
             )
             AND (NEW.depends_on_stage_ordinal IS NULL OR EXISTS (
               SELECT 1 FROM staged_transfer_stage_authorities prerequisite
                WHERE prerequisite.plan_id = NEW.plan_id
                  AND prerequisite.stage_ordinal = NEW.depends_on_stage_ordinal
                  AND prerequisite.stage_ordinal < NEW.stage_ordinal
             ))
        )
        BEGIN
          SELECT RAISE(ABORT, 'staged transfer stage authority is not exact');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_staged_transfer_stage_immutable
        BEFORE UPDATE ON staged_transfer_stage_authorities
        BEGIN
          SELECT RAISE(ABORT, 'staged transfer stage authority is immutable');
        END;
        CREATE TRIGGER IF NOT EXISTS trg_staged_transfer_stage_delete_immutable
        BEFORE DELETE ON staged_transfer_stage_authorities
        WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
        BEGIN
          SELECT RAISE(ABORT, 'staged transfer stage authority is immutable');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_staged_transfer_child_no_expected_work
        BEFORE INSERT ON expected_work_call_bindings
        WHEN EXISTS (
          SELECT 1 FROM staged_transfer_stage_authorities s
           WHERE s.session_id = NEW.session_id
             AND s.source_user_seq = NEW.source_user_seq
             AND s.logical_tool_call_id = NEW.logical_tool_call_id
             AND s.stage_kind != 'business_execute'
        )
        BEGIN
          SELECT RAISE(ABORT, 'staged control dependencies cannot bind expected work');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_staged_transfer_child_no_progress
        BEFORE INSERT ON logical_call_settlements
        WHEN EXISTS (
          SELECT 1 FROM staged_transfer_stage_authorities s
           WHERE s.session_id = NEW.session_id
             AND s.source_user_seq = NEW.source_user_seq
             AND s.logical_tool_call_id = NEW.logical_tool_call_id
             AND s.stage_kind != 'business_execute'
        ) AND (
          NEW.business_call != 0
          OR NEW.progress_claimed != 0
          OR NEW.requirement_id IS NOT NULL
          OR NEW.continues_requirement != 0
        )
        BEGIN
          SELECT RAISE(ABORT, 'staged control dependencies cannot discharge expected work');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_staged_transfer_physical_prerequisite
        BEFORE INSERT ON physical_dispatches
        WHEN EXISTS (
          SELECT 1 FROM staged_transfer_stage_authorities s
           WHERE s.session_id = NEW.session_id
             AND s.source_user_seq = NEW.source_user_seq
             AND s.logical_tool_call_id = NEW.logical_tool_call_id
             AND (
               s.physical_dispatch_id != NEW.physical_dispatch_id
               OR s.accepted_task_id != NEW.accepted_task_id
               OR s.tool_name != NEW.tool_name
               OR s.argument_digest != NEW.argument_digest
               OR s.lease_scope_id IS NOT NEW.lease_scope_id
               OR s.lease_id IS NOT NEW.lease_id
               OR (s.depends_on_stage_ordinal IS NOT NULL AND NOT EXISTS (
                 SELECT 1
                   FROM staged_transfer_stage_authorities dependency
                   JOIN staged_transfer_stage_receipts receipt
                     ON receipt.stage_authority_id = dependency.stage_authority_id
                  WHERE dependency.plan_id = s.plan_id
                    AND dependency.stage_ordinal = s.depends_on_stage_ordinal
                    AND receipt.terminal_state = 'returned'
               ))
               OR (s.stage_kind = 'business_execute' AND EXISTS (
                 SELECT 1 FROM staged_transfer_stage_authorities upload
                  WHERE upload.plan_id = s.plan_id
                    AND upload.stage_kind = 'upload_transfer'
                    AND NOT EXISTS (
                      SELECT 1 FROM staged_transfer_stage_receipts upload_receipt
                       WHERE upload_receipt.stage_authority_id = upload.stage_authority_id
                         AND upload_receipt.terminal_state = 'returned'
                    )
               ))
             )
        )
        BEGIN
          SELECT RAISE(ABORT, 'staged transfer physical dispatch lacks exact settled prerequisites');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_physical_return_checkpoint_exact_return
        BEFORE INSERT ON physical_dispatch_return_checkpoints
        WHEN NOT EXISTS (
          SELECT 1
            FROM physical_dispatches p
            JOIN staged_transfer_stage_authorities s
              ON s.stage_authority_id = NEW.stage_authority_id
           WHERE p.session_id = NEW.session_id
             AND p.source_user_seq = NEW.source_user_seq
             AND p.accepted_task_id = NEW.accepted_task_id
             AND p.logical_tool_call_id = NEW.logical_tool_call_id
             AND p.physical_dispatch_id = NEW.physical_dispatch_id
             AND p.tool_name = NEW.tool_name
             AND p.argument_digest = NEW.argument_digest
             AND p.lease_scope_id IS NEW.lease_scope_id
             AND p.lease_id IS NEW.lease_id
             AND p.state = 'returned'
             AND s.session_id = NEW.session_id
             AND s.source_user_seq = NEW.source_user_seq
             AND s.accepted_task_id = NEW.accepted_task_id
             AND s.logical_tool_call_id = NEW.logical_tool_call_id
             AND s.physical_dispatch_id = NEW.physical_dispatch_id
             AND s.stage_kind = NEW.stage_kind
             AND s.stage_ordinal = NEW.stage_ordinal
             AND s.tool_name = NEW.tool_name
             AND s.argument_digest = NEW.argument_digest
             AND s.lease_scope_id = NEW.lease_scope_id
             AND s.lease_id = NEW.lease_id
        )
        BEGIN
          SELECT RAISE(ABORT, 'physical return checkpoint requires its exact returned stage crossing');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_physical_return_checkpoint_immutable
        BEFORE UPDATE ON physical_dispatch_return_checkpoints
        BEGIN
          SELECT RAISE(ABORT, 'physical return checkpoints are immutable');
        END;
        CREATE TRIGGER IF NOT EXISTS trg_physical_return_checkpoint_delete_immutable
        BEFORE DELETE ON physical_dispatch_return_checkpoints
        WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
        BEGIN
          SELECT RAISE(ABORT, 'physical return checkpoints are immutable');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_staged_transfer_receipt_exact_crossing
        BEFORE INSERT ON staged_transfer_stage_receipts
        WHEN NOT EXISTS (
          SELECT 1
            FROM staged_transfer_stage_authorities s
            JOIN physical_dispatches p
              ON p.session_id = s.session_id
             AND p.source_user_seq = s.source_user_seq
             AND p.physical_dispatch_id = s.physical_dispatch_id
           WHERE s.stage_authority_id = NEW.stage_authority_id
             AND s.plan_id = NEW.plan_id
             AND s.session_id = NEW.session_id
             AND s.source_user_seq = NEW.source_user_seq
             AND s.stage_ordinal = NEW.stage_ordinal
             AND s.physical_dispatch_id = NEW.physical_dispatch_id
             AND p.accepted_task_id = s.accepted_task_id
             AND p.logical_tool_call_id = s.logical_tool_call_id
             AND p.tool_name = s.tool_name
             AND p.argument_digest = s.argument_digest
             AND p.lease_scope_id = s.lease_scope_id
             AND p.lease_id = s.lease_id
             AND p.state = NEW.terminal_state
             AND (NEW.terminal_state != 'returned' OR EXISTS (
               SELECT 1 FROM physical_dispatch_return_checkpoints c
                WHERE c.stage_authority_id = s.stage_authority_id
                  AND c.physical_dispatch_id = s.physical_dispatch_id
             ))
        )
        BEGIN
          SELECT RAISE(ABORT, 'staged transfer receipt requires its exact terminal crossing');
        END;

        CREATE TRIGGER IF NOT EXISTS trg_staged_transfer_receipt_immutable
        BEFORE UPDATE ON staged_transfer_stage_receipts
        BEGIN
          SELECT RAISE(ABORT, 'staged transfer receipts are append-only');
        END;
        CREATE TRIGGER IF NOT EXISTS trg_staged_transfer_receipt_delete_immutable
        BEFORE DELETE ON staged_transfer_stage_receipts
        WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
        BEGIN
          SELECT RAISE(ABORT, 'staged transfer receipts are append-only');
        END;
      `);
        }
      },
      {
        /**
         * Corrective staged-transfer authority. v61 was intentionally never a
         * production execution surface, but a daemon may already have stamped it.
         * Do not reinterpret those rows: retire every v61 logical owner, drop the
         * unsafe scaffolding, and rebuild an attempt-generational authority graph.
         */
        version: 62,
        sql: "",
        foreignKeysOff: true,
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) return;
          for (const prerequisite of [
            "logical_tool_calls",
            "physical_dispatches",
            "logical_call_settlements",
            "expected_work_call_bindings",
            "host_call_capability_bindings",
            "capability_manifests",
            "run_dispatch_leases"
          ]) {
            if (!tables.has(prerequisite)) {
              throw new Error(`schema v62 prerequisite missing: ${prerequisite}`);
            }
          }
          const physicalColumns = new Set(
            db.prepare("PRAGMA table_info(physical_dispatches)").all().map((column) => column.name)
          );
          if (!physicalColumns.has("staged_authority_digest")) {
            db.exec(`ALTER TABLE physical_dispatches ADD COLUMN staged_authority_digest TEXT
          CHECK (staged_authority_digest IS NULL OR (
            length(staged_authority_digest) = 64
            AND staged_authority_digest NOT GLOB '*[^0-9a-f]*'
          ))`);
          }
          if (tables.has("staged_transfer_plans")) {
            db.exec(`
          UPDATE logical_tool_calls
             SET state = 'conflict',
                 conflict_reason = 'staged_transfer_v61_authority_retired'
           WHERE state = 'open' AND EXISTS (
             SELECT 1 FROM staged_transfer_plans p
              WHERE p.session_id = logical_tool_calls.session_id
                AND p.source_user_seq = logical_tool_calls.source_user_seq
                AND p.parent_logical_tool_call_id = logical_tool_calls.logical_tool_call_id
           );
        `);
          }
          if (tables.has("staged_transfer_stage_authorities")) {
            db.exec(`
          UPDATE logical_tool_calls
             SET state = 'conflict',
                 conflict_reason = 'staged_transfer_v61_authority_retired'
           WHERE state = 'open' AND EXISTS (
             SELECT 1 FROM staged_transfer_stage_authorities s
              WHERE s.session_id = logical_tool_calls.session_id
                AND s.source_user_seq = logical_tool_calls.source_user_seq
                AND s.logical_tool_call_id = logical_tool_calls.logical_tool_call_id
           );
          UPDATE run_dispatch_leases
             SET revoked_at = COALESCE(revoked_at, CURRENT_TIMESTAMP)
           WHERE EXISTS (
             SELECT 1 FROM staged_transfer_stage_authorities s
              WHERE s.lease_scope_id = run_dispatch_leases.scope_id
                AND s.lease_id = run_dispatch_leases.lease_id
           );
        `);
          }
          for (const trigger of [
            "trg_staged_transfer_plan_exact_parent",
            "trg_staged_transfer_plan_identity_immutable",
            "trg_staged_transfer_plan_delete_immutable",
            "trg_staged_transfer_plan_state_machine",
            "trg_staged_transfer_plan_state_receipts",
            "trg_staged_transfer_stage_exact_plan",
            "trg_staged_transfer_stage_immutable",
            "trg_staged_transfer_stage_delete_immutable",
            "trg_staged_transfer_child_no_expected_work",
            "trg_staged_transfer_child_no_progress",
            "trg_staged_transfer_physical_prerequisite",
            "trg_physical_return_checkpoint_exact_return",
            "trg_physical_return_checkpoint_immutable",
            "trg_physical_return_checkpoint_delete_immutable",
            "trg_staged_transfer_receipt_exact_crossing",
            "trg_staged_transfer_receipt_immutable",
            "trg_staged_transfer_receipt_delete_immutable",
            "trg_staged_transfer_attempt_exact_stage",
            "trg_staged_transfer_attempt_immutable",
            "trg_staged_transfer_attempt_delete_immutable",
            "trg_staged_transfer_physical_exact_attempt",
            "trg_staged_transfer_receipt_exact_attempt",
            "trg_staged_transfer_one_success_per_stage",
            "trg_staged_transfer_download_topology_exact",
            "trg_staged_transfer_download_topology_immutable",
            "trg_staged_transfer_download_topology_delete_immutable",
            "trg_staged_transfer_logical_settlement_fence",
            "trg_staged_transfer_consent_exact_plan",
            "trg_staged_transfer_consent_immutable",
            "trg_staged_transfer_consent_delete_immutable",
            "trg_staged_transfer_reconciliation_exact_attempt",
            "trg_staged_transfer_reconciliation_requires_opaque_kernel",
            "trg_staged_transfer_reconciliation_immutable",
            "trg_staged_transfer_reconciliation_delete_immutable",
            "trg_staged_transfer_secret_exact_attempt",
            "trg_staged_transfer_secret_immutable",
            "trg_staged_transfer_secret_delete_immutable",
            "trg_staged_transfer_blob_exact_stage",
            "trg_staged_transfer_blob_immutable",
            "trg_staged_transfer_blob_delete_immutable"
          ]) db.exec(`DROP TRIGGER IF EXISTS ${trigger}`);
          db.exec(`
        DROP TABLE IF EXISTS staged_transfer_secret_payloads;
        DROP TABLE IF EXISTS staged_transfer_blob_owners;
        DROP TABLE IF EXISTS staged_transfer_consent_redemptions;
        DROP TABLE IF EXISTS staged_transfer_attempt_reconciliations;
        DROP TABLE IF EXISTS staged_transfer_download_topology_receipts;
        DROP TABLE IF EXISTS staged_transfer_stage_receipts;
        DROP TABLE IF EXISTS physical_dispatch_return_checkpoints;
        DROP TABLE IF EXISTS staged_transfer_stage_authorities;
        DROP TABLE IF EXISTS staged_transfer_stages;
        DROP TABLE IF EXISTS staged_transfer_plans;
      `);
          db.exec(`
        CREATE TABLE staged_transfer_plans (
          plan_id TEXT PRIMARY KEY CHECK (length(plan_id) BETWEEN 1 AND 256),
          session_id TEXT NOT NULL,
          source_user_seq INTEGER NOT NULL CHECK (source_user_seq > 0),
          accepted_task_id TEXT NOT NULL,
          parent_logical_tool_call_id TEXT NOT NULL,
          parent_tool_name TEXT NOT NULL CHECK (length(parent_tool_name) BETWEEN 1 AND 512),
          parent_argument_digest TEXT NOT NULL CHECK (
            length(parent_argument_digest) = 64 AND parent_argument_digest NOT GLOB '*[^0-9a-f]*'
          ),
          parent_effect_kind TEXT NOT NULL CHECK (parent_effect_kind IN (
            'read','compute','local_write','external_write','admin'
          )),
          expected_work_contract_id TEXT NOT NULL CHECK (length(expected_work_contract_id) BETWEEN 1 AND 256),
          expected_work_requirement_id TEXT NOT NULL CHECK (length(expected_work_requirement_id) BETWEEN 1 AND 256),
          expected_work_binding_digest TEXT NOT NULL CHECK (
            length(expected_work_binding_digest) = 64 AND expected_work_binding_digest NOT GLOB '*[^0-9a-f]*'
          ),
          host_durable_binding_digest TEXT NOT NULL CHECK (
            length(host_durable_binding_digest) = 64 AND host_durable_binding_digest NOT GLOB '*[^0-9a-f]*'
          ),
          host_capability_id TEXT NOT NULL CHECK (length(host_capability_id) BETWEEN 1 AND 512),
          host_account_id TEXT NOT NULL CHECK (length(host_account_id) BETWEEN 1 AND 512),
          host_invoke_port_id TEXT NOT NULL CHECK (length(host_invoke_port_id) BETWEEN 1 AND 512),
          host_operation_id TEXT NOT NULL CHECK (length(host_operation_id) BETWEEN 1 AND 512),
          host_provider_input_schema_digest TEXT NOT NULL CHECK (
            length(host_provider_input_schema_digest) = 64
            AND host_provider_input_schema_digest NOT GLOB '*[^0-9a-f]*'
          ),
          host_schema_fingerprint TEXT NOT NULL CHECK (
            length(host_schema_fingerprint) = 64 AND host_schema_fingerprint NOT GLOB '*[^0-9a-f]*'
          ),
          host_definition_fingerprint TEXT NOT NULL CHECK (
            length(host_definition_fingerprint) = 64
            AND host_definition_fingerprint NOT GLOB '*[^0-9a-f]*'
          ),
          host_manifest_id TEXT NOT NULL CHECK (length(host_manifest_id) BETWEEN 1 AND 512),
          host_manifest_digest TEXT NOT NULL CHECK (
            length(host_manifest_digest) = 64 AND host_manifest_digest NOT GLOB '*[^0-9a-f]*'
          ),
          input_schema_digest TEXT NOT NULL CHECK (
            length(input_schema_digest) = 64 AND input_schema_digest NOT GLOB '*[^0-9a-f]*'
          ),
          output_schema_digest TEXT CHECK (output_schema_digest IS NULL OR (
            length(output_schema_digest) = 64 AND output_schema_digest NOT GLOB '*[^0-9a-f]*'
          )),
          output_may_contain_downloads INTEGER NOT NULL CHECK (
            output_may_contain_downloads IN (0, 1)
          ),
          operation_version TEXT NOT NULL CHECK (length(operation_version) BETWEEN 1 AND 160),
          toolkit_slug TEXT NOT NULL CHECK (length(toolkit_slug) BETWEEN 1 AND 160),
          account_identity_digest TEXT NOT NULL CHECK (
            length(account_identity_digest) = 64 AND account_identity_digest NOT GLOB '*[^0-9a-f]*'
          ),
          transfer_manifest_digest TEXT NOT NULL CHECK (
            length(transfer_manifest_digest) = 64 AND transfer_manifest_digest NOT GLOB '*[^0-9a-f]*'
          ),
          manifest_binding_digest TEXT NOT NULL CHECK (
            length(manifest_binding_digest) = 64 AND manifest_binding_digest NOT GLOB '*[^0-9a-f]*'
          ),
          manifest_payload_id TEXT NOT NULL UNIQUE CHECK (manifest_payload_id GLOB 'authority-payload:*'),
          manifest_format TEXT NOT NULL CHECK (manifest_format = 'staged_transfer_manifest_v2'),
          manifest_plaintext_sha256 TEXT NOT NULL CHECK (
            length(manifest_plaintext_sha256) = 64 AND manifest_plaintext_sha256 NOT GLOB '*[^0-9a-f]*'
          ),
          manifest_plaintext_bytes INTEGER NOT NULL CHECK (manifest_plaintext_bytes >= 0),
          manifest_chunk_count INTEGER NOT NULL CHECK (manifest_chunk_count >= 0),
          manifest_sealed_sha256 TEXT NOT NULL CHECK (
            length(manifest_sealed_sha256) = 64 AND manifest_sealed_sha256 NOT GLOB '*[^0-9a-f]*'
          ),
          manifest_sealed_bytes INTEGER NOT NULL CHECK (manifest_sealed_bytes > 0),
          consent_requirement TEXT NOT NULL CHECK (consent_requirement IN ('none','exact_grant')),
          consent_subject_digest TEXT CHECK (consent_subject_digest IS NULL OR (
            length(consent_subject_digest) = 64 AND consent_subject_digest NOT GLOB '*[^0-9a-f]*'
          )),
          plan_authority_digest TEXT NOT NULL UNIQUE CHECK (
            length(plan_authority_digest) = 64 AND plan_authority_digest NOT GLOB '*[^0-9a-f]*'
          ),
          state TEXT NOT NULL DEFAULT 'prepared' CHECK (state IN (
            'prepared','executing','business_returned','downloads_complete',
            'reconciling','failed','settled'
          )),
          created_at TEXT NOT NULL,
          updated_at TEXT NOT NULL,
          CHECK (input_schema_digest = host_provider_input_schema_digest),
          CHECK (host_schema_fingerprint = host_definition_fingerprint),
          CHECK (output_may_contain_downloads = 0 OR output_schema_digest IS NOT NULL),
          CHECK (
            (consent_requirement = 'none' AND consent_subject_digest IS NULL)
            OR (consent_requirement = 'exact_grant' AND consent_subject_digest IS NOT NULL)
          ),
          FOREIGN KEY (session_id, source_user_seq, parent_logical_tool_call_id)
            REFERENCES logical_tool_calls(session_id, source_user_seq, logical_tool_call_id)
            ON DELETE CASCADE
        );
        CREATE UNIQUE INDEX uq_staged_transfer_parent_call
          ON staged_transfer_plans(session_id, source_user_seq, parent_logical_tool_call_id);

        CREATE TABLE staged_transfer_stages (
          stage_id TEXT PRIMARY KEY CHECK (length(stage_id) BETWEEN 1 AND 256),
          plan_id TEXT NOT NULL REFERENCES staged_transfer_plans(plan_id) ON DELETE CASCADE,
          session_id TEXT NOT NULL,
          source_user_seq INTEGER NOT NULL CHECK (source_user_seq > 0),
          accepted_task_id TEXT NOT NULL,
          stage_ordinal INTEGER NOT NULL CHECK (stage_ordinal > 0),
          stage_kind TEXT NOT NULL CHECK (stage_kind IN (
            'local_snapshot','source_download','upload_presign','upload_transfer',
            'business_execute','download_transfer','local_commit'
          )),
          json_pointer_digest TEXT CHECK (json_pointer_digest IS NULL OR (
            length(json_pointer_digest) = 64 AND json_pointer_digest NOT GLOB '*[^0-9a-f]*'
          )),
          tool_name TEXT NOT NULL CHECK (length(tool_name) BETWEEN 1 AND 512),
          effect_kind TEXT NOT NULL CHECK (effect_kind IN (
            'read','compute','local_write','external_write','admin'
          )),
          depends_on_stage_ordinal INTEGER CHECK (
            depends_on_stage_ordinal IS NULL OR depends_on_stage_ordinal > 0
          ),
          retry_policy TEXT NOT NULL CHECK (retry_policy IN (
            'none','safe_terminal','reconcile_before_retry'
          )),
          manifest_node_digest TEXT NOT NULL CHECK (
            length(manifest_node_digest) = 64 AND manifest_node_digest NOT GLOB '*[^0-9a-f]*'
          ),
          stage_digest TEXT NOT NULL UNIQUE CHECK (
            length(stage_digest) = 64 AND stage_digest NOT GLOB '*[^0-9a-f]*'
          ),
          created_at TEXT NOT NULL,
          UNIQUE (plan_id, stage_ordinal),
          UNIQUE (plan_id, manifest_node_digest)
        );

        CREATE TABLE staged_transfer_stage_authorities (
          stage_authority_id TEXT PRIMARY KEY CHECK (length(stage_authority_id) BETWEEN 1 AND 256),
          stage_id TEXT NOT NULL REFERENCES staged_transfer_stages(stage_id) ON DELETE CASCADE,
          plan_id TEXT NOT NULL REFERENCES staged_transfer_plans(plan_id) ON DELETE CASCADE,
          session_id TEXT NOT NULL,
          source_user_seq INTEGER NOT NULL CHECK (source_user_seq > 0),
          accepted_task_id TEXT NOT NULL,
          stage_ordinal INTEGER NOT NULL CHECK (stage_ordinal > 0),
          stage_kind TEXT NOT NULL,
          attempt_ordinal INTEGER NOT NULL CHECK (attempt_ordinal > 0),
          logical_tool_call_id TEXT NOT NULL,
          physical_dispatch_id TEXT NOT NULL,
          tool_name TEXT NOT NULL CHECK (length(tool_name) BETWEEN 1 AND 512),
          argument_digest TEXT NOT NULL CHECK (
            length(argument_digest) = 64 AND argument_digest NOT GLOB '*[^0-9a-f]*'
          ),
          provider_argument_digest TEXT NOT NULL CHECK (
            length(provider_argument_digest) = 64
            AND provider_argument_digest NOT GLOB '*[^0-9a-f]*'
          ),
          effect_kind TEXT NOT NULL,
          lease_scope_id TEXT NOT NULL CHECK (length(lease_scope_id) BETWEEN 1 AND 512),
          lease_id TEXT NOT NULL CHECK (length(lease_id) BETWEEN 1 AND 512),
          retry_of_stage_authority_id TEXT REFERENCES staged_transfer_stage_authorities(stage_authority_id),
          authority_digest TEXT NOT NULL UNIQUE CHECK (
            length(authority_digest) = 64 AND authority_digest NOT GLOB '*[^0-9a-f]*'
          ),
          created_at TEXT NOT NULL,
          UNIQUE (stage_id, attempt_ordinal),
          UNIQUE (session_id, source_user_seq, logical_tool_call_id),
          UNIQUE (session_id, source_user_seq, physical_dispatch_id),
          FOREIGN KEY (session_id, source_user_seq, logical_tool_call_id)
            REFERENCES logical_tool_calls(session_id, source_user_seq, logical_tool_call_id)
            ON DELETE CASCADE
        );

        CREATE TABLE physical_dispatch_return_checkpoints (
          checkpoint_id TEXT PRIMARY KEY CHECK (length(checkpoint_id) BETWEEN 1 AND 256),
          session_id TEXT NOT NULL,
          source_user_seq INTEGER NOT NULL CHECK (source_user_seq > 0),
          accepted_task_id TEXT NOT NULL,
          logical_tool_call_id TEXT NOT NULL,
          physical_dispatch_id TEXT NOT NULL,
          stage_authority_id TEXT NOT NULL UNIQUE
            REFERENCES staged_transfer_stage_authorities(stage_authority_id) ON DELETE CASCADE,
          stage_kind TEXT NOT NULL,
          stage_ordinal INTEGER NOT NULL CHECK (stage_ordinal > 0),
          attempt_ordinal INTEGER NOT NULL CHECK (attempt_ordinal > 0),
          tool_name TEXT NOT NULL CHECK (length(tool_name) BETWEEN 1 AND 512),
          argument_digest TEXT NOT NULL CHECK (
            length(argument_digest) = 64 AND argument_digest NOT GLOB '*[^0-9a-f]*'
          ),
          provider_argument_digest TEXT NOT NULL CHECK (
            length(provider_argument_digest) = 64
            AND provider_argument_digest NOT GLOB '*[^0-9a-f]*'
          ),
          lease_scope_id TEXT NOT NULL CHECK (length(lease_scope_id) BETWEEN 1 AND 512),
          lease_id TEXT NOT NULL CHECK (length(lease_id) BETWEEN 1 AND 512),
          payload_id TEXT NOT NULL UNIQUE CHECK (payload_id GLOB 'authority-payload:*'),
          payload_plaintext_sha256 TEXT NOT NULL CHECK (
            length(payload_plaintext_sha256) = 64 AND payload_plaintext_sha256 NOT GLOB '*[^0-9a-f]*'
          ),
          payload_plaintext_bytes INTEGER NOT NULL CHECK (payload_plaintext_bytes >= 0),
          payload_chunk_count INTEGER NOT NULL CHECK (payload_chunk_count >= 0),
          payload_sealed_sha256 TEXT NOT NULL CHECK (
            length(payload_sealed_sha256) = 64 AND payload_sealed_sha256 NOT GLOB '*[^0-9a-f]*'
          ),
          payload_sealed_bytes INTEGER NOT NULL CHECK (payload_sealed_bytes > 0),
          created_at TEXT NOT NULL,
          UNIQUE (session_id, source_user_seq, physical_dispatch_id),
          FOREIGN KEY (session_id, source_user_seq, logical_tool_call_id, physical_dispatch_id)
            REFERENCES physical_dispatches(
              session_id, source_user_seq, logical_tool_call_id, physical_dispatch_id
            ) ON DELETE CASCADE
        );

        CREATE TABLE staged_transfer_stage_receipts (
          stage_authority_id TEXT PRIMARY KEY
            REFERENCES staged_transfer_stage_authorities(stage_authority_id) ON DELETE CASCADE,
          stage_id TEXT NOT NULL REFERENCES staged_transfer_stages(stage_id) ON DELETE CASCADE,
          plan_id TEXT NOT NULL REFERENCES staged_transfer_plans(plan_id) ON DELETE CASCADE,
          session_id TEXT NOT NULL,
          source_user_seq INTEGER NOT NULL,
          stage_ordinal INTEGER NOT NULL,
          attempt_ordinal INTEGER NOT NULL,
          physical_dispatch_id TEXT NOT NULL,
          terminal_state TEXT NOT NULL CHECK (terminal_state IN (
            'returned','threw','timed_out','cancelled','unknown'
          )),
          result_digest TEXT CHECK (result_digest IS NULL OR (
            length(result_digest) = 64 AND result_digest NOT GLOB '*[^0-9a-f]*'
          )),
          recorded_at TEXT NOT NULL,
          UNIQUE (session_id, source_user_seq, physical_dispatch_id),
          UNIQUE (stage_id, attempt_ordinal),
          CHECK (
            (terminal_state = 'returned' AND result_digest IS NOT NULL)
            OR (terminal_state != 'returned' AND result_digest IS NULL)
          )
        );

        -- A successful business return is not enough to close a staged
        -- parent whose frozen output definition exists.  This append-only
        -- receipt proves that the encrypted returned payload was projected
        -- and that its complete, deterministic download successor set
        -- (including the empty set) was committed in the same transaction.
        CREATE TABLE staged_transfer_download_topology_receipts (
          plan_id TEXT PRIMARY KEY REFERENCES staged_transfer_plans(plan_id) ON DELETE CASCADE,
          business_stage_authority_id TEXT NOT NULL UNIQUE
            REFERENCES staged_transfer_stage_authorities(stage_authority_id) ON DELETE CASCADE,
          business_result_digest TEXT NOT NULL CHECK (
            length(business_result_digest) = 64
            AND business_result_digest NOT GLOB '*[^0-9a-f]*'
          ),
          topology_digest TEXT NOT NULL CHECK (
            length(topology_digest) = 64 AND topology_digest NOT GLOB '*[^0-9a-f]*'
          ),
          successor_stage_count INTEGER NOT NULL CHECK (
            successor_stage_count >= 0 AND successor_stage_count % 2 = 0
          ),
          recorded_at TEXT NOT NULL
        );

        CREATE TABLE staged_transfer_attempt_reconciliations (
          reconciliation_id TEXT PRIMARY KEY CHECK (length(reconciliation_id) BETWEEN 1 AND 256),
          prior_stage_authority_id TEXT NOT NULL UNIQUE
            REFERENCES staged_transfer_stage_authorities(stage_authority_id) ON DELETE CASCADE,
          plan_id TEXT NOT NULL REFERENCES staged_transfer_plans(plan_id) ON DELETE CASCADE,
          disposition TEXT NOT NULL CHECK (disposition IN ('not_applied','applied','unknown')),
          evidence_digest TEXT NOT NULL CHECK (
            length(evidence_digest) = 64 AND evidence_digest NOT GLOB '*[^0-9a-f]*'
          ),
          recorded_at TEXT NOT NULL
        );

        CREATE TABLE staged_transfer_consent_redemptions (
          plan_id TEXT PRIMARY KEY REFERENCES staged_transfer_plans(plan_id) ON DELETE CASCADE,
          approval_id TEXT NOT NULL UNIQUE,
          consent_subject_digest TEXT NOT NULL CHECK (
            length(consent_subject_digest) = 64 AND consent_subject_digest NOT GLOB '*[^0-9a-f]*'
          ),
          grant_digest TEXT NOT NULL CHECK (
            length(grant_digest) = 64 AND grant_digest NOT GLOB '*[^0-9a-f]*'
          ),
          redeemed_at TEXT NOT NULL
        );

        CREATE TABLE staged_transfer_secret_payloads (
          payload_id TEXT PRIMARY KEY CHECK (payload_id GLOB 'authority-payload:*'),
          stage_authority_id TEXT NOT NULL UNIQUE
            REFERENCES staged_transfer_stage_authorities(stage_authority_id) ON DELETE CASCADE,
          payload_kind TEXT NOT NULL CHECK (payload_kind = 'staged_signed_url'),
          binding_digest TEXT NOT NULL CHECK (
            length(binding_digest) = 64 AND binding_digest NOT GLOB '*[^0-9a-f]*'
          ),
          plaintext_sha256 TEXT NOT NULL CHECK (
            length(plaintext_sha256) = 64 AND plaintext_sha256 NOT GLOB '*[^0-9a-f]*'
          ),
          plaintext_bytes INTEGER NOT NULL CHECK (plaintext_bytes >= 0),
          chunk_count INTEGER NOT NULL CHECK (chunk_count >= 0),
          sealed_sha256 TEXT NOT NULL CHECK (
            length(sealed_sha256) = 64 AND sealed_sha256 NOT GLOB '*[^0-9a-f]*'
          ),
          sealed_bytes INTEGER NOT NULL CHECK (sealed_bytes > 0),
          expires_at TEXT NOT NULL,
          created_at TEXT NOT NULL
        );

        CREATE TABLE staged_transfer_blob_owners (
          plan_id TEXT NOT NULL REFERENCES staged_transfer_plans(plan_id) ON DELETE CASCADE,
          stage_id TEXT NOT NULL REFERENCES staged_transfer_stages(stage_id) ON DELETE CASCADE,
          blob_sha256 TEXT NOT NULL CHECK (
            length(blob_sha256) = 64 AND blob_sha256 NOT GLOB '*[^0-9a-f]*'
          ),
          blob_md5 TEXT NOT NULL CHECK (
            length(blob_md5) = 32 AND blob_md5 NOT GLOB '*[^0-9a-f]*'
          ),
          blob_bytes INTEGER NOT NULL CHECK (blob_bytes >= 0),
          created_at TEXT NOT NULL,
          PRIMARY KEY (plan_id, stage_id, blob_sha256)
        );

        CREATE INDEX idx_staged_transfer_plan_state
          ON staged_transfer_plans(session_id, source_user_seq, state, updated_at);
        CREATE INDEX idx_staged_transfer_stage_plan
          ON staged_transfer_stages(plan_id, stage_ordinal);
        CREATE INDEX idx_staged_transfer_attempt_stage
          ON staged_transfer_stage_authorities(stage_id, attempt_ordinal);
      `);
          db.exec(`
        CREATE TRIGGER trg_staged_transfer_plan_exact_parent
        BEFORE INSERT ON staged_transfer_plans
        WHEN NOT EXISTS (
          SELECT 1
            FROM logical_tool_calls l
            JOIN expected_work_call_bindings b
              ON b.session_id = l.session_id
             AND b.source_user_seq = l.source_user_seq
             AND b.logical_tool_call_id = l.logical_tool_call_id
            JOIN host_call_capability_bindings host
              ON host.session_id = l.session_id
             AND host.source_user_seq = l.source_user_seq
             AND host.logical_tool_call_id = l.logical_tool_call_id
            JOIN capability_manifests manifest
              ON manifest.manifest_id = host.manifest_id
             AND manifest.digest = host.manifest_digest
           WHERE l.session_id = NEW.session_id
             AND l.source_user_seq = NEW.source_user_seq
             AND l.accepted_task_id = NEW.accepted_task_id
             AND l.logical_tool_call_id = NEW.parent_logical_tool_call_id
             AND l.tool_name = NEW.parent_tool_name
             AND l.argument_digest = NEW.parent_argument_digest
             AND l.state = 'open'
             AND b.accepted_task_id = NEW.accepted_task_id
             AND b.contract_id = NEW.expected_work_contract_id
             AND b.requirement_id = NEW.expected_work_requirement_id
             AND b.tool_name = NEW.parent_tool_name
             AND b.argument_digest = NEW.parent_argument_digest
             AND b.effect_kind = NEW.parent_effect_kind
             AND host.accepted_task_id = NEW.accepted_task_id
             AND host.tool_name = NEW.parent_tool_name
             AND host.attested_argument_digest = NEW.parent_argument_digest
             AND host.effect = NEW.parent_effect_kind
             AND host.binding_kind = 'catalog_manifest'
             AND host.capability_id = NEW.host_capability_id
             AND host.account_id = NEW.host_account_id
             AND host.invoke_port_id = NEW.host_invoke_port_id
             AND host.operation_id = NEW.host_operation_id
             AND host.provider_input_schema_digest = NEW.host_provider_input_schema_digest
             AND host.schema_fingerprint = NEW.host_schema_fingerprint
             AND host.manifest_id = NEW.host_manifest_id
             AND host.manifest_digest = NEW.host_manifest_digest
             AND host.durable_binding_digest = NEW.host_durable_binding_digest
             AND manifest.lifecycle = 'current'
             AND json_valid(manifest.manifest_json)
             AND json_extract(manifest.manifest_json, '$.providerKind') = 'composio'
             AND json_extract(manifest.manifest_json, '$.operationId') = NEW.host_operation_id
             AND json_extract(manifest.manifest_json, '$.operationVersion') = NEW.operation_version
             AND json_extract(manifest.manifest_json, '$.definitionFingerprint')
                 = NEW.host_definition_fingerprint
             AND json_extract(manifest.manifest_json, '$.externalDefinition.providerInputSchemaDigest')
                 = NEW.host_provider_input_schema_digest
             AND json_extract(manifest.manifest_json,
                   '$.externalDefinition.providerOutputSchemaObserved') = 1
             AND (
               (NEW.output_schema_digest IS NULL
                 AND json_type(manifest.manifest_json,
                   '$.externalDefinition.providerOutputSchemaDigest') IS NULL)
               OR json_extract(manifest.manifest_json,
                    '$.externalDefinition.providerOutputSchemaDigest') = NEW.output_schema_digest
             )
             AND json_extract(manifest.manifest_json, '$.accountId') = NEW.host_account_id
             AND json_extract(manifest.manifest_json, '$.invokePortId') = NEW.host_invoke_port_id
             AND json_extract(manifest.manifest_json, '$.lifecycle.state') = 'current'
        )
        BEGIN SELECT RAISE(ABORT, 'staged transfer requires its exact open parent call'); END;

        CREATE TRIGGER trg_staged_transfer_plan_immutable
        BEFORE UPDATE ON staged_transfer_plans
        WHEN OLD.plan_id IS NOT NEW.plan_id
          OR OLD.session_id IS NOT NEW.session_id
          OR OLD.source_user_seq IS NOT NEW.source_user_seq
          OR OLD.accepted_task_id IS NOT NEW.accepted_task_id
          OR OLD.parent_logical_tool_call_id IS NOT NEW.parent_logical_tool_call_id
          OR OLD.parent_tool_name IS NOT NEW.parent_tool_name
          OR OLD.parent_argument_digest IS NOT NEW.parent_argument_digest
          OR OLD.parent_effect_kind IS NOT NEW.parent_effect_kind
          OR OLD.expected_work_contract_id IS NOT NEW.expected_work_contract_id
          OR OLD.expected_work_requirement_id IS NOT NEW.expected_work_requirement_id
          OR OLD.expected_work_binding_digest IS NOT NEW.expected_work_binding_digest
          OR OLD.host_durable_binding_digest IS NOT NEW.host_durable_binding_digest
          OR OLD.host_capability_id IS NOT NEW.host_capability_id
          OR OLD.host_account_id IS NOT NEW.host_account_id
          OR OLD.host_invoke_port_id IS NOT NEW.host_invoke_port_id
          OR OLD.host_operation_id IS NOT NEW.host_operation_id
          OR OLD.host_provider_input_schema_digest IS NOT NEW.host_provider_input_schema_digest
          OR OLD.host_schema_fingerprint IS NOT NEW.host_schema_fingerprint
          OR OLD.host_definition_fingerprint IS NOT NEW.host_definition_fingerprint
          OR OLD.host_manifest_id IS NOT NEW.host_manifest_id
          OR OLD.host_manifest_digest IS NOT NEW.host_manifest_digest
          OR OLD.input_schema_digest IS NOT NEW.input_schema_digest
          OR OLD.output_schema_digest IS NOT NEW.output_schema_digest
          OR OLD.output_may_contain_downloads IS NOT NEW.output_may_contain_downloads
          OR OLD.operation_version IS NOT NEW.operation_version
          OR OLD.toolkit_slug IS NOT NEW.toolkit_slug
          OR OLD.account_identity_digest IS NOT NEW.account_identity_digest
          OR OLD.transfer_manifest_digest IS NOT NEW.transfer_manifest_digest
          OR OLD.manifest_binding_digest IS NOT NEW.manifest_binding_digest
          OR OLD.manifest_payload_id IS NOT NEW.manifest_payload_id
          OR OLD.manifest_format IS NOT NEW.manifest_format
          OR OLD.manifest_plaintext_sha256 IS NOT NEW.manifest_plaintext_sha256
          OR OLD.manifest_plaintext_bytes IS NOT NEW.manifest_plaintext_bytes
          OR OLD.manifest_chunk_count IS NOT NEW.manifest_chunk_count
          OR OLD.manifest_sealed_sha256 IS NOT NEW.manifest_sealed_sha256
          OR OLD.manifest_sealed_bytes IS NOT NEW.manifest_sealed_bytes
          OR OLD.consent_requirement IS NOT NEW.consent_requirement
          OR OLD.consent_subject_digest IS NOT NEW.consent_subject_digest
          OR OLD.plan_authority_digest IS NOT NEW.plan_authority_digest
          OR OLD.created_at IS NOT NEW.created_at
        BEGIN SELECT RAISE(ABORT, 'staged transfer plan authority is immutable'); END;

        CREATE TRIGGER trg_staged_transfer_consent_exact_plan
        BEFORE INSERT ON staged_transfer_consent_redemptions
        WHEN NOT EXISTS (
          SELECT 1
            FROM staged_transfer_plans plan
            JOIN pending_approvals approval ON approval.approval_id = NEW.approval_id
           WHERE plan.plan_id = NEW.plan_id
             AND plan.consent_requirement = 'exact_grant'
             AND plan.consent_subject_digest = NEW.consent_subject_digest
             AND approval.session_id = plan.session_id
             AND approval.resume_key = 'host-consent:v1:' || NEW.consent_subject_digest
             AND approval.status = 'resolved'
             AND approval.resolution = 'approved'
             AND approval.resolved_at IS NOT NULL
             AND approval.resolved_at <= approval.expires_at
             AND approval.consumed_at IS NOT NULL
             AND approval.consumed_at <= approval.expires_at
        )
        BEGIN SELECT RAISE(ABORT, 'staged transfer consent grant is not exact and redeemed'); END;
        CREATE TRIGGER trg_staged_transfer_consent_immutable
        BEFORE UPDATE ON staged_transfer_consent_redemptions
        BEGIN SELECT RAISE(ABORT, 'staged transfer consent redemption is immutable'); END;
        CREATE TRIGGER trg_staged_transfer_consent_delete_immutable
        BEFORE DELETE ON staged_transfer_consent_redemptions
        WHEN EXISTS (SELECT 1 FROM staged_transfer_plans WHERE plan_id = OLD.plan_id)
        BEGIN SELECT RAISE(ABORT, 'staged transfer consent redemption is immutable'); END;

        CREATE TRIGGER trg_staged_transfer_reconciliation_exact_attempt
        BEFORE INSERT ON staged_transfer_attempt_reconciliations
        WHEN NOT EXISTS (
          SELECT 1
            FROM staged_transfer_stage_authorities attempt
            JOIN staged_transfer_stages stage ON stage.stage_id = attempt.stage_id
            JOIN staged_transfer_stage_receipts receipt
              ON receipt.stage_authority_id = attempt.stage_authority_id
           WHERE attempt.stage_authority_id = NEW.prior_stage_authority_id
             AND attempt.plan_id = NEW.plan_id
             AND stage.retry_policy = 'reconcile_before_retry'
             AND receipt.terminal_state != 'returned'
        )
        BEGIN SELECT RAISE(ABORT, 'staged reconciliation lacks its exact terminal attempt'); END;
        -- v62 does not yet ship a provider-owned reconciliation carrier. A
        -- copyable disposition/digest must never authorize a second external
        -- body; keep the table forensic-only until that exact kernel lands.
        CREATE TRIGGER trg_staged_transfer_reconciliation_requires_opaque_kernel
        BEFORE INSERT ON staged_transfer_attempt_reconciliations
        BEGIN SELECT RAISE(ABORT, 'staged reconciliation requires its opaque provider kernel'); END;
        CREATE TRIGGER trg_staged_transfer_reconciliation_immutable
        BEFORE UPDATE ON staged_transfer_attempt_reconciliations
        BEGIN SELECT RAISE(ABORT, 'staged reconciliation evidence is immutable'); END;
        CREATE TRIGGER trg_staged_transfer_reconciliation_delete_immutable
        BEFORE DELETE ON staged_transfer_attempt_reconciliations
        WHEN EXISTS (SELECT 1 FROM staged_transfer_plans WHERE plan_id = OLD.plan_id)
        BEGIN SELECT RAISE(ABORT, 'staged reconciliation evidence is immutable'); END;

        CREATE TRIGGER trg_staged_transfer_plan_delete_immutable
        BEFORE DELETE ON staged_transfer_plans
        WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
        BEGIN SELECT RAISE(ABORT, 'staged transfer plan authority is immutable'); END;

        -- State is a cache of the immutable receipt graph, never an authority
        -- source.  A writer may advance it only to the state derived from the
        -- exact receipts that are already durable in this transaction.
        CREATE TRIGGER trg_staged_transfer_plan_state_receipts
        BEFORE UPDATE OF state ON staged_transfer_plans
        WHEN NEW.state IS NOT (
          CASE
            WHEN EXISTS (
              SELECT 1 FROM logical_call_settlements settlement
               WHERE settlement.session_id = OLD.session_id
                 AND settlement.source_user_seq = OLD.source_user_seq
                 AND settlement.logical_tool_call_id = OLD.parent_logical_tool_call_id
            ) THEN 'settled'
            WHEN EXISTS (
              SELECT 1
                FROM staged_transfer_stages stage
                JOIN staged_transfer_stage_authorities attempt ON attempt.stage_id = stage.stage_id
                JOIN staged_transfer_stage_receipts receipt ON receipt.stage_authority_id = attempt.stage_authority_id
               WHERE stage.plan_id = OLD.plan_id
                 AND receipt.terminal_state IN ('unknown','timed_out')
                 AND NOT EXISTS (
                   SELECT 1
                     FROM staged_transfer_stage_authorities recovered_attempt
                     JOIN staged_transfer_stage_receipts recovered_receipt
                       ON recovered_receipt.stage_authority_id = recovered_attempt.stage_authority_id
                    WHERE recovered_attempt.stage_id = stage.stage_id
                      AND recovered_receipt.terminal_state = 'returned'
                 )
            ) THEN 'reconciling'
            WHEN EXISTS (
              SELECT 1
                FROM staged_transfer_stages stage
                JOIN staged_transfer_stage_authorities attempt ON attempt.stage_id = stage.stage_id
                JOIN staged_transfer_stage_receipts receipt ON receipt.stage_authority_id = attempt.stage_authority_id
               WHERE stage.plan_id = OLD.plan_id
                 AND receipt.terminal_state IN ('threw','cancelled')
                 AND NOT EXISTS (
                   SELECT 1
                     FROM staged_transfer_stage_authorities recovered_attempt
                     JOIN staged_transfer_stage_receipts recovered_receipt
                       ON recovered_receipt.stage_authority_id = recovered_attempt.stage_authority_id
                    WHERE recovered_attempt.stage_id = stage.stage_id
                      AND recovered_receipt.terminal_state = 'returned'
                 )
            ) THEN 'failed'
            WHEN EXISTS (
              SELECT 1
                FROM staged_transfer_stages business
                JOIN staged_transfer_stage_authorities attempt ON attempt.stage_id = business.stage_id
                JOIN staged_transfer_stage_receipts receipt ON receipt.stage_authority_id = attempt.stage_authority_id
               WHERE business.plan_id = OLD.plan_id
                 AND business.stage_kind = 'business_execute'
                 AND receipt.terminal_state = 'returned'
            ) AND NOT EXISTS (
              SELECT 1 FROM staged_transfer_stages required_stage
               WHERE required_stage.plan_id = OLD.plan_id
                 AND NOT EXISTS (
                   SELECT 1
                     FROM staged_transfer_stage_authorities required_attempt
                     JOIN staged_transfer_stage_receipts required_receipt
                       ON required_receipt.stage_authority_id = required_attempt.stage_authority_id
                    WHERE required_attempt.stage_id = required_stage.stage_id
                      AND required_receipt.terminal_state = 'returned'
                 )
            ) AND (
              OLD.output_may_contain_downloads = 0
              OR EXISTS (
                SELECT 1 FROM staged_transfer_download_topology_receipts topology
                 WHERE topology.plan_id = OLD.plan_id
              )
            ) THEN 'downloads_complete'
            WHEN EXISTS (
              SELECT 1
                FROM staged_transfer_stages business
                JOIN staged_transfer_stage_authorities attempt ON attempt.stage_id = business.stage_id
                JOIN staged_transfer_stage_receipts receipt ON receipt.stage_authority_id = attempt.stage_authority_id
               WHERE business.plan_id = OLD.plan_id
                 AND business.stage_kind = 'business_execute'
                 AND receipt.terminal_state = 'returned'
            ) THEN 'business_returned'
            WHEN EXISTS (
              SELECT 1 FROM staged_transfer_stage_authorities attempt
               WHERE attempt.plan_id = OLD.plan_id
            ) THEN 'executing'
            ELSE 'prepared'
          END
        )
        BEGIN SELECT RAISE(ABORT, 'staged transfer plan state lacks exact receipts'); END;

        CREATE TRIGGER trg_staged_transfer_stage_exact_plan
        BEFORE INSERT ON staged_transfer_stages
        WHEN NOT EXISTS (
          SELECT 1 FROM staged_transfer_plans p
           WHERE p.plan_id = NEW.plan_id
             AND p.session_id = NEW.session_id
             AND p.source_user_seq = NEW.source_user_seq
             AND p.accepted_task_id = NEW.accepted_task_id
             AND (NEW.depends_on_stage_ordinal IS NULL OR EXISTS (
               SELECT 1 FROM staged_transfer_stages prior
                WHERE prior.plan_id = NEW.plan_id
                  AND prior.stage_ordinal = NEW.depends_on_stage_ordinal
                  AND prior.stage_ordinal < NEW.stage_ordinal
             ))
             AND (NEW.stage_kind NOT IN ('download_transfer','local_commit')
               OR p.output_may_contain_downloads = 1)
             AND (NEW.stage_kind NOT IN ('download_transfer','local_commit') OR (
               NOT EXISTS (
                 SELECT 1 FROM staged_transfer_download_topology_receipts topology
                  WHERE topology.plan_id = NEW.plan_id
               )
               AND EXISTS (
                 SELECT 1
                   FROM staged_transfer_stages business
                   JOIN staged_transfer_stage_authorities business_attempt
                     ON business_attempt.stage_id = business.stage_id
                   JOIN staged_transfer_stage_receipts business_receipt
                     ON business_receipt.stage_authority_id = business_attempt.stage_authority_id
                  WHERE business.plan_id = NEW.plan_id
                    AND business.stage_kind = 'business_execute'
                    AND business_receipt.terminal_state = 'returned'
               )
             ))
             AND (
               (NEW.stage_kind = 'local_snapshot' AND NEW.effect_kind = 'local_write')
               OR (NEW.stage_kind IN ('source_download','download_transfer') AND NEW.effect_kind = 'read')
               OR (NEW.stage_kind IN ('upload_presign','upload_transfer') AND NEW.effect_kind = 'external_write')
               OR (NEW.stage_kind = 'local_commit' AND NEW.effect_kind = 'local_write')
               OR (NEW.stage_kind = 'business_execute' AND NEW.effect_kind = p.parent_effect_kind
                 AND NEW.retry_policy = 'none')
             )
        )
        BEGIN SELECT RAISE(ABORT, 'staged transfer stage is not in its exact plan'); END;

        CREATE TRIGGER trg_staged_transfer_stage_immutable
        BEFORE UPDATE ON staged_transfer_stages
        BEGIN SELECT RAISE(ABORT, 'staged transfer stages are immutable'); END;
        CREATE TRIGGER trg_staged_transfer_stage_delete_immutable
        BEFORE DELETE ON staged_transfer_stages
        WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
        BEGIN SELECT RAISE(ABORT, 'staged transfer stages are immutable'); END;

        CREATE TRIGGER trg_staged_transfer_attempt_exact_stage
        BEFORE INSERT ON staged_transfer_stage_authorities
        WHEN NOT EXISTS (
          SELECT 1
            FROM staged_transfer_stages stage
            JOIN staged_transfer_plans plan ON plan.plan_id = stage.plan_id
            JOIN logical_tool_calls logical
              ON logical.session_id = NEW.session_id
             AND logical.source_user_seq = NEW.source_user_seq
             AND logical.logical_tool_call_id = NEW.logical_tool_call_id
            JOIN run_dispatch_leases lease
              ON lease.scope_id = NEW.lease_scope_id AND lease.lease_id = NEW.lease_id
           WHERE stage.stage_id = NEW.stage_id
             AND stage.plan_id = NEW.plan_id
             AND stage.session_id = NEW.session_id
             AND stage.source_user_seq = NEW.source_user_seq
             AND stage.accepted_task_id = NEW.accepted_task_id
             AND stage.stage_ordinal = NEW.stage_ordinal
             AND stage.stage_kind = NEW.stage_kind
             AND stage.tool_name = NEW.tool_name
             AND stage.effect_kind = NEW.effect_kind
             AND (
               plan.consent_requirement = 'none'
               OR EXISTS (
                 SELECT 1 FROM staged_transfer_consent_redemptions consent
                  WHERE consent.plan_id = plan.plan_id
                    AND consent.consent_subject_digest = plan.consent_subject_digest
               )
             )
             AND logical.accepted_task_id = NEW.accepted_task_id
             AND logical.tool_name = NEW.tool_name
             AND logical.argument_digest = NEW.argument_digest
             AND logical.state = 'open'
             AND lease.session_id = NEW.session_id
             AND lease.source_user_seq = NEW.source_user_seq
             AND lease.accepted_task_id = NEW.accepted_task_id
             AND lease.logical_tool_call_id = NEW.logical_tool_call_id
             AND lease.revoked_at IS NULL
             AND (
               (NEW.stage_kind = 'business_execute'
                 AND NEW.attempt_ordinal = 1
                 AND NEW.retry_of_stage_authority_id IS NULL
                 AND NEW.logical_tool_call_id = plan.parent_logical_tool_call_id)
               OR
               (NEW.stage_kind != 'business_execute'
                 AND NEW.logical_tool_call_id != plan.parent_logical_tool_call_id
                 AND NOT EXISTS (
                   SELECT 1 FROM expected_work_call_bindings b
                    WHERE b.session_id = NEW.session_id
                      AND b.source_user_seq = NEW.source_user_seq
                      AND b.logical_tool_call_id = NEW.logical_tool_call_id
                 )
                 AND (
                   (NEW.attempt_ordinal = 1 AND NEW.retry_of_stage_authority_id IS NULL)
                   OR
                   (NEW.attempt_ordinal > 1 AND EXISTS (
                     SELECT 1
                       FROM staged_transfer_stage_authorities prior
                       JOIN staged_transfer_stage_receipts receipt
                         ON receipt.stage_authority_id = prior.stage_authority_id
                      WHERE prior.stage_id = NEW.stage_id
                        AND prior.attempt_ordinal = NEW.attempt_ordinal - 1
                        AND prior.stage_authority_id = NEW.retry_of_stage_authority_id
                        AND receipt.terminal_state != 'returned'
                        AND stage.retry_policy != 'none'
                        AND (
                          stage.effect_kind NOT IN ('external_write','admin')
                          OR EXISTS (
                            SELECT 1 FROM staged_transfer_attempt_reconciliations reconcile
                             WHERE reconcile.prior_stage_authority_id = prior.stage_authority_id
                               AND reconcile.disposition = 'not_applied'
                          )
                        )
                   ))
                 ))
             )
        )
        BEGIN SELECT RAISE(ABORT, 'staged transfer attempt authority is not exact'); END;

        CREATE TRIGGER trg_staged_transfer_attempt_immutable
        BEFORE UPDATE ON staged_transfer_stage_authorities
        BEGIN SELECT RAISE(ABORT, 'staged transfer attempt authority is immutable'); END;
        CREATE TRIGGER trg_staged_transfer_attempt_delete_immutable
        BEFORE DELETE ON staged_transfer_stage_authorities
        WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
        BEGIN SELECT RAISE(ABORT, 'staged transfer attempt authority is immutable'); END;

        CREATE TRIGGER trg_staged_transfer_child_no_expected_work
        BEFORE INSERT ON expected_work_call_bindings
        WHEN EXISTS (
          SELECT 1 FROM staged_transfer_stage_authorities attempt
           WHERE attempt.session_id = NEW.session_id
             AND attempt.source_user_seq = NEW.source_user_seq
             AND attempt.logical_tool_call_id = NEW.logical_tool_call_id
             AND attempt.stage_kind != 'business_execute'
        )
        BEGIN SELECT RAISE(ABORT, 'staged control dependencies cannot bind expected work'); END;

        CREATE TRIGGER trg_staged_transfer_physical_exact_attempt
        BEFORE INSERT ON physical_dispatches
        WHEN (
          EXISTS (
            SELECT 1 FROM staged_transfer_stage_authorities attempt
             WHERE attempt.session_id = NEW.session_id
               AND attempt.source_user_seq = NEW.source_user_seq
               AND (attempt.logical_tool_call_id = NEW.logical_tool_call_id
                 OR attempt.physical_dispatch_id = NEW.physical_dispatch_id)
               AND (
                 attempt.accepted_task_id != NEW.accepted_task_id
                 OR attempt.logical_tool_call_id != NEW.logical_tool_call_id
                 OR attempt.physical_dispatch_id != NEW.physical_dispatch_id
                 OR attempt.tool_name != NEW.tool_name
                 OR attempt.argument_digest != NEW.argument_digest
                 OR attempt.provider_argument_digest != NEW.provider_argument_digest
                 OR attempt.lease_scope_id IS NOT NEW.lease_scope_id
                 OR attempt.lease_id IS NOT NEW.lease_id
                 OR attempt.authority_digest IS NOT NEW.staged_authority_digest
                 OR EXISTS (
                   SELECT 1 FROM staged_transfer_stages stage
                    WHERE stage.stage_id = attempt.stage_id
                      AND stage.depends_on_stage_ordinal IS NOT NULL
                      AND NOT EXISTS (
                        SELECT 1
                          FROM staged_transfer_stages dependency
                          JOIN staged_transfer_stage_authorities dependency_attempt
                            ON dependency_attempt.stage_id = dependency.stage_id
                          JOIN staged_transfer_stage_receipts dependency_receipt
                            ON dependency_receipt.stage_authority_id = dependency_attempt.stage_authority_id
                         WHERE dependency.plan_id = stage.plan_id
                           AND dependency.stage_ordinal = stage.depends_on_stage_ordinal
                           AND dependency_receipt.terminal_state = 'returned'
                      )
                 )
                 OR (attempt.stage_kind = 'business_execute' AND EXISTS (
                   SELECT 1 FROM staged_transfer_stages upload
                    WHERE upload.plan_id = attempt.plan_id
                      AND upload.stage_kind = 'upload_transfer'
                      AND NOT EXISTS (
                        SELECT 1
                          FROM staged_transfer_stage_authorities upload_attempt
                          JOIN staged_transfer_stage_receipts upload_receipt
                            ON upload_receipt.stage_authority_id = upload_attempt.stage_authority_id
                         WHERE upload_attempt.stage_id = upload.stage_id
                           AND upload_receipt.terminal_state = 'returned'
                      )
                 ))
               )
          )
          OR (NEW.staged_authority_digest IS NOT NULL AND NOT EXISTS (
            SELECT 1 FROM staged_transfer_stage_authorities exact_attempt
             WHERE exact_attempt.session_id = NEW.session_id
               AND exact_attempt.source_user_seq = NEW.source_user_seq
               AND exact_attempt.physical_dispatch_id = NEW.physical_dispatch_id
               AND exact_attempt.logical_tool_call_id = NEW.logical_tool_call_id
               AND exact_attempt.authority_digest = NEW.staged_authority_digest
          ))
        )
        BEGIN SELECT RAISE(ABORT, 'staged physical dispatch lacks exact opaque attempt authority'); END;

        CREATE TRIGGER trg_physical_return_checkpoint_exact_return
        BEFORE INSERT ON physical_dispatch_return_checkpoints
        WHEN NOT EXISTS (
          SELECT 1
            FROM staged_transfer_stage_authorities attempt
            JOIN physical_dispatches physical
              ON physical.session_id = attempt.session_id
             AND physical.source_user_seq = attempt.source_user_seq
             AND physical.physical_dispatch_id = attempt.physical_dispatch_id
           WHERE attempt.stage_authority_id = NEW.stage_authority_id
             AND attempt.session_id = NEW.session_id
             AND attempt.source_user_seq = NEW.source_user_seq
             AND attempt.accepted_task_id = NEW.accepted_task_id
             AND attempt.logical_tool_call_id = NEW.logical_tool_call_id
             AND attempt.physical_dispatch_id = NEW.physical_dispatch_id
             AND attempt.stage_kind = NEW.stage_kind
             AND attempt.stage_ordinal = NEW.stage_ordinal
             AND attempt.attempt_ordinal = NEW.attempt_ordinal
             AND attempt.tool_name = NEW.tool_name
             AND attempt.argument_digest = NEW.argument_digest
             AND attempt.provider_argument_digest = NEW.provider_argument_digest
             AND attempt.lease_scope_id = NEW.lease_scope_id
             AND attempt.lease_id = NEW.lease_id
             AND physical.state = 'returned'
             AND physical.staged_authority_digest = attempt.authority_digest
        )
        BEGIN SELECT RAISE(ABORT, 'physical return checkpoint requires exact returned attempt'); END;

        CREATE TRIGGER trg_physical_return_checkpoint_immutable
        BEFORE UPDATE ON physical_dispatch_return_checkpoints
        BEGIN SELECT RAISE(ABORT, 'physical return checkpoints are immutable'); END;
        CREATE TRIGGER trg_physical_return_checkpoint_delete_immutable
        BEFORE DELETE ON physical_dispatch_return_checkpoints
        WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
        BEGIN SELECT RAISE(ABORT, 'physical return checkpoints are immutable'); END;

        CREATE TRIGGER trg_staged_transfer_receipt_exact_attempt
        BEFORE INSERT ON staged_transfer_stage_receipts
        WHEN NOT EXISTS (
          SELECT 1
            FROM staged_transfer_stage_authorities attempt
            JOIN physical_dispatches physical
              ON physical.session_id = attempt.session_id
             AND physical.source_user_seq = attempt.source_user_seq
             AND physical.physical_dispatch_id = attempt.physical_dispatch_id
           WHERE attempt.stage_authority_id = NEW.stage_authority_id
             AND attempt.stage_id = NEW.stage_id
             AND attempt.plan_id = NEW.plan_id
             AND attempt.session_id = NEW.session_id
             AND attempt.source_user_seq = NEW.source_user_seq
             AND attempt.stage_ordinal = NEW.stage_ordinal
             AND attempt.attempt_ordinal = NEW.attempt_ordinal
             AND attempt.physical_dispatch_id = NEW.physical_dispatch_id
             AND physical.staged_authority_digest = attempt.authority_digest
             AND physical.state = NEW.terminal_state
             AND (NEW.terminal_state != 'returned' OR EXISTS (
               SELECT 1 FROM physical_dispatch_return_checkpoints checkpoint
                WHERE checkpoint.stage_authority_id = NEW.stage_authority_id
                  AND checkpoint.physical_dispatch_id = NEW.physical_dispatch_id
                  AND checkpoint.payload_plaintext_sha256 = NEW.result_digest
             ))
        )
        BEGIN SELECT RAISE(ABORT, 'staged transfer receipt requires exact terminal attempt'); END;

        CREATE TRIGGER trg_staged_transfer_one_success_per_stage
        BEFORE INSERT ON staged_transfer_stage_receipts
        WHEN NEW.terminal_state = 'returned' AND EXISTS (
          SELECT 1
            FROM staged_transfer_stage_receipts prior_receipt
            JOIN staged_transfer_stage_authorities prior_attempt
              ON prior_attempt.stage_authority_id = prior_receipt.stage_authority_id
           WHERE prior_attempt.stage_id = NEW.stage_id
             AND prior_receipt.terminal_state = 'returned'
        )
        BEGIN SELECT RAISE(ABORT, 'staged transfer stage already has a returned attempt'); END;

        CREATE TRIGGER trg_staged_transfer_receipt_immutable
        BEFORE UPDATE ON staged_transfer_stage_receipts
        BEGIN SELECT RAISE(ABORT, 'staged transfer receipts are append-only'); END;
        CREATE TRIGGER trg_staged_transfer_receipt_delete_immutable
        BEFORE DELETE ON staged_transfer_stage_receipts
        WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
        BEGIN SELECT RAISE(ABORT, 'staged transfer receipts are append-only'); END;

        CREATE TRIGGER trg_staged_transfer_download_topology_exact
        BEFORE INSERT ON staged_transfer_download_topology_receipts
        WHEN clementine_staged_topology_admitted_v1(
          NEW.plan_id,
          NEW.business_stage_authority_id,
          NEW.business_result_digest,
          NEW.topology_digest,
          NEW.successor_stage_count
        ) != 1 OR NOT EXISTS (
          SELECT 1
            FROM staged_transfer_plans plan
            JOIN staged_transfer_stages business
              ON business.plan_id = plan.plan_id
             AND business.stage_kind = 'business_execute'
            JOIN staged_transfer_stage_authorities business_attempt
              ON business_attempt.stage_id = business.stage_id
            JOIN staged_transfer_stage_receipts business_receipt
              ON business_receipt.stage_authority_id = business_attempt.stage_authority_id
            JOIN physical_dispatch_return_checkpoints checkpoint
              ON checkpoint.stage_authority_id = business_attempt.stage_authority_id
           WHERE plan.plan_id = NEW.plan_id
             AND plan.output_may_contain_downloads = 1
             AND business_attempt.stage_authority_id = NEW.business_stage_authority_id
             AND business_receipt.terminal_state = 'returned'
             AND business_receipt.result_digest = NEW.business_result_digest
             AND checkpoint.payload_plaintext_sha256 = NEW.business_result_digest
             AND NEW.successor_stage_count = (
               SELECT COUNT(*) FROM staged_transfer_stages successor
                WHERE successor.plan_id = plan.plan_id
                  AND successor.stage_ordinal > business.stage_ordinal
             )
             AND NOT EXISTS (
               SELECT 1 FROM staged_transfer_stages successor
                WHERE successor.plan_id = plan.plan_id
                  AND successor.stage_ordinal > business.stage_ordinal
                  AND (
                    ((successor.stage_ordinal - business.stage_ordinal) % 2 = 1 AND (
                      successor.stage_kind != 'download_transfer'
                      OR successor.depends_on_stage_ordinal != business.stage_ordinal
                    ))
                    OR
                    ((successor.stage_ordinal - business.stage_ordinal) % 2 = 0 AND (
                      successor.stage_kind != 'local_commit'
                      OR successor.depends_on_stage_ordinal != successor.stage_ordinal - 1
                      OR NOT EXISTS (
                        SELECT 1 FROM staged_transfer_stages download
                         WHERE download.plan_id = successor.plan_id
                           AND download.stage_ordinal = successor.stage_ordinal - 1
                           AND download.stage_kind = 'download_transfer'
                           AND download.json_pointer_digest IS successor.json_pointer_digest
                      )
                    ))
                  )
             )
        )
        BEGIN SELECT RAISE(ABORT, 'staged download topology lacks its exact business projection'); END;
        CREATE TRIGGER trg_staged_transfer_download_topology_immutable
        BEFORE UPDATE ON staged_transfer_download_topology_receipts
        BEGIN SELECT RAISE(ABORT, 'staged download topology receipts are append-only'); END;
        CREATE TRIGGER trg_staged_transfer_download_topology_delete_immutable
        BEFORE DELETE ON staged_transfer_download_topology_receipts
        WHEN EXISTS (SELECT 1 FROM staged_transfer_plans WHERE plan_id = OLD.plan_id)
        BEGIN SELECT RAISE(ABORT, 'staged download topology receipts are append-only'); END;

        CREATE TRIGGER trg_staged_transfer_secret_exact_attempt
        BEFORE INSERT ON staged_transfer_secret_payloads
        WHEN NOT EXISTS (
          SELECT 1
            FROM staged_transfer_stage_authorities attempt
            JOIN staged_transfer_stage_receipts receipt
              ON receipt.stage_authority_id = attempt.stage_authority_id
           WHERE attempt.stage_authority_id = NEW.stage_authority_id
             AND attempt.stage_kind = 'upload_presign'
             AND receipt.terminal_state = 'returned'
        )
        BEGIN SELECT RAISE(ABORT, 'staged signed URL lacks its exact returned presign attempt'); END;
        CREATE TRIGGER trg_staged_transfer_secret_immutable
        BEFORE UPDATE ON staged_transfer_secret_payloads
        BEGIN SELECT RAISE(ABORT, 'staged secret payload ownership is immutable'); END;
        CREATE TRIGGER trg_staged_transfer_secret_delete_immutable
        BEFORE DELETE ON staged_transfer_secret_payloads
        WHEN EXISTS (
          SELECT 1 FROM staged_transfer_stage_authorities
           WHERE stage_authority_id = OLD.stage_authority_id
        )
        BEGIN SELECT RAISE(ABORT, 'staged secret payload ownership is immutable'); END;

        CREATE TRIGGER trg_staged_transfer_blob_exact_stage
        BEFORE INSERT ON staged_transfer_blob_owners
        WHEN clementine_staged_blob_owner_admitted_v1(
          NEW.plan_id,
          NEW.stage_id,
          NEW.blob_sha256,
          NEW.blob_md5,
          NEW.blob_bytes
        ) != 1 OR NOT EXISTS (
          SELECT 1
            FROM staged_transfer_stages stage
            JOIN staged_transfer_stage_authorities attempt ON attempt.stage_id = stage.stage_id
            JOIN staged_transfer_stage_receipts receipt
              ON receipt.stage_authority_id = attempt.stage_authority_id
           WHERE stage.plan_id = NEW.plan_id
             AND stage.stage_id = NEW.stage_id
             AND stage.stage_kind IN ('local_snapshot','source_download','download_transfer','local_commit')
             AND receipt.terminal_state = 'returned'
        )
        BEGIN SELECT RAISE(ABORT, 'staged blob ownership lacks its exact returned stage'); END;
        CREATE TRIGGER trg_staged_transfer_blob_immutable
        BEFORE UPDATE ON staged_transfer_blob_owners
        BEGIN SELECT RAISE(ABORT, 'staged blob ownership is immutable'); END;
        CREATE TRIGGER trg_staged_transfer_blob_delete_immutable
        BEFORE DELETE ON staged_transfer_blob_owners
        WHEN EXISTS (SELECT 1 FROM staged_transfer_plans WHERE plan_id = OLD.plan_id)
        BEGIN SELECT RAISE(ABORT, 'staged blob ownership is immutable'); END;

        CREATE TRIGGER trg_staged_transfer_logical_settlement_fence
        BEFORE INSERT ON logical_call_settlements
        WHEN (
          EXISTS (
            SELECT 1 FROM staged_transfer_stage_authorities attempt
             WHERE attempt.session_id = NEW.session_id
               AND attempt.source_user_seq = NEW.source_user_seq
               AND attempt.logical_tool_call_id = NEW.logical_tool_call_id
               AND attempt.stage_kind != 'business_execute'
               AND (
                 NOT EXISTS (
                   SELECT 1 FROM staged_transfer_stage_receipts receipt
                    WHERE receipt.stage_authority_id = attempt.stage_authority_id
                 )
                 OR (NEW.outcome_kind IN ('succeeded','empty_result') AND NOT EXISTS (
                   SELECT 1 FROM staged_transfer_stage_receipts receipt
                    WHERE receipt.stage_authority_id = attempt.stage_authority_id
                      AND receipt.terminal_state = 'returned'
                 ))
               )
          )
          OR EXISTS (
            SELECT 1 FROM staged_transfer_plans plan
             WHERE plan.session_id = NEW.session_id
               AND plan.source_user_seq = NEW.source_user_seq
               AND plan.parent_logical_tool_call_id = NEW.logical_tool_call_id
               AND (
                 NOT EXISTS (
                   SELECT 1
                     FROM staged_transfer_stages business
                     JOIN staged_transfer_stage_authorities business_attempt
                       ON business_attempt.stage_id = business.stage_id
                     JOIN staged_transfer_stage_receipts business_receipt
                       ON business_receipt.stage_authority_id = business_attempt.stage_authority_id
                    WHERE business.plan_id = plan.plan_id
                      AND business.stage_kind = 'business_execute'
                 )
                 OR (NEW.outcome_kind IN ('succeeded','empty_result') AND EXISTS (
                   SELECT 1 FROM staged_transfer_stages required_stage
                    WHERE required_stage.plan_id = plan.plan_id
                      AND NOT EXISTS (
                        SELECT 1
                          FROM staged_transfer_stage_authorities required_attempt
                          JOIN staged_transfer_stage_receipts required_receipt
                            ON required_receipt.stage_authority_id = required_attempt.stage_authority_id
                         WHERE required_attempt.stage_id = required_stage.stage_id
                           AND required_receipt.terminal_state = 'returned'
                      )
                 ))
                 OR (NEW.outcome_kind IN ('succeeded','empty_result')
                   AND plan.output_may_contain_downloads = 1
                   AND NOT EXISTS (
                     SELECT 1 FROM staged_transfer_download_topology_receipts topology
                      WHERE topology.plan_id = plan.plan_id
                   ))
               )
          )
        )
        BEGIN SELECT RAISE(ABORT, 'staged logical settlement lacks exact terminal receipt graph'); END;

        CREATE TRIGGER trg_staged_transfer_child_no_progress
        BEFORE INSERT ON logical_call_settlements
        WHEN EXISTS (
          SELECT 1 FROM staged_transfer_stage_authorities attempt
           WHERE attempt.session_id = NEW.session_id
             AND attempt.source_user_seq = NEW.source_user_seq
             AND attempt.logical_tool_call_id = NEW.logical_tool_call_id
             AND attempt.stage_kind != 'business_execute'
        ) AND (
          NEW.business_call != 0 OR NEW.progress_claimed != 0
          OR NEW.requirement_id IS NOT NULL OR NEW.continues_requirement != 0
        )
        BEGIN SELECT RAISE(ABORT, 'staged control dependencies cannot discharge expected work'); END;
      `);
          const foreignKeyViolations = [
            "staged_transfer_plans",
            "staged_transfer_stages",
            "staged_transfer_stage_authorities",
            "physical_dispatch_return_checkpoints",
            "staged_transfer_stage_receipts",
            "staged_transfer_download_topology_receipts",
            "staged_transfer_attempt_reconciliations",
            "staged_transfer_consent_redemptions",
            "staged_transfer_secret_payloads",
            "staged_transfer_blob_owners"
          ].flatMap((table) => db.prepare(`PRAGMA foreign_key_check(${table})`).all());
          if (foreignKeyViolations.length > 0) {
            throw new Error(
              `schema v62 staged-transfer rebuild failed foreign-key validation: ${JSON.stringify(foreignKeyViolations.slice(0, 8))}`
            );
          }
        }
      },
      {
        /**
         * v62 could persist a returned presign secret through a copyable SQL row.
         * No production upload body consumed it, so v63 retires every such row and
         * requires the physical-return module's connection-local, tuple-exact
         * admission for all newly returned presign attempts.
         */
        version: 63,
        sql: "",
        backfill: (db) => {
          const secretTable = db.prepare(
            "SELECT 1 AS ok FROM sqlite_master WHERE type = 'table' AND name = 'staged_transfer_secret_payloads'"
          ).get();
          if (!secretTable) return;
          db.exec(`
      DROP TRIGGER IF EXISTS trg_staged_transfer_secret_exact_attempt;
      DROP TRIGGER IF EXISTS trg_staged_transfer_secret_immutable;
      DROP TRIGGER IF EXISTS trg_staged_transfer_secret_delete_immutable;

      DELETE FROM staged_transfer_secret_payloads;

      CREATE TRIGGER trg_staged_transfer_secret_exact_attempt
      BEFORE INSERT ON staged_transfer_secret_payloads
      WHEN clementine_staged_secret_admitted_v1(
        NEW.payload_id,
        NEW.stage_authority_id,
        NEW.binding_digest,
        NEW.plaintext_sha256,
        NEW.plaintext_bytes,
        NEW.chunk_count,
        NEW.sealed_sha256,
        NEW.sealed_bytes,
        NEW.expires_at
      ) != 1 OR NOT EXISTS (
        SELECT 1
          FROM staged_transfer_stage_authorities attempt
          JOIN staged_transfer_stage_receipts receipt
            ON receipt.stage_authority_id = attempt.stage_authority_id
          JOIN physical_dispatch_return_checkpoints checkpoint
            ON checkpoint.stage_authority_id = attempt.stage_authority_id
         WHERE attempt.stage_authority_id = NEW.stage_authority_id
           AND attempt.stage_kind = 'upload_presign'
           AND receipt.terminal_state = 'returned'
           AND receipt.result_digest = NEW.plaintext_sha256
           AND checkpoint.payload_plaintext_sha256 = NEW.plaintext_sha256
      )
      BEGIN SELECT RAISE(ABORT, 'staged signed URL lacks its exact opaque return authority'); END;

      CREATE TRIGGER trg_staged_transfer_secret_immutable
      BEFORE UPDATE ON staged_transfer_secret_payloads
      BEGIN SELECT RAISE(ABORT, 'staged secret payload ownership is immutable'); END;
      CREATE TRIGGER trg_staged_transfer_secret_delete_immutable
      BEFORE DELETE ON staged_transfer_secret_payloads
      WHEN EXISTS (
        SELECT 1 FROM staged_transfer_stage_authorities
         WHERE stage_authority_id = OLD.stage_authority_id
      )
      BEGIN SELECT RAISE(ABORT, 'staged secret payload ownership is immutable'); END;
      `);
        }
      },
      {
        /**
         * Durable workflow_v3 call identity. Existing v1/v2 activation rows stay
         * byte-identical; one append-only 1:1 binding supplies the exact mutation
         * authority fields that v63 could not represent. The accepted-root rebuild
         * widens only its closed authority-kind CHECK and adds the exact v3 join.
         * Any pre-standard lookalike table is retired instead of trusted.
         */
        version: 64,
        sql: "",
        foreignKeysOff: true,
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) return;
          for (const prerequisite of [
            "sessions",
            "events",
            "pending_approvals",
            "workflow_node_invocation_activations",
            "accepted_turn_call_authorities",
            "logical_tool_calls"
          ]) {
            if (!tables.has(prerequisite)) {
              throw new Error(`schema v64 prerequisite missing: ${prerequisite}`);
            }
          }
          db.exec(`
        DROP TRIGGER IF EXISTS trg_workflow_v3_call_binding_exact_activation;
        DROP TRIGGER IF EXISTS trg_workflow_v3_call_binding_immutable;
        DROP TRIGGER IF EXISTS trg_workflow_v3_call_binding_delete_immutable;
        DROP TABLE IF EXISTS workflow_v3_call_activation_bindings;

        CREATE TABLE workflow_v3_call_activation_bindings (
          activation_id              TEXT PRIMARY KEY
                                     REFERENCES workflow_node_invocation_activations(activation_id)
                                     ON DELETE CASCADE,
          session_id                 TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
          authority_binding_digest   TEXT NOT NULL UNIQUE
                                     CHECK (length(authority_binding_digest) = 64
                                       AND authority_binding_digest NOT GLOB '*[^0-9a-f]*'),
          requirement_id             TEXT NOT NULL CHECK (length(requirement_id) BETWEEN 1 AND 256),
          logical_capability_id      TEXT NOT NULL CHECK (length(logical_capability_id) BETWEEN 1 AND 256),
          effect                     TEXT NOT NULL
                                     CHECK (effect IN ('host_only','local_write','external_write','admin')),
          canonical_argument_digest  TEXT NOT NULL
                                     CHECK (length(canonical_argument_digest) = 64
                                       AND canonical_argument_digest NOT GLOB '*[^0-9a-f]*'),
          source_argument_digest     TEXT NOT NULL
                                     CHECK (length(source_argument_digest) = 64
                                       AND source_argument_digest NOT GLOB '*[^0-9a-f]*'),
          obligation_digest          TEXT NOT NULL
                                     CHECK (length(obligation_digest) = 64
                                       AND obligation_digest NOT GLOB '*[^0-9a-f]*'),
          capability_id              TEXT NOT NULL CHECK (length(capability_id) BETWEEN 1 AND 512),
          manifest_id                TEXT NOT NULL CHECK (length(manifest_id) BETWEEN 1 AND 512),
          manifest_digest            TEXT NOT NULL
                                     CHECK (length(manifest_digest) = 64
                                       AND manifest_digest NOT GLOB '*[^0-9a-f]*'),
          operation_id               TEXT NOT NULL CHECK (length(operation_id) BETWEEN 1 AND 512),
          operation_version          TEXT NOT NULL CHECK (length(operation_version) BETWEEN 1 AND 128),
          schema_digest              TEXT NOT NULL
                                     CHECK (length(schema_digest) = 64
                                       AND schema_digest NOT GLOB '*[^0-9a-f]*'),
          provider_version           TEXT NOT NULL CHECK (length(provider_version) BETWEEN 1 AND 128),
          live_fingerprint           TEXT NOT NULL
                                     CHECK (length(live_fingerprint) = 64
                                       AND live_fingerprint NOT GLOB '*[^0-9a-f]*'),
          account_id                 TEXT NOT NULL CHECK (length(account_id) BETWEEN 1 AND 512),
          invoke_port_id             TEXT NOT NULL CHECK (length(invoke_port_id) BETWEEN 1 AND 512),
          argument_compiler_id       TEXT NOT NULL CHECK (length(argument_compiler_id) BETWEEN 1 AND 256),
          argument_compiler_version  TEXT NOT NULL CHECK (length(argument_compiler_version) BETWEEN 1 AND 128),
          activated_at               TEXT NOT NULL
        );

        CREATE INDEX idx_workflow_v3_call_binding_session
          ON workflow_v3_call_activation_bindings(session_id, activated_at);

        CREATE TRIGGER trg_workflow_v3_call_binding_exact_activation
        BEFORE INSERT ON workflow_v3_call_activation_bindings
        WHEN clementine_workflow_v3_binding_admitted_v1(
          NEW.activation_id,
          NEW.session_id,
          NEW.authority_binding_digest,
          NEW.requirement_id,
          NEW.logical_capability_id,
          NEW.effect,
          NEW.canonical_argument_digest,
          NEW.source_argument_digest,
          NEW.obligation_digest,
          NEW.capability_id,
          NEW.manifest_id,
          NEW.manifest_digest,
          NEW.operation_id,
          NEW.operation_version,
          NEW.schema_digest,
          NEW.provider_version,
          NEW.live_fingerprint,
          NEW.account_id,
          NEW.invoke_port_id,
          NEW.argument_compiler_id,
          NEW.argument_compiler_version,
          NEW.activated_at
        ) != 1 OR NOT EXISTS (
          SELECT 1 FROM workflow_node_invocation_activations activation
           WHERE activation.activation_id = NEW.activation_id
             AND activation.session_id = NEW.session_id
             AND activation.logical_call_id IS NOT NULL
             AND activation.activated_at = NEW.activated_at
        )
        BEGIN
          SELECT RAISE(ABORT, 'workflow v3 binding lacks its exact opaque activation admission');
        END;

        CREATE TRIGGER trg_workflow_v3_call_binding_immutable
        BEFORE UPDATE ON workflow_v3_call_activation_bindings
        BEGIN
          SELECT RAISE(ABORT, 'workflow v3 activation bindings are immutable');
        END;

        CREATE TRIGGER trg_workflow_v3_call_binding_delete_immutable
        BEFORE DELETE ON workflow_v3_call_activation_bindings
        WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
        BEGIN
          SELECT RAISE(ABORT, 'workflow v3 activation bindings are append-only');
        END;
      `);
          rebuildAcceptedCallAuthoritiesForProductionHost(db, true);
          const relevantTables = foreignKeyClosure(db, [
            "workflow_v3_call_activation_bindings",
            "accepted_turn_call_authorities"
          ]);
          const violations = db.pragma("foreign_key_check").filter((violation) => relevantTables.has(violation.table));
          if (violations.length > 0) {
            throw new Error(`schema v64 foreign-key check failed for ${violations.length} workflow-v3 row(s)`);
          }
          const integrity = db.pragma("integrity_check");
          if (integrity.length !== 1 || integrity[0]?.integrity_check !== "ok") {
            throw new Error(`schema v64 integrity check failed: ${JSON.stringify(integrity).slice(0, 240)}`);
          }
        }
      },
      {
        // Lossless large tool-output storage. The legacy row remains the compact
        // lookup/preview record; bytes beyond its inline segment live in ordered,
        // content-addressed chunks. This removes the old 16MB terminal tail cliff
        // without forcing every lookup or SQL scan to hydrate a very large value.
        // Exact invocation rows get an independent chunk spine so reused SDK call
        // ids cannot alias evidence across physical attempts.
        version: 65,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          const addManifestColumns = (table) => {
            if (!tables.has(table)) return;
            const columns = new Set(
              db.prepare(`PRAGMA table_info(${table})`).all().map((column) => column.name)
            );
            if (!columns.has("output_sha256")) {
              db.exec(`ALTER TABLE ${table} ADD COLUMN output_sha256 TEXT`);
            }
            if (!columns.has("chunk_count")) {
              db.exec(`ALTER TABLE ${table} ADD COLUMN chunk_count INTEGER NOT NULL DEFAULT 0`);
            }
            if (!columns.has("output_chars")) {
              db.exec(`ALTER TABLE ${table} ADD COLUMN output_chars INTEGER`);
            }
            if (!columns.has("inline_sha256")) {
              db.exec(`ALTER TABLE ${table} ADD COLUMN inline_sha256 TEXT`);
            }
            if (!columns.has("inline_bytes")) {
              db.exec(`ALTER TABLE ${table} ADD COLUMN inline_bytes INTEGER`);
            }
            if (!columns.has("inline_chars")) {
              db.exec(`ALTER TABLE ${table} ADD COLUMN inline_chars INTEGER`);
            }
          };
          addManifestColumns("tool_outputs");
          addManifestColumns("tool_output_invocations");
          for (const table of [
            "tool_output_chunks",
            "tool_output_invocation_chunks",
            "tool_search_continuations"
          ]) {
            if (tables.has(table)) {
              throw new Error(`schema v65 refuses preexisting unsanctioned table ${table}`);
            }
          }
          if (tables.has("tool_outputs")) {
            const select = db.prepare(
              `SELECT session_id, call_id, output_full FROM tool_outputs
            WHERE output_sha256 IS NULL OR output_chars IS NULL OR inline_sha256 IS NULL
               OR inline_bytes IS NULL OR inline_chars IS NULL
            ORDER BY session_id, call_id LIMIT 1`
            );
            const update = db.prepare(
              `UPDATE tool_outputs
              SET output_sha256 = COALESCE(output_sha256, ?),
                  output_chars = COALESCE(output_chars, ?),
                  inline_sha256 = ?, inline_bytes = ?, inline_chars = ?
            WHERE session_id = ? AND call_id = ?`
            );
            while (true) {
              const row = select.get();
              if (!row) break;
              const bytes = Buffer.from(row.output_full, "utf8");
              const sha2562 = (0, import_node_crypto6.createHash)("sha256").update(bytes).digest("hex");
              update.run(
                sha2562,
                row.output_full.length,
                sha2562,
                bytes.length,
                row.output_full.length,
                row.session_id,
                row.call_id
              );
            }
          }
          if (tables.has("tool_output_invocations")) {
            const select = db.prepare(
              `SELECT session_id, call_id, invocation_nonce, output_full
             FROM tool_output_invocations
            WHERE output_sha256 IS NULL OR output_chars IS NULL OR inline_sha256 IS NULL
               OR inline_bytes IS NULL OR inline_chars IS NULL
            ORDER BY session_id, call_id, invocation_nonce LIMIT 1`
            );
            const update = db.prepare(
              `UPDATE tool_output_invocations
              SET output_sha256 = COALESCE(output_sha256, ?),
                  output_chars = COALESCE(output_chars, ?),
                  inline_sha256 = ?, inline_bytes = ?, inline_chars = ?
            WHERE session_id = ? AND call_id = ? AND invocation_nonce = ?`
            );
            while (true) {
              const row = select.get();
              if (!row) break;
              const bytes = Buffer.from(row.output_full, "utf8");
              const sha2562 = (0, import_node_crypto6.createHash)("sha256").update(bytes).digest("hex");
              update.run(
                sha2562,
                row.output_full.length,
                sha2562,
                bytes.length,
                row.output_full.length,
                row.session_id,
                row.call_id,
                row.invocation_nonce
              );
            }
          }
          if (tables.has("tool_outputs")) {
            db.exec(`
          CREATE TABLE tool_output_chunks (
            session_id    TEXT NOT NULL,
            call_id       TEXT NOT NULL,
            chunk_index   INTEGER NOT NULL CHECK (chunk_index >= 0),
            chunk_bytes   BLOB NOT NULL,
            content_bytes INTEGER NOT NULL CHECK (content_bytes >= 0),
            char_start     INTEGER NOT NULL CHECK (char_start >= 0),
            char_count     INTEGER NOT NULL CHECK (char_count > 0),
            chunk_sha256  TEXT NOT NULL
                          CHECK (length(chunk_sha256) = 64
                            AND chunk_sha256 NOT GLOB '*[^0-9a-f]*'),
            PRIMARY KEY (session_id, call_id, chunk_index),
            FOREIGN KEY (session_id, call_id)
              REFERENCES tool_outputs(session_id, call_id) ON DELETE CASCADE
          );
          CREATE INDEX idx_tool_outputs_session_created_call
            ON tool_outputs(session_id, created_at DESC, call_id ASC);
        `);
          }
          if (tables.has("tool_output_invocations")) {
            db.exec(`
          CREATE TABLE tool_output_invocation_chunks (
            session_id       TEXT NOT NULL,
            call_id          TEXT NOT NULL,
            invocation_nonce TEXT NOT NULL,
            chunk_index      INTEGER NOT NULL CHECK (chunk_index >= 0),
            chunk_bytes      BLOB NOT NULL,
            content_bytes    INTEGER NOT NULL CHECK (content_bytes >= 0),
            char_start        INTEGER NOT NULL CHECK (char_start >= 0),
            char_count        INTEGER NOT NULL CHECK (char_count > 0),
            chunk_sha256     TEXT NOT NULL
                             CHECK (length(chunk_sha256) = 64
                               AND chunk_sha256 NOT GLOB '*[^0-9a-f]*'),
            PRIMARY KEY (session_id, call_id, invocation_nonce, chunk_index),
            FOREIGN KEY (session_id, call_id, invocation_nonce)
              REFERENCES tool_output_invocations(session_id, call_id, invocation_nonce)
              ON DELETE CASCADE
          );
        `);
          }
          if (tables.has("sessions")) {
            db.exec(`
          CREATE TABLE tool_search_continuations (
            session_id     TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
            kind           TEXT NOT NULL CHECK (kind IN ('page', 'schema')),
            content_sha256 TEXT NOT NULL
                           CHECK (length(content_sha256) = 64
                             AND content_sha256 NOT GLOB '*[^0-9a-f]*'),
            content_text   TEXT NOT NULL,
            content_bytes  INTEGER NOT NULL
                           CHECK (content_bytes BETWEEN 1 AND 1048576),
            created_at     TEXT NOT NULL,
            accessed_at    TEXT NOT NULL,
            access_seq     INTEGER NOT NULL CHECK (access_seq > 0),
            PRIMARY KEY (session_id, kind, content_sha256)
          );
          CREATE INDEX idx_tool_search_continuations_lru
            ON tool_search_continuations(session_id, access_seq);
        `);
          }
          const violations = db.pragma("foreign_key_check");
          const relevant = /* @__PURE__ */ new Set([
            "tool_output_chunks",
            "tool_output_invocation_chunks",
            "tool_search_continuations"
          ]);
          const v65Violations = violations.filter((row) => relevant.has(row.table));
          if (v65Violations.length > 0) {
            throw new Error(`schema v65 foreign-key check failed for ${v65Violations.length} row(s)`);
          }
        }
      },
      {
        // Exact, encrypted provider-request reconstruction. The append-only owner
        // row contains only hashes, durable source references, and an encrypted
        // payload reference; raw model-visible bytes remain outside SQLite.
        version: 66,
        sql: "",
        backfill: (db) => {
          const existing = db.prepare(
            `SELECT 1 AS ok FROM sqlite_master
          WHERE type = 'table' AND name = 'model_request_provenance'`
          ).get();
          if (existing) {
            throw new Error("schema v66 refuses preexisting unsanctioned table model_request_provenance");
          }
          const prerequisites = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!prerequisites.has("sessions") || !prerequisites.has("events")) {
            throw new Error("schema v66 prerequisite missing: sessions/events");
          }
          db.exec(`
        CREATE TABLE model_request_provenance (
          record_id                  TEXT PRIMARY KEY,
          session_id                 TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
          source_user_seq            INTEGER NOT NULL CHECK (source_user_seq > 0),
          source_event_id            TEXT NOT NULL REFERENCES events(id) ON DELETE RESTRICT,
          request_ordinal            INTEGER NOT NULL CHECK (request_ordinal > 0),
          protocol_version           INTEGER NOT NULL CHECK (protocol_version = 1),
          boundary                   TEXT NOT NULL
                                     CHECK (boundary IN ('whole_instructions','layered')),
          normalized_request_digest  TEXT NOT NULL
                                     CHECK (length(normalized_request_digest) = 64
                                       AND normalized_request_digest NOT GLOB '*[^0-9a-f]*'),
          host_projection_digest     TEXT NOT NULL
                                     CHECK (length(host_projection_digest) = 64
                                       AND host_projection_digest NOT GLOB '*[^0-9a-f]*'),
          provenance_digest          TEXT NOT NULL
                                     CHECK (length(provenance_digest) = 64
                                       AND provenance_digest NOT GLOB '*[^0-9a-f]*'),
          provenance_json            TEXT NOT NULL CHECK (length(provenance_json) BETWEEN 2 AND 1048576),
          payload_id                 TEXT NOT NULL UNIQUE,
          payload_binding_digest     TEXT NOT NULL
                                     CHECK (length(payload_binding_digest) = 64
                                       AND payload_binding_digest NOT GLOB '*[^0-9a-f]*'),
          payload_reference_json     TEXT NOT NULL
                                     CHECK (length(payload_reference_json) BETWEEN 2 AND 8192),
          created_at                 TEXT NOT NULL,
          UNIQUE (session_id, source_user_seq, request_ordinal)
        );
        CREATE INDEX idx_model_request_provenance_source
          ON model_request_provenance(session_id, source_user_seq, request_ordinal);

        CREATE TRIGGER trg_model_request_provenance_exact_source
        BEFORE INSERT ON model_request_provenance
        WHEN NOT EXISTS (
          SELECT 1 FROM events source
           WHERE source.id = NEW.source_event_id
             AND source.session_id = NEW.session_id
             AND source.seq = NEW.source_user_seq
             AND source.role = 'user'
             AND source.type = 'user_input_received'
             AND COALESCE(json_extract(source.data_json, '$.synthetic'), 0) != 1
        )
        BEGIN
          SELECT RAISE(ABORT, 'model request provenance requires its exact accepted source');
        END;

        CREATE TRIGGER trg_model_request_provenance_immutable
        BEFORE UPDATE ON model_request_provenance
        BEGIN
          SELECT RAISE(ABORT, 'model request provenance is immutable');
        END;

        CREATE TRIGGER trg_model_request_provenance_delete_immutable
        BEFORE DELETE ON model_request_provenance
        -- A parent-session retention delete remains the only deletion
        -- authority. Direct deletion while that parent exists is forbidden.
        WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
        BEGIN
          SELECT RAISE(ABORT, 'model request provenance is immutable');
        END;
      `);
          const violations = db.pragma("foreign_key_check").filter((row) => row.table === "model_request_provenance");
          if (violations.length > 0) {
            throw new Error(`schema v66 foreign-key check failed for ${violations.length} model request row(s)`);
          }
        }
      },
      {
        /**
         * Exact, non-business results committed by the foreground host.
         *
         * A locally refused/not-started/user-rejected function call has no logical
         * settlement because it crossed neither the logical-call nor provider
         * boundary. Those results are still model-visible history, so durable
         * request provenance needs an immutable receipt for the exact output field.
         * The receipt is deliberately metadata-only: no provider response and no
         * raw output are duplicated here. Its parent model-batch admission proves
         * the exact accepted source, call id, and tool name.
         */
        version: 67,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) return;
          for (const prerequisite of [
            "accepted_turn_call_authorities",
            "accepted_model_batch_admissions",
            "logical_tool_calls",
            "logical_call_settlements"
          ]) {
            if (!tables.has(prerequisite)) {
              throw new Error(`schema v67 prerequisite missing: ${prerequisite}`);
            }
          }
          if (tables.has("host_model_result_receipts")) {
            throw new Error("schema v67 refuses preexisting unsanctioned table host_model_result_receipts");
          }
          db.exec(`
        CREATE TABLE host_model_result_receipts (
          receipt_id          TEXT PRIMARY KEY CHECK (
                                length(receipt_id) = 64
                                AND receipt_id NOT GLOB '*[^0-9a-f]*'
                              ),
          session_id          TEXT NOT NULL,
          source_user_seq     INTEGER NOT NULL CHECK (source_user_seq > 0),
          source_event_id     TEXT NOT NULL REFERENCES events(id) ON DELETE RESTRICT,
          accepted_task_id    TEXT NOT NULL CHECK (length(accepted_task_id) BETWEEN 1 AND 512),
          batch_ordinal       INTEGER NOT NULL CHECK (batch_ordinal > 0),
          batch_id            TEXT NOT NULL CHECK (length(batch_id) = 64),
          call_id             TEXT NOT NULL CHECK (length(call_id) BETWEEN 1 AND 512),
          tool_name           TEXT NOT NULL CHECK (length(tool_name) BETWEEN 1 AND 512),
          disposition         TEXT NOT NULL CHECK (disposition IN (
                                'refused_pre_dispatch','not_started','user_rejected'
                              )),
          frame_digest        TEXT CHECK (
                                frame_digest IS NULL OR (
                                  length(frame_digest) = 64
                                  AND frame_digest NOT GLOB '*[^0-9a-f]*'
                                )
                              ),
          frame_index         INTEGER CHECK (frame_index IS NULL OR frame_index >= 0),
          frame_size          INTEGER CHECK (frame_size IS NULL OR frame_size > 0),
          counts_refusal      INTEGER NOT NULL DEFAULT 0 CHECK (counts_refusal IN (0, 1)),
          retry_mode          TEXT NOT NULL CHECK (retry_mode IN ('replan','do_not_retry')),
          output_bytes        INTEGER NOT NULL CHECK (output_bytes > 0),
          output_sha256       TEXT NOT NULL CHECK (
                                length(output_sha256) = 64
                                AND output_sha256 NOT GLOB '*[^0-9a-f]*'
                              ),
          recorded_at         TEXT NOT NULL,
          UNIQUE (session_id, source_user_seq, call_id),
          FOREIGN KEY (session_id, source_user_seq)
            REFERENCES accepted_turn_call_authorities(session_id, source_user_seq)
            ON DELETE CASCADE,
          FOREIGN KEY (session_id, source_user_seq, batch_ordinal, batch_id)
            REFERENCES accepted_model_batch_admissions(
              session_id, source_user_seq, batch_ordinal, batch_id
            ) ON DELETE CASCADE,
          CHECK (
            (disposition = 'user_rejected'
              AND frame_digest IS NULL AND frame_index IS NULL AND frame_size IS NULL
              AND counts_refusal = 0 AND retry_mode = 'do_not_retry')
            OR
            (disposition = 'not_started'
              AND frame_digest IS NOT NULL AND frame_index IS NOT NULL
              AND frame_size IS NOT NULL AND frame_index < frame_size
              AND counts_refusal = 0 AND retry_mode = 'replan')
            OR
            (disposition = 'refused_pre_dispatch'
              AND frame_digest IS NOT NULL AND frame_index IS NOT NULL
              AND frame_size IS NOT NULL AND frame_index < frame_size)
          )
        );

        CREATE INDEX idx_host_model_result_receipts_call
          ON host_model_result_receipts(session_id, call_id, source_user_seq);

        CREATE TRIGGER trg_host_model_result_receipt_exact_lineage
        BEFORE INSERT ON host_model_result_receipts
        WHEN NOT EXISTS (
          SELECT 1
            FROM accepted_model_batch_admissions admission
            JOIN accepted_turn_call_authorities root
              ON root.session_id = admission.session_id
             AND root.source_user_seq = admission.source_user_seq
            JOIN events source ON source.id = root.source_event_id
           WHERE admission.session_id = NEW.session_id
             AND admission.source_user_seq = NEW.source_user_seq
             AND admission.accepted_task_id = NEW.accepted_task_id
             AND admission.batch_ordinal = NEW.batch_ordinal
             AND admission.batch_id = NEW.batch_id
             AND root.accepted_task_id = NEW.accepted_task_id
             AND root.source_event_id = NEW.source_event_id
             AND root.state = 'open'
             AND source.session_id = NEW.session_id
             AND source.seq = NEW.source_user_seq
             AND source.role = 'user'
             AND source.type = 'user_input_received'
             AND COALESCE(json_extract(source.data_json, '$.synthetic'), 0) != 1
             AND (
               SELECT COUNT(*)
                 FROM json_each(admission.frame_history_json) item
                WHERE json_extract(item.value, '$.type') = 'function_call'
                  AND json_extract(item.value, '$.callId') = NEW.call_id
                  AND json_extract(item.value, '$.name') = NEW.tool_name
             ) = 1
        )
        BEGIN
          SELECT RAISE(ABORT, 'host model result receipt requires exact accepted source/call lineage');
        END;

        CREATE TRIGGER trg_host_model_result_receipt_no_business_call
        BEFORE INSERT ON host_model_result_receipts
        WHEN EXISTS (
          SELECT 1 FROM logical_tool_calls logical
           WHERE logical.session_id = NEW.session_id
             AND logical.logical_tool_call_id = NEW.call_id
        ) OR EXISTS (
          SELECT 1 FROM logical_call_settlements settlement
           WHERE settlement.session_id = NEW.session_id
             AND (settlement.logical_tool_call_id = NEW.call_id
               OR settlement.observer_call_id = NEW.call_id)
        )
        BEGIN
          SELECT RAISE(ABORT, 'host model result receipt cannot replace logical settlement evidence');
        END;

        CREATE TRIGGER trg_host_model_result_receipt_immutable
        BEFORE UPDATE ON host_model_result_receipts
        BEGIN
          SELECT RAISE(ABORT, 'host model result receipts are immutable');
        END;

        CREATE TRIGGER trg_host_model_result_receipt_delete_immutable
        BEFORE DELETE ON host_model_result_receipts
        WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
        BEGIN
          SELECT RAISE(ABORT, 'host model result receipts are immutable');
        END;
      `);
          const violations = db.pragma("foreign_key_check").filter((row) => row.table === "host_model_result_receipts");
          if (violations.length > 0) {
            throw new Error(`schema v67 foreign-key check failed for ${violations.length} host result row(s)`);
          }
        }
      },
      {
        /**
         * Durable reason for the one boot-only dispatch quarantine.
         *
         * `revoked_at` predates reasoned revocation and therefore remains valid on
         * its own. New daemon-boot quarantine writes use the one closed reason
         * below, while ordinary exact-generation release stays backward-compatible
         * with a NULL reason. The lease row is reused by scope, so a successor
         * generation clears both fields when it wins that scope.
         */
        version: 68,
        sql: "",
        backfill: (db) => {
          const table = db.prepare(
            `SELECT 1 AS ok FROM sqlite_master
          WHERE type = 'table' AND name = 'run_dispatch_leases'`
          ).get();
          if (!table) throw new Error("schema v68 prerequisite missing: run_dispatch_leases");
          const columns = new Set(
            db.prepare("PRAGMA table_info(run_dispatch_leases)").all().map((column) => column.name)
          );
          if (!columns.has("revocation_reason")) {
            db.exec(`
          ALTER TABLE run_dispatch_leases
          ADD COLUMN revocation_reason TEXT
            CHECK (
              revocation_reason IS NULL
              OR revocation_reason = 'terminal_run_attempt_at_daemon_boot'
            )
        `);
          }
        }
      },
      {
        /**
         * Immutable evidence for the exact model-visible projection of a settled
         * logical call. Logical settlement/result-handle rows prove provider-side
         * outcome bytes, but the host may preserve structured SDK content or apply
         * a deterministic presentation transform before the next model request.
         * This metadata receipt seals that whole `function_call_result` item's
         * canonical byte count and SHA-256 without copying either the projection
         * payload or a separate raw provider payload into SQLite.
         *
         * Existing ready checkpoints are safe backfill authority: only an exact
         * admission call/result pair with one unambiguous same-source/task logical
         * or observer settlement is adopted. Ambiguous and unsettled history is
         * deliberately skipped rather than inferred.
         */
        version: 69,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) return;
          for (const prerequisite of [
            "accepted_turn_call_authorities",
            "accepted_model_batch_admissions",
            "accepted_model_batch_checkpoints",
            "logical_tool_calls",
            "logical_call_settlements"
          ]) {
            if (!tables.has(prerequisite)) {
              throw new Error(`schema v69 prerequisite missing: ${prerequisite}`);
            }
          }
          if (tables.has("logical_model_result_projection_receipts")) {
            throw new Error(
              "schema v69 refuses preexisting unsanctioned table logical_model_result_projection_receipts"
            );
          }
          db.exec(`
        CREATE TABLE logical_model_result_projection_receipts (
          receipt_id                       TEXT PRIMARY KEY CHECK (
                                             length(receipt_id) = 64
                                             AND receipt_id NOT GLOB '*[^0-9a-f]*'
                                           ),
          protocol_version                 INTEGER NOT NULL CHECK (protocol_version = 1),
          session_id                       TEXT NOT NULL,
          source_user_seq                  INTEGER NOT NULL CHECK (source_user_seq > 0),
          source_event_id                  TEXT NOT NULL REFERENCES events(id) ON DELETE RESTRICT,
          accepted_task_id                 TEXT NOT NULL CHECK (
                                             length(accepted_task_id) BETWEEN 1 AND 512
                                           ),
          batch_ordinal                    INTEGER NOT NULL CHECK (batch_ordinal > 0),
          batch_id                         TEXT NOT NULL CHECK (
                                             length(batch_id) = 64
                                             AND batch_id NOT GLOB '*[^0-9a-f]*'
                                           ),
          call_id                          TEXT NOT NULL CHECK (length(call_id) BETWEEN 1 AND 512),
          tool_name                        TEXT NOT NULL CHECK (length(tool_name) BETWEEN 1 AND 512),
          call_namespace                   TEXT CHECK (
                                             call_namespace IS NULL
                                             OR length(call_namespace) <= 512
                                           ),
          settlement_identity_kind         TEXT NOT NULL CHECK (
                                             settlement_identity_kind IN ('logical','observer')
                                           ),
          settlement_logical_tool_call_id  TEXT NOT NULL CHECK (
                                             length(settlement_logical_tool_call_id) BETWEEN 1 AND 512
                                           ),
          settlement_observer_call_id      TEXT CHECK (
                                             settlement_observer_call_id IS NULL
                                             OR length(settlement_observer_call_id) BETWEEN 1 AND 256
                                           ),
          settlement_event_id              TEXT NOT NULL REFERENCES events(id) ON DELETE RESTRICT,
          settlement_semantic_digest       TEXT NOT NULL CHECK (
                                             length(settlement_semantic_digest) = 64
                                             AND settlement_semantic_digest NOT GLOB '*[^0-9a-f]*'
                                           ),
          result_class                     TEXT NOT NULL CHECK (result_class IN (
                                             'text','structured','media',
                                             'refused_pre_dispatch','not_started',
                                             'user_rejected','effect_unknown'
                                           )),
          result_item_bytes                INTEGER NOT NULL CHECK (result_item_bytes > 0),
          result_item_sha256               TEXT NOT NULL CHECK (
                                             length(result_item_sha256) = 64
                                             AND result_item_sha256 NOT GLOB '*[^0-9a-f]*'
                                           ),
          recorded_at                      TEXT NOT NULL,
          UNIQUE (session_id, source_user_seq, call_id),
          FOREIGN KEY (session_id, source_user_seq)
            REFERENCES accepted_turn_call_authorities(session_id, source_user_seq)
            ON DELETE CASCADE,
          FOREIGN KEY (session_id, source_user_seq, batch_ordinal, batch_id)
            REFERENCES accepted_model_batch_admissions(
              session_id, source_user_seq, batch_ordinal, batch_id
            ) ON DELETE CASCADE,
          FOREIGN KEY (session_id, source_user_seq, settlement_logical_tool_call_id)
            REFERENCES logical_call_settlements(
              session_id, source_user_seq, logical_tool_call_id
            ) ON DELETE CASCADE
        );

        CREATE INDEX idx_logical_model_result_projection_receipts_call
          ON logical_model_result_projection_receipts(
            session_id, call_id, source_user_seq, batch_ordinal
          );
        CREATE INDEX idx_logical_model_result_projection_receipts_settlement
          ON logical_model_result_projection_receipts(
            session_id, source_user_seq, settlement_logical_tool_call_id
          );

        CREATE TRIGGER trg_logical_model_result_projection_exact_lineage
        BEFORE INSERT ON logical_model_result_projection_receipts
        WHEN NOT EXISTS (
          SELECT 1
            FROM accepted_model_batch_admissions admission
            JOIN accepted_turn_call_authorities root
              ON root.session_id = admission.session_id
             AND root.source_user_seq = admission.source_user_seq
            JOIN events source ON source.id = root.source_event_id
           WHERE admission.session_id = NEW.session_id
             AND admission.source_user_seq = NEW.source_user_seq
             AND admission.accepted_task_id = NEW.accepted_task_id
             AND admission.batch_ordinal = NEW.batch_ordinal
             AND admission.batch_id = NEW.batch_id
             AND root.accepted_task_id = NEW.accepted_task_id
             AND root.source_event_id = NEW.source_event_id
             AND source.session_id = NEW.session_id
             AND source.seq = NEW.source_user_seq
             AND source.role = 'user'
             AND source.type = 'user_input_received'
             AND COALESCE(json_extract(source.data_json, '$.synthetic'), 0) != 1
             AND (
               SELECT COUNT(*)
                 FROM json_each(admission.frame_history_json) item
                WHERE json_extract(item.value, '$.type') = 'function_call'
                  AND json_extract(item.value, '$.callId') = NEW.call_id
                  AND json_extract(item.value, '$.name') = NEW.tool_name
                  AND json_extract(item.value, '$.namespace') IS NEW.call_namespace
             ) = 1
        )
        BEGIN
          SELECT RAISE(ABORT,
            'logical model result projection requires exact accepted source/call lineage');
        END;

        CREATE TRIGGER trg_logical_model_result_projection_exact_settlement
        BEFORE INSERT ON logical_model_result_projection_receipts
        WHEN (
          SELECT COUNT(*)
            FROM logical_call_settlements settlement
            JOIN logical_tool_calls logical
              ON logical.session_id = settlement.session_id
             AND logical.source_user_seq = settlement.source_user_seq
             AND logical.logical_tool_call_id = settlement.logical_tool_call_id
           WHERE settlement.session_id = NEW.session_id
             AND settlement.source_user_seq = NEW.source_user_seq
             AND logical.accepted_task_id = NEW.accepted_task_id
             AND logical.state = 'settled'
             AND logical.settlement_event_id = settlement.settlement_event_id
             AND (
               settlement.logical_tool_call_id = NEW.call_id
               OR settlement.observer_call_id = NEW.call_id
             )
        ) != 1
        OR NOT EXISTS (
          SELECT 1
            FROM logical_call_settlements settlement
            JOIN logical_tool_calls logical
              ON logical.session_id = settlement.session_id
             AND logical.source_user_seq = settlement.source_user_seq
             AND logical.logical_tool_call_id = settlement.logical_tool_call_id
           WHERE settlement.session_id = NEW.session_id
             AND settlement.source_user_seq = NEW.source_user_seq
             AND settlement.logical_tool_call_id = NEW.settlement_logical_tool_call_id
             AND settlement.observer_call_id IS NEW.settlement_observer_call_id
             AND settlement.settlement_event_id = NEW.settlement_event_id
             AND settlement.semantic_digest = NEW.settlement_semantic_digest
             AND logical.accepted_task_id = NEW.accepted_task_id
             AND logical.state = 'settled'
             AND logical.settlement_event_id = settlement.settlement_event_id
             AND (
               (NEW.settlement_identity_kind = 'logical'
                 AND settlement.logical_tool_call_id = NEW.call_id
                 AND logical.tool_name = NEW.tool_name)
               OR
               (NEW.settlement_identity_kind = 'observer'
                 AND settlement.observer_call_id = NEW.call_id
                 AND (
                   settlement.logical_tool_call_id != NEW.call_id
                   OR logical.tool_name != NEW.tool_name
                 ))
             )
        )
        BEGIN
          SELECT RAISE(ABORT,
            'logical model result projection requires one exact settled logical identity');
        END;

        CREATE TRIGGER trg_logical_model_result_projection_immutable
        BEFORE UPDATE ON logical_model_result_projection_receipts
        BEGIN
          SELECT RAISE(ABORT, 'logical model result projections are immutable');
        END;

        CREATE TRIGGER trg_logical_model_result_projection_delete_immutable
        BEFORE DELETE ON logical_model_result_projection_receipts
        WHEN EXISTS (SELECT 1 FROM sessions WHERE id = OLD.session_id)
        BEGIN
          SELECT RAISE(ABORT, 'logical model result projections are immutable');
        END;
      `);
          const checkpointRows = db.prepare(`
        SELECT checkpoint.session_id, checkpoint.source_user_seq,
               checkpoint.accepted_task_id, checkpoint.batch_ordinal,
               checkpoint.batch_id, checkpoint.history_json,
               checkpoint.committed_at, admission.pre_history_json,
               admission.frame_history_json,
               root.source_event_id
          FROM accepted_model_batch_checkpoints checkpoint
          JOIN accepted_model_batch_admissions admission
            ON admission.session_id = checkpoint.session_id
           AND admission.source_user_seq = checkpoint.source_user_seq
           AND admission.batch_ordinal = checkpoint.batch_ordinal
           AND admission.batch_id = checkpoint.batch_id
          JOIN accepted_turn_call_authorities root
            ON root.session_id = checkpoint.session_id
           AND root.source_user_seq = checkpoint.source_user_seq
           AND root.accepted_task_id = checkpoint.accepted_task_id
         WHERE checkpoint.disposition = 'ready'
         ORDER BY checkpoint.session_id, checkpoint.source_user_seq, checkpoint.batch_ordinal
      `).all();
          const settlementCandidates = db.prepare(`
        SELECT settlement.logical_tool_call_id,
               logical.tool_name AS logical_tool_name,
               settlement.observer_call_id,
               settlement.settlement_event_id,
               settlement.semantic_digest
          FROM logical_call_settlements settlement
          JOIN logical_tool_calls logical
            ON logical.session_id = settlement.session_id
           AND logical.source_user_seq = settlement.source_user_seq
           AND logical.logical_tool_call_id = settlement.logical_tool_call_id
         WHERE settlement.session_id = ?
           AND settlement.source_user_seq = ?
           AND logical.accepted_task_id = ?
           AND logical.state = 'settled'
           AND logical.settlement_event_id = settlement.settlement_event_id
           AND (settlement.logical_tool_call_id = ? OR settlement.observer_call_id = ?)
         ORDER BY settlement.logical_tool_call_id
      `);
          const insertReceipt = db.prepare(`
        INSERT INTO logical_model_result_projection_receipts
          (receipt_id, protocol_version, session_id, source_user_seq,
           source_event_id, accepted_task_id, batch_ordinal, batch_id,
           call_id, tool_name, call_namespace, settlement_identity_kind,
           settlement_logical_tool_call_id, settlement_observer_call_id,
           settlement_event_id, settlement_semantic_digest, result_class,
           result_item_bytes, result_item_sha256, recorded_at)
        VALUES (?, 1, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
      `);
          for (const checkpoint of checkpointRows) {
            let preHistory;
            let frameHistory;
            let history;
            try {
              preHistory = JSON.parse(checkpoint.pre_history_json);
              frameHistory = JSON.parse(checkpoint.frame_history_json);
              history = JSON.parse(checkpoint.history_json);
            } catch {
              continue;
            }
            if (!Array.isArray(preHistory) || !Array.isArray(frameHistory) || !Array.isArray(history) || !exactOpenAdmissionFrameForMigration(preHistory, frameHistory) || !inspectProtocolForMigration(history, true).valid) continue;
            for (const candidateCall of frameHistory) {
              if (!candidateCall || typeof candidateCall !== "object" || Array.isArray(candidateCall)) continue;
              const call = candidateCall;
              const callId = typeof call.callId === "string" ? call.callId : "";
              const toolName = typeof call.name === "string" ? call.name : "";
              if (call.type !== "function_call" || !callId || !toolName || call.namespace !== void 0 && typeof call.namespace !== "string") continue;
              const callNamespace = typeof call.namespace === "string" ? call.namespace : null;
              const resultCandidates = history.filter((candidate) => {
                if (!candidate || typeof candidate !== "object" || Array.isArray(candidate)) return false;
                const result = candidate;
                return result.type === "function_call_result" && result.callId === callId && result.name === toolName;
              });
              if (resultCandidates.length !== 1) continue;
              let material = null;
              try {
                material = projectionMaterialForMigration(resultCandidates[0]);
              } catch {
                material = null;
              }
              if (!material || material.callId !== callId || material.toolName !== toolName || material.callNamespace !== callNamespace) continue;
              const settlements = settlementCandidates.all(
                checkpoint.session_id,
                checkpoint.source_user_seq,
                checkpoint.accepted_task_id,
                callId,
                callId
              );
              if (settlements.length !== 1) continue;
              const settlement = settlements[0];
              const logicalMatch = settlement.logical_tool_call_id === callId;
              const observerMatch = settlement.observer_call_id === callId;
              if (!logicalMatch && !observerMatch) continue;
              if (settlement.logical_tool_name !== toolName && !observerMatch) continue;
              const identityKind = logicalMatch && settlement.logical_tool_name === toolName ? "logical" : "observer";
              const receiptIdentity = {
                protocol: "clementine.logical_model_result_projection_receipt.v1",
                sessionId: checkpoint.session_id,
                sourceUserSeq: checkpoint.source_user_seq,
                sourceEventId: checkpoint.source_event_id,
                acceptedTaskId: checkpoint.accepted_task_id,
                batchOrdinal: checkpoint.batch_ordinal,
                batchId: checkpoint.batch_id,
                callId,
                toolName,
                callNamespace,
                settlementIdentityKind: identityKind,
                settlementLogicalToolCallId: settlement.logical_tool_call_id,
                settlementObserverCallId: settlement.observer_call_id,
                settlementEventId: settlement.settlement_event_id,
                settlementSemanticDigest: settlement.semantic_digest,
                resultClass: material.resultClass,
                resultItemBytes: material.resultItemBytes,
                resultItemSha256: material.resultItemSha256
              };
              const receiptId = (0, import_node_crypto6.createHash)("sha256").update(canonicalProjectionJsonForMigration(receiptIdentity), "utf8").digest("hex");
              insertReceipt.run(
                receiptId,
                checkpoint.session_id,
                checkpoint.source_user_seq,
                checkpoint.source_event_id,
                checkpoint.accepted_task_id,
                checkpoint.batch_ordinal,
                checkpoint.batch_id,
                callId,
                toolName,
                callNamespace,
                identityKind,
                settlement.logical_tool_call_id,
                settlement.observer_call_id,
                settlement.settlement_event_id,
                settlement.semantic_digest,
                material.resultClass,
                material.resultItemBytes,
                material.resultItemSha256,
                checkpoint.committed_at
              );
            }
          }
          const relevantTables = foreignKeyClosure(db, [
            "logical_model_result_projection_receipts"
          ]);
          const violations = db.pragma("foreign_key_check").filter((violation) => relevantTables.has(violation.table));
          if (violations.length > 0) {
            throw new Error(
              `schema v69 foreign-key check failed for ${violations.length} logical result projection row(s)`
            );
          }
        }
      },
      {
        // Session retention is the sole sanctioned owner of durable task cleanup.
        // V37 amendments accidentally used RESTRICT parents and had no session FK,
        // so one valid amendment stranded the entire old terminal session. Rebuild
        // only that child with exact session-bound contract/event CASCADE parents;
        // its retention-aware immutability trigger continues to reject standalone
        // deletion while the session exists.
        version: 70,
        sql: "",
        foreignKeysOff: true,
        backfill: rebuildExpectedWorkUniverseAmendmentCascade
      },
      {
        /** Close both plan_task post-persist crash windows. The immutable
         * preparation checkpoint names the exact plan call, graph, contract,
         * preamble content address, and presentation owner before delivery. */
        version: 71,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) return;
          for (const prerequisite of [
            "accepted_turn_call_authorities",
            "accepted_task_authority",
            "accepted_task_work_contracts",
            "accepted_task_resolutions",
            "accepted_model_batch_admissions",
            "accepted_model_batch_checkpoints",
            "logical_tool_calls",
            "logical_call_settlements",
            "logical_call_settlement_crossings",
            "physical_dispatches",
            "durable_result_handles",
            PLAN_TASK_ACTIVATION_RECEIPTS_TABLE
          ]) {
            if (!tables.has(prerequisite)) throw new Error(`schema v71 prerequisite missing: ${prerequisite}`);
          }
          createPlanTaskPreparationCheckpointSchema(db);
          createAsyncReadRefinementSchema(db);
          backfillPlanTaskPreparationCheckpointsV71(db);
          refreshCheckpointBackedPlanContinuationTriggersV71(db);
          const relevantTables = foreignKeyClosure(db, [
            PLAN_TASK_BINDING_SEAL_RECOVERY_CURSOR_TABLE,
            PLAN_TASK_BINDING_SEAL_INTENTS_TABLE,
            PLAN_TASK_PREPARATION_CHECKPOINTS_TABLE,
            PLAN_TASK_ACTIVATION_RECEIPTS_TABLE,
            ASYNC_READ_REFINEMENT_INTENTS_TABLE,
            ASYNC_READ_REFINEMENT_START_RECEIPTS_TABLE,
            ASYNC_READ_REFINEMENT_COMPLETION_RECEIPTS_TABLE
          ]);
          const violations = db.pragma("foreign_key_check").filter((violation) => relevantTables.has(violation.table));
          if (violations.length > 0) {
            throw new Error(
              `schema v71 foreign-key check failed for ${violations.length} plan preparation row(s)`
            );
          }
        }
      },
      {
        /** Add the mutually-exclusive terminal outcome and the durable paged crash
         * claimant after the v71 async owner shipped. This must remain its own
         * migration: developer homes may already have recorded v71. */
        version: 72,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) return;
          for (const prerequisite of [
            ASYNC_READ_REFINEMENT_INTENTS_TABLE,
            ASYNC_READ_REFINEMENT_START_RECEIPTS_TABLE,
            ASYNC_READ_REFINEMENT_COMPLETION_RECEIPTS_TABLE
          ]) {
            if (!tables.has(prerequisite)) throw new Error(`schema v72 prerequisite missing: ${prerequisite}`);
          }
          createAsyncReadRefinementTerminalRecoverySchemaV72(db);
          db.exec(`
        CREATE INDEX IF NOT EXISTS idx_sessions_chat_run_in_flight_updated
          ON sessions(updated_at, id)
          WHERE kind = 'chat'
            AND json_type(metadata_json, '$.__run_in_flight') IS NOT NULL;
      `);
          const relevantTables = foreignKeyClosure(db, [
            ASYNC_READ_REFINEMENT_RECOVERY_CURSOR_TABLE,
            ASYNC_READ_REFINEMENT_TERMINAL_RECEIPTS_TABLE
          ]);
          const violations = db.pragma("foreign_key_check").filter((violation) => relevantTables.has(violation.table));
          if (violations.length > 0) {
            throw new Error(
              `schema v72 foreign-key check failed for ${violations.length} async refinement terminal row(s)`
            );
          }
        }
      },
      {
        /** Normalize stamped v71/v72 development candidates. v73 is intentionally
         * additive even though the release is not final: a running daemon may have
         * already recorded either earlier version and numbered migrations never
         * rewrite history. */
        version: 73,
        sql: "",
        backfill: (db) => {
          const tables = new Set(
            db.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all().map((row) => row.name)
          );
          if (!tables.has("sessions") || !tables.has("events")) return;
          for (const prerequisite of [
            ASYNC_READ_REFINEMENT_INTENTS_TABLE,
            ASYNC_READ_REFINEMENT_START_RECEIPTS_TABLE,
            ASYNC_READ_REFINEMENT_COMPLETION_RECEIPTS_TABLE
          ]) {
            if (!tables.has(prerequisite)) throw new Error(`schema v73 prerequisite missing: ${prerequisite}`);
          }
          createPlanTaskPreparationCheckpointSchema(db);
          createAsyncReadRefinementSchema(db);
          createAsyncReadRefinementTerminalRecoverySchema(db);
          db.exec(`
        CREATE INDEX IF NOT EXISTS idx_sessions_chat_run_in_flight_updated
          ON sessions(updated_at, id)
          WHERE kind = 'chat'
            AND json_type(metadata_json, '$.__run_in_flight') IS NOT NULL;
      `);
          const relevantTables = foreignKeyClosure(db, [
            PLAN_TASK_BINDING_SEAL_RECOVERY_CURSOR_TABLE,
            PLAN_TASK_BINDING_SEAL_INTENTS_TABLE,
            PLAN_TASK_PREPARATION_CHECKPOINTS_TABLE,
            ASYNC_READ_REFINEMENT_RECOVERY_CURSOR_TABLE,
            ASYNC_READ_REFINEMENT_TERMINAL_RECEIPTS_TABLE
          ]);
          const violations = db.pragma("foreign_key_check").filter((violation) => relevantTables.has(violation.table));
          if (violations.length > 0) {
            throw new Error(
              `schema v73 foreign-key check failed for ${violations.length} recovery authority row(s)`
            );
          }
        }
      }
    ];
    newestMigrationVersion = MIGRATIONS2[MIGRATIONS2.length - 1]?.version ?? 0;
    if (newestMigrationVersion !== HARNESS_SCHEMA_VERSION) {
      throw new Error(
        `HARNESS_SCHEMA_VERSION=${HARNESS_SCHEMA_VERSION} does not match newest migration ${newestMigrationVersion}`
      );
    }
    STRICT_TAIL_TABLES = Object.freeze([
      // v65
      "tool_output_chunks",
      "tool_output_invocation_chunks",
      "tool_search_continuations",
      // v66
      "model_request_provenance",
      // v67
      "host_model_result_receipts",
      // v69
      "logical_model_result_projection_receipts"
    ]);
  }
});

// src/runtime/harness/host-call-capability-binding.ts
var init_host_call_capability_binding = __esm({
  "src/runtime/harness/host-call-capability-binding.ts"() {
    "use strict";
    init_closed_canonical_json();
    init_eventlog_schema();
  }
});

// src/runtime/harness/atomic-content-commit-proof.ts
var init_atomic_content_commit_proof = __esm({
  "src/runtime/harness/atomic-content-commit-proof.ts"() {
    "use strict";
    init_documented_create_result_evidence();
    init_atomic_input_content_contract();
    init_host_call_capability_binding();
  }
});

// src/runtime/harness/authority-argument-seal.ts
var import_node_path9, VAULT_FILE;
var init_authority_argument_seal = __esm({
  "src/runtime/harness/authority-argument-seal.ts"() {
    "use strict";
    import_node_path9 = __toESM(require("node:path"), 1);
    init_config();
    VAULT_FILE = import_node_path9.default.join(BASE_DIR, "state", "secrets-vault.json");
  }
});

// src/runtime/semantic-boundary/host-authority.ts
var init_host_authority = __esm({
  "src/runtime/semantic-boundary/host-authority.ts"() {
    "use strict";
  }
});

// src/runtime/harness/shipped-implementation-identity.ts
var import_node_module, import_node_path10, import_node_url2, MODULE_DIR, SRC_ARTIFACT_ROOT, requireFromHere;
var init_shipped_implementation_identity = __esm({
  "src/runtime/harness/shipped-implementation-identity.ts"() {
    "use strict";
    import_node_module = require("node:module");
    import_node_path10 = __toESM(require("node:path"), 1);
    import_node_url2 = require("node:url");
    init_isolated_test_contract();
    MODULE_DIR = import_node_path10.default.dirname((0, import_node_url2.fileURLToPath)(import_meta_url));
    SRC_ARTIFACT_ROOT = import_node_path10.default.join(MODULE_DIR, "implementation-artifacts", "emitted");
    requireFromHere = (0, import_node_module.createRequire)(import_meta_url);
  }
});

// src/runtime/harness/production-capability-ports.ts
var init_production_capability_ports = __esm({
  "src/runtime/harness/production-capability-ports.ts"() {
    "use strict";
    init_capability_manifest();
    init_isolated_test_contract();
    init_shipped_implementation_identity();
  }
});

// src/runtime/harness/independent-capability-observation.ts
var init_independent_capability_observation = __esm({
  "src/runtime/harness/independent-capability-observation.ts"() {
    "use strict";
    init_shipped_implementation_identity();
  }
});

// src/runtime/harness/production-capability-catalog.ts
var init_production_capability_catalog = __esm({
  "src/runtime/harness/production-capability-catalog.ts"() {
    "use strict";
    init_capability_manifest();
    init_capability_manifest_store();
    init_production_capability_ports();
    init_independent_capability_observation();
    init_production_capability_adapters();
    init_shipped_implementation_identity();
    init_production_capability_adapters();
  }
});

// src/runtime/harness/physical-crossing-identity.ts
var init_physical_crossing_identity = __esm({
  "src/runtime/harness/physical-crossing-identity.ts"() {
    "use strict";
  }
});

// src/runtime/harness/resolved-call-authority.ts
var init_resolved_call_authority = __esm({
  "src/runtime/harness/resolved-call-authority.ts"() {
    "use strict";
    init_host_authority();
    init_capability_manifest();
    init_host_capability_catalog_factory();
    init_production_capability_catalog();
    init_logical_call_contract();
    init_independent_capability_observation();
    init_shipped_implementation_identity();
    init_physical_crossing_identity();
  }
});

// src/runtime/harness/typed-physical-authority-proof.ts
var init_typed_physical_authority_proof = __esm({
  "src/runtime/harness/typed-physical-authority-proof.ts"() {
    "use strict";
    init_authority_argument_seal();
    init_resolved_call_authority();
    init_host_capability_catalog_factory();
  }
});

// src/runtime/harness/host-local-write-commit.ts
function canonicalIdentity(input) {
  return JSON.stringify({
    version: 1,
    createdId: input.createdId,
    handle: input.handle,
    contentDigest: input.contentDigest
  });
}
function validCreatedId(createdId) {
  return createdId.length > 0 && createdId.length <= 512 && createdId === createdId.trim() && !/[\u0000-\u001f\u007f]/.test(createdId);
}
function validRelativeHandle(handle) {
  return handle.length > 0 && handle.length <= 1024 && handle === handle.trim() && !import_node_path11.default.posix.isAbsolute(handle) && !handle.includes("\\") && handle.split("/").every((part) => part.length > 0 && part !== "." && part !== "..");
}
function parseHostLocalWriteCommitFacts(result) {
  if (typeof result !== "string") return null;
  const newline = result.indexOf("\n");
  if (newline < 0) return null;
  const receipt = result.slice(0, newline);
  if (!receipt.startsWith(`${HOST_LOCAL_WRITE_COMMIT_PREFIX} `) || receipt.length > 2500) return null;
  const encoded = receipt.slice(HOST_LOCAL_WRITE_COMMIT_PREFIX.length + 1);
  try {
    const parsed = JSON.parse(encoded);
    const createdId = typeof parsed.createdId === "string" ? parsed.createdId : "";
    const handle = typeof parsed.handle === "string" ? parsed.handle : "";
    const contentDigest = typeof parsed.contentDigest === "string" ? parsed.contentDigest : "";
    const identity = { createdId, handle, contentDigest };
    if (parsed.version !== 1 || encoded !== canonicalIdentity(identity) || !validCreatedId(createdId) || !validRelativeHandle(handle) || !/^[a-f0-9]{64}$/.test(contentDigest)) return null;
    return { ...identity, receipt };
  } catch {
    return null;
  }
}
function hostLocalWriteCommitResultIsProven(result) {
  const facts = parseHostLocalWriteCommitFacts(result);
  if (!facts) return false;
  if (!facts.handle.endsWith(`/${HOST_LOCAL_WORKSPACE_COMMIT_BASENAME}`)) return true;
  return workspaceCompoundReadbackIsProven(facts);
}
function safeCommittedFile(root, filePath) {
  const nominated = import_node_path11.default.resolve(filePath);
  const before = (0, import_node_fs4.lstatSync)(nominated);
  if (!before.isFile() || before.isSymbolicLink()) {
    throw new Error("Local authoring commit target is not a direct regular file.");
  }
  const fd = (0, import_node_fs4.openSync)(nominated, import_node_fs4.constants.O_RDONLY | import_node_fs4.constants.O_NOFOLLOW);
  try {
    const opened = (0, import_node_fs4.fstatSync)(fd);
    if (!opened.isFile() || opened.dev !== before.dev || opened.ino !== before.ino) {
      throw new Error("Local authoring commit target changed before open.");
    }
    const committed = (0, import_node_fs4.realpathSync)(nominated);
    const rel = import_node_path11.default.relative(root, committed);
    if (!rel || rel.startsWith("..") || import_node_path11.default.isAbsolute(rel)) {
      throw new Error("Local authoring commit path is outside the Clementine root.");
    }
    const handle = rel.split(import_node_path11.default.sep).join("/");
    if (!validRelativeHandle(handle)) {
      throw new Error("Local authoring commit produced an invalid relative handle.");
    }
    const bytes = (0, import_node_fs4.readFileSync)(fd);
    const afterFd = (0, import_node_fs4.fstatSync)(fd);
    const afterPath = (0, import_node_fs4.lstatSync)(nominated);
    if (!afterPath.isFile() || afterPath.isSymbolicLink() || afterFd.dev !== opened.dev || afterFd.ino !== opened.ino || afterFd.size !== opened.size || afterPath.dev !== opened.dev || afterPath.ino !== opened.ino || bytes.byteLength !== afterFd.size) throw new Error("Local authoring commit target changed during read.");
    return { path: committed, handle, bytes };
  } finally {
    (0, import_node_fs4.closeSync)(fd);
  }
}
function safeTargetHandle(root, filePath) {
  const resolved = import_node_path11.default.resolve(filePath);
  let target;
  try {
    target = (0, import_node_fs4.realpathSync)(resolved);
  } catch {
    target = import_node_path11.default.join((0, import_node_fs4.realpathSync)(import_node_path11.default.dirname(resolved)), import_node_path11.default.basename(resolved));
  }
  const rel = import_node_path11.default.relative(root, target);
  if (!rel || rel.startsWith("..") || import_node_path11.default.isAbsolute(rel)) {
    throw new Error("Local authoring commit path is outside the Clementine root.");
  }
  const handle = rel.split(import_node_path11.default.sep).join("/");
  if (!validRelativeHandle(handle)) {
    throw new Error("Local authoring commit produced an invalid relative handle.");
  }
  return handle;
}
function reopenWorkspaceCompoundCommit(facts) {
  try {
    const root = (0, import_node_fs4.realpathSync)(import_node_path11.default.resolve(BASE_DIR));
    const receipt = safeCommittedFile(root, import_node_path11.default.resolve(root, facts.handle));
    if ((0, import_node_crypto7.createHash)("sha256").update(receipt.bytes).digest("hex") !== facts.contentDigest) return null;
    if (receipt.bytes.byteLength > 16384) return null;
    const parsed = JSON.parse(receipt.bytes.toString("utf8"));
    if (parsed.version !== 1 || parsed.kind !== "workspace_bundle_v1" || parsed.createdId !== facts.createdId || !Array.isArray(parsed.components) || parsed.components.length !== 3) return null;
    const workspacePrefix = `spaces/${facts.createdId}/`;
    const expectedRoles = ["manifest", "view", "data"];
    for (let index = 0; index < expectedRoles.length; index += 1) {
      const component = parsed.components[index];
      if (component?.role !== expectedRoles[index] || !validRelativeHandle(component.handle) || !component.handle.startsWith(workspacePrefix) || !/^[a-f0-9]{64}$/.test(component.contentDigest) || !Number.isSafeInteger(component.bytes) || component.bytes < 0) return null;
      if (component.role === "manifest" && component.handle !== `${workspacePrefix}space.json`) return null;
      if (component.role === "view" && !component.handle.startsWith(`${workspacePrefix}view/`)) return null;
      if (component.role === "data" && component.handle !== `${workspacePrefix}data.json`) return null;
      const reopened = safeCommittedFile(root, import_node_path11.default.resolve(root, component.handle));
      if (reopened.handle !== component.handle || reopened.path !== import_node_path11.default.resolve(root, component.handle) || reopened.bytes.byteLength !== component.bytes || (0, import_node_crypto7.createHash)("sha256").update(reopened.bytes).digest("hex") !== component.contentDigest) return null;
    }
    if (JSON.stringify(parsed) !== receipt.bytes.toString("utf8")) return null;
    return { ...facts, components: parsed.components };
  } catch {
    return null;
  }
}
function workspaceCompoundReadbackIsProven(facts) {
  return reopenWorkspaceCompoundCommit(facts) !== null;
}
function withHostLocalWriteCommitFromFile(input) {
  if (!validCreatedId(input.createdId)) {
    throw new Error("Local authoring commit supplied an invalid created id.");
  }
  const root = (0, import_node_fs4.realpathSync)(import_node_path11.default.resolve(input.rootDir ?? BASE_DIR));
  const committed = safeCommittedFile(root, input.committedPath);
  const identity = {
    createdId: input.createdId,
    handle: committed.handle,
    contentDigest: (0, import_node_crypto7.createHash)("sha256").update(committed.bytes).digest("hex")
  };
  return `${HOST_LOCAL_WRITE_COMMIT_PREFIX} ${canonicalIdentity(identity)}
${input.result}`;
}
function writeHostLocalWorkspaceCommitDocument(input) {
  if (!validCreatedId(input.createdId)) {
    throw new Error("Local Workspace commit supplied an invalid created id.");
  }
  const root = (0, import_node_fs4.realpathSync)(import_node_path11.default.resolve(input.rootDir ?? BASE_DIR));
  const component = (role, source) => {
    const bytes = Buffer.isBuffer(source.bytes) ? source.bytes : Buffer.from(source.bytes, "utf8");
    return {
      role,
      handle: safeTargetHandle(root, source.path),
      contentDigest: (0, import_node_crypto7.createHash)("sha256").update(bytes).digest("hex"),
      bytes: bytes.byteLength
    };
  };
  const document = {
    version: 1,
    kind: "workspace_bundle_v1",
    createdId: input.createdId,
    components: [
      component("manifest", input.manifest),
      component("view", input.view),
      component("data", input.data)
    ]
  };
  const receiptHandle = safeTargetHandle(root, input.receiptPath);
  if (receiptHandle !== `spaces/${input.createdId}/${HOST_LOCAL_WORKSPACE_COMMIT_BASENAME}`) {
    throw new Error("Local Workspace compound receipt path is not canonical.");
  }
  const tempPath = `${input.receiptPath}.${process.pid}.tmp`;
  (0, import_node_fs4.writeFileSync)(tempPath, JSON.stringify(document), "utf8");
  (0, import_node_fs4.renameSync)(tempPath, input.receiptPath);
  return input.receiptPath;
}
var import_node_crypto7, import_node_fs4, import_node_path11, HOST_LOCAL_WRITE_COMMIT_PREFIX, HOST_LOCAL_WORKSPACE_COMMIT_BASENAME;
var init_host_local_write_commit = __esm({
  "src/runtime/harness/host-local-write-commit.ts"() {
    "use strict";
    import_node_crypto7 = require("node:crypto");
    import_node_fs4 = require("node:fs");
    import_node_path11 = __toESM(require("node:path"), 1);
    init_config();
    HOST_LOCAL_WRITE_COMMIT_PREFIX = "[clementine:host-local-write-commit:v1]";
    HOST_LOCAL_WORKSPACE_COMMIT_BASENAME = ".clementine-workspace-commit.json";
  }
});

// src/runtime/harness/expected-work-matcher.ts
var init_expected_work_matcher = __esm({
  "src/runtime/harness/expected-work-matcher.ts"() {
    "use strict";
    init_provider_read_evidence();
  }
});

// src/runtime/harness/host-local-workspace-derivation.ts
var init_host_local_workspace_derivation = __esm({
  "src/runtime/harness/host-local-workspace-derivation.ts"() {
    "use strict";
    init_host_local_write_commit();
    init_logical_call_contract();
    init_provider_read_evidence();
    init_expected_work_matcher();
  }
});

// src/runtime/harness/terminal-publication-proof.ts
var init_terminal_publication_proof = __esm({
  "src/runtime/harness/terminal-publication-proof.ts"() {
    "use strict";
    init_provider_read_evidence();
    init_result_facts();
    init_result_payload_storage();
    init_settlement_crossing_authority();
    init_atomic_content_commit_proof();
    init_typed_physical_authority_proof();
    init_authority_argument_seal();
    init_logical_call_contract();
    init_host_call_capability_binding();
    init_mutation_verification_contract();
    init_host_local_write_commit();
    init_host_local_workspace_derivation();
    init_tool_registry();
    init_capability_manifest();
    init_sealed_node_binding_digest();
  }
});

// src/runtime/exact-origin-delivery.ts
var init_exact_origin_delivery = __esm({
  "src/runtime/exact-origin-delivery.ts"() {
    "use strict";
  }
});

// src/runtime/harness/eventlog.ts
var import_better_sqlite33, import_node_path12, HARNESS_STATE_DIR, HARNESS_DB_PATH, EVENT_TYPES, EVENT_TYPE_SET, PRIMARY_MODEL_PLANNING_CARD_SNAPSHOT_MAX_BYTES, TOOL_SEARCH_CONTINUATION_MAX_SESSION_BYTES;
var init_eventlog = __esm({
  "src/runtime/harness/eventlog.ts"() {
    "use strict";
    import_better_sqlite33 = __toESM(require("better-sqlite3"), 1);
    import_node_path12 = __toESM(require("node:path"), 1);
    init_config();
    init_sqlite_statement_cache();
    init_action_bus();
    init_eventlog_operational_mirror();
    init_audit_ledger();
    init_public_presentation();
    init_tool_evidence();
    init_provider_read_evidence();
    init_runtime_tool_identity();
    init_settled_read_replay_semantics();
    init_turn_outcome();
    init_turn_graph_compiler();
    init_terminal_publication_proof();
    init_exact_origin_delivery();
    init_eventlog_schema();
    init_host_planned_resolution_coexistence();
    init_eventlog_schema();
    HARNESS_STATE_DIR = import_node_path12.default.join(BASE_DIR, "state");
    HARNESS_DB_PATH = import_node_path12.default.join(HARNESS_STATE_DIR, "harness.db");
    EVENT_TYPES = [
      "session_started",
      "turn_started",
      "turn_ended",
      "condenser_applied",
      "plan_drafted",
      "plan_approved",
      "plan_revised",
      "plan_rejected",
      "step_started",
      // Private Claude local-MCP handoff: canUseTool has the provider's outer
      // toolUseID before the in-process handler sees its transport request. This
      // is permission/correlation state, not a physical tool attempt, so it must
      // never share tool_called accounting.
      "claude_local_permission_admitted",
      // Private handler-consumption proof used only when the SDK stream authored
      // the outer canonical call before the local MCP handler entered. Keeping the
      // claim separate preserves append-only event history and prevents a second
      // identical handler from consuming the same admission.
      "claude_local_permission_claimed",
      "tool_called",
      "tool_returned",
      "step_verified",
      "step_failed",
      "handoff",
      "awaiting_user_input",
      "user_input_received",
      // One model-authored conversational acknowledgement shown before execution.
      // This is presentation only: no terminal status, outcome, need, approval, or
      // effect authority is carried by this event.
      "conversation_preamble",
      // Exact awaited transport acknowledgement for a model-authored plan
      // preamble. This is presentation evidence only. Plan activation additionally
      // requires the bound first-class plan_task logical call to settle
      // successfully; the receipt alone grants no work or effect authority.
      "conversation_preamble_delivered",
      // Mid-run steering (2026-08-07): a user message that arrived while the
      // session had an active attempt — delivered to the model at the next
      // tool-result boundary instead of superseding the running work.
      "user_steer_note",
      "user_steer_note_delivered",
      // One-release read compatibility for align rows persisted by Clementine 3.5.
      // New turns do not append this event; remove after the upgrade window.
      "turn_preflight_decision",
      "approval_requested",
      "approval_resolved",
      // Token-level streaming: emitted for each output_text_delta from the model.
      // Not persisted to SQLite — only broadcast via actionBus for real-time UI.
      "stream_token",
      // Loop intent proposal: surfaced before tools fire on multi-step requests.
      // Contains the planned objective, steps, and risks.
      "loop_intent_proposed",
      // Goal-contract validation (goal-contract Phase 3): emitted when a session's
      // parked goal is validated on self-declared completion — pass/fail, attempt.
      "goal_validation",
      "guardrail_tripped",
      "stuck_detected",
      // Emitted from the conversation loop when stuck_detected fires AND
      // the harness has retry budget remaining. The retry sends a synthetic
      // "act now" message to the same sub-agent before giving up. If the
      // retry also stalls, the original sub_agent_stalled outcome surfaces
      // as today.
      "stall_retry_attempted",
      "heartbeat",
      "kill_requested",
      "run_paused",
      "run_resumed",
      "run_completed",
      "run_failed",
      // Multi-turn auto-continuation: emitted at the boundary between
      // two runTurn() calls inside the same runConversation(). The
      // OrchestratorDecision drives whether the loop recurses.
      "conversation_step",
      // Private half of the source-group workflow handoff. One row proves a
      // trusted workflow_run producer prepared a held queue member for the exact
      // accepted human source. It is never projected to chat; the loop seals and
      // activates the complete source group before publishing one dispatch ACK.
      "async_work_dispatch_prepared",
      // Durable foreground-release boundary for an exact workflow source group.
      // This freezes the complete ordered set of prepared members before any run
      // can become executable. Prepared-only groups remain held across restarts.
      "async_work_dispatch_batch_closed",
      // Typed nonterminal boundary: an exact workflow_run tool receipt handed
      // continuing ownership to the background workflow daemon. The foreground
      // model/transport may release, but this accepted logical intent remains
      // pending until the workflow's separately sourced outcome reports back.
      "async_work_dispatched",
      // A brain stopped without a safe structured outcome. This is execution-graph
      // state only: the bridge may recover on another brain, then commits exactly
      // one public terminal for the accepted user turn.
      "conversation_recovery_candidate",
      "conversation_completed",
      "conversation_limit_exceeded",
      // Auto-capture writeback: emitted from the harness loop whenever a
      // user message produced durable facts or a profile patch via
      // captureInteractionSignals. Lets the trace show "Clementine learned
      // X from this turn" so memory growth is observable.
      "memory_signals_captured",
      // Content-addressed host authority for an acknowledgement-only durable
      // memory action. Unlike memory_signals_captured telemetry, this event is
      // issued only after the host independently redeems the exact accepted source
      // against its episode and auto-capture candidate rows.
      "durable_memory_intake_receipt",
      // Evidence-first procedural learning boundary. Every automatic candidate
      // records whether it was eligible, which runtime authority verified it, and
      // why a paused/degraded/ambiguous run was refused. The event never drives the
      // run; it makes "why did Clem learn (or not learn) this?" auditable.
      "learning_candidate_evaluated",
      // Cross-session prefix: when a new Discord (or other channel) session
      // opens within the continuity window of a prior same-channel session,
      // the harness prepends ONE event of this type carrying the prior
      // session's last user message + agent reply. session_history then
      // returns this context so back-references like "first 10 please"
      // can be interpreted. Added 2026-05-24.
      "cross_session_prefix",
      // v0.5.19 F2 — auto-elevate emits this when the preflight gate sees
      // a 'warn' or 'block' verdict early in a `standard`-preset
      // conversation. Carries the from/to caps so the dashboard can show
      // why the budget changed mid-run.
      "budget_elevated",
      // SDK brain auto-continued past a per-query max-turns budget instead of parking
      // on "say continue" (F1). Carries the attempt # and whether it's still limited.
      "sdk_auto_continue",
      // Stage 4 fan-out slice (2026-07-20): the active run token window {baseline,
      // ceiling}, recorded at window open by BOTH lanes so run_worker — which runs
      // in the SDK-brain MCP child, a separate process — can refuse to spawn past
      // an exhausted ceiling. Latest event wins.
      "run_token_window",
      // Fail-closed approval park (2026-07-20): the chat/worker WAIT gate hit its
      // hold ceiling with the card still pending — the turn ended honestly and the
      // durable card stayed resumable. {approvalId, tool, subject}. Read by
      // chat-approval-resume to auto-resume the session on a later approval.
      "approval_parked",
      // SDK local-MCP startup guard retried because the required local tool surface
      // was empty or no init message arrived before the startup budget.
      "sdk_tool_surface_retry",
      // The modern Claude Agent SDK reported an internal provider retry. This is
      // observational only, but it prevents the legacy outer retry layer from
      // replaying an already-exhausted physical query.
      "sdk_api_retry",
      // Spawn→first SDK frame latency for one query. In current Claude SDKs this is
      // normally the child-process init frame, not model TTFT; retained for
      // compatibility with existing telemetry consumers.
      "sdk_first_byte",
      // Spawn→first genuine model/provider activity after SDK initialization
      // (assistant/stream/thinking/tool-progress/result). This is the truthful
      // user-wait metric; init and api_retry control frames do not satisfy it.
      "sdk_first_model_activity",
      // Per-turn prompt-prefix cache-hit ratio on the default brain lane — makes the
      // freeze-stable-prefix cache lever scoreable from the eventlog (2026-07-09).
      "sdk_cache",
      // WHICH model/lane served a chat turn (respond bridge, once per turn):
      // {model, routeKind, surface}. The durable answer to "who actually served
      // this?" — brain-matrix assertions, fallover forensics, route-policy audit.
      "turn_model_routed",
      // Exact model/tool-loop owner selected at the RunRunnerFn boundary. This is
      // distinct from turn_model_routed: a provider can be routed through the
      // shared harness while the legacy SDK or Clem's host engine owns stepping.
      "turn_engine_selected",
      // The Claude Agent SDK's child process compacted its own context mid-run
      // (subtype 'compact_boundary' relay; pre/post tokens + trigger). Mirror of the
      // Codex lane's condenser_applied — proves long runs manage context instead of
      // dying at the window cliff.
      "sdk_compact_boundary",
      // The SDK reported a FAILED compaction (status message compact_result:'failed').
      "sdk_compact_failed",
      // Tool-injection scoping: emitted at agent construction so traces can
      // explain why a run saw a small external MCP surface instead of every
      // configured server tool.
      "mcp_tool_scope",
      // Per-turn memory primer: emitted when the harness runs the local
      // FTS memory lookup for the latest user message before the model call.
      // The actual hits are injected transiently through callModelInputFilter
      // so they do not bloat persisted conversation history.
      "turn_memory_primer",
      // Post-turn memory credit: recall runs whose candidates demonstrably shaped
      // the turn's output (reply / tool args / drafted plan). Replaces the
      // never-called memory_mark_used tool with code-level attribution.
      "recall_auto_credit",
      // The negative half of the credit loop: a user correction of the prior turn
      // recorded a bounded `not_useful` signal against the facts that fed it.
      "memory_correction",
      // Per-turn deterministic context packet: summarizes the memory
      // primer, likely skills/workflows, MCP health, local health, and
      // complexity classification that were injected transiently before
      // the model call.
      "agent_context_packet",
      // Provider-neutral, shadow-only turn graph compiled from one exact accepted
      // chat source. Observational only: it cannot grant authority or alter the
      // active v3.6 execution path.
      "turn_graph_compiled",
      // One tool-less semantic interpretation per accepted source. Telemetry and
      // exact-replay of the admitted proposal; not a competing task store.
      "turn_semantics_interpreted",
      // Fail-closed cutover marker for the Clem 4 evidence protocol. The graph
      // remains observational by itself; this row records that the host accepted
      // its exact identity as the authority contract before provider work began.
      "accepted_task_authority_armed",
      // Planner-first gate: fresh complex requests get a read-only plan
      // proposal before the full external MCP surface is opened.
      "plan_first_started",
      "plan_first_failed",
      // Flag-only native Codex compaction proof: emitted when the harness
      // persists a Codex `compaction` item from raw model responses and
      // prunes replay history for the next continuation turn.
      "native_compaction_applied",
      // v0.5.19 F3 — preflight gate fires this for workflow/execution/
      // agent kinds when a turn projects over the context block
      // threshold. Workflows have no user to consult mid-step so they
      // proceed — but the dashboard now sees the risk and a future
      // workflow-runner extension can react (split / abort / retry).
      "workflow_step_overbudget",
      // The DELIVERABLE of a workflow-step conversation: the full unclipped
      // workflow_step_result payload, written by the harness loop at capture
      // completion so the step output rides a durable, restart-surviving carrier
      // next to its conversation_completed terminal instead of living only in a
      // process-local map. Live 2026-08-23 (runs 1787745601613-p49r2/-trnr2): a
      // demoted blocked terminal threw before process-local adoption, stranding
      // the payload and reporting the capture tool's own ack echo. Private to the
      // workflow runner's adoption seam; never projected to chat.
      "workflow_step_result_captured",
      // Move 2 (confirm-first gate): emitted by the tool-boundary gate each
      // time a mutating external write is ALLOWED through. The gate counts
      // these per session+shape to detect a batch (≥ threshold same-shape
      // writes) and require an instruction-reviewed plan scope before the
      // batch proceeds. Emitted from the gate (not hooks) so worker/sub-agent
      // writes — which share the parent session via AsyncLocalStorage but may
      // not log tool_called under it — are counted reliably.
      "external_write",
      // Compensation record: the dispatch behind an external_write demonstrably
      // FAILED (e.g. composio schema rejection) — the duplicate-target gate nets
      // one matching prior per failure so corrected retries aren't "duplicates".
      "external_write_failed",
      // Exact terminal receipt for a pre-dispatch external_write reservation. A
      // reservation alone is never completion evidence: only this same-call
      // settlement proves the provider returned cleanly.
      "external_write_succeeded",
      // S3 orphan ledger: a MUTATING external write TIMED OUT. The harness stops
      // waiting but the request MAY have landed server-side (it is aborted at the
      // network layer — recorded in `aborted`). Durable audit of maybe-landed
      // writes, and the signal the orphaned-write retry corrective consults before
      // a blind same-shape retry.
      "external_write_orphaned",
      // Explicit, execution-owned retry lineage. Authorization names one proven
      // failed call; consumption is a durable one-shot CAS recorded before a
      // corrected provider attempt is reserved.
      "external_write_retry_authorized",
      "external_write_retry_consumed",
      // Always-on telemetry: a run_worker sub-agent hit its turn ceiling
      // (MaxTurnsExceeded). Worker nested runs carry no harness hooks, so this is
      // the only signal of worker turn-cap hits — used to recalibrate
      // CLEMMY_WORKER_MAX_TURNS from real data.
      "worker_capped",
      // A fan-out worker STARTING — lets the chat/board render the specialist as
      // running the moment it spawns (not only when worker_result lands).
      "worker_started",
      // Deterministic batch runner (run_batch): plan execution started / a single
      // item failed (with consecutive-failure count) / the whole batch finished
      // with honest counts. The loop makes NO model calls, so these events are the
      // primary visibility into what it did.
      "batch_started",
      "batch_progress",
      "batch_item_failed",
      "batch_completed",
      // Historical program-executor visibility. These event names stay readable so
      // old sessions render and migrate; no active control flow writes or consumes
      // them as an execution surface.
      "codemode_progress",
      "codemode_program_summary",
      // Historical provenance for a parked oversized program return: the child call ids that
      // produced it. The handle itself is recall-only and never evidence
      // authority; gates that refuse it read this record to name the real
      // evidence ids instead of looping the model (live 2026-08-12).
      "codemode_result_parked",
      // NON-halting record that, in YOLO, an approval-shaped ask_user_question was
      // auto-resolved (standing approval) and the run proceeded instead of pausing.
      // Distinct from awaiting_user_input precisely so it does NOT halt the loop.
      "autonomy_note",
      // Per-turn dynamic reasoning effort: which effort tier (low/medium/high) was
      // selected for this turn's model call and why. gpt-5.x reasons before emitting
      // tokens, so this is the main per-turn latency lever — recorded for observability.
      "reasoning_effort",
      // A fan-out item was routed to a per-task model by an intent rule (model
      // role registry) — records the attempted intent, whether it matched, and the
      // resolved model/provider, so a trace can show "ran on Opus because 'design'".
      "worker_model_routed",
      // A fan-out worker COMPLETED — durable record of {item, ok, model, toolUses,
      // tokens} (Move 5). The honest N-of-M coverage map was in-memory only, so a
      // mid-run daemon restart lost it; this makes the swarm's coverage + per-worker
      // spend restart-surviving and queryable for a 30-60min 100-subagent run.
      "worker_result",
      // Durable long-horizon work graph. A manifest declares the canonical item
      // universe and ordered phases once; checkpoints update one canonical
      // item/phase with evidence; contract revisions decide whether completed
      // evidence remains valid. These are control-plane facts, not model verdicts.
      "work_manifest_declared",
      "work_item_checkpoint",
      "expected_work_universe_amended",
      "work_contract_revised",
      // A user changed the objective/constraints of a durable background task while
      // it was queued or running. The task record owns the revision history; this
      // event makes the course correction visible in the same live trace.
      "background_contract_revised",
      // Wave 4 Stage 2: a run/continue boundary for a background task's stable
      // runSessionId, so fan-out coverage (summarizeFanoutCoverage) counts only THIS
      // run's worker_results and a prior run's failures don't leak into a later
      // continue's completion check (would permanently block a re-completed task).
      "fanout_run_boundary",
      // Move 2: deterministic pre-execution coherence critique of a surfaced plan
      // (uncovered success criteria, unverifiable steps) — surfaced before approval so
      // a walk-away user never green-lights a structurally weak 100-subagent plan.
      "plan_critiqued",
      // Turn-start swarm governance decision: the context packet detected a
      // multi-item request and either offered fanout, constrained it, or kept the
      // work centralized under the current coordination policy. Pure telemetry.
      "fanout_policy_decision",
      // Engine-over-prompt A/B substrate: emitted at agent construction with the
      // rubric variant in force (CLEMMY_RUBRIC_VARIANT) so a live session is
      // attributable to an arm (legacy vs a future lean prune). Sibling of
      // mcp_tool_scope — pure telemetry, never alters behavior.
      "rubric_variant",
      // Phase 1 Tool-RAG: emitted at agent construction when JIT tool loading
      // (CLEMMY_TOOL_JIT) actually reduced the built-in surface — records how many
      // tools were dropped + the selection reason, so a trace explains a smaller
      // surface and an A/B can attribute token/accuracy deltas. Sibling of
      // mcp_tool_scope; only emitted when a reduction occurred.
      "tool_jit_scope",
      // Schema-on-demand surface (SCHEMA-ON-DEMAND-PLAN-2026-07-07): when the Codex lane
      // moves discovery tools off the first-class schema surface into the catalog block
      // (reachable via call_tool), this records the arm + first-class/catalog counts and
      // estimated tokens so the A/B can attribute the token delta. Sibling of
      // tool_jit_scope; pure telemetry, never alters dispatch.
      "tool_search_scope",
      // Central tool-policy resolver: emitted at model-boundary construction after
      // allow/deny resolution so every brain can show which local tool surface it
      // actually received. Telemetry only; never alters dispatch.
      "tool_policy_resolved",
      // The goal-alignment judge ran on an irreversible write and PASSED
      // (fulfills=true) — the aligned-proceed case is otherwise silent, so this
      // proves the judge fired BEFORE a YOLO silent-proceed (the 2026-06-22
      // CLEMMY_GOAL_ALIGNMENT_GATE fix). Pure telemetry; never alters behavior.
      "goal_alignment_judged",
      // The numeric/output-grounding gate ran on a deliverable (a chat-delivered
      // report or an irreversible-write payload) and reached a verdict — pass,
      // advisory (a load-bearing figure could not be traced to a tool result), or
      // the gate confirmed every figure traces to captured data. The bounce case
      // emits guardrail_tripped(kind:output_grounding_blocked) instead (mirrors
      // grounding_blocked). Pure telemetry on the non-block paths. (2026-06-23
      // trust-layer P1.)
      "output_grounding_judged",
      // OODA re-Orient feedback edge: emitted when a self-driving goal resume folds
      // fresh monitor observations (inbox/calendar needs-you items that landed since
      // the last cycle and overlap the goal) into the resume directive — so the turn
      // re-reads the world before continuing instead of re-pursuing blind. Carries
      // {phase:'reorient', scope:'goal', observationsInjected}. Sibling of
      // tool_jit_scope / rubric_variant — pure telemetry, never alters behavior; the
      // measurement spine for the goal re-Orient feature (default since 2026-06-27). (2026-06-24.)
      "ooda_cycle",
      // W1a chat step-boundary brain fallover: a transient model/codex error on one
      // brain was re-dispatched to the next brain mid-conversation (carries
      // {reason, kind, toModel, attempt}). Telemetry + the visible parity twin of the
      // workflow runner's step_advisory{reason:'brain_fallover'}.
      "brain_fallover",
      // Bounded infra self-heal: an attended run quietly retries one transient
      // blip, while a workflow/background run retries twice because no human is
      // present to answer the "retry/switch/stop" ask. Carries
      // {kind, attempt, max, sourceUserSeq, logicalErrorEpisodeId}. The accepted
      // source + stable episode id is the durable retry-budget key across daemon
      // restarts; a genuinely new source retains its full allowance. Bounded —
      // after the budget the run fails honestly.
      "infra_auto_recover",
      // Stranded-tool reunification: a turn DIED on an infra error while a tool
      // (e.g. a run_batch) was still IN FLIGHT. `orphaned_tool_inflight` {callId,
      // toolName} registers it at death; once the tool completes, drainOrphanedTool-
      // Completions emits `orphaned_tool_reported` {callId} (dedup) and fires a
      // follow-up report turn so the session self-reports the result to the user.
      "orphaned_tool_inflight",
      "orphaned_tool_reported",
      // Legacy parse-exhaustion marker retained for replay compatibility. New
      // graph turns use conversation_recovery_candidate and never publish the
      // exhausted brain's proposal as a terminal in the first place.
      "conversation_superseded",
      // Restart recovery decision: emitted once per interrupted chat session found
      // on boot, before the visible recovery notice/resume dispatch. Carries the
      // safety evidence behind auto-resume vs manual-continue so restart recovery is
      // auditable from the event log, not only from daemon boot logs.
      "restart_recovery_decision",
      // Judge verdict audit row (T3-B4 verdict door): ONE canonical event for every
      // completion/goal/target/delivery judge verdict, emitted by the call sites
      // that own the session context. Carries {door, pass, reason, failedOpen,
      // selfJudge, durationMs?, detail?} so run views and forensics read verdicts
      // from the event log instead of scraping heartbeats and prose.
      "verdict_recorded",
      // E4 read lane: the durable receipt row a verified read dispatch appends —
      // the promotion/verification authority the resolver reads back by id.
      // Internal evidence: the public-presentation allowlist does not carry it.
      "read_receipt",
      // A governed warm candidate needed one physical exact-slug provider metadata
      // refresh to restore schema authority. This is not a business tool call and
      // not cold-lane discovery. Payload is deliberately identifier/account-free:
      // {sourceUserSeq, attemptId, artifactId, durationMs, outcome}.
      "warm_schema_metadata_refresh",
      // Durable discovery-budget admission and settlement telemetry. These rows
      // make the one-broad/one-exact-refresh task budget auditable across process
      // restarts without exposing provider identifiers or request payloads.
      "discovery_governor_decision",
      "discovery_governor_outcome",
      // Why a spent discovery budget reopened. Carries {kind, outcome,
      // previousEpoch, epoch, detail} — the observed fact that made the previous
      // search stale, so a task that keeps searching can be told apart from one
      // that keeps LEARNING.
      "discovery_governor_evidence",
      // One typed settlement per tool attempt, from the shared seam every lane
      // ends in. Carries {lane, tool, kind, evidence, action, businessCall,
      // mutating, openedDiscoveryEpoch, creditedProgress} — the audit trail for
      // "which recovery ran, and why".
      "tool_attempt_settled",
      // Durable requirement satisfaction for an accepted task, so a restart does
      // not lose what the task already proved.
      "requirement_state",
      "evidence_receipt",
      "write_evidence_proved",
      "obligation_manifest",
      "obligation_satisfied",
      // One bounded, host-owned chance to gather missing terminal proof. These are
      // control-plane facts, never model-facing scripts or new user turns.
      "terminal_authority_repair_granted",
      "terminal_authority_repair_consumed",
      "terminal_authority_backstop_fired",
      "resolution_operation",
      "resolution_finalized",
      "logical_call_contract_refined",
      "provider_dispatch_started",
      "provider_dispatch_settled",
      // An authorized tool the turn did not advertise was named and fetched.
      // Carries {tool, serverSlug, reason, scopeReason, advertised,
      // widenedBeyondTurnBase} — the audit trail for "the cap was wrong, and the
      // run recovered anyway".
      "mcp_tool_acquired",
      // Typed capability resolution: what this turn's ask can already rely on
      // (proven procedures), what has failed before (invalidated memos), and the
      // connection state of each — resolved by the runtime at preflight. The UI's
      // live "what Clem knows going in" frame and the graph's future admission
      // input. Carries {entries, registryAvailable, sourceUserSeq?}.
      "capability_resolution",
      // Host work-plan card: one line per frozen expected-work operation, projected
      // from the same oracle as admission refusals. Closed payload only
      // {version, sourceUserSeq, lines[{id, effect, state, settled, observed,
      // required, dependsOn}]} — no model prose. The strip humanizes ids.
      "expected_work_progress",
      // Exact schema-backed capabilities surfaced by one governed discovery call.
      // This is restart-safe continuation evidence only: no query, arguments,
      // provider payload, or dispatch authority is stored. Carries
      // {sourceUserSeq, capabilities[{kind,identifier,effectClass,
      // schemaFingerprint?}]}.
      "capability_discovered",
      // Immutable, source-bound copy of the exact bounded planning card installed
      // before the first foreground model request. Recovery reopens these exact
      // bytes and revalidates every named live definition; it never rebuilds the
      // displayed card from capabilities discovered later in the same turn.
      "primary_model_planning_card_snapshot",
      // Exact source-bound authority installed by the workflow runner after it
      // reopens the immutable run definition and revalidates every authored
      // external catalog binding. This grants only ordinary, non-destructive
      // writes for the named step; direct-call consent reopens and revalidates the
      // complete receipt before use.
      "authored_workflow_write_authority",
      // The capability catalog the planner was ACTUALLY shown for one turn — the
      // post-truncation union of all three contributors, each descriptor tagged
      // with which leg supplied it, plus whether resolution completed or hit its
      // deadline. Record only; replay never reads it. Carries {sourceUserSeq,
      // resolution, count, ceiling, withheld, frozenCatalogAdvisories,
      // capabilities[{id,effect,source}]}.
      "planning_catalog_disclosed",
      // What occupied this turn's prompt, split by whether it can be cached.
      // Per-step prompt cost is paid once per step and therefore ~100x per task,
      // so the lever on latency is keeping the LARGE part invariant and the
      // VARIABLE part small — which is only auditable if the split is recorded.
      // Observation only; it never changes a byte of any prompt. Carries
      // {lane, totalTokens, stableTokens, variableTokens, stableShare, toolCount,
      // buckets[], sourceUserSeq?}.
      "prompt_composition",
      // A file the runtime ACTUALLY wrote (local deliverable). Emitted by the
      // write path after the filesystem write succeeds — runtime truth, not model
      // prose — so drafted emails / reports appear in the live feed as they land
      // instead of vanishing into a folder. Carries {name, dir, bytes}.
      "deliverable_saved",
      // Durable record of the Claude lane's inner SDK business-tool uses. That
      // lane's tools do not cross the dispatch ledger, so without this marker a
      // worked action turn looks evidence-free to store-driven terminal
      // adjudication and fails closed. Carries {sourceUserSeq, tools[]}.
      "sdk_tool_use_recorded",
      // HOST TURN LOOP (2026-08-19): the host claimed the NEXT STEP of the SAME
      // turn after a pipe/step ceiling. A step ceiling is a STEP ending, never a
      // TURN ending; model-visible means logged, so the claim is a durable session
      // fact — a banner or a fire-and-forget scheduler without this event is a
      // stall, not a continue. Carries {sourceUserSeq, reason, attempt, cap, …}.
      "next_step_claimed",
      "turn_phase_timings",
      "workflow_candidate_recorded",
      // Immutable activation receipt for one provider-neutral workflow node
      // invocation. This is a system-owned source event, never a user-input alias
      // and never a fabricated TurnGraph. v51 binds it to the shared logical /
      // physical / settlement kernel through workflow_v1_read_only authority.
      //
      // A mutating v3 activation may additionally have one exact canonical-Auto
      // decision. That decision is a separate, private receipt so ordinary work
      // never has to masquerade as a hidden human approval. The v3 authority
      // transaction appends this receipt and the activation atomically.
      "workflow_v3_auto_consent_decided",
      "workflow_node_invocation_activated",
      // Immutable parent for one provider-neutral paginated workflow read. Every
      // page is a child call of this one activation/node attempt; no page is
      // represented as another workflow attempt or fabricated user turn.
      "workflow_paginated_read_activated",
      "fanout_window_settled",
      "dependency_request",
      "connection_request",
      "connection_request_satisfied"
    ];
    EVENT_TYPE_SET = new Set(EVENT_TYPES);
    PRIMARY_MODEL_PLANNING_CARD_SNAPSHOT_MAX_BYTES = 64 * 1024;
    TOOL_SEARCH_CONTINUATION_MAX_SESSION_BYTES = 32 * 1048576;
  }
});

// src/memory/temporal-memory.ts
function normalize(text) {
  return text.replace(/\s+/g, " ").trim();
}
function contentHash(text) {
  return (0, import_node_crypto8.createHash)("sha256").update(text).digest("hex");
}
function episodeIdFor(input, normalized) {
  if (input.sessionId && input.callId) {
    return `call:${contentHash(`${input.sessionId}:${input.callId}`).slice(0, 24)}`;
  }
  return `episode:${contentHash([
    input.kind,
    input.sourceApp ?? "",
    input.sessionId ?? "",
    input.sourceUri ?? "",
    input.occurredAt ?? "",
    normalized
  ].join(":")).slice(0, 24)}`;
}
function recordMemoryEpisode(input) {
  const db = openMemoryDb();
  const now = (/* @__PURE__ */ new Date()).toISOString();
  const excerpt = input.content ? normalize(input.content).slice(0, MAX_EVIDENCE_CHARS) : "";
  const id = episodeIdFor(input, excerpt);
  const status = input.status ?? (excerpt ? "available" : "missing");
  const subtype = input.subtype?.trim().slice(0, 80) || null;
  const title = input.title?.replace(/\s+/g, " ").trim().slice(0, 240) || null;
  const metadataJson = JSON.stringify(input.metadata ?? {});
  db.prepare(`
    INSERT INTO memory_episodes
      (id, kind, source_app, session_id, call_id, source_uri, occurred_at,
       ingested_at, content_hash, evidence_excerpt, raw_retained_until, status,
       subtype, title, metadata_json)
    VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
    ON CONFLICT(id) DO UPDATE SET
      source_app = COALESCE(excluded.source_app, memory_episodes.source_app),
      source_uri = COALESCE(excluded.source_uri, memory_episodes.source_uri),
      occurred_at = excluded.occurred_at,
      subtype = COALESCE(excluded.subtype, memory_episodes.subtype),
      title = COALESCE(excluded.title, memory_episodes.title),
      metadata_json = CASE
        WHEN excluded.metadata_json != '{}' THEN excluded.metadata_json
        ELSE memory_episodes.metadata_json
      END,
      evidence_excerpt = CASE
        WHEN excluded.evidence_excerpt IS NOT NULL AND length(excluded.evidence_excerpt) > 0
          THEN excluded.evidence_excerpt
        ELSE memory_episodes.evidence_excerpt
      END,
      content_hash = CASE
        WHEN excluded.evidence_excerpt IS NOT NULL AND length(excluded.evidence_excerpt) > 0
          THEN excluded.content_hash
        ELSE memory_episodes.content_hash
      END,
      raw_retained_until = COALESCE(excluded.raw_retained_until, memory_episodes.raw_retained_until),
      status = CASE
        WHEN excluded.status = 'available' THEN 'available'
        WHEN memory_episodes.status = 'available' THEN 'available'
        ELSE excluded.status
      END
  `).run(
    id,
    input.kind,
    input.sourceApp ?? null,
    input.sessionId ?? null,
    input.callId ?? null,
    input.sourceUri ?? null,
    input.occurredAt ?? now,
    now,
    contentHash(excerpt),
    excerpt || null,
    input.rawRetainedUntil ?? null,
    status,
    subtype,
    title,
    metadataJson
  );
  return db.prepare("SELECT * FROM memory_episodes WHERE id = ?").get(id);
}
var import_node_crypto8, MAX_EVIDENCE_CHARS;
var init_temporal_memory = __esm({
  "src/memory/temporal-memory.ts"() {
    "use strict";
    import_node_crypto8 = require("node:crypto");
    init_db();
    init_eventlog();
    init_standing_policy_compiler();
    init_policy_enforcement();
    MAX_EVIDENCE_CHARS = 2e3;
  }
});

// src/memory/workspace-observation-bridge.ts
function digest(value) {
  return (0, import_node_crypto9.createHash)("sha256").update(value).digest("hex");
}
function byteLength(value) {
  return Buffer.byteLength(value, "utf8");
}
function clipUtf8(value, maxBytes) {
  const normalized = value.trim();
  if (byteLength(normalized) <= maxBytes) return normalized;
  let end = Math.min(normalized.length, maxBytes);
  while (end > 0 && byteLength(normalized.slice(0, end)) > maxBytes) end -= 1;
  return normalized.slice(0, end).trimEnd();
}
function sanitizedText(value, maxBytes) {
  return clipUtf8(
    redactSensitiveText(value).replace(/\s+/g, " ").trim(),
    maxBytes
  );
}
function safeReference(value, label) {
  const raw = typeof value === "string" ? value.trim() : "";
  const structurallySafe = /^[A-Za-z0-9][A-Za-z0-9._:-]*$/.test(raw);
  if (raw && structurallySafe && !SECRETISH_REFERENCE_RE.test(raw) && byteLength(raw) <= MAX_REFERENCE_BYTES) {
    return raw;
  }
  return `${label}-${digest(raw).slice(0, 20)}`;
}
function validSignalReferences(signal) {
  return [
    signal.workspaceId,
    signal.sourceId,
    signal.observationId,
    signal.contentHash
  ].every((value) => typeof value === "string" && value.trim().length > 0);
}
function normalizedOccurredAt(value) {
  const timestamp = Date.parse(value);
  return Number.isFinite(timestamp) ? new Date(timestamp).toISOString() : (/* @__PURE__ */ new Date(0)).toISOString();
}
function utcDayBucket(value) {
  return normalizedOccurredAt(value).slice(0, 10);
}
function boundedChangeCount(value) {
  if (typeof value !== "number" || !Number.isFinite(value) || value < 0) return null;
  return Math.min(1e6, Math.trunc(value));
}
function observationSummary(signal) {
  const add = boundedChangeCount(signal.changeCounts?.add);
  const remove = boundedChangeCount(signal.changeCounts?.remove);
  const replace = boundedChangeCount(signal.changeCounts?.replace);
  if (add === null || remove === null || replace === null) return null;
  if (add + remove + replace === 0 && signal.truncated !== true) return null;
  const label = (count, singular) => `${count} ${singular}${count === 1 ? "" : "s"}`;
  return [
    `Workspace data changed: ${label(add, "addition")}, ${label(remove, "removal")}, and ${label(replace, "replacement")}.`,
    signal.truncated === true ? "Additional structural changes were omitted from the bounded comparison." : ""
  ].filter(Boolean).join(" ");
}
function sourceUriFor(refs) {
  return [
    `workspace://${encodeURIComponent(refs.workspaceId)}`,
    `sources/${encodeURIComponent(refs.sourceId)}`,
    `observations/${encodeURIComponent(refs.observationId)}`
  ].join("/");
}
function episodeIdentity(input) {
  const sessionId = `workspace:${input.workspaceId}`;
  const fingerprint = input.eventKind === "observation" ? [
    BRIDGE_VERSION,
    input.workspaceId,
    input.sourceId,
    input.eventKind,
    `utc-day:${utcDayBucket(input.occurredAt)}`
  ].join("") : [
    BRIDGE_VERSION,
    input.workspaceId,
    input.sourceId,
    input.observationId,
    input.contentHash,
    input.eventKind,
    input.discriminator
  ].join("");
  return {
    sessionId,
    callId: `workspace-memory:${digest(fingerprint).slice(0, 32)}`
  };
}
function payloadBytes(input) {
  return byteLength(JSON.stringify(input));
}
function fitPayload(input) {
  let content = input.content ?? "";
  const metadata = { ...input.metadata ?? {} };
  let provenance = typeof metadata.provenanceSummary === "string" ? metadata.provenanceSummary : "";
  const fitted = { ...input, metadata, content };
  while (payloadBytes(fitted) > MAX_WORKSPACE_MEMORY_PAYLOAD_BYTES) {
    if (byteLength(content) > MIN_CONTENT_BYTES) {
      content = clipUtf8(content, Math.max(
        MIN_CONTENT_BYTES,
        Math.floor(byteLength(content) * 0.8)
      ));
      fitted.content = content;
      continue;
    }
    if (byteLength(provenance) > MIN_PROVENANCE_BYTES) {
      provenance = clipUtf8(provenance, Math.max(
        MIN_PROVENANCE_BYTES,
        Math.floor(byteLength(provenance) * 0.8)
      ));
      metadata.provenanceSummary = provenance;
      continue;
    }
    break;
  }
  return fitted;
}
function prepareEpisode(signal) {
  const refs = {
    workspaceId: safeReference(signal.workspaceId, "workspace"),
    sourceId: safeReference(signal.sourceId, "source"),
    observationId: safeReference(signal.observationId, "observation"),
    contentHash: safeReference(signal.contentHash, "content")
  };
  const provenanceSummary2 = sanitizedText(signal.provenanceSummary, MAX_PROVENANCE_BYTES) || "Workspace event.";
  const sourceUri = sourceUriFor(refs);
  let kind;
  let subtype;
  let title;
  let content;
  let wake;
  let discriminator;
  let metadata;
  if (signal.kind === "observation") {
    const summary = observationSummary(signal);
    if (!summary) return null;
    content = summary;
    kind = "tool_result";
    subtype = signal.previousContentHash === null ? "workspace_observation_first" : "workspace_observation_changed";
    title = signal.previousContentHash === null ? "Workspace baseline observation" : "Workspace observation changed";
    wake = false;
    discriminator = signal.previousContentHash ?? "first";
    metadata = {
      bridgeVersion: BRIDGE_VERSION,
      workspaceId: refs.workspaceId,
      sourceId: refs.sourceId,
      observationId: refs.observationId,
      contentHash: refs.contentHash,
      previousContentHash: signal.previousContentHash === null ? null : safeReference(signal.previousContentHash, "content"),
      eventKind: signal.kind,
      bucketUtcDay: utcDayBucket(signal.occurredAt),
      provenanceSummary: provenanceSummary2
    };
  } else if (signal.kind === "user_correction") {
    const correction = sanitizedText(signal.correction, MAX_CONTENT_BYTES);
    if (!correction) return null;
    content = `User correction: ${correction}`;
    kind = "user_turn";
    subtype = "workspace_user_correction";
    title = "Workspace user correction";
    wake = true;
    discriminator = digest(correction);
    metadata = {
      bridgeVersion: BRIDGE_VERSION,
      workspaceId: refs.workspaceId,
      sourceId: refs.sourceId,
      observationId: refs.observationId,
      contentHash: refs.contentHash,
      eventKind: signal.kind,
      provenanceSummary: provenanceSummary2
    };
  } else {
    const summary = sanitizedText(signal.summary, MAX_CONTENT_BYTES);
    content = summary ? `Workspace effect ${signal.decision}: ${summary}` : `Workspace effect ${signal.decision}.`;
    kind = "tool_result";
    subtype = `workspace_effect_${signal.decision}`;
    title = `Workspace effect ${signal.decision}`;
    wake = true;
    discriminator = signal.decision;
    metadata = {
      bridgeVersion: BRIDGE_VERSION,
      workspaceId: refs.workspaceId,
      sourceId: refs.sourceId,
      observationId: refs.observationId,
      contentHash: refs.contentHash,
      eventKind: signal.kind,
      decision: signal.decision,
      provenanceSummary: provenanceSummary2
    };
  }
  const identity = episodeIdentity({
    ...refs,
    eventKind: signal.kind,
    discriminator,
    occurredAt: signal.occurredAt
  });
  return {
    wake,
    input: fitPayload({
      kind,
      subtype,
      title,
      metadata,
      sourceApp: "workspace",
      sessionId: identity.sessionId,
      callId: identity.callId,
      sourceUri,
      occurredAt: normalizedOccurredAt(signal.occurredAt),
      content,
      status: "available"
    })
  };
}
function ensureWorkspaceProjectionReceiptSchema(db) {
  if (projectionSchemaReady.has(db)) return;
  db.exec(`
    CREATE TABLE IF NOT EXISTS workspace_memory_projection_receipts (
      workspace_id   TEXT NOT NULL,
      observation_id TEXT NOT NULL,
      disposition    TEXT NOT NULL CHECK (disposition IN ('captured','suppressed')),
      reason         TEXT NOT NULL,
      projected_at   TEXT NOT NULL,
      PRIMARY KEY (workspace_id, observation_id)
    );
    CREATE INDEX IF NOT EXISTS idx_workspace_memory_projection_receipts_workspace
      ON workspace_memory_projection_receipts(workspace_id, projected_at);
  `);
  projectionSchemaReady.add(db);
}
function validatedWorkspaceSlug(workspaceId) {
  if (typeof workspaceId !== "string" || workspaceId.trim() !== workspaceId || !WORKSPACE_SLUG_RE2.test(workspaceId)) {
    throw new Error(`invalid workspace slug: ${String(workspaceId)}`);
  }
  return workspaceId;
}
function validatedObservationId(observationId) {
  if (typeof observationId !== "string" || observationId.trim() !== observationId || observationId.length < 1 || observationId.length > 128 || /[\u0000-\u001f\u007f]/.test(observationId)) {
    throw new Error("invalid Workspace observation id");
  }
  return observationId;
}
function hasWorkspaceObservationMemoryProjection(workspaceId, observationId, db = openMemoryDb()) {
  const slug = validatedWorkspaceSlug(workspaceId);
  const id = validatedObservationId(observationId);
  ensureWorkspaceProjectionReceiptSchema(db);
  return Boolean(db.prepare(`
    SELECT 1
    FROM workspace_memory_projection_receipts
    WHERE workspace_id = ? AND observation_id = ?
    LIMIT 1
  `).get(slug, id));
}
function recordWorkspaceObservationMemoryProjection(input, db = openMemoryDb()) {
  const workspaceId = validatedWorkspaceSlug(input.workspaceId);
  const observationId = validatedObservationId(input.observationId);
  const reason = sanitizedText(input.reason, 80) || "unspecified";
  const projectedAt = normalizedOccurredAt(input.projectedAt ?? (/* @__PURE__ */ new Date()).toISOString());
  ensureWorkspaceProjectionReceiptSchema(db);
  db.prepare(`
    INSERT INTO workspace_memory_projection_receipts
      (workspace_id, observation_id, disposition, reason, projected_at)
    VALUES (?, ?, ?, ?, ?)
    ON CONFLICT(workspace_id, observation_id) DO NOTHING
  `).run(
    workspaceId,
    observationId,
    input.disposition,
    reason,
    projectedAt
  );
  const row = db.prepare(`
    SELECT workspace_id, observation_id, disposition, reason, projected_at
    FROM workspace_memory_projection_receipts
    WHERE workspace_id = ? AND observation_id = ?
  `).get(workspaceId, observationId);
  return {
    workspaceId: row.workspace_id,
    observationId: row.observation_id,
    disposition: row.disposition,
    reason: row.reason,
    projectedAt: row.projected_at
  };
}
function pruneWorkspaceObservationMemoryProjections(workspaceId, retainedObservationIds, db = openMemoryDb()) {
  const slug = validatedWorkspaceSlug(workspaceId);
  const retained = [...new Set(
    [...retainedObservationIds].map((observationId) => validatedObservationId(observationId))
  )];
  ensureWorkspaceProjectionReceiptSchema(db);
  if (retained.length === 0) {
    return db.prepare(`
      DELETE FROM workspace_memory_projection_receipts
      WHERE workspace_id = ?
    `).run(slug).changes;
  }
  return db.prepare(`
    DELETE FROM workspace_memory_projection_receipts
    WHERE workspace_id = ?
      AND observation_id NOT IN (
        SELECT CAST(value AS TEXT)
        FROM json_each(?)
      )
  `).run(slug, JSON.stringify(retained)).changes;
}
function purgeWorkspaceObservationMemory(workspaceId, db = openMemoryDb()) {
  const slug = validatedWorkspaceSlug(workspaceId);
  const sessionId = `workspace:${slug}`;
  ensureWorkspaceProjectionReceiptSchema(db);
  ensureMemoryForeignKeysEnabled(db);
  const episodeSelector = `
    SELECT id
    FROM memory_episodes
    WHERE source_app = 'workspace' AND session_id = ?
  `;
  return db.transaction(() => {
    const episodesBefore = Number(db.prepare(`
      SELECT COUNT(*) AS count FROM (${episodeSelector})
    `).get(sessionId).count);
    let pendingRowsDeleted = 0;
    for (const table of [
      "reflection_pending_extractions",
      "memory_reflection_receipts",
      "memory_reflection_candidates",
      "episodic_pointers"
    ]) {
      const exists = db.prepare(`
        SELECT 1 FROM sqlite_master WHERE type = 'table' AND name = ? LIMIT 1
      `).get(table);
      if (!exists) continue;
      pendingRowsDeleted += db.prepare(
        `DELETE FROM "${table}" WHERE session_id = ?`
      ).run(sessionId).changes;
    }
    const derivedFactRows = db.prepare(`
      SELECT id FROM consolidated_facts
      WHERE source_session_id = ? OR derived_from_session_id = ?
    `).all(sessionId, sessionId);
    const derivedFactsDeleted = hardDeleteConsolidatedFacts(
      db,
      derivedFactRows.map((row) => row.id)
    );
    let evidenceRowsDeleted = 0;
    let evidenceReferencesCleared = 0;
    for (const [table, column] of [
      ["fact_evidence", "episode_id"],
      ["entity_edge_evidence", "episode_id"],
      ["entity_observations", "episode_id"]
    ]) {
      evidenceRowsDeleted += db.prepare(`
        DELETE FROM "${table}"
        WHERE "${column}" IN (${episodeSelector})
      `).run(sessionId).changes;
    }
    for (const [table, column] of [
      ["entity_edges", "evidence_episode_id"],
      ["entity_aliases", "evidence_episode_id"],
      ["entity_identifiers", "evidence_episode_id"],
      ["entity_redirects", "evidence_episode_id"],
      ["entity_edge_validity_intervals", "evidence_episode_id"],
      ["fact_entities", "evidence_episode_id"],
      ["fact_resources", "evidence_episode_id"],
      ["memory_reflection_candidates", "episode_id"]
    ]) {
      evidenceReferencesCleared += db.prepare(`
        UPDATE "${table}"
        SET "${column}" = NULL
        WHERE "${column}" IN (${episodeSelector})
      `).run(sessionId).changes;
    }
    const episodesDeleted = db.prepare(`
      DELETE FROM memory_episodes
      WHERE source_app = 'workspace' AND session_id = ?
    `).run(sessionId).changes;
    if (episodesDeleted !== episodesBefore) {
      throw new Error(
        `Workspace memory purge verification failed for "${slug}"`
      );
    }
    const projectionReceiptsDeleted = db.prepare(`
      DELETE FROM workspace_memory_projection_receipts
      WHERE workspace_id = ?
    `).run(slug).changes;
    const violations = db.prepare("PRAGMA foreign_key_check").all();
    if (violations.length > 0) {
      throw new Error(
        `Workspace memory purge foreign-key check failed for "${slug}"`
      );
    }
    return {
      workspaceId: slug,
      episodesDeleted,
      derivedFactsDeleted,
      pendingRowsDeleted,
      evidenceRowsDeleted,
      evidenceReferencesCleared,
      projectionReceiptsDeleted
    };
  }).immediate();
}
function createWorkspaceObservationMemoryBridge(dependencies = defaultDependencies, options = {}) {
  return {
    async capture(signal) {
      if (!validSignalReferences(signal)) {
        return {
          status: "suppressed",
          reason: "invalid_signal",
          episodeId: null,
          wake: false
        };
      }
      if (signal.kind === "observation") {
        if (signal.outcome !== "succeeded") {
          return {
            status: "suppressed",
            reason: "unsuccessful_observation",
            episodeId: null,
            wake: false
          };
        }
        if (signal.previousContentHash === signal.contentHash) {
          return {
            status: "suppressed",
            reason: "unchanged",
            episodeId: null,
            wake: false
          };
        }
        if (signal.previousContentHash === null && !options.includeFirstObservation) {
          return {
            status: "suppressed",
            reason: "first_observation_disabled",
            episodeId: null,
            wake: false
          };
        }
      }
      const prepared = prepareEpisode(signal);
      if (!prepared) {
        return {
          status: "suppressed",
          reason: signal.kind === "user_correction" ? "empty_correction" : "no_meaningful_change",
          episodeId: null,
          wake: false
        };
      }
      try {
        const sessionId = prepared.input.sessionId;
        const callId = prepared.input.callId;
        if (!sessionId || !callId) throw new Error("missing episode identity");
        const existing = await dependencies.findEpisode({ sessionId, callId });
        if (existing) {
          return {
            status: "deduped",
            reason: null,
            episodeId: existing.id,
            wake: false
          };
        }
        const recorded = await dependencies.recordEpisode(prepared.input);
        return {
          status: "recorded",
          reason: null,
          episodeId: recorded.id,
          wake: prepared.wake
        };
      } catch {
        return {
          status: "failed",
          reason: "memory_unavailable",
          episodeId: null,
          wake: false
        };
      }
    }
  };
}
var import_node_crypto9, MAX_WORKSPACE_MEMORY_PAYLOAD_BYTES, BRIDGE_VERSION, MAX_REFERENCE_BYTES, MAX_PROVENANCE_BYTES, MAX_CONTENT_BYTES, MIN_CONTENT_BYTES, MIN_PROVENANCE_BYTES, SECRETISH_REFERENCE_RE, WORKSPACE_SLUG_RE2, projectionSchemaReady, defaultDependencies;
var init_workspace_observation_bridge = __esm({
  "src/memory/workspace-observation-bridge.ts"() {
    "use strict";
    import_node_crypto9 = require("node:crypto");
    init_security();
    init_db();
    init_temporal_memory();
    MAX_WORKSPACE_MEMORY_PAYLOAD_BYTES = 2048;
    BRIDGE_VERSION = 1;
    MAX_REFERENCE_BYTES = 80;
    MAX_PROVENANCE_BYTES = 240;
    MAX_CONTENT_BYTES = 720;
    MIN_CONTENT_BYTES = 120;
    MIN_PROVENANCE_BYTES = 64;
    SECRETISH_REFERENCE_RE = /(?:^|[._:/?&=-])(?:api[_-]?key|authorization|bearer|password|private[_-]?key|secret|token)(?:$|[._:/?&=-])/i;
    WORKSPACE_SLUG_RE2 = /^[a-z0-9][a-z0-9-]{0,61}[a-z0-9]$/;
    projectionSchemaReady = /* @__PURE__ */ new WeakSet();
    defaultDependencies = {
      findEpisode: ({ sessionId, callId }) => {
        const row = openMemoryDb().prepare(`
      SELECT id
      FROM memory_episodes
      WHERE session_id = ? AND call_id = ?
      LIMIT 1
    `).get(sessionId, callId);
        return row ?? null;
      },
      recordEpisode: (input) => recordMemoryEpisode(input)
    };
  }
});

// src/runtime/boundary-error.ts
var BoundaryError;
var init_boundary_error = __esm({
  "src/runtime/boundary-error.ts"() {
    "use strict";
    BoundaryError = class _BoundaryError extends Error {
      kind;
      retryable;
      userMessage;
      operatorMessage;
      context;
      cause;
      constructor(init) {
        super(init.operatorMessage);
        this.name = "BoundaryError";
        this.kind = init.kind;
        this.retryable = init.retryable;
        this.userMessage = init.userMessage;
        this.operatorMessage = init.operatorMessage;
        this.context = init.context ?? {};
        this.cause = init.cause;
        Object.setPrototypeOf(this, _BoundaryError.prototype);
      }
      /**
       * Wrap an unknown error caught at a boundary. If the caught value
       * is already a BoundaryError we pass it through — boundaries should
       * not re-wrap each other (the inner boundary already classified).
       *
       * The `hint` lets the catch site name the kind it expects when it
       * has no other classifier (e.g. an MCP shim catching anything from
       * a server-side call → 'mcp.tool_call_failed' is the safe default).
       */
      static from(err, hint) {
        if (err instanceof _BoundaryError) return err;
        const rawMsg = err instanceof Error ? err.message : String(err);
        return new _BoundaryError({
          kind: hint.kind,
          retryable: hint.retryable ?? false,
          userMessage: hint.userMessage,
          operatorMessage: hint.operatorMessage ?? `${hint.kind}: ${rawMsg}`,
          context: { ...hint.context ?? {}, rawMessage: rawMsg },
          cause: err
        });
      }
      /**
       * Quick predicate for "should a retry loop retry this?". Used by the
       * Codex backoff path and the MCP reconnect helper. Distinct from the
       * `retryable` field so a CALLER can also infer transience from kind
       * alone (e.g. SSE truncation is always worth one more attempt).
       */
      static isTransient(err) {
        if (!(err instanceof _BoundaryError)) return false;
        if (err.retryable) return true;
        switch (err.kind) {
          case "codex.http_5xx":
          case "codex.sse_truncated":
          case "codex.wall_clock":
          case "codex.transport_timeout":
          case "model.rate_limited":
          case "model.overloaded":
          case "model.transport_timeout":
          case "model.empty_completion":
          case "model.http_5xx":
          case "mcp.server_unavailable":
          case "notification.delivery_failed":
            return true;
          default:
            return false;
        }
      }
      /**
       * Render for structured logging. Pino picks this up via its
       * `.toJSON()` convention.
       */
      toJSON() {
        return {
          name: this.name,
          kind: this.kind,
          retryable: this.retryable,
          userMessage: this.userMessage,
          operatorMessage: this.operatorMessage,
          context: this.context,
          cause: this.cause instanceof Error ? { name: this.cause.name, message: this.cause.message } : this.cause
        };
      }
    };
  }
});

// src/runtime/atomic-json.ts
function assertFileLockIsNotReentrant(filePath) {
  let scope = heldFileLocks.getStore();
  while (scope) {
    if (scope.active && scope.filePath === filePath) break;
    scope = scope.parent;
  }
  if (!scope) return;
  throw new BoundaryError({
    kind: "state.write_failed",
    retryable: false,
    userMessage: `Couldn't safely re-enter the write lock on ${import_node_path13.default.basename(filePath)}.`,
    operatorMessage: `file lock is not re-entrant: ${filePath}`,
    context: { filePath, reason: "non_reentrant_lock" }
  });
}
function runWithHeldFileLockSync(filePath, work) {
  const scope = {
    filePath,
    active: true,
    parent: heldFileLocks.getStore()
  };
  return heldFileLocks.run(scope, () => {
    try {
      return work();
    } finally {
      scope.active = false;
    }
  });
}
function lockFilePath(filePath) {
  return `${filePath}.lock`;
}
function sameFileGeneration(left, right) {
  return right !== null && left.dev === right.dev && left.ino === right.ino;
}
function readFileLockSnapshot(lockPath) {
  let fd;
  try {
    fd = (0, import_node_fs5.openSync)(lockPath, "r");
    const stat = (0, import_node_fs5.fstatSync)(fd, { bigint: true });
    if (!stat.isFile()) return null;
    return {
      ownerToken: (0, import_node_fs5.readFileSync)(fd, "utf-8"),
      dev: stat.dev,
      ino: stat.ino,
      ctimeMs: Number(stat.ctimeMs)
    };
  } catch (err) {
    if (err.code === "ENOENT") return null;
    throw err;
  } finally {
    if (fd !== void 0) (0, import_node_fs5.closeSync)(fd);
  }
}
function createOwnerToken() {
  return `${process.pid}:${Date.now()}:${(0, import_node_crypto10.randomUUID)()}`;
}
function lockOwnerPid(ownerToken) {
  const match = OWNER_TOKEN_PATTERN.exec(ownerToken);
  if (!match) return null;
  const pid = Number(match[1]);
  const startedAt = Number(match[2]);
  return Number.isSafeInteger(pid) && pid > 0 && Number.isSafeInteger(startedAt) && startedAt > 0 ? pid : null;
}
function isPidAlive(pid) {
  try {
    process.kill(pid, 0);
    return true;
  } catch (err) {
    return err.code === "EPERM";
  }
}
function lockSnapshotIsStale(snapshot) {
  const ownerPid = lockOwnerPid(snapshot.ownerToken);
  if (ownerPid !== null) return !isPidAlive(ownerPid);
  return Date.now() - snapshot.ctimeMs > STALE_LOCK_MS;
}
function waitAfterStaleLockObservationForTest() {
  const marker = process.env.CLEMENTINE_TEST_FILE_LOCK_STALE_OBSERVED_MARKER;
  if (!marker) return;
  const markerFd = (0, import_node_fs5.openSync)(marker, "w");
  try {
    (0, import_node_fs5.writeSync)(markerFd, `${process.pid}
`);
  } finally {
    (0, import_node_fs5.closeSync)(markerFd);
  }
  const release = process.env.CLEMENTINE_TEST_FILE_LOCK_STALE_OBSERVED_RELEASE;
  if (!release) return;
  const wait = new Int32Array(new SharedArrayBuffer(4));
  while (!(0, import_node_fs5.existsSync)(release)) Atomics.wait(wait, 0, 0, 10);
}
function waitAfterDeletionGuardValidationForTest() {
  const marker = process.env.CLEMENTINE_TEST_FILE_LOCK_DELETE_VALIDATED_MARKER;
  if (!marker) return;
  const markerFd = (0, import_node_fs5.openSync)(marker, "w");
  try {
    (0, import_node_fs5.writeSync)(markerFd, `${process.pid}
`);
  } finally {
    (0, import_node_fs5.closeSync)(markerFd);
  }
  const release = process.env.CLEMENTINE_TEST_FILE_LOCK_DELETE_VALIDATED_RELEASE;
  if (!release) return;
  const wait = new Int32Array(new SharedArrayBuffer(4));
  while (!(0, import_node_fs5.existsSync)(release)) Atomics.wait(wait, 0, 0, 10);
}
function deletionGuardStillOwned(guard) {
  const current = readFileLockSnapshot(guard.path);
  return current !== null && current.ownerToken === guard.ownerToken && sameFileGeneration(guard, current);
}
function deletionGuardOwnerParts(ownerToken) {
  const match = OWNER_TOKEN_PATTERN.exec(ownerToken);
  if (!match?.[3]) throw new Error("Internal error: deletion guard owner token is malformed.");
  return { pid: Number(match[1]), startedAt: Number(match[2]), token: match[3] };
}
function deletionGuardPrefixes(lockPath) {
  const basename = import_node_path13.default.basename(lockPath);
  return {
    choose: `${basename}.reclaim-choose.`,
    ticket: `${basename}.reclaim-ticket.`
  };
}
function parseDeletionGuardMarker(directory, name, prefixes) {
  let ticket;
  let ownerText;
  if (name.startsWith(prefixes.choose)) {
    ownerText = name.slice(prefixes.choose.length);
  } else if (name.startsWith(prefixes.ticket)) {
    const rest = name.slice(prefixes.ticket.length);
    const dot = rest.indexOf(".");
    if (dot <= 0) return null;
    ticket = Number(rest.slice(0, dot));
    if (!Number.isSafeInteger(ticket) || ticket <= 0) return null;
    ownerText = rest.slice(dot + 1);
  } else {
    return null;
  }
  const ownerMatch = new RegExp(`^([1-9]\\d*)\\.([1-9]\\d*)\\.(${UUID_TOKEN_PATTERN})$`, "i").exec(ownerText);
  if (!ownerMatch) return null;
  const ownerPid = Number(ownerMatch[1]);
  const startedAt = Number(ownerMatch[2]);
  if (!Number.isSafeInteger(ownerPid) || !Number.isSafeInteger(startedAt)) return null;
  return {
    path: import_node_path13.default.join(directory, name),
    ownerToken: `${ownerPid}:${startedAt}:${ownerMatch[3]}`,
    ownerPid,
    ...ticket === void 0 ? {} : { ticket }
  };
}
function listDeletionGuardMarkers(lockPath) {
  const directory = import_node_path13.default.dirname(lockPath);
  const prefixes = deletionGuardPrefixes(lockPath);
  let names;
  try {
    names = (0, import_node_fs5.readdirSync)(directory);
  } catch (err) {
    if (err.code === "ENOENT") return [];
    throw err;
  }
  return names.flatMap((name) => {
    const marker = parseDeletionGuardMarker(directory, name, prefixes);
    return marker ? [marker] : [];
  });
}
function createDeletionGuardMarker(markerPath, ownerToken) {
  let fd;
  try {
    fd = (0, import_node_fs5.openSync)(markerPath, "wx", 384);
    (0, import_node_fs5.writeFileSync)(fd, ownerToken, "utf-8");
    const stat = (0, import_node_fs5.fstatSync)(fd, { bigint: true });
    return { ownerToken, dev: stat.dev, ino: stat.ino };
  } catch (err) {
    try {
      (0, import_node_fs5.unlinkSync)(markerPath);
    } catch {
    }
    throw err;
  } finally {
    if (fd !== void 0) (0, import_node_fs5.closeSync)(fd);
  }
}
function removeUniqueDeletionGuardMarker(markerPath) {
  try {
    (0, import_node_fs5.unlinkSync)(markerPath);
  } catch (err) {
    if (err.code !== "ENOENT") throw err;
  }
}
function tryAcquireDeletionGuard(lockPath) {
  const ownerToken = createOwnerToken();
  const owner = deletionGuardOwnerParts(ownerToken);
  const prefixes = deletionGuardPrefixes(lockPath);
  const directory = import_node_path13.default.dirname(lockPath);
  const ownerName = `${owner.pid}.${owner.startedAt}.${owner.token}`;
  const choosingPath = import_node_path13.default.join(directory, `${prefixes.choose}${ownerName}`);
  createDeletionGuardMarker(choosingPath, ownerToken);
  let guard = null;
  try {
    const existingTickets = listDeletionGuardMarkers(lockPath).filter((marker) => marker.ticket !== void 0 && isPidAlive(marker.ownerPid));
    const maxTicket = existingTickets.reduce(
      (max, marker) => Math.max(max, marker.ticket ?? 0),
      0
    );
    if (maxTicket >= Number.MAX_SAFE_INTEGER) {
      throw new Error(`Deletion guard ticket space exhausted for ${lockPath}`);
    }
    const ticket = maxTicket + 1;
    const ticketPath = import_node_path13.default.join(directory, `${prefixes.ticket}${ticket}.${ownerName}`);
    const lease = createDeletionGuardMarker(ticketPath, ownerToken);
    guard = { path: ticketPath, ticket, ...lease };
    removeUniqueDeletionGuardMarker(choosingPath);
    const markers = listDeletionGuardMarkers(lockPath);
    let blocked = false;
    for (const marker of markers) {
      const live = isPidAlive(marker.ownerPid);
      if (!live) {
        try {
          removeUniqueDeletionGuardMarker(marker.path);
        } catch {
        }
        continue;
      }
      if (marker.ticket === void 0) {
        blocked = true;
        continue;
      }
      if (marker.path !== guard.path && (marker.ticket < guard.ticket || marker.ticket === guard.ticket && marker.ownerToken < guard.ownerToken)) blocked = true;
    }
    if (blocked || !deletionGuardStillOwned(guard)) {
      releaseDeletionGuard(guard);
      guard = null;
      return null;
    }
    return guard;
  } catch (err) {
    if (guard) {
      try {
        releaseDeletionGuard(guard);
      } catch {
      }
    }
    throw err;
  } finally {
    try {
      removeUniqueDeletionGuardMarker(choosingPath);
    } catch {
    }
  }
}
function releaseDeletionGuard(guard) {
  if (!deletionGuardStillOwned(guard)) return;
  removeUniqueDeletionGuardMarker(guard.path);
}
function tryReapStaleFileLock(lockPath, observed) {
  const guard = tryAcquireDeletionGuard(lockPath);
  if (!guard) return null;
  try {
    const current = readFileLockSnapshot(lockPath);
    const currentOwnerPid = current ? lockOwnerPid(current.ownerToken) : null;
    const currentIsStillStale = current ? currentOwnerPid !== null ? !isPidAlive(currentOwnerPid) : Date.now() - observed.ctimeMs > STALE_LOCK_MS : false;
    if (!current || current.ownerToken !== observed.ownerToken || !sameFileGeneration(observed, current) || !currentIsStillStale || !deletionGuardStillOwned(guard)) return null;
    waitAfterDeletionGuardValidationForTest();
    return replaceStaleFileLock(lockPath);
  } catch (err) {
    if (err.code !== "ENOENT") throw err;
    return null;
  } finally {
    releaseDeletionGuard(guard);
  }
}
function replaceStaleFileLock(lockPath) {
  const ownerToken = createOwnerToken();
  const stagingPath = `${lockPath}.reap.${(0, import_node_crypto10.randomUUID)()}`;
  let fd;
  try {
    fd = (0, import_node_fs5.openSync)(stagingPath, "wx");
    (0, import_node_fs5.writeFileSync)(fd, ownerToken, "utf-8");
    (0, import_node_fs5.fsyncSync)(fd);
    const stat = (0, import_node_fs5.fstatSync)(fd, { bigint: true });
    (0, import_node_fs5.closeSync)(fd);
    fd = void 0;
    (0, import_node_fs5.renameSync)(stagingPath, lockPath);
    return { ownerToken, dev: stat.dev, ino: stat.ino };
  } catch {
    try {
      (0, import_node_fs5.unlinkSync)(lockPath);
    } catch {
    }
    return null;
  } finally {
    if (fd !== void 0) {
      try {
        (0, import_node_fs5.closeSync)(fd);
      } catch {
      }
    }
    try {
      (0, import_node_fs5.unlinkSync)(stagingPath);
    } catch {
    }
  }
}
function tryAcquireFileLock(filePath) {
  const lockPath = lockFilePath(filePath);
  const ownerToken = createOwnerToken();
  let fd;
  try {
    fd = (0, import_node_fs5.openSync)(lockPath, "wx");
    (0, import_node_fs5.writeFileSync)(fd, ownerToken, "utf-8");
    (0, import_node_fs5.fsyncSync)(fd);
    const stat = (0, import_node_fs5.fstatSync)(fd, { bigint: true });
    return { ownerToken, dev: stat.dev, ino: stat.ino };
  } catch (err) {
    if (err.code !== "EEXIST") throw err;
    const observed = readFileLockSnapshot(lockPath);
    if (observed && lockSnapshotIsStale(observed)) {
      waitAfterStaleLockObservationForTest();
      const claimed = tryReapStaleFileLock(lockPath, observed);
      if (claimed) return claimed;
    }
    return null;
  } finally {
    if (fd !== void 0) (0, import_node_fs5.closeSync)(fd);
  }
}
function releaseFileLockOwned(filePath, lease) {
  const lockPath = lockFilePath(filePath);
  const deadline = Date.now() + LOCK_MAX_WAIT_MS;
  while (true) {
    const current = readFileLockSnapshot(lockPath);
    if (!current || current.ownerToken !== lease.ownerToken || !sameFileGeneration(lease, current)) return;
    const guard = tryAcquireDeletionGuard(lockPath);
    if (!guard) {
      if (Date.now() >= deadline) return;
      sleepSync(LOCK_RETRY_MS);
      continue;
    }
    try {
      const guarded = readFileLockSnapshot(lockPath);
      if (guarded && guarded.ownerToken === lease.ownerToken && sameFileGeneration(lease, guarded) && deletionGuardStillOwned(guard)) {
        waitAfterDeletionGuardValidationForTest();
        (0, import_node_fs5.unlinkSync)(lockPath);
      }
    } catch (err) {
      if (err.code !== "ENOENT") throw err;
    } finally {
      releaseDeletionGuard(guard);
    }
    return;
  }
}
function releaseFileLock(filePath, lease) {
  try {
    releaseFileLockOwned(filePath, lease);
  } catch {
  }
}
function sleepSync(ms) {
  Atomics.wait(new Int32Array(new SharedArrayBuffer(4)), 0, 0, ms);
}
function withFileLockSyncStrict(filePath, work) {
  assertFileLockIsNotReentrant(filePath);
  const startedAt = Date.now();
  let lease = tryAcquireFileLock(filePath);
  while (!lease) {
    if (Date.now() - startedAt > LOCK_MAX_WAIT_MS) {
      throw new BoundaryError({
        kind: "state.write_failed",
        retryable: true,
        userMessage: `Couldn't get a write lock on ${import_node_path13.default.basename(filePath)} \u2014 another process may be stuck. Try again.`,
        operatorMessage: `withFileLockSyncStrict timeout after ${LOCK_MAX_WAIT_MS}ms on ${filePath}`,
        context: { filePath, waitedMs: Date.now() - startedAt }
      });
    }
    sleepSync(LOCK_RETRY_MS);
    lease = tryAcquireFileLock(filePath);
  }
  try {
    return runWithHeldFileLockSync(filePath, work);
  } finally {
    releaseFileLock(filePath, lease);
  }
}
var import_node_fs5, import_node_async_hooks3, import_node_path13, import_node_crypto10, heldFileLocks, STALE_LOCK_MS, LOCK_RETRY_MS, LOCK_MAX_WAIT_MS, UUID_TOKEN_PATTERN, OWNER_TOKEN_PATTERN;
var init_atomic_json = __esm({
  "src/runtime/atomic-json.ts"() {
    "use strict";
    import_node_fs5 = require("node:fs");
    import_node_async_hooks3 = require("node:async_hooks");
    import_node_path13 = __toESM(require("node:path"), 1);
    import_node_crypto10 = require("node:crypto");
    init_boundary_error();
    heldFileLocks = new import_node_async_hooks3.AsyncLocalStorage();
    STALE_LOCK_MS = 6e4;
    LOCK_RETRY_MS = 25;
    LOCK_MAX_WAIT_MS = 1e4;
    UUID_TOKEN_PATTERN = "[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}";
    OWNER_TOKEN_PATTERN = new RegExp(`^([1-9]\\d*):([1-9]\\d*)(?::(${UUID_TOKEN_PATTERN}))?$`, "i");
  }
});

// src/spaces/workspace-db-schema.ts
function ensureWorkspaceSchema(db) {
  const version = Number(db.pragma("user_version", { simple: true }) ?? 0);
  if (!Number.isInteger(version) || version < 0) {
    throw new Error(`invalid Workspace schema version: ${String(version)}`);
  }
  if (version > WORKSPACE_SCHEMA_VERSION) {
    throw new Error(
      `Workspace DB schema ${version} is newer than supported version ${WORKSPACE_SCHEMA_VERSION}`
    );
  }
  const hasWorkspaces = hasTable(db, "workspaces");
  if (!hasWorkspaces) {
    const create = db.transaction(() => {
      db.exec(WORKSPACE_SCHEMA_SQL);
      setUserVersion(db, WORKSPACE_SCHEMA_VERSION);
    });
    create.immediate();
    return;
  }
  const migrate = db.transaction(() => {
    if (hasTable(db, "workspace_datasets")) {
      addColumnIfMissing(db, "workspace_datasets", "source_key", "TEXT");
      addColumnIfMissing(db, "workspace_datasets", "bytes", "INTEGER NOT NULL DEFAULT 0");
      addColumnIfMissing(db, "workspace_datasets", "first_seen_at", "TEXT");
      addColumnIfMissing(db, "workspace_datasets", "last_seen_at", "TEXT");
      db.exec(`
        UPDATE workspace_datasets
        SET source_key = CASE
          WHEN source_key IS NOT NULL AND trim(source_key) <> '' THEN source_key
          WHEN source_id IS NOT NULL AND instr(source_id, ':source:') > 0
            THEN substr(source_id, instr(source_id, ':source:') + length(':source:'))
          WHEN source_id IS NOT NULL AND trim(source_id) <> '' THEN source_id
          ELSE 'legacy:' || substr(id, 1, 48)
        END
        WHERE source_key IS NULL OR trim(source_key) = '';

        UPDATE workspace_datasets
        SET bytes = length(CAST(doc_json AS BLOB))
        WHERE bytes IS NULL OR bytes = 0;

        UPDATE workspace_datasets
        SET first_seen_at = refreshed_at
        WHERE first_seen_at IS NULL OR trim(first_seen_at) = '';

        UPDATE workspace_datasets
        SET last_seen_at = refreshed_at
        WHERE last_seen_at IS NULL OR trim(last_seen_at) = '';
      `);
    }
    db.exec(WORKSPACE_SCHEMA_SQL);
    setUserVersion(db, WORKSPACE_SCHEMA_VERSION);
  });
  migrate.immediate();
}
function hasTable(db, table) {
  return Boolean(db.prepare(`
    SELECT 1
    FROM sqlite_master
    WHERE type = 'table' AND name = ?
  `).get(table));
}
function addColumnIfMissing(db, table, column, declaration) {
  const columns = db.pragma(`table_info(${quoteSqlIdentifier(table)})`);
  if (columns.some((entry) => entry.name === column)) return;
  db.exec(
    `ALTER TABLE ${quoteSqlIdentifier(table)} ADD COLUMN ${quoteSqlIdentifier(column)} ${declaration}`
  );
}
function quoteSqlIdentifier(value) {
  return `"${value.replaceAll('"', '""')}"`;
}
function setUserVersion(db, version) {
  db.pragma(`user_version = ${version}`);
}
var WORKSPACE_SCHEMA_VERSION, WORKSPACE_SCHEMA_SQL;
var init_workspace_db_schema = __esm({
  "src/spaces/workspace-db-schema.ts"() {
    "use strict";
    WORKSPACE_SCHEMA_VERSION = 5;
    WORKSPACE_SCHEMA_SQL = `
CREATE TABLE IF NOT EXISTS workspaces (
  id                 TEXT PRIMARY KEY,
  slug               TEXT NOT NULL UNIQUE,
  title              TEXT NOT NULL,
  status             TEXT NOT NULL CHECK (status IN ('active','paused','archived')),
  root_dir           TEXT NOT NULL,
  view_entry         TEXT NOT NULL DEFAULT 'view/index.html',
  origin_session_id  TEXT,
  focus_id           INTEGER,
  recipe_json        TEXT,
  metadata_json      TEXT NOT NULL DEFAULT '{}',
  created_at         TEXT NOT NULL,
  updated_at         TEXT NOT NULL,
  last_opened_at     TEXT,
  last_refreshed_at  TEXT
);

CREATE INDEX IF NOT EXISTS idx_workspaces_status_updated
  ON workspaces(status, updated_at DESC);

CREATE TABLE IF NOT EXISTS workspace_files (
  id            TEXT PRIMARY KEY,
  workspace_id  TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  rel_path      TEXT NOT NULL,
  kind          TEXT NOT NULL CHECK (kind IN ('view','asset','runner','data','note','audit','manifest','snapshot','other')),
  content_hash  TEXT NOT NULL,
  bytes         INTEGER NOT NULL DEFAULT 0,
  version       INTEGER NOT NULL DEFAULT 1,
  created_at    TEXT NOT NULL,
  updated_at    TEXT NOT NULL,
  UNIQUE(workspace_id, rel_path)
);

CREATE INDEX IF NOT EXISTS idx_workspace_files_workspace_kind
  ON workspace_files(workspace_id, kind);

CREATE TABLE IF NOT EXISTS workspace_revisions (
  id                 TEXT PRIMARY KEY,
  workspace_id       TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  file_id            TEXT REFERENCES workspace_files(id) ON DELETE SET NULL,
  version            INTEGER NOT NULL,
  snapshot_path      TEXT NOT NULL,
  content_hash       TEXT NOT NULL,
  bytes              INTEGER NOT NULL DEFAULT 0,
  author_session_id  TEXT,
  created_at         TEXT NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_workspace_revisions_workspace_created
  ON workspace_revisions(workspace_id, created_at DESC);

CREATE TABLE IF NOT EXISTS workspace_data_sources (
  id             TEXT PRIMARY KEY,
  workspace_id   TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  runner         TEXT,
  composio_slug  TEXT,
  args_json      TEXT NOT NULL DEFAULT '{}',
  schedule       TEXT,
  timezone       TEXT,
  last_status    TEXT,
  last_error     TEXT,
  last_run_at    TEXT,
  created_at     TEXT NOT NULL,
  updated_at     TEXT NOT NULL,
  CHECK (runner IS NOT NULL OR composio_slug IS NOT NULL)
);

CREATE INDEX IF NOT EXISTS idx_workspace_data_sources_workspace
  ON workspace_data_sources(workspace_id);

CREATE TABLE IF NOT EXISTS workspace_actions (
  id                  TEXT PRIMARY KEY,
  workspace_id        TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  runner              TEXT,
  composio_slug       TEXT,
  args_template_json  TEXT NOT NULL DEFAULT '{}',
  side_effect         TEXT NOT NULL DEFAULT 'write' CHECK (side_effect IN ('read','write','send')),
  approval_policy     TEXT NOT NULL DEFAULT 'required' CHECK (approval_policy IN ('auto','required','forbidden')),
  last_run_at         TEXT,
  created_at          TEXT NOT NULL,
  updated_at          TEXT NOT NULL,
  CHECK (runner IS NOT NULL OR composio_slug IS NOT NULL)
);

CREATE INDEX IF NOT EXISTS idx_workspace_actions_workspace
  ON workspace_actions(workspace_id);

CREATE TABLE IF NOT EXISTS workspace_datasets (
  id             TEXT PRIMARY KEY,
  workspace_id   TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  source_id      TEXT REFERENCES workspace_data_sources(id) ON DELETE SET NULL,
  source_key     TEXT NOT NULL,
  doc_json       TEXT NOT NULL DEFAULT '{}',
  content_hash   TEXT NOT NULL,
  bytes          INTEGER NOT NULL DEFAULT 0,
  status         TEXT NOT NULL DEFAULT 'ok' CHECK (status IN ('ok','error','stale')),
  error          TEXT,
  refreshed_at   TEXT NOT NULL,
  first_seen_at  TEXT NOT NULL,
  last_seen_at   TEXT NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_workspace_datasets_workspace_refreshed
  ON workspace_datasets(workspace_id, refreshed_at DESC);
CREATE INDEX IF NOT EXISTS idx_workspace_datasets_source_hash
  ON workspace_datasets(workspace_id, source_key, content_hash);

CREATE TABLE IF NOT EXISTS workspace_dataset_observations (
  id                       TEXT PRIMARY KEY,
  workspace_id             TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  source_key               TEXT NOT NULL,
  refresh_id               TEXT NOT NULL,
  batch_id                 TEXT NOT NULL,
  batch_index              INTEGER NOT NULL DEFAULT 0,
  cause                    TEXT NOT NULL,
  projection_mode          TEXT NOT NULL DEFAULT 'source'
                           CHECK (projection_mode IN ('source','document')),
  dataset_id               TEXT REFERENCES workspace_datasets(id) ON DELETE SET NULL,
  content_hash             TEXT,
  previous_observation_id  TEXT REFERENCES workspace_dataset_observations(id) ON DELETE SET NULL,
  previous_dataset_id      TEXT REFERENCES workspace_datasets(id) ON DELETE SET NULL,
  status                   TEXT NOT NULL
                           CHECK (status IN ('ok','error','awaiting_approval')),
  changed                  INTEGER CHECK (changed IS NULL OR changed IN (0,1)),
  is_current               INTEGER NOT NULL DEFAULT 0 CHECK (is_current IN (0,1)),
  provenance_json          TEXT NOT NULL DEFAULT '{}',
  error                    TEXT,
  commit_hash              TEXT NOT NULL,
  observed_at              TEXT NOT NULL,
  created_at               TEXT NOT NULL,
  UNIQUE(workspace_id, source_key, refresh_id)
);

CREATE UNIQUE INDEX IF NOT EXISTS idx_workspace_dataset_observations_current
  ON workspace_dataset_observations(workspace_id, source_key)
  WHERE is_current = 1;
CREATE INDEX IF NOT EXISTS idx_workspace_dataset_observations_source_observed
  ON workspace_dataset_observations(workspace_id, source_key, observed_at DESC);
CREATE INDEX IF NOT EXISTS idx_workspace_dataset_observations_workspace_observed
  ON workspace_dataset_observations(workspace_id, observed_at DESC);
CREATE INDEX IF NOT EXISTS idx_workspace_dataset_observations_batch
  ON workspace_dataset_observations(batch_id, batch_index);

CREATE TABLE IF NOT EXISTS workspace_dataset_source_retirements (
  workspace_id     TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  source_key       TEXT NOT NULL,
  projection_mode  TEXT NOT NULL DEFAULT 'source'
                   CHECK (projection_mode IN ('source','document')),
  retired_after_rowid INTEGER NOT NULL DEFAULT 0,
  retired_at       TEXT NOT NULL,
  PRIMARY KEY(workspace_id, source_key)
);

CREATE INDEX IF NOT EXISTS idx_workspace_dataset_source_retirements_workspace
  ON workspace_dataset_source_retirements(workspace_id, retired_at DESC);

CREATE TABLE IF NOT EXISTS workspace_state_events (
  id            TEXT PRIMARY KEY,
  workspace_id  TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  seq           INTEGER NOT NULL,
  run_id        TEXT,
  session_id    TEXT,
  event_type    TEXT NOT NULL,
  actor         TEXT,
  payload_json  TEXT NOT NULL DEFAULT '{}',
  created_at    TEXT NOT NULL,
  UNIQUE(workspace_id, seq)
);

CREATE INDEX IF NOT EXISTS idx_workspace_state_events_workspace_created
  ON workspace_state_events(workspace_id, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_workspace_state_events_run
  ON workspace_state_events(run_id) WHERE run_id IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_workspace_state_events_session
  ON workspace_state_events(session_id) WHERE session_id IS NOT NULL;

CREATE TABLE IF NOT EXISTS workspace_workflow_bindings (
  binding_id          TEXT PRIMARY KEY,
  workspace_id        TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  workflow_id         TEXT NOT NULL,
  role                TEXT NOT NULL CHECK (role IN ('primary','supporting')),
  projection_version  INTEGER NOT NULL CHECK (projection_version = 1),
  schedule_authority  TEXT NOT NULL CHECK (schedule_authority = 'workflow'),
  state               TEXT NOT NULL CHECK (state IN ('active','paused','retired')),
  revision            INTEGER NOT NULL CHECK (revision >= 1),
  binding_digest      TEXT NOT NULL,
  created_at          TEXT NOT NULL,
  updated_at          TEXT NOT NULL,
  UNIQUE(workspace_id, workflow_id)
);

CREATE UNIQUE INDEX IF NOT EXISTS idx_workspace_workflow_bindings_primary
  ON workspace_workflow_bindings(workspace_id)
  WHERE role = 'primary' AND state = 'active';
CREATE INDEX IF NOT EXISTS idx_workspace_workflow_bindings_workflow
  ON workspace_workflow_bindings(workflow_id, state, updated_at DESC);

CREATE TABLE IF NOT EXISTS workspace_run_projections (
  binding_id          TEXT PRIMARY KEY
                      REFERENCES workspace_workflow_bindings(binding_id) ON DELETE CASCADE,
  run_id              TEXT,
  binding_digest      TEXT NOT NULL,
  projection_digest   TEXT NOT NULL,
  projection_json     TEXT NOT NULL,
  updated_at          TEXT NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_workspace_run_projections_run
  ON workspace_run_projections(run_id, updated_at DESC) WHERE run_id IS NOT NULL;

CREATE TABLE IF NOT EXISTS workspace_run_partitions (
  binding_id             TEXT NOT NULL
                         REFERENCES workspace_workflow_bindings(binding_id) ON DELETE CASCADE,
  run_id                 TEXT,
  partition_id           TEXT NOT NULL,
  state                  TEXT NOT NULL
                         CHECK (state IN ('pending','running','completed','skipped','failed','blocked')),
  attempt                INTEGER NOT NULL CHECK (attempt >= 0),
  observations_committed INTEGER NOT NULL CHECK (observations_committed >= 0),
  canonical_records      INTEGER NOT NULL CHECK (canonical_records >= 0),
  duplicate_observations INTEGER NOT NULL CHECK (duplicate_observations >= 0),
  failure_ref            TEXT,
  updated_at             TEXT NOT NULL,
  PRIMARY KEY(binding_id, partition_id)
);

CREATE INDEX IF NOT EXISTS idx_workspace_run_partitions_state
  ON workspace_run_partitions(binding_id, state, partition_id);

/**
 * One rebuildable, reference-only canonical-entity projection head. Entity
 * bodies and partition collections remain in their normalized authority
 * stores; this row pins the exact source roots used for one visual snapshot.
 * It has no schedule, trigger, retry, lease, or execution column.
 */
CREATE TABLE IF NOT EXISTS workspace_canonical_entity_projection_heads (
  binding_id                  TEXT PRIMARY KEY
                              REFERENCES workspace_workflow_bindings(binding_id) ON DELETE CASCADE,
  workflow_id                 TEXT NOT NULL,
  workspace_id                TEXT NOT NULL,
  run_id                      TEXT NOT NULL,
  dataset_id                  TEXT NOT NULL,
  binding_digest              TEXT NOT NULL,
  dataset_contract_digest     TEXT NOT NULL,
  resolution_revision         INTEGER NOT NULL
                              CHECK (resolution_revision BETWEEN 0 AND 9007199254740991),
  resolution_root             TEXT NOT NULL,
  coverage_revision           INTEGER NOT NULL
                              CHECK (coverage_revision BETWEEN 0 AND 9007199254740991),
  coverage_root               TEXT NOT NULL,
  canonical_source_digest     TEXT NOT NULL,
  workspace_projection_digest TEXT NOT NULL,
  head_digest                 TEXT NOT NULL,
  sidecar_json                TEXT NOT NULL,
  projected_at                TEXT NOT NULL,
  CHECK (length(binding_id) BETWEEN 1 AND 256),
  CHECK (length(workflow_id) BETWEEN 1 AND 256),
  CHECK (length(workspace_id) BETWEEN 1 AND 256),
  CHECK (length(run_id) BETWEEN 1 AND 256),
  CHECK (length(dataset_id) BETWEEN 1 AND 256),
  CHECK (length(binding_digest) = 64 AND binding_digest NOT GLOB '*[^0-9a-f]*'),
  CHECK (length(dataset_contract_digest) = 64 AND dataset_contract_digest NOT GLOB '*[^0-9a-f]*'),
  CHECK (length(resolution_root) = 64 AND resolution_root NOT GLOB '*[^0-9a-f]*'),
  CHECK (length(coverage_root) = 64 AND coverage_root NOT GLOB '*[^0-9a-f]*'),
  CHECK (length(canonical_source_digest) = 64 AND canonical_source_digest NOT GLOB '*[^0-9a-f]*'),
  CHECK (length(workspace_projection_digest) = 64 AND workspace_projection_digest NOT GLOB '*[^0-9a-f]*'),
  CHECK (length(head_digest) = 64 AND head_digest NOT GLOB '*[^0-9a-f]*'),
  CHECK (json_valid(sidecar_json) AND length(CAST(sidecar_json AS BLOB)) <= 131072)
);

CREATE INDEX IF NOT EXISTS idx_workspace_canonical_entity_heads_dataset
  ON workspace_canonical_entity_projection_heads(dataset_id, resolution_revision, coverage_revision);

CREATE TABLE IF NOT EXISTS workspace_memory_scope (
  workspace_id  TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  fact_id       INTEGER,
  entity_id     INTEGER,
  resource_id   INTEGER,
  scope         TEXT NOT NULL CHECK (scope IN ('local','shared','global')),
  created_at    TEXT NOT NULL,
  CHECK (fact_id IS NOT NULL OR entity_id IS NOT NULL OR resource_id IS NOT NULL)
);

CREATE INDEX IF NOT EXISTS idx_workspace_memory_scope_workspace
  ON workspace_memory_scope(workspace_id, scope);
CREATE INDEX IF NOT EXISTS idx_workspace_memory_scope_fact
  ON workspace_memory_scope(fact_id) WHERE fact_id IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_workspace_memory_scope_entity
  ON workspace_memory_scope(entity_id) WHERE entity_id IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_workspace_memory_scope_resource
  ON workspace_memory_scope(resource_id) WHERE resource_id IS NOT NULL;

CREATE TABLE IF NOT EXISTS workspace_embeddings (
  id            TEXT PRIMARY KEY,
  workspace_id  TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  object_type   TEXT NOT NULL,
  object_id     TEXT NOT NULL,
  model         TEXT NOT NULL,
  dim           INTEGER NOT NULL,
  vector        BLOB NOT NULL,
  created_at    TEXT NOT NULL,
  UNIQUE(workspace_id, object_type, object_id, model)
);

CREATE INDEX IF NOT EXISTS idx_workspace_embeddings_object
  ON workspace_embeddings(workspace_id, object_type, object_id);
`;
  }
});

// src/spaces/workspace-db.ts
function openWorkspaceDb() {
  if (cachedDb2) return cachedDb2;
  ensureStateDir3();
  const db = new import_better_sqlite34.default(WORKSPACE_DB_PATH);
  db.pragma("journal_mode = WAL");
  db.pragma("synchronous = NORMAL");
  db.pragma("foreign_keys = ON");
  db.pragma("busy_timeout = 5000");
  ensureWorkspaceSchema(db);
  cachedDb2 = db;
  return db;
}
function indexWorkspaceRecord(record3, options = {}) {
  const db = options.db ?? openWorkspaceDb();
  const rootDir = options.rootDir ?? resolveWorkspaceRoot(record3.id);
  const now = (options.now ?? /* @__PURE__ */ new Date()).toISOString();
  let retiredSourceKeys = [];
  try {
    const tx = db.transaction(() => {
      upsertWorkspace(db, record3, rootDir);
      retiredSourceKeys = replaceDataSources(db, record3, now);
      replaceActions(db, record3, now);
      replaceFilesAndRevisions(db, record3, rootDir);
      if (options.appendStateEvent !== false) {
        appendWorkspaceStateEvent(db, record3.id, {
          eventType: options.eventType ?? "workspace_file_changed",
          actor: options.actor ?? "workspace-index",
          payload: {
            title: record3.title,
            status: record3.status,
            version: record3.version,
            ...options.payload ?? {}
          },
          createdAt: now
        });
      }
    });
    tx();
  } catch (error) {
    if (options.strict) throw error;
    return;
  }
  if (retiredSourceKeys.length > 0) {
    try {
      const healed = healWorkspaceDataProjection(record3.id, { db, rootDir });
      if (healed.changed) {
        const reindexHealedFiles = db.transaction(() => {
          replaceFilesAndRevisions(db, record3, rootDir);
        });
        reindexHealedFiles.immediate();
      }
    } catch (error) {
      if (options.strict) throw error;
    }
  }
  if (options.emitOperational !== false && !options.db) {
    try {
      recordOperationalEvent({
        source: "workspace",
        type: options.eventType ?? "workspace_file_changed",
        severity: "info",
        workspaceId: record3.id,
        sessionId: record3.originSessionId,
        actor: options.actor ?? "workspace-index",
        now: new Date(now),
        payload: {
          title: record3.title,
          status: record3.status,
          version: record3.version,
          rootDir,
          ...options.payload ?? {}
        }
      });
    } catch {
    }
  }
}
function deleteWorkspaceIndex(slug, options = {}) {
  if (!isValidWorkspaceSlug(slug)) return false;
  const db = options.db ?? openWorkspaceDb();
  const remove = db.transaction(() => {
    db.prepare("DELETE FROM workspaces WHERE slug = ?").run(slug);
    const remaining = db.prepare(`
      SELECT COUNT(*) AS count
      FROM workspaces
      WHERE slug = ?
    `).get(slug).count;
    if (remaining !== 0) {
      throw new Error(`durable Workspace delete verification failed for "${slug}"`);
    }
  });
  remove.immediate();
  if (options.emitOperational !== false && !options.db) {
    try {
      recordOperationalEvent({
        source: "workspace",
        type: "workspace_file_changed",
        severity: "warn",
        workspaceId: slug,
        actor: options.actor ?? "workspace-index",
        now: options.now,
        payload: { deleted: true }
      });
    } catch {
    }
  }
  return true;
}
function reindexWorkspaceRecords(records, options = {}) {
  let indexed = 0;
  for (const record3 of records) {
    indexWorkspaceRecord(record3, {
      ...options,
      eventType: "workspace_file_changed",
      appendStateEvent: false,
      payload: { reindexed: true, ...options.payload ?? {} }
    });
    indexed += 1;
  }
  return indexed;
}
function commitWorkspaceObservationBatch(input) {
  return commitWorkspaceObservationBatchInternal(input, {
    project: true,
    baselineOnly: false
  });
}
function getWorkspaceDatasetObservation(workspaceId, observationId, db = openWorkspaceDb()) {
  const row = db.prepare(`
    SELECT *
    FROM workspace_dataset_observations
    WHERE workspace_id = ? AND id = ?
    LIMIT 1
  `).get(workspaceId, observationId);
  return row ? mapWorkspaceObservation(row) : null;
}
function getWorkspaceDatasetObservationByRefreshId(workspaceId, sourceKey, refreshId, db = openWorkspaceDb()) {
  const row = db.prepare(`
    SELECT *
    FROM workspace_dataset_observations
    WHERE workspace_id = ?
      AND source_key = ?
      AND refresh_id = ?
    LIMIT 1
  `).get(
    normalizeWorkspaceId(workspaceId),
    normalizeSourceKeyForLookup(sourceKey),
    normalizeBoundedText(refreshId, "refreshId", 240)
  );
  return row ? mapWorkspaceObservation(row) : null;
}
function getWorkspaceObservationDocument(workspaceId, observationId, db = openWorkspaceDb()) {
  const row = db.prepare(`
    SELECT d.doc_json
    FROM workspace_dataset_observations o
    JOIN workspace_datasets d
      ON d.id = o.dataset_id
     AND d.workspace_id = o.workspace_id
    WHERE o.workspace_id = ?
      AND o.id = ?
      AND o.status = 'ok'
    LIMIT 1
  `).get(workspaceId, observationId);
  if (!row) return void 0;
  const bytes = Buffer.byteLength(row.doc_json, "utf-8");
  if (bytes > MAX_WORKSPACE_DATA_BYTES) return void 0;
  try {
    return JSON.parse(row.doc_json);
  } catch {
    return void 0;
  }
}
function healWorkspaceDataProjection(workspaceId, options = {}) {
  const db = options.db ?? openWorkspaceDb();
  const rootDir = options.rootDir ?? workspaceRootFromDb(db, workspaceId);
  const existing = readWorkspaceProjection(rootDir);
  const projection = buildWorkspaceProjection(db, workspaceId, existing);
  const serialized = serializeWorkspaceProjection(projection.document);
  const changed = atomicWriteWorkspaceProjectionIfChanged(rootDir, serialized.text);
  return { bytes: serialized.bytes, sources: projection.sources, changed };
}
function bootstrapWorkspaceObservationHistory(workspaceId, options = {}) {
  const db = options.db ?? openWorkspaceDb();
  let rootDir;
  try {
    rootDir = options.rootDir ?? workspaceRootFromDb(db, workspaceId);
  } catch (err) {
    return { ok: false, error: err.message };
  }
  try {
    const currentDocumentOwner = db.prepare(`
      SELECT 1
      FROM workspace_dataset_observations
      WHERE workspace_id = ?
        AND projection_mode = 'document'
        AND status = 'ok'
        AND is_current = 1
      LIMIT 1
    `).get(workspaceId);
    if (currentDocumentOwner) return { ok: true, imported: 0, skipped: 0 };
  } catch (err) {
    return {
      ok: false,
      error: `could not inspect current document authority: ${err.message}`
    };
  }
  const file = import_node_path14.default.join(rootDir, "data.json");
  if (!(0, import_node_fs6.existsSync)(file)) return { ok: true, imported: 0, skipped: 0 };
  let raw;
  try {
    raw = (0, import_node_fs6.readFileSync)(file, "utf-8");
  } catch (err) {
    return { ok: false, error: `could not read legacy data.json: ${err.message}` };
  }
  const bytes = Buffer.byteLength(raw, "utf-8");
  if (bytes > MAX_WORKSPACE_DATA_BYTES) {
    return {
      ok: false,
      error: `legacy data.json exceeds ${MAX_WORKSPACE_DATA_BYTES} byte cap (${bytes} bytes)`
    };
  }
  let document;
  try {
    document = JSON.parse(raw);
  } catch {
    return { ok: false, error: "legacy data.json is not valid JSON" };
  }
  const candidates = [];
  let staticSnapshot = false;
  try {
    const manifest = JSON.parse((0, import_node_fs6.readFileSync)(import_node_path14.default.join(rootDir, "space.json"), "utf8"));
    staticSnapshot = manifest?.contentMode === "static_snapshot";
  } catch {
  }
  if (staticSnapshot) {
    const sourceKey = "$document";
    const canonical = canonicalizeJson(document);
    candidates.push({
      sourceKey,
      refreshId: legacyRefreshId(workspaceId, sourceKey, canonical.contentHash),
      cause: "legacy_import",
      projectionMode: "document",
      status: "ok",
      data: document,
      observedAt: fileTimestamp(file)
    });
  } else if (isJsonObject(document)) {
    for (const [sourceKey, data] of Object.entries(document)) {
      if (sourceKey === "_meta") continue;
      const canonical = canonicalizeJson(data);
      candidates.push({
        sourceKey,
        refreshId: legacyRefreshId(workspaceId, sourceKey, canonical.contentHash),
        cause: "legacy_import",
        status: "ok",
        data,
        observedAt: fileTimestamp(file)
      });
    }
  } else {
    const sourceKey = "$document";
    const canonical = canonicalizeJson(document);
    candidates.push({
      sourceKey,
      refreshId: legacyRefreshId(workspaceId, sourceKey, canonical.contentHash),
      cause: "legacy_import",
      projectionMode: "document",
      status: "ok",
      data: document,
      observedAt: fileTimestamp(file)
    });
  }
  if (candidates.length === 0) return { ok: true, imported: 0, skipped: 0 };
  try {
    const result = commitWorkspaceObservationBatchInternal({
      db,
      rootDir,
      workspaceId,
      batchId: `legacy:${hashString(`${workspaceId}\0${raw}`).slice(0, 32)}`,
      observations: candidates
    }, {
      project: false,
      baselineOnly: true
    });
    const imported = result.observations.filter((entry) => !entry.deduped).length;
    return {
      ok: true,
      imported,
      skipped: candidates.length - imported
    };
  } catch (err) {
    return { ok: false, error: `could not bootstrap legacy history: ${err.message}` };
  }
}
function pruneWorkspaceDatasetHistory(workspaceId, options = {}) {
  const db = options.db ?? openWorkspaceDb();
  const maxPerSource = boundedInteger(
    options.maxObservationsPerSource,
    256,
    1,
    1e4,
    "maxObservationsPerSource"
  );
  const maxAgeDays = boundedInteger(options.maxAgeDays, 90, 1, 3650, "maxAgeDays");
  const maxDatasetBytes = boundedInteger(
    options.maxDatasetBytes,
    64 * 1024 * 1024,
    1,
    2 * 1024 * 1024 * 1024,
    "maxDatasetBytes"
  );
  const now = options.now ?? /* @__PURE__ */ new Date();
  const cutoff = new Date(now.getTime() - maxAgeDays * 864e5).toISOString();
  const tx = db.transaction(() => {
    let observationsDeleted = 0;
    let datasetsDeleted = 0;
    const protectedObservationIds = /* @__PURE__ */ new Set();
    const sourceRows = db.prepare(`
      SELECT DISTINCT source_key
      FROM workspace_dataset_observations
      WHERE workspace_id = ?
    `).all(workspaceId);
    const list = db.prepare(`
      SELECT id, status, is_current, previous_observation_id, observed_at
      FROM workspace_dataset_observations
      WHERE workspace_id = ? AND source_key = ?
      ORDER BY observed_at DESC, created_at DESC, rowid DESC
    `);
    const removeObservation = db.prepare(`
      DELETE FROM workspace_dataset_observations
      WHERE workspace_id = ? AND id = ? AND is_current = 0
    `);
    for (const source of sourceRows) {
      const rows = list.all(workspaceId, source.source_key);
      const current = rows.find((row) => row.is_current === 1);
      if (current) protectedObservationIds.add(current.id);
      const priorSuccess = (current?.previous_observation_id ? rows.find((row) => row.id === current.previous_observation_id) : void 0) ?? rows.find((row) => row.is_current === 0 && row.status === "ok");
      if (priorSuccess) protectedObservationIds.add(priorSuccess.id);
      rows.forEach((row, index) => {
        if (protectedObservationIds.has(row.id)) return;
        if (index < maxPerSource && row.observed_at >= cutoff) return;
        observationsDeleted += removeObservation.run(workspaceId, row.id).changes;
      });
    }
    const removeOrphanDatasets = () => db.prepare(`
      DELETE FROM workspace_datasets
      WHERE workspace_id = ?
        AND id GLOB 'wds_*'
        AND NOT EXISTS (
          SELECT 1
          FROM workspace_dataset_observations o
          WHERE o.dataset_id = workspace_datasets.id
        )
    `).run(workspaceId).changes;
    datasetsDeleted += removeOrphanDatasets();
    let retainedBytes = workspaceDatasetBytes(db, workspaceId);
    const oldestPrunable = db.prepare(`
      SELECT o.id
      FROM workspace_dataset_observations o
      JOIN workspace_datasets d ON d.id = o.dataset_id
      WHERE o.workspace_id = ?
        AND o.is_current = 0
        AND d.id GLOB 'wds_*'
      ORDER BY o.observed_at ASC, o.created_at ASC, o.rowid ASC
    `);
    while (retainedBytes > maxDatasetBytes) {
      const candidates = oldestPrunable.all(workspaceId);
      const candidate = candidates.find((entry) => !protectedObservationIds.has(entry.id));
      if (!candidate) break;
      const removed = removeObservation.run(workspaceId, candidate.id).changes;
      if (removed === 0) break;
      observationsDeleted += removed;
      datasetsDeleted += removeOrphanDatasets();
      retainedBytes = workspaceDatasetBytes(db, workspaceId);
    }
    return {
      observationsDeleted,
      datasetsDeleted,
      datasetBytesRetained: retainedBytes
    };
  });
  return tx.immediate();
}
function commitWorkspaceObservationBatchInternal(input, behavior) {
  const db = input.db ?? openWorkspaceDb();
  if (!Array.isArray(input.observations) || input.observations.length === 0) {
    throw new Error("Workspace observation batch requires at least one observation");
  }
  if (input.observations.length > 500) {
    throw new Error("Workspace observation batch exceeds 500-source cap");
  }
  const workspaceId = normalizeWorkspaceId(input.workspaceId);
  const rootDir = input.rootDir ?? workspaceRootFromDb(db, workspaceId);
  assertWorkspaceExists(db, workspaceId);
  const batchId = normalizeBoundedText(
    input.batchId ?? (0, import_node_crypto11.randomUUID)(),
    "batchId",
    200
  );
  const prepared = input.observations.map(prepareWorkspaceObservation);
  const sourceKeys = /* @__PURE__ */ new Set();
  for (const item of prepared) {
    if (sourceKeys.has(item.sourceKey)) {
      throw new Error(`Workspace observation batch repeats sourceKey "${item.sourceKey}"`);
    }
    sourceKeys.add(item.sourceKey);
  }
  const existingProjection = behavior.project ? readWorkspaceProjection(rootDir) : {};
  let projectedText = null;
  let projectedBytes = 0;
  let projectedSources = 0;
  const tx = db.transaction(() => {
    const committed = [];
    for (const [batchIndex, item] of prepared.entries()) {
      const existing = db.prepare(`
        SELECT *
        FROM workspace_dataset_observations
        WHERE workspace_id = ? AND source_key = ? AND refresh_id = ?
        LIMIT 1
      `).get(
        workspaceId,
        item.sourceKey,
        item.refreshId
      );
      if (existing) {
        if (existing.commit_hash !== item.commitHash) {
          throw new Error(
            `refreshId conflict for source "${item.sourceKey}": "${item.refreshId}" was already committed with different content`
          );
        }
        committed.push({ ...mapWorkspaceObservation(existing), deduped: true });
        continue;
      }
      if (behavior.baselineOnly && db.prepare(`
          SELECT 1
          FROM workspace_dataset_source_retirements
          WHERE workspace_id = ? AND source_key = ?
        `).get(workspaceId, item.sourceKey)) {
        continue;
      }
      const current = db.prepare(`
        SELECT *
        FROM workspace_dataset_observations
        WHERE workspace_id = ? AND source_key = ? AND is_current = 1
        LIMIT 1
      `).get(workspaceId, item.sourceKey);
      if (behavior.baselineOnly && current) continue;
      const retirement = db.prepare(`
        SELECT 1
        FROM workspace_dataset_source_retirements
        WHERE workspace_id = ? AND source_key = ?
      `).get(workspaceId, item.sourceKey);
      const sourceIsActive = Boolean(db.prepare(`
        SELECT 1
        FROM workspace_data_sources
        WHERE workspace_id = ? AND id = ?
      `).get(workspaceId, `${workspaceId}:source:${item.sourceKey}`));
      const canBecomeCurrent = !retirement || sourceIsActive;
      let datasetId = null;
      let changed = null;
      if (item.status === "ok") {
        if (!behavior.baselineOnly && sourceIsActive) {
          db.prepare(`
            DELETE FROM workspace_dataset_source_retirements
            WHERE workspace_id = ? AND source_key = ?
          `).run(workspaceId, item.sourceKey);
        }
        datasetId = upsertWorkspaceDatasetBlob(db, workspaceId, item);
        changed = current ? current.content_hash !== item.contentHash : null;
        if (current) {
          db.prepare(`
            UPDATE workspace_dataset_observations
            SET is_current = 0
            WHERE id = ? AND workspace_id = ?
          `).run(current.id, workspaceId);
        }
      }
      const id = (0, import_node_crypto11.randomUUID)();
      const createdAt = (/* @__PURE__ */ new Date()).toISOString();
      db.prepare(`
        INSERT INTO workspace_dataset_observations (
          id, workspace_id, source_key, refresh_id, batch_id, batch_index,
          cause, projection_mode, dataset_id, content_hash,
          previous_observation_id, previous_dataset_id, status, changed,
          is_current, provenance_json, error, commit_hash, observed_at,
          created_at
        ) VALUES (
          @id, @workspaceId, @sourceKey, @refreshId, @batchId, @batchIndex,
          @cause, @projectionMode, @datasetId, @contentHash,
          @previousObservationId, @previousDatasetId, @status, @changed,
          @isCurrent, @provenanceJson, @error, @commitHash, @observedAt,
          @createdAt
        )
      `).run({
        id,
        workspaceId,
        sourceKey: item.sourceKey,
        refreshId: item.refreshId,
        batchId,
        batchIndex,
        cause: item.cause,
        projectionMode: item.projectionMode,
        datasetId,
        contentHash: item.contentHash,
        previousObservationId: current?.id ?? null,
        previousDatasetId: current?.dataset_id ?? null,
        status: item.status,
        changed: changed === null ? null : changed ? 1 : 0,
        isCurrent: item.status === "ok" && canBecomeCurrent ? 1 : 0,
        provenanceJson: item.provenanceJson,
        error: item.error,
        commitHash: item.commitHash,
        observedAt: item.observedAt,
        createdAt
      });
      const row = db.prepare(`
        SELECT *
        FROM workspace_dataset_observations
        WHERE id = ?
      `).get(id);
      committed.push({ ...mapWorkspaceObservation(row), deduped: false });
    }
    if (behavior.project) {
      const projection = buildWorkspaceProjection(db, workspaceId, existingProjection);
      const serialized = serializeWorkspaceProjection(projection.document);
      projectedText = serialized.text;
      projectedBytes = serialized.bytes;
      projectedSources = projection.sources;
    }
    return committed;
  });
  const observations = tx.immediate();
  input.afterCommit?.();
  if (behavior.project) {
    if (projectedText === null) throw new Error("Workspace projection was not prepared");
    atomicWriteWorkspaceProjection(rootDir, projectedText);
  }
  return {
    batchId,
    observations,
    projection: {
      bytes: projectedBytes,
      sources: projectedSources
    }
  };
}
function prepareWorkspaceObservation(item) {
  if (!item || typeof item !== "object") {
    throw new Error("Workspace observation must be an object");
  }
  const sourceKey = normalizeSourceKey(item.sourceKey);
  const refreshId = normalizeBoundedText(item.refreshId, "refreshId", 240);
  const cause = normalizeBoundedText(item.cause, "cause", 120);
  const status = normalizeObservationStatus(item.status);
  const projectionMode = item.projectionMode ?? "source";
  if (projectionMode !== "source" && projectionMode !== "document") {
    throw new Error(`invalid Workspace projectionMode: ${String(projectionMode)}`);
  }
  const observedAt = normalizeTimestamp(item.observedAt ?? /* @__PURE__ */ new Date(), "observedAt");
  const provenance = sanitizeWorkspaceObservationProvenance(item.provenance);
  const provenanceJson = stableStringify(provenance);
  if (Buffer.byteLength(provenanceJson, "utf-8") > MAX_PROVENANCE_BYTES2) {
    throw new Error(`Workspace observation provenance exceeds ${MAX_PROVENANCE_BYTES2} byte cap`);
  }
  let canonicalData = null;
  let contentHash2 = null;
  let dataBytes = 0;
  let error = null;
  if (status === "ok") {
    if (!Object.prototype.hasOwnProperty.call(item, "data")) {
      throw new Error(`successful observation "${sourceKey}" requires data`);
    }
    const canonical = canonicalizeJson(item.data);
    canonicalData = canonical.text;
    contentHash2 = canonical.contentHash;
    dataBytes = canonical.bytes;
    if (dataBytes > MAX_WORKSPACE_DATA_BYTES) {
      throw new Error(
        `Workspace observation data exceeds ${MAX_WORKSPACE_DATA_BYTES} byte cap (${dataBytes} bytes)`
      );
    }
  } else {
    if (Object.prototype.hasOwnProperty.call(item, "data")) {
      throw new Error(`${status} observation "${sourceKey}" cannot include data`);
    }
    error = scrubWorkspaceObservationError(
      item.error ?? (status === "error" ? "refresh failed" : "awaiting approval")
    );
  }
  const commitHash = hashString(stableStringify({
    sourceKey,
    refreshId,
    cause,
    status,
    projectionMode,
    contentHash: contentHash2,
    error,
    provenance
  }));
  return {
    sourceKey,
    refreshId,
    cause,
    status,
    projectionMode,
    canonicalData,
    contentHash: contentHash2,
    dataBytes,
    error,
    provenanceJson,
    observedAt,
    commitHash
  };
}
function upsertWorkspaceDatasetBlob(db, workspaceId, item) {
  if (item.status !== "ok" || item.canonicalData === null || item.contentHash === null) {
    throw new Error("only successful observations can create Workspace dataset blobs");
  }
  const candidates = db.prepare(`
    SELECT id, doc_json
    FROM workspace_datasets
    WHERE workspace_id = ?
      AND source_key = ?
      AND content_hash = ?
      AND status = 'ok'
    ORDER BY refreshed_at ASC, id ASC
  `).all(workspaceId, item.sourceKey, item.contentHash);
  const reusable = candidates.find((candidate) => candidate.doc_json === item.canonicalData);
  const sourceId = db.prepare(`
    SELECT id
    FROM workspace_data_sources
    WHERE workspace_id = ? AND id = ?
    LIMIT 1
  `).get(
    workspaceId,
    `${workspaceId}:source:${item.sourceKey}`
  );
  if (reusable) {
    db.prepare(`
      UPDATE workspace_datasets
      SET last_seen_at = ?,
          source_id = COALESCE(source_id, ?)
      WHERE id = ? AND workspace_id = ?
    `).run(item.observedAt, sourceId?.id ?? null, reusable.id, workspaceId);
    return reusable.id;
  }
  const id = `wds_${hashString(
    `${workspaceId}\0${item.sourceKey}\0${item.contentHash}`
  )}`;
  db.prepare(`
    INSERT INTO workspace_datasets (
      id, workspace_id, source_id, source_key, doc_json, content_hash, bytes,
      status, error, refreshed_at, first_seen_at, last_seen_at
    ) VALUES (
      @id, @workspaceId, @sourceId, @sourceKey, @docJson, @contentHash, @bytes,
      'ok', NULL, @refreshedAt, @firstSeenAt, @lastSeenAt
    )
    ON CONFLICT(id) DO NOTHING
  `).run({
    id,
    workspaceId,
    sourceId: sourceId?.id ?? null,
    sourceKey: item.sourceKey,
    docJson: item.canonicalData,
    contentHash: item.contentHash,
    bytes: item.dataBytes,
    refreshedAt: item.observedAt,
    firstSeenAt: item.observedAt,
    lastSeenAt: item.observedAt
  });
  const stored = db.prepare(`
    SELECT workspace_id, source_key, content_hash, doc_json
    FROM workspace_datasets
    WHERE id = ?
  `).get(id);
  if (!stored || stored.workspace_id !== workspaceId || stored.source_key !== item.sourceKey || stored.content_hash !== item.contentHash || stored.doc_json !== item.canonicalData) {
    throw new Error("Workspace dataset content-address collision");
  }
  return id;
}
function buildWorkspaceProjection(db, workspaceId, existing) {
  const retirements = db.prepare(`
    SELECT source_key, projection_mode, retired_after_rowid, retired_at
    FROM workspace_dataset_source_retirements
    WHERE workspace_id = ?
  `).all(workspaceId);
  const retirementBySource = new Map(
    retirements.map((entry) => [entry.source_key, entry])
  );
  const activeSourceKeys = new Set(
    db.prepare(`
      SELECT id
      FROM workspace_data_sources
      WHERE workspace_id = ?
    `).all(workspaceId).map(
      (entry) => sourceKeyFromStoredRowId(workspaceId, entry.id)
    )
  );
  const current = db.prepare(`
    SELECT
      o.rowid AS row_id,
      o.source_key,
      o.projection_mode,
      o.status,
      d.doc_json,
      o.error,
      o.cause,
      o.provenance_json,
      o.observed_at,
      o.created_at,
      o.batch_index
    FROM workspace_dataset_observations o
    JOIN workspace_datasets d
      ON d.id = o.dataset_id
     AND d.workspace_id = o.workspace_id
    WHERE o.workspace_id = ?
      AND o.is_current = 1
      AND o.status = 'ok'
  `).all(workspaceId);
  const latestNonSuccess = db.prepare(`
    SELECT
      o.rowid AS row_id,
      o.source_key,
      o.projection_mode,
      o.status,
      NULL AS doc_json,
      o.error,
      o.cause,
      o.provenance_json,
      o.observed_at,
      o.created_at,
      o.batch_index
    FROM workspace_dataset_observations o
    WHERE o.workspace_id = ?
      AND o.status <> 'ok'
      AND o.rowid = (
        SELECT newest.rowid
        FROM workspace_dataset_observations newest
        WHERE newest.workspace_id = o.workspace_id
          AND newest.source_key = o.source_key
        ORDER BY newest.rowid DESC
        LIMIT 1
      )
  `).all(workspaceId);
  const events = [...current, ...latestNonSuccess].filter((event) => {
    const retirement = retirementBySource.get(event.source_key);
    if (!retirement) return true;
    if (event.status === "ok") return false;
    return activeSourceKeys.has(event.source_key) && event.row_id > retirement.retired_after_rowid;
  }).sort(compareProjectionEvents);
  let document = pruneRetiredSourceProjections(existing, retirements);
  const sources = /* @__PURE__ */ new Set();
  for (const event of events) {
    sources.add(event.source_key);
    if (event.status === "ok") {
      let data;
      try {
        data = JSON.parse(event.doc_json ?? "null");
      } catch {
        continue;
      }
      if (event.projection_mode === "document") {
        document = data;
      } else {
        document = applySourceProjection(document, event.source_key, data, {
          refreshedAt: event.observed_at,
          ok: true,
          provenance: event.cause
        });
      }
      continue;
    }
    const provenance = parseProvenanceJson(event.provenance_json);
    const approvalId = typeof provenance.approvalId === "string" ? provenance.approvalId : void 0;
    const approvalResolution = event.status === "error" && typeof provenance.approvalResolution === "string" ? provenance.approvalResolution : void 0;
    const approvalResolvedAt = event.status === "error" && typeof provenance.approvalResolvedAt === "string" ? provenance.approvalResolvedAt : void 0;
    document = applySourceMeta(document, event.source_key, {
      refreshedAt: event.observed_at,
      ok: event.status === "awaiting_approval" ? null : false,
      status: event.status,
      error: event.error ?? (event.status === "awaiting_approval" ? "awaiting approval" : "refresh failed"),
      ...approvalId ? { approvalId } : {},
      ...approvalResolution ? { approvalResolution } : {},
      ...approvalResolvedAt ? { approvalResolvedAt } : {}
    });
  }
  return { document, sources: sources.size };
}
function pruneRetiredSourceProjections(document, retirements) {
  if (retirements.length === 0) return cloneJsonValue(document);
  if (retirements.some((entry) => entry.projection_mode === "document")) return {};
  const root = isJsonObject(document) ? { ...document } : {};
  const meta = isJsonObject(root._meta) ? { ...root._meta } : null;
  for (const retirement of retirements) {
    Reflect.deleteProperty(root, retirement.source_key);
    if (meta) Reflect.deleteProperty(meta, retirement.source_key);
  }
  if (meta) defineOwnEnumerable(root, "_meta", meta);
  return root;
}
function compareProjectionEvents(a, b) {
  return a.row_id - b.row_id;
}
function applySourceProjection(document, sourceKey, data, meta) {
  const root = isJsonObject(document) ? { ...document } : {};
  defineOwnEnumerable(root, sourceKey, data);
  return applySourceMeta(root, sourceKey, meta);
}
function applySourceMeta(document, sourceKey, entry) {
  const root = isJsonObject(document) ? { ...document } : {};
  const existingMeta = isJsonObject(root._meta) ? { ...root._meta } : {};
  defineOwnEnumerable(existingMeta, sourceKey, entry);
  defineOwnEnumerable(root, "_meta", existingMeta);
  return root;
}
function defineOwnEnumerable(target, key, value) {
  Object.defineProperty(target, key, {
    value,
    enumerable: true,
    configurable: true,
    writable: true
  });
}
function readWorkspaceProjection(rootDir) {
  const file = import_node_path14.default.join(rootDir, "data.json");
  if (!(0, import_node_fs6.existsSync)(file)) return {};
  try {
    const raw = (0, import_node_fs6.readFileSync)(file, "utf-8");
    if (Buffer.byteLength(raw, "utf-8") > MAX_WORKSPACE_DATA_BYTES) return {};
    return JSON.parse(raw);
  } catch {
    return {};
  }
}
function serializeWorkspaceProjection(document) {
  let text;
  try {
    const serialized = JSON.stringify(document === void 0 ? {} : document);
    if (serialized === void 0) {
      throw new Error("top-level value is not representable in JSON");
    }
    text = serialized;
  } catch (err) {
    throw new Error(`Workspace data projection is not JSON-serializable: ${err.message}`);
  }
  const bytes = Buffer.byteLength(text, "utf-8");
  if (bytes > MAX_WORKSPACE_DATA_BYTES) {
    throw new Error(
      `Workspace data projection exceeds ${MAX_WORKSPACE_DATA_BYTES} byte cap (${bytes} bytes)`
    );
  }
  return { text, bytes };
}
function atomicWriteWorkspaceProjection(rootDir, text) {
  if (!(0, import_node_fs6.existsSync)(rootDir)) (0, import_node_fs6.mkdirSync)(rootDir, { recursive: true });
  const file = import_node_path14.default.join(rootDir, "data.json");
  const tmp = `${file}.${process.pid}.${(0, import_node_crypto11.randomUUID)().slice(0, 8)}.tmp`;
  (0, import_node_fs6.writeFileSync)(tmp, text, "utf-8");
  (0, import_node_fs6.renameSync)(tmp, file);
}
function atomicWriteWorkspaceProjectionIfChanged(rootDir, text) {
  const file = import_node_path14.default.join(rootDir, "data.json");
  try {
    const expected = Buffer.from(text, "utf-8");
    if ((0, import_node_fs6.readFileSync)(file).equals(expected)) return false;
  } catch {
  }
  atomicWriteWorkspaceProjection(rootDir, text);
  return true;
}
function workspaceRootFromDb(db, workspaceId) {
  const row = db.prepare(`
    SELECT root_dir
    FROM workspaces
    WHERE id = ?
    LIMIT 1
  `).get(workspaceId);
  if (!row) throw new Error(`Workspace "${workspaceId}" is not indexed`);
  return row.root_dir;
}
function assertWorkspaceExists(db, workspaceId) {
  if (!db.prepare("SELECT 1 FROM workspaces WHERE id = ?").get(workspaceId)) {
    throw new Error(`Workspace "${workspaceId}" is not indexed`);
  }
}
function mapWorkspaceObservation(row) {
  const provenance = parseProvenanceJson(row.provenance_json);
  return {
    id: row.id,
    workspaceId: row.workspace_id,
    sourceKey: row.source_key,
    refreshId: row.refresh_id,
    batchId: row.batch_id,
    batchIndex: row.batch_index,
    cause: row.cause,
    projectionMode: row.projection_mode,
    datasetId: row.dataset_id,
    contentHash: row.content_hash,
    previousObservationId: row.previous_observation_id,
    previousDatasetId: row.previous_dataset_id,
    status: row.status,
    changed: row.changed === null ? null : row.changed === 1,
    isCurrent: row.is_current === 1,
    provenance,
    error: row.error,
    observedAt: row.observed_at,
    createdAt: row.created_at
  };
}
function parseProvenanceJson(value) {
  try {
    const parsed = JSON.parse(value);
    return isJsonObject(parsed) ? parsed : {};
  } catch {
    return {};
  }
}
function sanitizeWorkspaceObservationProvenance(input) {
  if (input === void 0) return {};
  if (!isJsonObject(input)) throw new Error("Workspace observation provenance must be an object");
  let raw;
  try {
    raw = JSON.stringify(input);
  } catch {
    throw new Error("Workspace observation provenance must be JSON-serializable");
  }
  if (Buffer.byteLength(raw, "utf-8") > MAX_PROVENANCE_INPUT_BYTES) {
    throw new Error(
      `Workspace observation provenance input exceeds ${MAX_PROVENANCE_INPUT_BYTES} byte cap`
    );
  }
  const out = {};
  for (const [key, value] of Object.entries(input)) {
    if (!PROVENANCE_ALLOWLIST.has(key)) continue;
    if (value === null || typeof value === "boolean" || typeof value === "number" && Number.isFinite(value)) {
      out[key] = value;
      continue;
    }
    if (typeof value === "string") {
      out[key] = provenanceString(value);
    }
  }
  return out;
}
function scrubWorkspaceObservationError(input) {
  let value = redactSensitiveText(String(input ?? "")).replaceAll("\0", "");
  value = value.replace(
    /-----BEGIN [^-]*PRIVATE KEY-----[\s\S]*?-----END [^-]*PRIVATE KEY-----/gi,
    "[REDACTED PRIVATE KEY]"
  ).replace(
    /(["']?(?:authorization|proxy-authorization|x-api-key|api(?:[_\s-]?key)|access(?:[_\s-]?token)|refresh(?:[_\s-]?token)|client(?:[_\s-]?secret)|password|passwd|secret|cookie|set-cookie)["']?\s*[:=]\s*)(["'])(.*?)\2/gi,
    "$1$2[REDACTED]$2"
  ).replace(/\b(?:Bearer|Basic)\s+[A-Za-z0-9._~+/=-]+/gi, "[REDACTED]").replace(
    /\b(?:authorization|proxy-authorization|x-api-key|api(?:[_\s-]?key)|access(?:[_\s-]?token)|refresh(?:[_\s-]?token)|client(?:[_\s-]?secret)|password|passwd|secret|cookie|set-cookie)\b\s*[:=]\s*(?:"[^"]*"|'[^']*'|[^\s,;]+)/gi,
    "[REDACTED]"
  ).replace(
    /\b([a-z][a-z0-9+.-]*:\/\/)[^/\s:@]+:[^@\s/]+@/gi,
    "$1[REDACTED]@"
  ).replace(
    /([?&](?:api[_-]?key|access[_-]?token|refresh[_-]?token|client[_-]?secret|password|secret)=)[^&#\s]+/gi,
    "$1[REDACTED]"
  ).replace(/\b(?:sk|pk)_(?:live|test)_[A-Za-z0-9_-]{8,}\b/gi, "[REDACTED]").replace(/\b(?:sk-|gh[pousr]_|xox[baprs]-|rk_|whsec_)[A-Za-z0-9_-]{4,}\b/gi, "[REDACTED]").replace(/\bAIza[A-Za-z0-9_-]{12,}\b/g, "[REDACTED]").replace(/\bAKIA[A-Z0-9]{16}\b/g, "[REDACTED]").replace(/\beyJ[A-Za-z0-9_-]{8,}\.[A-Za-z0-9_-]{8,}\.[A-Za-z0-9_-]{8,}\b/g, "[REDACTED]");
  return value.slice(0, MAX_OBSERVATION_ERROR_CHARS);
}
function provenanceString(value) {
  const bounded = value.slice(0, 1e3);
  const scrubbed = scrubWorkspaceObservationError(bounded);
  return scrubbed !== bounded ? "[REDACTED]" : scrubbed;
}
function canonicalizeJson(value) {
  let normalized;
  try {
    const ordinary = JSON.stringify(value === void 0 ? {} : value);
    if (ordinary === void 0) {
      throw new Error("top-level value is not representable in JSON");
    }
    normalized = JSON.parse(ordinary);
  } catch (err) {
    throw new Error(`Workspace observation data is not JSON-serializable: ${err.message}`);
  }
  const text = stableStringify(normalized);
  return {
    text,
    contentHash: hashString(text),
    bytes: Buffer.byteLength(text, "utf-8")
  };
}
function stableStringify(value) {
  if (value === null || typeof value === "string" || typeof value === "boolean") {
    return JSON.stringify(value);
  }
  if (typeof value === "number") return JSON.stringify(value);
  if (Array.isArray(value)) {
    return `[${value.map((entry) => stableStringify(entry)).join(",")}]`;
  }
  if (isJsonObject(value)) {
    return `{${Object.keys(value).sort().map(
      (key) => `${JSON.stringify(key)}:${stableStringify(value[key])}`
    ).join(",")}}`;
  }
  throw new Error(`unsupported JSON value: ${typeof value}`);
}
function cloneJsonValue(value) {
  try {
    return JSON.parse(JSON.stringify(value ?? {}));
  } catch {
    return {};
  }
}
function isJsonObject(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function normalizeWorkspaceId(value) {
  const id = normalizeBoundedText(value, "workspaceId", 128);
  if (!isValidWorkspaceSlug(id)) throw new Error(`invalid workspace slug: ${id}`);
  return id;
}
function normalizeSourceKey(value) {
  const sourceKey = normalizeSourceKeyForLookup(value);
  if (sourceKey === "_meta") throw new Error('"_meta" is a reserved Workspace source key');
  return sourceKey;
}
function normalizeSourceKeyForLookup(value) {
  const sourceKey = normalizeBoundedText(value, "sourceKey", 512);
  if (/[\u0000-\u001f\u007f]/.test(sourceKey)) {
    throw new Error("Workspace sourceKey cannot contain control characters");
  }
  return sourceKey;
}
function normalizeObservationStatus(value) {
  if (value === "ok" || value === "error" || value === "awaiting_approval") return value;
  throw new Error(`invalid Workspace observation status: ${String(value)}`);
}
function normalizeBoundedText(value, field, max) {
  if (typeof value !== "string") throw new Error(`${field} must be a string`);
  const normalized = value.trim();
  if (!normalized) throw new Error(`${field} cannot be blank`);
  if (normalized.length > max) throw new Error(`${field} exceeds ${max} character cap`);
  if (/[\u0000-\u0008\u000b\u000c\u000e-\u001f\u007f]/.test(normalized)) {
    throw new Error(`${field} contains control characters`);
  }
  return normalized;
}
function normalizeTimestamp(value, field) {
  const date = value instanceof Date ? value : new Date(value);
  if (!Number.isFinite(date.getTime())) throw new Error(`${field} must be a valid timestamp`);
  return date.toISOString();
}
function boundedInteger(value, fallback, min, max, field) {
  const selected = value ?? fallback;
  if (!Number.isInteger(selected) || selected < min || selected > max) {
    throw new Error(`${field} must be an integer between ${min} and ${max}`);
  }
  return selected;
}
function legacyRefreshId(workspaceId, sourceKey, contentHash2) {
  return `legacy:${hashString(`${workspaceId}\0${sourceKey}\0${contentHash2}`).slice(0, 48)}`;
}
function fileTimestamp(file) {
  try {
    return (0, import_node_fs6.statSync)(file).mtime.toISOString();
  } catch {
    return (/* @__PURE__ */ new Date(0)).toISOString();
  }
}
function workspaceDatasetBytes(db, workspaceId) {
  return Number(db.prepare(`
    SELECT COALESCE(SUM(bytes), 0) AS bytes
    FROM workspace_datasets
    WHERE workspace_id = ? AND id GLOB 'wds_*'
  `).get(workspaceId)?.bytes ?? 0);
}
function upsertWorkspace(db, record3, rootDir) {
  db.prepare(`
    INSERT INTO workspaces (
      id, slug, title, status, root_dir, view_entry, origin_session_id, focus_id,
      recipe_json, metadata_json, created_at, updated_at, last_opened_at,
      last_refreshed_at
    ) VALUES (
      @id, @slug, @title, @status, @rootDir, @viewEntry, @originSessionId, @focusId,
      @recipeJson, @metadataJson, @createdAt, @updatedAt, @lastOpenedAt,
      @lastRefreshedAt
    )
    ON CONFLICT(id) DO UPDATE SET
      slug = excluded.slug,
      title = excluded.title,
      status = excluded.status,
      root_dir = excluded.root_dir,
      view_entry = excluded.view_entry,
      origin_session_id = excluded.origin_session_id,
      focus_id = excluded.focus_id,
      recipe_json = excluded.recipe_json,
      metadata_json = excluded.metadata_json,
      updated_at = excluded.updated_at,
      last_opened_at = excluded.last_opened_at,
      last_refreshed_at = excluded.last_refreshed_at
  `).run({
    id: record3.id,
    slug: record3.id,
    title: record3.title,
    status: record3.status,
    rootDir,
    viewEntry: record3.viewEntry,
    originSessionId: record3.originSessionId ?? null,
    focusId: record3.focusId ?? null,
    recipeJson: record3.recipe ? JSON.stringify({ text: record3.recipe }) : null,
    metadataJson: JSON.stringify({
      contract: record3.contract,
      reengage: record3.reengage,
      manifestErrors: record3.manifestErrors,
      version: record3.version
    }),
    createdAt: record3.createdAt,
    updatedAt: record3.updatedAt,
    lastOpenedAt: record3.lastOpenedAt ?? null,
    lastRefreshedAt: record3.lastRefreshedAt ?? null
  });
}
function replaceDataSources(db, record3, now) {
  const previousIds = db.prepare(`
    SELECT id
    FROM workspace_data_sources
    WHERE workspace_id = ?
  `).all(record3.id).map((entry) => entry.id);
  const insert = db.prepare(`
    INSERT INTO workspace_data_sources (
      id, workspace_id, runner, composio_slug, args_json, schedule, timezone,
      created_at, updated_at
    ) VALUES (
      @id, @workspaceId, @runner, @composioSlug, @argsJson, @schedule, @timezone,
      @createdAt, @updatedAt
    )
    ON CONFLICT(id) DO UPDATE SET
      runner = excluded.runner,
      composio_slug = excluded.composio_slug,
      args_json = excluded.args_json,
      schedule = excluded.schedule,
      timezone = excluded.timezone,
      updated_at = excluded.updated_at
  `);
  const retainedIds = [];
  for (const [index, source] of record3.dataSources.entries()) {
    if (!source.runner && !source.composioSlug) continue;
    const id = sourceRowId(record3.id, source, index);
    retainedIds.push(id);
    insert.run({
      id,
      workspaceId: record3.id,
      runner: source.runner ?? null,
      composioSlug: source.composioSlug ?? null,
      argsJson: JSON.stringify(source.composioArgs ?? {}),
      schedule: source.schedule ?? null,
      timezone: source.timezone ?? null,
      createdAt: record3.createdAt,
      updatedAt: now
    });
  }
  if (retainedIds.length === 0) {
    db.prepare("DELETE FROM workspace_data_sources WHERE workspace_id = ?").run(record3.id);
  } else {
    const placeholders = retainedIds.map(() => "?").join(",");
    db.prepare(`
      DELETE FROM workspace_data_sources
      WHERE workspace_id = ?
        AND id NOT IN (${placeholders})
    `).run(record3.id, ...retainedIds);
  }
  const retained = new Set(retainedIds);
  const retiredSourceKeys = previousIds.filter((id) => !retained.has(id)).map((id) => sourceKeyFromStoredRowId(record3.id, id));
  const latestProjectionMode = db.prepare(`
    SELECT rowid AS row_id, projection_mode
    FROM workspace_dataset_observations
    WHERE workspace_id = ? AND source_key = ?
    ORDER BY rowid DESC
    LIMIT 1
  `);
  const retire = db.prepare(`
    INSERT INTO workspace_dataset_source_retirements (
      workspace_id, source_key, projection_mode, retired_after_rowid, retired_at
    ) VALUES (
      @workspaceId, @sourceKey, @projectionMode, @retiredAfterRowId, @retiredAt
    )
    ON CONFLICT(workspace_id, source_key) DO UPDATE SET
      projection_mode = excluded.projection_mode,
      retired_after_rowid = excluded.retired_after_rowid,
      retired_at = excluded.retired_at
  `);
  const clearCurrent = db.prepare(`
    UPDATE workspace_dataset_observations
    SET is_current = 0
    WHERE workspace_id = ? AND source_key = ? AND is_current = 1
  `);
  for (const sourceKey of retiredSourceKeys) {
    const latest = latestProjectionMode.get(record3.id, sourceKey);
    retire.run({
      workspaceId: record3.id,
      sourceKey,
      projectionMode: latest?.projection_mode ?? "source",
      retiredAfterRowId: latest?.row_id ?? 0,
      retiredAt: now
    });
    clearCurrent.run(record3.id, sourceKey);
  }
  return retiredSourceKeys;
}
function replaceActions(db, record3, now) {
  db.prepare("DELETE FROM workspace_actions WHERE workspace_id = ?").run(record3.id);
  const insert = db.prepare(`
    INSERT INTO workspace_actions (
      id, workspace_id, runner, composio_slug, args_template_json, side_effect,
      approval_policy, created_at, updated_at
    ) VALUES (
      @id, @workspaceId, @runner, @composioSlug, @argsTemplateJson, @sideEffect,
      @approvalPolicy, @createdAt, @updatedAt
    )
  `);
  for (const [index, action] of record3.actions.entries()) {
    if (!action.runner && !action.composioSlug) continue;
    insert.run({
      id: actionRowId(record3.id, action, index),
      workspaceId: record3.id,
      runner: action.runner ?? null,
      composioSlug: action.composioSlug ?? null,
      argsTemplateJson: JSON.stringify(action.argsTemplate ?? {}),
      sideEffect: sideEffectForAction(action),
      approvalPolicy: "required",
      createdAt: record3.createdAt,
      updatedAt: now
    });
  }
}
function replaceFilesAndRevisions(db, record3, rootDir) {
  db.prepare("DELETE FROM workspace_revisions WHERE workspace_id = ?").run(record3.id);
  db.prepare("DELETE FROM workspace_files WHERE workspace_id = ?").run(record3.id);
  const files = collectWorkspaceFiles(record3, rootDir);
  const fileIdsByRel = /* @__PURE__ */ new Map();
  const insertFile = db.prepare(`
    INSERT INTO workspace_files (
      id, workspace_id, rel_path, kind, content_hash, bytes, version, created_at,
      updated_at
    ) VALUES (
      @id, @workspaceId, @relPath, @kind, @contentHash, @bytes, @version, @createdAt,
      @updatedAt
    )
  `);
  for (const file of files) {
    const id = fileRowId(record3.id, file.relPath);
    fileIdsByRel.set(file.relPath, id);
    insertFile.run({
      id,
      workspaceId: record3.id,
      relPath: file.relPath,
      kind: file.kind,
      contentHash: file.contentHash,
      bytes: file.bytes,
      version: file.relPath === record3.viewEntry ? record3.version : 1,
      createdAt: file.createdAt,
      updatedAt: file.updatedAt
    });
  }
  replaceRevisions(db, record3, rootDir, fileIdsByRel);
}
function replaceRevisions(db, record3, rootDir, fileIdsByRel) {
  const insert = db.prepare(`
    INSERT INTO workspace_revisions (
      id, workspace_id, file_id, version, snapshot_path, content_hash, bytes,
      author_session_id, created_at
    ) VALUES (
      @id, @workspaceId, @fileId, @version, @snapshotPath, @contentHash, @bytes,
      @authorSessionId, @createdAt
    )
  `);
  for (const revision of record3.revisions) {
    const snapshot = statWorkspaceFile(rootDir, revision.file);
    insert.run({
      id: revisionRowId(record3.id, revision),
      workspaceId: record3.id,
      fileId: fileIdsByRel.get(revision.file) ?? null,
      version: revision.version,
      snapshotPath: revision.file,
      contentHash: snapshot?.contentHash ?? hashString(`${record3.id}:${revision.file}:${revision.version}:${revision.ts}`),
      bytes: snapshot?.bytes ?? revision.bytes,
      authorSessionId: record3.originSessionId ?? null,
      createdAt: revision.ts
    });
  }
}
function appendWorkspaceStateEvent(db, workspaceId, input) {
  const seq = db.prepare(`
    SELECT COALESCE(MAX(seq), 0) + 1 AS seq
    FROM workspace_state_events
    WHERE workspace_id = ?
  `).get(workspaceId)?.seq ?? 1;
  db.prepare(`
    INSERT INTO workspace_state_events (
      id, workspace_id, seq, session_id, event_type, actor, payload_json, created_at
    ) VALUES (
      @id, @workspaceId, @seq, @sessionId, @eventType, @actor, @payloadJson, @createdAt
    )
  `).run({
    id: (0, import_node_crypto11.randomUUID)(),
    workspaceId,
    seq,
    sessionId: typeof input.payload.originSessionId === "string" ? input.payload.originSessionId : null,
    eventType: input.eventType,
    actor: input.actor,
    payloadJson: JSON.stringify(input.payload),
    createdAt: input.createdAt
  });
}
function collectWorkspaceFiles(record3, rootDir) {
  const rels = /* @__PURE__ */ new Set(["space.json", record3.viewEntry, "data.json", "notes.jsonl", "audit.jsonl"]);
  for (const source of record3.dataSources) if (source.runner) rels.add(import_node_path14.default.posix.join("data", source.runner));
  for (const action of record3.actions) if (action.runner) rels.add(import_node_path14.default.posix.join("data", action.runner));
  for (const revision of record3.revisions) rels.add(revision.file);
  for (const rel of walkWorkspaceFiles(rootDir, "view")) rels.add(rel);
  for (const rel of walkWorkspaceFiles(rootDir, "data")) rels.add(rel);
  for (const rel of walkWorkspaceFiles(rootDir, "view-history")) rels.add(rel);
  const out = [];
  for (const relPath of rels) {
    const stat = statWorkspaceFile(rootDir, relPath);
    if (!stat) continue;
    out.push({
      relPath,
      kind: classifyWorkspaceFile(record3, relPath),
      ...stat
    });
  }
  return out.sort((a, b) => a.relPath.localeCompare(b.relPath));
}
function walkWorkspaceFiles(rootDir, relDir) {
  const abs = import_node_path14.default.join(rootDir, relDir);
  if (!(0, import_node_fs6.existsSync)(abs)) return [];
  const out = [];
  const visit = (dir) => {
    let entries = [];
    try {
      entries = (0, import_node_fs6.readdirSync)(dir);
    } catch {
      return;
    }
    for (const entry of entries) {
      const full = import_node_path14.default.join(dir, entry);
      let st;
      try {
        st = (0, import_node_fs6.statSync)(full);
      } catch {
        continue;
      }
      if (st.isDirectory()) {
        visit(full);
      } else if (st.isFile()) {
        out.push(toPosix(import_node_path14.default.relative(rootDir, full)));
      }
    }
  };
  visit(abs);
  return out;
}
function statWorkspaceFile(rootDir, relPath) {
  const full = safeJoin(rootDir, relPath);
  if (!full || !(0, import_node_fs6.existsSync)(full)) return null;
  try {
    const st = (0, import_node_fs6.statSync)(full);
    if (!st.isFile()) return null;
    return {
      contentHash: hashBuffer((0, import_node_fs6.readFileSync)(full)),
      bytes: st.size,
      createdAt: st.birthtime.toISOString(),
      updatedAt: st.mtime.toISOString()
    };
  } catch {
    return null;
  }
}
function classifyWorkspaceFile(record3, relPath) {
  if (relPath === "space.json") return "manifest";
  if (relPath === record3.viewEntry) return "view";
  if (relPath === "data.json") return "data";
  if (relPath === "notes.jsonl") return "note";
  if (relPath === "audit.jsonl") return "audit";
  if (relPath.startsWith("view-history/")) return "snapshot";
  if (relPath.startsWith("view/")) return "asset";
  if (relPath.startsWith("data/")) return "runner";
  return "other";
}
function resolveWorkspaceRoot(slug) {
  if (!isValidWorkspaceSlug(slug)) throw new Error(`invalid workspace slug: ${slug}`);
  return import_node_path14.default.join(WORKSPACE_SPACES_DIR, slug);
}
function isValidWorkspaceSlug(slug) {
  return /^[a-z0-9][a-z0-9-]{0,61}[a-z0-9]$/.test(slug);
}
function sourceRowId(workspaceId, source, index) {
  return `${workspaceId}:source:${source.id || index}`;
}
function sourceKeyFromStoredRowId(workspaceId, rowId) {
  const prefix = `${workspaceId}:source:`;
  return rowId.startsWith(prefix) ? rowId.slice(prefix.length) : rowId;
}
function actionRowId(workspaceId, action, index) {
  return `${workspaceId}:action:${action.id || index}`;
}
function fileRowId(workspaceId, relPath) {
  return `${workspaceId}:file:${hashString(relPath).slice(0, 16)}`;
}
function revisionRowId(workspaceId, revision) {
  return `${workspaceId}:revision:${revision.version}:${hashString(`${revision.file}:${revision.ts}`).slice(0, 12)}`;
}
function sideEffectForAction(action) {
  const slug = `${action.composioSlug ?? ""} ${action.runner ?? ""}`.toLowerCase();
  return slug.includes("send") || slug.includes("email") || slug.includes("mail") ? "send" : "write";
}
function safeJoin(rootDir, relPath) {
  const target = import_node_path14.default.resolve(rootDir, relPath);
  const rel = import_node_path14.default.relative(rootDir, target);
  if (rel === "" || rel.startsWith("..") || import_node_path14.default.isAbsolute(rel)) return null;
  return target;
}
function toPosix(value) {
  return value.split(import_node_path14.default.sep).join("/");
}
function hashBuffer(value) {
  return (0, import_node_crypto11.createHash)("sha256").update(value).digest("hex");
}
function hashString(value) {
  return (0, import_node_crypto11.createHash)("sha256").update(value).digest("hex");
}
function ensureStateDir3() {
  if (!(0, import_node_fs6.existsSync)(WORKSPACE_STATE_DIR)) (0, import_node_fs6.mkdirSync)(WORKSPACE_STATE_DIR, { recursive: true });
}
var import_node_crypto11, import_node_fs6, import_node_path14, import_better_sqlite34, WORKSPACE_STATE_DIR, WORKSPACE_DB_PATH, WORKSPACE_SPACES_DIR, MAX_WORKSPACE_DATA_BYTES, MAX_OBSERVATION_ERROR_CHARS, MAX_PROVENANCE_INPUT_BYTES, MAX_PROVENANCE_BYTES2, PROVENANCE_ALLOWLIST, cachedDb2;
var init_workspace_db = __esm({
  "src/spaces/workspace-db.ts"() {
    "use strict";
    import_node_crypto11 = require("node:crypto");
    import_node_fs6 = require("node:fs");
    import_node_path14 = __toESM(require("node:path"), 1);
    import_better_sqlite34 = __toESM(require("better-sqlite3"), 1);
    init_config();
    init_operational_telemetry();
    init_security();
    init_workspace_db_schema();
    WORKSPACE_STATE_DIR = import_node_path14.default.join(BASE_DIR, "state");
    WORKSPACE_DB_PATH = import_node_path14.default.join(WORKSPACE_STATE_DIR, "workspaces.db");
    WORKSPACE_SPACES_DIR = import_node_path14.default.join(BASE_DIR, "spaces");
    MAX_WORKSPACE_DATA_BYTES = 5 * 1024 * 1024;
    MAX_OBSERVATION_ERROR_CHARS = 2e3;
    MAX_PROVENANCE_INPUT_BYTES = 64 * 1024;
    MAX_PROVENANCE_BYTES2 = 8 * 1024;
    PROVENANCE_ALLOWLIST = /* @__PURE__ */ new Set([
      "provider",
      "adapter",
      "toolSlug",
      "runner",
      "schedule",
      "accountRef",
      "connectionId",
      "requestId",
      "approvalId",
      "runId",
      "sessionId",
      "argsHash",
      "runnerHash",
      "trigger",
      "initiatedBy",
      "attempt",
      "sourceVersion",
      "fetchedAt",
      "approvalResolution",
      "approvalResolvedAt"
    ]);
    cachedDb2 = null;
  }
});

// src/spaces/store.ts
function isValidSpaceSlug(slug) {
  return typeof slug === "string" && SLUG_RE.test(slug);
}
function workspaceDeletionQuarantinePath(slug, deletionId) {
  if (!isValidSpaceSlug(slug)) throw new Error(`invalid workspace slug: ${slug}`);
  if (!DELETE_ID_RE.test(deletionId)) throw new Error("invalid workspace deletion id");
  return import_node_path15.default.join(WORKSPACE_DELETE_QUARANTINE_DIR, `${slug}--${deletionId}`);
}
function quarantinedWorkspaceSlug(entry) {
  const match = /^(.*)--([0-9a-f-]{36})$/i.exec(entry);
  if (!match || !isValidSpaceSlug(match[1]) || !DELETE_ID_RE.test(match[2])) return null;
  return match[1];
}
function recoverWorkspaceDeletionQuarantine(slug) {
  if (slug !== void 0 && !isValidSpaceSlug(slug)) {
    throw new Error(`invalid workspace slug: ${slug}`);
  }
  if (!(0, import_node_fs7.existsSync)(WORKSPACE_DELETE_QUARANTINE_DIR)) return 0;
  let entries;
  try {
    entries = (0, import_node_fs7.readdirSync)(WORKSPACE_DELETE_QUARANTINE_DIR);
  } catch (err) {
    throw new Error(`could not inspect Workspace deletion quarantine: ${err.message}`);
  }
  let recovered = 0;
  for (const entry of entries.sort()) {
    const quarantinedSlug = quarantinedWorkspaceSlug(entry);
    if (!quarantinedSlug || slug !== void 0 && quarantinedSlug !== slug) continue;
    const quarantinedDir = import_node_path15.default.join(WORKSPACE_DELETE_QUARANTINE_DIR, entry);
    let isDirectory;
    try {
      isDirectory = (0, import_node_fs7.statSync)(quarantinedDir).isDirectory();
    } catch (err) {
      throw new Error(
        `could not inspect quarantined Workspace "${quarantinedSlug}": ${err.message}`
      );
    }
    if (!isDirectory) {
      throw new Error(`invalid deletion quarantine marker for Workspace "${quarantinedSlug}"`);
    }
    deleteWorkspaceIndex(quarantinedSlug, {
      actor: "space-store-delete-recovery"
    });
    purgeWorkspaceObservationMemory(quarantinedSlug);
    (0, import_node_fs7.rmSync)(quarantinedDir, { recursive: true, force: true });
    recovered += 1;
  }
  return recovered;
}
function runnerFilenameError(runner) {
  const name = runner.trim();
  if (!name) return "runner filename is empty";
  if (name === "." || name === ".." || name.includes("/") || name.includes("\\") || name.includes("\0")) {
    return `runner must be a filename under data/ (for example "refresh.mjs"), not a path: ${runner}`;
  }
  return null;
}
function cliArgvError(argv) {
  if (!Array.isArray(argv) || argv.length === 0) {
    return 'cli_argv must be a non-empty array of strings (e.g. ["sf", "data", "query", "-q", "...", "-r", "json"])';
  }
  if (argv.length > CLI_ARGV_MAX_ITEMS) {
    return `cli_argv has ${argv.length} items; the maximum is ${CLI_ARGV_MAX_ITEMS}`;
  }
  let chars = 0;
  for (const [i, item] of argv.entries()) {
    if (typeof item !== "string" || item.length === 0) {
      return `cli_argv[${i}] must be a non-empty string`;
    }
    if (item.includes("\0") || WORKSPACE_IDENTITY_CONTROL_RE.test(item)) {
      return `cli_argv[${i}] contains control characters`;
    }
    chars += item.length;
  }
  if (chars > CLI_ARGV_MAX_CHARS) {
    return `cli_argv totals ${chars} characters; the maximum is ${CLI_ARGV_MAX_CHARS}`;
  }
  const command = argv[0];
  if (command === "." || command === ".." || command.includes("/") || command.includes("\\") || command.startsWith("-")) {
    return `cli_argv[0] must be a bare installed-command name resolved on PATH (for example "sf"), not a path or flag: ${command}`;
  }
  return null;
}
function workspaceIdentityDisplay(value, fallback) {
  const printable = value.replace(/[\u0000-\u001f\u007f-\u009f]/g, "\uFFFD");
  if (!printable) return fallback;
  return `${printable.slice(0, 80)}${printable.length > 80 ? "\u2026" : ""}`;
}
function workspaceIdentityErrors(dataSources, actions) {
  const errors = [];
  const validate = (items, kind, duplicateKind) => {
    const firstIndexById = /* @__PURE__ */ new Map();
    for (const [index, item] of items.entries()) {
      const raw = typeof item?.id === "string" ? item.id : "";
      const canonical = raw.trim();
      const fallback = `#${index + 1}`;
      const label = workspaceIdentityDisplay(raw, fallback);
      const canonicalLabel = workspaceIdentityDisplay(canonical, fallback);
      if (!canonical) {
        errors.push(`${kind} "${label}" id must contain a non-whitespace value.`);
        continue;
      }
      if (raw !== canonical) {
        errors.push(
          `${kind} "${label}" id must not have leading or trailing whitespace (use "${canonicalLabel}").`
        );
      }
      if (canonical.length > WORKSPACE_IDENTITY_MAX_CHARS) {
        errors.push(
          `${kind} "${label}" id exceeds the ${WORKSPACE_IDENTITY_MAX_CHARS} character limit.`
        );
      }
      if (WORKSPACE_IDENTITY_CONTROL_RE.test(canonical)) {
        errors.push(`${kind} "${label}" id cannot contain control characters.`);
      }
      if (kind === "Data source" && canonical === "_meta") {
        errors.push(`${kind} "${label}" uses reserved id "_meta".`);
      }
      const firstIndex = firstIndexById.get(canonical);
      if (firstIndex !== void 0) {
        errors.push(
          `Duplicate ${duplicateKind} id "${canonicalLabel}" (entries ${firstIndex + 1} and ${index + 1}).`
        );
      } else {
        firstIndexById.set(canonical, index);
      }
    }
  };
  validate(dataSources ?? [], "Data source", "data source");
  validate(actions ?? [], "Action", "action");
  return errors;
}
function ensureDir(dir) {
  if (!(0, import_node_fs7.existsSync)(dir)) (0, import_node_fs7.mkdirSync)(dir, { recursive: true });
}
function atomicWrite(file, content) {
  ensureDir(import_node_path15.default.dirname(file));
  const tmp = `${file}.${process.pid}.${(0, import_node_crypto12.randomUUID)().slice(0, 8)}.tmp`;
  (0, import_node_fs7.writeFileSync)(tmp, content, "utf-8");
  (0, import_node_fs7.renameSync)(tmp, file);
}
function resolveSpaceDir(slug) {
  if (!isValidSpaceSlug(slug)) throw new Error(`invalid space slug: ${slug}`);
  const dir = import_node_path15.default.resolve(SPACES_DIR, slug);
  if (import_node_path15.default.dirname(dir) !== import_node_path15.default.resolve(SPACES_DIR)) {
    throw new Error(`space dir escaped SPACES_DIR: ${slug}`);
  }
  return dir;
}
function resolveInSpace(slug, rel) {
  const base = resolveSpaceDir(slug);
  const target = import_node_path15.default.resolve(base, rel);
  const relative = import_node_path15.default.relative(base, target);
  if (relative === "" || relative.startsWith("..") || import_node_path15.default.isAbsolute(relative)) {
    throw new Error(`path escaped space dir: ${slug} / ${rel}`);
  }
  return target;
}
function manifestPath(slug) {
  return import_node_path15.default.join(resolveSpaceDir(slug), "space.json");
}
function asStr(v) {
  return typeof v === "string" && v ? v : void 0;
}
function asObj(v) {
  return v && typeof v === "object" && !Array.isArray(v) ? v : void 0;
}
function contractText(v, maxChars) {
  if (typeof v !== "string") return void 0;
  const text = v.replace(/\s+/g, " ").trim().slice(0, maxChars);
  return text || void 0;
}
function contractList(v) {
  if (!Array.isArray(v)) return void 0;
  const out = [];
  const seen = /* @__PURE__ */ new Set();
  for (const raw of v) {
    const text = contractText(raw, CONTRACT_ITEM_MAX_CHARS);
    if (!text) continue;
    const key = text.toLowerCase();
    if (seen.has(key)) continue;
    seen.add(key);
    out.push(text);
    if (out.length >= CONTRACT_LIST_MAX_ITEMS) break;
  }
  return out;
}
function mergeSpaceContract(current, patch) {
  const requestedObjective = patch.objective == null ? void 0 : contractText(patch.objective, CONTRACT_OBJECTIVE_MAX_CHARS);
  const objective = requestedObjective || current?.objective;
  if (!objective) return current;
  const successCriteria = patch.successCriteria == null ? [...current?.successCriteria ?? []] : contractList(patch.successCriteria) ?? [...current?.successCriteria ?? []];
  const invariants = patch.invariants == null ? [...current?.invariants ?? []] : contractList(patch.invariants) ?? [...current?.invariants ?? []];
  return { objective, successCriteria, invariants };
}
function normalizeContract(m) {
  const nested = asObj(m.contract);
  return mergeSpaceContract(void 0, {
    objective: nested?.objective ?? m.objective,
    successCriteria: nested?.successCriteria ?? nested?.success_criteria ?? m.successCriteria ?? m.success_criteria,
    invariants: nested?.invariants ?? nested?.guardrails ?? m.invariants ?? m.guardrails
  });
}
function parseJsonObjField(v, label, manifestErrors) {
  if (asObj(v)) return v;
  if (typeof v === "string" && v.trim()) {
    try {
      const p = JSON.parse(v);
      const obj = asObj(p);
      if (obj) return obj;
      manifestErrors.push(`${label} must be a JSON object, not ${Array.isArray(p) ? "an array" : typeof p}.`);
    } catch (err) {
      manifestErrors.push(`${label} is not valid JSON: ${err instanceof Error ? err.message : String(err)}.`);
    }
  } else if (v != null) {
    manifestErrors.push(`${label} must be a JSON object string, not ${Array.isArray(v) ? "an array" : typeof v}.`);
  }
  return void 0;
}
function normDataSource(raw, manifestErrors, index) {
  const d = asObj(raw) ?? {};
  const ds = { id: asStr(d.id) ?? "" };
  const label = `Data source "${ds.id || `#${index + 1}`}"`;
  const runner = asStr(d.runner);
  if (runner) {
    ds.runner = runner;
    const err = runnerFilenameError(runner);
    if (err) manifestErrors.push(`${label} ${err}.`);
  }
  const cliArgvRaw = d.cliArgv ?? d.cli_argv;
  if (cliArgvRaw != null) {
    const err = cliArgvError(cliArgvRaw);
    if (err) manifestErrors.push(`${label} ${err}.`);
    else ds.cliArgv = cliArgvRaw.slice();
    if (runner) manifestErrors.push(`${label} declares both a runner and cli_argv; declare exactly one execution mode.`);
  }
  const cs = asStr(d.composioSlug) ?? asStr(d.composio_slug);
  if (cs) ds.composioSlug = cs;
  if (cs && ds.cliArgv) manifestErrors.push(`${label} declares both cli_argv and a composio_slug; declare exactly one execution mode.`);
  if (d.composioArgs != null) {
    const ca = parseJsonObjField(d.composioArgs, `${label} composioArgs`, manifestErrors);
    if (ca) ds.composioArgs = ca;
  } else {
    const ca = parseJsonObjField(d.composio_args_json, `${label} composio_args_json`, manifestErrors);
    if (ca) ds.composioArgs = ca;
  }
  const sched = asStr(d.schedule);
  if (sched) ds.schedule = sched;
  const tz = asStr(d.timezone);
  if (tz) ds.timezone = tz;
  if (d.allowEmpty === true || d.allow_empty === true) ds.allowEmpty = true;
  return ds;
}
function normAction(raw, manifestErrors, index) {
  const a = asObj(raw) ?? {};
  const act = { id: asStr(a.id) ?? "" };
  const label = asStr(a.label);
  if (label) act.label = label;
  const cs = asStr(a.composioSlug) ?? asStr(a.composio_slug);
  if (cs) act.composioSlug = cs;
  const diagnosticLabel = `Action "${act.id || `#${index + 1}`}"`;
  const runner = asStr(a.runner);
  if (runner) {
    act.runner = runner;
    const err = runnerFilenameError(runner);
    if (err) manifestErrors.push(`${diagnosticLabel} ${err}.`);
  }
  if (a.argsTemplate != null) {
    const tpl = parseJsonObjField(a.argsTemplate, `${diagnosticLabel} argsTemplate`, manifestErrors);
    if (tpl) act.argsTemplate = tpl;
  } else {
    const tpl = parseJsonObjField(a.args_template_json, `${diagnosticLabel} args_template_json`, manifestErrors);
    if (tpl) act.argsTemplate = tpl;
  }
  if (a.confirm === true) act.confirm = true;
  return act;
}
function normalizeManifest(raw, slug, fallbackTime) {
  const m = asObj(raw);
  if (!m || m.id !== slug) return void 0;
  const manifestErrors = [];
  let reengage = asObj(m.reengage);
  if (!reengage && (Array.isArray(m.reengageTriggers) || m.reengageGuidance)) {
    reengage = {
      triggers: Array.isArray(m.reengageTriggers) ? m.reengageTriggers : [],
      guidance: asStr(m.reengageGuidance)
    };
  }
  const status = m.status === "paused" || m.status === "archived" ? m.status : "active";
  const rec = {
    id: slug,
    title: asStr(m.title) ?? slug,
    status,
    contract: normalizeContract(m),
    viewEntry: asStr(m.viewEntry) ?? "view/index.html",
    dataSources: Array.isArray(m.dataSources) ? m.dataSources.map((src, index) => normDataSource(src, manifestErrors, index)) : [],
    actions: Array.isArray(m.actions) ? m.actions.map((act, index) => normAction(act, manifestErrors, index)) : [],
    reengage,
    originSessionId: asStr(m.originSessionId),
    focusId: typeof m.focusId === "number" ? m.focusId : null,
    version: typeof m.version === "number" ? m.version : 1,
    revisions: Array.isArray(m.revisions) ? m.revisions : [],
    createdAt: asStr(m.createdAt) ?? fallbackTime,
    updatedAt: asStr(m.updatedAt) ?? fallbackTime,
    lastOpenedAt: asStr(m.lastOpenedAt),
    lastRefreshedAt: asStr(m.lastRefreshedAt),
    recipe: asStr(m.recipe),
    mobile: m.mobile && typeof m.mobile === "object" && !Array.isArray(m.mobile) && typeof m.mobile.show === "boolean" ? { show: m.mobile.show } : void 0,
    contentMode: m.contentMode === "static_snapshot" ? "static_snapshot" : void 0
  };
  manifestErrors.push(...workspaceIdentityErrors(rec.dataSources, rec.actions));
  if (manifestErrors.length > 0) rec.manifestErrors = manifestErrors;
  return rec;
}
function missingManifestFixes(errors, hasDataSources, hasActions) {
  const missing = [];
  if (!errors || errors.length === 0) return missing;
  if (errors.some((e) => /^Data source /.test(e)) && !hasDataSources) missing.push("dataSources");
  if (errors.some((e) => /^Action /.test(e)) && !hasActions) missing.push("actions");
  return missing;
}
function declaredRunnerErrors(dataSources, actions) {
  const errors = [];
  for (const [index, src] of (dataSources ?? []).entries()) {
    const label = `Data source "${src.id || `#${index + 1}`}"`;
    if (src.cliArgv !== void 0) {
      const cliErr = cliArgvError(src.cliArgv);
      if (cliErr) errors.push(`${label} ${cliErr}.`);
      if (src.runner !== void 0) errors.push(`${label} declares both a runner and cli_argv; declare exactly one execution mode.`);
      if (src.composioSlug?.trim()) errors.push(`${label} declares both cli_argv and a composio_slug; declare exactly one execution mode.`);
    }
    if (src.runner === void 0) continue;
    const err = runnerFilenameError(src.runner);
    if (err) errors.push(`${label} ${err}.`);
  }
  for (const [index, action] of (actions ?? []).entries()) {
    if (action.runner === void 0) continue;
    const err = runnerFilenameError(action.runner);
    if (err) errors.push(`Action "${action.id || `#${index + 1}`}" ${err}.`);
  }
  return errors;
}
function assertValidDeclaredRunners(dataSources, actions) {
  const errors = declaredRunnerErrors(dataSources, actions);
  if (errors.length > 0) {
    throw new Error(`invalid workspace runner declarations:
- ${errors.join("\n- ")}`);
  }
}
function assertValidWorkspaceIdentities(dataSources, actions) {
  const errors = workspaceIdentityErrors(dataSources, actions);
  if (errors.length > 0) {
    throw new Error(`invalid workspace identities:
- ${errors.join("\n- ")}`);
  }
}
function persistableRecord(record3) {
  const { manifestErrors: _manifestErrors, ...persisted } = record3;
  return persisted;
}
function readManifest(slug) {
  try {
    const file = manifestPath(slug);
    const raw = JSON.parse((0, import_node_fs7.readFileSync)(file, "utf-8"));
    let fallbackTime;
    try {
      fallbackTime = (0, import_node_fs7.statSync)(file).mtime.toISOString();
    } catch {
      fallbackTime = (/* @__PURE__ */ new Date(0)).toISOString();
    }
    return normalizeManifest(raw, slug, fallbackTime);
  } catch {
    return void 0;
  }
}
function declaredRunnerHealth(record3) {
  const out = [];
  for (const src of record3.dataSources) {
    if (!src.runner) continue;
    const invalid = runnerFilenameError(src.runner) ?? void 0;
    let present = false;
    if (!invalid) {
      try {
        present = (0, import_node_fs7.existsSync)(resolveInSpace(record3.id, import_node_path15.default.join("data", src.runner)));
      } catch {
        present = false;
      }
    }
    out.push({ kind: "dataSource", id: src.id, runner: src.runner, present, ...invalid ? { invalid } : {} });
  }
  for (const action of record3.actions) {
    if (!action.runner) continue;
    const invalid = runnerFilenameError(action.runner) ?? void 0;
    let present = false;
    if (!invalid) {
      try {
        present = (0, import_node_fs7.existsSync)(resolveInSpace(record3.id, import_node_path15.default.join("data", action.runner)));
      } catch {
        present = false;
      }
    }
    out.push({ kind: "action", id: action.id, runner: action.runner, present, ...invalid ? { invalid } : {} });
  }
  return out;
}
function readDataMeta(record3) {
  try {
    const raw = JSON.parse((0, import_node_fs7.readFileSync)(resolveInSpace(record3.id, "data.json"), "utf-8"));
    if (!raw || typeof raw !== "object" || Array.isArray(raw)) return {};
    const meta = raw._meta;
    if (!meta || typeof meta !== "object" || Array.isArray(meta)) return {};
    return meta;
  } catch {
    return {};
  }
}
function sourceFailureFromMeta(meta, sourceId) {
  const entry = meta[sourceId];
  if (!entry || typeof entry !== "object" || Array.isArray(entry)) return null;
  const rec = entry;
  if (rec.ok !== false) return null;
  return {
    error: typeof rec.error === "string" && rec.error.trim() ? rec.error.trim() : void 0,
    refreshedAt: typeof rec.refreshedAt === "string" && rec.refreshedAt.trim() ? rec.refreshedAt.trim() : void 0
  };
}
function sourcePendingApprovalFromMeta(meta, sourceId) {
  const entry = meta[sourceId];
  if (!entry || typeof entry !== "object" || Array.isArray(entry)) return null;
  const rec = entry;
  if (rec.ok !== null || rec.status !== "awaiting_approval") return null;
  return {
    approvalId: typeof rec.approvalId === "string" && rec.approvalId.trim() ? rec.approvalId.trim() : void 0,
    refreshedAt: typeof rec.refreshedAt === "string" && rec.refreshedAt.trim() ? rec.refreshedAt.trim() : void 0
  };
}
function buildSpaceHealthSnapshot(record3, opts = {}) {
  const now = opts.now ?? Date.now();
  const staleAfterMs = opts.staleAfterMs ?? DEFAULT_STALE_AFTER_MS;
  const issues = [...record3.manifestErrors ?? []];
  let view = { entry: record3.viewEntry, exists: false, bytes: 0 };
  try {
    const viewFile = resolveInSpace(record3.id, record3.viewEntry);
    const st = (0, import_node_fs7.statSync)(viewFile);
    if (st.isFile()) {
      view = {
        entry: record3.viewEntry,
        exists: true,
        bytes: st.size,
        mtime: st.mtime.toISOString()
      };
    }
  } catch {
    view = { entry: record3.viewEntry, exists: false, bytes: 0 };
  }
  if (!view.exists) issues.push(`view entry "${record3.viewEntry}" is missing`);
  const runners = declaredRunnerHealth(record3);
  for (const runner of runners) {
    const label = runner.kind === "dataSource" ? "data source" : "action";
    if (runner.invalid) {
      issues.push(`${label} "${runner.id}" runner "${runner.runner}" is invalid: ${runner.invalid}`);
    } else if (!runner.present) {
      issues.push(`${label} "${runner.id}" declares data/${runner.runner}, but the file is missing`);
    }
  }
  const dataMeta = readDataMeta(record3);
  for (const source of record3.dataSources) {
    const pending = sourcePendingApprovalFromMeta(dataMeta, source.id);
    if (pending) {
      const when2 = pending.refreshedAt ? ` since ${pending.refreshedAt}` : "";
      const approval = pending.approvalId ? ` (${pending.approvalId})` : "";
      issues.push(`data source "${source.id}" is awaiting pinned-entrypoint approval${when2}${approval}`);
      continue;
    }
    const failure = sourceFailureFromMeta(dataMeta, source.id);
    if (!failure) continue;
    const when = failure.refreshedAt ? ` at ${failure.refreshedAt}` : "";
    const why = failure.error ? `: ${failure.error}` : "";
    issues.push(`data source "${source.id}" last refresh failed${when}${why}`);
  }
  let freshness;
  if (record3.dataSources.length === 0) {
    freshness = { state: "no_sources", staleAfterMs };
  } else if (!record3.lastRefreshedAt) {
    freshness = { state: "never_refreshed", staleAfterMs };
    issues.push("data sources have never refreshed");
  } else {
    const parsed = Date.parse(record3.lastRefreshedAt);
    const ageMs = Number.isFinite(parsed) ? Math.max(0, now - parsed) : void 0;
    const state = ageMs !== void 0 && ageMs <= staleAfterMs ? "fresh" : "stale";
    freshness = { state, lastRefreshedAt: record3.lastRefreshedAt, ...ageMs !== void 0 ? { ageMs } : {}, staleAfterMs };
    if (state === "stale") issues.push(`last data refresh is older than ${Math.round(staleAfterMs / 36e5)} hours`);
  }
  return {
    id: record3.id,
    title: record3.title,
    status: record3.status,
    version: record3.version,
    generatedAt: new Date(now).toISOString(),
    counts: {
      dataSources: record3.dataSources.length,
      actions: record3.actions.length,
      revisions: record3.revisions.length,
      runners: runners.length
    },
    view,
    runners,
    freshness,
    issues
  };
}
function workspaceMutationLockPath(slug) {
  return import_node_path15.default.join(SPACES_DIR, `.workspace-${slug}`);
}
function sameStoredJson(left, right) {
  try {
    return JSON.stringify(left) === JSON.stringify(right);
  } catch {
    return false;
  }
}
function repairStaticSnapshotCommitReceiptUnlocked(input) {
  const existing = readManifest(input.id);
  if (!existing || input.initialData === void 0 || input.viewContent === void 0 || existing.status !== "active" || existing.contentMode !== "static_snapshot" || existing.viewEntry !== (input.viewEntry ?? "view/index.html") || existing.version !== 1 || existing.revisions.length !== 0 || existing.title !== (input.title.trim().slice(0, 200) || input.id) || existing.dataSources.length !== 0 || !sameStoredJson(existing.actions, input.actions ?? []) || !sameStoredJson(existing.contract, input.contract) || !sameStoredJson(existing.reengage, input.reengage) || existing.originSessionId !== input.originSessionId) {
    return { ok: false, reason: "the visible Workspace no longer matches the exact static create contract" };
  }
  let dataText;
  try {
    if (!input.initialData || typeof input.initialData !== "object" || Array.isArray(input.initialData)) {
      return { ok: false, reason: "the requested static dataset is not one JSON object" };
    }
    dataText = JSON.stringify(input.initialData);
  } catch {
    return { ok: false, reason: "the requested static dataset is not canonically serializable" };
  }
  const manifestFile = manifestPath(input.id);
  const viewFile = resolveInSpace(input.id, existing.viewEntry);
  const dataFile = resolveInSpace(input.id, "data.json");
  const receiptFile = resolveInSpace(input.id, HOST_LOCAL_WORKSPACE_COMMIT_BASENAME);
  const manifestText = JSON.stringify(persistableRecord(existing), null, 2);
  try {
    if ((0, import_node_fs7.readFileSync)(manifestFile, "utf8") !== manifestText || (0, import_node_fs7.readFileSync)(viewFile, "utf8") !== input.viewContent || (0, import_node_fs7.readFileSync)(dataFile, "utf8") !== dataText) {
      return { ok: false, reason: "the Workspace manifest, view, or data bytes changed" };
    }
    if ((0, import_node_fs7.existsSync)(receiptFile)) {
      const current = withHostLocalWriteCommitFromFile({
        createdId: input.id,
        committedPath: receiptFile,
        result: "Workspace compound commit generation check."
      });
      if (hostLocalWriteCommitResultIsProven(current)) {
        return { ok: true, record: existing, receiptPath: receiptFile, repaired: false };
      }
    }
    writeHostLocalWorkspaceCommitDocument({
      createdId: input.id,
      receiptPath: receiptFile,
      manifest: { path: manifestFile, bytes: manifestText },
      view: { path: viewFile, bytes: input.viewContent },
      data: { path: dataFile, bytes: dataText }
    });
    const repaired = withHostLocalWriteCommitFromFile({
      createdId: input.id,
      committedPath: receiptFile,
      result: "Workspace compound commit generation check."
    });
    if (!hostLocalWriteCommitResultIsProven(repaired)) {
      (0, import_node_fs7.rmSync)(receiptFile, { force: true });
      return { ok: false, reason: "the reconstructed compound receipt did not re-prove the exact generation" };
    }
    return { ok: true, record: existing, receiptPath: receiptFile, repaired: true };
  } catch (error) {
    try {
      (0, import_node_fs7.rmSync)(`${receiptFile}.${process.pid}.tmp`, { force: true });
    } catch {
    }
    return {
      ok: false,
      reason: error instanceof Error ? error.message : "the compound receipt could not be reconstructed"
    };
  }
}
function saveSpaceUnlocked(input) {
  recoverWorkspaceDeletionQuarantine(input.id);
  const dir = resolveSpaceDir(input.id);
  const now = (/* @__PURE__ */ new Date()).toISOString();
  const existing = readManifest(input.id);
  if (existing && input.initialData !== void 0) {
    throw new Error("initialData is create-only; update an existing Workspace through its data source or space_set_data");
  }
  const missingFixes = missingManifestFixes(
    existing?.manifestErrors,
    input.dataSources !== void 0,
    input.actions !== void 0
  );
  if (missingFixes.length > 0) {
    throw new Error(`existing space manifest has invalid fields; pass corrected ${missingFixes.join(" and ")} before saving`);
  }
  const dataSources = input.dataSources ?? existing?.dataSources ?? [];
  const actions = input.actions ?? existing?.actions ?? [];
  if (input.initialData !== void 0 && dataSources.length > 0) {
    throw new Error("initialData is for a static snapshot and cannot be combined with data sources");
  }
  if (input.initialData !== void 0 && input.viewContent === void 0) {
    throw new Error("initialData requires viewContent so view and phone data share one create commit");
  }
  let initialDataText = null;
  if (input.initialData !== void 0) {
    if (!input.initialData || typeof input.initialData !== "object" || Array.isArray(input.initialData)) {
      throw new Error("initialData must be a JSON object");
    }
    try {
      initialDataText = JSON.stringify(input.initialData);
    } catch (error) {
      throw new Error(`initialData is not JSON-serializable: ${error.message}`);
    }
    const initialBytes = Buffer.byteLength(initialDataText, "utf8");
    if (initialBytes > SPACE_INITIAL_DATA_MAX_BYTES) {
      throw new Error(`initialData exceeds ${SPACE_INITIAL_DATA_MAX_BYTES} byte cap (${initialBytes} bytes)`);
    }
  }
  assertValidWorkspaceIdentities(dataSources, actions);
  assertValidDeclaredRunners(input.dataSources, input.actions);
  const viewEntry = input.viewEntry ?? existing?.viewEntry ?? "view/index.html";
  let version = existing?.version ?? 1;
  let revisions = existing?.revisions ?? [];
  if (input.viewContent !== void 0) {
    const viewFile = resolveInSpace(input.id, viewEntry);
    const previousView = (0, import_node_fs7.existsSync)(viewFile) ? (0, import_node_fs7.readFileSync)(viewFile, "utf-8") : null;
    if (previousView !== input.viewContent) {
      if (previousView !== null && existing) {
        const stamp = now.replace(/[:.]/g, "-");
        const snapshotRel = import_node_path15.default.posix.join("view-history", `${stamp}-v${existing.version}.html`);
        atomicWrite(resolveInSpace(input.id, snapshotRel), previousView);
        revisions = [...existing.revisions, {
          version: existing.version,
          ts: now,
          bytes: Buffer.byteLength(previousView, "utf-8"),
          file: snapshotRel
        }].slice(-50);
        version = existing.version + 1;
      }
      atomicWrite(viewFile, input.viewContent);
    }
  }
  if (initialDataText !== null) {
    atomicWrite(resolveInSpace(input.id, "data.json"), initialDataText);
  }
  const record3 = {
    id: input.id,
    title: input.title.trim().slice(0, 200) || input.id,
    status: input.status ?? existing?.status ?? "active",
    contract: input.contract ?? existing?.contract,
    viewEntry,
    dataSources,
    actions,
    reengage: input.reengage ?? existing?.reengage,
    originSessionId: input.originSessionId ?? existing?.originSessionId,
    focusId: input.focusId ?? existing?.focusId ?? null,
    version,
    revisions,
    createdAt: existing?.createdAt ?? now,
    updatedAt: now,
    lastOpenedAt: existing?.lastOpenedAt,
    lastRefreshedAt: existing?.lastRefreshedAt,
    recipe: input.recipe ?? existing?.recipe,
    mobile: input.mobile ?? existing?.mobile,
    // Installing a real source turns a former snapshot into a dynamic
    // Workspace automatically; source removal never invents snapshot status.
    contentMode: dataSources.length > 0 ? void 0 : input.initialData !== void 0 ? "static_snapshot" : existing?.contentMode
  };
  ensureDir(dir);
  const manifestFile = manifestPath(input.id);
  const manifestText = JSON.stringify(persistableRecord(record3), null, 2);
  if (initialDataText !== null) {
    const viewFile = resolveInSpace(input.id, viewEntry);
    const dataFile = resolveInSpace(input.id, "data.json");
    const receiptFile = resolveInSpace(input.id, HOST_LOCAL_WORKSPACE_COMMIT_BASENAME);
    writeHostLocalWorkspaceCommitDocument({
      createdId: input.id,
      receiptPath: receiptFile,
      manifest: { path: manifestFile, bytes: manifestText },
      view: { path: viewFile, bytes: input.viewContent },
      data: { path: dataFile, bytes: initialDataText }
    });
    input.beforeInitialManifestVisibility?.();
    if ((0, import_node_fs7.readFileSync)(viewFile, "utf8") !== input.viewContent || (0, import_node_fs7.readFileSync)(dataFile, "utf8") !== initialDataText) {
      throw new Error("initial Workspace components changed before the manifest visibility barrier");
    }
  }
  atomicWrite(manifestFile, manifestText);
  if (initialDataText !== null) {
    const receiptFile = resolveInSpace(input.id, HOST_LOCAL_WORKSPACE_COMMIT_BASENAME);
    const proof = withHostLocalWriteCommitFromFile({
      createdId: input.id,
      committedPath: receiptFile,
      result: "Workspace compound commit generation check."
    });
    if (!hostLocalWriteCommitResultIsProven(proof)) {
      try {
        (0, import_node_fs7.rmSync)(manifestFile, { force: true });
      } catch {
      }
      throw new Error("initial Workspace compound commit failed generation verification");
    }
  }
  indexWorkspaceRecord(record3, {
    eventType: existing ? "workspace_file_changed" : "workspace_created",
    actor: "space-store",
    payload: { mutation: existing ? "save:update" : "save:create" }
  });
  return record3;
}
function commitSpaceViewRevisionUnlocked(slug, nextView) {
  const existing = readManifest(slug);
  if (!existing) throw new Error(`No workspace named "${slug}".`);
  if (existing.manifestErrors && existing.manifestErrors.length > 0) {
    throw new Error(`Workspace "${slug}" has an invalid manifest.`);
  }
  const viewFile = resolveInSpace(slug, existing.viewEntry);
  if (!(0, import_node_fs7.existsSync)(viewFile)) throw new Error(`Workspace "${slug}" has no view.`);
  const previousView = (0, import_node_fs7.readFileSync)(viewFile, "utf-8");
  const previousManifest = (0, import_node_fs7.readFileSync)(manifestPath(slug), "utf-8");
  const now = /* @__PURE__ */ new Date();
  const timestamp = now.toISOString();
  const stamp = timestamp.replace(/[:.]/g, "-");
  const snapshotRel = import_node_path15.default.posix.join("view-history", `${stamp}-v${existing.version}.html`);
  const snapshotFile = resolveInSpace(slug, snapshotRel);
  const revision = {
    version: existing.version,
    ts: timestamp,
    bytes: Buffer.byteLength(previousView, "utf-8"),
    file: snapshotRel
  };
  const updated = {
    ...existing,
    version: existing.version + 1,
    revisions: [...existing.revisions, revision].slice(-50),
    updatedAt: timestamp
  };
  delete updated.manifestErrors;
  atomicWrite(snapshotFile, previousView);
  try {
    atomicWrite(viewFile, nextView);
    atomicWrite(manifestPath(slug), JSON.stringify(persistableRecord(updated), null, 2));
    indexWorkspaceRecord(updated, {
      eventType: "workspace_file_changed",
      actor: "space-store",
      payload: { mutation: "viewRevision" },
      strict: true
    });
    return updated;
  } catch (error) {
    try {
      atomicWrite(viewFile, previousView);
    } catch {
    }
    try {
      atomicWrite(manifestPath(slug), previousManifest);
    } catch {
    }
    try {
      (0, import_node_fs7.rmSync)(snapshotFile, { force: true });
    } catch {
    }
    try {
      indexWorkspaceRecord(existing, {
        eventType: "workspace_file_changed",
        actor: "space-store-rollback",
        payload: { mutation: "viewRevision:rollback" },
        strict: true
      });
    } catch {
    }
    throw error;
  }
}
var import_node_crypto12, import_node_fs7, import_node_path15, SPACES_DIR, WORKSPACE_DELETE_QUARANTINE_DIR, SLUG_RE, DELETE_ID_RE, CLI_ARGV_MAX_ITEMS, CLI_ARGV_MAX_CHARS, WORKSPACE_IDENTITY_MAX_CHARS, WORKSPACE_IDENTITY_CONTROL_RE, SPACE_INITIAL_DATA_MAX_BYTES, DEFAULT_STALE_AFTER_MS, CONTRACT_OBJECTIVE_MAX_CHARS, CONTRACT_LIST_MAX_ITEMS, CONTRACT_ITEM_MAX_CHARS, SpaceStore, spaceStore;
var init_store = __esm({
  "src/spaces/store.ts"() {
    "use strict";
    import_node_crypto12 = require("node:crypto");
    import_node_fs7 = require("node:fs");
    import_node_path15 = __toESM(require("node:path"), 1);
    init_config();
    init_workspace_observation_bridge();
    init_atomic_json();
    init_host_local_write_commit();
    init_workspace_db();
    SPACES_DIR = import_node_path15.default.join(BASE_DIR, "spaces");
    WORKSPACE_DELETE_QUARANTINE_DIR = import_node_path15.default.join(
      SPACES_DIR,
      ".delete-quarantine"
    );
    SLUG_RE = /^[a-z0-9][a-z0-9-]{0,61}[a-z0-9]$/;
    DELETE_ID_RE = /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
    CLI_ARGV_MAX_ITEMS = 64;
    CLI_ARGV_MAX_CHARS = 4e3;
    WORKSPACE_IDENTITY_MAX_CHARS = 120;
    WORKSPACE_IDENTITY_CONTROL_RE = /[\u0000-\u001f\u007f-\u009f]/;
    SPACE_INITIAL_DATA_MAX_BYTES = 256 * 1024;
    DEFAULT_STALE_AFTER_MS = 36 * 60 * 60 * 1e3;
    CONTRACT_OBJECTIVE_MAX_CHARS = 1200;
    CONTRACT_LIST_MAX_ITEMS = 12;
    CONTRACT_ITEM_MAX_CHARS = 500;
    SpaceStore = class {
      /** List every Space by scanning each spaces/<slug>/space.json (newest first). */
      list(includeArchived = false) {
        try {
          recoverWorkspaceDeletionQuarantine();
        } catch {
        }
        if (!(0, import_node_fs7.existsSync)(SPACES_DIR)) return [];
        const out = [];
        let entries = [];
        try {
          entries = (0, import_node_fs7.readdirSync)(SPACES_DIR);
        } catch {
          return [];
        }
        for (const name of entries) {
          if (!isValidSpaceSlug(name)) continue;
          let isDir = false;
          try {
            isDir = (0, import_node_fs7.statSync)(import_node_path15.default.join(SPACES_DIR, name)).isDirectory();
          } catch {
            continue;
          }
          if (!isDir) continue;
          const rec = readManifest(name);
          if (!rec) continue;
          if (!includeArchived && rec.status === "archived") continue;
          out.push(rec);
        }
        return out.sort((a, b) => (b.updatedAt ?? "").localeCompare(a.updatedAt ?? ""));
      }
      get(slug) {
        if (!isValidSpaceSlug(slug)) return void 0;
        return readManifest(slug);
      }
      /** Create or update a Space manifest (idempotent by slug). */
      save(input) {
        if (!isValidSpaceSlug(input.id)) {
          throw new Error(`invalid space slug "${input.id}" \u2014 use lowercase kebab-case (2-63 chars).`);
        }
        ensureDir(SPACES_DIR);
        return withFileLockSyncStrict(workspaceMutationLockPath(input.id), () => saveSpaceUnlocked(input));
      }
      /** Reconstruct only the content-addressed proof carrier for an otherwise
       * byte-exact static create. The slug lock and full manifest/view/data
       * comparison make this a self-heal, never an update or overwrite. */
      repairStaticSnapshotCommitReceipt(input) {
        if (!isValidSpaceSlug(input.id)) {
          return { ok: false, reason: "invalid Workspace slug" };
        }
        ensureDir(SPACES_DIR);
        return withFileLockSyncStrict(
          workspaceMutationLockPath(input.id),
          () => repairStaticSnapshotCommitReceiptUnlocked(input)
        );
      }
      /** Snapshot V1 and atomically publish/index one complete V2 view revision. */
      commitViewRevision(slug, nextView) {
        if (!isValidSpaceSlug(slug)) throw new Error(`invalid workspace slug: ${slug}`);
        ensureDir(SPACES_DIR);
        return withFileLockSyncStrict(
          workspaceMutationLockPath(slug),
          () => commitSpaceViewRevisionUnlocked(slug, nextView)
        );
      }
      /** Create one exact Space without ever overwriting an existing directory or
       * manifest. The same cross-process slug lock is shared with save(), so a
       * concurrent UI/agent upsert wins visibly and this caller can refuse rather
       * than replacing user-owned bytes. */
      createIfAbsent(input) {
        if (!isValidSpaceSlug(input.id)) {
          throw new Error(`invalid space slug "${input.id}" \u2014 use lowercase kebab-case (2-63 chars).`);
        }
        ensureDir(SPACES_DIR);
        return withFileLockSyncStrict(workspaceMutationLockPath(input.id), () => {
          recoverWorkspaceDeletionQuarantine(input.id);
          const dir = resolveSpaceDir(input.id);
          if ((0, import_node_fs7.existsSync)(dir)) {
            return { created: false, record: readManifest(input.id) };
          }
          return { created: true, record: saveSpaceUnlocked(input) };
        });
      }
      /** Patch a subset of fields on an existing Space. */
      update(slug, patch) {
        const existing = readManifest(slug);
        if (!existing) return void 0;
        const missingFixes = missingManifestFixes(
          existing.manifestErrors,
          "dataSources" in patch,
          "actions" in patch
        );
        if (missingFixes.length > 0) return existing;
        assertValidWorkspaceIdentities(
          patch.dataSources ?? existing.dataSources,
          patch.actions ?? existing.actions
        );
        assertValidDeclaredRunners(patch.dataSources, patch.actions);
        const record3 = {
          ...existing,
          ...patch,
          id: existing.id,
          createdAt: existing.createdAt,
          updatedAt: (/* @__PURE__ */ new Date()).toISOString(),
          contentMode: (patch.dataSources ?? existing.dataSources).length > 0 ? void 0 : patch.contentMode ?? existing.contentMode
        };
        delete record3.manifestErrors;
        atomicWrite(manifestPath(slug), JSON.stringify(persistableRecord(record3), null, 2));
        indexWorkspaceRecord(record3, {
          eventType: "workspace_file_changed",
          actor: "space-store",
          payload: { mutation: "update" }
        });
        return record3;
      }
      /**
       * Snapshot the current view file into view-history/ and bump the version.
       * Call BEFORE overwriting view/index.html so a bad edit is always revertible.
       * No-op if there's no current view yet.
       */
      recordRevision(slug) {
        const existing = readManifest(slug);
        if (!existing) return void 0;
        const viewFile = resolveInSpace(slug, existing.viewEntry);
        if (!(0, import_node_fs7.existsSync)(viewFile)) return existing;
        const ts = (/* @__PURE__ */ new Date()).toISOString().replace(/[:.]/g, "-");
        const snapRel = import_node_path15.default.join("view-history", `${ts}-v${existing.version}.html`);
        const snapAbs = resolveInSpace(slug, snapRel);
        ensureDir(import_node_path15.default.dirname(snapAbs));
        const content = (0, import_node_fs7.readFileSync)(viewFile, "utf-8");
        (0, import_node_fs7.writeFileSync)(snapAbs, content, "utf-8");
        const revision = {
          version: existing.version,
          ts: (/* @__PURE__ */ new Date()).toISOString(),
          bytes: Buffer.byteLength(content, "utf-8"),
          file: snapRel
        };
        if (existing.manifestErrors && existing.manifestErrors.length > 0) {
          try {
            const file = manifestPath(slug);
            const raw = JSON.parse((0, import_node_fs7.readFileSync)(file, "utf-8"));
            const obj = asObj(raw);
            if (!obj) return existing;
            obj.version = existing.version + 1;
            obj.revisions = [...existing.revisions, revision].slice(-50);
            obj.updatedAt = (/* @__PURE__ */ new Date()).toISOString();
            atomicWrite(file, JSON.stringify(obj, null, 2));
            const updated = readManifest(slug) ?? existing;
            indexWorkspaceRecord(updated, {
              eventType: "workspace_file_changed",
              actor: "space-store",
              payload: { mutation: "recordRevision" }
            });
            return updated;
          } catch {
            return existing;
          }
        }
        return this.update(slug, {
          version: existing.version + 1,
          revisions: [...existing.revisions, revision].slice(-50)
        });
      }
      /** Archive (soft) — keeps files for restore. */
      archive(slug) {
        const existing = readManifest(slug);
        if (existing?.manifestErrors && existing.manifestErrors.length > 0) {
          try {
            const file = manifestPath(slug);
            const raw = JSON.parse((0, import_node_fs7.readFileSync)(file, "utf-8"));
            const obj = asObj(raw);
            if (!obj) return void 0;
            obj.status = "archived";
            obj.updatedAt = (/* @__PURE__ */ new Date()).toISOString();
            atomicWrite(file, JSON.stringify(obj, null, 2));
            const updated = readManifest(slug);
            if (updated) {
              indexWorkspaceRecord(updated, {
                eventType: "workspace_file_changed",
                actor: "space-store",
                payload: { mutation: "archive" }
              });
            }
            return updated;
          } catch {
            return void 0;
          }
        }
        return this.update(slug, { status: "archived" });
      }
      /**
       * Hard-delete a Workspace without allowing temporal history to reattach.
       *
       * Files first move atomically to a hidden quarantine. The directory is
       * removed only after both SQLite stores confirm their cascading deletes.
       * A Workspace DB failure restores the original directory where possible.
       * Once that first DB commits, a memory purge failure leaves the quarantine
       * marker in place so restart recovery finishes before slug reuse.
       */
      remove(slug) {
        if (!isValidSpaceSlug(slug)) return false;
        const dir = resolveSpaceDir(slug);
        let recovered = 0;
        try {
          recovered = recoverWorkspaceDeletionQuarantine(slug);
        } catch {
          return false;
        }
        if (!(0, import_node_fs7.existsSync)(dir)) return recovered > 0;
        const deletionId = (0, import_node_crypto12.randomUUID)();
        const quarantinedDir = workspaceDeletionQuarantinePath(slug, deletionId);
        try {
          ensureDir(WORKSPACE_DELETE_QUARANTINE_DIR);
          (0, import_node_fs7.renameSync)(dir, quarantinedDir);
        } catch {
          return false;
        }
        let durableDeleteCommitted = false;
        try {
          deleteWorkspaceIndex(slug, { actor: "space-store" });
          durableDeleteCommitted = true;
          purgeWorkspaceObservationMemory(slug);
          (0, import_node_fs7.rmSync)(quarantinedDir, { recursive: true, force: true });
          return true;
        } catch {
          if (!durableDeleteCommitted && (0, import_node_fs7.existsSync)(quarantinedDir) && !(0, import_node_fs7.existsSync)(dir)) {
            try {
              (0, import_node_fs7.renameSync)(quarantinedDir, dir);
            } catch {
            }
          }
          return false;
        }
      }
      /** Rebuild the queryable DB index from manifest files. Manifests remain source of truth. */
      reindex(includeArchived = true) {
        return reindexWorkspaceRecords(this.list(includeArchived), {
          actor: "space-store",
          payload: { mutation: "reindex" }
        });
      }
      health(slug) {
        const rec = this.get(slug);
        return rec ? buildSpaceHealthSnapshot(rec) : void 0;
      }
      listHealth(includeArchived = false) {
        return this.list(includeArchived).map((rec) => buildSpaceHealthSnapshot(rec));
      }
    };
    spaceStore = new SpaceStore();
  }
});

// src/spaces/data-store.ts
function appendLine(file, obj) {
  const dir = import_node_path16.default.dirname(file);
  if (!(0, import_node_fs8.existsSync)(dir)) (0, import_node_fs8.mkdirSync)(dir, { recursive: true });
  (0, import_node_fs8.appendFileSync)(file, `${JSON.stringify(obj)}
`, "utf-8");
}
function appendAudit(slug, entry) {
  try {
    appendLine(resolveInSpace(slug, "audit.jsonl"), { ts: (/* @__PURE__ */ new Date()).toISOString(), ...entry });
  } catch {
  }
}
var import_node_fs8, import_node_path16, MAX_DATA_BYTES;
var init_data_store = __esm({
  "src/spaces/data-store.ts"() {
    "use strict";
    import_node_fs8 = require("node:fs");
    import_node_path16 = __toESM(require("node:path"), 1);
    init_store();
    MAX_DATA_BYTES = 5 * 1024 * 1024;
  }
});

// src/spaces/observation-diff.ts
function isRecord2(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function pointerPart(value) {
  return value.replace(/~/g, "~0").replace(/\//g, "~1");
}
function joinPath(base, part) {
  return `${base}/${pointerPart(part)}`;
}
function scalarKey(value) {
  if (typeof value === "string" && value.trim()) return value.trim();
  if (typeof value === "number" && Number.isFinite(value)) return String(value);
  return null;
}
function inferredStableKey(before, after) {
  const rows = [...before, ...after];
  if (rows.length === 0 || !rows.every(isRecord2)) return null;
  for (const candidate of STABLE_ROW_KEYS) {
    const beforeKeys = before.map((row) => scalarKey(row[candidate]));
    const afterKeys = after.map((row) => scalarKey(row[candidate]));
    if (beforeKeys.some((key) => key === null) || afterKeys.some((key) => key === null)) continue;
    if (new Set(beforeKeys).size !== beforeKeys.length || new Set(afterKeys).size !== afterKeys.length) continue;
    return candidate;
  }
  return null;
}
function redactedPreviewValue(value, depth = 0) {
  if (depth > 3) return "[nested]";
  if (Array.isArray(value)) {
    const shown = value.slice(0, 8).map((entry) => redactedPreviewValue(entry, depth + 1));
    return value.length > shown.length ? [...shown, `[+${value.length - shown.length} more]`] : shown;
  }
  if (isRecord2(value)) {
    const entries = Object.entries(value).sort(([left], [right]) => left.localeCompare(right));
    return Object.fromEntries(entries.slice(0, 12).map(([key, entry]) => [
      key,
      isSecretLikeKey(key) ? "[redacted]" : redactedPreviewValue(entry, depth + 1)
    ]));
  }
  if (typeof value === "string") return value.length > 300 ? `${value.slice(0, 297)}\u2026` : value;
  return value;
}
function preview(value, maxChars, path17) {
  const containsSecretKey = path17.split("/").some((part) => isSecretLikeKey(part.replace(/~1/g, "/").replace(/~0/g, "~")));
  if (containsSecretKey) return "[redacted]";
  let rendered;
  try {
    rendered = JSON.stringify(redactedPreviewValue(value));
  } catch {
    rendered = String(value);
  }
  if (rendered === void 0) rendered = String(value);
  return rendered.length > maxChars ? `${rendered.slice(0, Math.max(0, maxChars - 1))}\u2026` : rendered;
}
function valuesEqual(left, right) {
  if (Object.is(left, right)) return true;
  if (Array.isArray(left) && Array.isArray(right)) {
    return left.length === right.length && left.every((entry, index) => valuesEqual(entry, right[index]));
  }
  if (isRecord2(left) && isRecord2(right)) {
    const leftKeys = Object.keys(left).sort();
    const rightKeys = Object.keys(right).sort();
    return leftKeys.length === rightKeys.length && leftKeys.every((key, index) => key === rightKeys[index] && valuesEqual(left[key], right[key]));
  }
  return false;
}
function countLabel(count, singular) {
  return `${count} ${singular}${count === 1 ? "" : "s"}`;
}
function diffWorkspaceObservationDocuments(before, after, options = {}) {
  const maxChanges = Math.max(1, Math.min(500, options.maxChanges ?? 80));
  const maxDepth = Math.max(1, Math.min(20, options.maxDepth ?? 8));
  const maxPreviewChars = Math.max(40, Math.min(2e3, options.maxPreviewChars ?? 240));
  const maxCollectionEntries = Math.max(10, Math.min(5e3, options.maxCollectionEntries ?? 500));
  const changes = [];
  let truncated = false;
  const emit = (op, path17, prior, next, entityKey) => {
    if (changes.length >= maxChanges) {
      truncated = true;
      return;
    }
    changes.push({
      op,
      path: path17,
      ...entityKey ? { entityKey } : {},
      ...op !== "add" ? { before: preview(prior, maxPreviewChars, path17) } : {},
      ...op !== "remove" ? { after: preview(next, maxPreviewChars, path17) } : {}
    });
  };
  const visit = (prior, next, path17, depth, entityKey) => {
    if (changes.length >= maxChanges) {
      if (!valuesEqual(prior, next)) truncated = true;
      return;
    }
    if (valuesEqual(prior, next)) return;
    if (depth >= maxDepth) {
      emit("replace", path17, prior, next, entityKey);
      truncated = true;
      return;
    }
    if (Array.isArray(prior) && Array.isArray(next)) {
      if (prior.length + next.length > maxCollectionEntries * 2) {
        emit("replace", path17, prior, next, entityKey);
        truncated = true;
        return;
      }
      const stableKey = inferredStableKey(prior, next);
      if (stableKey) {
        const previousRows = new Map(prior.map((row) => [
          scalarKey(row[stableKey]),
          row
        ]));
        const currentRows = new Map(next.map((row) => [
          scalarKey(row[stableKey]),
          row
        ]));
        const identities = [.../* @__PURE__ */ new Set([...previousRows.keys(), ...currentRows.keys()])].sort();
        for (const identity of identities) {
          const priorRow = previousRows.get(identity);
          const nextRow = currentRows.get(identity);
          const rowIdentity = `${stableKey}=${identity}`;
          const rowPath = joinPath(path17, `@${rowIdentity}`);
          if (priorRow === void 0) emit("add", rowPath, void 0, nextRow, rowIdentity);
          else if (nextRow === void 0) emit("remove", rowPath, priorRow, void 0, rowIdentity);
          else visit(priorRow, nextRow, rowPath, depth + 1, rowIdentity);
        }
        return;
      }
      const common = Math.min(prior.length, next.length);
      for (let index = 0; index < common; index += 1) {
        visit(prior[index], next[index], joinPath(path17, String(index)), depth + 1, entityKey);
      }
      for (let index = common; index < prior.length; index += 1) {
        emit("remove", joinPath(path17, String(index)), prior[index], void 0, entityKey);
      }
      for (let index = common; index < next.length; index += 1) {
        emit("add", joinPath(path17, String(index)), void 0, next[index], entityKey);
      }
      return;
    }
    if (isRecord2(prior) && isRecord2(next)) {
      const keys = [.../* @__PURE__ */ new Set([...Object.keys(prior), ...Object.keys(next)])].sort();
      if (keys.length > maxCollectionEntries) {
        emit("replace", path17, prior, next, entityKey);
        truncated = true;
        return;
      }
      for (const key of keys) {
        const nextPath = joinPath(path17, key);
        if (!Object.hasOwn(prior, key)) emit("add", nextPath, void 0, next[key], entityKey);
        else if (!Object.hasOwn(next, key)) emit("remove", nextPath, prior[key], void 0, entityKey);
        else visit(prior[key], next[key], nextPath, depth + 1, entityKey);
      }
      return;
    }
    emit("replace", path17, prior, next, entityKey);
  };
  visit(before, after, "", 0);
  const counts = { add: 0, remove: 0, replace: 0 };
  for (const change of changes) counts[change.op] += 1;
  const changed = changes.length > 0 || truncated;
  const pieces = [
    countLabel(counts.add, "addition"),
    countLabel(counts.remove, "removal"),
    countLabel(counts.replace, "replacement")
  ];
  return {
    changed,
    summary: changed ? `${pieces.join(", ")}${truncated ? " (bounded; more changes exist)" : ""}` : "No data changes.",
    counts,
    changes,
    truncated
  };
}
var STABLE_ROW_KEYS;
var init_observation_diff = __esm({
  "src/spaces/observation-diff.ts"() {
    "use strict";
    init_security();
    STABLE_ROW_KEYS = ["id", "_id", "uuid", "key", "slug", "email"];
  }
});

// src/spaces/workspace-observation-finalize.ts
function pruneWorkspaceObservationProjectionReceipts(workspaceId) {
  const db = openWorkspaceDb();
  const retained = db.prepare(`
    SELECT id
    FROM workspace_dataset_observations
    WHERE workspace_id = ?
  `).all(workspaceId).map((row) => row.id);
  return pruneWorkspaceObservationMemoryProjections(workspaceId, retained);
}
function provenanceSummary(cause) {
  switch (cause) {
    case "scheduled":
      return "Scheduled Workspace observation committed.";
    case "creation_smoke":
      return "Workspace creation-check observation committed.";
    case "retry":
      return "Workspace recovery observation committed.";
    case "direct_put":
      return "Direct Workspace document observation committed.";
    case "manual":
      return "Manual Workspace observation committed.";
    case "legacy_import":
      return "Pre-3.0 Workspace baseline imported.";
    default:
      return "Workspace observation committed.";
  }
}
function decodedPointerLeaf(path17) {
  const part = path17.split("/").at(-1) ?? "";
  return part.replace(/~1/g, "/").replace(/~0/g, "~");
}
function normalizedFieldName(value) {
  return value.replace(/[^a-z0-9]/gi, "").toLowerCase();
}
function memoryRelevantDiff(diff) {
  const changes = diff.changes.filter((change) => !VOLATILE_FIELD_NAMES.has(normalizedFieldName(decodedPointerLeaf(change.path))));
  const counts = { add: 0, remove: 0, replace: 0 };
  for (const change of changes) counts[change.op] += 1;
  return {
    changed: changes.length > 0 || diff.truncated,
    summary: "",
    counts,
    changes,
    truncated: diff.truncated
  };
}
async function projectObservationMemory(workspaceId, observation, dependencies) {
  if (observation.status !== "ok" || observation.changed !== true || !observation.contentHash || !observation.previousObservationId) return null;
  if (dependencies.hasProjection?.(workspaceId, observation.id)) return null;
  const previous = dependencies.getObservation(
    workspaceId,
    observation.previousObservationId
  );
  if (!previous || previous.workspaceId !== workspaceId || previous.sourceKey !== observation.sourceKey || previous.status !== "ok" || !previous.contentHash) {
    dependencies.recordProjection?.({
      workspaceId,
      observationId: observation.id,
      disposition: "suppressed",
      reason: "previous_observation_unavailable"
    });
    return null;
  }
  const before = dependencies.getDocument(workspaceId, previous.id);
  const after = dependencies.getDocument(workspaceId, observation.id);
  if (before === void 0 || after === void 0) {
    dependencies.recordProjection?.({
      workspaceId,
      observationId: observation.id,
      disposition: "suppressed",
      reason: "observation_document_unavailable"
    });
    return null;
  }
  const diff = memoryRelevantDiff(diffWorkspaceObservationDocuments(before, after));
  if (!diff.changed) {
    dependencies.recordProjection?.({
      workspaceId,
      observationId: observation.id,
      disposition: "suppressed",
      reason: "volatile_only"
    });
    return null;
  }
  const captured = await dependencies.captureMemory({
    kind: "observation",
    workspaceId,
    sourceId: observation.sourceKey,
    observationId: observation.id,
    contentHash: observation.contentHash,
    previousContentHash: previous.contentHash,
    occurredAt: observation.observedAt,
    provenanceSummary: provenanceSummary(observation.cause),
    outcome: "succeeded",
    changeCounts: diff.counts,
    truncated: diff.truncated
  });
  if (captured.status !== "failed") {
    dependencies.recordProjection?.({
      workspaceId,
      observationId: observation.id,
      disposition: "captured",
      reason: captured.status
    });
  }
  return captured;
}
async function finalizeWorkspaceObservationCommit(workspaceId, committed, dependencies = defaultDependencies2) {
  const memory = [];
  let memoryCandidates = 0;
  for (const observation of committed.observations) {
    try {
      const captured = await projectObservationMemory(
        workspaceId,
        observation,
        dependencies
      );
      if (!captured) continue;
      memoryCandidates += 1;
      memory.push(captured);
    } catch {
      memory.push({
        status: "failed",
        reason: "memory_unavailable",
        episodeId: null,
        wake: false
      });
    }
  }
  let retention = null;
  let projectionReceiptsPruned = 0;
  try {
    retention = dependencies.prune(workspaceId);
    projectionReceiptsPruned = dependencies.pruneProjectionReceipts?.(workspaceId) ?? 0;
  } catch {
  }
  return {
    memory,
    memoryCandidates,
    memoryRecorded: memory.filter((entry) => entry.status === "recorded").length,
    retention,
    projectionReceiptsPruned
  };
}
var WORKSPACE_HISTORY_RETENTION, memoryBridge, defaultDependencies2, VOLATILE_FIELD_NAMES;
var init_workspace_observation_finalize = __esm({
  "src/spaces/workspace-observation-finalize.ts"() {
    "use strict";
    init_workspace_db();
    init_observation_diff();
    init_workspace_observation_bridge();
    WORKSPACE_HISTORY_RETENTION = Object.freeze({
      maxObservationsPerSource: 256,
      maxAgeDays: 90,
      maxDatasetBytes: 64 * 1024 * 1024
    });
    memoryBridge = createWorkspaceObservationMemoryBridge();
    defaultDependencies2 = {
      getObservation: (workspaceId, observationId) => getWorkspaceDatasetObservation(workspaceId, observationId),
      getDocument: (workspaceId, observationId) => getWorkspaceObservationDocument(workspaceId, observationId),
      captureMemory: (signal) => memoryBridge.capture(signal),
      hasProjection: (workspaceId, observationId) => hasWorkspaceObservationMemoryProjection(workspaceId, observationId),
      recordProjection: (input) => {
        recordWorkspaceObservationMemoryProjection(input);
      },
      prune: (workspaceId) => pruneWorkspaceDatasetHistory(
        workspaceId,
        WORKSPACE_HISTORY_RETENTION
      ),
      pruneProjectionReceipts: (workspaceId) => pruneWorkspaceObservationProjectionReceipts(workspaceId)
    };
    VOLATILE_FIELD_NAMES = /* @__PURE__ */ new Set([
      "createdat",
      "fetchedat",
      "generatedat",
      "ingestedat",
      "lastrefreshedat",
      "lastsyncedat",
      "modifiedat",
      "observedat",
      "refreshedat",
      "retrievedat",
      "syncedat",
      "timestamp",
      "updatedat"
    ]);
  }
});

// src/spaces/workspace-set-data-executor.ts
function rowCount(data) {
  if (Array.isArray(data)) return data.length;
  if (data && typeof data === "object") {
    const record3 = data;
    if (Array.isArray(record3.records)) return record3.records.length;
    if (Array.isArray(record3.items)) return record3.items.length;
    if (Array.isArray(record3.data)) return record3.data.length;
  }
  return null;
}
function observationMatches(observation, workspace, prepared, refreshId) {
  return observation.workspaceId === workspace.workspaceId && observation.sourceId === prepared.sourceId && observation.refreshId === refreshId && observation.status === "ok" && observation.projectionMode === "source" && observation.contentDigest === prepared.contentDigest && observation.argsDigest === prepared.argsDigest;
}
function resultFromObservation(input) {
  const { workspace, prepared, observation, storage } = input;
  return Object.freeze({
    artifactId: workspaceDatasetArtifactId(prepared),
    workspaceId: workspace.workspaceId,
    slug: workspace.slug,
    sourceId: prepared.sourceId,
    observationId: observation.observationId,
    refreshId: observation.refreshId,
    created: input.created,
    contentDigest: observation.contentDigest,
    revisionDigest: observation.contentDigest,
    handle: storage.handleFor({ workspace, observation }),
    receipt: storage.receiptFor({ workspace, observation }),
    bytes: observation.bytes,
    rows: rowCount(prepared.data)
  });
}
function executeWorkspaceSetDataWithStorage(args, storage, options) {
  const prepared = prepareWorkspaceSetData(args);
  const workspace = storage.resolveWorkspace(prepared.slug);
  if (!workspace) {
    throw new WorkspaceSetDataExecutionError(
      "workspace_not_found",
      `No workspace named "${prepared.slug}".`
    );
  }
  if (workspace.status !== "active") {
    throw new WorkspaceSetDataExecutionError(
      "workspace_not_active",
      `Workspace "${prepared.slug}" is ${workspace.status}; data writes are disabled until it is active.`
    );
  }
  const refreshId = options.mode === "content_addressed" ? prepared.refreshId : options.manualRefreshId?.trim() ?? "";
  if (!refreshId) {
    throw new WorkspaceSetDataExecutionError(
      "commit_mismatch",
      "manual Workspace data commit requires a fresh refresh identity"
    );
  }
  if (options.mode === "content_addressed") {
    const retained = storage.inspectExact({ workspace, prepared, refreshId });
    if (retained) {
      if (!observationMatches(retained, workspace, prepared, refreshId)) {
        throw new WorkspaceSetDataExecutionError(
          "observation_conflict",
          "the deterministic Workspace observation identity is already bound to different content"
        );
      }
      return resultFromObservation({
        workspace,
        prepared,
        observation: retained,
        created: false,
        storage
      });
    }
  }
  const committed = storage.commit({
    workspace,
    prepared,
    refreshId,
    cause: options.cause,
    provenance: { ...options.provenance, argsHash: prepared.argsDigest }
  });
  if (!observationMatches(committed, workspace, prepared, refreshId)) {
    throw new WorkspaceSetDataExecutionError(
      "commit_mismatch",
      "Workspace observation commit did not return the exact requested artifact"
    );
  }
  return resultFromObservation({
    workspace,
    prepared,
    observation: committed,
    created: !committed.deduped,
    storage
  });
}
var WorkspaceSetDataExecutionError;
var init_workspace_set_data_executor = __esm({
  "src/spaces/workspace-set-data-executor.ts"() {
    "use strict";
    init_workspace_set_data_contract();
    WorkspaceSetDataExecutionError = class extends Error {
      constructor(code, message) {
        super(message);
        this.code = code;
        this.name = "WorkspaceSetDataExecutionError";
      }
      code;
    };
  }
});

// src/spaces/workspace-set-data-carrier.ts
var workspace_set_data_carrier_exports = {};
__export(workspace_set_data_carrier_exports, {
  executeManualWorkspaceSetData: () => executeManualWorkspaceSetData,
  executeReviewedWorkspaceSetData: () => executeReviewedWorkspaceSetData,
  reconcileWorkspaceDatasetArtifact: () => reconcileWorkspaceDatasetArtifact
});
function prepareObservationStore(rec) {
  const db = openWorkspaceDb();
  const indexed = db.prepare("SELECT 1 FROM workspaces WHERE id = ? LIMIT 1").get(rec.id);
  if (!indexed) {
    indexWorkspaceRecord(rec, {
      db,
      actor: "workspace-set-data-carrier",
      emitOperational: false,
      appendStateEvent: false,
      payload: { legacyIndex: true }
    });
    if (!db.prepare("SELECT 1 FROM workspaces WHERE id = ? LIMIT 1").get(rec.id)) {
      throw new Error("workspace index could not be prepared");
    }
  }
  let checked = observationBootstrapChecks.get(db);
  if (!checked) {
    checked = /* @__PURE__ */ new Set();
    observationBootstrapChecks.set(db, checked);
  }
  if (!checked.has(rec.id)) {
    const bootstrap = bootstrapWorkspaceObservationHistory(rec.id, { db });
    if (!bootstrap.ok) {
      throw new Error(`legacy comparison baseline could not be imported: ${bootstrap.error}`);
    }
    checked.add(rec.id);
  }
  return db;
}
function workspaceForRecord(rec) {
  return { workspaceId: rec.id, slug: rec.id, status: rec.status };
}
function mappedObservation(input) {
  const argsHash = typeof input.row.provenance.argsHash === "string" ? input.row.provenance.argsHash : null;
  return {
    observationId: input.row.id,
    workspaceId: input.row.workspaceId,
    sourceId: input.row.sourceKey,
    refreshId: input.row.refreshId,
    status: input.row.status,
    projectionMode: input.row.projectionMode,
    cause: input.row.cause,
    contentDigest: input.row.contentHash ?? "",
    argsDigest: argsHash,
    data: input.data,
    bytes: input.bytes,
    deduped: input.row.deduped === true
  };
}
function createStoragePort() {
  let committed = null;
  const storage = {
    resolveWorkspace(slug) {
      const rec = spaceStore.get(slug);
      return rec ? workspaceForRecord(rec) : null;
    },
    inspectExact({ workspace, prepared, refreshId }) {
      const rec = spaceStore.get(workspace.slug);
      if (!rec || rec.id !== workspace.workspaceId) return null;
      const db = prepareObservationStore(rec);
      const row = getWorkspaceDatasetObservationByRefreshId(
        workspace.workspaceId,
        prepared.sourceId,
        refreshId,
        db
      );
      if (!row) return null;
      const data = getWorkspaceObservationDocument(workspace.workspaceId, row.id, db);
      return mappedObservation({
        row,
        data,
        bytes: Buffer.byteLength(prepared.canonicalData, "utf8")
      });
    },
    commit({ workspace, prepared, refreshId, cause, provenance }) {
      const rec = spaceStore.get(workspace.slug);
      if (!rec || rec.id !== workspace.workspaceId || rec.status !== "active") {
        throw new Error("Workspace identity/status changed before the observation commit");
      }
      const db = prepareObservationStore(rec);
      const batch = commitWorkspaceObservationBatch({
        db,
        workspaceId: workspace.workspaceId,
        observations: [{
          sourceKey: prepared.sourceId,
          refreshId,
          cause,
          status: "ok",
          data: prepared.data,
          provenance
        }]
      });
      const row = batch.observations[0];
      if (!row) throw new Error("Workspace observation commit returned no observation");
      committed = { workspaceId: workspace.workspaceId, batch };
      return mappedObservation({
        row,
        data: prepared.data,
        bytes: Buffer.byteLength(prepared.canonicalData, "utf8")
      });
    },
    handleFor({ workspace, observation }) {
      return `${resolveInSpace(workspace.slug, "data.json")}#source=${encodeURIComponent(observation.sourceId)}`;
    },
    receiptFor({ observation }) {
      return `workspace-observation:${observation.observationId}`;
    }
  };
  return { storage, committed: () => committed };
}
async function executeWithHostStorage(args, mode) {
  const carrier = createStoragePort();
  const result = executeWorkspaceSetDataWithStorage(args, carrier.storage, mode === "manual" ? {
    mode,
    manualRefreshId: (0, import_node_crypto13.randomUUID)(),
    cause: "manual",
    provenance: { adapter: "manual", initiatedBy: "model" }
  } : {
    mode,
    cause: "reviewed_local",
    provenance: { adapter: "reviewed_local", initiatedBy: "workflow_v3" }
  });
  const committed = carrier.committed();
  if (result.created && committed) {
    try {
      await finalizeWorkspaceObservationCommit(committed.workspaceId, committed.batch);
    } catch {
    }
    appendAudit(result.slug, {
      method: "SET_DATA",
      path: `/set_data/${result.sourceId}`,
      outcome: "ok",
      bytes: result.bytes
    });
  }
  return result;
}
function executeManualWorkspaceSetData(args) {
  return executeWithHostStorage(args, "manual");
}
function executeReviewedWorkspaceSetData(args) {
  return executeWithHostStorage(args, "content_addressed");
}
function reconcileWorkspaceDatasetArtifact(artifactId) {
  const identity = parseWorkspaceDatasetArtifactId(artifactId);
  if (!identity) return { exists: false };
  const rec = spaceStore.get(identity.slug);
  if (!rec) return { exists: false };
  const db = openWorkspaceDb();
  const row = getWorkspaceDatasetObservationByRefreshId(
    rec.id,
    identity.sourceId,
    identity.refreshId,
    db
  );
  if (!row || row.status !== "ok" || row.projectionMode !== "source" || row.contentHash !== identity.contentDigest || row.cause !== "reviewed_local" || row.provenance.adapter !== "reviewed_local" || row.provenance.initiatedBy !== "workflow_v3" || row.provenance.argsHash !== identity.argsDigest) return { exists: false };
  const content = getWorkspaceObservationDocument(rec.id, row.id, db);
  if (content === void 0 || workspaceDataContentDigest(content) !== identity.contentDigest) {
    return { exists: false };
  }
  const prepared = prepareWorkspaceSetData({
    slug: identity.slug,
    source_id: identity.sourceId,
    data_json: JSON.stringify(content)
  });
  if (prepared.argsDigest !== identity.argsDigest || prepared.refreshId !== identity.refreshId || prepared.contentDigest !== identity.contentDigest) return { exists: false };
  const observation = mappedObservation({
    row,
    data: content,
    bytes: Buffer.byteLength(prepared.canonicalData, "utf8")
  });
  const workspace = workspaceForRecord(rec);
  const carrier = createStoragePort().storage;
  return {
    exists: true,
    artifactId,
    handle: carrier.handleFor({ workspace, observation }),
    contentDigest: identity.contentDigest,
    receipt: carrier.receiptFor({ workspace, observation }),
    content
  };
}
var import_node_crypto13, observationBootstrapChecks;
var init_workspace_set_data_carrier = __esm({
  "src/spaces/workspace-set-data-carrier.ts"() {
    "use strict";
    import_node_crypto13 = require("node:crypto");
    init_data_store();
    init_workspace_db();
    init_workspace_observation_finalize();
    init_store();
    init_workspace_set_data_contract();
    init_workspace_set_data_executor();
    observationBootstrapChecks = /* @__PURE__ */ new WeakMap();
  }
});

// src/runtime/harness/production-capability-adapters.ts
function looksLikeContentDigest(value) {
  return /^[a-f0-9]{64}$/i.test(value);
}
function manifestHasCompatibleAttestedReconcile(manifest) {
  if (!manifest.reconciliation.supported || manifest.reconciliation.policy !== "exact_artifact" || !manifest.reconcilePortId || manifest.reconcilePortId === manifest.invokePortId) return false;
  return manifest.providerKind === "local_registry" || manifest.providerKind === "composio" && manifest.operationId === BETA_PROVIDER_OPERATIONS.create && manifest.purpose === "persist_collection";
}
function reconcileForSealedManifest(manifest) {
  const supported = manifest.reconciliation.supported;
  const policy = manifest.reconciliation.policy;
  const accountId = manifest.accountId;
  const operationId = manifest.operationId === BETA_PROVIDER_OPERATIONS.create ? SHEET_RECONCILE : manifest.operationId;
  const reviewedLocal = manifest.providerKind === "local_registry" ? observeReviewedLocalTool(manifest.operationId) : null;
  const reviewedManifest = reviewedLocal ? reviewedLocalCapabilityManifest(reviewedLocal) : null;
  const workspaceDatasetReconcile = reviewedLocal?.execution.adapter === "workspace_dataset_v1" && reviewedManifest !== null && capabilityManifestDigest(reviewedManifest) === capabilityManifestDigest(manifest);
  return async ({ artifactId, intendedDigest }) => {
    if (!supported || policy === "uncertain_if_absent" || !manifestHasCompatibleAttestedReconcile(manifest)) {
      return { exists: false };
    }
    const id = artifactId?.trim() || "";
    if (!id || looksLikeContentDigest(id)) return { exists: false };
    if (workspaceDatasetReconcile) {
      const carrier = await Promise.resolve().then(() => (init_workspace_set_data_carrier(), workspace_set_data_carrier_exports));
      const recovered = carrier.reconcileWorkspaceDatasetArtifact(id);
      if (!recovered.exists || recovered.contentDigest !== intendedDigest) return { exists: false };
      return {
        exists: true,
        id: recovered.artifactId,
        handle: recovered.handle,
        receipt: recovered.receipt,
        contentDigest: recovered.contentDigest
      };
    }
    const attested = (() => {
      try {
        return requireAttestedTransport();
      } catch {
        return null;
      }
    })();
    if (!attested) return { exists: false };
    const result = await attested.reconcile({
      artifactId: id,
      accountId,
      operationId
    });
    if (!result.exists || result.artifactId !== id) return { exists: false };
    return {
      exists: true,
      id: result.artifactId,
      handle: result.handle,
      receipt: result.receipt,
      ...result.contentDigest ? { contentDigest: result.contentDigest } : {}
    };
  };
}
var SHEET_CREATE, SHEET_READBACK, SHEET_RECONCILE, BETA_PROVIDER_OPERATIONS;
var init_production_capability_adapters = __esm({
  "src/runtime/harness/production-capability-adapters.ts"() {
    "use strict";
    init_composio_schema_cache();
    init_tool_contract_store();
    init_capability_live_identity();
    init_capability_manifest();
    init_isolated_test_contract();
    init_attested_transport();
    init_reviewed_local_tool_transport();
    SHEET_CREATE = "GOOGLESHEETS_SHEET_FROM_JSON";
    SHEET_READBACK = "GOOGLESHEETS_BATCH_GET";
    SHEET_RECONCILE = "GOOGLESHEETS_GET_SPREADSHEET_INFO";
    BETA_PROVIDER_OPERATIONS = {
      locator: "TAVILY_TAVILY_SEARCH",
      collection: "TAVILY_TAVILY_EXTRACT",
      transform: "host_transform",
      create: SHEET_CREATE,
      readback: SHEET_READBACK,
      calendar: "GOOGLECALENDAR_CREATE_EVENT"
    };
  }
});

// src/runtime/harness/implementation-artifacts/reconcile-entry.ts
var reconcile_entry_exports = {};
__export(reconcile_entry_exports, {
  bindAttestedTransport: () => bindAttestedTransport,
  reconcileForSealedManifest: () => reconcileForSealedManifest
});
module.exports = __toCommonJS(reconcile_entry_exports);
init_production_capability_adapters();
init_attested_transport();
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  bindAttestedTransport,
  reconcileForSealedManifest
});
