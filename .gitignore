# Dependencies
node_modules/
dist/

# Environment + secrets — NEVER commit
.env*
!.env.example

# Signing and provisioning material
*.pem
*.p8
*.p12
*.pfx
*.key
*.cer
*.mobileprovision
*.provisionprofile

# Logs
*.log
logs/

# Local 3D memory-graph artifacts — contain REAL memory data, never commit.
# Regenerate the snapshot/prototype any time with: npx tsx scripts/dump-memory-graph.ts
/scripts/memory-graph-snapshot.json
/clementine-memory-constellation.html

# Build / packaging artifacts
*.tgz
release/
apps/desktop/native/notch-click-helper/.build/
# Local hot-patch staging copies of the built desktop app (feedback_clemmy_desktop_patch)
apps/desktop/release-*/
*.dmg
*.zip
*.AppImage
*.exe
*.pkg

# Editor / OS
.DS_Store
*.swp
*.swo
.vscode/
.idea/
.history/

# Local scratch dirs
.firecrawl/
.codex/
.tmp/
tmp/
/.private/
/private/

# Local agent state. The checked-in authoring skill under .claude/skills stays
# tracked; these files are per-user runtime state only.
.claude/settings.local.json
.claude/scheduled_tasks.*
.claude/worktrees/
.claude/checkpoints/
.claude/mailbox/
.claude/agent-registry.json
.claude/agent-memory-local
.claude/first-run
.claude/assistant-daemon-state.json

# Per-user workflow run artifacts (audit CSVs, drafts, prompts) —
# test scratch, not source to ship.
runs/

# Test artifacts
coverage/
.nyc_output/

# Local runtime stores and event streams
*.db
*.db-*
*.sqlite
*.sqlite3
*.ndjson

# TypeScript build info
*.tsbuildinfo

# Debug / scratch artifacts
.playwright-mcp/
.mib2c-updaterc
install-v040.sh
hot-patch.sh
patcheck.tmp
cvelist
cvelist-our
cvelist.diff

# Root-level captures and dated work products are local review artifacts, not
# public source. Product imagery belongs under an app's public/build assets and
# maintained project documentation belongs under docs/.
/*.png
/*.jpg
/*.jpeg
/*.webp
/*-20[0-9][0-9]-[0-9][0-9]-[0-9][0-9].md
/*-20[0-9][0-9]-[0-9][0-9]-[0-9][0-9].json
/*-20[0-9][0-9]-[0-9][0-9]-[0-9][0-9].csv
/*-drafts-*.md
/*-evidence-*.md
/*-send-confirmations-*.md
/client-*
/outreach-*
/prospecting-*
/seo-*
/workspaces/
/one-pagers/
/lunar-audits/


# Vendored native runtimes — uv is generated for npm/desktop packages; whisper.cpp is desktop-only. Never commit.
vendor/

# Local Netlify folder
.netlify
proof-report.json

# Local demo/scratch outputs — never release content
output/
*.clemplug
