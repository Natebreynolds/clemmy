# v3.18.21 — Jev as the router, a chat that reads as a page, model accounts and coding runs

Release type: integration of four lines of work made after v3.18.20, on one
release branch from `main`: harness efficiency and the phone's model
settings; model accounts and provider credit; coding runs; answer streaming
and the chat redesign with its answer receipts. Two fixes found while the
owner tested the release are in it too. The gates below ran on the release
branch; the table says which commit each ran on.

## What a user notices

**The chat reads as a page, and the answer streams in as it is written.**
- Replies render as a page on desktop and phone, with tables and links and
  no box around them; the chat column is a reading measure.
- A turn's work is one live line (what Clem is doing now, and the clock)
  that folds to "Worked 58s · 4 steps" and reopens to the steps.
- A question's suggested answers are one-tap replies, only while the
  question is the newest message.
- Under an answer: the file Clem saved, "Checked" only when a review passed
  ("Not checked" when none ran), what the turn touched, and the model that
  did the work.
- The answer streams into the chat as it is written, and it stays on screen
  while it is checked: "Checking this answer…" appears under it. If the check
  sends it back, it stays, dimmed, with "Found issues, correcting…" until the
  corrected answer replaces it in place; it never becomes the delivered reply,
  and it no longer vanishes back to "thinking". OpenAI-compatible backends that used
  to deliver a reply all at once now stream it.

**One place for model accounts.** Settings shows each provider account,
whether it is refusing work for lack of credit, and where to add more. A
prepaid account that runs out is marked until its next successful answer.
Connecting an app says what went wrong in plain words, and a Connect link
arrives with the account details already filled in.

**Clem runs Claude Code in its own worktree.** A coding task agreed in chat
becomes a durable run on its own branch: its activity streams into the chat
and a Coding card on desktop and phone, it can be stopped, and it reports
back. Pushes, pull requests, publishing, writes outside the worktree and
credentials are refused at the hook with a reason the agent can act on, and
Clem runs the tests herself before a turn counts.

**Who does each part of a request, on the phone too.** Settings on the
phone has one Models card: does the work, writes the final answer, checks
the work, helps in parallel. Each row names the model that will run and
picks from the connected catalog. A chosen writer writes the evidence-heavy
final answer and is reviewed by a model from another family. Desktop,
phone and chat save role choices through one owner.

**Jev works as the fast router and simple reviewer it is built to be.**
- At the start of a request, one Jev request decides whether a remembered
  run fits and which single operation the request needs, so the brain calls
  it directly instead of paying a search round. The wait is bounded at
  1.5 s, and a remembered run that only shares words with a request is
  checked instead of taken on sight.
- The final-answer check asks five typed yes/no questions over compact
  state inside Jev's documented budget, where one broad verdict over the
  whole evidence dump used to fail on research turns. A NOT-DONE from Jev
  is never final; the reviewer decides it.
- Every Jev call names a pinned model (`jev-1.13.0`) and leaves a decision
  record, so its thresholds can be measured against outcomes.

**A question asked back gets an answer.** When Clem paused before a write
or send and the reply was a question of its own ("which channel will you post
it to?"), the host re-asked Clem's question word for word, a held draft's line
breaks collapsed into one line, and the chat called the turn "Didn't finish".
A side question read as conversation fared worse: with Clem's question still
open, planning refused the turn and asked the user to retry. Jev now reads a
reply that settles nothing in one choice, and a question back goes to Clem:
she answers it with the whole tool surface (before a save, she checked the
folder), keeps the pending step on hold, and asks again. Only a sure attempt
to answer is re-asked as before. The question keeps its exact text, and a
turn that waits on you reads "Waiting for you" on desktop and phone.

**Every answer says who did the work and who checked it.** The receipt under
an answer names the model that did the work and the model that reviewed it,
on desktop and phone. A confirmed write to another app shows as a card, for
example "5 drafts in Outlook · not sent", with a link to the app once the
connected-app catalog has refreshed.

**A scheduled workflow's checker sees what the run did.** The calls a
scheduled step made reached the run's goal and target reviews as "0 logical
settlements, no retained execution evidence", so a strict checker failed
runs whose work had landed: a scheduled refresh that read six record sets and
saved one dataset was reported as failed on two mornings. The reviewers now
see each step's authenticated result, a write step's receipt, and the write
contract the tool registry declares for it ("an exact replay of the same
content adds no write").

**A request for two operations keeps both on the first discovery page**,
the regression named under Known limitations in v3.18.20.

## Known limitations, named honestly

- Jev's confidence thresholds are not yet calibrated; the decision log
  started collecting the data for that with this release.
- Turn-start routing offers only operations the request names by a
  distinctive word; an operation never used before still costs a search.
- The model that checks the work is the owner's choice. A flagship checker
  makes completion reviews and escalated watcher windows the slowest and
  most expensive part of a turn.
- The model accounts work was committed from its agent's worktree for this
  release; its own tests pass and the installed-app acceptance below covers
  it.
- Coding runs: `project_run start` stays closed; runs start only through the
  chat dispatch.
- The completion checker sees one turn's evidence. A reply that repeats a
  check made in an earlier turn can be rejected as unverified even when the
  work is done; the turn then reads blocked with its work kept.
- Jev reads a reply to an open question only after the turn interpreter has
  run. Reading it first, and skipping the interpreter for a question back,
  needs a shadow run first.
- After a failed goal check, a workflow whose step wrote something is still
  escalated rather than re-run, even when that write is content-addressed.
- Windows: signing secrets remain absent; this tag is Mac-only.

## Gates on the release commit

Run on 2026-09-25 with the owner's app running. Rows marked "final" ran on
the final commit `aabdb2182`, which adds the question-back fix, the answer
receipts and the draft that stays on screen (the journeys ran on
`92b47fb4b`, its parent, whose only later change is the answer stream). The
unmarked rows below them ran earlier that day at `cf3cb86d3`.

| Gate | Result |
|---|---|
| final: typecheck (daemon, desktop, phone), public hygiene, operation identity | pass |
| final: full isolated suite | 17,384 tests: 17,371 pass, 6 skipped, 7 fail. Three are the build-stamp test, which needs a built tree and passes 4/4 in the built candidate. Four are timing-bound tests (a cross-process approval race, a checkpoint reopen, two 3-second host deadlines) that failed with the machine's load average above 100; run alone they pass 47/47. The same kind of load failure in the previous full run on `92b47fb4b` also passed alone (302/302). |
| final: release closure, release assets, smoke gate | release closure 138/138, release assets 57/57, smoke gate pass |
| final: packaged-candidate smoke, v3.14 upgrade rehearsal, packaged upgrade | pass, pass, 21/21 |
| final: journeys | On `92b47fb4b`: 169 tests, 134 pass, 34 fail, one file stopped by the 10-minute watchdog. The failing set matches v3.18.20 except `automation-partition-ledger`, which timed out beside a concurrent rerun and passes alone on `aabdb2182`. `restaurant-sheet-natural-request`, failing at v3.18.20 too, hung under that load; alone it fails fast with the runner's known "Unable to deserialize cloned data". |
| final: installed-app acceptance | Fingerprint-exact install of `aabdb2182` (daemon, desktop and phone web apps). A read-only question the owner's checker sent back twice streamed as: draft, checking, sent back (review), a second draft replacing it in place, checking, sent back (review), a third draft, checking, delivered. Before it, on `92b47fb4b`: A held draft, then a question back ("will saving it overwrite anything?"): Jev read a question (1.0), Clem checked the folder with reads only, answered, restated the draft and waited; nothing was written. The go saved the exact draft and read it back. The owner's checker then rejected that reply three times on incidental claims (a byte count, and a pre-check made in the previous turn), so the turn ended blocked with the file saved: named under Known limitations. |
| typecheck (daemon, desktop, phone) | pass |
| public hygiene, operation identity | pass |
| full isolated suite (on `2ca1d1802`, before the workflow evidence fix) | 17,373 tests: 17,363 pass, 7 skipped, 3 fail. The 3 are the build-stamp test, which reads a build the unbuilt worktree did not have; it passes 4/4 in the built candidate. |
| execution area after the workflow evidence fix (218 files) | 2,382/2,382 |
| release closure | 138/138 |
| release assets | 57/57, once the desktop app's dependencies were present in the release worktree |
| smoke gate | pass, once the desktop app was built in the release worktree |
| journeys | 169 tests: 135 pass, 34 fail. The failing tests are the identical set that failed at v3.18.20: no new failure, none fixed. |
| packaged-candidate smoke | pass |
| v3.14 upgrade rehearsal | pass |
| packaged upgrade | 21/21 |
| installed-app acceptance | Fingerprint-exact install of `cf3cb86d3` (daemon, desktop and phone web apps). A named fixture workflow (read one file, commit one dataset into a fixture Space, pinned goal with a "replay adds no second write" criterion) ran twice under the owner's configured checker, not failed open: every criterion passed both times, and the second run's commit returned `created:false` with one observation in total. Two read-only chat asks took 10 s and 15 s; Jev served the pinned `jev-1.13.0` for turn-start (one request, 405 ms) and completion (about 400 ms, abstaining to the reviewer). Model status reports each provider account's billing link and credit state. |
