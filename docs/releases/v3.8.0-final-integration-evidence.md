# v3.8.0 final-integration evidence (E0–E10)

Branch `codex/v3.8.0-final-integration`, base `ac9ae24c` (clean at audit).
This file records the E0 red matrix and each stage's evidence checkpoint,
updated per stage. Nothing here is a release claim until the hard release
gate at E10 is evaluated by Codex.

## E0 — the red matrix at ac9ae24c

Every independently reproduced finding is a committed adversarial test that
asserts the REQUIRED behavior and fails at the starting implementation for
the reproduced reason. Grouped by subsystem:

### Graph/replay/lifecycle — `src/runtime/graph/graph-final-closeout.test.ts` (12 red / 12)

| # | Red assertion at ac9ae24c |
|---|---|
| F1 | same-wave planner emission joins the in-flight reducer; run completes with trace `planner -> reducer -> worker` |
| F2 | changed root-input manifest reruns the planner but the old proven patch topology (`w1`) executes although the new planner emitted no patch; `run2.patches.length = 1` |
| F3 | a forged admission with `journalSchemaVersion: 999` dispatches and journals (status `completed`, 2 rows) |
| F4 | attempt ids `'   '`, `''`, 300-char, control-char all reach the journal and complete |
| F5 | waves `-1`, `2.5`, `NaN`, and a child settled in an earlier wave than its parent all reconstruct as valid history |
| F6 | mutating the caller's nested `runner.version` after sealing does not throw and drifts `nodeDigestFor` away from the frozen admission digest |
| F7 | `mode: 'production'` without semantic identity admits (structural fallback is silent) |
| F8 | `verifyArtifact` accepts a record whose `ref` differs from the requested ref and records with empty `storeContract`/`mediaType`/`commitId` |
| F9a | a renewal racing a release on one fence resurrects the released lease (fence-only CAS) |
| F9b | a reclaim landing between `holds()` and the journal append admits the superseded owner's settlement |
| F10 | the artifact commit boundary has no CommitAuthority/fence concept — post-cancellation commits are unfenced |
| F11 | cancellation journals as `failed`/`settlementClass: 'cancelled'`; `result.cancelled` is empty — no coherent cancelled status |

### Usage/trace/budget — `src/runtime/usage-final-closeout.test.ts` (8 red / 8)

| # | Red assertion at ac9ae24c |
|---|---|
| F12 | a contradictory declared-inclusive sample debits **0** uncached work (invalid = free budget) |
| F13 | `chargeOnce` identity is absent from the snapshot; a continuation re-charges the same logical retry (lifetime 2, expected 1) |
| F14 | `budget-contract.ts` contains 1 literal NUL byte (Git shows the file as binary) |
| F15 | `measure:efficiency` prints `pct(cachedInputTokens, inputTokens)` — an exclusive fixture displays 900%; no canonical prompt tokens |
| F16 | `claude-model.ts` records usage with no declared cache dialect; `guest-harness.ts` parses no Codex usage |
| F17 | no production model adapter threads a trace envelope |
| F18 | the budget meter has no `acquireConcurrency`/`releaseConcurrency`; sequential occupancy exhausts a ceiling of 2 at iteration 2 |
| F19 | `read-lane.ts` never debits `uncachedInputTokens` or `elapsedMs` |

### Read-lane authority/procedure/terminal — `src/runtime/read-path/read-lane-final-closeout.test.ts` (11 red / 11)

| # | Red assertion at ac9ae24c |
|---|---|
| F20 | a warm dispatch composes a terminal from a receipt proving a **send** on another provider/tenant |
| F21 | a lane sealed for tenant A dispatches under scope B and promotes B's artifact |
| F22 | `liveSchemaFingerprint() === undefined` warm-dispatches |
| F23 | a cold acquisition under schema `fp-A` promotes from a receipt proving `fp-B` |
| F24 | two acquisitions from one revision both mint revision 2; a binding is lost |
| F25 | `intersectWithMcpScope(lane, null)` widens to every bound capability |
| F26 | dispatch membership is a name lookup — no account/schema/effect/revision binding |
| F27 | a tampered artifact (provider/operation rewritten in place) still resolves `bound` — content identity is not recomputed on load |
| F28 | promotion is an in-process promise mutex plus separate crashable writes; no cross-process transaction |
| F29 | `compose(): Promise<string>` is terminal authority; no accepted-source commit/dedupe/delivery identity |
| F30 | a cold missing-slot acquisition persists nothing; "tomorrow" would rediscover |

### Production/continuity/activity + gates — `src/runtime/production-final-closeout.test.ts` (10 red / 10)

| # | Red assertion at ac9ae24c |
|---|---|
| F31 | zero non-test callers of the read lane — the principal feature is dormant |
| F32 | `respond-bridge.ts` never consults a shared accepted-turn read resolver (Claude SDK path bypasses any loop-only wiring) |
| F33 | `background-tasks.ts` carries no continuation capsule id — completed work is inferred from origin-history prose |
| F34 | the compiled chat fanout node still emits `multi_item_fanout_shadow_only` and hosts the old core |
| F35 | no windowing adapter above the 256-item worker schema exists |
| F36 | `surface-projection.ts` carries no privacy-safe label field |
| F37 | no Slack/Discord transport consumes the activity projection |
| F38 | the projection exports no lease/heartbeat liveness derivation |
| GATE | `hasAutomaticDataPipelineShape` still routes workload disposition (`background-promote.ts`) |
| GATE | tracked source contains a NUL byte (`src/runtime/budget-contract.ts`) |

Fixture-scrutiny notes (false greens caught and fixed during E0 authoring —
recorded because a red suite is only as good as its reasons):

- graph fixtures initially halted on `admitted run allows expansions but has
  no patch admitter`, masking F4/F5/F9/F11; budgets split into
  zero-expansion and patch budgets.
- F9a's interposition originally ran before the read (release then renew —
  correctly refused); moved between read and CAS where the race lives.
- F21/F23 initially reused one logical key across tests, so an earlier
  test's warm artifact intercepted the cold path; operations are unique per
  finding and the F21 dispatch shim stamps the run's scope as a real adapter
  would.
- F37/F38 initially matched the presentation allowlist and a comment
  containing "lease"; retargeted to the transport-projection consumer and an
  exported liveness derivation.
- The file scanner initially shelled to `rg` and swallowed spawn failure,
  which would have made reachability gates vacuously green; replaced with a
  pure `git ls-files` + content scan.

Unrelated suites at the same tree: graph closeout 18/18, resume 29/29,
usage-log 21/21, read-lane 16/16, procedure-receipts 7/7; typecheck 0
errors.

## Stage checkpoints

- **E0**: complete (this section). 41 red tests committed; no production
  behavior changed.
- E1–E10: pending; each stage appends its checkpoint here.
