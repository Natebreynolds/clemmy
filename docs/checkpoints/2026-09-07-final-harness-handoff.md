# Final reviewer handoff — complete Clem and qualify the combined release

The owner has made this the reviewer's **final handoff**. You now own implementation, coordinating the UI work, running live qualification, auditing the evidence and maintaining the checkpoint through the release decision. Do not wait for another reviewer window, ruling or scheduled poll. The reviewer's recurring monitor is paused and no reviewer live campaign or cutover hold remains. Existing authorization for scoped live tests continues; this transfer does not expand authority to send messages, change unrelated customer data or replay settled effects.

This document supersedes older coordination instructions promising continued Codex review. It is the starting point; the [detailed implementation assignment](/Users/nathan.reynolds/clementine-next/docs/checkpoints/2026-09-05-harness-refinement-agent-prompt.md) supplies the source-level correction requirements, and the [canonical checkpoint](/Users/nathan.reynolds/clementine-next/docs/checkpoints/2026-09-05-harness-refinement-handoff.md) preserves the evidence history. Maintain these documents as your work progresses. Do not turn historical failures into passes when a successor improves.

## What we are building and why

Clem should accept a business owner's ordinary instructions, find the right tools and complete authorized work with little supervision. Simple reads and edits should be direct. Substantial jobs should decompose into dependent steps and independent workers, retain their results, adapt to delivered corrections and resume after interruption. Each step can have its own correction loop; the graph coordinates dependencies, shared results and the final deliverable. Existing graph machinery should be completed and exercised, not replaced merely to fit an architectural slogan.

The product standard is **teach → perform → correct → reuse → resume**, with accurate accounts of what actually happened. Preserve native Workflow and Space creation/editing, complete schemas, useful discovery, natural history recall and delegated work. Explicit Plan must be selectable on desktop and mobile; reviewing or revising a plan must preserve its identity, and Execute must run the exact reviewed revision. Ordinary Normal work must not inherit a universal Plan gate.

Remove demonstrated wasted turns, unnecessary discovery loops and lossy context handling. Preserve durable state, access to full evidence and productive continuation. Do not remove every bound indiscriminately: ownership, account boundaries, duplicate protection and cancellation must continue to work. Distinguish an observational test timeout from a runtime limit on useful work.

Completion review is optional, supports owner-selected same-provider operation and retains honest requested/actual model identity. A judge may give actionable feedback against the effective accepted objective and an available owner-approved framework—for example, identify which of ten drafts violate the framework and explain the correction. That feedback can guide the brain's next step within the existing job; it cannot authorize new writes, override a delivered cancellation, invent a remembered framework or restart obsolete work after terminal. Turn review off without disabling deterministic execution checks or labeling unjudged work reviewed.

## Baseline you are inheriting

Last attested **2026-09-07 04:50:05 UTC**; recheck before operating because these are observations, not a permanent lock:

| Item | Recorded identity or status |
| --- | --- |
| Serving checkout | `/Users/nathan.reynolds/clementine-next-live-iteration-31` |
| C34 source fingerprint | `e5b702d0824b23e818972bbce8efc6955d65b3701191776c129b658a760357e0` |
| Daemon | PID `23567`, instance `c10daff1-4319-435c-ad07-3a8f7e38dd74`, matching disk, no active attempts at observation |
| Serving entry and home | `/Users/nathan.reynolds/clementine-next-live-iteration-31/src/index.ts`; `/Users/nathan.reynolds/.clementine-next`; local port `8520` |
| Serving Git state | Dirty `9cdf31fc39523eadeeb15e9be18dfa54165d58a9`; version `3.16.0`, schema `80` |
| Original/UI checkout | `/Users/nathan.reynolds/clementine-next`, HEAD `c7e9454ff84ea2ab136321d8247f5c60110ecad8`, including intended dirty/untracked UI work |
| Latest handover | [C34 HANDOVER-25](/Users/nathan.reynolds/clementine-next-live-iteration-31/output/candidate34-live/HANDOVER-25-READY.md) |
| Review scope | Four implementation sources audited; zero new reviewer live calls; historical reviewer total remains 30: 22 C30 + 8 C31 |
| Release | **Not qualified.** All 18 declared C34 manifest hashes matched; that is identity evidence, not whole-product qualification. |

The [C34 review](/Users/nathan.reynolds/clementine-next/output/reviewer-monitor/2026-09-07-c34-review.md) and [four-source live audit](/Users/nathan.reynolds/clementine-next/output/reviewer-monitor/c34-review-0438/live/review.md) distinguish each intermediate build. Key results:

- **142281: real scope failure.** A CREATE request received a known duplicate, then `workflow_update` changed an existing workflow without another owner instruction. Receipt 142367 proves a 769-byte file became 755 bytes, changing only its description. The final reply disclosed the update truthfully and completion review certified its current content. Neither grants authority for the update. Two more completion requests started after terminal.
- **142450: real continuation failure.** An intermediate C33 build closed its attempt before 14 later parent generations, produced five child-lease refusals and used 24 Terra plus 27 Opus calls. This is retained failure evidence, not proof that the final C34 path has been exercised successfully.
- **142715: limited duplicate positive.** Final C33 ended with a truthful question and no write, but used Plan, had an initial schema refusal and no judge call. It is not a clean Normal-path pass.
- **142822: limited restart positive.** C34 resumed the same accepted source across a process restart and produced one unrun workflow, zero Plan/child refusals and positive completion review. Interruption preceded any retained hold or effect. The driver failed its blanket unchanged-instance assertion; preserve that failure and the independently proven narrower restart result. Held-window and post-effect recovery remain unqualified.

Retain the nominal known-non-write/uncertainty correction supported by 13 earlier pure controls and cold recall's real assistant-answer/frontier correction supported by the earlier 4/4 isolated checks. Those checks were not rerun in this final review and do not establish unlimited recall.

## Work sequence and ownership

Own one coherent correction and qualification program. Parallelize independent work with explicit file ownership; one integration owner changes the serving daemon or shared settings. A helper patch plus one healthy creation is not completion of the batch.

**1. Bind every mutation to the accepted job before dispatch.** This is the first priority. Current graphless consent derives coverage from the proposed call itself, then checks agreement. The call can be correctly source-bound and schema-valid while performing an unrequested update. Reuse the existing consent/handoff boundary to compare typed operation, target, destination posture and allowed change against independently accepted, source-owned scope, including adopted owner amendments. The scope producer must not manufacture permission from the proposed tool call.

Preserve legitimate explicit edits, requested mixed create/update jobs and editing an artifact created by the same accepted source, using per-operation targets and dependency lineage. Keep authoring siblings discoverable. Do not fix this with English keyword matching, a universal Plan gate, a same-family permission grant or a global ban on updates after creates. The [native audit](/Users/nathan.reynolds/clementine-next/output/reviewer-monitor/c34-review-0438/native/report.md) traces `host-turn-runner.ts` → `local-planning-capability.ts` → `host-interactive-consent.ts` → `host-consent-evidence.ts` → `interactive-consent-policy.ts` → `work-call.ts`.

Prove the actual accepted-scope producer → nomination → consent → dispatch chain rejects CREATE-existing → duplicate → unrequested patch with unchanged bytes, while separately explicit Normal edit makes exactly one correct write. Include legitimate same-source mixed work and uncertain-effect reconciliation. A fixture that writes matching permission from its own proposed call is not a discriminating test.

**2. Complete durable continuation ownership in the same batch.** Failed claims now stop adoption; installed saves claim responsibility; matching-source terminal cleanup is transactional. Four of six callers still discard the save result; save and claim are separate writes; initial rejection can return held into an outer retry. Finish consistent outcomes through save, claim, adoption, caller return, desktop finally and terminal. Validate current accepted source/session/unfinished attempt and terminal eligibility; numeric ordering alone admits stale same-source replacement and post-terminal reinstallation. Fix undefined-load and approval-resume branches. Retain counters across adoption and one owner between timer and immediate continuation. Stop obsolete reviews and execution at terminal without weakening the finished-attempt lease guard.

Use production-boundary regressions for competing valid attempts, rejected saves/claims/adoption, terminal races and held desktop completion. Manual helper tests that still pass without the production change do not prove it. The [lifecycle audit](/Users/nathan.reynolds/clementine-next/output/reviewer-monitor/c34-review-0438/lifecycle/report.md) contains the six-call inventory, source hashes and focused controls.

**3. Finish native contracts and exact Plan revision/Execute.** Audit all five model-visible schema carriers against actual callable definitions and name them in the next inventory. Preserve complete schemas and distinguish one discovery from subsequent schema-page reads. Make authoritative exact-slug reads fail clearly when absent; fuzzy suggestions cannot select a different artifact for read/edit. Support native reads in compound work without blanket-widening deliberate control-reader exclusions or fingerprint-bound `projectEffect`. Address `workspace_list` discovery.

Plan revision must reopen full saved executable content and `base_ref_json`, preserve plan lineage and unrelated arguments/layout/bindings, and Execute the inspected plan ID/revision/digest. Prove new Plan → one-field revision → exact diff → Execute, including Stop/rejoin. No C33/C34 work closes this requirement.

**4. Close learning/review continuity and the integrated owner experience.** Complete real producer → persistence → reopen → learning proof, owner-selected judge provenance across actual production callers, captured policy on interrupted judging, fallback identity and usage/request linkage. Prove correction reuse in an unseen fresh session and natural recall without operator warming. Keep accepted steering and task-specific exceptions distinct from durable instructions or untrusted imported text. Test review-on, review-off and deliberately selected same-provider operation; configuration labels and unit routing tests alone are insufficient.

Use the [UI assignment](/Users/nathan.reynolds/clementine-next/docs/checkpoints/2026-09-06-ui-integration-agent-prompt.md) as a required integration checklist. It retains concrete Stop, stale/mobile session, offline, history, needs-input/resume, Open run, microphone-copy, notification-delivery and relay findings. Reconcile those with actual UI-agent changes; earlier findings remain open until their changed bytes and behavior are checked.

## Run qualification yourself after the connected fixes

The owner has transferred the live window to you. There is no reviewer prerequisite. Recheck current activity, freeze the corrected bytes, run supported focused checks in an isolated environment, and record the actual source/build before testing. Use separate homes for settings changes, denied resources and controlled restart; do not rebuild shared dependencies under the serving candidate.

Follow the [expanded live qualification document](/Users/nathan.reynolds/clementine-next/docs/checkpoints/2026-09-06-expanded-live-qualification.md): all 12 core journeys, roughly 30–40 accepted turns as a coverage estimate, not a quota. First run the affected scope-negative/explicit-edit pair, native create/read/edit/readback, held continuation and exact Plan revision/Execute. Then complete all runnable review-policy, repair, post-effect restart, recall, learning and delegated-work cases. Run independent ready lanes while another has a named prerequisite. Use fresh natural wording and plausible distractors; do not put expected tool selections into the owner's request.

Use the successor's actual `/Users/nathan.reynolds/clementine-next-live-iteration-31/scripts/live-foreground-proof.mts` or the corresponding path in your next candidate, after checking its current arguments. Existing mechanics and isolated proof runners are described in the expanded document. Negative/pause/restart expectations need explicit driver support; do not catch an exception and label it success. Expected restarts must assert stable code/home/source/effect identity and the intended instance transition, rather than weakening all identity checks. Keep healthy Normal performance bounds distinct from repair/recovery cases and never silently loosen a failed assertion.

The chosen cross-provider qualification is **Terra `gpt-5.6-terra` with Opus `claude-opus-5`**. Resolve supported provider routing in the current candidate and verify actual route/purpose evidence. A watcher or account-selection call is not completion review. Deliberate same-provider and disabled cases are separate product requirements; silently substituted models or fail-open outcomes cannot qualify the requested Terra/Opus case.

For every first attempt and repeat, retain fixture digest, build/entry/instance, source/session/run and call IDs, accepted objective/amendments/policy, actual brain/judge identities, model starts/outcomes, tool searches/calls, Plan/refusals, attempted and successful effects, receipts and independent current readback. Preserve edit preimages/diffs, immutable terminals, latency and failures. Unknown measurements are unknown, not zero. Report both task outcome and evidence completeness. A terminal `done`, a positive judge or a returned success flag alone is insufficient.

The [Sheets → Salesforce → SCO scenario](/Users/nathan.reynolds/clementine-next/docs/checkpoints/2026-09-06-business-owner-sheets-qualification.md) remains unqualified. The exact SCO source, test sheet/accounts and ranking criteria need owner input if unavailable from current context. Prepare independent work while obtaining those inputs. Use only authorized test destinations; local/stub data is not live connector proof. Verify complete pagination, account/row identity, unchanged existing formulas/cells, written-range readback, supported ranking and recovery without duplicate rows. The observed local native defect neither proves nor disproves the connector path's authority checks.

## Preserve evidence and finish the release responsibly

Do not replay old C10 Outlook drafts, execute the incomplete C10 mobile Space plan or the failed Cedar plans (140854/141337), or restore the overwritten `clemmy-frozen-c31-0907` to conceal source 142281. Keep both preimage and changed receipt. Preserve historical failed sources 138644, 139952, 140867, 141915 and 142450. Reconcile uncertain effects before any retry; a fresh equivalent write cannot repair an old assertion.

Combine the intended harness and UI changes in an isolated integration candidate, inventorying dirty/untracked work and overlapping files. Preserve candidate Plan/Execute transport and persisted review fields while incorporating UI improvements. Do not copy either tree wholesale or tag the original stale checkout. Rebuild backend and both web clients with independent dependencies, then render-qualify desktop and actual mobile controls on that combined identity. API tests do not prove phone rendering, native permissions, push delivery or reconnect behavior.

The final dependency inventory found C34 root, mobile and console `node_modules` symlinked into `clementine-next-live-iteration-12`; desktop dependencies are absent. Install independently in the integration target's root, desktop, mobile and console packages. Packaging rebuilds native modules and must not operate through the serving candidate's shared dependency links.

Run the current repository's release instructions, required checks and clean committed-candidate/install/upgrade requirements against the intended release artifact. Record exact commit, source/package fingerprints, generated assets, version/tag choice and upgrade results. If behavior changes after qualification, renew affected checks on the final bytes. Do not invent a tag name, weaken release checks or silently defer a stated blocker to claim success. Follow existing owner authorization for release actions; any unresolved release decision goes directly to the owner with a concrete candidate and evidence, never to this retired reviewer.

The [release workflow](/Users/nathan.reynolds/clementine-next/.github/workflows/release-desktop.yml:151) specifies independent installs, `npm test`, `journeys`, `test:measurement`, `proof:selftest`, `check:public-hygiene`, `test:public-hygiene`, `test:release-assets`, `test:release-closure`, `prepack`, `test:packed-candidate`, `test:packaged-upgrade`, `typecheck`, `bench:gates`, `eval:memory` and strict `eval:passk`. Follow the complete current workflow, including any later steps and platform requirements, not this excerpt alone. These repository checks supplement the live/rendered campaign. Record the exact passing commands, environment and exit results; do not claim them run from this handoff.

The older [next-tag gate](/Users/nathan.reynolds/clementine-next/docs/NEXT-TAG-RELEASE-GATE.md) retains an August date and defers some mutation canaries. Its clean-candidate, upgrade and tag procedures still apply; that old date/deferral does not waive the current owner's explicitly requested live native, authority, continuity, Plan and integration qualification. Confirm intended version/platforms: root and desktop versions must match the stable tag on the required `origin/main` commit. Settle signing prerequisites or an intentionally selected mac-only release before packaging; do not silently change platform scope to bypass a failure.

Maintain one final scorecard with every requirement marked **implemented, unit-proven, live-proven, failed, blocked or not run**, including build and evidence links. Report user-visible limitations plainly. Quality determines readiness; the time budget does not qualify a release. Finite tests cannot promise perfection or superiority over other harnesses. The desired release claim is demonstrated useful work, correct authority, durable continuity and a premium integrated experience within the scenarios actually proven.

Your next update to the owner should name the connected batch, its production-boundary evidence and the broader live campaign results. Continue through integration and the release decision without another reviewer handoff.
