# Harness latency refinements — September 8, 2026 Pacific

Harness ownership remains `/Users/nathan.reynolds/clementine-next-post-3161-review`, branch `codex/post-3161-refinements`. UI work belongs to the other agent. No version bump, tag, push or UI edit is part of this pass.

## Evidence behind the changes

The preceding clean live local edit, source 166393 on `bd9b6693`, took 44.5 seconds: five brain requests, four tool calls, one mutation, zero Plan and zero refusals. Its second call searched for “write or edit a local file, save text to disk.” Although a native write_file result was found, the search also resolved Google Drive and Slack writes and reviewed their accounts. That search occupied 13.3 seconds and grew the next prompt from roughly 17K to 29K input tokens. No provider write occurred; the extra discovery and reviews were preparation for unused alternatives.

The same turn recorded two auxiliary responses twice. Provider records had response identities and cache evidence; semantic-port aggregate records repeated their tokens anonymously a few milliseconds later. Both producers charged `sessions.tokens_used`. The duplicate account review rows were 2,435 input / 115 output and 2,463 input / 99 output. Raw evidence remains in `output/post-3161-live/argument-repair-live-usage-summary.json` and the exact live report. Recorded row counts and token totals from that earlier report must not be presented as distinct requests or a clean cost benchmark.

Earlier website turns repeatedly guessed list_files(path), whereas its actual schema requires directory. The schema was deferred and the prompt carried names only. These were recoverable refusals, but each consumed a model step.

## Refinements

1. **Native file inspection carries its actual schema.** workspace_roots, list_files and read_file join the existing acquisition tools. The orchestrator no longer strips an already-hot registered read merely because it is classified as business work. It still intersects with the configured tool scope, exclusions, current source, actual schema and Plan execution contract. Writes retain work_call. The directory-listing description no longer requires a preflight listing before reading a known path.

2. **Native authoring tools are visible by name.** Fresh planning contexts previously showed a native-read inventory while omitting the corresponding authoring inventory. The new catalog derives the available native authoring names from the configured registry surface. It explains exact native schema lookup so a local file edit need not be described as a broad connected-app search. This is presentation, not permission, a provider blacklist or task-keyword routing. Existing provider discovery remains available for work that needs it. The call_tool description now points repairs at the schema already returned instead of requiring another search.

3. **One completion has one usage producer.** Each tool-less semantic role attempt observes whether its actual model adapter recorded usage. If it did, the semantic wrapper keeps the adapter's receipt and does not append/debit an aggregate copy. An adapter without accounting retains the existing aggregate fallback. Observation is scoped to the individual async role attempt, so parallel calls and failed-role fallback cannot inherit a sibling's recording fact. This does not suppress model calls, alter verdicts or increase a budget ceiling.

## Controlled validation

- The actual production Plan journey lists and reads fixture files directly with no discovery or refused call, then asks the requested question. Wrong-field, forbidden-write, repeated-recovery, and accepted-source controls remain. The intentionally unpublished-reader fixture now uses skill_read, because list_files is deliberately published; the test still asserts that the chosen control is absent from the advertised surface.
- Surface tests verify the real directory schema, native write_file inventory, and exclusion/allowlist behavior. The schema-count fixture accounts for the three newly visible readers; this is a test expectation, not a changed runtime cap.
- The real semantic Runner plus raw Claude accounting wrapper persists one usage record and debits 110 uncached tokens for 100 input / 10 output. Its adapter-free control still records once. Temporarily disabling the consumption of the adapter receipt reproduces two rows and fails the test. Parallel and failed-scope controls preserve independent recording ownership.
- Related host, native, semantic, usage, budget and catalog batch: 353 passed, zero failed; typechecking passed. The subsequent wording-only tool descriptions are included in the final full run.

The first full attempt (`latency-full-suite.log`) was stopped after six stale presentation assertions: two expected the previous schema count, and four compared combined catalog telemetry with only the reader block. The corrected tests account for exactly the three added reader schemas and parse/count both inventories. They still check scope exclusions, unchanged authority when instructions render, no direct authoring exposure, and the existing schema-token bounds. All 11 cases in the two affected files passed (`latency-catalog-fixtures.log`). This stopped attempt is not a full-suite pass.

The final full suite, builds and live cases are recorded under `output/post-3161-live/latency-*`. Read completed reports and the final qualification record for exact candidate fingerprints and results; this checkpoint does not claim an unfinished run passed. The prior 15,459-test suite predates these refinements.

The completed run on `1a757dae` (`latency-full-suite-final.log`) reported 15,472 passed, six failed, two skipped in 1,326.6 seconds. Five failures still expected append/overwrite to be irreversible after the recoverable file-revision implementation; one expected the standard action surface to hide registered reads. The revised fixtures retain separate mode identities, admit recoverable revisions, and use workflow deletion as the destructive counterexample. They also explicitly keep write_file behind work_call while publishing available readers. The only runtime-source edit in this follow-up corrects an outdated explanatory comment; it does not change the consent reducer. The first focused rerun exposed my own mistaken expectation that the cold user_profile_read schema was loaded; it remains deferred, whereas workspace_roots is available. The corrected six-file batch, including actual file-writer and native-write behavior, passed 56/56 (`latency-final-expectations-2.log`). A subsequent full run must be read separately; the six-failure run is not called green.

## Live qualification and remaining limits

The live comparison uses natural requests with expectations declared before submission: a cold native read, a local draft revision, a same-session follow-up revision, a saved manual workflow run, and read-only connected-capability inspection. No outbound message, provider file write, paid generation or public deployment is authorized by these cases. File revisions are checked against captured prior bytes and current artifact receipts.

The owner's additional North Star request adds a separate isolated live-model journey: one synthetic social-metrics MCP connection, a terse first request, a same-session yesterday follow-up, explicit teaching of a reusable reporting procedure, then daemon restart and a new session. Today's fixture data changes from 90 to 110 engagements before the cold recall; yesterday remains 60. The test must distinguish remembered procedure from stale numbers, inspect durable learning evidence, and check that a useful suggested next step does not become an unrequested post. It uses real Opus 5 through durable HTTP ingress, with owner-selected same-provider review and no copied owner memory or external-app credentials. This qualifies the harness against a simulated connector, not Facebook itself. The predeclared protocol and results are `output/post-3161-live/social-metrics-spec.md` and `social-metrics-live-report.json`.

Sequential timings are observations, not randomized benchmarks: cache warmth, context and provider latency vary. Compare brain requests, actual tool searches, refused calls and useful effects alongside elapsed time. Accounting corrections are not API latency savings.

Earlier open issues remain in `2026-09-08-post-3161-refinements.md`, including focus pause lifecycle, stale capability claims, prompt/cache duplication, and cancelled-worker/async ownership. Full combined UI/release qualification still needs the final merged candidate; a harness-only pass cannot stand in for it.

## First-use connected read: a real discovery failure, then a structural refinement

The five owner-home cases passed on frozen `bd1f56b0` (fingerprint `ead3be87c6e20ef479fe6b0baaf7014129084f7e829a1d4495e7b60c0b959368`): native inspection 22.5s / 3 brain requests / 3 calls; native revision 31.4s / 5 / 4; same-session revision 23.3s / 3 / 2; explicitly requested manual workflow 34.5s / 3 / 2; connected capability inspection 35.0s / 2 / 2. All five had zero Plan and zero refusals. Revisions each wrote once and preserved all other bytes. The final capability case exercised two account reviews, with five distinct provider response records (two brain, two account reviews, one completion review) and no aggregate duplicate. Evidence is in `latency-file-artifact-proof.json`, the five `latency-*-report.json` files, and `latency-account-review-usage.json`. These results belong to that candidate, not a future merged tag.

The additional isolated social first-use test failed on that same candidate. Its one MCP reader was connected and its tools/list schema was available, but “social media post engagement metrics likes comments analytics” did not disclose it. A second query naming the server also missed it. Source 1 in `sess-desktop-67b431de65b1503cb710e713` ended blocked after 26.0s, six brain requests, eight calls, three searches and one repaired argument refusal, with zero metrics reads. The subsequent follow-up and learning cases did not run. Original evidence is retained in `output/post-3161-live/social-failed-bd1f56b0/`; this is not a successful Facebook test.

The cause was the use of automatic read acquisition as an ordinary search filter. Its advisory nomination requires every meaningful query term in the tool metadata, then exactly one matching operation globally. Those are automatic-selection constraints, not reasonable requirements for showing the model its connected choices. Ordinary discovery now enumerates authorized native MCP metadata alongside a disjoint reviewed-CLI registry. Selected MCP candidates enter the existing read materializer by exact operation identity, preserving its account, schema, effect, source-bound authority and independent execution observer. The automatic acquisition rules themselves remain unchanged. Discovery does not execute a business call, and showing a candidate does not grant permission.

The production foreground journey now searches with extra natural-language words absent from the reader metadata, then completes both pages through the original provider-neutral carrier. A separate two-reader case asserts both choices have distinct valid references with zero business dispatches. Denied MCP scope still starts no server; a reviewed CLI proof cannot be relabelled as native MCP. The journey also exposed an existing terminal projection inconsistency: downgrading a completion after a failed review kept an upstream legacy reason of success. The projection now derives that label from its actual blocked outcome; review policy and execution permissions are unchanged. The four-file journey/committer batch passed 28/28 in `social-discovery-after-3.log`; the acquisition, native carrier, scope, search, relevance, nomination, deferred-page, independent-publication and account-evidence batch passed 150/150 in `social-discovery-focused.log`. Typechecking caught an optional search deadline passed into the required page-preparation field; the exact-name path now preserves the existing no-deadline value explicitly.

The new isolated live rerun, follow-up, teaching and cold recall must be judged from their completed reports. No improvement in those cases is claimed before the real model runs. Full-suite status remains separate from focused checks.


## Follow-up continuity after ordinary reads

The fresh social question passed on `dd2626e9`: 90 current engagements from the actual synthetic MCP reader, 20.2 seconds, four brain requests, four calls (two connection-status reads, one search, one business read), zero refusals, zero Plan, and a positive same-provider Opus completion review. Its five exact usage records include the review; total input was 128,387 tokens, 35,997 cached. First-use status probes and prompt size remain efficiency opportunities.

The same-session “What about yesterday?” then failed the task assertions despite a terminal labelled done: one model request, zero calls, and only “On it — pulling Sept 7.” The trace proves the host classified this as plain conversation and removed every schema. `sessionHasPriorRetrieveOrAct` only consulted `turn_graph_compiled`, although ordinary reads deliberately no longer create a graph. It now also consults the same session's earlier durable business settlements. Discovery/control rows do not establish that history, and the current/future source cannot become its own predecessor. This keeps the model's ordinary tool surface available; it authorizes no write, invents no graph, and adds no model routing call.

The real two-page foreground fixture reproduces the missed follow-up after database reopen with the refinement neutralized (`social-followup-before.log`). With the refinement it passes, including current-source and unrelated-session controls. The related graph, conversation shortcut, and control-settlement batch passed 28/28 (`social-followup-after.log`). Bare greetings and closed-world arithmetic retain their existing shortcut.

The teaching turn on `dd2626e9` failed before any tool call. The raw Claude adapter logged a brain rate limit and the existing fallback router's 600,000ms pause. It is not evidence of successful procedure storage or a diagnosed skill-distillation defect. Cold recall was not submitted. All prior first-use/follow-up/teaching evidence is preserved in `output/post-3161-live/social-failed-dd2626e9/` and the sanitized proof home. Subsequent live qualification must use a new isolated home after the provider cooldown, retaining the original assertions.
