# Beta stopping checkpoint — 2026-09-08

The owner requested a stopping point: commit the current integrated work, freeze the tree, then run one real chat turn with Opus 5. The next agent owns the release/tag decision. This checkpoint does not certify the broader release gates as passing.

## Included work

- Desktop Chat is first and permanently visible; the default entry opens Chat or the previous conversation while retaining explicit Home/project preferences.
- Integrated desktop/mobile chat, history, task-mode and completion-review work is retained alongside native workflow/Space and source-owned recovery/evidence changes.
- Focused takeover fixes preserve selected memory context, keep the leading discovered schema usable, retain worker completion/no-repeat guidance, and allow the existing unused repair opportunities to run. Compacted history retains exact result-recall addresses and durable bytes.
- Generic discovery ranking retains the actual Airtable operation and native Space/workflow regression controls. No query-specific operation grammar was added.

## Evidence at stopping

- The final focused discovery, schema, compaction and release-note group passed **135/135**. Backend TypeScript checking passed.
- The earlier memory/ranking/context group passed **326/329**; its three failures were the release-note heading, compaction overhead and Airtable ranking. The final focused group above covers those corrected production areas; the full broader group has not been repeated.
- The latest affected journey group passed **58/72**, with **14 failures**. The 100-item worker journey passed its full end-to-end assertions. The group is not green.
- The previous full unit suite had **15 failures**, and the previous complete journey suite had **30 failures**. Their original logs remain preserved. Subsequent focused passes are not a substitute for a fresh full-suite result on this commit.
- The earlier packaged smoke passed on candidate06; later source changes mean it does not qualify this frozen checkpoint.
- The five requested business cases on each of Terra and Grok have not been rerun on this checkpoint. Candidate07 fixtures are prepared but unsubmitted.

## Unfinished work for the next agent

1. Sheet fixtures must consume exact, source-disclosed definition-scoped capability references rather than hardcoded bare references. Cold database resets must also reset the cached manifest store. Read retained full tool output by its exact call identity rather than parsing clipped activity text. Preserve current-source authority and all actual provider/readback assertions.
2. The foreground read fixture incorrectly expects the public completion reason `blocked`; the producer reports `success` with a separate truthful unavailable-review disposition. Restaurant fixtures still fail before their new completion/replay/learning assertions because they expect bare write references.
3. Provider-neutral fixtures still need grounded follow-up on the named-dispatch `business_call` classification and the measured three-call repair exhaustion. Do not change expectations solely to make the group pass.
4. Account-review failure caching and misleading account-selection recovery remain open. Their production patches are **unapplied**. The reason proposal also needs mixed-operation coverage. The intentionally failing standalone producer control was preserved in local output rather than committed without its implementation.
5. Repeat required build, package, release and live qualification against the exact intended release commit. The large-data CPU follow-up and unqualified later restaurant surface/evidence cases remain documented.

## Local handoff evidence

`output/takeover-stop-handoff/` contains the detailed agent findings, typecheck and emission logs, preserved deferred-path hashes, final commit/fingerprint record, and the requested Opus chat report once run. `output/takeover-release-gates/` contains the original failure logs. These are local evidence, not published package content.

Private investigation notes and two deferred operator utilities remain locally preserved outside the commit. The session-pruning SQL was not executed. No release tag or remote push is part of this stopping checkpoint.
