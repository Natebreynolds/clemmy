# Plan publication ownership — 2026-09-13

The owner requested focused repairs and a completed research Google Doc through Clem's Plan → Execute path. Do not add new planning gates, task-specific vocabulary, or another orchestration layer. Preserve the existing Home/UI and Claude support. No commit or tag is requested.

## Measured failure

Source 201259 in session sess-mob-68e8c5c64cb28705e5ae5ef4c608ee5b was cancelled by the owner after repeated publication recovery. Its second publish_plan started at 00:26:31 UTC on September 14. The local 60-second tool deadline expired while the selected Grok completion review continued. A positive verdict arrived roughly 90 seconds after publication started, and the abandoned invocation then saved immutable plan bytes. The brain had already moved into recovery, so later candidates collided with that saved revision. The immutable-revision check itself was correct; publication outliving its invocation was not.

An authenticated first-request read also proved that the host injected a mandatory proposal-builder /build-brief route based on keyword matches. The user asked for competitive research and a Google Doc. The project hint was useful only if relevant; it had no basis to overrule that destination or advertise unavailable project_run start arguments.

Evidence is preserved under output/reviewer-monitor/2026-09-13-planning-review-refinements/live-201259 and output/reviewer-monitor/2026-09-13-plan-publication-owner. The cancelled plan remains untouched and is not the candidate to execute.

## Focused changes

- The existing host invocation pauses its local tool timer only while its publication model review runs, then resumes the remaining budget. Caller cancellation and kill ownership remain active. No global timeout increase or second model conversation.
- Plan publication checks the current cancellation signal and existing dispatch lease after asynchronous preparation/review and before retaining or publishing bytes. A cancelled invocation cannot publish a late positive verdict.
- If the same source already owns a published revision, a retry returns that exact saved receipt without judging a different draft or replacing its immutable bytes. The existing terminal path delivers the stored plan text.
- Early graph errors retain the complete draft, allowing targeted repairs after SQLite reopen. A step patch can remove forEach with null when changing interpreted repeated work to a single bound batch input. Existing collection validation remains intact.
- Project procedures are advisory candidates in both shared and Claude context rendering. Their purpose must fit the current objective; keyword relevance grants no authority and cannot introduce a mandatory deliverable.

Changed ownership seams: src/runtime/tool-abort-context.ts, src/runtime/harness/host-tool-invocation.ts, src/runtime/harness/plan-publication-review.ts, src/tools/publish-plan.ts, src/runtime/harness/context-packet.ts and their tests.

## Verification before build

Typecheck passed. The focused and adjoining Plan/Execute, deadline, evidence-review, worker, and hotpatch regression set passed 169 tests with zero failures or skips. It covers slow review beyond the local tool deadline, caller cancellation during review, no late publication, saved-revision recovery without another review, early cyclic graph repair after reopen, and the actual interpreted-collection repair shape from the failed live run. No live success is claimed by these local tests.

Tests used disposable homes. The runner's full production-home immutability sentinel was not performed because the live daemon owned that home; do not claim otherwise.

## Live qualification

Build once after this document, hotpatch the idle installed app, and verify its source fingerprint. Run the research Plan with the selected Terra brain and Grok judge, inspect the saved plan, then execute its exact revision to create the Google Doc. Re-open the final document and assess coverage of the source brief's five research categories, Legal/Home Services, competitor matrix, strategic opportunities, and concrete source evidence. Record model calls, review outcomes, mutations, timing, and any limitations. Keep post-build evidence in output/ so the source fingerprint stays stable.

## Live finding and focused handoff correction

On fingerprint 525692ba..., source 201494 published a ready plan in 342,264 ms. Its successful publication waited 85 seconds for Grok and returned through the original invocation without timing out, saving late, or colliding. Two preceding argument failures repaired from retained draft digests. Nine Terra requests, 19 top-level tool calls, zero business mutations. One Grok completion approval; other Grok calls served account/trajectory checks. This proves the publication-lifetime repair, not a clean first-attempt plan.

Execute source 201697 then exposed a separate ownership gap before any business writes. The current registry contained the exact approved provider tools, but they were omitted from the new source's small planning card. Revalidation checked their canonical identities without attaching their selection to this source. A subsequent exact tool_search returned an already-live ref but the live-entry disclosure branch neither staged that descriptor nor included it in the display repack event. plan_task therefore reported missing write authority repeatedly. The watcher incorrectly interpreted these required activation attempts as replanning and steered away from them. The reviewer cancelled the attempt to preserve the failure and avoid more loops; the cancellation terminal misleadingly described uncertain tool effects during a discovery call, while no external-write settlement existed. That terminal wording remains an observed limitation.

The follow-up uses the existing disclosure and revalidation path: exact live selections enter the source's staging ledger and carry their descriptor; current reviewed provider identities are attached by exact capability reference after canonical/account/schema comparison and before initial activation. Already-published definitions remain revalidation-only rather than being republished from a nonexistent new resolution proof. An active graph keeps its existing source authority on revalidation. No business dispatch occurs during this process. Provider identity or account drift still fails revalidation. The adjoining provider/native MCP/Plan/Execute/selection set passed 92 tests; additional publication and invocation regression results are stored in output/.

A cancelled Execute revision cannot be relaunched by the existing ingress contract. Preserve its receipts; use a newly reviewed revision for the continued live qualification, rather than deleting claims or replaying cancelled source state.

## Machine restart and retained planning recovery

The Mac restarted on September 13 at 18:18:43 PDT. Uncommitted source, installed 6c8e85b3... daemon bytes, logs, the first approved plan and the next preparation draft survived. LaunchServices initially rejected the modified outer app signature. The app was signed again with its existing Developer ID, preserving its identifier, entitlements, requirements and flags; codesign verification passed and the original app launched. No quarantine removal or ad-hoc identity replacement.

The daemon automatically resumed source 201813 in session sess-desktop-cfe63e360af377b7f9d32675. It recovered the conversation and draft, then stopped at 202106 with a partial revision 2 (digest 1ce96ebc...). The source's retained Composio disclosures still described the selected Docs and Apify definitions, but the new process catalog no longer contained their callable entries. Fresh DataForSEO search reacquired its native reference; retrieving a schema cursor alone did not. The reviewer sent one diagnostic steering note at 202069, delivered at 202078, so this resumed attempt is not unassisted qualification. No research Doc was created. Preserve the partial artifact and the failed/cancelled attempts.

The narrow restart repair runs during preparation only when a selected callable is missing. It reads the exact same-source retained discovery and uses existing selected-definition provisioning to revalidate the current provider schema, operation identity, verification contract and connected account before restoring the callable. Cancellation and dispatch-lease ownership still apply. Warm callables add no metadata request. It does not execute the selected operation, invent a capability, ask for new approval, or substitute another account. This repair currently covers retained Composio selections; native MCP materialization retains its existing path.

Three regression cases use the real provisioning/disclosure producers, clear the process catalog, reopen SQLite and prepare the retained selection: unchanged definitions prepare successfully without model discovery or business I/O; changed schema and changed account refuse specifically. Cross-source and cancelled attempts restore nothing; already-live restoration adds no metadata request. The initial attempt to extend a legacy hand-built provider fixture was invalid because its fake invoke-port/readiness setup did not reproduce production provisioning; those experimental extensions were removed. The production fixtures and adjacent publication/Execute suite pass 50 tests, zero failures/skips, and typecheck passes. Logs: /tmp/clem-restart-planning-final-tests.log and /tmp/clem-restart-planning-final-types.log. These checks do not claim a completed live research document.

The next live window must publish a ready successor of partial revision 2, then Execute that exact new reference. Do not execute the partial revision or clear the cancelled revision 1 claim. Verify the final Google Doc and distinguish framework repairs, model mistakes and source-data limits in the outcome.

## Ready revision 3 and the next measured execution failure

Fingerprint 59965bbc... published ready revision 3 on the first publish_plan call: source 202117, terminal 202197, about 176 seconds, four Terra requests and zero business mutations. Grok's completion call used 22,827 input tokens and about 81.5 seconds; the other Grok call used 7,188 input tokens for account review. The primary brain carried 135k–148k actual input tokens because this conversation contains several earlier failed/repaired plans; those are per-request tokens, distinct from judge input or aggregate usage. This is a remaining efficiency concern, not proof that the judge saw that entire history.

Execute source 202198 activated the exact plan on the first plan_task, read the brief, and recorded prepare_search_protocol. The first DataForSEO collection member was refused before dispatch. The generic settlement detail sibling_frame_replanned_before_dispatch hid the decisive consent diagnostic: bound_catalog_risk_projection_current_definition_unavailable. The manifest, prepared binding, argument binding and plan activation were present. The native independent observation uses a 60-second freshness window; the review alone had exceeded it. Consent had no async refresh before its synchronous risk loader, while physical invocation's later freshness preparation was unreachable behind that refusal. The reviewer cancelled after the model started repeating the same already-completed plan_step_result. No SERP/crawl request or Google Doc creation occurred. Preserve the cancelled source and revision 3 claim. Evidence includes restart-execute-live.json, execute-request-audit.json and failed-native-manifest.json under output/.

The next focused repair adds an async entry to the existing catalog risk loader, used by prepared-call consent. Only a native MCP current-definition-unavailable result, after exact manifest/catalog/schema checks, can refresh the attested metadata observation once. The existing synchronous loader then reopens every check. A fresh contradictory observation is not erased by a fallback; an unavailable connection, definition drift, account drift or catalog removal still refuses. Warm evidence adds no request. The selected operation is never invoked by this refresh and no new consent gate is added. The old synchronous loader remains available to existing pure consumers.

The risk/consent/native-carrier suite passes 66 tests, zero failures/skips; typecheck passes. New cases reproduce an observation unavailable after an 81-second review and cover same-definition recovery, schema/account drift, catalog removal, outage, invalid binding, and zero business invocations. They qualify the refresh/recheck behavior locally, not the live transport or finished research document. Logs: /tmp/clem-native-consent-refresh-tests.log and /tmp/clem-native-consent-refresh-types.log. The next live run must verify both the first research call and the completed document.

## Account clarification after the native-refresh patch

Installed fingerprint 8f8c803bdac9155421a6dde490033193174e8fd946a5bab1d728531eea35fffd. The next Plan was branched by ingress to sess-branch-2fccc00a2dadb8c0db15f566107c04a49cf7d154, accepted source 202346. It stopped needs_input at 202494 after 202 seconds, nine Terra requests and zero business mutations. Native discovery initially returned unsupported_unmaterialized; the research-call freshness repair has still not been exercised live. Preserve native-refresh-plan-live.json and its failed assertion.

The reviewer sent one account reaffirmation at 202423, saved in native-refresh-account-steer.json. Clem received it, acknowledged the existing account, and quoted it exactly in discovery. Routing nevertheless returned quote_not_in_source repeatedly and asked the same account question. This is an assisted failure, not a passing qualification. The router's acceptedSource read only user_input_received, while the brain and completion judge already incorporated delivered same-source steering.

The focused correction reuses adoptedSteerNotesForSource and objectiveWithAdoptedSteering in account routing. The text and digest include delivered owner clarifications, so cached routing decisions are rechecked when that context changes. Queued notes, later-source notes and unrelated-session notes remain excluded. This supplies account context only; the existing connection checks and write review remain. The account-routing suite passes 30 tests, including clarification across SQLite reopen, queued/delivered distinction, correction invalidating cached routing, and source/session isolation; typecheck passes. The first test attempt used a non-chat session prefix, so the real delivery helper correctly delivered nothing; the fixtures now use real sess- chat identities. No end-to-end completion is claimed.

## Native startup reconstruction owner

On d561e1a..., source 202504 resumed the account naturally on its first search ("the account I just confirmed"). It did not quote the prior delivered note directly, so this is live continuity evidence, not the exact regression control for that note. The source then retried stale native references without fresh DataForSEO discovery and saved partial revision 4, f6109c4c707d90f24cd79c35ae17fd4e203b98a26327f5dbe5d2cc0b63e027bc. It ended needs_input after nine Terra requests. Grok rejected the first status-only proposal, then accepted a substantive partial plan; that positive verdict does not mean ready or executable. No research business calls or document creation occurred. The reviewer sent a discovery nudge at 202601, but it was still undelivered when the next publication terminated the source. Preserve account-reopen-plan-live.json and account-reopen-discovery-steer.json. A steering note queued just before terminal publication is another remaining lifecycle edge.

The first-search failure now has a discriminating repository reproduction. Existing native reopen fixtures cleared ports and then called discovery; the installed app instead runs reconstructShippedPortsForDurableSuccessors first. Adding that actual startup call makes both objective-discovery cases fail with port_registration_failed / "the exact port is not a shipped implementation" and retires the previously valid manifest. The generic boot path had claimed the immutable native port with invoke only, omitting its preparation hooks. Subsequent native discovery could not install its own complete port.

The repair removes that competing owner: generic durable reconstruction defers native_mcp entries to the existing native materializer, which checks current tools/list and registers the full native port. It neither invents an observation nor weakens exact identity checks. The reopen cases now include real startup reconstruction for objective, exact read and exact write discovery; they retain the saved manifest and exercise the existing execution proofs. The focused native/risk/consent run passes 67 tests, no failures or skips; typecheck passes. Red control: /tmp/clem-mcp-boot-red.log. Green: /tmp/clem-mcp-boot-green.log. Live qualification remains outstanding until fresh discovery and execution cross successfully.

## Exact opaque external calls (continuation)

The first Execute research call was valid and currently bound but the generic
external operation projected unknown consequences. The reducer treated that as
an argument repair, so the model could neither fix it nor reach the existing
exact approval mechanism. External unknown risk now uses that mechanism, with
unknown facts preserved; it is not classified safe or auto-approved. Missing
coverage, source/account/schema drift, protected actions, and possibly-started
effects retain their existing dispositions. Local unknown semantics still
repair. No provider names or endpoint vocabulary were added.

Two real-host tests reproduce the former failure for both call_tool and
work_call; after the change each pauses before I/O, persists exact approval,
reopens SQLite and dispatches once on unchanged approval. Pure tests reject
grant reuse after argument/account/schema/call/risk changes. Forty-three focused
tests and typecheck passed. Live qualification and the final research document
remain outstanding. This is an assisted qualification run: the reviewer may
resolve only exact research actions authorized in this conversation and must
record each such action; it must not be reported as autonomous approval-free
completion.

### Execute context contamination observed live

Source 202886 on fe89a238 activated revision 6 but repeatedly recorded the same
prepare_search_protocol compute step. Reviewer steering 202983 was delivered
202991 and did not end the loop. The reviewer cancelled the exact attempt;
there were no research dispatches or report writes. Grok's trajectory review
called the repeated work on_track. This is a failed assisted run.

The actual request snapshot contained global AGENT SYSTEM GUIDANCE to repair
an unrelated OUTLOOK_LIST_EVENTS workflow, with fanout blocked and worker wave
size zero, plus a keyword-derived “External MCP scope: none”. These are real
prompt conflicts, not proof of the sole cause of Terra's repetition. For a
durable accepted Execute mode, the packet now omits global coordination advice,
fanout restructuring directives, and the initial discovery heuristic. The
reviewed plan owns its topology; exact tool-edge policy still owns authority.
A regression with the real failing global telemetry reproduces the conflict.
The plan_step_result response now clearly says the compute step is finished;
this adds no limit and changes no result/provenance storage.


## Fresh Plan revision: catalog freeze defect (2026-09-14 04:30 UTC)

Fresh Plan source 203099 produced a ready plan and Grok accepted it, but the reviewer caught a multi-task Live Advanced request incompatible with the endpoint. The narrow correction source 203305 began rediscovery after same-source preparation refused the base revision bindings. At event 203370 it stopped with catalog_snapshot_identity_mismatch, before research I/O or a Doc write. The native docs-search reference in the starting catalog had been retired and reacquired; no field changes besides identity/issuance/lifecycle were found. The precise reason for the earlier retirement is not yet established.

The host treated explicit Plan (publish_plan, no plan_task) as a frozen execution catalog. This made retirement of any prior catalog entry poison investigation. A discriminating host-runner test seeds an unrelated native read, retires it after model request start, and publishes a local plan. Before the fix it reproduces catalog_snapshot_identity_mismatch. Explicit Plan now uses the existing progressive, graph-neutral host root; current read admission and the Plan write prohibition remain in force. Accepted Execute still freezes and re-proves its selected work. Test and build results are in output/reviewer-monitor/2026-09-13-plan-publication-owner and /tmp/clem-plan-catalog-*.log. Live document completion remains pending.

## Approval resume lost the original research source

Execute 203522 activated the expanded plan and completed the brief, endpoint-doc,
and actor-metadata reads. Its first exact DataForSEO search paused at apr-gigw.
After approval, the production wrapper constructed tools from synthetic control
source 203594 as direct_reply, then the inner runner resumed original 203522.
That empty surface conflicted with the original host root; no search dispatched.
The failed source, its consumed-plan claim, and resolved approval are preserved.

The wrapper now restores the original source from the persisted host interruption
before capability construction, checks the batch and consent subjects agree, and
reopens that source's existing planning catalog. All production approval callers
forward this planning context. The synthetic approval stays an audit event; exact
call/grant checks and root identity checks are unchanged. Legacy SDK control-source
behavior is unchanged. A new full production-agent/wrapper test reopens SQLite,
resumes one exact approved operation and proves one settlement on the original
source. Neutralizing the repair reproduces the live root conflict and zero dispatch.
32 focused tests and 28 approval/resume loop tests passed; typecheck passed. The
existing orchestrator connector-scope test passes. A wider orchestrator test run
also found four worker provenance-storage failures (authority_payload_storage_failed);
the worker failure repeats alone. Those were not changed or counted as passing.
See /tmp/clem-approval-wrapper-{red,green,loop,factory,types}.log.

Owner scope correction: the five companies are examples. Research must discover
additional providers and AIO/AI-search terminology from current sources. Earlier
reviewer-imposed fixed query/page/company limits are withdrawn; the accepted $5
Apify budget remains. No final report yet. The next live run must be recorded as
assisted execution, with approval interventions and actual provider results visible.

The final fixture uses the full initial runConversation as well as the production
approval wrapper and verifies the public success reply, not only the provider
settlement. The approval-control source still owns its response; the original
source owns the agent and business calls. Switching the entire wrapper to the
original source incorrectly replayed its earlier awaiting_approval terminal;
that abandoned approach is not shipped. The exact initial/resumed identity
split is necessary, and the final fixture discriminates the root failure.
