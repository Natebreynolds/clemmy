# Configuration preservation versus connector access

62580fa85ddfe91d7dbbe0fa387d403be4d8c755 installed clean, Terminal recipe/same Developer ID/strict signature verification passed. Fingerprint d1e819f5149b45c44a6d0f6eb771d5423c0f8f8121f8b52dfe32776e73dc33b9, daemon57374, instance0add41dd-2e25-422c-8221-a4b3197a45c7. Desktop asar unchanged44cf6d91…. Rollbacks dist.backup-RAJ23K / builtin-skills.backup-LJ9YmU, signing backup/recipe/logs in output/harness-acceptance/2026-09-23-pilot-625/installed-patch/.

## Live evidence

Original session/proposal continued once, no manual tools-list prewarm. Request4be4d788-82a0-497a-847c-39672dc3269e, run desktop:916d3c97b6e89156517128321c8c31daf51242cd. Returned sinceSeq292692, but actual source292699; terminal292791 failed,1attempt,0unfinished.58957ms,7topcalls,534969input/4722output (full cache/usage totals in measurement.json), exact usage certified. No successful pilot/card/execution/schedule/send. Do not claim schema/cold fix live acceptance.

Both pilot requests returned capability_acquisition_missing; the brain queried mcp_status (unknown) and then mcp_reconnect after one bad-argument repair, clearing connections unnecessarily. A checkpoint recovery led to a provider400 response: You're out of extra usage. This is the observed Claude wire error, NOT proof of overall subscription exhaustion. No more Claude test calls; use Grok next, selecting through actual supported per-request mechanism and verify served model. Do not change user's default brain needlessly. Together comparison remains after stabilization.

Definitive cause found in events292703/292776 mcp_tool_scope: reason user refused external connectors, allowedServerSlugs[],maxTools0. Request forbade changing provider settings, not reading tools. Thus acquisition was denied before the server and Jev were reached. Prior cold-shim failure was real and pinned, but NOT sufficient to fix this live run. Do not mislabel inference as validated root cause. Approval pilot remains unchanged rev3/digest9574eff…f207, existing output Space rev1.

## Candidate repair

Existing accessNegativeBefore already distinguishes configuration preservation from access refusal, but omitted bare change and only looked BEFORE the connector noun. Add bare change to the configuration verbs, and recognize a no-settings-have-changed state report within its own clause when it has no access predicate. No provider/model names or new settings/flags. Explicit use/query/connect refusals remain controlling, including do not change OR use and a following separate do not use clause.

Red test reproduced deny_all for Do not change connector or provider settings. Added past-state and mixed do-not list variants.68checks across scope and Sheet-followup files pass, TypeScript passed; final extra negative assertions also pass. First raw test launch safely refused live-home binding before tests; rerun used mktemp CLEMENTINE_HOME with live transports off. No destructive operation hit live home. Candidate not built/installed yet.

## Remaining

Install candidate and run original pilot once with Grok, preserving review approval and output contract. Need data/provenance, schedule lifecycle, full release gates. Fix preflight exact usage attribution still owed (respond-bridge preflight precedes loop usage scope). Observe inaccurate completion verdicts and no-progress recovery loops; don't accept a refusal report as completed requested work. Do not activate personal workflows or alter integrations. Main/UI worktree untouched.
