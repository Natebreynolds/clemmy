# Native tool schemas on demand

The owner clarified that first-class capability means discoverable and executable, not every schema present on every turn. This supersedes the unconditional native-product floor described in the earlier context-measurement checkpoint.

Native workflow and Space tools now use the existing same-turn catalog/search/carrier path. Exact names, resolved controls, verified recall and session use can still promote schemas directly. Uniquely named workflow reads acquire workflow_get; affirmative execution adds workflow_run. Tool discovery stays reachable on proven-operation paths. Authoring remains excluded from a proven-operation surface independently of the default kernel, preserving the earlier unrelated-workflow-creation regression pin. This introduces no extra Jev request: existing strategy selection and JIT discovery remain the routing paths. Live schemas and call admission remain authority.

Evidence: the new cold-listing regression failed with the unconditional authoring floor and passes after removal. Forty focused action-surface/catalog tests pass, including exact native authoring discovery without connector searches. Typecheck passes. The broader brain/orchestrator run is 203/205: `run_worker requires a structured parent-planned job packet` expects a field list missing retainedResultIds and itemContexts; `fresh-host run_worker is enabled and invokes a worker before any plan is compiled` injects the legacy SDK worker seam although fresh-host workers now use host_harness. These failures remain unresolved, not attributed as pre-existing or waived. No live model was used by these isolated tests; the latter fixture's attempted transport was blocked by the test isolation guard.

Not yet accepted or hotpatched. Installed app remains d631668f6. Before acceptance: resolve the worker contracts, build the exact candidate, verify installed gitSha/fingerprint, then run matched listing and controlled native creation/execution with canonical wall/call/token receipts. The 68.6% registry-byte concentration in three authoring schemas is diagnostic evidence, not a billed-token reduction. A cold request may require discovery; measure its full cost rather than only initial context. Broad release and mobile/restart gates remain open.


## Worker follow-through

The two worker test contracts are now corrected. The required packet keys include retainedResultIds and itemContexts. The former SDK-only fixture now runs the real fresh-host delegation path with a recording model and an actual read_file in the isolated home. It verifies two model steps, the returned file nonce in the second request, a separate child session tied to the parent accepted source, workerScope, one successful child settlement, one successful parent worker_result, host_harness routing, and no manufactured parent planning contract. Entering the legacy SDK worker fails the test.

This exposed an additional real context-budget failure: the compact run_worker definition had grown to 5,308 bytes, beyond the unchanged 4,600-byte pin. Descriptions are shortened without changing packet fields, Zod validation, leases, or dispatch. The obsolete numeric delegation recommendation is replaced with the actual tradeoff: each worker costs a model session; quick reads can run directly, independent work can use a batch. The final compact-definition pin passes. A 94-test orchestrator/packet/child-mode run passed 93, with only the intermediate 4,603-byte schema failing; after the final description trim that exact failing test passed on its own. Both real Plan/Execute child tests continue to refuse unreviewed writes after event-store reopen. Typecheck passed for the test rewrite. No live generative testing or hotpatch has occurred for this candidate yet.

Trap: a fixture intercepting the retired SDK worker can accidentally attempt a real provider request after host routing changes. Keep the live-model isolation guard, inject RouterModelProvider, and assert actual child receipts rather than restoring legacy execution solely to satisfy a fixture.


## Installed candidate regression: accepted source 286254

Hotpatched 957f8408e via Terminal recipe; installed SHA and fingerprint d774191b3f148cd008f43536b63c646c28dd836e35489419aa65a4812033036b matched the clean build. Same Opus 5.5 listing prompt as source 286060. Canonical comparison: wall 26.447s -> 52.089s; primary model calls 2 -> 7; workflow_list stays one call, but two tool_output_query plus four recall_tool_result calls were added. Canonical uncached input including helpers 63,435 -> 81,814 (+29.0%), output 1,940 -> 4,840. This is a failed efficiency acceptance, not a win. Initial prompt-composition telemetry now correctly reports host/16 tools; the smaller starting surface did not compensate for result recovery. Cancellation was requested, but the successful terminal won the race; the installed app is idle afterward.

Cause: the model called workflow_list through call_tool with detail null and received the full envelope. tool_output_query only recognized a fixed list of array keys and failed to recognize the unfamiliar workflows collection. It incorrectly told the model to query record fields after rejecting the correct `name` field, causing raw recalls. The class-level fix recognizes a structurally unique unfamiliar nonempty record array without adding workflow/provider names. Multiple unfamiliar collections stay ambiguous; scalar arrays are not implicitly promoted. Existing known collection precedence remains unchanged. The exact new query regression failed before the fix; 43 recall/digest tests pass afterward, including ambiguity and scalar-array controls. This follow-up is not installed yet. Next: rebuild/hotpatch, rerun the same read, then validate native creation/execution and Sol. Do not certify the current JIT candidate from unit tests alone.
