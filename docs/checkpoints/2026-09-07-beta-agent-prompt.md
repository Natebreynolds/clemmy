Start with the [staged master prompt](/Users/nathan.reynolds/clementine-next/docs/checkpoints/2026-09-07-final-master-prompt.md) and its linked evening takeover baseline. The fixes below are the earlier review, retained as evidence rather than the latest ownership/status ruling.

Fix these shared boundaries before the beta. Exact evidence and acceptance criteria are in `/Users/nathan.reynolds/clementine-next/docs/checkpoints/2026-09-07-beta-release-handoff.md`, including its latest Facebook and in-progress mirror-patch review.

**Facebook correction: fix both layers; auto-heal is not a substitute for the validator repair.** The verified URL was already present in `upstream.find_official_page.facebook_page_url`, and Clem's first payload included `startUrls`. This failure was not missing runtime URL data. The workflow nevertheless repeats a hardcoded URL in its prose/examples instead of binding the actor input structurally to that verified result. Use the existing typed dependency/input binding to feed the verified upstream URL into `input.startUrls[0].url`, preserve its provenance and validate the fully resolved payload before dispatch. Auto-heal can repair that workflow binding, stale tool guidance or output contract within the accepted job; it must identify a host/provider-schema contradiction as a runtime defect, not rewrite the workflow until it passes a broken gate. Never delete required inputs, weaken deliverables, switch to metadata and call it post analysis, or mint a new page/account to make the run green. Prove changed verified URLs flow through unchanged, missing upstream evidence blocks clearly, and valid open actor input crosses the corrected host validator intact. Test workflow-only and host-only changes separately, then the combined live normal/fallback paths. Retain completed upstream evidence and reconcile existing actor runs before retry; an actor invocation can incur cost even though classified as a read. Preserve exactly-once notification behavior. No additional planning or owner-approval ceremony for routine authorized recovery.

1. **Facebook: the host rejects the provider’s valid open input shape.** Source 146969 first supplied `input:{startUrls:[{url:...}],resultsLimit:25}`. The current provider contract explicitly has `input.additionalProperties:true`; the host calls those keys unknown, then accepts `{}`, which Apify rejects because `startUrls` is missing. The real leaf validator reproduces both outcomes. Fix `proof-provider-args.ts` so provider data follows its actual schema or the correct open-payload binding path. Preserve separate operation/account/effect authority. Test open nested objects, schema-constrained maps, explicitly closed objects and intact payload delivery. Don’t special-case Apify or loosen every schema.

2. **Finish the mirror fix through actual stored bytes.** Preferring the top-level lifecycle removes double-counting, but the real stored producer is `composio_execute_tool` and the selected occurrence is `work_call`; `outputFallsWithinOccurrence` still requires identical tool names. The new test never stores the real invocation payload and asserts only “not ambiguous,” which allows `missing` to pass. Test a real nonce/payload/hash, exact producer/carrier names, reopen, `status:ok`, byte equality and a working consumer. Preserve rejection of genuine reused IDs, including a top-level invocation plus a separate unmarked legacy invocation. Authenticate mirror lineage rather than dropping every non-top-level record.

3. **Give recovery a usable route.** Remove the five-call recall cliff and the plain-text recall→JSON-query→exhausted-recall cycle. Keep full authenticated results accessible through paging/structured queries. Facebook’s schema error forbids discovery, while the next refusal recommends discovery absent from the step’s allowed surface. Expose applicable read-only exact-schema inspection; don’t force guessed tool slugs or a new Plan. Keep prior results, allow authorized alternative reads, and stop describing every mismatch as exhausted progress. Carry source lineage through derived result readers. Reconcile hidden pre-dispatch refusals with terminal counts and stop recording budget-error text as semantic success.

4. **Finish the deliverables honestly.** Platform 49 Plan must inspect current Sheet/source evidence and publish concrete corrections; revision→Execute must preserve the exact approved version and unrelated content. Facebook must preserve page verification, obtain real post rows or declare a metadata-only fallback, align normalized evidence with its output contract, and notify only with a supported report. Friday dashboard completion must not require a second production write to prove replay safety. External Sheet writes must appear in completion evidence; distinguish receipts from cell readback. Label inferred prospects and qualify activity-history claims.

5. **Close the remaining beta checks already listed in the handoff.** Accepted owner scope and exact targets; one continuation owner; counters, Stop/steering and post-write restart without duplication; optional same-provider/off review across production callers; actual learning/reopen/correction reuse; native tools and five schema carriers; provider-neutral execution across chat/workers/workflows/recovery; combined desktop/mobile Plan, review settings, history, run identity and reconnect. Reconcile current fixes rather than copying stale green/red labels. SDK packages and Claude worker branches still exist—prove reachability and complete the intended migration without blindly deleting adapters.

Prove the connected paths on frozen bytes: Platform 49 Plan→revision→Execute, Facebook normal/fallback, Sheet edit/readback, bad-query recovery, native authoring, restart/Stop, review modes, memory and delegated dependencies. Preserve first failures and scenario-specific assertions. Finish combined rendered and packaged install/upgrade checks. Update the handoff and scorecard with actual evidence, not only helper tests or a positive judge. No extra universal gates or production write replays.
