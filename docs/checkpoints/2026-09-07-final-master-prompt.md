# Final implementation master prompt — September 7, 2026

**Takeover active:** The implementation handoff has arrived. Read the [current implementation and live qualification checkpoint](/Users/you/clementine-next-live-iteration-31/docs/checkpoints/2026-09-07-takeover-qualification.md) first. This document preserves the morning staged review; reconcile its source-specific findings against the current checkpoint rather than reimplementing completed fixes. Include the [database durability checks](/Users/you/clementine-next/docs/checkpoints/2026-09-07-database-direction.md) in lifecycle qualification; no engine migration is recommended for this tag.

Continue from your current work. You own implementation, live qualification, checkpoint updates, UI integration and release preparation. There is no further reviewer window to wait for. The reviewer monitor stays paused. This document supersedes earlier sequencing and coordination instructions; preserve their evidence and the owner's existing authorization. Reconcile changes made after this review before editing: do not reimplement a fix that already exists, or treat its old failure as current proof.

## Outcome and working rules

Build toward Clem as a capable business assistant: understand the owner's current job, retrieve the right evidence, use tools without unnecessary ceremonies, retain progress through conversation and interruption, and verify the actual result. Normal mode must handle ordinary authorized work directly. Explicit Plan must investigate and produce a useful revisable plan without business mutations; Execute must act on the exact accepted revision. Native workflows, Spaces and memory remain first-class capabilities. Completion review is optional, supports one-provider users, and never grants execution authority.

Slim the harness by removing conflicting control stories and duplicated decisions. Keep exact operation/account/target authority at dispatch, truthful settlements and duplicate-effect protection. Do not replace the present problems with keyword grammar, more universal Plan gates, arbitrary truncation, task-name special cases or stronger instructions to retry an unusable tool. Do not remove every resource limit: make full retained evidence reachable through bounded reads, and make recovery reflect tools that actually work in the current state.

Work in the stages below. Prepare independent fixtures and UI integration while fixing the critical path. Use focused controls to isolate defects, then connected live journeys to qualify behavior. Preserve first failures, record repairs honestly, and distinguish implemented, unit-proven, live-proven and packaged-proven. Do not consume the remaining time in another series of tiny handover-only passes.

## Review baseline — credit the progress, keep the proof boundary

The inspected implementation checkout is `/Users/you/clementine-next-live-iteration-31`. The observed daemon boot at **2026-09-07 14:33:53 UTC** was PID 25696, source fingerprint `a4634bc0fbd13b7e260f273638f87b22448a7823582542ed31a4ab8d50422f6a`, Git `9cdf31fc39523eadeeb15e9be18dfa54165d58a9` with dirty source. Later disk edits do not inherit that live run's qualification. The separate root/UI checkout was at `ced9a5e6`; older UI findings require reconciliation against that newer work and the final combined build.

Current work adds top-level mirror selection, clearer single-occurrence failure classification and a shared retained-result routing helper wired through recall/loop paths. The reviewer ran **10 focused tests successfully** in `mirrored-call-identity.test.ts` and `retained-result-routes.test.ts`. These do not yet prove real retained bytes are usable or the recovery cycle is closed. The runner's home-isolation sentinel was not performed because the live daemon owned the home; do not report that separate check as passed.

The latest captured Platform 49 Plan, source **147032**, still ended blocked at event **147313** after **321.607 seconds**, with **31 top-level calls**, no business writes and no published plan. It includes “reused by 1 invocations,” exhausted recall, a derived-result authority refusal and a final recovery-surface mismatch. Source **146537** previously failed after 334.112 seconds. These runs precede the newest disk patches: use them as exact replays, not evidence that the newest patch ran and failed.

Current review artifacts, hashes and focused test output: [final review evidence](/Users/you/clementine-next/output/reviewer-monitor/2026-09-07-final-master-review/). Detailed prior diagnosis: [beta release handoff](/Users/you/clementine-next/docs/checkpoints/2026-09-07-beta-release-handoff.md).

## Stage 1 — Finish retained-result access and usable read recovery

**Fix the producer identity, not just the error wording.** In `src/runtime/harness/eventlog.ts`, `outputFallsWithinOccurrence` still requires `record.tool === occurrence.tool`. The real stored producer is `composio_execute_tool`, while the selected top-level carrier is `work_call`. Authenticate the carrier-to-producer lineage using the actual invocation/source/dispatch evidence and payload identity. Do not simply remove identity checks. Collapse authenticated transport mirrors while preserving detection of a genuinely separate unmarked legacy invocation reusing the same call ID.

The mirror tests currently create lifecycle records without the real persisted invocation payload and accept any result other than `ambiguous`; `missing` therefore passes. Add the real nonce, payload, hash and outer/inner names. Require `status:ok`, exact bytes and a successful consumer before and after SQLite reopen. Keep negative cases for genuine reuse, mismatched source and corrupted payload.

**Finish the new shared reader routing.** `src/runtime/harness/retained-result-routes.ts` currently uses presentation-store presence/JSON shape, defaults unknown recall budget to available, and offers `file_query` without checking its stronger authoritative resolver. `src/tools/recall-tools.ts` invokes it for plain text without supplying the remaining recall budget. That can still recommend exhausted recall; excluding `tool_output_query` alone does not break the cycle.

Pass the actual current calls/bytes budget and exposed, admissible reader surface. Use existing canonical evidence and capability checks to establish whether each proposed route can serve this result; do not invent another authorization system. Preserve source lineage through derived readers. Make full plain text, nested Sheet grids and structured results accessible through paging/projection beyond five recall calls. A limit must not strand already-paid-for evidence.

Reconcile `brackets.ts`, `loop.ts`, `recall-tools.ts`, `file-query-tools.ts` and `host-turn-runner.ts`: a fallback must be executable in the current Plan/recovery state. Retain successful sibling reads, support a legitimate alternative query, and classify budget/error payloads as failures rather than semantic successes. Explain missing evidence specifically. Include host pre-dispatch refusals in appropriate attempt accounting without confusing them with provider calls or successful effects.

**Exit proof:** actual mirrored result → reopen → authoritative reader succeeds; mixed text/JSON beyond the old budget reaches useful evidence without a cycle; every advertised fallback is exercised through its real consumer. Then replay Platform 49 in explicit Plan: inspect the sheet and relevant prior/source data, publish concrete proposed corrections, and perform zero business writes. No five-minute recovery treadmill and no fabricated plan based on inaccessible evidence.

## Stage 2 — Repair provider schema fidelity and Facebook execution

Source **146969**, workflow run `trigger-e1bacae2afb1c6099caefd2dbbffa2ce`, already had the verified URL in `upstream.find_official_page.facebook_page_url`. Clem initially supplied a valid-looking actor payload containing `input.startUrls` and `resultsLimit`. The cached provider schema explicitly permits `input.additionalProperties:true`. The host rejected those keys, then accepted `{}`, which Apify rejected because `startUrls` was required. The pure production-validator reproduction confirms that contradiction. This was not simply a missing runtime URL.

Repair `providerReadyValueMatchesSchema` and `collectProviderSchemaFailures` in `src/runtime/harness/proof-provider-args.ts`, or the appropriate canonical binding path: honor actual open objects and schema-constrained maps while preserving explicitly closed shapes and independent operation/account/effect authority. Do not special-case Apify or weaken every object. Preserve the payload intact through all schema carriers to dispatch. Expose applicable exact-schema inspection during read-only repair; current advice forbids discovery, then recommends discovery outside the step's available surface.

Also repair the workflow's hardcoded guidance with an existing typed dependency binding from verified upstream URL to the actor input. Align declared outputs with the actual normalized post evidence and downstream report. Auto-heal may correct workflow bindings, stale guidance or output contracts within the authorized job. It must distinguish those from a runtime validator defect. It must never delete required inputs, substitute metadata for post analysis silently, weaken success criteria, or pick an unrelated page to manufacture success.

Preserve completed upstream evidence and reconcile any already-started actor run/dataset before retrying; a read-classified actor invocation can still incur cost. Do not duplicate downstream notifications.

**Exit proof:** open/closed/map schema controls; two different verified URLs flow through correctly; missing upstream evidence is explicit; workflow-only and host-only controls isolate the two repairs. Then live normal and honest fallback journeys produce supported reports with real source evidence and correct notification counts. Retain the original failed run.

Evidence: [Facebook review](/Users/you/clementine-next/output/reviewer-monitor/2026-09-07-facebook-review/), especially `apify-contract.json` and `pure-validator-repro.json`.

## Stage 3 — Complete Plan → revision → exact Execute, and preserve simple Normal work

After Stage 1's investigation, revise the Plan conversationally, then Execute the exact accepted revision. Preserve target identity, dependency bindings and unrelated content. Bind authorization to the owner's effective accepted job, not the model's first proposed mutation. Prove a legitimate same-session edit is accepted and same-kind work on an unrelated target is refused. Delivered, adopted steering must govern where it conflicts; unrelated or undelivered notes must not broaden authority. Cancellation must structurally prevent a superseded pending write.

Reconcile the existing native read/Plan exclusions and `projectEffect` fingerprint binding deliberately; no blanket widening. Finish exact-slug lookup and the five schema-carrier work by tracing the actual carriers in this checkout and asserting their equivalence end to end, including provider open payloads and strict host inputs. Native workflow and Space tools must remain discoverable and callable with the real schemas. The latest captured recovery call used `arguments` instead of `args_json` and string `"null"` for nullable fields; the recovery gate stopped it first, so fixing that gate alone does not prove argument validity.

**Exit proof:** Plan investigates with zero mutations, revision changes the proposed work, Execute makes only the accepted changes, and readback verifies preservation. Normal workflow create/edit and Space create/targeted edit/readback remain direct, without `plan_task`. Test missing definition → exact discovery → valid invocation, and a malformed first attempt → useful repair without replaying an effect. Keep a clean-attempt case separately; do not relax its assertion to hide a regression.

Design reference: [explicit Plan/Execute](/Users/you/clementine-next/docs/EXPLICIT-PLAN-EXECUTE-DESIGN-2026-09-05.md). Broader rationale: [harness simplification](/Users/you/clementine-next/docs/checkpoints/2026-09-07-harness-simplification.md).

## Stage 4 — One execution owner through interruption, recovery and provider routing

Close or demonstrate closure of the retained lifecycle gaps: continuation counters survive checkpoint adoption; timer and immediate continuation have one activation owner; a durable hold keeps its exact attempt alive; rejected recovery saves cannot claim recovery is armed; terminal completion closes once. Stop and adopted cancellation prevent new effects. After a write and process restart, reconcile the settled effect and resume remaining work without replaying it. A pre-write restart test does not establish post-write safety.

Audit actual production reachability across chat, workers, workflow steps, background jobs, recovery, judges and auxiliary calls. Ordinary production chat uses the shared host path, but SDK dependencies and Claude worker/workflow branches remain. Trace them before claiming the SDK migration is complete. Migrate active alternate execution ownership into the common harness, remove proven dead branches, and retain necessary provider authentication/transport adapters. Do not blame these shared-harness failures on Claude or Composio without evidence.

**Exit proof:** restart after a confirmed write, Stop during held recovery, competing wakeups and provider-routed delegated work all retain exact source/activation identity, truthful counters and no duplicated effect. Record actual engine/provider/model and request/usage linkage on every exercised lane. Reconcile the existing `loop.ts`/`verifyDelivered` selection-provenance and usage-linkage work against current source rather than assuming C30 gaps remain unchanged.

## Stage 5 — Verify the business outcome and teach only from supported evidence

**Friday dashboard:** the captured run completed all eight steps and saved the dataset; it was marked blocked because its completion criteria demanded production replay proof of a single content-addressed commit. Separate business-result checks from controlled replay qualification. Do not execute a second production write to satisfy a judge or disable the retry guard. Check the saved `data.dashboard` and rendered consumption path; whole-file hashes and legacy top-level fields are not equivalent evidence.

**External Sheets:** source 146393 successfully created and populated a sheet, but the collector reported zero effects despite two mutating logical settlements. Audit the native-registry filtering in `settledSourceArtifacts` and represent provider receipts/resources with typed evidence. Acknowledgements, host-file receipts and external resource readback are different contracts. Read the actual written cells when claiming their content is verified; do not force every effect into a local file. Label inferred prospects in the artifact itself, and do not infer “no meeting/reply/follow-up ever” from limited Task queries or unscoped history.

Complete the connected review matrix: disabled, owner-selected same-provider/same-model where supported, cross-provider, unavailable/negative, resumed judging and fallback identity. Capture the effective policy and objective at acceptance/adoption and preserve them through reopen. Verify current artifacts at publication, coverage and failure wording. A corrective judge may return specific evidence-backed repair guidance against the accepted task; it must not invent requirements from unverified memory or authorize new work. Repair must re-read/reconcile rather than repeat effects.

Finish producer → settlement → persistence → reopen → learning proof. Owner-selected same-provider review must retain honest provenance; failed-open must not become a verdict. Test teaching, correction and fresh-session reuse with natural recall, including cold indexing and pending sessions, without operator warming.

**Exit proof:** dashboard status reflects the actual result; changed/missing evidence invalidates verification; external written cells and relevant business criteria are checked; each review mode behaves truthfully after settings changes/restart; learning consumes the real persisted producer chain and reuses the correction correctly.

Evidence: [dashboard and Sheet review](/Users/you/clementine-next/docs/checkpoints/2026-09-07-dashboard-and-current-live-review.md) and [final lifecycle/authority handoff](/Users/you/clementine-next/docs/checkpoints/2026-09-07-final-harness-handoff.md).

## Stage 6 — Qualify the combined product and the real graph journey

Combine the UI and harness work while preserving both agents' changes. Recheck historical UI findings against current code, then test the rendered combined desktop/mobile build: visible Plan and exact Execute, persisted Completion review distinct from Second opinion, complete history paging, correct run links and Stop identity, run A→B navigation with delayed responses, offline freshness, needs-input/held states, relay reconnect, and notification/push/voice states that match actual capability and permissions. Source review is not rendered qualification.

Exercise the business-owner journey: inspect a Google Sheet; resolve the same accounts in Salesforce; retrieve the requested enrichment source; join by verified identities; propose or apply the authorized new section; read back cells; explain potential market leaders using disclosed criteria and evidence. Resolve what “SCO” means from available owner context, or ask one necessary clarification rather than inventing a source. Use genuine independent reads/workers in parallel where useful, dependency-aware joins, retained results and interruption/resume. Exercise a substantial multi-step task; do not pad its duration or make every simple chat a graph.

**Exit proof:** connected desktop/mobile journeys complete with appropriate business assertions, and a dependent multi-tool/delegated journey survives one controlled failure or interruption without losing completed work. Measure model requests, searches, calls, refusals, successful effects, review identity, readback and elapsed time. Set scenario-specific bounds: create-and-populate can legitimately need two writes; Plan needs zero; repair tests allow a repair; clean-attempt tests still require a clean first attempt.

Use the [expanded campaign](/Users/you/clementine-next/docs/checkpoints/2026-09-06-expanded-live-qualification.md), [business-owner Sheet qualification](/Users/you/clementine-next/docs/checkpoints/2026-09-06-business-owner-sheets-qualification.md) and [historical UI repros](/Users/you/clementine-next/docs/checkpoints/2026-09-06-ui-integration-agent-prompt.md). The earlier 30–40 turn estimate is coverage guidance, not a quota or permission to replace meaningful tests with repetitions.

## Stage 7 — Qualify the actual tag candidate

Use one identified combined source/build for final acceptance. Record commit, dirty state, fingerprint, package version, daemon and test source identities. Rerun affected qualification after changes invalidate its evidence. Update [RELEASE-SCORECARD.md](/Users/you/clementine-next/docs/checkpoints/RELEASE-SCORECARD.md) with concrete artifacts and unresolved limitations; do not carry forward stale pass labels.

Inspect the current release workflow and run its required checks, including applicable type/unit/journey, measurement, proof, public-hygiene, release-asset/closure, packed-candidate, install/upgrade, benchmark and memory/pass-k gates. Verify the packaged product with independently installed dependencies; a dev checkout borrowing another checkout's modules is not package proof. Test rendered desktop/mobile on the integrated candidate and the supported release platforms. Preserve owner-agreed release scope; do not silently reduce it or invent a tag/version.

Prepare a clean, reviewable combined commit and release evidence. Follow the owner's existing push/tag authorization. Do not declare beta-ready while the core elementary journeys remain blocked, and do not promise perfect execution of every possible task. The release claim should be specific: what connected business work this build has repeatedly completed, how it recovers, and what remains limited.

The architectural direction is sound only when demonstrated: a dependency graph coordinates the job, local loops repair its steps, durable state preserves what happened, and tool edges enforce the owner's authority. The immediate job is to make those existing pieces cooperate without trapping Clem between contradictory instructions.
