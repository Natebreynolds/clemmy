# Installed pilot continuation — nomination failure

Installed clean319096ec1 / fingerprint166f6cec65458b904481450ec3879cbff4f236574017103929301968294e3c75, daemon21631. Original approved proposal automation_3da1c635b0a7dd98a928b7957369731f remains revision3/digest9574eff2192f943e63a3ae49e3f4d7122082f04d8ab8f716e623bf444ff8f207. No existing pilot projection before this attempt. Existing named acceptance Space retained.

Accepted source292497, terminal292592 blocked, one attempt, no unfinished attempt. Actual primary Opus5.5. Canonical measurement:75273ms;7 top-level calls,1 discovery;389501 input/248923 cached/140578 uncached/7111 output;19 usage rows (Opus7,Grok4.3 six,Jev1.13 six), exact attribution certified,0 uncertified calls. Not a matched performance win: work failed and prior context differs. Evidence output/harness-acceptance/2026-09-23-pilot-319/{request,accepted,build,events,measurement}.json.

space_get now succeeds on the manifest-only Space. Search resolves dataforseo__docs_list_sections, current callable read schema. Four pilot submissions: expired retained acquisition ref; missing outcome_authority; explicit text mapping diagnostics; then capability_acquisition_missing after arguments corrected. No pilot projection, no provider body read, local data write, external send or schedule. Do not retry unchanged or recancel terminal source.

Concrete acquisition finding: automation-read-pilot-control-plane.ts supplies requirement.description as retrieval objective. The unchanged requirement says Read-only listing of the provider's documentation section inventory (free metadata only). createAttestedLiveReadCarrierAdapter filters eligible read rows through rowIsAdvisoryObjectiveNomination; for non-CLI carriers that helper requires EVERY non-stopword objective term in identifier/displayName/description/parent/selector. The discovered tool describes returning available DataForSEO API documentation section names and does not contain all task constraints. Normal discovery finds it, pilot acquisition rejects it before attestation/materialization. The completion judge's claim that the documentation read is outside allowed sources is not established by this receipt; policy exclusion has a different error message.

Next repair: provider-neutral semantic nomination or an exact current independently validated capability selection from retained discovery, preserving effect/account/schema checks, exact-one ambiguity and approval boundaries. Pin the ORIGINAL natural-language requirement and actual carrier metadata, including distractors and unavailable semantic resolution. Do not rewrite the proposal to an exact operation name, loosen execution authority, add provider/name exceptions, or substitute an easier dataset. Installed acceptance still requires the original durable pilot lifecycle and broader release gates. Paid pilot repeats held until the nomination defect is pinned and repaired.

## Candidate semantic fallback

Native MCP production acquisition now invokes an advisory Jev decision only when eligible attested-read metadata has no literal nomination. Each candidate is judged independently; all matches survive so downstream exact-one/account checks retain ambiguity. Empty means an evaluated no-match; timeout, missing service, low confidence or uncertainty mean unavailable. Unknown/write effect metadata never enters the decision. Exact and lexical hits keep their existing fast path. Full metadata sets beyond the decision transport window report unavailable rather than truncating away a possible competing match. No task/provider/model name exceptions or prompt edits to the approved proposal.

New original-wording/real-description pin failed against the previous registry implementation and passed on the candidate. Separate tests retain ambiguity, exclude writes, avoid execution/publication during nomination, and distinguish unavailable from missing. Focused control-plane + acquisition registry run41/41 pass, no skips; source restored byte-identically after negative run. Candidate is not installed. Real Jev nomination, final types, broader pilot regression and original live lifecycle are still owed. Installed app remains319096ec1. No new paid call in this candidate test phase.

Final candidate TypeScript check passed (exit0), whitespace check clean. No commit/build/hotpatch of this semantic candidate yet.

## Real Jev qualification rejected this candidate

Metadata-only live candidate probe returned TWO matches, docs_list_sections and docs_search,574ms. Thus the injected test pass did not establish correct production matching. Do not install this version or report pilot repaired. No provider body was invoked.

Candidate now includes independently attested current input schemas in semantic decision context and states that fetching a known item is not enumerating unknown items. Definitions are re-attested in the existing nomination path afterward; no authority is created by the advisory decision. Focused tests41/41 still pass; broader pilot convergence/control-plane21/21 pass before this schema addition.

First schema probe incorrectly used workspace zod-to-json-schema against provider Zod4, producing empty schemas; null754ms is INVALID evidence of the intended input. Corrected probe uses installed provider package z.toJSONSchema and explicitly asserts docs_search requires url. Corrected live result is still null444ms (unavailable/uncertain), so the candidate remains UNQUALIFIED. Do not spend another whole Opus pilot retry on this version. Need distinguish model uncertainty, low confidence, and unavailable transport in diagnostics before choosing further change. Three small live Jev metadata probes, no business tool calls, no hotpatch or commit. Evidence live-jev.json, live-jev-with-schemas.json (invalid schema probe), live-jev-native-schemas.json under output/harness-acceptance/2026-09-23-semantic-nomination/.

Diagnostic fourth Jev-only call transparently observed the real HTTP response (no substitution, credentials never logged): HTTP200 jev-1.13.0. Correct inventory candidate voted match at confidence0.26 (match probability0.50); URL-fetch candidate voted match at0.99. Other candidates no_match0.97. This is an incorrect/low-confidence semantic decision, not auth failure, timeout, or missing model. Helper correctly refused. Response saved live-jev-diagnostic.json. No additional full pilot. Final schema candidate typecheck passed.

## Revised decision: best documented read, not independent topic votes

Explicitly binding metadata per question removed URL-fetch confusion in forward/reverse probes, but broad index and exact inventory still both received match, while generic gateway uncertainty blocked the whole set. That independent-vote candidate was rejected, not installed.

Replaced independent votes with one choice over full attested metadata/schema: best direct documented read, none, ambiguous, uncertain. Operation choice retains EVERY account row for the selected operation; semantic selection cannot choose a credential. Ambiguous returns competing rows for the existing registry to refuse, uncertain/transport/low-confidence remains unavailable. No first-item fallback, relaxed account/schema/effect rule or provider-name exception. Exact/literal matches bypass this fallback. At most252 candidates plus three semantic outcomes fit the existing255-choice transport window; larger sets report unavailable without truncation.

Live source-candidate probes with installed provider native Zod schemas: original unchanged requirement selects docs_list_sections only379ms; reversed candidate order selects the same378ms; unrelated unread-email request selects none268ms. These are real Jev decisions, no provider body invocation, and not installed pilot qualification. Evidence live-jev-best-read.json. Four-file regression run62/62 passes, no skips; added separate exact-operation fast-path test afterward. Daemon-owned isolation sentinel NOT PERFORMED; it observed background memory files change while testing, so no isolation proof claimed. Still require final types/build/hotpatch and full original workflow lifecycle.

Final typecheck passed; exact-operation fast-path pin1/1 passed. Whitespace check clean. Candidate is ready to build, not yet installed or accepted end-to-end.
