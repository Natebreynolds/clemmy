NEXT ASSIGNMENT — BIND MUTATIONS TO THE ACCEPTED JOB; COMPLETE CONTINUATION OWNERSHIP

**Final ownership transfer — September 7:** The owner has ended the reviewer role. The implementation agent owns qualification, checkpoint updates, UI integration and release preparation from here. No future reviewer response or live-window handoff is required. The recurring monitor is paused. Read the [final handoff](/Users/you/clementine-next/docs/checkpoints/2026-09-07-final-harness-handoff.md); it supersedes older coordination instructions below. Historical evidence remains scoped to its recorded build.

Reread before changing anything and as work progresses:
/Users/you/clementine-next/docs/checkpoints/2026-09-05-harness-refinement-handoff.md
/Users/you/clementine-next/output/reviewer-monitor/2026-09-07-c34-review.md

C33/C34 review is complete. C34 e5b702d0… is serving, but no tag is qualified. The reviewer returns the window to implementation; no live campaign, cutover hold or permission question remains. Four implementation turns were audited; no additional reviewer live call was needed to establish the defects. Preserve the first-attempt reports and exact build/source/receipt identities.

WHY THE PRIORITY HAS SHARPENED
Source142281 asked to CREATE a workflow. After a duplicate result, a graphless workflow_update changed the existing frozen workflow's description. This was one real physical write, not an intent-counter error. The reply honestly disclosed the update and a completion judge certified the resulting artifact; neither establishes permission to replace the requested creation with a patch. Later wording helps the model stop and ask, but C34 adds no structural comparison of owner-accepted scope at that native dispatch seam.

The business-owner standard requires correct work inside the authorized job, with truthful partial results and little supervision. Keep ordinary work direct, full tool guidance available, and review optional. A judge can suggest corrections; it cannot grant additional write authority.

1. CLOSE THE OBSERVED GRAPHLESS MUTATION-SCOPE GAP.
Use the real sequence142320 duplicate create→142361 update→142367 changed-file receipt. The exact call is bound to source142281 and the correct workflow_update schema. There is no Plan, intervening owner instruction or approval. The source-disclosed card correctly labels create_new versus named_existing; discovery is not the problem.

Current graphless consent builds accepted-call coverage from the selected call's own operation/destination/arguments, then checks that call and coverage agree. It proves source/call consistency without independently comparing the mutation to the accepted job. Strengthen that EXISTING coverage/handoff seam before dispatch. Reopen source-owned per-operation target, destination posture and allowed-change scope; compare the actual typed capability and canonical arguments against it. The scope producer must reflect accepted owner instructions, including adopted amendments, rather than minting permission from the proposed call itself.

Keep all authoring siblings discoverable. Do not globally prohibit updates after creates, grant all same-family operations, parse English keywords, or require Plan for ordinary edits. Legitimate mixed work and editing an artifact created by the same accepted source must retain their own operation scopes and dependency lineage. Use the existing typed descriptors, exact consent comparisons and structured needs-input/repair outcomes.

Prove at actual accepted-scope production→nomination→consent→dispatch:
- Create of an existing named artifact→known duplicate→proposed patch cannot dispatch or change bytes without an accepted scope change.
- An explicit existing-artifact edit still makes one authorized mutation in Normal mode.
- Same-source create→edit and mixed create/update work remain possible when actually requested, with per-operation targets/lineage.
- Generic uncertain local failures still require reconciliation; a nominal known non-write remains repairable within its accepted scope.

A self-authored matching policy fixture is insufficient. Use the real producers and retained failure. Then qualify a NEW test-owned live artifact with a captured preimage and a separately explicit edit request. Do not replay or silently restore clemmy-frozen-c31-0907 to erase the incorrect-effect measurement. Preserve its original769-byte and changed755-byte snapshots and the receipt diff. Clearly distinguish unchanged artifacts from retained evidence of an overwritten one.

2. COMPLETE THE CONTINUATION CONTRACT IN THE SAME CONNECTED BATCH.
Accept C34's real progress: failed claims now stop adoption; successful installed saves claim responsibility before immediate reentry; matching-source terminal cleanup is transactional even without a resolved owner. Preserve the native uncertainty fix and independent continuation marker.

“All six use a helper” still leaves four boolean results discarded. The two initial failures return held into an outer retry, and save and claim are separate durable writes. Make every caller consume a consistent save/claim/adopt outcome. A stopped process or refused claim must not leave apparently armed work without a valid owner. Distinguish ownership conflict from unreadable state instead of blaming a newer source on every false result.

Bind claims to actual accepted source/session/current unfinished attempt/terminal eligibility. Numeric source ordering currently permits stale same-source replacement and post-terminal reinstallation. Fix the undefined-session-load branch, which logs unreadable yet allows finish, and the approval-resume branch that still drops its held result. Keep exactly one timer/immediate continuation owner. Retain completion/no-progress/watcher progress across adoption instead of resetting it when only history and lastResponseId survive.

Use real held save/claim rejection→outer runTurn→desktop callback/finally→next protected child tests, including a valid competing attempt, failed adoption and terminal interleaving. Preserve the finished-attempt lease guard and existing atomic terminal closure. The new manual save/claim tests do not execute these loops; the already-closed terminal fixture also passed the prior branch because that owner row is still selected. Use discriminating production-boundary regressions.

Preserve exact failures141915 and142450. The latter has24 Terra/27 Opus calls, a completed attempt before14 later parent generations, five child refusals and continuationsUsed:0. Source142281 also starts two new completion requests after terminal and appends more verdicts. Stop obsolete execution/review at logical terminal; do not merely hide those events from accounting.

C34's same-source interrupted resume is a valid positive: one post-restart write and verified artifact, no Plan. The kill preceded any held checkpoint or effect, so it does not qualify the waiting ownership window or post-effect replay prevention. For a future declared restart fixture, retain exact same code/source/effect identity across the expected instance transition; preserve the existing failed blanket-identity driver report rather than relabeling it passed.

3. FINISH THE ALREADY-ASSIGNED NATIVE AND PLAN CONTRACTS.
Plan revision→exact Execute has no C33/C34 work. Native full schema carriers, exact-slug authoritative lookup, compound-work read support and the workspace_list discovery case are still untouched. Keep them in bounded parallel ownership rather than allowing another small successful probe to replace the connected assignment.

For Plan, retrieve full saved content and base_ref_json; revise only requested fields, preserve lineage and unrequested static arguments/layout/bindings, then Execute exactly the inspected planId/revision/digest. The artifact review must see the executable content it certifies. Keep explicit desktop/mobile Plan and direct authorized Normal work. The failed Cedar artifacts140854/141337 stay unexecuted evidence; qualify a new Plan→one-field revision→exact diff→Execute journey after implementation.

For discovery, preserve complete schemas and distinguish discovery from schema paging. C34 restart's three searches were one discovery plus two schema chunks. Do not truncate required arguments or broadly widen fingerprint-bound read/projectEffect contracts. Publish capabilities freely within existing discovery authority while admitting each effect against its actual job scope.

4. RETAIN LEARNING, RECALL AND INTEGRATION REQUIREMENTS.
The nominal no-write/uncertainty source is unchanged from the accepted13-control review; do not rework it unnecessarily. Cold recall's actual assistant-answer/frontier fix carries forward with its finite limits and existing4/4 isolated proof. Finish useful coverage assertions and realistic unseen recall later without manual live warming or derailing the immediate scope/ownership correction.

Source-bound steering adoption, producer→persistence→reopen→learning, owner-selected judge provenance and usage/request linkage remain open. Distinguish actual accepted verdicts from started/completed model requests and duplicated usage rows. Review-off and owner-selected same-provider operation must work; completion review is not an authority substitute.

Use bounded parallel owners for scope admission, continuation, and native/Plan work, with one integration/cutover owner. Finish meaningful boundary tests and preserve real logs/source hashes, then freeze the successor. Report implemented, unit-proven, live-proven and open separately. No endless new candidate consisting only of a helper plus a healthy creation.

NEXT AFFECTED QUALIFICATION
After the connected correction and frozen handover, use Codex Terra gpt-5.6-terra and Opus5 claude-opus-5 with actual route/purpose and accepted-verdict evidence. Requalify the scope-negative/explicit-edit pair, same-session native create/read/edit/readback, held continuation and exact Plan revision/Execute. Preserve policy, account/schema/source/effect identity, preimages/diffs, attempt/refusal/Plan counts, main/auxiliary requests, final bytes and truthful terminal states. Existing30 reviewer turns remain historical; this review adds zero. No silent fallback/fail-open counts, no equivalent write to repair an assertion, no old C10 draft or failed Cedar execution.

Preserve original UI work, integrate actual intended harness/UI source, and render-qualify desktop/mobile Plan/Execute/Stop/rejoin/settings plus packaged install/upgrade on that combined identity. The Google Sheets→Salesforce→SCO journey is still unqualified; this local scope finding is not proof of a corresponding connector defect or of connector safety. The eventual business workflow must prove its own account/target/change authority and outcome.

EVIDENCE
/Users/you/clementine-next/output/reviewer-monitor/c34-review-0438/native/report.md
/Users/you/clementine-next/output/reviewer-monitor/c34-review-0438/lifecycle/report.md
/Users/you/clementine-next/output/reviewer-monitor/c34-review-0438/live/review.md
/Users/you/clementine-next/docs/checkpoints/2026-09-06-expanded-live-qualification.md
/Users/you/clementine-next/docs/checkpoints/2026-09-06-ui-integration-agent-prompt.md
/Users/you/clementine-next/docs/checkpoints/2026-09-06-business-owner-sheets-qualification.md

You maintain the checkpoint and independently audit each pass, using a separate bounded reviewer where useful. Reread it as changes land; no further Codex reviewer response is a prerequisite. Quality determines the tag; finite tests cannot promise universal perfection or comparative superiority.
