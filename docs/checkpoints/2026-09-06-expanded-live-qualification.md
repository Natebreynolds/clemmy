# Expanded live qualification after the current implementation round

**Final ownership transfer — September 7:** The owner has ended the reviewer role. The implementation agent owns qualification, checkpoint updates, UI integration and release preparation from here. No future reviewer response or live-window handoff is required. The recurring monitor is paused. Read the [final handoff](/Users/nathan.reynolds/clementine-next/docs/checkpoints/2026-09-07-final-harness-handoff.md); it supersedes older coordination instructions below. Historical evidence remains scoped to its recorded build.

Owner direction: “After this next round we need to take out a bigger bite and have more live test run.” C30 completed the first breadth pass:22 new accepted reviewer turns across all12 IDs, with later stages blocked where their prerequisites failed. Preserve that baseline and rerun the affected cases on the next frozen corrected candidate. This extends the assignment; it does not replace the outstanding correctness findings or declare them fixed.

The implementation lead now owns submitting and auditing the campaign, preparing runnable fixtures, freezing the source/build and correcting failures. Use a separate bounded audit owner where useful; one named owner controls daemon cutovers. The UI owner prepares the rendered cases and integrated candidate. Reread the [canonical checkpoint](/Users/nathan.reynolds/clementine-next/docs/checkpoints/2026-09-05-harness-refinement-handoff.md) before a pass or cutover.

## Campaign size and execution

Target 12 core live LLM journeys, approximately 30–40 accepted turns including selected fresh variations, plus the connected business-owner journey when its inputs are ready. This is a coverage target, not a success quota or runtime ceiling. Count actual accepted turns and report the achieved coverage; do not manufacture extra turns to reach a number. Private unit controls, synthetic verdict injection and re-auditing old traces are useful supporting evidence but do not count as new live journeys.

Run the ready, independent cases across the frozen candidate. An unavailable prerequisite or a known defect in one lane must not stall the rest. Preserve the first attempt for every case, then group failures by root cause and correct a coherent batch. A failure that risks duplicate effects or wrong authority stops the affected lane until reconciled; it does not authorize blind retry. Give the owner the first campaign scorecard before another sequence of narrowly scoped candidate claims.

Use natural owner wording, fresh test-owned artifacts and varying content. Expected behavior belongs in assertions, not hints telling the model which tool to select. Targeted repair continues the original artifact; recreating it cannot turn a failed attempt into a pass. Healthy simple native cases retain the enforced C29 baseline. Long tasks have scenario-specific expectations and a generous observational timeout, with progress recorded; the 120-second simple-case bound is not a long-task harness limiter.

## Twelve core journeys

| ID | Live journey | Required evidence |
| --- | --- | --- |
| L01 | Find and inspect existing test-owned work from a natural request | Useful discovery, exact source/object selection, complete requested read, zero mutation and zero user-facing Plan for the simple request. Include a plausible distractor. |
| L02 | Normal Workflow create, then targeted same-session edit | Exact callable/schema selection, one mutation on each write turn, zero Plan/refusals, exact before/after content and preserved unrelated fields; no workflow dispatch/delete. |
| L03 | Normal Space create, then targeted same-session edit | Read before editing when needed, correct view/Space identity, one mutation per requested write, zero Plan/refusals, preserved content and independent readback. |
| L04 | Explicitly selected same-provider completion review | Actual live selected provider/model, positive judgment and truthful learning authority through real callers. Exercise a connected cross-provider alternative without bypassing the available owner choice; exercise one-provider-only availability in isolated configuration. Record both topologies separately. Exercise actual supported production completion/learning callers, including the shared host loop used by ordinary Claude. The retained standalone Claude SDK caller is test-only; its controls do not qualify production learning. |
| L05 | Completion review deliberately off during native work | No completion-judge invocation, honest disabled disposition, deterministic write/receipt/readback still holds; no added Plan/provider requirement and no fabricated review-based learning authority. |
| L06 | Accepted review policy survives settings changes and restart/resume | Accept under one policy, change later settings in a disposable daemon, restart and resume. Actual calls and terminal follow the accepted policy and preserve requested/actual/fallback identity. Prove both off→on and on→off directions; absence/read failure must not silently select current policy. |
| L07 | Unsuccessful completion followed by targeted repair | A real LLM task with a controlled missing result or inaccessible test resource produces a specific truthful outcome; a new accepted repair fills only the missing work, preserves settled effects and reads the result back. Keep immutable historical terminal. Synthetic committer events alone do not qualify this live case. |
| L08 | Interrupt after a write settles and before delivery, then resume | Exact original settlement/artifact survives actual daemon restart; continuation reconciles it, does not repeat the mutation and delivers the correct current result. Use an isolated daemon with an observable, supported interruption seam; do not claim precise timing unless recorded. |
| L09 | Recall prior work naturally from another session | Time/topic phrasing, distractor sessions, correct prior artifact and task status; no exact session ID supplied in the prompt. Recall then continue a targeted authorized change to the correct object, preserving unrelated work. |
| L10 | Teach → perform → correct → reuse in a fresh session | An owner instruction is learned through the actual eligible path, a correction supersedes it and unseen later work applies the corrected instruction. Distinguish durable instruction, one-task exception and untrusted artifact text. Receipt eligibility alone does not prove behavioral reuse. |
| L11 | Substantial delegated work with dependencies and a merged deliverable | A realistic 20–30-minute-scale task with bounded independent subtasks, dependent joins, durable worker results and useful progress. Verify parent/worker lineage, no lost/duplicated work, final synthesis and interruption/resume. Do not pad runtime with waits or force needless delegation. |
| L12 | Explicit Plan → revise → Execute the reviewed revision | No requested business mutation during Plan; owner revision survives, Execute uses that exact revision and produces the intended outcome once. Exercise Stop/rejoin against the exact run and retain truthful paused/final state. Build each Execute fixture from the newly fetched and reviewed plan reference, never a historical ref. This explicit Plan case does not justify gating ordinary Normal requests. |

Initial pass: exercise every runnable core journey. Then repeat the highest-risk direct native, selected-review and continuity cases with unseen wording/data or a different applicable review mode. Retain both first-attempt and repeat outcomes. A repaired-build pass is reported alongside the earlier failure, under its own source fingerprint. Missing actual model access or an unavailable interruption seam is a named qualification gap, not a skipped assertion reported as success.

## Connected owner journey and UI integration

Add Google Sheet review → Salesforce account matching → additional SCO data → new section → supported market-leader assessment. Resolve the exact test sheet, authorized accounts, enrichment source and ranking criteria. “SCO” meaning remains pending. Prepare synthetic/test-owned data and all independent cases while that clarification is outstanding; a local CSV or stubbed CRM run cannot be labelled live Google Sheets/Salesforce proof. The example does not authorize modifying an unspecified customer sheet or CRM.

The [business-owner scenario](/Users/nathan.reynolds/clementine-next/docs/checkpoints/2026-09-06-business-owner-sheets-qualification.md) specifies pagination, row/account identity, formula preservation, written-range readback, correction and interruption after settlement. Record read-only CRM use and only the authorized destination writes. This is the end-to-end graph qualification target.

Run applicable UI cases on the combined harness/UI build: visible Completion review controls on both surfaces, persisted save/error state, Normal versus explicit Plan, exact reviewed Execute, correct workflow Stop, mobile A→B with failed/late reads, honest offline state, input→resume, full filtered history and correct Open run targets. Include the pending native/notification and relay reconnect cases from the [UI assignment](/Users/nathan.reynolds/clementine-next/docs/checkpoints/2026-09-06-ui-integration-agent-prompt.md). HTTP-only mobile proofs and isolated UI helpers do not establish rendered phone behavior.

A harness-only campaign can start before UI integration is ready. Its report must name that boundary. Requalify interacting behavior on the exact combined build before tag readiness; never transfer a historical candidate pass to changed integrated bytes.

## Reuse the existing drivers

Use the frozen successor's `scripts/live-foreground-proof.mts`, not the original checkout's older copy. The reviewed C29 driver supports strict message/expectation objects, `taskMode` and `--mobile-credentials`, real desktop or paired mobile ingress, `--session` continuity and exact source/build checks. Write one fixture/report per journey so one failure does not prevent unrelated cases from running. Supply `--home`, `--messages`, `--out`, `--expected-source-fingerprint` and `--expected-entry` explicitly for each actual candidate. Mobile HTTP still needs the separate rendered check. Do not expose private credentials in reports.

The current foreground checker expects successful completion and stops its file on blocked/needs-input/timeout; its timeout may cancel the exact attempt. Extend or use a purpose-built driver with explicit expected negative/pause and restart checkpoints for L06–L08 and the paused stages of L12. Never catch an error and relabel it pass, drop source/build checks or retry the original write blindly. The driver currently permits at most1,800,000ms per observed turn; that is an observation limit requiring deliberate extension/rejoin for longer proof, not evidence that productive harness work must end there.

The successor also contains `scripts/proof/run-proof.ts` and reusable scenarios: `restart-resume`, `capability-reconnect-resume`, `correction-sticks`, `conversation-switch-resume-horizon`, `long-horizon-manifest` and `bookkeeping-receipt-exact-once`. Reuse their isolated lifecycle and evidence machinery after reviewing their assumptions against this campaign. The proof runner can build/change distribution files and pins model/fallback/Fusion/cross-family policy; run it only in an isolated snapshot with independent dependencies, and adapt the policy to the case. Connected-service shims in those scenarios are not actual Sheets/CRM proof. Do not run a broad proof script against the frozen serving checkout or let its defaults override the selected-model case. `--brain codex` selects a runner lane, not Terra identity; require the intended lane and assert the actual model. Controlled restart needs an explicit two-boot check: same source/home/task, the intended PID/instance transition and unchanged settled effects.

Exact driver, template and provisioning references are recorded in the [supplementary mechanics review](/Users/nathan.reynolds/clementine-next/output/reviewer-monitor/c29-review/live-campaign-mechanics.md); this twelve-case campaign is the authoritative scope.

## Per-case evidence and assertions

Before submission, write the scenario ID, purpose, prerequisite, review policy, expected outcomes and readback comparison. Record start/end source fingerprint, entry, daemon instance, accepted source/session/run and idempotency/call identities. Separate primary brain, completion judge and auxiliary model requests, with actual model/provider, substitutions and measurement availability. Keep canonical tool/search/Plan/refusal counts, attempted versus successful mutations and settlement/receipt identities.

The current driver does not yet enforce every item below. Retain the independent exact-source audit while extending those fields; driver success alone cannot establish them. Enforce the expanded assertions from the current harness assignment: accepted-policy order and durability; objective and authored/final reply binding; required coverage and actual current artifact bytes/readback; edit preservation; exact effects and no unintended dispatch; expected negative dispositions. Keep disabled, unavailable, negative and reviewed distinct. Preserve raw evidence and link every claim to it. When a measurement is unavailable, the related assertion is unqualified rather than zero or pass.

Report each journey as passed, failed, blocked by a named prerequisite, or not run, alongside the actual accepted-turn count, latency and model/tool usage. Also report a separate evidence-quality result; reaching done does not imply the required evidence was captured. Use one row per first attempt and retained repeat, with source fingerprint and paths to input/output/readback. Summarize failure clusters and next changes after the first breadth pass; do not substitute a string of healthy-create handovers for the scorecard.

## Isolation and ownership

Keep ordinary live work in fresh test sessions and distinct test-owned artifacts. Settings changes, denied resources, review failures and restarts run in a disposable qualification home/daemon, with credentials handled through existing supported private setup. No live-home SQL injection, trigger removal, manual fabricated terminal or generic process kill. Keep private proof seams clearly labelled. Snapshot before change, reconcile uncertain effects before retry and preserve failure artifacts.

One owner changes a daemon or global setting at a time. Independent preparation, readback and source analysis can run in parallel; concurrent live work requires separate resources and no shared mutable test policy. Do not interrupt the owner's ongoing phone work. Record actual command/entry/PID before an intended test-daemon restart. Build final source and required web assets before freezing; no source edits during a measured run.

The first campaign ran on C30; each successor campaign starts after its ready handover and build attestation. It does not require another owner permission cycle for already-authorized test-owned work. Completion of the campaign is evidence toward release; the combined artifact, package/install/upgrade and remaining acceptance checks still determine tag readiness.

## C30 first-pass result — preserve this baseline

The reviewer accepted22 new live turns:14 on the shared frozen daemon and8 across7 isolated homes;3 setup failures accepted no source and made no model call.14 done,7 blocked and1 accepted-without-terminal are an outcome inventory, not a pass rate. All12 core IDs were exercised, but Plan rev2/Execute/Stop, retained negative→repair, recall→edit and20–30minute interrupted delegation were not completed. No additional turns were manufactured to meet the30–40 estimate. Read the full per-ID scorecard and exact evidence:
/Users/nathan.reynolds/clementine-next/output/reviewer-monitor/2026-09-06-c30-review.md

Positive scope: clean Workflow create/edit, selected same-provider and off policy behavior, accepted-policy flips/settled reopen, corrected owner memory used in a fresh session, and a135s three-worker merge independently correct on all24 rows and ranking. Failed scope: native Space/mixed-read Plan contracts, disabled authoring, exact missing-name lookup, cold-history progress, post-write automatic recovery and next-turn Plan revision ownership. Completion availability and configured-review learning remain incomplete. Keep the genuine successes alongside first failures; the later simpler policy controls do not qualify the original disabled-workflow tasks.

Next candidate must address the connected failure clusters in the current implementation assignment. Correct the L11 fixture to separately assert authorized durable coordination and zero business/provider effects; retain its original failed mutation assertion and four actual refusals. Require a logical terminal or an owned live continuation, not merely a completed attempt row. Source138644 is explicitly unresolved. No old Plan rev1 may be executed as the requested unpublished rev2.

## C31 follow-up qualification boundary

C31's implementer supplied clean first-build Workflow create/edit and a later task completed after an actual restart before any write. Source retirement was corrected and21/21 focused reviewer repository tests pass; five private controls still expose save-side same-source metadata loss. The actual post-write/pre-terminal L08 case and delayed-old-response L12 retirement path remain unqualified. A later quick arithmetic request does not prove those transitions. The reviewer has not started another live campaign while native/cold-recall fixes remain unfinished. Preserve the C30 first failures and scope new evidence to its actual source/build and interruption point.
/Users/nathan.reynolds/clementine-next/output/reviewer-monitor/2026-09-07-c31-review.md

The C31 audit covers six implementer accepted sources through139840, not new reviewer campaign turns. Extend the existing continuity family with amendment while active, replacement before dispatch, follow-up during restart recovery and explicit separate-task controls. Preserve exact source/session/effect linkage. The recorded “Never mind that” input auto-branched and answered before the original write; the ordinary steering contract remains open. Keep the real post-effect L08 and stale-terminal L12 journeys as separate acceptance cases.
/Users/nathan.reynolds/clementine-next/output/reviewer-monitor/c31-review/live/review.md

## Final C31 follow-up — corrected live diagnosis

The next assignment is based on four additional implementer accepted sources, separate from the22 reviewer C30 turns.139852 proves one retained post-write effect and verified Opus completion across restart under1fdeb4b9…; its original driver still fails the unchanged-boot guard.140016 on6c4fbc1c… shows the model obeying the steer, the judge evaluating the old objective, and one create followed by an already-exists non-write incorrectly counted as another successful mutation. Qualify current objective propagation and typed non-write outcomes, rather than claiming this trace proves two physical writes.139952 remains without a terminal; old138644 aged out to needs_input without a revised Plan.

Add meaningful accepted amendment/replacement, exact source/attempt/held lifecycle and typed native non-write assertions to the existing campaign. Cold recall/native contract fixes remain pending; the next reviewer live window is not yet open. Keep planned restart identity transitions explicit, canonical effects distinct from nominal mutating calls, and all first failures visible.
/Users/nathan.reynolds/clementine-next/output/reviewer-monitor/2026-09-07-c31-final-review-0142.md

## September 7, frozen C31 reviewer campaign

Eight new reviewer accepted turns plus one bound steering note completed on de0d3826/PID16440. Keep these separate from historical22 C30 turns and four newly audited implementer turns. Cold recall and frozen steering improved; same-session Workflow edit preserved one line. Workflow create failed the three-search bound through schema paging, Space create failed120s, Space edit failed after premature attempt completion and repeated reviews, and Plan revision changed unrequested content/new identity. No Execute followed the failed revision. The current bounded window is released; remaining recovery/learning/long-task/mobile/integrated journeys are unqualified and must follow the connected correction. No new effectful campaign should run every heartbeat.

[Measured results and next batch](/Users/nathan.reynolds/clementine-next/output/reviewer-monitor/2026-09-07-c31-frozen-qualification.md). The next acceptance must count actual watcher/completion calls and source-bound continuation progress, not only main-model calls or terminal done. Use exact saved Plan payload preservation as a behavioral assertion.

## C32 review update — September 7, 03:40 UTC

Six new implementation sources were audited across their actual builds; this review launched zero reviewer live turns. The completed22 C30 plus8 C31 reviewer turns remain30. Final C32 still finished an active owner before a protected child call, and generic local failure can newly bypass uncertain-write reconciliation. The correct intermediate-build Space edit and independent4/4 cold-recall checks are useful narrower positives. Return the window for the connected correction; no new effectful campaign is pending. After the successor is fixed/frozen, qualify the actual held/adoption continuation, new same-session native journey and exact Plan revision/Execute under the existing measurement contract. [C32 ruling](/Users/nathan.reynolds/clementine-next/output/reviewer-monitor/2026-09-07-c32-review.md).

## C34 review update — September 7, 04:38 UTC

Four more implementation sources were audited; zero reviewer live turns were added. Source142281 proves actual create→update scope expansion through graphless native dispatch. Add the accepted-scope producer/consent/dispatch negative and explicit-edit/mixed-lineage positives before the next affected live window. Completion review of resulting content is not authorization. C34's plain same-source restart proves one post-restart creation and positive artifact review, while parked-hold and post-effect recovery remain unqualified. Retain all original driver errors. The current window returns for the connected correction. [C34 ruling](/Users/nathan.reynolds/clementine-next/output/reviewer-monitor/2026-09-07-c34-review.md).
